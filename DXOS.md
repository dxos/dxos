# DXOS

The DXOS vision — why we build local-first software, and what agents can do once your data is
genuinely yours.

Read it at
[packages/plugins/plugin-onboarding/src/content/DXOS.md](packages/plugins/plugin-onboarding/src/content/DXOS.md).
That file is the canonical copy: it is seeded into every new Composer workspace, so the document
you read in the app and the document you read here are the same text.
