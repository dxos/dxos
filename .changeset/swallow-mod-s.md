---
'@dxos/plugin-navtree': patch
---

Swallow Cmd/Ctrl+S everywhere in Composer so the browser's "Save page" dialog no longer opens; edits already persist as they are made. The binding is listed as "Save document" in the shortcuts list.
