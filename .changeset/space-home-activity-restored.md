---
'@dxos/plugin-space': patch
---

The space home Activity section is back. Its counts and heatmap now come from host-side aggregates, so opening a large space no longer loads every object into the page; the same counts feed the Stream Deck and LaMetric displays.
