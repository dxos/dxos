---
'@dxos/plugin-claude': minor
---

Rename the Claude plugin's skill and events entry points to `ClaudeSkill` and `ClaudeEvents`, and declare Tasks as a dependency of the Projects plugin so enabling Projects enables Tasks with it.
