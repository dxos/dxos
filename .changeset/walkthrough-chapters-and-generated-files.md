---
'@dxos/plugin-github': minor
---

Walkthroughs of large pull requests are now planned into chapters and written a chapter at a time, so a change too big for one prompt is described rather than left to the appended diff. Lockfiles, build output and binaries are detected and named instead of rendered, and the prompt spends its budget on code instead.
