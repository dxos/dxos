---
'@dxos/plugin-tasks': minor
---

The task list's filter is remembered per device for each task set, and the status menu now reads and writes the query text's `status:` terms, so the menu and the text are one filter.
