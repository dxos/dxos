---
'@dxos/react-ui-task': patch
---

`TaskList.Edit` now saves the task when Cmd+Enter (Ctrl+Enter off macOS) is pressed in the description editor, the same as the Save button, instead of letting the markdown editor handle the key.
