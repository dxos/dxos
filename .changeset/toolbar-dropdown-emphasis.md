---
'@dxos/react-ui-menu': patch
'@dxos/plugin-connector': patch
---

Fix a disabled toolbar dropdown still opening an empty menu, and let a dropdown trigger render with primary emphasis — so an object's Connect button looks actionable when it has connectors to offer, and inert when it does not.
