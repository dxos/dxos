---
'@dxos/react-ui-form': patch
---

A root `Form.FieldSet` carries its top spacing on the legend, so section headings keep their spacing in WebKit, which lays a rendered legend at the field set's border edge.
