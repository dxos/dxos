---
'@dxos/plugin-space': patch
---

The space home page no longer shows the Activity section, which stalled the main thread in large spaces. It will come back once its statistics are computed off the main thread.
