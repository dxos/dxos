---
'@dxos/react-ui-markdown': patch
---

`MarkdownEditable` now shows a bullet when a line starts with `- `, where the marker used to vanish off the field's left edge, and a task row's three-line description clamp ends on a whole line when the description contains a list.
