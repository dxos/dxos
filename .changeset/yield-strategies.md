---
'@dxos/effect': patch
'@dxos/plugin-search': patch
---

Add `Yield.yieldOrContinue` to `@dxos/effect`, and move ECHO client update integration, link loading, query hydration, search matching and app-graph yields onto the `interactive`/`smooth`/`idle` yield strategies.
