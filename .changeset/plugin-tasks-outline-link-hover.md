---
'@dxos/plugin-tasks': patch
---

Hovering a promoted item's link in an outline no longer follows it. The outline now follows a link on click or Enter only, so hovering the chip shows the task's preview card as elsewhere instead of swapping the outline for the task.
