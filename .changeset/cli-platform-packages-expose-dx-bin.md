---
'@dxos/plugin-markdown': minor
---

Each `@dxos/cli-<platform>-<arch>` package now exposes the `dx` binary directly, so a single platform package can be installed on its own without pulling every other platform.
