---
'@dxos/types': minor
'@dxos/plugin-tasks': minor
---

Add `Task.attachments`, files a task owns, with `Task.addAttachment`/`Task.removeAttachment`; files dropped or pasted onto a task's article are stored and attached, and previewed there with a remove action.
