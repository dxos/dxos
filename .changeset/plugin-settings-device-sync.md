---
'@dxos/echo': minor
'@dxos/plugin-markdown': minor
---

Sync plugin settings and the plugin set through the settings space, so they follow the identity across devices. Values are shared by default; any individual setting — or an individual plugin's enabled state — can be pinned to one device from the settings surface or the plugin detail view.
