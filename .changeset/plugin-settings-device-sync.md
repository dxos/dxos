---
'@dxos/echo': minor
'@dxos/plugin-markdown': minor
---

Sync plugin settings and the plugin set through the settings space, so they follow the identity across devices. Settings are shared by default; a plugin's settings panel can be switched to this device only from its header, and the plugin registry offers a matching switch for using a different plugin set on one device.
