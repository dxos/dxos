---
'@dxos/mcp-projection': minor
'@dxos/cli': minor
---

Add `@dxos/mcp-projection`, which projects the operation registry onto an MCP tool and prompt surface, and `dx mcp serve`, which runs that surface locally over stdio against the active profile's identity and spaces.
