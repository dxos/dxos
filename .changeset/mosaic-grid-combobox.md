---
'@dxos/react-ui': minor
'@dxos/react-ui-list': patch
---

Grid layout primitive: track lists (`cols={['min-content', '1fr']}`), `subgrid`, `gap` from the spacing ramp, `align`/`center`, `contents`, and `asChild`. Combobox: the popover now aligns exactly with its trigger (width moved from the viewport to the content).
