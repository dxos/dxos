---
'@dxos/plugin-debug': patch
'@dxos/types': patch
---

Add a "Composer — Review queue" space template: a project whose tasks carry real dxos pull requests, their screenshots and their demo recordings. `Video` is now a shared type in `@dxos/types`, and a task's pull-request artifacts resolve without the GitHub plugin enabled.
