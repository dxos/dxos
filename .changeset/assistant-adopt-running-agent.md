---
'@dxos/plugin-assistant': patch
---

Chat now re-attaches to an agent that is still working when the view is remounted (e.g. navigating to another page mid-turn), so the running indicator and streamed output are no longer lost.
