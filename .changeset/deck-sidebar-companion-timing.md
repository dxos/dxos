---
'@dxos/plugin-deck': patch
'@dxos/ui-theme': patch
---

Clicking the active space on the rail now slides the navigation sidebar instead of snapping it, and opening a companion shows the pane at once rather than after its URL resolves. Sidebar and layout slides run faster: 150 ms with an ease-out curve instead of 200 ms ease-in-out.
