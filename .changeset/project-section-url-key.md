---
'@dxos/plugin-projects': patch
---

The Project sidebar section is now addressed as `project` in URLs instead of `topic`, a leftover from the Topic→Project succession. Existing `/topic/<id>` links no longer resolve.
