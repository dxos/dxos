---
'@dxos/plugin-illustrator': minor
---

Add a UML class-diagram dialect (mermaid `classDiagram` subset with automated layered layout) plus a `UML` skill that generates class diagrams from source code; the `generate` operation now dispatches between the flowchart and classDiagram dialects by mermaid header.
