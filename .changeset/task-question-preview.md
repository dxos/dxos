---
'@dxos/react-ui-task': patch
---

`TaskList` rows now preview a task's questions one line each (`TaskQuestion` gains a `compact` variant); the full question, with its answer controls, renders in `TaskList.Edit`. The project task companion no longer shows a task's artifacts twice.
