---
'@dxos/plugin-markdown': minor
---

Bind credentials to Claude-managed agent sessions by AccessToken reference, upsert or revoke them on a running session, and carry required scopes on the connector prompt.
