---
'@dxos/edge-client': minor
---

Add an Effect-native `EdgeApiService`/`makeEdgeApiClient` derived from `@dxos/edge-protocol`'s `EdgeApi` `HttpApi` contract, wired into `ServiceHost` via `runtime.services.edge.url`.
