---
'@dxos/assistant-toolkit': minor
---

Add an optional `attach` flag to the `database.objectCreate` operation that files the created object in the space root collection, making it visible in the navigation tree.
