---
'@dxos/react-ui-form': patch
'@dxos/plugin-assistant': patch
---

`Form.FieldSet` actions sit at the end of the heading row instead of overlapping the first field, and the Assistant, Debug, Script and Spaces settings panels now show the synced/local scope toggle.
