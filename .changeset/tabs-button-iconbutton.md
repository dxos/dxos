---
'@dxos/react-ui-tabs': minor
---

Rename `Tabs.Tab` → `Tabs.Button` and `Tabs.IconTab` → `Tabs.IconButton`. Both now honor a caller-supplied `variant` and skip label-alignment classes when `iconOnly`.
