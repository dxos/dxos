---
'@dxos/echo-client': patch
---

Release ECHO objects and their automerge documents once nothing holds them, so a space's client-side footprint tracks what is open rather than everything it has ever loaded.
