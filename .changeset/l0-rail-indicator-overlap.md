---
'@dxos/plugin-navtree': patch
---

Fix the L0 rail insetting its items by the scrollbar strip, which narrowed the space around the
workspace avatars and pushed the active-tab indicator underneath them.
