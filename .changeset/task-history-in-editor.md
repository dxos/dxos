---
'@dxos/react-ui-task': minor
---

The task editor lays out as three explicit grid rows — header, description, history — and shows the task's activity log under its description: newest first, laid out as a grid so the glyphs and the times line up as columns rather than trailing each description. Placement is explicit rather than by flow, which previously dropped the description into the icon column whenever the toolbar was absent.
