---
'@dxos/react-ui-task': minor
---

The task editor lays out as three explicit grid rows — header, description, history — and shows the task's activity log under its description: the newest entries first, each with who did it and how long ago. Placement is explicit rather than by flow, which previously dropped the description into the icon column whenever the toolbar was absent.
