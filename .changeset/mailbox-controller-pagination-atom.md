---
'@dxos/echo-react': minor
---

Add `paginationAtom` (and export `createPaginationStore`): atom-native pagination over the store backing `usePagination`, for controllers and atom computations constructed outside React.
