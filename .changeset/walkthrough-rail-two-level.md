---
'@dxos/ui-editor': patch
---

Give the walkthrough navigation rail two levels: each file a section touches is its own row beneath it, with its own change counts, scrolling to that file's first diff. Counts are stated once — a section with a single file leaves them to the file row.
