---
'@dxos/react-ui-task': minor
'@dxos/plugin-tasks': minor
---

Task lists can be ordered by status, priority, estimate, created, updated or title, and grouped by status, priority, assignee or milestone under collapsible headers with counts; both choices are remembered per device for each task set. `TaskList.Root` takes a `groups` prop for host-defined groups.
