---
'@dxos/react-ui-task': patch
---

A task's estimate control shows a dot when unset, matching the priority control beside it, instead
of an en dash. `@dxos/util` gains `concat` (a tagged template that joins its lines with a space) and
`lines` (the same dedent as `trim`, returning the lines unjoined).
