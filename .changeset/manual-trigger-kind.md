---
'@dxos/compute': minor
---

Add a `manual` trigger kind that is invoked on demand rather than scheduled by the dispatcher, along with its spec/event constructors and an `isManuallyInvokable` helper.
