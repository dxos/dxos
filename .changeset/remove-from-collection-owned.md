---
'@dxos/plugin-space': patch
---

The object menu offers "Remove from collection" for objects the collection owns, not only ones it links to. Removing an owned object moves it to the space root instead of deleting it. Delete is still only offered for owned objects, and "Show original" only for linked ones.
