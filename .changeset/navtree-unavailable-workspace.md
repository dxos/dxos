---
'@dxos/plugin-navtree': patch
---

Show a message in the sidebar when the active workspace is not available instead of rendering an empty nav tree, and stop serializing the unresolved-workspace sentinel into the URL.
