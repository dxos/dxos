---
'@dxos/assistant': patch
---

Report a tool call the model makes to a tool that does not exist back to the model instead of failing the request, and render system-generated conversation turns instead of dropping them.
