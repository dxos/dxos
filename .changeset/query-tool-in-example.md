---
'@dxos/assistant-toolkit': patch
---

Fix the database `query` tool description so the `in` example uses the URI strings its input schema declares, removing a failed call and retry on every scoped query.
