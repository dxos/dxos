---
'@dxos/plugin-space': patch
---

"Go to connection" on a rejected GitHub token now opens the connection whose token failed, and a navigation error after a successful space import no longer reports the import as failed.
