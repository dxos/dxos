---
'@dxos/app-framework': patch
---

Surfaces register with the mount registry under a profiler provider in production builds, so the devtools Surfaces card lists them and its highlight toggle works outside dev.
