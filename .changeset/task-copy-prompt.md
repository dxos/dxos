---
'@dxos/plugin-projects': minor
---

Add a "Copy prompt" action to a task's row menu: it renders the task, its addresses (task, task set, project, space) and the project's context as one prompt, copies it to the clipboard, and instructs the agent to assign itself to the task before starting. Available as the `org.dxos.operation.projects.copyTaskPrompt` operation.
