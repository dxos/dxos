---
'@dxos/assistant': patch
---

The chat checklist renders each task's ref as a mnemonic-labelled markdown link — `ref: [AE36U7](echo:/<space>/<object>)` instead of a bare URI — so the short form a person reads and the handle the task tools take stay together when a model pastes the line back.
