---
'@dxos/plugin-projects': patch
---

The project task companion no longer shows a task's artifacts twice: the task article already lists them, so the companion stops adding a second card grid beneath it.
