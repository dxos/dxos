---
'@dxos/plugin-file-system': minor
---

Rename `@dxos/plugin-native-filesystem` to `@dxos/plugin-file-system`. The plugin key is now `org.dxos.plugin.fileSystem` and the exported symbols drop the `Native` prefix and use `FileSystem` casing (`FileSystemPlugin`, `FileSystemCapabilities`, `FileSystemEvents`, `FileSystemOperation`).
