#/bin/bash

set -eo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

pnpm --silent vite-node --root "$ROOT_DIR/packages/devtools/cli-next" "$ROOT_DIR/packages/devtools/cli-next/src/bin.ts" "$@"