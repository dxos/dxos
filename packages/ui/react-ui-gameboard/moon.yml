type: library
language: typescript
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--preactSignalTracking'
    deps:
      - gen-pieces
  gen-pieces:
    command: pnpm run gen:pieces
    inputs:
      - assets/*.*
    outputs:
      - src/gen
