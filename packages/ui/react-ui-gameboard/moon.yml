layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - ts-test-storybook
  - pack
  - storybook
tasks:
  build:
    deps:
      - gen-pieces
  gen-pieces:
    command: pnpm run --silent gen:pieces
    inputs:
      - assets/*.*
    outputs:
      - src/gen
