type: library
language: typescript
tags:
  - ts-build
  - pack
  - ts-test
  - storybook
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--platform=browser'
      - '--preactSignalTracking'
  test:
    inputs:
      - .storybook/*
