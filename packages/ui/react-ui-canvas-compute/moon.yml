layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
  - storybook
tasks:
  build:
    args:
      - '--entryPoint=src/index.ts'
      - '--injectGlobals'
  test:
    inputs:
      - .storybook/*
      - /tools/storybook-react/.storybook/main.ts
