layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
  - storybook
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--injectGlobals'
  test:
    inputs:
      - .storybook/*
      - /tools/storybook-react/.storybook/main.ts
