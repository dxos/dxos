# @dxos/react-ui-audio

Audio hooks and visualizations.

- `useAudioStream` — Web Audio analyser channel (microphone or `AudioNode` source).
- `useSoundEffect` — preloaded sound-effect playback.
- `Oscilloscope` — frequency / waveform visualization.
