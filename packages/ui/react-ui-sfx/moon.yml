layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - ts-test-storybook
  - pack
  - storybook
tasks:
  build:
    deps:
      - glsl
  glsl:
    command: pnpm run --silent glsl
    inputs:
      - src/shaders/glsl/*.*
    outputs:
      - src/shaders/glsl/gen
