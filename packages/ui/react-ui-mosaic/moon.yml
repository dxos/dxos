layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
  - storybook
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/testing/index.ts'
      - '--platform=browser'
      - '--platform=node'
  test:
    inputs:
      - .storybook/*
      - /tools/storybook-react/.storybook/main.ts