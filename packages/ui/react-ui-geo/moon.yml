type: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
tasks:
  compile:
    args:
      - '--entryPoint=src/data.ts'
      - '--entryPoint=src/index.ts'
      - '--preactSignalTracking'
  test:
    inputs:
      - .storybook/*
