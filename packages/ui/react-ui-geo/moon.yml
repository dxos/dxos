type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - test
tasks:
  compile:
    args:
      - '--entryPoint=src/data.ts'
      - '--entryPoint=src/index.ts'
      - '--preactSignalTracking'
