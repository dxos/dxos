layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - ts-test-storybook
  - pack
  - storybook
tasks:
  build:
    deps:
      - compile-node
  compile-node:
    command: pnpm exec dx-compile
    args:
      - '--entryPoint=src/plugins/ThemePlugin.ts'
      - '--entryPoint=src/main.css'
      - '--outputPath=dist/plugin'
      - '--moduleFormat=cjs'
      - '--moduleFormat=esm'
      - '--platform=node'
    deps:
      - ^:build
      - ^:compile
      - dx-compile:compile
    options:
      mergeArgs: replace
    outputs:
      - dist/plugin/
  test:
    deps:
      - compile-node
  test-storybook:
    deps:
      - compile-node
