layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
  - storybook
tasks:
  build:
    args:
      - '--entryPoint=src/index.ts'
      - '--platform=browser'
      - '--platform=node'
