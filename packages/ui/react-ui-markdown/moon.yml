layer: library
language: typescript
tags:
  - ts-build
  - pack
  - ts-test
  - ts-test-storybook
  - storybook
tasks:
  build:
    args:
      - '--entryPoint=src/index.ts'
      - '--platform=browser'
