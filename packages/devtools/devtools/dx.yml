version: 1

package:
  modules:
    - name: devtools
      type: dxos:type/app
      build:
        command: moon run devtools:bundle

runtime:
  services:
    signaling:
      - server: wss://kube.dxos.org/.well-known/dx/signal
#      server: wss://localhost/.well-known/dx/signal
