{
  "name": "app-to-register",
  "version": "1.2.3",
  "description": "Mock application",
  "scripts": {
    "build": "rm -rf dist out && mkdir dist out && cp index.html dist && cp index.html out"
  }
}
