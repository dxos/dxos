version: 1

package:
  modules:
    - name: inspector
      type: dxos:type/app
      build:
        command: pnpm -w nx bundle inspector-app

runtime:
  services:
    signal:
      server: wss://kube.dxos.org/.well-known/dx/signal
