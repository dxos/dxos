layer: automation
language: typescript
tags:
  - typecheck
  - ts-test
  - pack
tasks:
  compile:
    command: echo "No compilation needed for test-only package"
    outputs: []
    options:
      merge: replace
