{
  "sourceRoot": "packages/mesh/protocol-plugin-replicator/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "outputs": [
        "packages/mesh/protocol-plugin-replicator/dist",
        "packages/mesh/protocol-plugin-replicator/src/proto/gen"
      ],
      "options": {
        "script": "build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/mesh/protocol-plugin-replicator/**/*.{ts,js}"
        ]
      }
    }
  }
}
