{
  "sourceRoot": "packages/mesh/signaling/src",
  "projectType": "library",
  "targets": {
    "proto": {
      "executor": "@dxos/protobuf-compiler:generate",
      "outputs": [
        "{options.basePath}/gen"
      ],
      "options": {
        "basePath": "packages/mesh/signaling/src/proto"
      }
    },
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/mesh/signaling/dist",
        "tsConfig": "packages/mesh/signaling/tsconfig.json",
        "main": "packages/mesh/signaling/src/index.ts"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/mesh/signaling/**/*.{ts,js}"
        ]
      }
    }
  },
  "implicitDependencies": [
    "protobuf-compiler"
  ]
}
