{
  "sourceRoot": "packages/mesh/network-manager/src",
  "projectType": "library",
  "targets": {
    "proto": {
      "executor": "@dxos/protobuf-compiler:generate",
      "outputs": [
        "{options.basePath}{options.outputPath}"
      ],
      "options": {
        "basePath": "packages/mesh/network-manager/src/proto/"
      }
    },
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/mesh/network-manager/dist",
        "tsConfig": "packages/mesh/network-manager/tsconfig.json",
        "main": "packages/mesh/network-manager/src/index.ts",
        "updateBuildableProjectDepsInPackageJson": false,
        "assets": ["packages/mesh/network-manager/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/mesh/network-manager/**/*.{ts,js}"
        ]
      }
    }
  },
  "implicitDependencies": [
    "protobuf-compiler"
  ]
}
