# Agent fixture

The sidecar story scopes the agent's `cwd` to this directory and asks it to read this file, so the
turn produces a real allowed tool call whose result the projection has to correlate.

MAGIC_TOKEN: pelican-42
