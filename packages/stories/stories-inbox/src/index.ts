//
// Copyright 2026 DXOS.org
//

// Storybook-only package — stories are picked up by `.storybook/main.mts`.
// This index keeps the package buildable via `moon run stories-inbox:compile`.
export {};
