//
// Copyright 2026 DXOS.org
//

// Storybook-only package — stories are picked up by the storybook-react config.
// This index keeps the package buildable via `moon run stories-projects:compile`.
export {};
