layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
tasks:
  compile:
    command: pnpm exec dx-compile
    args:
      - --outputPath=dist/lib
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/extension/index.ts'
    deps:
      - dx-compile:compile
    inputs:
      - '@group(production)'
    outputs:
      - dist/lib/
    options:
      merge: replace
      timeout: 10
