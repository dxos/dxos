//
// Copyright 2026 DXOS.org
//

// Browser-safe entry: reaches the host over HTTP rather than spawning it, so it can run inside the
// agent process wherever that process runs.

export * as Producer from './Producer.ts';
