{
  "sourceRoot": "packages/core/mesh/protocol-plugin-replicator/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/core/mesh/protocol-plugin-replicator/dist",
        "tsConfig": "packages/core/mesh/protocol-plugin-replicator/tsconfig.json",
        "main": "packages/core/mesh/protocol-plugin-replicator/src/index.ts"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/core/mesh/protocol-plugin-replicator/**/*.{ts,js}"
        ]
      }
    },
    "test": {
      "executor": "@dxos/mocha:mocha",
      "outputs": [
        "{options.outputPath}",
        "{options.resultsPath}"
      ],
      "options": {
        "ciEnvironments": ["nodejs"],
        "testPatterns": [
          "packages/core/mesh/protocol-plugin-replicator/src/**/*.test.{ts,js}"
        ],
        "watchPatterns": [
          "packages/core/mesh/protocol-plugin-replicator/src/**/*"
        ],
        "outputPath": "tmp/mocha/packages/core/mesh/protocol-plugin-replicator",
        "resultsPath": "test-results/packages/core/mesh/protocol-plugin-replicator"
      }
    }
  },
  "implicitdependencies": [
    "mocha"
  ]
}
