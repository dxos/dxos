layer: library
language: typescript
tags:
  - dist-runtime
  - ts-build
  - ts-test
  - pack
tasks:
  build:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/testing/index.ts'
      - '--injectGlobals'
      - '--bundlePackage=varint'
      - '--platform=neutral'
