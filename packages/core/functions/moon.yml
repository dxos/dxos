layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/process/AgentService.ts'
      - '--entryPoint=src/Trace.ts'
      - '--platform=neutral'
