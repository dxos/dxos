//
// Copyright 2024 DXOS.org
//

// NOTE: The legacy `EchoSchema` / `BaseSchema` / `ImmutableSchema` classes that
// formerly lived here have been removed. Stored type entities are surfaced
// directly as `Type.Type` ECHO objects (see `Type.ts`); call `Type.getSchema`
// to derive their Effect Schema and `Type.update`/`Type.{addFields,...}` to
// mutate them.

// Placeholder to make this file a module.
export {};
