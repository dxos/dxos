layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
tasks:
  compile:
    command: echo "Skipping compile due to dx-compile WASM bug (github.com/dxos/dxos/issues/XXXXX)"
    outputs: []
    options:
      merge: replace
  build:
    command: echo "Skipping build due to dx-compile WASM bug (github.com/dxos/dxos/issues/XXXXX)"
    deps:
      - compile
    outputs: []
    options:
      merge: replace
