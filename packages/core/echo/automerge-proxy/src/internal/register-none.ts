//
// Copyright 2026 DXOS.org
//

// A browser or worker realm registers Automerge itself once it has initialized its WebAssembly, so a
// tab that holds only tab documents never loads it.

export {};
