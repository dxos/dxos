layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--platform=neutral'
  # TODO(wittjosiah): Get working with vitest.base.config.ts.
  test:
    options:
      mergeOutputs: replace
    outputs: []
