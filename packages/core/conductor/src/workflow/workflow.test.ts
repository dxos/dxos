//
// Copyright 2025 DXOS.org
//

import { describe, test } from 'vitest';

describe('workflow', () => {
  test('basic workflow', ({ expect }) => {
    // TODO(yaroslav): add tests
  });

  describe('multi-inputs graph execution paths optionality', () => {
    test('target optional input resolves to undefined if source output is NotExecuted', ({ expect }) => {
      // TODO(yaroslav): add tests
    });

    test('target required input resolves to NotExecuted if source output is NotExecuted', ({ expect }) => {
      // TODO(yaroslav): add tests
    });
  });
});
