layer: library
language: typescript
tags:
  - ts-compile
  - ts-test
tasks:
  gen-buf:
    command: buf generate
    inputs:
      - src/proto/dxos/**/*.proto
    outputs:
      - src/buf/proto/gen/dxos
  compile:
    deps:
      - gen-buf
