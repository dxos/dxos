import { PublicKey } from "@dxos/protocols";
import { Credential } from "./proto";

export interface CredentialCreationOpts {
  subject: PublicKey
  assertion: 
}

export function createCredential(): Promise<Credential> {

}