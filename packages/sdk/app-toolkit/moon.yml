layer: library
language: typescript
tags:
  - ts-build
  - pack
  - storybook
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/ui/index.ts'
  test:
    inputs:
      - .storybook/*
      - /tools/storybook-react/.storybook/main.ts
