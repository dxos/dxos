layer: library
language: typescript
tags:
  - pack
  - ts-vite-build
  - ts-test
tasks:
  # TODO(wittjosiah): Get working with vitest.base.config.ts.
  test:
    options:
      mergeOutputs: replace
    outputs: []
