layer: library
language: typescript
tags:
  - ts-test
  - typecheck
  - vite
  - pack
  - storybook
tasks:
  compile:
    command: echo "No compilation needed for vite examples package"
    outputs: []
    options:
      merge: replace
  test:
    inputs:
      - .storybook/*
      - vite.config.ts
