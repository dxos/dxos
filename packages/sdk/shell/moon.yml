layer: library
language: typescript
tags:
  - ts-build
  - ts-test-browser
  - ts-test-storybook
  - vite
  - pack
  - storybook
tasks:
  bundle:
    options:
      mergeOutputs: replace
    outputs:
      - dist/bundle
  test:
    inputs:
      - vite.config.ts
