type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - test
tasks:
  compile:
    args:
      - '--entryPoint=src/react.ts'
      - '--entryPoint=src/testing/index.ts'
      - '--preactSignalTracking'
