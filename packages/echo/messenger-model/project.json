{
  "sourceRoot": "packages/echo/messenger-model/src",
  "projectType": "library",
  "targets": {
    "proto": {
      "executor": "@dxos/protobuf-compiler:generate",
      "outputs": [
        "{options.basePath}/gen"
      ],
      "options": {
        "basePath": "packages/echo/messenger-model/src/proto"
      }
    },
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/echo/messenger-model/dist",
        "tsConfig": "packages/echo/messenger-model/tsconfig.json",
        "main": "packages/echo/messenger-model/src/index.ts"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/echo/messenger-model/**/*.{ts,js}"
        ]
      }
    }
  },
  "implicitDependencies": [
    "protobuf-compiler"
  ]
}
