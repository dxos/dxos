type: library
language: typescript
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--platform=browser'
      - '--ignorePackage=virtual:pwa-register'
      - '--preactSignalTracking'
