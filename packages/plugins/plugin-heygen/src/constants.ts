//
// Copyright 2026 DXOS.org
//

/** Provider id contributed as `GenerationService.id`. */
export const HEYGEN_ID = 'heygen';

/** `Connector.id` / `Connection.connectorId` for the HeyGen connector. */
export const HEYGEN_CONNECTOR_ID = 'org.dxos.plugin.heygen.connector';

/** Matches `AccessToken.source`; the key `CredentialsService` resolves the API key under. */
export const HEYGEN_SOURCE = 'heygen.com';
