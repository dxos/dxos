# Judge packets are byte-copies of the corpus, regenerated by `judge-packet.ts prepare`
# (the shuffle is seeded per fixture, so the letters come back the same).
evals/judge*/*/[A-Z].md
evals/judge*/*/diff.patch
