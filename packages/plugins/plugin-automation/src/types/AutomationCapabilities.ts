//
// Copyright 2025 DXOS.org
//

/**
 * Deprecated namespace retained while call sites migrate to
 * {@link Capabilities.ProcessManagerRuntime} / {@link Capabilities.LayerSpec} from `@dxos/app-framework`.
 *
 * Former `AutomationCapabilities.ComputeRuntime` is replaced by layered contributions in
 * `plugin-automation` (see `./capabilities/layer-specs.ts`).
 */
export namespace AutomationCapabilities {}
