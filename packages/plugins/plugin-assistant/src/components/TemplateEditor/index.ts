//
// Copyright 2023 DXOS.org
//

// TODO(wip): Factor TemplateEditor into a shared package (e.g. @dxos/react-ui-editor) so plugins can depend on it without taking plugin-assistant as a dep.
export * from './TemplateEditor';
