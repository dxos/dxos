type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - test
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/types/index.ts'
      - '--platform=browser'
      - '--preactSignalTracking'
