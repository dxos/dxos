//
// Copyright 2026 DXOS.org
//

/** Matches `AccessToken.source` for Typefully credentials. */
export const TYPEFULLY_SOURCE = 'typefully.com';

/** Connector id matching the `ConnectorEntry.id` contributed by this plugin. */
export const TYPEFULLY_CONNECTOR_ID = 'typefully';
