# @dxos/plugin-brain

The Facts layer for Composer: a per-space semantic index of RDF `Fact` tuples extracted from user
content by upstream pipelines. Provides the `FactStore` service to operations, assistant tools for
querying and summarizing facts, and a `FactViewer` companion surface.

See [PLUGIN.mdl](./PLUGIN.mdl) for the specification.
