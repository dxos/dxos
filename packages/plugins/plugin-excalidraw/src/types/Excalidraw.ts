//
// Copyright 2026 DXOS.org
//

// @import-as-namespace

/**
 * Renderer dialect stored on `Drawing.Canvas.schema`; how a canvas is matched to this variant.
 * The canvas type itself is the base `Drawing.Canvas` — excalidraw adds no fields of its own.
 */
export const EXCALIDRAW_SCHEMA = 'excalidraw.com/2';
