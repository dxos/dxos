//
// Copyright 2026 DXOS.org
//

// `TopicArticle` moved to `@dxos/plugin-brain`; this retains the mailbox-feed thread resolver that wires
// a topic's `threadIds` back to live messages (a follow-up).
export * from './resolve-threads';
