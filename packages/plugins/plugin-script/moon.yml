layer: library
language: typescript
tags:
  - ts-build
  - ts-test
  - ts-test-storybook
  - pack
  - storybook
tasks:
  e2e:
    command: vitest run -c vitest.config.ts --reporter=verbose --tagsFilter=functions-e2e src/e2e/*.test.ts
    deps:
      - ^:build
      - ^:compile
    inputs:
      - src/e2e/**
      - vitest.config.ts
