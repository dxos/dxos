//
// Copyright 2026 DXOS.org
//

/** Option entry for the FeedbackForm's "Area" picker. Sourced from the plugin manager. */
export type FeedbackPluginOption = {
  /** Plugin id used as the option value (e.g. `org.dxos.plugin.echo`). */
  id: string;
  /** Display name shown above the id in the select. */
  name: string;
};
