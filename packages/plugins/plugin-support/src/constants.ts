//
// Copyright 2023 DXOS.org
//

import { meta } from '#meta';

export const SHORTCUTS_DIALOG = `${meta.id}.ShortcutsDialog`;
export const WELCOME_ID = `${meta.id}.welcome`;
export const WELCOME_TYPE = `${meta.id}.type.welcome`;
