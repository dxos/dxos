//
// Copyright 2026 DXOS.org
//

/** `Connector.id` / `Connection.connectorId` for the TypeSafe connector. */
export const TYPESAFE_CONNECTOR_ID = 'org.dxos.plugin.typesafe.connector';

/** Matches `AccessToken.source`, which is how `CredentialsService` resolves the API key. */
export const TYPESAFE_SOURCE = 'typesafe.ai';

/** Where the user mints the key the connector form asks for. */
export const TYPESAFE_API_KEYS_URL = 'https://docs.typesafe.ai/quickstart';

/** The System One model asked; the alias tracks the vendor's current stable release. */
export const TYPESAFE_MODEL = 'jev-latest';
