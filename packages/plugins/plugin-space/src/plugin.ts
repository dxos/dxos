//
// Copyright 2025 DXOS.org
//

// Eager (non-lazy) re-export of {@link SpacePlugin}, intended for hosts that
// cannot afford the dynamic-import path used by the default `.` export. See
// `@dxos/plugin-attention/plugin` for the full explanation.
export * from './SpacePlugin';
