layer: library
language: typescript
tags:
  - composer-plugin
  - ts-build
  - ts-test
  - ts-test-storybook
  - pack
  - storybook
tasks:
  check-module-structure:
    command: >-
      pnpm exec dx-trace-imports
      --export ./AtprotoPlugin
      --to "{react,react-dom}"
      --conditions workerd,worker
      --conditions node
      --fail-on present
    deps:
      - dx-trace-imports:build
      - build
    inputs:
      - tsconfig.json
      - '@group(production)'
      - /pnpm-lock.yaml
    options:
      timeout: 60
  build:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/plugin.tsx'
      - '--entryPoint=src/capabilities/index.ts'
      # Generated by `dx-plugin gen`; listed here because this pipeline takes entry points by hand.
      - '--entryPoint=src/capabilities/gen/node.ts'
      - '--entryPoint=src/capabilities/gen/workerd.ts'
      - '--entryPoint=src/types/index.ts'
      - '--entryPoint=src/components/index.ts'
      - '--entryPoint=src/containers/index.ts'
      - '--entryPoint=src/meta.ts'
      - '--entryPoint=src/AtprotoPlugin.ts'
      - '--entryPoint=src/testing.ts'
      - '--entryPoint=src/translations.ts'
      - '--entryPoint=src/types/AtprotoCapabilities.ts'
      - '--entryPoint=src/types/AtprotoEvents.ts'
      - '--entryPoint=src/types/AtprotoPublication.ts'
      - '--platform=neutral'
