//
// Copyright 2026 DXOS.org
//

/** Source string used in `AccessToken.source` and `Obj.Meta.keys[i].source` for Trello. */
export const TRELLO_SOURCE = 'trello.com';

/** Base URL for the Trello REST API. */
export const TRELLO_API_BASE = 'https://api.trello.com/1';

/** Pivot field on Expando cards that drives Kanban column membership. */
export const TRELLO_PIVOT_FIELD = 'listName';
