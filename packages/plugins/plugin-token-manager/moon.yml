type: library
language: typescript
tags:
  - ts-build
  - ts-test
  - pack
  - storybook
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/cli/index.ts'
      - '--entryPoint=src/types/index.ts'
      - '--preactSignalTracking'
  test:
    inputs:
      - .storybook/*
