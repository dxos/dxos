//
// Copyright 2022 DXOS.org
//

import { BaseCommand } from '../../base-command';

export default class Create extends BaseCommand {
  static description = 'Create spaces.';
  static flags = {}; // Required.

  async run (): Promise<void> {}
}
