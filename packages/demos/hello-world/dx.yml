version: 1

package:
  modules:
    - name: dxos:story/hello-world
      type: dxos:type/app
      build:
        command: npm run build
        outdir: out/book

runtime:
  client:
    storage:
      persistent: true
