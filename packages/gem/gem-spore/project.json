{
  "sourceRoot": "packages/gem/gem-spore/src",
  "projectType": "library",
  "tags": ["scope:gem"],
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/gem/gem-spore/dist",
        "tsConfig": "packages/gem/gem-spore/tsconfig.json",
        "main": "packages/gem/gem-spore/src/index.ts"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/gem/gem-spore/**/*.{ts,tsx,js,jsx}"]
      }
    },
    // TODO(wittjosiah): Mocha fails because of ESM import issue with D3.
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["packages/gem/gem-spore/coverage"],
      "options": {
        "jestConfig": "packages/gem/gem-spore/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}
