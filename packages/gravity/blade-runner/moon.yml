type: automation
language: typescript
tags:
  - ts-build
  - ts-test
tasks:
  # TODO(wittjosiah): Actually run build here.
  build:
    command: 'true'
    options:
      mergeArgs: replace
      mergeInputs: replace
      mergeOutputs: replace
    args: []
    inputs: []
    outputs: []
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/main.ts'
