type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      # TODO(wittjosiah): Why is build excluded?
      - build
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/main.ts'
