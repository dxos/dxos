{
  "name": "@dxos/browser-tests",
  "version": "2.10.10",
  "private": true,
  "description": "Browser tests for SDK.",
  "bugs": {
    "url": "'https://github.com/dxos/protocols/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dxos/protocols.git"
  },
  "license": "AGPL-3.0",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "toolchain build",
    "build:test": "toolchain build:test",
    "lint": "toolchain lint",
    "test": "toolchain test",
    "test:browser": "browser-mocha --setup browser-test/setup.js 'browser-test/**/*.test.ts' --browser=chromium --browser=firefox --browser=webkit"
  },
  "toolchain": {
    "additionalTestSteps": [
      "test:browser"
    ]
  },
  "devDependencies": {
    "@dxos/toolchain-node-library": "workspace:*",
    "@dxos/browser-mocha": "workspace:*",
    "mocha": "~8.4.0",
    "@types/mocha": "~8.2.2",
    "expect": "~27.0.2",
    "@dxos/signal": "workspace:*"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@dxos/client": "workspace:*",
    "@dxos/crypto": "workspace:*",
    "source-map-support": "^0.5.12",
    "@dxos/echo-db": "workspace:*",
    "@dxos/object-model": "workspace:*",
    "@dxos/credentials": "workspace:*"
  }
}
