layer: application
language: typescript
tags:
  - no-compile
  - e2e
  - tauri
  - typecheck
  - vite
  - ts-test
  - storybook
tasks:
  prebuild:
    command: pnpm run --silent copy:assets
    deps:
      - ^:prebuild
    inputs:
      - package.json
      - moon.yml
    outputs:
      - public/assets/plugin-sketch
  serve:
    deps:
      - prebuild
  test:
    inputs:
      - .storybook/*
      - vite.config.ts
