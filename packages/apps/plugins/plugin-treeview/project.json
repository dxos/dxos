{
  "sourceRoot": "packages/apps/plugins/plugin-treeview/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "options": {
        "main": "packages/apps/plugins/plugin-treeview/src/index.ts",
        "outputPath": "packages/apps/plugins/plugin-treeview/dist/types",
        "tsConfig": "packages/apps/plugins/plugin-treeview/tsconfig.json"
      },
      "outputs": [
        "{options.outputPath}"
      ]
    },
    "compile": {
      "executor": "@dxos/esbuild:build",
      "options": {
        "entryPoints": [
          "packages/apps/plugins/plugin-treeview/src/index.ts"
        ],
        "outputPath": "packages/apps/plugins/plugin-treeview/dist/lib",
        "platforms": [
          "browser"
        ]
      },
      "outputs": [
        "{options.outputPath}"
      ]
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "options": {
        "format": "unix",
        "lintFilePatterns": [
          "packages/apps/plugins/plugin-treeview/src/**/*.{js,jsx,ts,tsx}"
        ],
        "quiet": true
      },
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "storybook": {
      "configurations": {
        "ci": {
          "quiet": true
        }
      },
      "executor": "@nx/storybook:storybook",
      "options": {
        "configDir": "packages/apps/plugins/plugin-treeview/.storybook"
      }
    }
  },
  "implicitDependencies": [
    "esbuild"
  ]
}
