version: 1

package:
  modules:
    - name: halo
      type: dxos:type/app
      displayName: HALO
      build:
        command: pnpm -w run nx bundle halo-app

    # - name: dxos:storybook/halo
    #   type: dxos:type/app
    #   displayName: HALO Storybook
    #   build:
    #     command: pnpm -w run nx build-storybook halo-app

runtime:
  client:
    storage:
      persistent: true

  services:
    signal:
      server: wss://halo.dxos.org/.well-known/dx/signal
    ice:
      - urls: turn:halo.dxos.org:3478
        username: dxos
        credential: dxos
