//
// Copyright 2024 DXOS.org
//

// Storybook-only stub for `webext-bridge/popup` (opens a runtime port on import in-extension).
export const sendMessage = async () => undefined;
export const onMessage = () => () => {};
