type: application
language: typescript
tags:
  - vite
workspace:
  inheritedTasks:
    exclude:
      - compile
      - test
tasks:
  build:
    options:
      mergeOutputs: 'replace'
    outputs:
      - dist/
