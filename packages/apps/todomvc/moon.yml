type: library
language: typescript
tags:
  - vite
workspace:
  inheritedTasks:
    exclude:
      - compile
      - test
tasks:
  build:
    options:
      mergeOutputs: 'replace'
    outputs:
      - dist/
  bundle:
    deps:
      - shell:bundle
