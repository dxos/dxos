version: 1

runtime:
  client:
    remoteSource: http://localhost:3967/vault.html
    storage:
      persistent: false

  services:
    signaling:
      - server: ws://localhost:8888/.well-known/dx/signal
