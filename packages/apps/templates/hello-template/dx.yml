version: 1

package:
  modules:
    - name: example:app/hello
      type: dxos:type/app
      build:
        command: npm run bundle

runtime:
  client:
    # TODO(wittjosiah): Remote.
    mode: local
    storage:
      persistent: true

  services:
    signal:
      server: ws://localhost:8888/.well-known/dx/signal
    ice:
      - urls: turn:kube.dxos.org:3478
        username: dxos
        credential: dxos
