{
  "sourceRoot": "packages/apps/templates/bare-template/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "outputs": [
        "packages/apps/templates/bare-template/dist"
      ]
    },
    "gen": {
      "executor": "nx:run-script",
      "options": {
        "script": "gen"
      },
      "outputs": [
        "packages/apps/templates/bare-template/test/variants"
      ]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "format": "unix",
        "lintFilePatterns": [
          "packages/apps/templates/bare-template/{src,test}/**/*.{ts,js,tsx,jsx}"
        ]
      },
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
