{
  "name": "@dxos/bare-template",
  "version": "0.1.23",
  "description": "An application template for local-first progressive web apps",
  "homepage": "https://dxos.org",
  "bugs": "https://github.com/dxos/dxos/issues",
  "license": "MIT",
  "author": "info@dxos.org",
  "main": "dist/src/config.t.js",
  "types": "dist/src/config.t.d.ts",
  "scripts": {
    "build": "tsc && plate --printMessage=false --inheritance=false --executeFileTemplates=false --output=dist/src src",
    "build-resources": "plate --printMessage=false --inheritance=false --executeFileTemplates=false --output=dist/src src",
    "clean": "rm -rf test/out",
    "gen": "test/generate",
    "tsc": "tsc"
  },
  "dependencies": {
    "@dxos/plate": "workspace:*",
    "chalk": "^4.1.0",
    "lodash.merge": "^4.6.2",
    "types-package-json": "^2.0.39"
  },
  "devDependencies": {
    "@types/lodash.merge": "^4.6.6",
    "@types/node": "^18.11.9",
    "ts-node": "10.9.1",
    "typescript": "^4.8.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
