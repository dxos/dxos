layer: application
language: typescript
tags:
  - ts-test
  - typecheck
  - vite
  - storybook
tasks:
  compile:
    command: echo "No compilation needed for vite application"
    outputs: []
    options:
      merge: replace
  test:
    inputs:
      - .storybook/*
      - vite.config.ts
