type: library
language: typescript
tags:
  - e2e
  - vite
workspace:
  inheritedTasks:
    exclude:
      - compile
      - test
tasks:
  build:
    options:
      mergeOutputs: 'replace'
    outputs:
      - dist/
  e2e:
    deps:
      - bundle
