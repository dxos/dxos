//
// Copyright 2022 DXOS.org
//

export enum AppView {
  DMG = 'registry',
  SETTINGS = 'settings',
  DASHBOARD = 'dashboard',
  META = 'data',
  KANBAN = 'kanban',
  TASKS = 'tasks',
  CALENDAR = 'events',
  DOCUMENTS = 'documents',
  EXPLORER = 'explorer',
  MAPS = 'map',
  CHESS = 'chess'
}

export const views = [
  AppView.DMG,
  AppView.DASHBOARD,
  AppView.META,
  AppView.TASKS,
  AppView.DOCUMENTS,
  AppView.KANBAN,
  AppView.CALENDAR,
  AppView.EXPLORER,
  AppView.MAPS,
  AppView.CHESS
];
