name = "discord-worker"
main = "src/worker.ts"
compatibility_date = "2024-04-05"

# Deliberately unset: the worker reads "unset" as "not wired up" and answers without forwarding.
# Set it to an edge base URL to enable forwarding -- never to a placeholder, which would be
# indistinguishable from unconfigured.
[vars]
