version: 1

package:
  modules:
    - name: composer
      type: dxos:type/app
      display_name: Composer
      build:
        command: pnpm -w run nx bundle halo-app

    # - name: dxos:storybook/halo
    #   type: dxos:type/app
    #   display_name: HALO Storybook
    #   build:
    #     command: pnpm -w run nx build-storybook halo-app

runtime:
  client:
    mode: remote
    storage:
      persistent: true

  services:
    signal:
      server: wss://halo.dxos.org/.well-known/dx/signal
    ice:
      - urls: turn:halo.dxos.org:3478
        username: dxos
        credential: dxos
