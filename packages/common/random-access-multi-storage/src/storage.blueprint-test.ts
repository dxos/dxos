import { IStorage } from ".";

export function storageTests(createStorage: () => IStorage) {
  it('open & close')

  it('open file, read & write')

  it('multiple files')

  it('destroy clears all data')

  it('subdirectories')
}