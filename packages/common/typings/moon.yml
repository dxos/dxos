layer: library
language: typescript
tags:
  - typecheck
  - pack
tasks:
  compile:
    command: echo "No compilation needed for type definitions package"
    outputs: []
    options:
      merge: replace
