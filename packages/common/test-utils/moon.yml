layer: library
language: typescript
tags:
  - ts-build
  - pack
tasks:
  compile:
    command: pnpm exec dx-compile
    args:
      - --outputPath=dist/lib
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/playwright.ts'
    deps:
      - dx-compile:compile
    inputs:
      - '@group(production)'
    outputs:
      - dist/lib/
    options:
      merge: replace
      timeout: 10
