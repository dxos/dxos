//
// Copyright 2022 DXOS.org
//

// TODO(wittjosiah): Custom compilation script which only bundles the module being exported and replaces the internal usage of other stdlib modules with @dxos/node-std external refs.

import assert from 'assert';

export default assert;
