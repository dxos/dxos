//
// Copyright 2023 DXOS.org
//

import { getSize } from '../../styles';
import { mx } from '../../util';

export const defaultListItemEndcap = mx(getSize(10), 'overflow-hidden flex items-start justify-center');
export const defaultListItemHeading = 'flex-1 min-bs-10';
