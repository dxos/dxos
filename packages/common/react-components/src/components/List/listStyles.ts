//
// Copyright 2023 DXOS.org
//

import { getSize } from '../../styles';
import { mx } from '../../util';

export const defaultListItemEndcap = mx(getSize(10), 'shrink-0 flex items-start justify-center');
export const defaultListItemHeading = 'flex-1 min-bs-10 min-is-0';
