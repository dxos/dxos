//
// Copyright 2023 DXOS.org
//

/*
export const initSentry = (options: Partial<InitOptions>) => {
  log.info('Initializing Sentry', { SENTRY_DESTINATION, environment, release });
  Sentry.init({
    ...options,
    destination: SENTRY_DESTINATION,
  });
  Sentry.enableSentryLogProcessor();
};
*/
