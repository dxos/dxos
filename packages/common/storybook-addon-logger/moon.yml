layer: library
language: typescript
tags:
  - ts-build
  - pack
tasks:
  compile:
    args:
      - '--entryPoint=src/index.ts'
      - '--entryPoint=src/manager.tsx'
      - '--entryPoint=src/preview.tsx'
      - '--platform=browser'
