---
description: Starting work
auto_execution_mode: 3
---

If you starting work on a new worktree, make sure to bootstrap the repo first: run `moon :build` -- this installs deps and build all packages.

Some other useful commands:

```
moon :test
moon :lint -- --fix
```