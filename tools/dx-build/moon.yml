layer: tool
language: typescript
tasks:
  compile:
    command: pnpm exec tsx ./src/main.ts
    inputs:
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60
