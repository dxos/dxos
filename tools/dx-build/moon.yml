type: tool
language: typescript
tasks:
  compile:
    command: pnpm run --silent build
    inputs:
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60
