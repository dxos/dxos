type: tool
language: typescript
tasks:
  compile:
    command: vite-node ./src/main.ts
    inputs:
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60
