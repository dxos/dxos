layer: tool
language: typescript
tasks:
  build:
    command: pnpm exec vite-node ./src/main.ts
    inputs:
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60
