---
title: Slide 2
---

import { Test } from '../components';

export const now = new Date();

[//]: # (TODO: Layout with top and two columns.)

- This slide was created on {now.toDateString()} at {now.toTimeString()}.

```ts
// This is inline.
const x = 100;
```

```ts file=./code/example.ts
```

This is a React component:

<Test />
