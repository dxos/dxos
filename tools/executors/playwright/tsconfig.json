{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",

    // Compile executors to CommonJS, so that NX can load them correctly.
    "module": "CommonJS"
  },
  "include": [
    "src"
  ]
}
