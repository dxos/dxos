layer: tool
language: typescript
tags:
  - storybook
  - no-compile
tasks:
  serve:
    command: pnpm exec storybook dev
    deps:
      - ^:build
    preset: server
  build:
    command: pnpm exec storybook build
    deps:
      - ^:build
    outputs:
      - storybook-static/
