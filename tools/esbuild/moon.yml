type: tool
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - compile
      - test
    rename:
      build: compile
tasks:
  compile:
    deps:
      - esbuild-plugins:compile
    options:
      mergeOutputs: replace
    outputs:
      - dist/
