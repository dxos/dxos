layer: tool
language: typescript
tasks:
  compile:
    command: vite-node ./src/main.ts
    deps:
      - esbuild-plugins:compile
    inputs:
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60
