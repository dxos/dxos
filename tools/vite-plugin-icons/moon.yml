type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - compile
      - lint
    rename:
      build: compile
tasks:
  compile:
    options:
      mergeOutputs: 'replace'
    outputs:
      - dist/
