# TODO(wittjosiah): This is in tools because we aren't publishing it.
#   It should be removed and we should use @chui/vite-plugin-icons again.
layer: library
language: typescript
tags:
  - vite-plugin
  - ts-compile
  - ts-test

tasks:
  test:
    options:
      mergeOutputs: replace
    outputs: []
