type: library
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - compile
      - lint
      - test
    rename:
      build: compile
tasks:
  compile:
    options:
      mergeOutputs: 'replace'
    outputs:
      - dist/
