layer: scaffolding
language: typescript
workspace:
  inheritedTasks:
    exclude:
      - lint
tasks:
  compile:
    command: echo "No compilation needed for scaffolding/test tool"
    outputs: []
    options:
      merge: replace
