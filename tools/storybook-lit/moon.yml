layer: tool
language: typescript
tasks:
  serve:
    command: storybook dev
    args:
      - --port=9009
      - --no-open
    deps:
      - ^:build
    inputs:
      - .storybook/*
    preset: server
  serve-e2e:
    command: storybook dev
    args:
      - --port=9009
      - --ci
      - --quiet
    deps:
      - ^:build
    inputs:
      - .storybook/*
    preset: server
