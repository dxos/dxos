name: Publish
on:
  workflow_dispatch:
    inputs:
      publish_npm:
        description: 'Publish a non-production branch to NPM'
        default: 'false'
  push:
    branches:
      - labs
      - main
      - production
      - staging
      - dev
  release:
    types:
      - published

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write  # Required for npm trusted publisher OIDC

jobs:
  publish:
    # Must use GitHub-hosted runner for npm provenance (sigstore attestation).
    runs-on: ubuntu-24.04-4
    container:
      image: ghcr.io/dxos/gh-actions:24.11.1
      credentials:
        username: dxos-bot
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          lfs: true
      # TODO: read secrets directly from 1p?
      - name: Set environment variables from secrets, env files. Also set DX_ENVIRONMENT and BRANCH
        run: .github/workflows/scripts/populate-env.sh ${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}} >> $GITHUB_ENV
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          POSTHOG_CLI_API_KEY: ${{ secrets.POSTHOG_CLI_API_KEY }}
          COMPOSER_APP_POSTHOG_API_HOST: ${{ secrets.COMPOSER_APP_POSTHOG_API_HOST }}
          COMPOSER_APP_DEV_POSTHOG_API_KEY: ${{ secrets.COMPOSER_APP_DEV_POSTHOG_API_KEY }}
          COMPOSER_APP_DEV_POSTHOG_PROJECT_ID: ${{ secrets.COMPOSER_APP_DEV_POSTHOG_PROJECT_ID }}
          COMPOSER_APP_DEV_POSTHOG_FEEDBACK_SURVEY_ID: ${{ secrets.COMPOSER_APP_DEV_POSTHOG_FEEDBACK_SURVEY_ID }}
          COMPOSER_APP_PROD_POSTHOG_API_KEY: ${{ secrets.COMPOSER_APP_PROD_POSTHOG_API_KEY }}
          COMPOSER_APP_PROD_POSTHOG_PROJECT_ID: ${{ secrets.COMPOSER_APP_PROD_POSTHOG_PROJECT_ID }}
          COMPOSER_APP_PROD_POSTHOG_FEEDBACK_SURVEY_ID: ${{ secrets.COMPOSER_APP_PROD_POSTHOG_FEEDBACK_SURVEY_ID }}
          COMPOSER_APP_STAGING_POSTHOG_API_KEY: ${{ secrets.COMPOSER_APP_STAGING_POSTHOG_API_KEY }}
          COMPOSER_APP_STAGING_POSTHOG_PROJECT_ID: ${{ secrets.COMPOSER_APP_STAGING_POSTHOG_PROJECT_ID }}
          COMPOSER_APP_STAGING_POSTHOG_FEEDBACK_SURVEY_ID: ${{ secrets.COMPOSER_APP_STAGING_POSTHOG_FEEDBACK_SURVEY_ID }}
          DX_DISCORD_WEBHOOK_URL: ${{ secrets.DX_DISCORD_WEBHOOK_URL }}
          DX_OTEL_ENDPOINT: ${{ secrets.DX_OTEL_ENDPOINT }}
          DX_OTEL_HEADERS: ${{ secrets.DX_OTEL_HEADERS }}
          IPDATA_API_KEY: ${{ secrets.IPDATA_API_KEY }}
          IPFS_API_SECRET: ${{ secrets.IPFS_API_SECRET }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Setup
        uses: ./.github/actions/setup

      - name: Bump Versions
        run: node .github/workflows/scripts/bump-version.js ${{ env.DX_ENVIRONMENT }}
        if: env.BRANCH != 'production'

      - name: Build
        run: moon run :build
        env:
          NODE_ENV: production

      # NOTE: Shell must be bundled before npm publish.
      - name: Bundle Shell
        run: moon run shell:bundle
        env:
          NODE_ENV: production

      - name: Publish to NPM
        id: npm-publish
        run: .github/workflows/scripts/publish.sh
        if: env.BRANCH == 'production' || env.BRANCH == 'staging' || github.event.inputs.publish_npm == 'true'

      - name: Publish Apps
        run: .github/workflows/scripts/publish-apps.sh
        env:
          NODE_ENV: production
          NODE_OPTIONS: '--max_old_space_size=12288'

      - name: Bundle Devtools
        run: moon run devtools-extension:bundle
        env:
          NODE_ENV: production
          NODE_OPTIONS: '--max_old_space_size=12288'

      - name: Gather Artifacts for Devtools extension
        run: |
          ROOT_CWD="$(pwd)"
          mkdir artifacts
          pushd packages/devtools/devtools-extension/out/devtools-extension && zip -r $ROOT_CWD/artifacts/devtools.zip . && popd
        if: github.event_name == 'release' && github.event.action == 'published'

      - name: Upload Devtools extension
        uses: fnkr/github-action-ghr@v1
        env:
          GHR_PATH: artifacts/
          GITHUB_TOKEN: ${{ secrets.CREATE_PR_TOKEN }}
        if: github.event_name == 'release' && github.event.action == 'published'
