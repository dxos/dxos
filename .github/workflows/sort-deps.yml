#
# Copyright 2021 DXOS.org
#

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1,3,5'

name: Standardize configs

jobs:
  update:
    name: Update package.json and tsconfig.json files
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 'chore: Update package.json and tsconfig.json files'
        uses: technote-space/create-pr-action@v2
        with:
          EXECUTE_COMMANDS: |
            npx @sfomin/for-each-package@0.0.2 -n "@dxos/*" "npx sort-package-json@1.53.1"
            npx @monorepo-utils/workspaces-to-typescript-project-references@2.7.4
          COMMIT_MESSAGE: 'chore: Sort package.json and update references in tsconfig.json files'
          COMMIT_NAME: 'GitHub Actions'
          COMMIT_EMAIL: 'ci@dxos.org'
          PR_BRANCH_NAME: 'standardize-configs-${PR_ID}'
          PR_TITLE: 'chore: Sort package.json and update references in tsconfig.json files'
          GITHUB_TOKEN: ${{ secrets.CREATE_PR_TOKEN }}
