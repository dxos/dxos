<!-- Group 03: no-casts. Append diagnostics below in the header format "# WARN|ERROR `file:line[:col]`". Leave empty if clean. -->

# ERROR `packages/apps/composer-app/src/main.tsx:92`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const importMeta = import.meta as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-app/src/main.tsx:216`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(window as any).composer = { profiler, otel };`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-app/src/main.tsx:560`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const root = document.getElementById('root')!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:189`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const eventKeyOf = (event: any): string =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:191`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `inventory = modules.map((module: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:201`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `requires: (spec.requires ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:202`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `provides: (spec.provides ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:294`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const modules = (data.snapshot?.modules ?? []).map((entry: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/harness-helpers.ts:318`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `slowestModules: (data.snapshot?.modules ?? []).slice(0, 10).map((entry: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:245`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `args: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:247`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/playwright/plugins/markdown.ts:16`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const composer = (window as any).composer;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-app/src/vite/trace-boot-leak.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `handler(this: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/vite/trace-boot-leak.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const infoCache = new Map<string, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/src/vite/trace-boot-leak.ts:62`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const current = queue.shift()!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-app/vite.config.ts:405`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const url = new URL(req.url!, `http://${req.headers.host}`);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-app/vite.config.ts:651`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function chunkFileNames(chunkInfo: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-app/vite.config.ts:653`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let segments: any[] = chunkInfo.facadeModuleId.split('/').reverse().slice(1);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/background.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(msg: any, sender: browser.Runtime.MessageSender): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/background.ts:105`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/background.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/background.ts:156`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.contextMenus.onClicked.addListener(async (info: any, tab: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/content.ts:63`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/content.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/content.ts:219`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/content.ts:232`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:93`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.actionId).toBe('a-1');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:94`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.source.url).toBe('https://example.com/');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:95`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.source.title).toBe('Example');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.selection?.text).toBe('hello');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:97`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.selection?.html).toBe('<p>hello</p>');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:98`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.hints?.ogTitle).toBe('OG Title');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/deliver.test.ts:99`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.hints?.h1).toBe('Heading');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:50`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:51`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions[0].id).toBe('action-1');`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:152`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:169`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((messages[0] as any).message.type).toBe(PAGE_ACTIONS_LIST_MESSAGE_TYPE);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/actions/registry.test.ts:170`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((messages[0] as any).tabId).toBe(5);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/composer-crx/src/core/extractors/snapshot.test.ts:39`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(snapshot.html!.length).toBe(MAX_HTML_LENGTH);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/apps/testbench-app/src/components/Error.tsx:14`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const stack = (error as any)?.stack;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/testbench-app/src/components/Error.tsx:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [{ joke, punchline }, setJoke] = useState<any>({});`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/testbench-app/src/components/ItemList.tsx:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const setValue = (object: Obj.Any, prop: string, value: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/testbench-app/src/components/ItemList.tsx:83`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `checked={(object as any)[property]}`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/apps/testbench-app/src/components/status/ErrorIndicator.tsx:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const errorListener = (event: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/todomvc/src/components/TodoFooter.tsx:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onClearCompleted: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/todomvc/src/components/TodoItem.tsx:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const input: any = event.target;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/apps/todomvc/src/components/Todos.tsx:54`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `inputRef.current!.value = '';`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/chain.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Transform = (...args: any) => Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.test.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let lastArgs: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.test.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fn = throttle((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.test.ts:86`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let lastArgs: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.test.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fn = debounce((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Callback = (...args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/debounce.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/errors.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const toError = (err: any) => (err === undefined || typeof err === 'string' ? new Error(er...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/errors.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onError(err: any): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/errors.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const observableError = (observable: ObservableProvider<AsyncEvents>, err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/event-emitter.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const onEvent = (eventEmitter: EventEmitter, eventName: string, callback: (args: any) => v...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/event-emitter.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `test?: (args: any) => boolean,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/event-emitter.ts:37`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(event: string, cb: (data?: any) => void): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `off(event: string, cb: (data?: any) => void): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onEvent = (data: any) => event.emit(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:131`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(_ctx: any, _callback?: EventCallback<T>, options?: ListenerOptions): CleanupFn {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:168`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `once(_ctx: any, _callback?: (data: T) => void): CleanupFn {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:304`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return this as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/async/src/events.ts:466`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:481`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:493`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `event: WeakRef<Event<any>>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/events.ts:494`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `listener: EventListener<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/mutex.test.ts:131`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/mutex.test.ts:139`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(error!.stack!.includes('throwsError')).to.be.true;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/mutex.test.ts:140`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(error!.stack!.includes('callmutex')).to.be.true;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/mutex.ts:108`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const enableWarning = !FORCE_DISABLE_WARNING && (globalThis as any).mochaExecutor;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/async/src/mutex.ts:116`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/mutex.ts:118`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor: TypedPropertyDescriptor<(...args: any) => Promise<any>>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/mutex.ts:120`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/mutex.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = async function synchronizedMethod(this: any & LockableClass, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/observable.test.ts:148`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const concat = observable.losslessConcat((a: any, b: any) => a.concat(...b), MulticastObservable....`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/persistent-lifecycle.ts:123`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await cancelWithContext(this._ctx!, sleep(this._restartAfter));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/persistent-lifecycle.ts:154`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._restartTask!.schedule();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/async/src/push-iterable.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `throw(value?: any): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/push-iterable.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const buf: ({ kind: 'next'; value: T } | { kind: 'return'; value: TReturn } | { kind: 'throw'; va...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/push-iterable.ts:63`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `throw: (value?: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/stream-to-array.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const streamToArray = (stream: NodeJS.ReadableStream): Promise<any[]> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/stream-to-array.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let deferred: Promise<any[]>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/stream-to-array.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let arr: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/stream-to-array.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onData = (doc: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/stream-to-array.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onEnd = (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/task-scheduling.ts:181`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/task-scheduling.ts:189`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/test-stream.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `override _write(chunk: any, encoding: BufferEncoding, callback: (error?: Error | null) => void): ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/testing.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const waitForCondition = <FunctionType extends (...args: any) => any>({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/testing.ts:42`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/testing.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const untilError = (cb: () => Promise<any>) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/track-leaks.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/track-leaks.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target.prototype[open] = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/track-leaks.ts:68`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target.prototype[close] = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/async/src/track-leaks.ts:91`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(global as any).dxDumpLeaks = dumpLeaks;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/async/src/trigger.test.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [value, setValue] = trigger<any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/buffer-patch.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const patchBufferCodec = (codec: Codec<any>) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/buffer-patch.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (x: any) => arrayToBuffer(codec.encode(x)),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:68`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get schema(): Schema<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:91`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fromObject(obj: any): T {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addJson(schema: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:102`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encodeToJson(value: T, options: EncodingOptions = {}): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/codec.ts:107`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decodeFromJson(data: any, options: EncodingOptions = {}): T {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/common.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: T, context: TypeMapperContext, schema: Schema<any>, options: EncodingOptions) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/common.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any, context: TypeMapperContext, schema: Schema<any>, options: EncodingOptions) =...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/common.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Substitutions = Record<string, SubstitutionDescriptor<any>>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/compression.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const compressSchema = (data: any): any => compress(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/compression.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const decompressSchema = (data: any): any => decompress(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type TypeMapper = (value: any, context: TypeMapperContext, ...extraArgs: any) => any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:42`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type FieldMapper = (value: any, typeName: string) => Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const mapMessage = async (type: protobufjs.Type, mapper: FieldMapper, obj: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const mapField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return await Promise.all(value.map((value: any) => mapScalarField(field, mapper, value)));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:69`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const mapScalarField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/mapping.ts:90`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const res: Record<K, U> = {} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const ref = (value: any): Ref => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:40`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isRef = (value: any): value is Ref => value[Ref] === true;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `gen: (c: (parts: TemplateStringsArray, ...args: any[]) => void) => void,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `ctx: Record<string, any> = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:74`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): ((...args: any[]) => any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const preprocessArg = (arg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Mapper = (obj: any, extraArgs: any[]) => any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `cache[type.fullName] = {} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/sanitizer.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const sanitize = (type: pb.Type, value: any, path: string, context: SanitizeContext) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/sanitizer.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const sanitizeEnum = (type: pb.Enum, value: any, path: string, context: SanitizeContext): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/schema.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static fromJson<T extends Record<string, any>, S extends Record<string, any> = {}>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/schema.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/schema.ts:107`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addJson(schema: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/service.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/service.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(backend: ServiceBackend, service: pb.Service, schema: Schema<any>, encodingOptions?: ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/service.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[methodName] = (request: unknown, requestOptions?: RequestOptions) => {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/service.ts:75`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedRequestType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:79`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return Stream.map(stream, (data) => responseCodec.decode(data.value!, encodingOptions));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:82`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[methodName] = async (request: unknown, requestOptions?: RequestOptions) => {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/service.ts:88`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedRequestType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:97`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Object.defineProperty((this as any)[methodName], 'name', {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/service.ts:110`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/service.ts:126`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:132`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedResponseType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:147`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:155`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedResponseType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/codec-protobuf/src/service.ts:164`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (handler as any).bind(service);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/stream.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/stream.ts:251`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/stream.ts:262`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/stream.ts:284`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:33`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return value as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/any.ts:50`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): WithTypeUrl<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Struct = Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const encodeStructValue = (structValue: any, visitedObjects: WeakSet<any>): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const encodeStruct = (struct: Struct, visitedObjects = new WeakSet<any>()): any => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:49`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decodeStructValue = (structValue: any): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decodeStruct = (struct: any): Struct =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: Struct): any => encodeStruct(value),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/struct.ts:81`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): Struct => decodeStruct(value),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/timestamp.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: Date): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/codec-protobuf/src/substitutions/timestamp.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): Date => new Date(parseInt(value.seconds ?? '0') * 1000 + (value.nanos ?? 0)...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.test.ts:44`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((undefined as any) instanceof Context).toBeFalsy();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/context/src/context.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type DisposeCallback = () => any | Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly #attributes: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.ts:130`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.ts:195`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/context.ts:260`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getAttribute(key: string): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/resource.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `open?(ctx?: Context): Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/resource.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `close?(): Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/resource.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (doubleErr: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/context/src/resource.ts:188`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type JsonML = [string, Record<string, any>?, ...(JsonML | string)[]];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `header: (config?: any) => JsonML | null;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `hasBody?: (config?: any) => boolean;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `body?: (config?: any) => JsonML | null;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:44`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `((window as any).devtoolsFormatters ??= []).push({`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/debug/src/devtools-formatter.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `header: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `hasBody: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/devtools-formatter.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `body: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/equality.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol]: (other: any) => boolean;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/equality.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const isEquatable = (value: any): value is Equatable => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/equality.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const isEqual = (value: Equatable, other: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/equality.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const loadashEqualityFn = (value: any, other: any): boolean | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/exposed-modules.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const exposeModule = (name: string, module: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/exposed-modules.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const EXPOSED_MODULES: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/inspect.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const inspectObject = (obj: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/log-method.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/log-method.ts:10`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor: TypedPropertyDescriptor<(...args: any) => any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/log-method.ts:12`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/debug/src/log-method.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/log-method.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `result.catch((err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/log-method.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:19`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const stack = err.stack!.split('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/debug/src/snoop.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `in(label: string, level: SnoopLevel, ...args: any[]): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `out(label: string, level: SnoopLevel, result: any): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `sync(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:54`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `async(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/snoop.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return async (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/strings.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const truncateKey = (key: any, length = 8) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/timeout-warning.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (target: any, propertyName: string, descriptor: TypedPropertyDescriptor<(...args: any) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/debug/src/timeout-warning.ts:47`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/debug/src/timeout-warning.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtom.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomInitialValues.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function useAtomInitialValues(initialValues: Iterable<readonly [Atom.Writable<any, any>, a...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Registry.getResult(registry, atom as Atom.Atom<Result.Result<any, any>>, { suspendOnWaiting: true...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:41`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return options!.mode === 'promise' ? promise.then(flattenExit) : promise;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:45`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `registry.set(atom, typeof value === 'function' ? (value as any)(registry.get(atom)) : value);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/hooks/useAtomSuspense.ts:55`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} else if (next._tag === 'Initial' || (next as any).waiting) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect-atom-solid/src/registry.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `children: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-atom-solid/src/registry.ts:36`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `initialValues?: Iterable<readonly [Atom.Atom<any>, any]>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get(fullyQualifiedName: string): Schema.Schema<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const schemas = new Map<string, Schema.Schema<any>>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const buildMessage = (fullName: string): Schema.Schema<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:174`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fieldToSchema = (field: protobuf.Field, resolveRef: (fq: string) => Schema.Schema<any>): Sc...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:218`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const enumToSchema = (e: protobuf.Enum): Schema.Schema<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const SCALAR_SCHEMAS: Record<string, Schema.Schema<any>> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect-proto/src/proto-to-effect.ts:254`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const WELL_KNOWN_SCHEMAS: Record<string, Schema.Schema<any>> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/ast.test.ts:217`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect(() => TestSchema.pipe(Schema.pluck('missing' as any), Schema.typeSchema)).to.throw();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect/src/ast.test.ts:223`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(isArrayType(findProperty(Schema.Struct({ a: Schema.Array(Schema.String) }), 'a' as JsonPat...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/effect/src/dynamic-runtime.test.ts:283`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.test.ts:307`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type TagsToContext<Tags extends ReadonlyArray<Context.Tag<any, any>>> = Tags extends reado...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `? Head extends Context.Tag<infer Id, any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `? Tail extends ReadonlyArray<Context.Tag<any, any>>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export interface DynamicRuntime<Tags extends ReadonlyArray<Context.Tag<any, any>>> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly managedRuntime: ManagedRuntime.ManagedRuntime<any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const validateTags = <Tags extends ReadonlyArray<Context.Tag<any, any>>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `context: Context.Context<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function make<const Tags extends ReadonlyArray<Context.Tag<any, any>>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `managedRuntime: ManagedRuntime.ManagedRuntime<any, any> | ManagedRuntime.ManagedRuntime<never, ne...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/dynamic-runtime.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const managedRuntimeAny = managedRuntime as ManagedRuntime.ManagedRuntime<any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/ast.ts:338`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getLiteralValues = <S extends Schema.Schema<any, any, any>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/ast.ts:401`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: Record<string, any> = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/ast.ts:432`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const literal = SchemaAST.getPropertySignatures(type).find((p) => p.name.toString() === prop)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/effect/src/internal/async-task-tagging.ts:27`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const trace = (console as any).createTask(name);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect/src/internal/async-task-tagging.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(span as any)[runInTask] = (f: any) => trace.run(f);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect/src/internal/errors.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const prettyErrorStack = (error: any, appendStacks: string[] = []): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:137`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const causeToError = (cause: Cause.Cause<any>): Error => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:149`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return err.stack!.split('\n').slice(1);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/effect/src/internal/errors.ts:173`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const throwCause = (cause: Cause.Cause<any>): never => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:177`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const unwrapExit = <A>(exit: Exit.Exit<A, any>): A => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} = (...args: any[]): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:233`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [runtime] = args as [Runtime.Runtime<any>];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:235`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `effect: Effect.Effect<any, any, any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:237`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:243`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Runtime.Runtime<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/errors.ts:244`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Effect.Effect<any, any, any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/json-path.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `}) as any as Schema.Schema<JsonPath>;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect/src/internal/json-path.ts:33`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `) as any as Schema.Schema<JsonProp>;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/effect/src/internal/json-path.ts:108`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getField = (object: any, path: JsonPath): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/json-path.ts:116`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getValue = <T extends object>(obj: T, path: JsonPath): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/internal/json-path.ts:123`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const setValue = <T extends object>(obj: T, path: JsonPath, value: any): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/effect/src/sanity.test.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `.catch((error: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/errors/src/errors.test.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/errors/src/errors.test.ts:30`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const stackLines = error.stack!.split('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-factory.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onwrite?: (index: number, data: any, peer: null, cb: (err: Error | null) => void) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-factory.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `FeedFactory<any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.browser.test.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.browser.test.ts:31`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await localBuilder._properties.generator!.writeBlocks(feed.createFeedWriter(), { count: 10 });`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-queue.test.ts:38`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.test.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.test.ts:113`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.test.ts:164`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.test.ts:215`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-queue.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `write: (data: any, next: () => void) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-set-iterator.test.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const randomFeedBlockSelector: FeedBlockSelector<any> = (blocks: FeedBlock<any>[]) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-store.node.test.ts:15`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const feedKey = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-store.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `FeedStore<any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:57`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:71`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:89`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:99`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:144`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:198`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:255`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder1.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:263`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `await feed1.append(`block-${i}` as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.test.ts:301`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `await source.append(random.lorem.sentence() as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:74`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `transform(data: any, cb: (err?: Error | null, data?: any) => void) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readStream.pipe(transform, (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:168`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:169`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (this._hypercore as any).on(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:172`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `off(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:173`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (this._hypercore as any).off(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:177`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.open.bind(this._hypercore) as any)(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:181`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.close.bind(this._hypercore) as any)(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:202`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.get.bind(this._hypercore) as any)(index, options);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:206`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `append(data: any | any[]): Promise<number> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:233`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `proof(index: number, options?: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:242`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify((this._hypercore as any)._putBuffer.bind(this._hypercore) as any)(index, data, p...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:258`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `valueEncoding: { decode: (x: Uint8Array) => x } as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:268`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `valueEncoding: { decode: (x: Uint8Array) => x } as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:284`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _feed: Hypercore<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:289`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(feed: Hypercore<any>, opts: ReadStreamOptions = {}) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/feed-wrapper.ts:307`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `if (this._feed.bitfield!.total(this._cursor, this._cursor + this._batch) === this._batch) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/feed-store/src/testing/test-generator.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const defaultCodec: Codec<any> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/testing/test-generator.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (obj: any) => Buffer.from(JSON.stringify(obj)),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/feed-store/src/testing/test-generator.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const defaultValueEncoding: AbstractValueEncoding<any> = createCodecEncoding(defaultCodec);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/graph/src/Graph.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Any = Specialize<BaseNode, { data?: any }>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/graph/src/Graph.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Any = Specialize<BaseEdge, { data?: any }>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/hypercore-protocol.test.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onmessage: (msg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/hypercore-protocol.test.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onmessage: (msg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/hypercore.test.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const block: any = await get(0);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/iterator.ts:22`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return new Readable({ objectMode: true }).wrap(stream as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/hypercore/src/iterator.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const createAsyncIterator = (stream: Readable): AsyncIterator<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/replicate.test.ts:351`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onClose = (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/streams.test.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `write: (data: any, next: () => void) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/hypercore/src/util.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const py = (obj: any, fn: Function) => promisify(fn.bind(obj));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/invariant/src/meta.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `S: any | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/invariant/src/meta.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `C?: (fn: Function, args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keyboard/src/keyboard.test.ts:30`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/keyboard/src/keyboard.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keyboard/src/keyboard.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keyboard/src/keyboard.ts:136`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const tagName = (event.target as any)?.tagName;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/keyboard/src/keyboard.ts:138`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `tagName === 'INPUT' || tagName === 'TEXTAREA' || (event.target as any)?.getAttribute('contentedit...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/keys/src/DXN.ts:171`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/keys/src/EID.ts:168`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/keys/src/URI.ts:34`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/keys/src/public-key.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isLikeArrayBuffer = (value: any): value is ArrayBuffer =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keys/src/public-key.ts:61`

The angle-bracket cast `<any>source` is equivalent to `as any` and silences the type checker, violating the `no-casts` rule: `(<any>source).asUint8Array`. Replace it with a type guard or a precise type for `source` instead of casting to `any`.

# ERROR `packages/common/keys/src/public-key.ts:62`

The angle-bracket cast `<any>source` is equivalent to `as any` and silences the type checker, violating the `no-casts` rule: `(<any>source).asUint8Array()`. Replace it with a type guard or a precise type for `source` instead of casting to `any`.

# ERROR `packages/common/keys/src/public-key.ts:85`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keys/src/public-key.ts:126`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static isPublicKey(value: any): value is PublicKey {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keys/src/public-key.ts:133`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static assertValidPublicKey(value: any): asserts value is PublicKey {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/keys/src/public-key.ts:265`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return `PublicKey(${printControlCode(inspectFn.colors[color]![0])}${this.truncate()}${printContro...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/keys/src/public-key.ts:266`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `inspectFn.colors.reset![0],`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/keys/src/public-key.ts:319`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol](other: any): boolean {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/lock-file/src/sys.ts:66`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const result = this._flockNative!(fd, op);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/lock-file/src/sys.ts:70`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const errno = this._koffi!.errno();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log-store-idb/src/idb-log-store.browser.test.ts:23`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/log-store-idb/src/idb-log-store.ts:333`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const locks = (globalThis as any).navigator?.locks as LockManager | undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log-store-idb/src/idb-log-store.ts:367`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `byteLength: byteLengthUtf8(rows[index]!.lines),`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log-store-idb/src/idb-log-store.ts:384`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((globalThis as any).document?.visibilityState === 'hidden') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log-store-idb/src/idb-log-store.ts:503`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `parts.push('\n', chunks[index]!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log-store-idb/src/idb-log-store.ts:528`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const chunk = chunks[index]!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/context.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type LogContext = Record<string, any> | Error | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/context.ts:211`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getContextFromEntry = (entry: LogEntry): Record<string, any> | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/context.ts:224`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const c = (entryContext as any).context;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/context.ts:280`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `mergeInto((rawContext as any).context);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/context.ts:286`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `mergeInto((entry.error as any).context);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/context.ts:340`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const ctxErr = (rawContext as any).error ?? (rawContext as any).err;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/decorators.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any, propertyKey: string | symbol, descriptor: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:17`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/decorators.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `S: this as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/decorators.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:49`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:59`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<F extends (...args: any[]) => any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `opts: { transformOutput?: (result: ReturnType<F>) => Promise<any> | any } = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decoratedFn = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:70`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:79`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `transformedResult = result.then(opts.transformOutput as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/decorators.ts:101`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/decorators.ts:110`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isThenable = (obj: any): obj is Promise<unknown> => obj && typeof obj.then === 'function';`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/environment.test.ts:199`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const scope = makeWorkerScope(FakeServiceWorkerGlobalScope, 'ServiceWorkerGlobalScope') as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/environment.ts:18`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const cryptoRef = (globalThis as any).crypto as Crypto | undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/environment.ts:58`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const ctor = (scope as any)?.[ctorName];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/environment.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const scope: any = options.scope ?? globalThis;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `public instance: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `public constr: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const info: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function decorateMethodWeakReturnOwnership(prototype: any, key: string) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `prototype[key] = function (...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:50`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `res.then((value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function ownershipClass<T extends { new (...args: any[]): {} }>(constr: T) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:74`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const currentCausality = (globalThis as any)[kCurrentOwnershipScope];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/experimental/ownership.ts:75`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(globalThis as any)[kCurrentOwnershipScope] = new OwnershipScope(constr, currentCausality);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/experimental/ownership.ts:77`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[kOwnershipScope] = (globalThis as any)[kCurrentOwnershipScope];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/experimental/ownership.ts:78`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[kOwnershipScope].instance = this;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/experimental/ownership.ts:79`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(globalThis as any)[kCurrentOwnershipScope] = currentCausality;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/experimental/ownership.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const debugInfo = (target: any, propertyKey: string, descriptor: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/experimental/ownership.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getCurrentOwnershipScope = (thisRef: any) => thisRef;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/jsonl.test.ts:116`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(line!.indexOf('\n')).toBe(-1);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/jsonl.test.ts:118`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const record = JSON.parse(line!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/log-buffer.test.ts:107`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(record.c!.length).toBe(500);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/log-buffer.test.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const circular: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log-buffer.test.ts:129`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(JSON.parse(record.c!).self).toBe('[object Object]');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/log.test.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly context?: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.test.ts:77`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.test.ts:85`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.test.ts:93`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.ts:83`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function: <F extends (...args: any[]) => any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `transformOutput?: (result: ReturnType<F>) => Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.ts:207`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const log: Log = ((globalThis as any).DX_LOG ??= createLog());`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/log.ts:215`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const debug = (label?: any, args?: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/log.ts:227`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const getFormattedStackTrace = () => new Error().stack!.split('\n').slice(3).join('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/meta.ts:41`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `'S': any | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/meta.ts:47`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `'C'?: (fn: Function, args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/options.ts:59`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `file: process!.env.LOG_CONFIG,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/options.ts:60`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `filter: process!.env.LOG_FILTER,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/options.ts:61`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `processor: process!.env.LOG_PROCESSOR,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/platform/browser/index.ts:14`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((globalThis as any).localStorage_dxlog) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/platform/browser/index.ts:15`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `dxlog = (globalThis as any).localStorage_dxlog;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/log/src/processors/browser-processor.ts:76`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const levels: any = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/browser-processor.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const levels: any = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/browser-processor.ts:144`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const unwrapEffectError = (error: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/console-processor.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `context?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/console-processor.ts:37`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `scope?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/file-processor.ts:67`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/processors/file-processor.ts:90`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return logFilePath!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/log/src/scope.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const logInfo = (target: any, propertyKey: string, descriptor?: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/scope.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const gatherLogInfoFromScope = (scope: any): Record<string, any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/scope.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/log/src/scope.ts:40`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/merkle-search-tree/src/forest.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `invariant(node!.level > 0);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/merkle-search-tree/src/forest.ts:97`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `node = this.#nodes.get(node!.children[node!.items.length]);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/merkle-search-tree/src/forest.ts:194`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return await this.merge(root, newTree, async (key, value1, value2) => (value2 !== null ? value2 :...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/merkle-search-tree/src/forest.ts:305`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = (item1?.key ?? item2?.key)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/protobuf-compiler/src/main.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `parser.add_argument('-v', '--version', { action: 'version', version } as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/protobuf-compiler/src/parser/resolver.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return function (this: any, origin, target) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/src/protobuf-json.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[K: string]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/src/protobuf-json.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const serializeSchemaToJson = (root: protobuf.Root): any =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/src/util.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getFlags = (enumType: any, flags: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/test/any.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[key: string]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/test/proto/substitutions.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any) => new MyKey(value.data),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/test/proto/substitutions.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): MyInterfaceSubstitution => value,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/protobuf-compiler/test/types.test.ts:67`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const encoded = codec.encode({ badname: 'badvalue' } as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:37`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `database.getdb(res!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:70`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).destroyed = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:71`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).unlinked = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).closed = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:78`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).deletable = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/idb-storage.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `reject(request.error!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:55`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const entry of (root as any).keys()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile(path: string, filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:115`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `await file.destroy().catch((err: any) => log.warn(err));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:123`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const filename of await (this._root as any).keys()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:124`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await this._root!.removeEntry(filename, { recursive: true }).catch((err: any) =>`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:124`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `await this._root!.removeEntry(filename, { recursive: true }).catch((err: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:155`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const entry of (handle as any).values()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:172`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await recurse(this._root!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/browser/web-fs.ts:252`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any as RandomAccessStorage;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/common/abstract-storage.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/abstract-storage.ts:81`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected getOrCreateFile(path: string, filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/abstract-storage.ts:114`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected abstract _createFile(path: string, filename: string, opts?: any): RandomAccessStorage;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/abstract-storage.ts:134`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Array.from((await this._getFiles(path)).values()).map((file) => file.close().catch((err: any) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/abstract-storage.ts:149`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `.catch((err: any) => log.error(err.message));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/directory.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile: (path: string, filename: string, opts?: any) => File;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/directory.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _getOrCreateFile: (path: string, filename: string, opts?: any) => File;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/directory.ts:67`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile(filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/file.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const pifyFields = (object: any, type: StorageType, fields: string[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/file.ts:55`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `object[field] = async (...args: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/random-access-storage/src/common/memory-storage.ts:25`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const newFile = file.clone!();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/common/memory-storage.ts:26`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(newFile as any).closed = false;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/common/memory-storage.ts:39`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return cb(err, arrayToBuffer(data!));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/random-access-storage/src/common/memory-storage.ts:50`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const size = (file as any).length;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/random-access-storage/src/node/node-storage.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected override _createFile(path: string, filename: string, opts: any = {}): RandomAccessStora...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:70`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.vfs_register(vfs as any, false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onMessage = (event: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:151`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:155`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:185`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (e: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/OpfsWorker.ts:190`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `options.port.postMessage([messageId!, message, undefined]);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:148`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.vfs_register(vfs as any, false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:176`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:181`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:187`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let index = 0; index < columns!.length; index++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:188`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![index]] = row[index];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:219`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:224`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let index = 0; index < columns!.length; index++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:225`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![index]] = row[index];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/internal/opfs-client.ts:297`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/common/sql-sqlite/src/testing/00-opfs-pool-sync.browser.test.ts:89`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(dxos!.payloadBytes).toBeGreaterThanOrEqual(source.byteLength);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:81`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `console.log({ reg: sqlite3.vfs_register(vfs as any, false) });`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `sqlite3.update_hook(db, (_op: any, _db: any, table: any, rowid: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:107`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:112`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const obj: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:113`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let i = 0; i < columns!.length; i++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:114`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![i]] = row[i];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:139`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:143`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const obj: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:144`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let i = 0; i < columns!.length; i++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:145`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![i]] = row[i];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/sql-sqlite/src/testing/sqlite-idb.browser.test.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let vfs: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/storybook-utils/src/stories/test/Test.stories.tsx:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `play: async ({ args, canvasElement }: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/test-utils/src/playwright.ts:43`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const packageDir = packageJson!.split('/').slice(0, -1).join('/');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/test-utils/src/resource.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `open(): Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/test-utils/src/resource.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `close(): Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/timeframe/src/timeframe.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol](other: any): boolean {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any, propertyKey: string, descriptor: TypedPropertyDescriptor<(...args: any) => any>) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:33`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/api.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const spanAttributes: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:88`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `instance: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:126`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const spanAttributes: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/api.ts:166`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const addLink = (parent: any, child: any, opts: AddLinkOptions = {}) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/diagnostic.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/diagnostic.ts:61`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `registerDiagnostic(params: TraceDiagnosticProps<any>): TraceDiagnostic {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/diagnostic.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:71`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (this._serveChannel && typeof (this._serveChannel as any).unref === 'function') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this._serveChannel as any).unref();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:73`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this._clientChannel as any).unref();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:83`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._serveChannel!.postMessage({`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:99`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._serveChannel!.postMessage({`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/diagnostics-channel.ts:110`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._ctx.onDispose(() => this._serveChannel!.removeEventListener('message', listener));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/trace-processor.ts:69`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addLink(parent: any, child: any, opts: AddLinkOptions): void {}`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/tracing/src/trace-processor.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const TRACE_PROCESSOR: TraceProcessor = ((globalThis as any).TRACE_PROCESSOR ??= new Trace...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/tracing/src/tracing.test.ts:81`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.options.parentContext?.traceparent).toBe(parentTrace.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:84`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(childCtx?.getAttribute(TRACE_SPAN_ATTRIBUTE)?.traceparent).toBe(span!.spanContext.tracepar...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:86`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.ended).toBe(false);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:88`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:159`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:183`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(childSpan!.options.parentContext?.traceparent).toBe(parentSpan!.spanContext.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:209`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await svc.laterWork(capturedCtx!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:215`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(laterSpan!.options.parentContext?.traceparent).toBe(setupSpan!.spanContext.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:236`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(failSpan!.error).toBe(testError);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:237`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(failSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:261`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(longSpan!.ended).toBe(false);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:263`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `resolveWork!();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:266`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(longSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:288`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeTypeOf('number');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:289`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeGreaterThanOrEqual(beforeStart);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:290`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeLessThanOrEqual(afterEnd);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:292`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeTypeOf('number');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:293`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeGreaterThanOrEqual(workSpan!.options.startTime!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/tracing/src/tracing.test.ts:294`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeLessThanOrEqual(afterEnd);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/binder.ts:11`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const createBinder = (obj: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/callback-collection.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export class CallbackCollection<F extends (...args: any[]) => Promise<any>> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/callback.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export class Callback<T extends (...args: any[]) => any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/callback.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Obj = { [i: string | symbol]: any };`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/callback.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return new Proxy<any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/callback.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/complex.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: T, value2: T, set: Set<T>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/complex.ts:192`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/complex.ts:226`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._values.get(primitive)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/complex.ts:242`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._values.get(primitive)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/deep.ts:11`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const setDeep = <T>(obj: any, path: readonly (string | number)[], value: T): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/deep.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getDeep = <T>(obj: any, path: readonly (string | number)[]): T | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/defer-function.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends (...args: any[]) => any>(fnProvider: () => T) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/defer.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((result as any) instanceof Promise) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/di-container.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `#entries = new Map<DiKey<any>, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `new (...args: any[]): T;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `new (...args: any[]): T;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return id as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/di-key.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `stringify(id: DiKey<any>): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:90`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `combine(...ids: DiKey<any>[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:96`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (id as any)[symbolSingleton];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/di-key.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _lookupCombined(map: WeakMap<any, any>, [first, ...rest]: DiKey<any>[]): WeakMap<any, any...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/di-key.ts:108`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _combinedDescription([first, ...rest]: DiKey<any>[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/entry.ts:32`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return this as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/instance-id.ts:9`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const instanceContexts = ((globalThis as any)[symbol] ??= new WeakMap<`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/instance-id.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `instanceIds: WeakMap<any, number>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/instance-id.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getPrototypeSpecificInstanceId = (instance: any): number => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/instance-id.ts:25`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const instanceCtx = defaultMap(instanceContexts as any, prototype, () => ({`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/instance-id.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getDebugName = (instance: any): string => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonReplacer = (key: string, value: any): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:41`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonlogify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/json.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(key: string, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fn: (value: any, recurse: (value: any) => any, key: string | number | undefined) => any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _cyclic = new Map<any, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _fn: (value: any, recurse: (value: any) => any, key: string | number | undefined...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `map(value: any): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _map(value: any, key: string | number | undefined): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _recurse = (value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:59`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:77`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fn: (value: any, recurse: (value: any) => Promise<any>, key: string | number | undefined) => Prom...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _cyclic = new Map<any, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:88`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `recurse: (value: any) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:91`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `map(value: any): Promise<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _map(value: any, key: string | number | undefined): Promise<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _recurse = async (value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/map-values.ts:122`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/object.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const keys = <R>(obj: R): (keyof R)[] => Object.keys(obj as any) as (keyof R)[];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/object.ts:10`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const entries = <R>(obj: R): [keyof R, R[keyof R]][] => Object.entries(obj as any) as [key...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/pick.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const pickBy = (obj: Record<any, any>, predicate: (value: any) => boolean) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/pick.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const result: Record<any, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/platform.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const isBun = () => (globalThis as any).Bun !== undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/platform.ts:10`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const isTauri = () => !!(globalThis as any).__TAURI__;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/platform.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `})(navigator.userAgent || navigator.vendor || (window as any).opera);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/platform.ts:45`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `})(navigator.userAgent || navigator.vendor || (window as any).opera);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/platform.ts:76`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const platform = ((navigator as any).userAgentData?.platform || navigator.platform)?.toLowerCase();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/reducers.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `typeof accessor === 'function' ? accessor(value) : (value as Record<any, any>)[accessor];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/reducers.ts:59`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `.sort((a, b) => a! - b!) as number[];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/remove-undefined-keys.ts:7`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((object as any)[prop] === undefined) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/remove-undefined-keys.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `delete (object as any)[prop];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/remove-undefined-keys.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Test = string[] | ((key: string, value: any) => boolean);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/remove-undefined-keys.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const removeProperties = (root: any, condition: Test): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-await.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `taskFactory: (item: T) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-await.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-instanceof.test.ts:37`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((undefined as any) instanceof Tagged).to.eq(false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/safe-instanceof.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-instanceof.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: (instance: any) => instance?.[instanceTag] === tag,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-instanceof.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: (instance: any) => instance?.[instanceTag] === tag,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:10`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type StringifyReplacer = (key: string, value: any) => typeof SKIP | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function safeStringify(obj: any, filter: StringifyReplacer | undefined = defaultFilter, in...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function replacer(this: any, key: string, value: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/safe-stringify.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return function (this: any, key: string, value: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/sort.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(a: any, b: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/sort.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends Record<string, any>>(prop: string, sorter: Compare<any>, inc = true): Compare<any> =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/sort.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends Record<string, any>>(sorters: Compare<T>[]) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/string.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function inline(strings: TemplateStringsArray, ...values: any[]): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/string.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function trim(strings: TemplateStringsArray, ...values: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tracer.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tracer.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get(id: string, filter?: Record<string, any>): Event[] | undefined {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tracer.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `emit(id: string, value?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tracer.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `mark(id: string, value?: any): { start: number; end: () => void } {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tracer.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _createEvent(id: string, value?: any): Event {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/tree.ts:20`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `node.children!.forEach((child: TreeNode, i) => {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/types.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type GuardedType<T> = T extends ((value: any) => value is infer R) ? R : never;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/types.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const stripUndefined = <T extends { [index: string]: any }>(obj: T): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/util/src/types.ts:128`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(sorted as any)[key] = (obj as any)[key];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/uint8array.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const inner = (value as any)['/'];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/util/src/weak.ts:108`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._internal.has(key) && this._internal.get(key)!.deref() !== undefined;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR `packages/common/util/src/weak.ts:130`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/web-context-react/src/consumer.test.tsx:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const Consumer = ({ options }: { options?: any }) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/common/web-context-react/src/consumer.test.tsx:66`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const event = e as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/web-context-solid/src/consumer.test.tsx:124`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const event = e as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/common/web-context-solid/src/solid-element.tsx:83`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/core/compute/agent-runtime/src/agent-service/AgentService.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type AgentRpcs = ReturnType<typeof AgentProcess> extends Process.Process<any, any, any, infer Rpc...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/agent-runtime/src/assistant-session-tests/xml-response.test.ts:56`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const combined = textBlocks.map((block) => (block as any).text ?? '').join('\n');`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/agent-runtime/src/testing/assistant-test-layer.ts:328`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:400`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const PARTS: Response.StreamPart<any>[] = [`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:483`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `parts: readonly Response.StreamPart<any>[],`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:484`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Stream.Stream<Response.StreamPart<any>, never, never> => Stream.fromIterable(parts);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:490`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const text = (text: string[]): Iterable<Response.StreamPart<any>> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:499`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const reasoning = (text: string): Iterable<Response.StreamPart<any>> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.test.ts:508`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const toolCall = (id: string, name: string, params: Record<string, unknown>): Iterable<Response.S...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiParser.ts:438`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `{ parseReasoningTags }: Pick<ParseResponseOptions<any>, 'parseReasoningTags'>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/AiPreprocessor.ts:707`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `filtered.push(next as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly toolkit: Toolkit.Toolkit<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:61`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly layer: Layer.Layer<unknown, any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly handlers: Effect.Effect<Toolkit.WithHandler<any>, any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:85`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `}) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:118`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} = function (...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (self: OpaqueToolkit<any, any, any>) => provideImpl(self, layer);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:124`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:130`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const provided = Layer.provide(self.layer, layer as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:131`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return make(self.toolkit as any, provided as any) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:157`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:165`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Layer.mergeAll(...(toolkits.map((t) => t.layer) as [any, ...any[]])),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:166`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:191`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly [Schema.TypeId]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:192`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly make: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:193`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly Type: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:194`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly Encoded: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/OpaqueToolkit.ts:198`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly annotations: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:361`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `description: Tool.getDescription(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:362`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `parameters: Tool.getJsonSchema(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:606`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:616`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (HttpClientError.isHttpClientError(err) && (err as any).cause?.code === 'ConnectionRefused') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:621`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: (err as any).request,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:694`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:704`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (HttpClientError.isHttpClientError(err) && (err as any).cause?.code === 'ConnectionRefused') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:709`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: (err as any).request,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:778`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const parsedStream: Stream.Stream<Response.StreamPartEncoded, any, any> = response.stream.pipe(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:785`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/anthropic/AnthropicResolver.ts:39`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `({ type: 'adaptive' as any } as const)`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:154`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let body: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:164`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let body: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:237`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const readBody = async (init?: RequestInit): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/testing/effect-ai.test.ts:194`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as ...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/effect-ai.test.ts:259`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as ...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:419`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `toolChoice: params.toolChoice as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:472`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `toolChoice: params.toolChoice as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:521`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `description: Tool.getDescription(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:522`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `inputSchema: Tool.getJsonSchema(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:654`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:728`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/testing/process-messages.ts:58`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const tk: Toolkit.Toolkit<{}> = opts.toolkit ?? (Toolkit.make() as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/tools.test.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/tools.test.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `yield* ToolExecutionService.handlersFor(dynamicToolkit) as Effect.Effect<any, never, ToolExecutio...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR `packages/core/compute/ai/src/testing/tools.test.ts:125`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((result as any).result).toBeUndefined();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR `packages/core/compute/ai/src/testing/tools.test.ts:126`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((result as any).error).toMatch(/SyntaxError/);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

