---
branch: claude/agentic-code-review-3wrnts
commit: 50877ea571c434fec6f31fc0c57201c924fb9981
base: 31612f6b9ec09d1e16652ae51ded2298a89a7aee
mode: default
createdAt: 2026-08-12T10:32:19.831Z
isFinalized: true
groups: 20
rules: [harness-script-hygiene, no-casts, no-compat-shims, no-sleep-in-test, private-new-packages, workspace-deps]
reviewId: 50877ea571
---

_5242 error(s), 31 warning(s)._

# WARN 50877ea571-1 harness-script-hygiene `.agents/skills/agentic-review/lib/store.mjs:99`

The bare `catch { return []; }` in `ruleIdsFromReviewDir` swallows any failure reading/parsing `groups.json` (corrupt JSON, permissions error, race after the `existsSync` check) and silently falls back to "no rules seen," which makes `prepare.mjs` treat every rule as new and full-scan — masking a real problem rather than tolerating a known, named condition. Per the harness-script-hygiene rule, either narrow the catch to the specific tolerated failure (mirroring the `ENOENT` handling in `readReview` just above) with a comment explaining why swallowing it is safe, or log/rethrow unexpected errors instead of defaulting to `[]` unconditionally.

# ERROR 50877ea571-2 no-casts `packages/apps/composer-app/src/main.tsx:92`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const importMeta = import.meta as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-3 no-casts `packages/apps/composer-app/src/main.tsx:216`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(window as any).composer = { profiler, otel };`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-4 no-casts `packages/apps/composer-app/src/main.tsx:560`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const root = document.getElementById('root')!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-5 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:189`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const eventKeyOf = (event: any): string =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-6 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:191`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `inventory = modules.map((module: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-7 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:201`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `requires: (spec.requires ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-8 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:202`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `provides: (spec.provides ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-9 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:294`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const modules = (data.snapshot?.modules ?? []).map((entry: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-10 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:318`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `slowestModules: (data.snapshot?.modules ?? []).slice(0, 10).map((entry: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-11 no-casts `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:245`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `args: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-12 no-casts `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:247`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-13 no-casts `packages/apps/composer-app/src/playwright/plugins/markdown.ts:16`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const composer = (window as any).composer;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-14 no-casts `packages/apps/composer-app/src/vite/trace-boot-leak.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `handler(this: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-15 no-casts `packages/apps/composer-app/src/vite/trace-boot-leak.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const infoCache = new Map<string, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-16 no-casts `packages/apps/composer-app/src/vite/trace-boot-leak.ts:62`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const current = queue.shift()!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-17 no-casts `packages/apps/composer-app/vite.config.ts:405`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const url = new URL(req.url!, `http://${req.headers.host}`);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-18 no-casts `packages/apps/composer-app/vite.config.ts:651`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function chunkFileNames(chunkInfo: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-19 no-casts `packages/apps/composer-app/vite.config.ts:653`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let segments: any[] = chunkInfo.facadeModuleId.split('/').reverse().slice(1);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-20 no-casts `packages/apps/composer-crx/src/background.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(msg: any, sender: browser.Runtime.MessageSender): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-21 no-casts `packages/apps/composer-crx/src/background.ts:105`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-22 no-casts `packages/apps/composer-crx/src/background.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-23 no-casts `packages/apps/composer-crx/src/background.ts:156`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.contextMenus.onClicked.addListener(async (info: any, tab: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-24 no-casts `packages/apps/composer-crx/src/content.ts:63`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-25 no-casts `packages/apps/composer-crx/src/content.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-26 no-casts `packages/apps/composer-crx/src/content.ts:219`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-27 no-casts `packages/apps/composer-crx/src/content.ts:232`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-28 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:93`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.actionId).toBe('a-1');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-29 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:94`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.source.url).toBe('https://example.com/');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-30 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:95`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.source.title).toBe('Example');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-31 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.selection?.text).toBe('hello');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-32 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:97`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.selection?.html).toBe('<p>hello</p>');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-33 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:98`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.hints?.ogTitle).toBe('OG Title');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-34 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:99`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(result!.snapshot.hints?.h1).toBe('Heading');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-35 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:50`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-36 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:51`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions[0].id).toBe('action-1');`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-37 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-38 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:152`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-39 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:169`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((messages[0] as any).message.type).toBe(PAGE_ACTIONS_LIST_MESSAGE_TYPE);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-40 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:170`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((messages[0] as any).tabId).toBe(5);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-41 no-casts `packages/apps/composer-crx/src/core/extractors/snapshot.test.ts:39`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(snapshot.html!.length).toBe(MAX_HTML_LENGTH);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-42 no-casts `packages/apps/testbench-app/src/components/Error.tsx:14`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const stack = (error as any)?.stack;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-43 no-casts `packages/apps/testbench-app/src/components/Error.tsx:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [{ joke, punchline }, setJoke] = useState<any>({});`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-44 no-casts `packages/apps/testbench-app/src/components/ItemList.tsx:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const setValue = (object: Obj.Any, prop: string, value: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-45 no-casts `packages/apps/testbench-app/src/components/ItemList.tsx:83`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `checked={(object as any)[property]}`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-46 no-casts `packages/apps/testbench-app/src/components/status/ErrorIndicator.tsx:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const errorListener = (event: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-47 no-casts `packages/apps/todomvc/src/components/TodoFooter.tsx:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onClearCompleted: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-48 no-casts `packages/apps/todomvc/src/components/TodoItem.tsx:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const input: any = event.target;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-49 no-casts `packages/apps/todomvc/src/components/Todos.tsx:54`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `inputRef.current!.value = '';`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-50 no-casts `packages/common/async/src/chain.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Transform = (...args: any) => Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# WARN 50877ea571-51 no-sleep-in-test `packages/common/async/src/debounce.test.ts:24`

`await sleep(150)` is used repeatedly (also lines 44, 59, 81, 98, 112) to wait out a real throttle/debounce timer before asserting the callback fired. This is a real-timer wait to synchronize with async work; prefer `vi.useFakeTimers()`/`vi.advanceTimersByTime()` (or an Effect `TestClock` if the implementation is ported to Effect) so the test doesn't depend on wall-clock timing.

# ERROR 50877ea571-52 no-casts `packages/common/async/src/debounce.test.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let lastArgs: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-53 no-casts `packages/common/async/src/debounce.test.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fn = throttle((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-54 no-casts `packages/common/async/src/debounce.test.ts:86`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let lastArgs: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-55 no-casts `packages/common/async/src/debounce.test.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fn = debounce((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-56 no-casts `packages/common/async/src/debounce.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Callback = (...args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-57 no-casts `packages/common/async/src/debounce.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-58 no-casts `packages/common/async/src/debounce.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-59 no-casts `packages/common/async/src/debounce.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-60 no-casts `packages/common/async/src/debounce.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return ((...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-61 no-casts `packages/common/async/src/errors.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const toError = (err: any) => (err === undefined || typeof err === 'string' ? new Error(er...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-62 no-casts `packages/common/async/src/errors.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onError(err: any): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-63 no-casts `packages/common/async/src/errors.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const observableError = (observable: ObservableProvider<AsyncEvents>, err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-64 no-casts `packages/common/async/src/event-emitter.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const onEvent = (eventEmitter: EventEmitter, eventName: string, callback: (args: any) => v...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-65 no-casts `packages/common/async/src/event-emitter.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `test?: (args: any) => boolean,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-66 no-casts `packages/common/async/src/event-emitter.ts:37`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# WARN 50877ea571-67 no-sleep-in-test `packages/common/async/src/events.test.ts:37`

`await sleep(5)` (and `sleep(2)` at lines 67 and 87) waits an arbitrary real duration for `event.debounce()`/`ctx.dispose()` side effects to settle before asserting on `pureCount`/`debounceCount`/`received`. Replace with a `Trigger`/`waitForCondition` on the actual completion signal instead of guessing a sufficient delay.

# ERROR 50877ea571-68 no-casts `packages/common/async/src/events.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(event: string, cb: (data?: any) => void): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-69 no-casts `packages/common/async/src/events.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `off(event: string, cb: (data?: any) => void): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-70 no-casts `packages/common/async/src/events.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onEvent = (data: any) => event.emit(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-71 no-casts `packages/common/async/src/events.ts:131`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(_ctx: any, _callback?: EventCallback<T>, options?: ListenerOptions): CleanupFn {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-72 no-casts `packages/common/async/src/events.ts:168`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `once(_ctx: any, _callback?: (data: T) => void): CleanupFn {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-73 no-casts `packages/common/async/src/events.ts:304`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return this as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-74 no-casts `packages/common/async/src/events.ts:466`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-75 no-casts `packages/common/async/src/events.ts:481`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-76 no-casts `packages/common/async/src/events.ts:493`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `event: WeakRef<Event<any>>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-77 no-casts `packages/common/async/src/events.ts:494`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `listener: EventListener<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-78 no-casts `packages/common/async/src/mutex.test.ts:131`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-79 no-casts `packages/common/async/src/mutex.test.ts:139`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(error!.stack!.includes('throwsError')).to.be.true;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-80 no-casts `packages/common/async/src/mutex.test.ts:140`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(error!.stack!.includes('callmutex')).to.be.true;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-81 no-casts `packages/common/async/src/mutex.ts:108`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const enableWarning = !FORCE_DISABLE_WARNING && (globalThis as any).mochaExecutor;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-82 no-casts `packages/common/async/src/mutex.ts:116`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-83 no-casts `packages/common/async/src/mutex.ts:118`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor: TypedPropertyDescriptor<(...args: any) => Promise<any>>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-84 no-casts `packages/common/async/src/mutex.ts:120`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-85 no-casts `packages/common/async/src/mutex.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = async function synchronizedMethod(this: any & LockableClass, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-86 no-casts `packages/common/async/src/observable.test.ts:148`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const concat = observable.losslessConcat((a: any, b: any) => a.concat(...b), MulticastObservable....`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# WARN 50877ea571-87 no-sleep-in-test `packages/common/async/src/persistent-lifecycle.test.ts:109`

`void persistentLifecycle.scheduleRestart(); await sleep(10); await persistentLifecycle.close();` blindly waits 10ms hoping the restart has begun before closing, rather than synchronizing on a `Trigger`/callback fired when the restart actually starts. This is a flaky guess-the-timing pattern.

# ERROR 50877ea571-88 no-casts `packages/common/async/src/persistent-lifecycle.ts:123`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await cancelWithContext(this._ctx!, sleep(this._restartAfter));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-89 no-casts `packages/common/async/src/persistent-lifecycle.ts:154`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._restartTask!.schedule();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-90 no-casts `packages/common/async/src/push-iterable.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `throw(value?: any): void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-91 no-casts `packages/common/async/src/push-iterable.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const buf: ({ kind: 'next'; value: T } | { kind: 'return'; value: TReturn } | { kind: 'throw'; va...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-92 no-casts `packages/common/async/src/push-iterable.ts:63`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `throw: (value?: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-93 no-casts `packages/common/async/src/stream-to-array.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const streamToArray = (stream: NodeJS.ReadableStream): Promise<any[]> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-94 no-casts `packages/common/async/src/stream-to-array.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let deferred: Promise<any[]>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-95 no-casts `packages/common/async/src/stream-to-array.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let arr: any[] = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-96 no-casts `packages/common/async/src/stream-to-array.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onData = (doc: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-97 no-casts `packages/common/async/src/stream-to-array.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onEnd = (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-98 no-casts `packages/common/async/src/task-scheduling.ts:181`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-99 no-casts `packages/common/async/src/task-scheduling.ts:189`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-100 no-casts `packages/common/async/src/test-stream.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `override _write(chunk: any, encoding: BufferEncoding, callback: (error?: Error | null) => void): ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-101 no-casts `packages/common/async/src/testing.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const waitForCondition = <FunctionType extends (...args: any) => any>({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-102 no-casts `packages/common/async/src/testing.ts:42`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-103 no-casts `packages/common/async/src/testing.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const untilError = (cb: () => Promise<any>) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-104 no-casts `packages/common/async/src/track-leaks.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-105 no-casts `packages/common/async/src/track-leaks.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target.prototype[open] = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-106 no-casts `packages/common/async/src/track-leaks.ts:68`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target.prototype[close] = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-107 no-casts `packages/common/async/src/track-leaks.ts:91`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(global as any).dxDumpLeaks = dumpLeaks;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-108 no-casts `packages/common/async/src/trigger.test.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [value, setValue] = trigger<any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-109 no-casts `packages/common/codec-protobuf/src/buffer-patch.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const patchBufferCodec = (codec: Codec<any>) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-110 no-casts `packages/common/codec-protobuf/src/buffer-patch.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (x: any) => arrayToBuffer(codec.encode(x)),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-111 no-casts `packages/common/codec-protobuf/src/codec.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-112 no-casts `packages/common/codec-protobuf/src/codec.ts:68`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get schema(): Schema<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-113 no-casts `packages/common/codec-protobuf/src/codec.ts:91`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fromObject(obj: any): T {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-114 no-casts `packages/common/codec-protobuf/src/codec.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addJson(schema: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-115 no-casts `packages/common/codec-protobuf/src/codec.ts:102`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encodeToJson(value: T, options: EncodingOptions = {}): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-116 no-casts `packages/common/codec-protobuf/src/codec.ts:107`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decodeFromJson(data: any, options: EncodingOptions = {}): T {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-117 no-casts `packages/common/codec-protobuf/src/common.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: T, context: TypeMapperContext, schema: Schema<any>, options: EncodingOptions) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-118 no-casts `packages/common/codec-protobuf/src/common.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any, context: TypeMapperContext, schema: Schema<any>, options: EncodingOptions) =...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-119 no-casts `packages/common/codec-protobuf/src/common.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Substitutions = Record<string, SubstitutionDescriptor<any>>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-120 no-casts `packages/common/codec-protobuf/src/compression.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const compressSchema = (data: any): any => compress(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-121 no-casts `packages/common/codec-protobuf/src/compression.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const decompressSchema = (data: any): any => decompress(data);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-122 no-casts `packages/common/codec-protobuf/src/mapping.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type TypeMapper = (value: any, context: TypeMapperContext, ...extraArgs: any) => any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-123 no-casts `packages/common/codec-protobuf/src/mapping.ts:42`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type FieldMapper = (value: any, typeName: string) => Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-124 no-casts `packages/common/codec-protobuf/src/mapping.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const mapMessage = async (type: protobufjs.Type, mapper: FieldMapper, obj: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-125 no-casts `packages/common/codec-protobuf/src/mapping.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-126 no-casts `packages/common/codec-protobuf/src/mapping.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const mapField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-127 no-casts `packages/common/codec-protobuf/src/mapping.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return await Promise.all(value.map((value: any) => mapScalarField(field, mapper, value)));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-128 no-casts `packages/common/codec-protobuf/src/mapping.ts:69`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const mapScalarField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-129 no-casts `packages/common/codec-protobuf/src/mapping.ts:90`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const res: Record<K, U> = {} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-130 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-131 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const ref = (value: any): Ref => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-132 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:40`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isRef = (value: any): value is Ref => value[Ref] === true;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-133 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `gen: (c: (parts: TemplateStringsArray, ...args: any[]) => void) => void,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-134 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `ctx: Record<string, any> = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-135 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:74`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): ((...args: any[]) => any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-136 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const preprocessArg = (arg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-137 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Mapper = (obj: any, extraArgs: any[]) => any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-138 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `cache[type.fullName] = {} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-139 no-casts `packages/common/codec-protobuf/src/sanitizer.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const sanitize = (type: pb.Type, value: any, path: string, context: SanitizeContext) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-140 no-casts `packages/common/codec-protobuf/src/sanitizer.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const sanitizeEnum = (type: pb.Enum, value: any, path: string, context: SanitizeContext): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-141 no-casts `packages/common/codec-protobuf/src/schema.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static fromJson<T extends Record<string, any>, S extends Record<string, any> = {}>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-142 no-casts `packages/common/codec-protobuf/src/schema.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-143 no-casts `packages/common/codec-protobuf/src/schema.ts:107`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addJson(schema: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-144 no-casts `packages/common/codec-protobuf/src/service.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-145 no-casts `packages/common/codec-protobuf/src/service.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(backend: ServiceBackend, service: pb.Service, schema: Schema<any>, encodingOptions?: ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-146 no-casts `packages/common/codec-protobuf/src/service.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[methodName] = (request: unknown, requestOptions?: RequestOptions) => {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-147 no-casts `packages/common/codec-protobuf/src/service.ts:75`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedRequestType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-148 no-casts `packages/common/codec-protobuf/src/service.ts:79`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return Stream.map(stream, (data) => responseCodec.decode(data.value!, encodingOptions));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-149 no-casts `packages/common/codec-protobuf/src/service.ts:82`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[methodName] = async (request: unknown, requestOptions?: RequestOptions) => {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-150 no-casts `packages/common/codec-protobuf/src/service.ts:88`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedRequestType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-151 no-casts `packages/common/codec-protobuf/src/service.ts:97`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Object.defineProperty((this as any)[methodName], 'name', {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-152 no-casts `packages/common/codec-protobuf/src/service.ts:110`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-153 no-casts `packages/common/codec-protobuf/src/service.ts:126`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-154 no-casts `packages/common/codec-protobuf/src/service.ts:132`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedResponseType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-155 no-casts `packages/common/codec-protobuf/src/service.ts:147`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-156 no-casts `packages/common/codec-protobuf/src/service.ts:155`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `type_url: method.resolvedResponseType!.fullName,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-157 no-casts `packages/common/codec-protobuf/src/service.ts:164`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (handler as any).bind(service);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-158 no-casts `packages/common/codec-protobuf/src/stream.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-159 no-casts `packages/common/codec-protobuf/src/stream.ts:251`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-160 no-casts `packages/common/codec-protobuf/src/stream.ts:262`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-161 no-casts `packages/common/codec-protobuf/src/stream.ts:284`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-162 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-163 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:17`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-164 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:33`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return value as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-165 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-166 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `schema: Schema<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-167 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:50`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): WithTypeUrl<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-168 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Struct = Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-169 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const encodeStructValue = (structValue: any, visitedObjects: WeakSet<any>): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-170 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const encodeStruct = (struct: Struct, visitedObjects = new WeakSet<any>()): any => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-171 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:49`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decodeStructValue = (structValue: any): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-172 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decodeStruct = (struct: any): Struct =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-173 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: Struct): any => encodeStruct(value),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-174 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:81`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): Struct => decodeStruct(value),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-175 no-casts `packages/common/codec-protobuf/src/substitutions/timestamp.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (value: Date): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-176 no-casts `packages/common/codec-protobuf/src/substitutions/timestamp.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): Date => new Date(parseInt(value.seconds ?? '0') * 1000 + (value.nanos ?? 0)...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-177 no-casts `packages/common/context/src/context.test.ts:44`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((undefined as any) instanceof Context).toBeFalsy();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-178 no-casts `packages/common/context/src/context.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type DisposeCallback = () => any | Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-179 no-casts `packages/common/context/src/context.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-180 no-casts `packages/common/context/src/context.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly #attributes: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-181 no-casts `packages/common/context/src/context.ts:130`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-182 no-casts `packages/common/context/src/context.ts:195`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-183 no-casts `packages/common/context/src/context.ts:260`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getAttribute(key: string): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-184 no-casts `packages/common/context/src/resource.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `open?(ctx?: Context): Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-185 no-casts `packages/common/context/src/resource.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `close?(): Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-186 no-casts `packages/common/context/src/resource.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (doubleErr: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-187 no-casts `packages/common/context/src/resource.ts:188`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-188 no-casts `packages/common/debug/src/devtools-formatter.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type JsonML = [string, Record<string, any>?, ...(JsonML | string)[]];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-189 no-casts `packages/common/debug/src/devtools-formatter.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `header: (config?: any) => JsonML | null;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-190 no-casts `packages/common/debug/src/devtools-formatter.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `hasBody?: (config?: any) => boolean;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-191 no-casts `packages/common/debug/src/devtools-formatter.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `body?: (config?: any) => JsonML | null;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-192 no-casts `packages/common/debug/src/devtools-formatter.ts:44`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `((window as any).devtoolsFormatters ??= []).push({`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-193 no-casts `packages/common/debug/src/devtools-formatter.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `header: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-194 no-casts `packages/common/debug/src/devtools-formatter.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `hasBody: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-195 no-casts `packages/common/debug/src/devtools-formatter.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `body: (value: any, config: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-196 no-casts `packages/common/debug/src/equality.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol]: (other: any) => boolean;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-197 no-casts `packages/common/debug/src/equality.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const isEquatable = (value: any): value is Equatable => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-198 no-casts `packages/common/debug/src/equality.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const isEqual = (value: Equatable, other: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-199 no-casts `packages/common/debug/src/equality.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const loadashEqualityFn = (value: any, other: any): boolean | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-200 no-casts `packages/common/debug/src/exposed-modules.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const exposeModule = (name: string, module: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-201 no-casts `packages/common/debug/src/exposed-modules.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const EXPOSED_MODULES: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-202 no-casts `packages/common/debug/src/inspect.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const inspectObject = (obj: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-203 no-casts `packages/common/debug/src/log-method.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `target: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-204 no-casts `packages/common/debug/src/log-method.ts:10`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor: TypedPropertyDescriptor<(...args: any) => any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-205 no-casts `packages/common/debug/src/log-method.ts:12`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-206 no-casts `packages/common/debug/src/log-method.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-207 no-casts `packages/common/debug/src/log-method.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `result.catch((err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-208 no-casts `packages/common/debug/src/log-method.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-209 no-casts `packages/common/debug/src/snoop.ts:19`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const stack = err.stack!.split('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-210 no-casts `packages/common/debug/src/snoop.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `in(label: string, level: SnoopLevel, ...args: any[]): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-211 no-casts `packages/common/debug/src/snoop.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `out(label: string, level: SnoopLevel, result: any): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-212 no-casts `packages/common/debug/src/snoop.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `sync(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-213 no-casts `packages/common/debug/src/snoop.ts:54`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-214 no-casts `packages/common/debug/src/snoop.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `async(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-215 no-casts `packages/common/debug/src/snoop.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return async (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-216 no-casts `packages/common/debug/src/strings.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const truncateKey = (key: any, length = 8) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-217 no-casts `packages/common/debug/src/timeout-warning.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (target: any, propertyName: string, descriptor: TypedPropertyDescriptor<(...args: any) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-218 no-casts `packages/common/debug/src/timeout-warning.ts:47`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-219 no-casts `packages/common/debug/src/timeout-warning.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-220 no-casts `packages/common/effect-atom-solid/src/hooks/useAtom.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-221 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomInitialValues.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function useAtomInitialValues(initialValues: Iterable<readonly [Atom.Writable<any, any>, a...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-222 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-223 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Registry.getResult(registry, atom as Atom.Atom<Result.Result<any, any>>, { suspendOnWaiting: true...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-224 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:41`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return options!.mode === 'promise' ? promise.then(flattenExit) : promise;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-225 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:45`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `registry.set(atom, typeof value === 'function' ? (value as any)(registry.get(atom)) : value);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-226 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly mode?: ([R] extends [Result.Result<any, any>] ? Mode : 'value') | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-227 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSuspense.ts:55`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} else if (next._tag === 'Initial' || (next as any).waiting) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-228 no-casts `packages/common/effect-atom-solid/src/registry.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `children: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-229 no-casts `packages/common/effect-atom-solid/src/registry.ts:36`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `initialValues?: Iterable<readonly [Atom.Atom<any>, any]>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-230 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:30`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get(fullyQualifiedName: string): Schema.Schema<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-231 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:56`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const schemas = new Map<string, Schema.Schema<any>>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-232 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const buildMessage = (fullName: string): Schema.Schema<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-233 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:174`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const fieldToSchema = (field: protobuf.Field, resolveRef: (fq: string) => Schema.Schema<any>): Sc...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-234 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:218`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const enumToSchema = (e: protobuf.Enum): Schema.Schema<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-235 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const SCALAR_SCHEMAS: Record<string, Schema.Schema<any>> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-236 no-casts `packages/common/effect-proto/src/proto-to-effect.ts:254`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const WELL_KNOWN_SCHEMAS: Record<string, Schema.Schema<any>> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-237 no-casts `packages/common/effect/src/ast.test.ts:217`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect(() => TestSchema.pipe(Schema.pluck('missing' as any), Schema.typeSchema)).to.throw();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-238 no-casts `packages/common/effect/src/ast.test.ts:223`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(isArrayType(findProperty(Schema.Struct({ a: Schema.Array(Schema.String) }), 'a' as JsonPat...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-239 no-casts `packages/common/effect/src/dynamic-runtime.test.ts:283`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-240 no-casts `packages/common/effect/src/dynamic-runtime.test.ts:307`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-241 no-casts `packages/common/effect/src/dynamic-runtime.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type TagsToContext<Tags extends ReadonlyArray<Context.Tag<any, any>>> = Tags extends reado...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-242 no-casts `packages/common/effect/src/dynamic-runtime.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `? Head extends Context.Tag<infer Id, any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-243 no-casts `packages/common/effect/src/dynamic-runtime.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `? Tail extends ReadonlyArray<Context.Tag<any, any>>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-244 no-casts `packages/common/effect/src/dynamic-runtime.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export interface DynamicRuntime<Tags extends ReadonlyArray<Context.Tag<any, any>>> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-245 no-casts `packages/common/effect/src/dynamic-runtime.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly managedRuntime: ManagedRuntime.ManagedRuntime<any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-246 no-casts `packages/common/effect/src/dynamic-runtime.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const validateTags = <Tags extends ReadonlyArray<Context.Tag<any, any>>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-247 no-casts `packages/common/effect/src/dynamic-runtime.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `context: Context.Context<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-248 no-casts `packages/common/effect/src/dynamic-runtime.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function make<const Tags extends ReadonlyArray<Context.Tag<any, any>>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-249 no-casts `packages/common/effect/src/dynamic-runtime.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `managedRuntime: ManagedRuntime.ManagedRuntime<any, any> | ManagedRuntime.ManagedRuntime<never, ne...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-250 no-casts `packages/common/effect/src/dynamic-runtime.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const managedRuntimeAny = managedRuntime as ManagedRuntime.ManagedRuntime<any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-251 no-casts `packages/common/effect/src/internal/ast.ts:338`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getLiteralValues = <S extends Schema.Schema<any, any, any>>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-252 no-casts `packages/common/effect/src/internal/ast.ts:401`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: Record<string, any> = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-253 no-casts `packages/common/effect/src/internal/ast.ts:432`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const literal = SchemaAST.getPropertySignatures(type).find((p) => p.name.toString() === prop)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-254 no-casts `packages/common/effect/src/internal/async-task-tagging.ts:27`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const trace = (console as any).createTask(name);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-255 no-casts `packages/common/effect/src/internal/async-task-tagging.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(span as any)[runInTask] = (f: any) => trace.run(f);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-256 no-casts `packages/common/effect/src/internal/errors.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const prettyErrorStack = (error: any, appendStacks: string[] = []): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-257 no-casts `packages/common/effect/src/internal/errors.ts:137`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const causeToError = (cause: Cause.Cause<any>): Error => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-258 no-casts `packages/common/effect/src/internal/errors.ts:149`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return err.stack!.split('\n').slice(1);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-259 no-casts `packages/common/effect/src/internal/errors.ts:173`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const throwCause = (cause: Cause.Cause<any>): never => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-260 no-casts `packages/common/effect/src/internal/errors.ts:177`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const unwrapExit = <A>(exit: Exit.Exit<A, any>): A => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-261 no-casts `packages/common/effect/src/internal/errors.ts:231`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} = (...args: any[]): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-262 no-casts `packages/common/effect/src/internal/errors.ts:233`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const [runtime] = args as [Runtime.Runtime<any>];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-263 no-casts `packages/common/effect/src/internal/errors.ts:235`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `effect: Effect.Effect<any, any, any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-264 no-casts `packages/common/effect/src/internal/errors.ts:237`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-265 no-casts `packages/common/effect/src/internal/errors.ts:243`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Runtime.Runtime<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-266 no-casts `packages/common/effect/src/internal/errors.ts:244`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Effect.Effect<any, any, any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-267 no-casts `packages/common/effect/src/internal/json-path.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `}) as any as Schema.Schema<JsonPath>;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-268 no-casts `packages/common/effect/src/internal/json-path.ts:33`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `) as any as Schema.Schema<JsonProp>;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-269 no-casts `packages/common/effect/src/internal/json-path.ts:108`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getField = (object: any, path: JsonPath): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-270 no-casts `packages/common/effect/src/internal/json-path.ts:116`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getValue = <T extends object>(obj: T, path: JsonPath): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-271 no-casts `packages/common/effect/src/internal/json-path.ts:123`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const setValue = <T extends object>(obj: T, path: JsonPath, value: any): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-272 no-casts `packages/common/effect/src/sanity.test.ts:80`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `.catch((error: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-273 no-casts `packages/common/errors/src/errors.test.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-274 no-casts `packages/common/errors/src/errors.test.ts:30`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const stackLines = error.stack!.split('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-275 no-casts `packages/common/feed-store/src/feed-factory.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onwrite?: (index: number, data: any, peer: null, cb: (err: Error | null) => void) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-276 no-casts `packages/common/feed-store/src/feed-factory.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `FeedFactory<any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-277 no-casts `packages/common/feed-store/src/feed-queue.browser.test.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-278 no-casts `packages/common/feed-store/src/feed-queue.browser.test.ts:31`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await localBuilder._properties.generator!.writeBlocks(feed.createFeedWriter(), { count: 10 });`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# WARN 50877ea571-279 no-sleep-in-test `packages/common/feed-store/src/feed-queue.browser.test.ts:35`

Same busy-poll pattern as `feed-queue.test.ts`: `while (true) { await queue.pop(); ...; await sleep(50); }` followed by `await sleep(400); await queue.close();`. Replace the polling loop with a close/error event wait rather than a fixed-interval sleep loop.

# ERROR 50877ea571-280 no-casts `packages/common/feed-store/src/feed-queue.test.ts:38`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# WARN 50877ea571-281 no-sleep-in-test `packages/common/feed-store/src/feed-queue.test.ts:50`

`while (true) { const next = await queue.pop(); ...; await sleep(50); }` (repeated at line 87) is a busy-poll loop that waits on a fixed sleep between pops to detect the queue closing, instead of relying purely on `queue.pop()` rejecting or a close event/`Trigger`. The `await sleep(400)` before `queue.close()`/`feedStore.close()` a few lines below is the same kind of arbitrary-timing dependency (hoping several pop/sleep cycles have run first).

# ERROR 50877ea571-282 no-casts `packages/common/feed-store/src/feed-queue.test.ts:75`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-283 no-casts `packages/common/feed-store/src/feed-queue.test.ts:113`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-284 no-casts `packages/common/feed-store/src/feed-queue.test.ts:164`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-285 no-casts `packages/common/feed-store/src/feed-queue.test.ts:215`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const queue = new FeedQueue<any>(feed);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-286 no-casts `packages/common/feed-store/src/feed-queue.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `write: (data: any, next: () => void) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-287 no-casts `packages/common/feed-store/src/feed-set-iterator.test.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const randomFeedBlockSelector: FeedBlockSelector<any> = (blocks: FeedBlock<any>[]) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-288 no-casts `packages/common/feed-store/src/feed-store.node.test.ts:15`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const feedKey = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-289 no-casts `packages/common/feed-store/src/feed-store.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `FeedStore<any>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-290 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:57`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-291 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:71`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-292 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:89`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-293 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:99`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-294 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:144`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-295 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:198`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-296 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:255`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key1 = await builder1.keyring!.createKey();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-297 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:263`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `await feed1.append(`block-${i}` as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-298 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:301`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `await source.append(random.lorem.sentence() as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-299 no-casts `packages/common/feed-store/src/feed-wrapper.ts:74`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `transform(data: any, cb: (err?: Error | null, data?: any) => void) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-300 no-casts `packages/common/feed-store/src/feed-wrapper.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readStream.pipe(transform, (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-301 no-casts `packages/common/feed-store/src/feed-wrapper.ts:168`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `on(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-302 no-casts `packages/common/feed-store/src/feed-wrapper.ts:169`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (this._hypercore as any).on(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-303 no-casts `packages/common/feed-store/src/feed-wrapper.ts:172`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `off(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-304 no-casts `packages/common/feed-store/src/feed-wrapper.ts:173`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (this._hypercore as any).off(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-305 no-casts `packages/common/feed-store/src/feed-wrapper.ts:177`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.open.bind(this._hypercore) as any)(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-306 no-casts `packages/common/feed-store/src/feed-wrapper.ts:181`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.close.bind(this._hypercore) as any)(...args);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-307 no-casts `packages/common/feed-store/src/feed-wrapper.ts:202`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify(this._hypercore.get.bind(this._hypercore) as any)(index, options);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-308 no-casts `packages/common/feed-store/src/feed-wrapper.ts:206`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `append(data: any | any[]): Promise<number> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-309 no-casts `packages/common/feed-store/src/feed-wrapper.ts:233`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `proof(index: number, options?: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-310 no-casts `packages/common/feed-store/src/feed-wrapper.ts:242`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return promisify((this._hypercore as any)._putBuffer.bind(this._hypercore) as any)(index, data, p...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-311 no-casts `packages/common/feed-store/src/feed-wrapper.ts:258`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `valueEncoding: { decode: (x: Uint8Array) => x } as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-312 no-casts `packages/common/feed-store/src/feed-wrapper.ts:268`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `valueEncoding: { decode: (x: Uint8Array) => x } as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-313 no-casts `packages/common/feed-store/src/feed-wrapper.ts:284`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _feed: Hypercore<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-314 no-casts `packages/common/feed-store/src/feed-wrapper.ts:289`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(feed: Hypercore<any>, opts: ReadStreamOptions = {}) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-315 no-casts `packages/common/feed-store/src/feed-wrapper.ts:307`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `if (this._feed.bitfield!.total(this._cursor, this._cursor + this._batch) === this._batch) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-316 no-casts `packages/common/feed-store/src/testing/test-generator.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const defaultCodec: Codec<any> = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-317 no-casts `packages/common/feed-store/src/testing/test-generator.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `encode: (obj: any) => Buffer.from(JSON.stringify(obj)),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-318 no-casts `packages/common/feed-store/src/testing/test-generator.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const defaultValueEncoding: AbstractValueEncoding<any> = createCodecEncoding(defaultCodec);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-319 no-casts `packages/common/graph/src/Graph.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Any = Specialize<BaseNode, { data?: any }>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-320 no-casts `packages/common/graph/src/Graph.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type Any = Specialize<BaseEdge, { data?: any }>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-321 no-casts `packages/common/hypercore/src/hypercore-protocol.test.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onmessage: (msg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-322 no-casts `packages/common/hypercore/src/hypercore-protocol.test.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `onmessage: (msg: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-323 no-casts `packages/common/hypercore/src/hypercore.test.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const block: any = await get(0);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-324 no-casts `packages/common/hypercore/src/iterator.ts:22`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return new Readable({ objectMode: true }).wrap(stream as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-325 no-casts `packages/common/hypercore/src/iterator.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const createAsyncIterator = (stream: Readable): AsyncIterator<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-326 no-casts `packages/common/hypercore/src/replicate.test.ts:351`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onClose = (err: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-327 no-casts `packages/common/hypercore/src/streams.test.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `write: (data: any, next: () => void) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-328 no-casts `packages/common/hypercore/src/util.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const py = (obj: any, fn: Function) => promisify(fn.bind(obj));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-329 no-casts `packages/common/invariant/src/meta.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `S: any | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-330 no-casts `packages/common/invariant/src/meta.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `C?: (fn: Function, args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-331 no-casts `packages/common/keyboard/src/keyboard.test.ts:30`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-332 no-casts `packages/common/keyboard/src/keyboard.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-333 no-casts `packages/common/keyboard/src/keyboard.ts:21`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-334 no-casts `packages/common/keyboard/src/keyboard.ts:136`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const tagName = (event.target as any)?.tagName;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-335 no-casts `packages/common/keyboard/src/keyboard.ts:138`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `tagName === 'INPUT' || tagName === 'TEXTAREA' || (event.target as any)?.getAttribute('contentedit...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-336 no-casts `packages/common/keys/src/DXN.ts:171`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-337 no-casts `packages/common/keys/src/EID.ts:168`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-338 no-casts `packages/common/keys/src/public-key.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isLikeArrayBuffer = (value: any): value is ArrayBuffer =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-339 no-casts `packages/common/keys/src/public-key.ts:61`

The angle-bracket cast `<any>source` is equivalent to `as any` and silences the type checker, violating the `no-casts` rule: `(<any>source).asUint8Array`. Replace it with a type guard or a precise type for `source` instead of casting to `any`.

# ERROR 50877ea571-340 no-casts `packages/common/keys/src/public-key.ts:62`

The angle-bracket cast `<any>source` is equivalent to `as any` and silences the type checker, violating the `no-casts` rule: `(<any>source).asUint8Array()`. Replace it with a type guard or a precise type for `source` instead of casting to `any`.

# ERROR 50877ea571-341 no-casts `packages/common/keys/src/public-key.ts:85`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-342 no-casts `packages/common/keys/src/public-key.ts:126`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static isPublicKey(value: any): value is PublicKey {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-343 no-casts `packages/common/keys/src/public-key.ts:133`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `static assertValidPublicKey(value: any): asserts value is PublicKey {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-344 no-casts `packages/common/keys/src/public-key.ts:265`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return `PublicKey(${printControlCode(inspectFn.colors[color]![0])}${this.truncate()}${printContro...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-345 no-casts `packages/common/keys/src/public-key.ts:266`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `inspectFn.colors.reset![0],`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-346 no-casts `packages/common/keys/src/public-key.ts:319`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol](other: any): boolean {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-347 no-casts `packages/common/keys/src/URI.ts:34`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-348 no-casts `packages/common/lock-file/src/sys.ts:66`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const result = this._flockNative!(fd, op);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-349 no-casts `packages/common/lock-file/src/sys.ts:70`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const errno = this._koffi!.errno();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-350 no-casts `packages/common/log-store-idb/src/idb-log-store.browser.test.ts:23`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-351 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:333`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const locks = (globalThis as any).navigator?.locks as LockManager | undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-352 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:367`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `byteLength: byteLengthUtf8(rows[index]!.lines),`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-353 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:384`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((globalThis as any).document?.visibilityState === 'hidden') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-354 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:503`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `parts.push('\n', chunks[index]!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-355 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:528`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const chunk = chunks[index]!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-356 no-casts `packages/common/log/src/context.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type LogContext = Record<string, any> | Error | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-357 no-casts `packages/common/log/src/context.ts:211`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getContextFromEntry = (entry: LogEntry): Record<string, any> | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-358 no-casts `packages/common/log/src/context.ts:224`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const c = (entryContext as any).context;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-359 no-casts `packages/common/log/src/context.ts:280`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `mergeInto((rawContext as any).context);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-360 no-casts `packages/common/log/src/context.ts:286`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `mergeInto((entry.error as any).context);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-361 no-casts `packages/common/log/src/context.ts:340`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const ctxErr = (rawContext as any).error ?? (rawContext as any).err;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-362 no-casts `packages/common/log/src/decorators.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any, propertyKey: string | symbol, descriptor: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-363 no-casts `packages/common/log/src/decorators.ts:17`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-364 no-casts `packages/common/log/src/decorators.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-365 no-casts `packages/common/log/src/decorators.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `S: this as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-366 no-casts `packages/common/log/src/decorators.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-367 no-casts `packages/common/log/src/decorators.ts:49`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-368 no-casts `packages/common/log/src/decorators.ts:59`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<F extends (...args: any[]) => any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-369 no-casts `packages/common/log/src/decorators.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `opts: { transformOutput?: (result: ReturnType<F>) => Promise<any> | any } = {},`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-370 no-casts `packages/common/log/src/decorators.ts:64`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const decoratedFn = function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-371 no-casts `packages/common/log/src/decorators.ts:70`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-372 no-casts `packages/common/log/src/decorators.ts:79`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `transformedResult = result.then(opts.transformOutput as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-373 no-casts `packages/common/log/src/decorators.ts:101`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-374 no-casts `packages/common/log/src/decorators.ts:110`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const isThenable = (obj: any): obj is Promise<unknown> => obj && typeof obj.then === 'function';`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-375 no-casts `packages/common/log/src/environment.test.ts:199`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const scope = makeWorkerScope(FakeServiceWorkerGlobalScope, 'ServiceWorkerGlobalScope') as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-376 no-casts `packages/common/log/src/environment.ts:18`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const cryptoRef = (globalThis as any).crypto as Crypto | undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-377 no-casts `packages/common/log/src/environment.ts:58`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const ctor = (scope as any)?.[ctorName];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-378 no-casts `packages/common/log/src/environment.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const scope: any = options.scope ?? globalThis;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-379 no-casts `packages/common/log/src/experimental/ownership.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `public instance: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-380 no-casts `packages/common/log/src/experimental/ownership.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `public constr: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-381 no-casts `packages/common/log/src/experimental/ownership.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const info: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-382 no-casts `packages/common/log/src/experimental/ownership.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function decorateMethodWeakReturnOwnership(prototype: any, key: string) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-383 no-casts `packages/common/log/src/experimental/ownership.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `prototype[key] = function (...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-384 no-casts `packages/common/log/src/experimental/ownership.ts:50`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `res.then((value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-385 no-casts `packages/common/log/src/experimental/ownership.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function ownershipClass<T extends { new (...args: any[]): {} }>(constr: T) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-386 no-casts `packages/common/log/src/experimental/ownership.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `constructor(...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-387 no-casts `packages/common/log/src/experimental/ownership.ts:74`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const currentCausality = (globalThis as any)[kCurrentOwnershipScope];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-388 no-casts `packages/common/log/src/experimental/ownership.ts:75`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(globalThis as any)[kCurrentOwnershipScope] = new OwnershipScope(constr, currentCausality);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-389 no-casts `packages/common/log/src/experimental/ownership.ts:77`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[kOwnershipScope] = (globalThis as any)[kCurrentOwnershipScope];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-390 no-casts `packages/common/log/src/experimental/ownership.ts:78`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this as any)[kOwnershipScope].instance = this;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-391 no-casts `packages/common/log/src/experimental/ownership.ts:79`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(globalThis as any)[kCurrentOwnershipScope] = currentCausality;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-392 no-casts `packages/common/log/src/experimental/ownership.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const debugInfo = (target: any, propertyKey: string, descriptor: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-393 no-casts `packages/common/log/src/experimental/ownership.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getCurrentOwnershipScope = (thisRef: any) => thisRef;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-394 no-casts `packages/common/log/src/jsonl.test.ts:116`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(line!.indexOf('\n')).toBe(-1);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-395 no-casts `packages/common/log/src/jsonl.test.ts:118`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const record = JSON.parse(line!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-396 no-casts `packages/common/log/src/log-buffer.test.ts:107`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(record.c!.length).toBe(500);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-397 no-casts `packages/common/log/src/log-buffer.test.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const circular: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-398 no-casts `packages/common/log/src/log-buffer.test.ts:129`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(JSON.parse(record.c!).self).toBe('[object Object]');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-399 no-casts `packages/common/log/src/log.test.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly context?: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-400 no-casts `packages/common/log/src/log.test.ts:77`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-401 no-casts `packages/common/log/src/log.test.ts:85`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-402 no-casts `packages/common/log/src/log.test.ts:93`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-403 no-casts `packages/common/log/src/log.ts:83`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function: <F extends (...args: any[]) => any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-404 no-casts `packages/common/log/src/log.ts:87`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `transformOutput?: (result: ReturnType<F>) => Promise<any> | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-405 no-casts `packages/common/log/src/log.ts:207`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const log: Log = ((globalThis as any).DX_LOG ??= createLog());`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-406 no-casts `packages/common/log/src/log.ts:215`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const debug = (label?: any, args?: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-407 no-casts `packages/common/log/src/log.ts:227`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const getFormattedStackTrace = () => new Error().stack!.split('\n').slice(3).join('\n');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-408 no-casts `packages/common/log/src/meta.ts:41`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `'S': any | undefined;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-409 no-casts `packages/common/log/src/meta.ts:47`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `'C'?: (fn: Function, args: any[]) => void;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-410 no-casts `packages/common/log/src/options.ts:59`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `file: process!.env.LOG_CONFIG,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-411 no-casts `packages/common/log/src/options.ts:60`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `filter: process!.env.LOG_FILTER,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-412 no-casts `packages/common/log/src/options.ts:61`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `processor: process!.env.LOG_PROCESSOR,`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-413 no-casts `packages/common/log/src/platform/browser/index.ts:14`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((globalThis as any).localStorage_dxlog) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-414 no-casts `packages/common/log/src/platform/browser/index.ts:15`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `dxlog = (globalThis as any).localStorage_dxlog;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-415 no-casts `packages/common/log/src/processors/browser-processor.ts:76`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const levels: any = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-416 no-casts `packages/common/log/src/processors/browser-processor.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const levels: any = {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-417 no-casts `packages/common/log/src/processors/browser-processor.ts:144`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const unwrapEffectError = (error: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-418 no-casts `packages/common/log/src/processors/console-processor.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `context?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-419 no-casts `packages/common/log/src/processors/console-processor.ts:37`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `scope?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-420 no-casts `packages/common/log/src/processors/file-processor.ts:67`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-421 no-casts `packages/common/log/src/processors/file-processor.ts:90`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return logFilePath!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-422 no-casts `packages/common/log/src/scope.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const logInfo = (target: any, propertyKey: string, descriptor?: PropertyDescriptor) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-423 no-casts `packages/common/log/src/scope.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const gatherLogInfoFromScope = (scope: any): Record<string, any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-424 no-casts `packages/common/log/src/scope.ts:33`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-425 no-casts `packages/common/log/src/scope.ts:40`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-426 no-casts `packages/common/merkle-search-tree/src/forest.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `invariant(node!.level > 0);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-427 no-casts `packages/common/merkle-search-tree/src/forest.ts:97`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `node = this.#nodes.get(node!.children[node!.items.length]);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-428 no-casts `packages/common/merkle-search-tree/src/forest.ts:194`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return await this.merge(root, newTree, async (key, value1, value2) => (value2 !== null ? value2 :...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-429 no-casts `packages/common/merkle-search-tree/src/forest.ts:305`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const key = (item1?.key ?? item2?.key)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-430 no-casts `packages/common/protobuf-compiler/src/main.ts:24`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `parser.add_argument('-v', '--version', { action: 'version', version } as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-431 no-casts `packages/common/protobuf-compiler/src/parser/resolver.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return function (this: any, origin, target) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-432 no-casts `packages/common/protobuf-compiler/src/protobuf-json.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[K: string]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-433 no-casts `packages/common/protobuf-compiler/src/protobuf-json.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const serializeSchemaToJson = (root: protobuf.Root): any =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-434 no-casts `packages/common/protobuf-compiler/src/util.ts:5`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getFlags = (enumType: any, flags: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-435 no-casts `packages/common/protobuf-compiler/test/any.ts:7`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[key: string]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-436 no-casts `packages/common/protobuf-compiler/test/proto/substitutions.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any) => new MyKey(value.data),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-437 no-casts `packages/common/protobuf-compiler/test/proto/substitutions.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `decode: (value: any): MyInterfaceSubstitution => value,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-438 no-casts `packages/common/protobuf-compiler/test/types.test.ts:67`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const encoded = codec.encode({ badname: 'badvalue' } as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-439 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:37`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `database.getdb(res!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-440 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:70`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).destroyed = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-441 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:71`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).unlinked = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-442 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).closed = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-443 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:78`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(file as any).deletable = true;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-444 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:96`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `reject(request.error!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-445 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:55`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const entry of (root as any).keys()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-446 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile(path: string, filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-447 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:115`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `await file.destroy().catch((err: any) => log.warn(err));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-448 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:123`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const filename of await (this._root as any).keys()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-449 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:124`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await this._root!.removeEntry(filename, { recursive: true }).catch((err: any) =>`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-450 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:124`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `await this._root!.removeEntry(filename, { recursive: true }).catch((err: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-451 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:155`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `for await (const entry of (handle as any).values()) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-452 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:172`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await recurse(this._root!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-453 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:252`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any as RandomAccessStorage;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-454 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-455 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:81`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected getOrCreateFile(path: string, filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-456 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:114`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected abstract _createFile(path: string, filename: string, opts?: any): RandomAccessStorage;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-457 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:134`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Array.from((await this._getFiles(path)).values()).map((file) => file.close().catch((err: any) => ...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-458 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:149`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `.catch((err: any) => log.error(err.message));`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-459 no-casts `packages/common/random-access-storage/src/common/directory.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile: (path: string, filename: string, opts?: any) => File;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-460 no-casts `packages/common/random-access-storage/src/common/directory.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _getOrCreateFile: (path: string, filename: string, opts?: any) => File;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-461 no-casts `packages/common/random-access-storage/src/common/directory.ts:67`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `getOrCreateFile(filename: string, opts?: any): File {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-462 no-casts `packages/common/random-access-storage/src/common/file.ts:48`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const pifyFields = (object: any, type: StorageType, fields: string[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-463 no-casts `packages/common/random-access-storage/src/common/file.ts:55`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `object[field] = async (...args: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-464 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:25`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const newFile = file.clone!();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-465 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:26`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(newFile as any).closed = false;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-466 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:39`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return cb(err, arrayToBuffer(data!));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-467 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:50`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const size = (file as any).length;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-468 no-casts `packages/common/random-access-storage/src/node/node-storage.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `protected override _createFile(path: string, filename: string, opts: any = {}): RandomAccessStora...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-469 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:148`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.vfs_register(vfs as any, false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-470 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:176`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-471 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:181`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-472 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:187`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let index = 0; index < columns!.length; index++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-473 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:188`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![index]] = row[index];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-474 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:219`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-475 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:224`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let index = 0; index < columns!.length; index++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-476 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:225`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![index]] = row[index];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-477 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:297`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-478 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:70`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.vfs_register(vfs as any, false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-479 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const onMessage = (event: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-480 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:151`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-481 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:155`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-482 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:185`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (e: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-483 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:190`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `options.port.postMessage([messageId!, message, undefined]);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-484 no-casts `packages/common/sql-sqlite/src/testing/00-opfs-pool-sync.browser.test.ts:89`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(dxos!.payloadBytes).toBeGreaterThanOrEqual(source.byteLength);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-485 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:81`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `console.log({ reg: sqlite3.vfs_register(vfs as any, false) });`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-486 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:92`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `sqlite3.update_hook(db, (_op: any, _db: any, table: any, rowid: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-487 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:104`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const results: Array<any> = [];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-488 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:107`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-489 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:112`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const obj: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-490 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:113`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let i = 0; i < columns!.length; i++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-491 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:114`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![i]] = row[i];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-492 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:139`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `sqlite3.bind_collection(stmt, params as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-493 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:143`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const obj: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-494 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:144`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `for (let i = 0; i < columns!.length; i++) {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-495 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:145`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `obj[columns![i]] = row[i];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-496 no-casts `packages/common/sql-sqlite/src/testing/sqlite-idb.browser.test.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let vfs: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-497 no-casts `packages/common/storybook-utils/src/stories/test/Test.stories.tsx:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `play: async ({ args, canvasElement }: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-498 no-casts `packages/common/test-utils/src/playwright.ts:43`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const packageDir = packageJson!.split('/').slice(0, -1).join('/');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-499 no-casts `packages/common/test-utils/src/resource.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `open(): Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-500 no-casts `packages/common/test-utils/src/resource.ts:9`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `close(): Promise<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-501 no-casts `packages/common/timeframe/src/timeframe.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `[equalsSymbol](other: any): boolean {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-502 no-casts `packages/common/tracing/src/api.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-503 no-casts `packages/common/tracing/src/api.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any, propertyKey: string, descriptor: TypedPropertyDescriptor<(...args: any) => any>) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-504 no-casts `packages/common/tracing/src/api.ts:33`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `const method = descriptor.value!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-505 no-casts `packages/common/tracing/src/api.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `descriptor.value = async function (this: any, ...args: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-506 no-casts `packages/common/tracing/src/api.ts:44`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const spanAttributes: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-507 no-casts `packages/common/tracing/src/api.ts:88`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `instance: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-508 no-casts `packages/common/tracing/src/api.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `attributes?: Record<string, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-509 no-casts `packages/common/tracing/src/api.ts:126`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const spanAttributes: Record<string, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-510 no-casts `packages/common/tracing/src/api.ts:166`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const addLink = (parent: any, child: any, opts: AddLinkOptions = {}) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-511 no-casts `packages/common/tracing/src/diagnostic.ts:29`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `data: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-512 no-casts `packages/common/tracing/src/diagnostic.ts:61`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `registerDiagnostic(params: TraceDiagnosticProps<any>): TraceDiagnostic {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-513 no-casts `packages/common/tracing/src/diagnostic.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-514 no-casts `packages/common/tracing/src/diagnostics-channel.ts:71`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (this._serveChannel && typeof (this._serveChannel as any).unref === 'function') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-515 no-casts `packages/common/tracing/src/diagnostics-channel.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this._serveChannel as any).unref();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-516 no-casts `packages/common/tracing/src/diagnostics-channel.ts:73`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(this._clientChannel as any).unref();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-517 no-casts `packages/common/tracing/src/diagnostics-channel.ts:83`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._serveChannel!.postMessage({`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-518 no-casts `packages/common/tracing/src/diagnostics-channel.ts:99`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._serveChannel!.postMessage({`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-519 no-casts `packages/common/tracing/src/diagnostics-channel.ts:110`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `this._ctx.onDispose(() => this._serveChannel!.removeEventListener('message', listener));`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-520 no-casts `packages/common/tracing/src/trace-processor.ts:69`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `addLink(parent: any, child: any, opts: AddLinkOptions): void {}`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-521 no-casts `packages/common/tracing/src/trace-processor.ts:72`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const TRACE_PROCESSOR: TraceProcessor = ((globalThis as any).TRACE_PROCESSOR ??= new Trace...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-522 no-casts `packages/common/tracing/src/tracing.test.ts:81`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.options.parentContext?.traceparent).toBe(parentTrace.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-523 no-casts `packages/common/tracing/src/tracing.test.ts:84`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(childCtx?.getAttribute(TRACE_SPAN_ATTRIBUTE)?.traceparent).toBe(span!.spanContext.tracepar...`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-524 no-casts `packages/common/tracing/src/tracing.test.ts:86`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.ended).toBe(false);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-525 no-casts `packages/common/tracing/src/tracing.test.ts:88`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(span!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-526 no-casts `packages/common/tracing/src/tracing.test.ts:159`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-527 no-casts `packages/common/tracing/src/tracing.test.ts:183`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(childSpan!.options.parentContext?.traceparent).toBe(parentSpan!.spanContext.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-528 no-casts `packages/common/tracing/src/tracing.test.ts:209`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `await svc.laterWork(capturedCtx!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-529 no-casts `packages/common/tracing/src/tracing.test.ts:215`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(laterSpan!.options.parentContext?.traceparent).toBe(setupSpan!.spanContext.traceparent);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-530 no-casts `packages/common/tracing/src/tracing.test.ts:236`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(failSpan!.error).toBe(testError);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-531 no-casts `packages/common/tracing/src/tracing.test.ts:237`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(failSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-532 no-casts `packages/common/tracing/src/tracing.test.ts:261`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(longSpan!.ended).toBe(false);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-533 no-casts `packages/common/tracing/src/tracing.test.ts:263`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `resolveWork!();`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-534 no-casts `packages/common/tracing/src/tracing.test.ts:266`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(longSpan!.ended).toBe(true);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-535 no-casts `packages/common/tracing/src/tracing.test.ts:288`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeTypeOf('number');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-536 no-casts `packages/common/tracing/src/tracing.test.ts:289`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeGreaterThanOrEqual(beforeStart);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-537 no-casts `packages/common/tracing/src/tracing.test.ts:290`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.options.startTime).toBeLessThanOrEqual(afterEnd);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-538 no-casts `packages/common/tracing/src/tracing.test.ts:292`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeTypeOf('number');`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-539 no-casts `packages/common/tracing/src/tracing.test.ts:293`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeGreaterThanOrEqual(workSpan!.options.startTime!);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-540 no-casts `packages/common/tracing/src/tracing.test.ts:294`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `expect(workSpan!.endTime).toBeLessThanOrEqual(afterEnd);`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-541 no-casts `packages/common/util/src/binder.ts:11`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const createBinder = (obj: any) => ({`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-542 no-casts `packages/common/util/src/callback-collection.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export class CallbackCollection<F extends (...args: any[]) => Promise<any>> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-543 no-casts `packages/common/util/src/callback.ts:16`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export class Callback<T extends (...args: any[]) => any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-544 no-casts `packages/common/util/src/callback.ts:45`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Obj = { [i: string | symbol]: any };`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-545 no-casts `packages/common/util/src/callback.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return new Proxy<any>(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-546 no-casts `packages/common/util/src/callback.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (...args: any[]) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-547 no-casts `packages/common/util/src/complex.ts:65`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: T, value2: T, set: Set<T>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-548 no-casts `packages/common/util/src/complex.ts:192`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-549 no-casts `packages/common/util/src/complex.ts:226`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._values.get(primitive)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-550 no-casts `packages/common/util/src/complex.ts:242`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._values.get(primitive)!;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-551 no-casts `packages/common/util/src/deep.ts:11`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const setDeep = <T>(obj: any, path: readonly (string | number)[], value: T): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-552 no-casts `packages/common/util/src/deep.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getDeep = <T>(obj: any, path: readonly (string | number)[]): T | undefined => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-553 no-casts `packages/common/util/src/defer-function.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends (...args: any[]) => any>(fnProvider: () => T) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-554 no-casts `packages/common/util/src/defer.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((result as any) instanceof Promise) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-555 no-casts `packages/common/util/src/di-container.ts:8`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `#entries = new Map<DiKey<any>, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-556 no-casts `packages/common/util/src/di-key.ts:32`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `new (...args: any[]): T;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-557 no-casts `packages/common/util/src/di-key.ts:46`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `new (...args: any[]): T;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-558 no-casts `packages/common/util/src/di-key.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return id as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-559 no-casts `packages/common/util/src/di-key.ts:72`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `stringify(id: DiKey<any>): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-560 no-casts `packages/common/util/src/di-key.ts:90`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `combine(...ids: DiKey<any>[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-561 no-casts `packages/common/util/src/di-key.ts:96`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return (id as any)[symbolSingleton];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-562 no-casts `packages/common/util/src/di-key.ts:99`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _lookupCombined(map: WeakMap<any, any>, [first, ...rest]: DiKey<any>[]): WeakMap<any, any...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-563 no-casts `packages/common/util/src/di-key.ts:108`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _combinedDescription([first, ...rest]: DiKey<any>[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-564 no-casts `packages/common/util/src/entry.ts:32`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return this as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-565 no-casts `packages/common/util/src/instance-id.ts:9`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const instanceContexts = ((globalThis as any)[symbol] ??= new WeakMap<`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-566 no-casts `packages/common/util/src/instance-id.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `instanceIds: WeakMap<any, number>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-567 no-casts `packages/common/util/src/instance-id.ts:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getPrototypeSpecificInstanceId = (instance: any): number => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-568 no-casts `packages/common/util/src/instance-id.ts:25`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const instanceCtx = defaultMap(instanceContexts as any, prototype, () => ({`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-569 no-casts `packages/common/util/src/instance-id.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const getDebugName = (instance: any): string => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-570 no-casts `packages/common/util/src/json.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonReplacer = (key: string, value: any): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-571 no-casts `packages/common/util/src/json.ts:41`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-572 no-casts `packages/common/util/src/json.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-573 no-casts `packages/common/util/src/json.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const jsonlogify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-574 no-casts `packages/common/util/src/json.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-575 no-casts `packages/common/util/src/json.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(key: string, value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-576 no-casts `packages/common/util/src/map-values.ts:18`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-577 no-casts `packages/common/util/src/map-values.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fn: (value: any, recurse: (value: any) => any, key: string | number | undefined) => any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-578 no-casts `packages/common/util/src/map-values.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-579 no-casts `packages/common/util/src/map-values.ts:25`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _cyclic = new Map<any, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-580 no-casts `packages/common/util/src/map-values.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _fn: (value: any, recurse: (value: any) => any, key: string | number | undefined...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-581 no-casts `packages/common/util/src/map-values.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `map(value: any): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-582 no-casts `packages/common/util/src/map-values.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _map(value: any, key: string | number | undefined): any {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-583 no-casts `packages/common/util/src/map-values.ts:43`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _recurse = (value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-584 no-casts `packages/common/util/src/map-values.ts:59`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-585 no-casts `packages/common/util/src/map-values.ts:77`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-586 no-casts `packages/common/util/src/map-values.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `fn: (value: any, recurse: (value: any) => Promise<any>, key: string | number | undefined) => Prom...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-587 no-casts `packages/common/util/src/map-values.ts:79`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-588 no-casts `packages/common/util/src/map-values.ts:84`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private readonly _cyclic = new Map<any, any>();`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-589 no-casts `packages/common/util/src/map-values.ts:88`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: any,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-590 no-casts `packages/common/util/src/map-values.ts:89`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `recurse: (value: any) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-591 no-casts `packages/common/util/src/map-values.ts:91`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-592 no-casts `packages/common/util/src/map-values.ts:94`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `map(value: any): Promise<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-593 no-casts `packages/common/util/src/map-values.ts:98`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _map(value: any, key: string | number | undefined): Promise<any> {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-594 no-casts `packages/common/util/src/map-values.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _recurse = async (value: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-595 no-casts `packages/common/util/src/map-values.ts:122`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const res: any = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-596 no-casts `packages/common/util/src/object.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const keys = <R>(obj: R): (keyof R)[] => Object.keys(obj as any) as (keyof R)[];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-597 no-casts `packages/common/util/src/object.ts:10`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const entries = <R>(obj: R): [keyof R, R[keyof R]][] => Object.entries(obj as any) as [key...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-598 no-casts `packages/common/util/src/pick.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const pickBy = (obj: Record<any, any>, predicate: (value: any) => boolean) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-599 no-casts `packages/common/util/src/pick.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const result: Record<any, any> = {};`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-600 no-casts `packages/common/util/src/platform.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const isBun = () => (globalThis as any).Bun !== undefined;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-601 no-casts `packages/common/util/src/platform.ts:10`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `export const isTauri = () => !!(globalThis as any).__TAURI__;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-602 no-casts `packages/common/util/src/platform.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `})(navigator.userAgent || navigator.vendor || (window as any).opera);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-603 no-casts `packages/common/util/src/platform.ts:45`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `})(navigator.userAgent || navigator.vendor || (window as any).opera);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-604 no-casts `packages/common/util/src/platform.ts:76`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const platform = ((navigator as any).userAgentData?.platform || navigator.platform)?.toLowerCase();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-605 no-casts `packages/common/util/src/reducers.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `typeof accessor === 'function' ? accessor(value) : (value as Record<any, any>)[accessor];`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-606 no-casts `packages/common/util/src/reducers.ts:59`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `.sort((a, b) => a! - b!) as number[];`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-607 no-casts `packages/common/util/src/remove-undefined-keys.ts:7`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if ((object as any)[prop] === undefined) {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-608 no-casts `packages/common/util/src/remove-undefined-keys.ts:8`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `delete (object as any)[prop];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-609 no-casts `packages/common/util/src/remove-undefined-keys.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type Test = string[] | ((key: string, value: any) => boolean);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-610 no-casts `packages/common/util/src/remove-undefined-keys.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const removeProperties = (root: any, condition: Test): any => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-611 no-casts `packages/common/util/src/safe-await.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `taskFactory: (item: T) => Promise<any>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-612 no-casts `packages/common/util/src/safe-await.ts:22`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-613 no-casts `packages/common/util/src/safe-instanceof.test.ts:37`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((undefined as any) instanceof Tagged).to.eq(false);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-614 no-casts `packages/common/util/src/safe-instanceof.ts:20`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(target: any) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-615 no-casts `packages/common/util/src/safe-instanceof.ts:24`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: (instance: any) => instance?.[instanceTag] === tag,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-616 no-casts `packages/common/util/src/safe-instanceof.ts:28`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value: (instance: any) => instance?.[instanceTag] === tag,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-617 no-casts `packages/common/util/src/safe-stringify.ts:10`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type StringifyReplacer = (key: string, value: any) => typeof SKIP | any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-618 no-casts `packages/common/util/src/safe-stringify.ts:15`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function safeStringify(obj: any, filter: StringifyReplacer | undefined = defaultFilter, in...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-619 no-casts `packages/common/util/src/safe-stringify.ts:19`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `function replacer(this: any, key: string, value: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-620 no-casts `packages/common/util/src/safe-stringify.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-621 no-casts `packages/common/util/src/safe-stringify.ts:73`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (error: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-622 no-casts `packages/common/util/src/safe-stringify.ts:100`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return function (this: any, key: string, value: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-623 no-casts `packages/common/util/src/sort.ts:12`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `(a: any, b: any) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-624 no-casts `packages/common/util/src/sort.ts:26`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends Record<string, any>>(prop: string, sorter: Compare<any>, inc = true): Compare<any> =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-625 no-casts `packages/common/util/src/sort.ts:31`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `<T extends Record<string, any>>(sorters: Compare<T>[]) =>`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-626 no-casts `packages/common/util/src/string.ts:27`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function inline(strings: TemplateStringsArray, ...values: any[]): string {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-627 no-casts `packages/common/util/src/string.ts:35`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export function trim(strings: TemplateStringsArray, ...values: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-628 no-casts `packages/common/util/src/tracer.ts:14`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `value?: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-629 no-casts `packages/common/util/src/tracer.ts:39`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `get(id: string, filter?: Record<string, any>): Event[] | undefined {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-630 no-casts `packages/common/util/src/tracer.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `emit(id: string, value?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-631 no-casts `packages/common/util/src/tracer.ts:66`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `mark(id: string, value?: any): { start: number; end: () => void } {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-632 no-casts `packages/common/util/src/tracer.ts:78`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `private _createEvent(id: string, value?: any): Event {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-633 no-casts `packages/common/util/src/tree.ts:20`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `node.children!.forEach((child: TreeNode, i) => {`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-634 no-casts `packages/common/util/src/types.ts:13`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export type GuardedType<T> = T extends ((value: any) => value is infer R) ? R : never;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-635 no-casts `packages/common/util/src/types.ts:106`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `export const stripUndefined = <T extends { [index: string]: any }>(obj: T): T => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-636 no-casts `packages/common/util/src/types.ts:128`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `(sorted as any)[key] = (obj as any)[key];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-637 no-casts `packages/common/util/src/uint8array.ts:69`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const inner = (value as any)['/'];`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-638 no-casts `packages/common/util/src/weak.ts:108`

A non-null assertion (`!`) forces past a possibly-`undefined`/`null` value instead of handling it, violating the `no-casts` rule: `return this._internal.has(key) && this._internal.get(key)!.deref() !== undefined;`. Replace it with a guard, a default, or a type that no longer allows the missing value at its source.

# ERROR 50877ea571-639 no-casts `packages/common/util/src/weak.ts:130`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-640 no-casts `packages/common/web-context-react/src/consumer.test.tsx:23`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const Consumer = ({ options }: { options?: any }) => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-641 no-casts `packages/common/web-context-react/src/consumer.test.tsx:66`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const event = e as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-642 no-casts `packages/common/web-context-solid/src/consumer.test.tsx:124`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const event = e as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-643 no-casts `packages/common/web-context-solid/src/solid-element.tsx:83`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-644 no-casts `packages/core/compute/agent-runtime/src/agent-service/AgentService.ts:34`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `type AgentRpcs = ReturnType<typeof AgentProcess> extends Process.Process<any, any, any, infer Rpc...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-645 no-casts `packages/core/compute/agent-runtime/src/assistant-session-tests/xml-response.test.ts:56`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const combined = textBlocks.map((block) => (block as any).text ?? '').join('\n');`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-646 no-casts `packages/core/compute/agent-runtime/src/testing/assistant-test-layer.ts:328`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-647 no-casts `packages/core/compute/ai/src/AiParser.test.ts:400`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const PARTS: Response.StreamPart<any>[] = [`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-648 no-casts `packages/core/compute/ai/src/AiParser.test.ts:483`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `parts: readonly Response.StreamPart<any>[],`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-649 no-casts `packages/core/compute/ai/src/AiParser.test.ts:484`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `): Stream.Stream<Response.StreamPart<any>, never, never> => Stream.fromIterable(parts);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-650 no-casts `packages/core/compute/ai/src/AiParser.test.ts:490`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const text = (text: string[]): Iterable<Response.StreamPart<any>> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-651 no-casts `packages/core/compute/ai/src/AiParser.test.ts:499`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const reasoning = (text: string): Iterable<Response.StreamPart<any>> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-652 no-casts `packages/core/compute/ai/src/AiParser.test.ts:508`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const toolCall = (id: string, name: string, params: Record<string, unknown>): Iterable<Response.S...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-653 no-casts `packages/core/compute/ai/src/AiParser.ts:438`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `{ parseReasoningTags }: Pick<ParseResponseOptions<any>, 'parseReasoningTags'>,`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-654 no-casts `packages/core/compute/ai/src/AiPreprocessor.ts:707`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `filtered.push(next as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-655 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:60`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly toolkit: Toolkit.Toolkit<any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-656 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:61`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly layer: Layer.Layer<unknown, any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-657 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:62`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly handlers: Effect.Effect<Toolkit.WithHandler<any>, any, any>;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-658 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:85`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `}) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-659 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:118`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} = function (...args: any[]) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-660 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:121`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `return (self: OpaqueToolkit<any, any, any>) => provideImpl(self, layer);`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-661 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:124`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-662 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:130`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const provided = Layer.provide(self.layer, layer as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-663 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:131`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `return make(self.toolkit as any, provided as any) as any;`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-664 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:157`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-665 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:165`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `Layer.mergeAll(...(toolkits.map((t) => t.layer) as [any, ...any[]])),`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-666 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:166`

An `as unknown as T` double-cast is the escape hatch the `no-casts` rule calls out explicitly — it bypasses the type checker entirely rather than fixing the mismatched type. Rework the underlying type (or the value being cast) so a single, sound assertion (or no assertion) is enough.

# ERROR 50877ea571-667 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:191`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly [Schema.TypeId]: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-668 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:192`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly make: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-669 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:193`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly Type: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-670 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:194`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly Encoded: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-671 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:198`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `readonly annotations: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-672 no-casts `packages/core/compute/ai/src/resolvers/anthropic/AnthropicResolver.ts:39`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `({ type: 'adaptive' as any } as const)`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-673 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:361`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `description: Tool.getDescription(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-674 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:362`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `parameters: Tool.getJsonSchema(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-675 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:606`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-676 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:616`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (HttpClientError.isHttpClientError(err) && (err as any).cause?.code === 'ConnectionRefused') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-677 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:621`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: (err as any).request,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-678 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:694`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-679 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:704`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `if (HttpClientError.isHttpClientError(err) && (err as any).cause?.code === 'ConnectionRefused') {`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-680 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:709`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: (err as any).request,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-681 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:778`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const parsedStream: Stream.Stream<Response.StreamPartEncoded, any, any> = response.stream.pipe(`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-682 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:785`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `request: undefined as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-683 no-casts `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:154`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let body: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-684 no-casts `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:164`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `let body: any;`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-685 no-casts `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:237`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `const readBody = async (init?: RequestInit): Promise<any> => {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-686 no-casts `packages/core/compute/ai/src/testing/effect-ai.test.ts:194`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as ...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-687 no-casts `packages/core/compute/ai/src/testing/effect-ai.test.ts:259`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as ...`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-688 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:419`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `toolChoice: params.toolChoice as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-689 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:472`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `toolChoice: params.toolChoice as any,`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-690 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:521`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `description: Tool.getDescription(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-691 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:522`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `inputSchema: Tool.getJsonSchema(tool as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-692 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:654`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-693 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:728`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `} catch (err: any) {`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-694 no-casts `packages/core/compute/ai/src/testing/process-messages.ts:58`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `const tk: Toolkit.Toolkit<{}> = opts.toolkit ?? (Toolkit.make() as any);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-695 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:28`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `} as any),`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-696 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:52`

A widened `any` type in a signature/declaration defeats the type checker, violating the `no-casts` rule: `yield* ToolExecutionService.handlersFor(dynamicToolkit) as Effect.Effect<any, never, ToolExecutio...`. Replace `any` with a precise type, a generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-697 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:125`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((result as any).result).toBeUndefined();`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-698 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:126`

An `as any` cast silences the type checker instead of fixing the underlying type, violating the `no-casts` rule: `expect((result as any).error).toMatch(/SyntaxError/);`. Give the value a precise type (or narrow it) at its source instead of casting to `any`.

# ERROR 50877ea571-699 no-casts `packages/core/compute/ai/src/tools/call.ts:39:60`

This line casts to `any` with `as any` (`const toolResult = yield* toolkit.handle(toolCall.name as any, input as any).pipe(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-700 no-casts `packages/core/compute/ai/src/tools/call.ts:39:74`

This line casts to `any` with `as any` (`const toolResult = yield* toolkit.handle(toolCall.name as any, input as any).pipe(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-701 no-casts `packages/core/compute/ai/src/tools/tool-execution-service.ts:27:104`

This line casts to `any` with `as any` (`toolkit.of(Record.map(toolkit.tools, (tool, name) => Effect.fail(new AiToolNotFoundError(name))) as any) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-702 no-casts `packages/core/compute/ai/src/tools/tool-execution-service.ts:27:112`

This line casts to `any` with `as any` (`toolkit.of(Record.map(toolkit.tools, (tool, name) => Effect.fail(new AiToolNotFoundError(name))) as any) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-703 no-casts `packages/core/compute/ai/src/tools/tool-resolver-service.ts:37:38`

This declares a widened `any` type (`) => Effect.Effect<Toolkit.Toolkit<any>, AiToolNotFoundError, ToolResolverService> = (ids) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-704 no-casts `packages/core/compute/ai/src/util/printer.ts:18:32`

This declares a widened `any` type (`export type Logger = (...data: any[]) => void;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-705 no-casts `packages/core/compute/ai/src/util/printer.ts:53:24`

This declares a widened `any` type (`private log(...data: any[]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-706 no-casts `packages/core/compute/ai/src/util/printer.ts:100:78`

This line casts to `any` with `as any` (`const input = Schema.decodeUnknownSync(tool.parametersSchema as any)(JSON.parse(content.input));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-707 no-casts `packages/core/compute/ai/src/util/printer.ts:130:78`

This line casts to `any` with `as any` (`const result = Schema.decodeUnknownSync(tool.successSchema as any)(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-708 no-casts `packages/core/compute/ai/src/util/printer.ts:184:60`

This declares a widened `any` type (`return Context.get(tool.annotations as Context.Context<any>, ToolFormatter);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-709 no-casts `packages/core/compute/ai/src/util/sse.ts:197:56`

This declares a widened `any` type (`export function readableStreamAsyncIterable<T>(stream: any): AsyncIterableIterator<T> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-710 no-casts `packages/core/compute/ai/src/util/sse.ts:243:16`

This declares a widened `any` type (`textDecoder: any; // TextDecoder found in browsers; not typed to avoid pulling in either "dom" or "node" types.`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-711 no-casts `packages/core/compute/ai/src/util/sse.ts:276:32`

This uses the non-null assertion `!` (`this.buffer.push(lines[0]!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-712 no-casts `packages/core/compute/ai/src/util/sse.ts:323:18`

This line casts to `any` with `as any` (`(bytes as any).constructor.name`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-713 no-casts `packages/core/compute/ai/src/util/tool-call-parsing.ts:27:33`

This line casts to `any` with `as any` (`const actual = (err.cause as any)?.issue?.actual;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-714 no-casts `packages/core/compute/assistant-toolkit/src/skills/agent/operations/get-context.ts:57:8`

This line casts to `any` with `as any` (`}) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-715 no-casts `packages/core/compute/assistant-toolkit/src/skills/database/operations/relation-delete.ts:18:26`

This line casts to `any` with `as any` (`db.remove(relation as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-716 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:139:47`

This declares a widened `any` type (`message.blocks.some((block: any) => block._tag === 'text' && block.text.trim().length > 0),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-717 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:196:27`

This declares a widened `any` type (`.filter((block: any) => block._tag === 'text')`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-718 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:197:24`

This declares a widened `any` type (`.map((block: any) => block.text)`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-719 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:83:102`

This uses the non-null assertion `!` (`const client = yield* HttpClient.HttpClient.pipe(Effect.map(withAuthorization(credential.apiKey!)));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-720 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:94:19`

This declares a widened `any` type (`const json: any = yield* response.json;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-721 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:95:50`

This line casts to `any` with `as any` (`const tasks = (json.data.team.issues.edges as any[]).map((edge: any) =>`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-722 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:95:71`

This declares a widened `any` type (`const tasks = (json.data.team.issues.edges as any[]).map((edge: any) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-723 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:35:29`

This line casts to `any` with `as any` (`if (!Ref.isRef((obj as any)[key])) {`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-724 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:38:39`

This line casts to `any` with `as any` (`const ref: Ref.Unknown = (obj as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-725 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:46:16`

This line casts to `any` with `as any` (`(obj as any)[key] = Ref.make(target);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-726 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:80:24`

This line casts to `any` with `as any` (`typeof (newObj as any)[key] !== 'string' &&`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-727 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:81:24`

This line casts to `any` with `as any` (`typeof (newObj as any)[key] !== 'number' &&`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-728 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:82:24`

This line casts to `any` with `as any` (`typeof (newObj as any)[key] !== 'boolean' &&`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-729 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:83:28`

This line casts to `any` with `as any` (`!Ref.isRef((newObj as any)[key])`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-730 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:88:17`

This line casts to `any` with `as any` (`(existing as any)[key] = (newObj as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-731 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:88:40`

This line casts to `any` with `as any` (`(existing as any)[key] = (newObj as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-732 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:98:26`

This line casts to `any` with `as any` (`delete (existing as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-733 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.test.ts:51:43`

This declares a widened `any` type (`const companies = entities.filter((e: any) => e.text === 'SpaceX' || e.text === 'Tesla');`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-734 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.test.ts:53:33`

This declares a widened `any` type (`companies.forEach((company: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-735 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:75:46`

This uses the non-null assertion `!` (`const lastTokenGroup = tokenGroups.at(-1)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-736 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:76:44`

This uses the non-null assertion `!` (`if (canBeCombined(lastTokenGroup.at(-1)!, token)) {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-737 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:88:24`

This uses the non-null assertion `!` (`entity: group.at(0)!.entity.split('-')[1],`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-738 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:90:23`

This uses the non-null assertion `!` (`index: group.at(0)!.index,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-739 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:91:22`

This uses the non-null assertion `!` (`word: group.at(0)!.word,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-740 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:92:23`

This uses the non-null assertion `!` (`start: group.at(0)!.start,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-741 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:93:22`

This uses the non-null assertion `!` (`end: group.at(-1)!.end,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-742 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:167:36`

This uses the non-null assertion `!` (`role: message.sender.role!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-743 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:172:36`

This uses the non-null assertion `!` (`role: message.sender.role!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-744 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:260:50`

This line casts to `any` with `as any` (`onPart: (part) => observer.onPart(part as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-745 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:389:32`

This declares a widened `any` type (`toolkit: Toolkit.WithHandler<any> | undefined,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-746 no-casts `packages/core/compute/assistant/src/session/AiContext.ts:140:55`

This uses the non-null assertion `!` (`await this._updateBindings(this.#bindingsQuery!.results);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-747 no-casts `packages/core/compute/assistant/src/session/AiContext.ts:346:54`

This uses the non-null assertion `!` (`EID.getEntityId(EID.tryParse(obj.uri)!) === EID.getEntityId(EID.tryParse(ref.uri)!))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-748 no-casts `packages/core/compute/assistant/src/session/AiContext.ts:346:98`

This uses the non-null assertion `!` (`EID.getEntityId(EID.tryParse(obj.uri)!) === EID.getEntityId(EID.tryParse(ref.uri)!))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-749 no-casts `packages/core/compute/assistant/src/session/Harness.ts:239:21`

This line uses the double-cast escape hatch `as unknown as T` (`return host.rpc as unknown as RpcClient.RpcClient<HarnessControlRpcs>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-750 no-casts `packages/core/compute/assistant/src/session/toolkit.ts:46:67`

This line casts to `any` with `as any` (`return OpaqueToolkit.make(mergedToolkit, combinedHandlerLayer as any) as OpaqueToolkit.OpaqueToolkit;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-751 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:18:43`

This declares a widened `any` type (`const decodeIn = (schema: Schema.Schema<any, any>, value: unknown): any[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-752 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:18:48`

This declares a widened `any` type (`const decodeIn = (schema: Schema.Schema<any, any>, value: unknown): any[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-753 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:18:71`

This declares a widened `any` type (`const decodeIn = (schema: Schema.Schema<any, any>, value: unknown): any[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-754 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:20:20`

This declares a widened `any` type (`const decoded: any = Schema.decodeUnknownSync(Schema.Struct(fields))({ in: value });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-755 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:75:94`

This declares a widened `any` type (`const makeHandler = (tool: Tool.Any): ((params: unknown) => Effect.Effect<unknown, any, any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-756 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:75:99`

This declares a widened `any` type (`const makeHandler = (tool: Tool.Any): ((params: unknown) => Effect.Effect<unknown, any, any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-757 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:76:84`

This declares a widened `any` type (`return Effect.fn(`toolFunctionHandler ${tool.name}`)(function* (input: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-758 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:83:66`

This line casts to `any` with `as any` (`const { result } = yield* (toolkitHandler.handle as any)(tool.name, input);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-759 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:92:111`

This line casts to `any` with `as any` (`Record.map(toolkit.tools, (tool, _name) => (Tool.isUserDefined(tool) ? makeHandler(tool) : null)) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-760 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:93:13`

This line casts to `any` with `as any` (`) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-761 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:151:29`

This uses the non-null assertion `!` (`return toolCache.get(fn)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-762 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:182:25`

This declares a widened `any` type (`schema: Schema.Schema<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-763 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:182:30`

This declares a widened `any` type (`schema: Schema.Schema<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-764 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:183:33`

This declares a widened `any` type (`): Record<string, Schema.Schema<any, any>> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-765 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:183:38`

This declares a widened `any` type (`): Record<string, Schema.Schema<any, any>> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-766 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:242:24`

This line casts to `any` with `as any` (`return typeof (value as any).tools === 'object' && typeof (value as any).handle === 'function';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-767 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:242:68`

This line casts to `any` with `as any` (`return typeof (value as any).tools === 'object' && typeof (value as any).handle === 'function';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-768 no-casts `packages/core/compute/assistant/src/util/execution-graph.ts:135:41`

This declares a widened `any` type (`private sortObjectsByCreated(objects: any[]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-769 no-casts `packages/core/compute/assistant/src/util/execution-graph.ts:138:47`

This declares a widened `any` type (`const createdObjects: { idx: number; obj: any }[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-770 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:24:39`

This declares a widened `any` type (`export type AsyncFunction = (...args: any) => Promise<InterpreterValue>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-771 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:56:53`

This declares a widened `any` type (`static createInvocationKey(name: string, ...args: any): string {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-772 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:124:11`

This declares a widened `any` type (`args: any[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-773 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:180:66`

This declares a widened `any` type (`return this.runFunction(ast.args, state, metadata, (...args: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-774 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:30:40`

This declares a widened `any` type (`async (binding: string, ...args: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-775 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:58:20`

This declares a widened `any` type (`let input: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-776 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:62:22`

This line casts to `any` with `as any` (`input = {} as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-777 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:70:23`

This line casts to `any` with `as any` (`return result as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-778 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:31:32`

This declares a widened `any` type (`invoke: (op: any, ...args: any[]) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-779 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:31:46`

This declares a widened `any` type (`invoke: (op: any, ...args: any[]) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-780 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:32:55`

This declares a widened `any` type (`const handler = handlers.find((h: any) => h.meta.key === op.meta.key);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-781 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:51:15`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-782 no-casts `packages/core/compute/compute-hyperformula/src/types/types.test.ts:56:33`

This declares a widened `any` type (`const cells: Record<string, any> = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-783 no-casts `packages/core/compute/compute-hyperformula/src/types/types.test.ts:57:43`

This declares a widened `any` type (`const setCell = (cell: string, value: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-784 no-casts `packages/core/compute/compute-hyperformula/src/types/types.ts:17:34`

This declares a widened `any` type (`export const isFormula = (value: any): value is string => typeof value === 'string' && value.charAt(0) === '=';`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-785 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:20:31`

This line casts to `any` with `as any` (`fetch: async () => response as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-786 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:60:19`

This line casts to `any` with `as any` (`expect((error as any)?.context?.cacheKey).toBe('abc');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-787 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:61:19`

This line casts to `any` with `as any` (`expect((error as any)?.context?.status).toBe(500);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-788 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:78:19`

This line casts to `any` with `as any` (`expect((error as any)?.context?.type).toBe('overloaded_error');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-789 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:79:19`

This line casts to `any` with `as any` (`expect((error as any)?.context?.status).toBe(529);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-790 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.ts:93:41`

This line casts to `any` with `as any` (`return send(request.body.body as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-791 no-casts `packages/core/compute/compute-runtime/src/functions-trace.ts:218:59`

This uses the non-null assertion `!` (`duration: isInProgress ? now - start.timestamp : end!.timestamp - start.timestamp,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-792 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:269:33`

This line casts to `any` with `as any` (`const process = 'pid-1' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-793 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:326:94`

This line casts to `any` with `as any` (`const resolvedA = yield* resolveWithScope(resolver.resolve(ServiceA, { process: 'p1' as any }));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-794 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:331:89`

This line casts to `any` with `as any` (`const exit = yield* resolveWithScope(resolver.resolve(ServiceB, { process: 'p1' as any })).pipe(Effect.exit);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-795 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:349:101`

This line casts to `any` with `as any` (`const resolvedB = yield* resolveWithScope(resolver.resolve(ServiceB, { space, process: 'p2' as any }));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-796 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:413:41`

This line casts to `any` with `as any` (`const process = 'sync-triggers' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-797 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:439:94`

This line casts to `any` with `as any` (`const conversation = 'echo://BBBBBBBBBBBBBBBBBBBBBBBBBB/01JTESTFEED0000000000000000' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-798 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:441:78`

This line casts to `any` with `as any` (`resolver.resolve(ServiceB, { space, conversation, process: 'agent' as any }),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-799 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:486:35`

This line casts to `any` with `as any` (`const process = 'p-multi' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-800 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:560:83`

This line casts to `any` with `as any` (`yield* resolveWithScope(resolver.resolve(ServiceC, { space, process: 'p1' as any }));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-801 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:561:83`

This line casts to `any` with `as any` (`yield* resolveWithScope(resolver.resolve(ServiceC, { space, process: 'p2' as any }));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-802 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:38:86`

This line casts to `any` with `as any` (`return ServiceResolver.make((tag, context) => this.#resolveService(tag, context) as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-803 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:55:22`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-804 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:55:27`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-805 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:67:22`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-806 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:67:27`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-807 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:163:23`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-808 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:163:28`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-809 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:186:22`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-810 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:186:27`

This declares a widened `any` type (`tag: Context.Tag<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-811 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:196:23`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-812 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:196:28`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-813 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:329:26`

This declares a widened `any` type (`#requires: Context.Tag<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-814 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:329:31`

This declares a widened `any` type (`#requires: Context.Tag<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-815 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:333:26`

This declares a widened `any` type (`#provides: Context.Tag<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-816 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:333:31`

This declares a widened `any` type (`#provides: Context.Tag<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-817 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:356:51`

This declares a widened `any` type (`#managedRuntimes: ManagedRuntime.ManagedRuntime<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-818 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:356:56`

This declares a widened `any` type (`#managedRuntimes: ManagedRuntime.ManagedRuntime<any, any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-819 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:417:31`

This declares a widened `any` type (`get provides(): Context.Tag<any, any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-820 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:417:36`

This declares a widened `any` type (`get provides(): Context.Tag<any, any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-821 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:421:31`

This declares a widened `any` type (`get requires(): Context.Tag<any, any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-822 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:421:36`

This declares a widened `any` type (`get requires(): Context.Tag<any, any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-823 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:511:50`

This declares a widened `any` type (`const provides = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-824 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:511:55`

This declares a widened `any` type (`const provides = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-825 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:533:23`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-826 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:533:28`

This declares a widened `any` type (`tags: Context.Tag<any, any>[],`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-827 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:567:38`

This declares a widened `any` type (`#layersNeededFor(tags: Context.Tag<any, any>[]): LayerSpec.LayerSpec[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-828 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:567:43`

This declares a widened `any` type (`#layersNeededFor(tags: Context.Tag<any, any>[]): LayerSpec.LayerSpec[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-829 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:617:105`

This line casts to `any` with `as any` (`const baseLayer: Layer.Layer<unknown, unknown, unknown> = Layer.syncContext(() => this.#services) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-830 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:664:55`

This declares a widened `any` type (`const providesByKey = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-831 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:664:60`

This declares a widened `any` type (`const providesByKey = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-832 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:671:57`

This declares a widened `any` type (`const requireTagByKey = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-833 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:671:62`

This declares a widened `any` type (`const requireTagByKey = new Map<string, Context.Tag<any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-834 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:698:28`

This uses the non-null assertion `!` (`const a = layers[i]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-835 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:699:28`

This uses the non-null assertion `!` (`const b = layers[j]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-836 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:700:59`

This declares a widened `any` type (`const depends = a.requires.some((req: Context.Tag<any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-837 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:700:64`

This declares a widened `any` type (`const depends = a.requires.some((req: Context.Tag<any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-838 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:701:46`

This declares a widened `any` type (`b.provides.some((prov: Context.Tag<any, any>) => prov.key === req.key),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-839 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:701:51`

This declares a widened `any` type (`b.provides.some((prov: Context.Tag<any, any>) => prov.key === req.key),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-840 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:704:17`

This uses the non-null assertion `!` (`adj[j]!.push(i);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-841 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:728:32`

This uses the non-null assertion `!` (`for (const v of adj[next]!) {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-842 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:733:51`

This uses the non-null assertion `!` (`this.#layers = sorted.map((idx) => layers[idx]!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-843 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:19:48`

This declares a widened `any` type (`const isPath = (value: unknown): value is Path<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-844 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:44:51`

This declares a widened `any` type (`const makeContext = (segments: string[]): Context<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-845 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:45:22`

This declares a widened `any` type (`const target: Path<any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-846 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:54:5`

This line casts to `any` with `as any` (`} as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-847 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:65:17`

This declares a widened `any` type (`}) as Context<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-848 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:125:80`

This declares a widened `any` type (`export class ProcessHandleImpl<I, O, R> implements ProcessManager.Handle<I, O, any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-849 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:131:37`

This declares a widened `any` type (`readonly rpc: RpcClient.RpcClient<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-850 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:154:51`

This declares a widened `any` type (`readonly #callbacks: Process.Callbacks<I, O, R, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-851 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:171:43`

This declares a widened `any` type (`callbacks: Process.Callbacks<I, O, R, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-852 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:182:30`

This declares a widened `any` type (`rpc: RpcClient.RpcClient<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-853 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:316:45`

This declares a widened `any` type (`hydrate(definition: Process.Process<I, O, any, any>): Effect.Effect<ProcessManager.Handle<I, O, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-854 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:316:50`

This declares a widened `any` type (`hydrate(definition: Process.Process<I, O, any, any>): Effect.Effect<ProcessManager.Handle<I, O, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-855 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:316:99`

This declares a widened `any` type (`hydrate(definition: Process.Process<I, O, any, any>): Effect.Effect<ProcessManager.Handle<I, O, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-856 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:369:70`

This declares a widened `any` type (`redeliver(event: PersistedEvent, definition: Process.Process<I, O, any, any>): Effect.Effect<void> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-857 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:369:75`

This declares a widened `any` type (`redeliver(event: PersistedEvent, definition: Process.Process<I, O, any, any>): Effect.Effect<void> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-858 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:380:44`

This line uses the double-cast escape hatch `as unknown as T` (`const defWithSchema = definition as unknown as { input: Schema.Schema<I, unknown, never> };`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-859 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:198:63`

This uses the non-null assertion `!` (`yield* Deferred.succeed(SlowChildGate.alarmStarted!, undefined);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-860 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:200:60`

This uses the non-null assertion `!` (`yield* Deferred.await(SlowChildGate.alarmResume!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-861 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:201:62`

This uses the non-null assertion `!` (`yield* Ref.set(SlowChildGate.alarmHandlerFinished!, true);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-862 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:513:23`

This uses the non-null assertion `!` (`expect(tree[0]!.pid).toEqual(handle.pid);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-863 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:514:23`

This uses the non-null assertion `!` (`expect(tree[0]!.parentPid).toBeNull();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-864 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:515:23`

This uses the non-null assertion `!` (`expect(tree[0]!.state).toEqual(Process.State.HYBERNATING);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-865 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:873:70`

This line casts to `any` with `as any` (`input.override !== undefined ? { spaceId: input.override as any } : undefined,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-866 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:906:28`

This line casts to `any` with `as any` (`return dbService as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-867 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:995:92`

This line casts to `any` with `as any` (`const conversation = 'echo://BBBBBBBBBBBBBBBBBBBBBBBBBB/01JTESTCONVERSATION00000000' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-868 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:1460:56`

This uses the non-null assertion `!` (`yield* Deferred.succeed(SlowChildGate.alarmResume!, undefined);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-869 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:60:27`

This declares a widened `any` type (`rpcs: RpcGroup.RpcGroup<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-870 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:61:32`

This declares a widened `any` type (`rpcHandlers: Context.Context<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-871 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:63:38`

This declares a widened `any` type (`): Effect.Effect<RpcClient.RpcClient<any>> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-872 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:72:45`

This declares a widened `any` type (`const EMPTY_RPC_CLIENT: RpcClient.RpcClient<any> = Effect.runSync(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-873 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:76:21`

This line uses the double-cast escape hatch `as unknown as T` (`RpcGroup.make() as unknown as RpcGroup.RpcGroup<any>,`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-874 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:76:53`

This declares a widened `any` type (`RpcGroup.make() as unknown as RpcGroup.RpcGroup<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-875 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:77:40`

This declares a widened `any` type (`Context.empty() as Context.Context<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-876 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:165:56`

This declares a widened `any` type (`hydrate(definition: Process.Process<_Input, _Output, any, any>): Effect.Effect<Handle<_Input, _Output, _Rpcs>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-877 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:165:61`

This declares a widened `any` type (`hydrate(definition: Process.Process<_Input, _Output, any, any>): Effect.Effect<Handle<_Input, _Output, _Rpcs>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-878 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:173:28`

This declares a widened `any` type (`export type Any = Handle<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-879 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:173:33`

This declares a widened `any` type (`export type Any = Handle<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-880 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:173:38`

This declares a widened `any` type (`export type Any = Handle<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-881 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:280:39`

This declares a widened `any` type (`definition: Process.Process<I, O, any, Rpcs>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-882 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:345:75`

This declares a widened `any` type (`readonly #handles = new Map<Process.ID, ProcessHandle.ProcessHandleImpl<any, any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-883 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:345:80`

This declares a widened `any` type (`readonly #handles = new Map<Process.ID, ProcessHandle.ProcessHandleImpl<any, any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-884 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:345:85`

This declares a widened `any` type (`readonly #handles = new Map<Process.ID, ProcessHandle.ProcessHandleImpl<any, any, any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-885 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:437:65`

This declares a widened `any` type (`static #isNonTerminal(handle: ProcessHandle.ProcessHandleImpl<any, any, any>): boolean {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-886 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:437:70`

This declares a widened `any` type (`static #isNonTerminal(handle: ProcessHandle.ProcessHandleImpl<any, any, any>): boolean {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-887 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:437:75`

This declares a widened `any` type (`static #isNonTerminal(handle: ProcessHandle.ProcessHandleImpl<any, any, any>): boolean {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-888 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:491:39`

This declares a widened `any` type (`definition: Process.Process<I, O, any, _Rpcs>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-889 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:524:60`

This declares a widened `any` type (`let handleRef: ProcessHandle.ProcessHandleImpl<I, O, any> | null = null;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-890 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:603:77`

This declares a widened `any` type (`const externalServices = definition.services.filter((tag: Context.Tag<any, any>) => !builtinTagKeys.has(tag.key));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-891 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:603:82`

This declares a widened `any` type (`const externalServices = definition.services.filter((tag: Context.Tag<any, any>) => !builtinTagKeys.has(tag.key));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-892 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:616:102`

This declares a widened `any` type (`const callbacks = yield* definition.create(ctx).pipe(Effect.provide(fullCtx as Context.Context<any>));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-893 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:649:33`

This line uses the double-cast escape hatch `as unknown as T` (`const defRaw = definition as unknown as { input: Schema.Schema<I, any, never> };`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-894 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:649:73`

This declares a widened `any` type (`const defRaw = definition as unknown as { input: Schema.Schema<I, any, never> };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-895 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:665:64`

This declares a widened `any` type (`const handle = new ProcessHandle.ProcessHandleImpl<I, O, any>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-896 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:714:21`

This line uses the double-cast escape hatch `as unknown as T` (`return handle as unknown as Handle<I, O, _Rpcs>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-897 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:723:33`

This declares a widened `any` type (`definition: Process.Process<any, any, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-898 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:723:38`

This declares a widened `any` type (`definition: Process.Process<any, any, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-899 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:723:43`

This declares a widened `any` type (`definition: Process.Process<any, any, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-900 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:723:48`

This declares a widened `any` type (`definition: Process.Process<any, any, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-901 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:724:52`

This declares a widened `any` type (`): Effect.Effect<ProcessHandle.ProcessHandleImpl<any, any, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-902 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:724:57`

This declares a widened `any` type (`): Effect.Effect<ProcessHandle.ProcessHandleImpl<any, any, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-903 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:724:62`

This declares a widened `any` type (`): Effect.Effect<ProcessHandle.ProcessHandleImpl<any, any, any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-904 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:732:75`

This declares a widened `any` type (`const outputQueue = yield* Queue.unbounded<ProcessHandle.OutputItem<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-905 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:749:54`

This declares a widened `any` type (`let handleRef: ProcessHandle.ProcessHandleImpl<any, any, any> | null = null;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-906 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:749:59`

This declares a widened `any` type (`let handleRef: ProcessHandle.ProcessHandleImpl<any, any, any> | null = null;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-907 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:749:64`

This declares a widened `any` type (`let handleRef: ProcessHandle.ProcessHandleImpl<any, any, any> | null = null;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-908 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:756:41`

This declares a widened `any` type (`const ctx: Process.ProcessContext<any, any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-909 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:756:46`

This declares a widened `any` type (`const ctx: Process.ProcessContext<any, any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-910 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:765:32`

This declares a widened `any` type (`submitOutput: (output: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-911 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:813:77`

This declares a widened `any` type (`const externalServices = definition.services.filter((tag: Context.Tag<any, any>) => !builtinTagKeys.has(tag.key));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-912 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:813:82`

This declares a widened `any` type (`const externalServices = definition.services.filter((tag: Context.Tag<any, any>) => !builtinTagKeys.has(tag.key));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-913 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:825:102`

This declares a widened `any` type (`const callbacks = yield* definition.create(ctx).pipe(Effect.provide(fullCtx as Context.Context<any>));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-914 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:852:33`

This line uses the double-cast escape hatch `as unknown as T` (`const defRaw = definition as unknown as { input: Schema.Schema<any, any, never> };`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-915 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:852:70`

This declares a widened `any` type (`const defRaw = definition as unknown as { input: Schema.Schema<any, any, never> };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-916 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:852:75`

This declares a widened `any` type (`const defRaw = definition as unknown as { input: Schema.Schema<any, any, never> };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-917 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:853:35`

This declares a widened `any` type (`const encodeInput = (input: any): Effect.Effect<unknown> => Schema.encode(defRaw.input)(input).pipe(Effect.orDie);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-918 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:857:58`

This declares a widened `any` type (`const handle = new ProcessHandle.ProcessHandleImpl<any, any, any>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-919 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:857:63`

This declares a widened `any` type (`const handle = new ProcessHandle.ProcessHandleImpl<any, any, any>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-920 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:857:68`

This declares a widened `any` type (`const handle = new ProcessHandle.ProcessHandleImpl<any, any, any>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-921 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:908:39`

This declares a widened `any` type (`definition: Process.Process<I, O, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-922 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:908:44`

This declares a widened `any` type (`definition: Process.Process<I, O, any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-923 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:914:25`

This line uses the double-cast escape hatch `as unknown as T` (`return existing as unknown as Handle<I, O, Rpcs>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-924 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:939:21`

This line uses the double-cast escape hatch `as unknown as T` (`return handle as unknown as Handle<I, O, Rpcs>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-925 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:951:21`

This line uses the double-cast escape hatch `as unknown as T` (`return handle as unknown as Handle<I, O, Rpcs>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-926 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1005:59`

This declares a widened `any` type (`this.#hydrateFromDefinition<unknown, unknown, any>(record.id, definition),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-927 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1030:51`

This declares a widened `any` type (`class DormantHandle<I, O> implements Handle<I, O, any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-928 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1040:37`

This declares a widened `any` type (`readonly rpc: RpcClient.RpcClient<any> = EMPTY_RPC_CLIENT;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-929 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1041:59`

This declares a widened `any` type (`readonly #rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-930 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1041:64`

This declares a widened `any` type (`readonly #rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-931 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1041:100`

This declares a widened `any` type (`readonly #rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-932 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1045:51`

This declares a widened `any` type (`rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-933 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1045:56`

This declares a widened `any` type (`rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-934 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1045:92`

This declares a widened `any` type (`rehydrate: (definition: Process.Process<I, O, any, any>) => Effect.Effect<Handle<I, O, any>>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-935 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1068:48`

This declares a widened `any` type (`hydrate = (definition: Process.Process<I, O, any, any>): Effect.Effect<Handle<I, O, any>> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-936 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1068:53`

This declares a widened `any` type (`hydrate = (definition: Process.Process<I, O, any, any>): Effect.Effect<Handle<I, O, any>> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-937 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:1068:87`

This declares a widened `any` type (`hydrate = (definition: Process.Process<I, O, any, any>): Effect.Effect<Handle<I, O, any>> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-938 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:62:60`

This declares a widened `any` type (`const fiberFromProcess = <T>(handle: ProcessManager.Handle<any, T, never>): Effect.Effect<OperationFiber<T>> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-939 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:121:52`

This declares a widened `any` type (`const pendingFibers = new Set<Fiber.RuntimeFiber<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-940 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:122:57`

This declares a widened `any` type (`const fiberCache = new Map<Process.ID, OperationFiber<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-941 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:200:49`

This declares a widened `any` type (`const handle = yield* opts.manager.attach<any, T>(pid);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-942 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:208:14`

This declares a widened `any` type (`...args: any[]`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-943 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:257:14`

This declares a widened `any` type (`...args: any[]`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-944 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:327:14`

This declares a widened `any` type (`...args: any[]`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-945 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:343:32`

This line casts to `any` with `as any` (`invoke(op, input, options) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-946 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:93:27`

This declares a widened `any` type (`} catch (error: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-947 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:114:21`

This declares a widened `any` type (`let result: any = await func.handler(dataWithDecodedRefs);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-948 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:140:27`

This declares a widened `any` type (`} catch (error: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-949 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:352:70`

This line casts to `any` with `as any` (`if (typeof value === 'object' && value !== null && typeof (value as any)['/'] === 'string') {`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-950 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:354:44`

This line casts to `any` with `as any` (`return refFromEncodedReference(value as any, resolver);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-951 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:365:59`

This line casts to `any` with `as any` (`const result: Record<string, unknown> = { ...(value as any) };`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-952 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:369:65`

This line casts to `any` with `as any` (`result[key] = decodeRefsFromSchema(prop.type, (result as any)[key], db);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-953 no-casts `packages/core/compute/compute-runtime/src/RemoteOperationInvoker.test.ts:16:96`

This line uses the double-cast escape hatch `as unknown as T` (`invoke: <I, O>(_ctx: DxosContext, _deployedId: string, input: I) => Effect.succeed(input as unknown as O),`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-954 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.test.ts:16:27`

This line casts to `any` with `as any` (`resolve: (tag) => ((tag as any) === MyTag ? Option.some({ value: 'test' } as any) : Option.none()),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-955 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.test.ts:16:77`

This line casts to `any` with `as any` (`resolve: (tag) => ((tag as any) === MyTag ? Option.some({ value: 'test' } as any) : Option.none()),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-956 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:14:37`

This declares a widened `any` type (`resolve: <T extends Context.Tag<any, any>>(tag: T) => Option.Option<Context.Tag.Service<T>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-957 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:14:42`

This declares a widened `any` type (`resolve: <T extends Context.Tag<any, any>>(tag: T) => Option.Option<Context.Tag.Service<T>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-958 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:28:42`

This declares a widened `any` type (`static resolve: <T extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-959 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:28:47`

This declares a widened `any` type (`static resolve: <T extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-960 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:37:32`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-961 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:37:37`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-962 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:37:58`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-963 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:37:63`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-964 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:47:20`

This line casts to `any` with `as any` (`(Function.flow as any)(...tags.map((tag) => Effect.provideServiceEffect(tag, ServiceRegistry.resolve(tag))));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-965 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:50:32`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-966 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:50:37`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-967 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:50:58`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-968 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:50:63`

This declares a widened `any` type (`<Tags extends [Context.Tag<any, any>, ...Context.Tag<any, any>[]]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-969 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:60:20`

This line casts to `any` with `as any` (`(Function.flow as any)(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-970 no-casts `packages/core/compute/compute-runtime/src/storage-service-layer.ts:25:35`

This declares a widened `any` type (`get: <S extends Schema.Schema<any, string, any>>(schema: S, key: string) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-971 no-casts `packages/core/compute/compute-runtime/src/storage-service-layer.ts:25:48`

This declares a widened `any` type (`get: <S extends Schema.Schema<any, string, any>>(schema: S, key: string) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-972 no-casts `packages/core/compute/compute-runtime/src/storage-service-layer.ts:35:35`

This declares a widened `any` type (`set: <S extends Schema.Schema<any, string, any>>(schema: S, key: string, value: Schema.Schema.Type<S>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-973 no-casts `packages/core/compute/compute-runtime/src/storage-service-layer.ts:35:48`

This declares a widened `any` type (`set: <S extends Schema.Schema<any, string, any>>(schema: S, key: string, value: Schema.Schema.Type<S>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-974 no-casts `packages/core/compute/compute-runtime/src/testing/layer.ts:78:41`

This uses the non-null assertion `!` (`yield* Effect.promise(() => db!.flush());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-975 no-casts `packages/core/compute/compute-runtime/src/trace-buffer.ts:34:23`

This uses the non-null assertion `!` (`if (buf[index]!.id === id) {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-976 no-casts `packages/core/compute/compute-runtime/src/trace-buffer.ts:42:33`

This uses the non-null assertion `!` (`const item = buf[read]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-977 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:8:102`

This declares a widened `any` type (`export const createInvocationPayload = (trigger: Trigger.Trigger, event: TriggerEvent.TriggerEvent): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-978 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:13:18`

This declares a widened `any` type (`const payload: any = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-979 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:21:28`

This declares a widened `any` type (`let valueSubstitution: any = propertyPath.startsWith('trigger')`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-980 no-casts `packages/core/compute/compute-runtime/src/triggers/jsonata.test.ts:99:47`

This uses the non-null assertion `!` (`const expression = jsonata(trigger.input!.value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-981 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:58:26`

This line casts to `any` with `as any` (`return dbService as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-982 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:402:39`

This line casts to `any` with `as any` (`runnable: Ref.make(badFn) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-983 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:983:113`

This uses the non-null assertion `!` (`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-984 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1010:113`

This uses the non-null assertion `!` (`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-985 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1043:113`

This uses the non-null assertion `!` (`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-986 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1074:116`

This uses the non-null assertion `!` (`spec: Trigger.specSubscription(Query.select(Filter.type(Task.Task)).from(Scope.feed(Feed.getFeedUri(feed)!))),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-987 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1167:39`

This line casts to `any` with `as any` (`runnable: Ref.make(badFn) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-988 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.ts:923:93`

This declares a widened `any` type (`private _prepareInputData = (trigger: Trigger.Trigger, event: TriggerEvent.TriggerEvent): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-989 no-casts `packages/core/compute/compute/src/LayerSpec.ts:25:43`

This declares a widened `any` type (`readonly requires: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-990 no-casts `packages/core/compute/compute/src/LayerSpec.ts:25:48`

This declares a widened `any` type (`readonly requires: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-991 no-casts `packages/core/compute/compute/src/LayerSpec.ts:26:43`

This declares a widened `any` type (`readonly provides: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-992 no-casts `packages/core/compute/compute/src/LayerSpec.ts:26:48`

This declares a widened `any` type (`readonly provides: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-993 no-casts `packages/core/compute/compute/src/LayerSpec.ts:67:43`

This declares a widened `any` type (`readonly requires: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-994 no-casts `packages/core/compute/compute/src/LayerSpec.ts:67:48`

This declares a widened `any` type (`readonly requires: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-995 no-casts `packages/core/compute/compute/src/LayerSpec.ts:68:43`

This declares a widened `any` type (`readonly provides: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-996 no-casts `packages/core/compute/compute/src/LayerSpec.ts:68:48`

This declares a widened `any` type (`readonly provides: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-997 no-casts `packages/core/compute/compute/src/LayerSpec.ts:89:16`

This line casts to `any` with `as any` (`make: make as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-998 no-casts `packages/core/compute/compute/src/Operation.ts:27:36`

This declares a widened `any` type (`type Schema<T> = Schema$.Schema<T, any, never>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-999 no-casts `packages/core/compute/compute/src/Operation.ts:36:39`

This declares a widened `any` type (`export interface Definition<I, O, S = any> extends Pipeable.Pipeable, Definition.Variance<I, O, S> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1000 no-casts `packages/core/compute/compute/src/Operation.ts:91:43`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1001 no-casts `packages/core/compute/compute/src/Operation.ts:91:48`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1002 no-casts `packages/core/compute/compute/src/Operation.ts:109:32`

This declares a widened `any` type (`export type Any = Definition<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1003 no-casts `packages/core/compute/compute/src/Operation.ts:109:37`

This declares a widened `any` type (`export type Any = Definition<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1004 no-casts `packages/core/compute/compute/src/Operation.ts:109:42`

This declares a widened `any` type (`export type Any = Definition<any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1005 no-casts `packages/core/compute/compute/src/Operation.ts:128:67`

This declares a widened `any` type (`T extends Variance<infer I, infer O, infer S> ? Handler<I, O, any, S> : never;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1006 no-casts `packages/core/compute/compute/src/Operation.ts:168:27`

This declares a widened `any` type (`<Def extends Definition<any, any>>(load: () => Promise<{ default: WithHandler<Def> }>): (op: Def) => LazyHandler<Def>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1007 no-casts `packages/core/compute/compute/src/Operation.ts:168:32`

This declares a widened `any` type (`<Def extends Definition<any, any>>(load: () => Promise<{ default: WithHandler<Def> }>): (op: Def) => LazyHandler<Def>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1008 no-casts `packages/core/compute/compute/src/Operation.ts:169:27`

This declares a widened `any` type (`<Def extends Definition<any, any>>(op: Def, load: () => Promise<{ default: WithHandler<Def> }>): LazyHandler<Def>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1009 no-casts `packages/core/compute/compute/src/Operation.ts:169:32`

This declares a widened `any` type (`<Def extends Definition<any, any>>(op: Def, load: () => Promise<{ default: WithHandler<Def> }>): LazyHandler<Def>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1010 no-casts `packages/core/compute/compute/src/Operation.ts:170:30`

This declares a widened `any` type (`} = (<Def extends Definition<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1011 no-casts `packages/core/compute/compute/src/Operation.ts:170:35`

This declares a widened `any` type (`} = (<Def extends Definition<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1012 no-casts `packages/core/compute/compute/src/Operation.ts:182:4`

This line casts to `any` with `as any` (`}) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1013 no-casts `packages/core/compute/compute/src/Operation.ts:213:69`

This declares a widened `any` type (`export const make = <const P extends Types.NoExcessProperties<Props<any, any>, P>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1014 no-casts `packages/core/compute/compute/src/Operation.ts:213:74`

This declares a widened `any` type (`export const make = <const P extends Types.NoExcessProperties<Props<any, any>, P>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1015 no-casts `packages/core/compute/compute/src/Operation.ts:230:5`

This line casts to `any` with `as any` (`} as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1016 no-casts `packages/core/compute/compute/src/Operation.ts:260:27`

This declares a widened `any` type (`<Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1017 no-casts `packages/core/compute/compute/src/Operation.ts:260:32`

This declares a widened `any` type (`<Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1018 no-casts `packages/core/compute/compute/src/Operation.ts:263:27`

This declares a widened `any` type (`<Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1019 no-casts `packages/core/compute/compute/src/Operation.ts:263:32`

This declares a widened `any` type (`<Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1020 no-casts `packages/core/compute/compute/src/Operation.ts:267:29`

This declares a widened `any` type (`} = <Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1021 no-casts `packages/core/compute/compute/src/Operation.ts:267:34`

This declares a widened `any` type (`} = <Def extends Definition<any, any>, E = never>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1022 no-casts `packages/core/compute/compute/src/Operation.ts:282:9`

This line casts to `any` with `as any` (`})) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1023 no-casts `packages/core/compute/compute/src/Operation.ts:290:5`

This line casts to `any` with `as any` (`} as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1024 no-casts `packages/core/compute/compute/src/Operation.ts:797:56`

This declares a widened `any` type (`invoke: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1025 no-casts `packages/core/compute/compute/src/Operation.ts:798:76`

This line casts to `any` with `as any` (`service.invoke(op, input, { ...options, ...invocationOptions })) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1026 no-casts `packages/core/compute/compute/src/Operation.ts:799:63`

This declares a widened `any` type (`invokePromise: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1027 no-casts `packages/core/compute/compute/src/Operation.ts:800:83`

This line casts to `any` with `as any` (`service.invokePromise(op, input, { ...options, ...invocationOptions })) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1028 no-casts `packages/core/compute/compute/src/Operation.ts:801:58`

This declares a widened `any` type (`schedule: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1029 no-casts `packages/core/compute/compute/src/Operation.ts:802:78`

This line casts to `any` with `as any` (`service.schedule(op, input, { ...options, ...invocationOptions })) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1030 no-casts `packages/core/compute/compute/src/Operation.ts:844:25`

This line casts to `any` with `as any` (`source: from.source as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1031 no-casts `packages/core/compute/compute/src/Process.ts:222:43`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1032 no-casts `packages/core/compute/compute/src/Process.ts:222:48`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1033 no-casts `packages/core/compute/compute/src/Process.ts:229:36`

This declares a widened `any` type (`readonly rpcs: RpcGroup.RpcGroup<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1034 no-casts `packages/core/compute/compute/src/Process.ts:236:62`

This declares a widened `any` type (`): Effect.Effect<Callbacks<_Input, _Output, _Requirements, any>, never, _Requirements | BaseServices | Scope.Scope>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1035 no-casts `packages/core/compute/compute/src/Process.ts:255:29`

This declares a widened `any` type (`export type Any = Process<any, any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1036 no-casts `packages/core/compute/compute/src/Process.ts:255:34`

This declares a widened `any` type (`export type Any = Process<any, any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1037 no-casts `packages/core/compute/compute/src/Process.ts:255:39`

This declares a widened `any` type (`export type Any = Process<any, any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1038 no-casts `packages/core/compute/compute/src/Process.ts:255:44`

This declares a widened `any` type (`export type Any = Process<any, any, any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1039 no-casts `packages/core/compute/compute/src/Process.ts:266:43`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1040 no-casts `packages/core/compute/compute/src/Process.ts:266:48`

This declares a widened `any` type (`readonly services: readonly Context.Tag<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1041 no-casts `packages/core/compute/compute/src/Process.ts:267:37`

This declares a widened `any` type (`readonly rpcs?: RpcGroup.RpcGroup<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1042 no-casts `packages/core/compute/compute/src/Process.ts:294:25`

This line casts to `any` with `as any` (`[ProcessTypeId]: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1043 no-casts `packages/core/compute/compute/src/Process.ts:317:20`

This declares a widened `any` type (`): Context.Context<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1044 no-casts `packages/core/compute/compute/src/Process.ts:323:60`

This declares a widened `any` type (`return provided ?? (Context.empty() as Context.Context<any>);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1045 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:29:35`

This declares a widened `any` type (`resolve<Tag extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1046 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:29:40`

This declares a widened `any` type (`resolve<Tag extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1047 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:41:28`

This declares a widened `any` type (`<Tag extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1048 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:41:33`

This declares a widened `any` type (`<Tag extends Context.Tag<any, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1049 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:47:68`

This declares a widened `any` type (`export const resolveAll = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1050 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:47:73`

This declares a widened `any` type (`export const resolveAll = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1051 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:93:20`

This line casts to `any` with `as any` (`return service as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1052 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:129:74`

This declares a widened `any` type (`export const fromRequirements = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1053 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:129:79`

This declares a widened `any` type (`export const fromRequirements = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1054 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:132:50`

This declares a widened `any` type (`Effect.contextWith((parentCtx: Context.Context<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1055 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:152:75`

This declares a widened `any` type (`export const layerRequirements = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1056 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:152:80`

This declares a widened `any` type (`export const layerRequirements = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1057 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:171:65`

This declares a widened `any` type (`export const provide = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1058 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:171:70`

This declares a widened `any` type (`export const provide = <const Tags extends readonly Context.Tag<any, any>[]>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1059 no-casts `packages/core/compute/compute/src/StorageService.ts:15:31`

This declares a widened `any` type (`get<S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1060 no-casts `packages/core/compute/compute/src/StorageService.ts:15:44`

This declares a widened `any` type (`get<S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1061 no-casts `packages/core/compute/compute/src/StorageService.ts:21:31`

This declares a widened `any` type (`set<S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1062 no-casts `packages/core/compute/compute/src/StorageService.ts:21:44`

This declares a widened `any` type (`set<S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1063 no-casts `packages/core/compute/compute/src/StorageService.ts:64:46`

This declares a widened `any` type (`export const cell = <S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1064 no-casts `packages/core/compute/compute/src/StorageService.ts:64:59`

This declares a widened `any` type (`export const cell = <S extends Schema.Schema<any, string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1065 no-casts `packages/core/compute/compute/src/StorageService.ts:73:19`

This declares a widened `any` type (`pipe(...args: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1066 no-casts `packages/core/compute/compute/src/StorageService.ts:100:21`

This declares a widened `any` type (`pipe(...args: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1067 no-casts `packages/core/compute/compute/src/Trace.test.ts:107:36`

This line casts to `any` with `as any` (`expect((decoded.events[0].data as any).progress.key).toBe('k1');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1068 no-casts `packages/core/compute/compute/src/Trace.ts:48:37`

This declares a widened `any` type (`readonly schema: Schema.Schema<T, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1069 no-casts `packages/core/compute/compute/src/Trace.ts:54:36`

This declares a widened `any` type (`opts: { schema: Schema.Schema<T, any>; isEphemeral: boolean },`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1070 no-casts `packages/core/compute/compute/src/Trace.ts:66:45`

This declares a widened `any` type (`export type PayloadType<E extends EventType<any>> = E extends EventType<infer T> ? T : never;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1071 no-casts `packages/core/compute/compute/src/types/Template.test.ts:51:28`

This line casts to `any` with `as any` (`expect(decode(kind as any)).toBe(kind);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1072 no-casts `packages/core/compute/compute/src/types/Template.test.ts:56:69`

This line casts to `any` with `as any` (`expect(() => Schema.decodeSync(Template.InputKind)('nonsense' as any)).toThrow();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1073 no-casts `packages/core/compute/compute/src/types/Template.test.ts:100:49`

This declares a widened `any` type (`const handlersByKey: Record<string, (input: any) => Effect.Effect<any, any, any>> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1074 no-casts `packages/core/compute/compute/src/types/Template.test.ts:100:71`

This declares a widened `any` type (`const handlersByKey: Record<string, (input: any) => Effect.Effect<any, any, any>> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1075 no-casts `packages/core/compute/compute/src/types/Template.test.ts:100:76`

This declares a widened `any` type (`const handlersByKey: Record<string, (input: any) => Effect.Effect<any, any, any>> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1076 no-casts `packages/core/compute/compute/src/types/Template.test.ts:100:81`

This declares a widened `any` type (`const handlersByKey: Record<string, (input: any) => Effect.Effect<any, any, any>> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1077 no-casts `packages/core/compute/compute/src/types/Template.test.ts:105:20`

This declares a widened `any` type (`invoke: (op: any, input: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1078 no-casts `packages/core/compute/compute/src/types/Template.test.ts:105:32`

This declares a widened `any` type (`invoke: (op: any, input: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1079 no-casts `packages/core/compute/compute/src/types/Template.test.ts:114:7`

This line uses the double-cast escape hatch `as unknown as T` (`} as unknown as Operation.OperationService);`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1080 no-casts `packages/core/compute/compute/src/types/Template.ts:111:66`

This line casts to `any` with `as any` (`const result = yield* Operation.invoke(fn, undefined as any).pipe(Effect.orDie);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1081 no-casts `packages/core/compute/compute/src/types/Trigger.ts:83:22`

This declares a widened `any` type (`query: Query.Query<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1082 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.test.ts:42:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1083 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:193:75`

This declares a widened `any` type (`private readonly _computeCache = new Map<string, ComputeResult<ValueBag<any>>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1084 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:241:32`

This uses the non-null assertion `!` (`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1085 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:251:32`

This uses the non-null assertion `!` (`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1086 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:261:32`

This uses the non-null assertion `!` (`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1087 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:269:62`

This declares a widened `any` type (`setOutputs(nodeId: string, outputs: ComputeResult<ValueBag<any>>): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1088 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:273:60`

This declares a widened `any` type (`setInputs(nodeId: string, inputs: ComputeResult<ValueBag<any>>): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1089 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:288:27`

This uses the non-null assertion `!` (`this._topology!.nodes.find((node) => node.id === input.sources[0].nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1090 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:298:50`

This uses the non-null assertion `!` (`const sourceNode = this._topology!.nodes.find((node) => node.id === source.nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1091 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:338:57`

This declares a widened `any` type (`computeInputs(nodeId: string): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1092 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:396:58`

This declares a widened `any` type (`computeOutputs(nodeId: string): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1093 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:400:61`

This uses the non-null assertion `!` (`const result = yield* this._computeCache.get(nodeId)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1094 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:408:34`

This uses the non-null assertion `!` (`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1095 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:467:29`

This declares a widened `any` type (`const res: ValueBag<any>['values'] = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1096 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:508:33`

This uses the non-null assertion `!` (`recurse(this._topology!.nodes.find((node) => node.id === bound.nodeId) ?? failedInvariant());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1097 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:514:32`

This uses the non-null assertion `!` (`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1098 no-casts `packages/core/compute/conductor/src/nodes/gpt/gpt.ts:134:69`

This declares a widened `any` type (`const tokenPubSub = yield* PubSub.unbounded<Response.StreamPart<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1099 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:43:32`

This declares a widened `any` type (`export const isFalsy = (value: any) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1100 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:52:33`

This declares a widened `any` type (`export const isTruthy = (value: any) => !isFalsy(value);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1101 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:119:77`

This uses the non-null assertion `!` (`exec: (_, node) => Effect.succeed(ValueBag.make({ [DEFAULT_OUTPUT]: node!.value })),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1102 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:226:48`

This declares a widened `any` type (`const mappedItems = items.map((item: any) => ({`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1103 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:252:65`

This line casts to `any` with `as any` (`const { id: _id, '@type': _type, ...rest } = item as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1104 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:342:32`

This declares a widened `any` type (`Effect.gen(function* (): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1105 no-casts `packages/core/compute/conductor/src/nodes/template/json.ts:56:30`

This line casts to `any` with `as any` (`return props[p1] as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1106 no-casts `packages/core/compute/conductor/src/sequence/logger.ts:15:52`

This declares a widened `any` type (`type ConsoleLogSignature = (message: string, arg?: any) => void;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1107 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:32:73`

This uses the non-null assertion `!` (`nodeResolver: async (node: ComputeNode) => this._nodes.get(node.type!)!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1108 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:32:75`

This uses the non-null assertion `!` (`nodeResolver: async (node: ComputeNode) => this._nodes.get(node.type!)!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1109 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:60:36`

This declares a widened `any` type (`runGraph<T extends ValueRecord = any>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1110 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:62:21`

This declares a widened `any` type (`input: ValueBag<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1111 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:75:21`

This declares a widened `any` type (`input: ValueBag<any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1112 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:76:44`

This declares a widened `any` type (`): Effect.Effect<Record<string, ValueBag<any>>, ConductorError, Exclude<ComputeRequirements, ComputeNodeContext>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1113 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:80:92`

This uses the non-null assertion `!` (`computeNodeResolver: async (node: ComputeNode) => workflow.getResolvedNode(node.id)!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1114 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:88:45`

This declares a widened `any` type (`const result: Record<string, ValueBag<any>> = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1115 no-casts `packages/core/compute/conductor/src/tests/gpt.test.ts:33:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1116 no-casts `packages/core/compute/conductor/src/tests/streaming.test.ts:33:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1117 no-casts `packages/core/compute/conductor/src/types/compute.test.ts:26:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1118 no-casts `packages/core/compute/conductor/src/types/compute.ts:27:38`

This declares a widened `any` type (`export const isNotExecuted = (value: any): value is NotExecuted => value.kind === 'not-executed';`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1119 no-casts `packages/core/compute/conductor/src/types/compute.ts:61:23`

This declares a widened `any` type (`isValueBag: (value: any): value is ValueBag => value._tag === 'ValueBag',`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1120 no-casts `packages/core/compute/conductor/src/types/compute.ts:67:30`

This line casts to `any` with `as any` (`values: mapValues(values as any, (value) => (Effect.isEffect(value) ? value : Effect.succeed(value))) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1121 no-casts `packages/core/compute/conductor/src/types/compute.ts:67:107`

This line casts to `any` with `as any` (`values: mapValues(values as any, (value) => (Effect.isEffect(value) ? value : Effect.succeed(value))) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1122 no-casts `packages/core/compute/conductor/src/types/compute.ts:83:63`

This declares a widened `any` type (`Object.entries(bag.values as Record<string, ValueEffect<any>>).map(([key, eff]) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1123 no-casts `packages/core/compute/conductor/src/util/ast.test.ts:32:77`

This line casts to `any` with `as any` (`const field = pickProperty(Schema.Struct({ foo: Schema.Number }), 'bar' as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1124 no-casts `packages/core/compute/conductor/src/util/stream.ts:11:26`

This declares a widened `any` type (`const isStream = (value: any): value is Stream.Stream<any> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1125 no-casts `packages/core/compute/conductor/src/util/stream.ts:11:55`

This declares a widened `any` type (`const isStream = (value: any): value is Stream.Stream<any> =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1126 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:42:5`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1127 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:175:48`

This declares a widened `any` type (`const executeEffect = (effect: ComputeResult<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1128 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:183:29`

This declares a widened `any` type (`const makeInput = (input: any) => ValueBag.make({ input });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1129 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:215:57`

This declares a widened `any` type (`const createFunctionTransform = (functionRef: Ref.Ref<any> | null): TestWorkflowGraph => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1130 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:259:26`

This declares a widened `any` type (`type Transform = (input: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1131 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:259:34`

This declares a widened `any` type (`type Transform = (input: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1132 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:34:23`

This declares a widened `any` type (`run(input: ValueBag<any>): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1133 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:34:53`

This declares a widened `any` type (`run(input: ValueBag<any>): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1134 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:42:48`

This declares a widened `any` type (`runFrom(inputNodeId: string, input: ValueBag<any>): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1135 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:42:78`

This declares a widened `any` type (`runFrom(inputNodeId: string, input: ValueBag<any>): ComputeResult<ValueBag<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1136 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:63:43`

This declares a widened `any` type (`const tasks: ComputeResult<ValueBag<any>>[] = allAffectedNodes.map((nodeId) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1137 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:91:75`

This declares a widened `any` type (`const resolveOutputsSchema = (sourceNode: ComputeNode): Schema.Struct<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1138 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:99:74`

This declares a widened `any` type (`const resolveInputsSchema = (targetNode: ComputeNode): Schema.Struct<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1139 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:128:51`

This uses the non-null assertion `!` (`return this._resolvedNodeById.get(node.id)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1140 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:148:25`

This declares a widened `any` type (`schema: Schema.Struct<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1141 no-casts `packages/core/compute/crawler/src/testing/index.ts:265:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1142 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.ts:103:20`

This line casts to `any` with `as any` (`httpPlugin as any as Plugin,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1143 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.ts:190:39`

This uses the non-null assertion `!` (`return Object.values(result.metafile!.outputs)[0].imports.map((entry): Import => {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1144 no-casts `packages/core/compute/edge-compute/src/edge-client.ts:83:96`

This declares a widened `any` type (`const functions: Operation.PersistentOperation[] = result.uploadedFunctions.flatMap((record: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1145 no-casts `packages/core/compute/edge-compute/src/edge-client.ts:86:39`

This declares a widened `any` type (`const versionMeta = safeParseJson<any>(latest.versionMetaJSON);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1146 no-casts `packages/core/compute/edge-compute/src/FunctionsServiceClient.ts:135:58`

This declares a widened `any` type (`return response.uploadedFunctions.flatMap((record: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1147 no-casts `packages/core/compute/edge-compute/src/FunctionsServiceClient.ts:137:43`

This declares a widened `any` type (`const versionMeta = safeParseJson<any>(latest.versionMetaJSON);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1148 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:104:47`

This uses the non-null assertion `!` (`join(build.initialOptions.outdir!, 'metafile.json'),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1149 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:108:47`

This uses the non-null assertion `!` (`join(build.initialOptions.outdir!, 'manifest.json'),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1150 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:111:53`

This uses the non-null assertion `!` (`files: Object.keys(result.metafile!.outputs).map((path) => ({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1151 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:112:63`

This uses the non-null assertion `!` (`name: relative(build.initialOptions.outdir!, path),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1152 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:129:49`

This uses the non-null assertion `!` (`const assetPaths = Object.keys(result.metafile!.outputs).map((path) => relative(outdir, path));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1153 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:141:37`

This uses the non-null assertion `!` (`Object.entries(result.metafile!.outputs)`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1154 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:162:34`

This uses the non-null assertion `!` (`Array.map((_) => _.original!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1155 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:220:36`

This declares a widened `any` type (`const postprocessSourceMap = (map: any, outDir: string, gitDir: string | null) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1156 no-casts `packages/core/compute/edge-compute/src/testing/services.ts:29:8`

This declares a widened `any` type (`ai?: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1157 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:55:39`

This uses the non-null assertion `!` (`sourceTypes: [Type.getTypename(Note)!],`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1158 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:57:46`

This uses the non-null assertion `!` (`targets: [{ type: Type.getTypename(Contact)!, identity: { fields: ['email'] } }],`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1159 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:88:3`

This line casts to `any` with `as any` (`} as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1160 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:105:33`

This uses the non-null assertion `!` (`[Type.getTypename(Contact)!]: ({ email }: { email: string }) =>`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1161 no-casts `packages/core/compute/extractor/src/IdentityIndex.ts:27:69`

This declares a widened `any` type (`export const makeIdentityIndex = (specs: ReadonlyArray<IdentitySpec<any>>): IdentityIndex => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1162 no-casts `packages/core/compute/extractor/src/IdentityIndex.ts:72:72`

This declares a widened `any` type (`export const overlayIdentityIndex = (specs: ReadonlyArray<IdentitySpec<any>>, parent: IdentityIndex): IdentityIndex => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1163 no-casts `packages/core/compute/extractor/src/IdentityIndex.ts:89:37`

This declares a widened `any` type (`specs: ReadonlyArray<IdentitySpec<any>>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1164 no-casts `packages/core/compute/extractor/src/IdentityIndex.ts:106:37`

This declares a widened `any` type (`specs: ReadonlyArray<IdentitySpec<any>>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1165 no-casts `packages/core/compute/extractor/src/IdentityIndex.ts:115:36`

This declares a widened `any` type (`const scoped = (spec: IdentitySpec<any>, key: string) => `${Type.getTypename(spec.type)}#${key}`;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1166 no-casts `packages/core/compute/extractor/src/IdentitySpec.ts:77:69`

This declares a widened `any` type (`export const fromIdentitySpecs = (specs: ReadonlyArray<IdentitySpec<any>>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1167 no-casts `packages/core/compute/extractor/src/Resolver.ts:13:55`

This declares a widened `any` type (`export type ResolverMap = Record<string, ResolverType<any, any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1168 no-casts `packages/core/compute/extractor/src/Resolver.ts:13:60`

This declares a widened `any` type (`export type ResolverMap = Record<string, ResolverType<any, any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1169 no-casts `packages/core/compute/extractor/src/Resolver.ts:37:30`

This declares a widened `any` type (`resolve: (type, input: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1170 no-casts `packages/core/compute/extractor/src/testing/mock-ai.ts:30:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1171 no-casts `packages/core/compute/functions-runtime-cloudflare/src/functions-client.ts:87:44`

This uses the non-null assertion `!` (`const space = this._spaces.get(spaceId)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1172 no-casts `packages/core/compute/functions-runtime-cloudflare/src/functions-client.ts:93:48`

This declares a widened `any` type (`export const createClientFromEnv = async (env: any): Promise<FunctionsClient> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1173 no-casts `packages/core/compute/functions-runtime-cloudflare/src/wrap-handler-for-cloudflare.ts:24:100`

This declares a widened `any` type (`export const wrapHandlerForCloudflare = (func: FunctionProtocol.Func): ExportedHandlerFetchHandler<any> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1174 no-casts `packages/core/compute/functions-runtime-cloudflare/src/wrap-handler-for-cloudflare.ts:57:21`

This declares a widened `any` type (`} catch (error: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1175 no-casts `packages/core/compute/functions-runtime-cloudflare/src/wrap-handler-for-cloudflare.ts:81:30`

This line casts to `any` with `as any` (`} = (await request.json()) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1176 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:102:22`

This uses the non-null assertion `!` (`trigger.input!.iterations = 100;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1177 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:156:20`

This uses the non-null assertion `!` (`trigger.input!.iterations = 1_000_000_000;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1178 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:162:20`

This uses the non-null assertion `!` (`trigger.input!.iterations = 100;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1179 no-casts `packages/core/compute/functions-testing/src/edge-routine.test.ts:98:22`

This declares a widened `any` type (`const runResult: any = await client.edge.http.forceRunCronTrigger(Context.default(), space.id, trigger.id);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1180 no-casts `packages/core/compute/link/src/Cursor.test.ts:396:34`

This uses the non-null assertion `!` (`expect([...state.foreignIndex!.keys()].sort()).toEqual(['id-10', 'id-30']);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1181 no-casts `packages/core/compute/link/src/Cursor.test.ts:398:30`

This uses the non-null assertion `!` (`expect(state.foreignIndex!.get('id-10')).toBe(items[0].id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1182 no-casts `packages/core/compute/link/src/Cursor.test.ts:399:30`

This uses the non-null assertion `!` (`expect(state.foreignIndex!.get('id-30')).toBe(items[2].id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1183 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:68:25`

This declares a widened `any` type (`const parameters: any = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1184 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:96:33`

This line casts to `any` with `as any` (`arguments: args as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1185 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:103:7`

This line casts to `any` with `as any` (`) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1186 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:115:89`

This line casts to `any` with `as any` (`const cause = error != null && typeof error === 'object' && 'error' in error ? (error as any).error : error;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1187 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:170:89`

This line casts to `any` with `as any` (`const inner = error != null && typeof error === 'object' && 'error' in error ? (error as any).error : error;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1188 no-casts `packages/core/compute/operation/src/invoker.test.ts:24:54`

This line uses the double-cast escape hatch `as unknown as T` (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1189 no-casts `packages/core/compute/operation/src/invoker.test.ts:24:98`

This declares a widened `any` type (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1190 no-casts `packages/core/compute/operation/src/invoker.test.ts:24:103`

This declares a widened `any` type (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1191 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:27:33`

This declares a widened `any` type (`export type InvocationEvent<I = any, O = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1192 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:27:42`

This declares a widened `any` type (`export type InvocationEvent<I = any, O = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1193 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:38:84`

This declares a widened `any` type (`export type DatabaseResolver = (spaceId: SpaceId) => Effect.Effect<Context.Context<any>>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1194 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:99:56`

This declares a widened `any` type (`type AnyManagedRuntime = ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1195 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:99:61`

This declares a widened `any` type (`type AnyManagedRuntime = ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1196 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:108:89`

This declares a widened `any` type (`private readonly _dynamicRuntimeCache = new Map<string, DynamicRuntime.DynamicRuntime<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1197 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:127:61`

This declares a widened `any` type (`private _getDynamicRuntime(services: readonly Context.Tag<any, any>[]): DynamicRuntime.DynamicRuntime<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1198 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:127:66`

This declares a widened `any` type (`private _getDynamicRuntime(services: readonly Context.Tag<any, any>[]): DynamicRuntime.DynamicRuntime<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1199 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:127:105`

This declares a widened `any` type (`private _getDynamicRuntime(services: readonly Context.Tag<any, any>[]): DynamicRuntime.DynamicRuntime<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1200 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:134:64`

This uses the non-null assertion `!` (`dynamicRuntime = DynamicRuntime.make(this._managedRuntime!, services);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1201 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:199:37`

This declares a widened `any` type (`operation: Operation.Definition<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1202 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:199:42`

This declares a widened `any` type (`operation: Operation.Definition<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1203 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:200:38`

This declares a widened `any` type (`): Effect.Effect<Operation.Handler<any, any, NoHandlerError, Operation.Service> | undefined> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1204 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:200:43`

This declares a widened `any` type (`): Effect.Effect<Operation.Handler<any, any, NoHandlerError, Operation.Service> | undefined> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1205 no-casts `packages/core/compute/operation/src/scheduler.test.ts:19:54`

This line uses the double-cast escape hatch `as unknown as T` (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1206 no-casts `packages/core/compute/operation/src/scheduler.test.ts:19:98`

This declares a widened `any` type (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1207 no-casts `packages/core/compute/operation/src/scheduler.test.ts:19:103`

This declares a widened `any` type (`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1208 no-casts `packages/core/compute/operation/src/scheduler.test.ts:48:33`

This declares a widened `any` type (`const invokeFn = ((_op: any, input: { id: string }) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1209 no-casts `packages/core/compute/operation/src/scheduler.test.ts:51:30`

This line casts to `any` with `as any` (`return undefined as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1210 no-casts `packages/core/compute/operation/src/scheduler.test.ts:78:33`

This declares a widened `any` type (`const invokeFn = ((_op: any, input: { id: string }) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1211 no-casts `packages/core/compute/operation/src/scheduler.test.ts:81:30`

This line casts to `any` with `as any` (`return undefined as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1212 no-casts `packages/core/compute/operation/src/scheduler.test.ts:106:58`

This line casts to `any` with `as any` (`const invokeFn = (() => Effect.succeed(undefined as any)) as Scheduler.InvokeFn;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1213 no-casts `packages/core/compute/operation/src/scheduler.test.ts:126:33`

This declares a widened `any` type (`const invokeFn = ((_op: any, input: { id: string }) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1214 no-casts `packages/core/compute/operation/src/scheduler.test.ts:132:30`

This line casts to `any` with `as any` (`return undefined as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1215 no-casts `packages/core/compute/operation/src/scheduler.test.ts:156:33`

This declares a widened `any` type (`const invokeFn = ((_op: any, input: { id: string }) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1216 no-casts `packages/core/compute/operation/src/scheduler.test.ts:162:30`

This line casts to `any` with `as any` (`return undefined as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1217 no-casts `packages/core/compute/pipeline-discord/src/stages/answer-questions.test.ts:124:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1218 no-casts `packages/core/compute/pipeline-email/src/stages/stats.test.ts:21:58`

This line casts to `any` with `as any` (`const ctx = Layer.succeed(EmailPipelineCtx, { db: {} as any, stats, summaries: [] });`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1219 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:23:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1220 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:34:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1221 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:50:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1222 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:68:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1223 no-casts `packages/core/compute/pipeline-transcription/src/asr/transcriber.ts:330:59`

This uses the non-null assertion `!` (`lastTimestamp: zeroTimestamp + filteredSegments.at(-1)!.end * 1_000,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1224 no-casts `packages/core/compute/pipeline-transcription/src/pipeline.ts:13:36`

This declares a widened `any` type (`readonly stages?: readonly Stage<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1225 no-casts `packages/core/compute/pipeline-transcription/src/pipeline.ts:13:41`

This declares a widened `any` type (`readonly stages?: readonly Stage<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1226 no-casts `packages/core/compute/pipeline-transcription/src/pipeline.ts:17:33`

This declares a widened `any` type (`const defaultStages = (): Stage<any, any>[] => [makeCorrectionStage(), makeExtractionStage(), makeSummarizationStage()];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1227 no-casts `packages/core/compute/pipeline-transcription/src/pipeline.ts:17:38`

This declares a widened `any` type (`const defaultStages = (): Stage<any, any>[] => [makeCorrectionStage(), makeExtractionStage(), makeSummarizationStage()];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1228 no-casts `packages/core/compute/pipeline-transcription/src/runtime/model-routing.ts:18:16`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1229 no-casts `packages/core/compute/pipeline-transcription/src/runtime/model-routing.ts:18:21`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1230 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.test.ts:53:33`

This declares a widened `any` type (`const slow: Stage<{ window: any[] }> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1231 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:57:35`

This declares a widened `any` type (`readonly stages: readonly Stage<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1232 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:57:40`

This declares a widened `any` type (`readonly stages: readonly Stage<any, any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1233 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:87:38`

This declares a widened `any` type (`const triggerMatches = (stage: Stage<any, any>, kind: TranscriptEvent['kind']): boolean =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1234 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:87:43`

This declares a widened `any` type (`const triggerMatches = (stage: Stage<any, any>, kind: TranscriptEvent['kind']): boolean =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1235 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:92:35`

This declares a widened `any` type (`const triggerKind = (stage: Stage<any, any>): TranscriptEvent['kind'] =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1236 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:92:40`

This declares a widened `any` type (`const triggerKind = (stage: Stage<any, any>): TranscriptEvent['kind'] =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1237 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:102:16`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1238 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:102:21`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1239 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:124:16`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1240 no-casts `packages/core/compute/pipeline-transcription/src/runtime/PipelineRuntime.ts:124:21`

This declares a widened `any` type (`stage: Stage<any, any>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1241 no-casts `packages/core/compute/pipeline-transcription/src/stages/correction-llm.test.ts:24:9`

This line casts to `any` with `as any` (`} as any),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1242 no-casts `packages/core/compute/pipeline-transcription/src/stages/correction-llm.test.ts:25:5`

This line casts to `any` with `as any` (`} as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1243 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:23:96`

This uses the non-null assertion `!` (`const write = await EffectEx.runPromise(stage.run({ window: [block] }, { model: stage.model! }));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1244 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:47:90`

This uses the non-null assertion `!` (`stage.run({ window: [block] }, { lookup: makeDatabaseLookup(db), model: stage.model! }),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1245 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:50:38`

This uses the non-null assertion `!` (`const update = write.blockUpdates![0];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1246 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:69:90`

This uses the non-null assertion `!` (`stage.run({ window: [block] }, { lookup: makeDatabaseLookup(db), model: stage.model! }),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1247 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.ts:57:23`

This declares a widened `any` type (`references: Ref.Ref<any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1248 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.ts:65:29`

This declares a widened `any` type (`const references: Ref.Ref<any>[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1249 no-casts `packages/core/compute/pipeline-transcription/src/stages/index.ts:25:67`

This declares a widened `any` type (`export const STAGE_FACTORIES: Readonly<Record<string, () => Stage<any, any>>> = Object.freeze({`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1250 no-casts `packages/core/compute/pipeline-transcription/src/stages/index.ts:25:72`

This declares a widened `any` type (`export const STAGE_FACTORIES: Readonly<Record<string, () => Stage<any, any>>> = Object.freeze({`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1251 no-casts `packages/core/compute/pipeline-transcription/src/stages/index.ts:34:90`

This declares a widened `any` type (`export const buildStages = (configs: readonly { id: string; enabled: boolean }[]): Stage<any, any>[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1252 no-casts `packages/core/compute/pipeline-transcription/src/stages/index.ts:34:95`

This declares a widened `any` type (`export const buildStages = (configs: readonly { id: string; enabled: boolean }[]): Stage<any, any>[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1253 no-casts `packages/core/compute/pipeline-transcription/src/types/lookup.ts:13:16`

This declares a widened `any` type (`ref: Ref.Ref<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1254 no-casts `packages/core/compute/pipeline-transcription/src/types/stage.ts:30:23`

This declares a widened `any` type (`references: Ref.Ref<any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1255 no-casts `packages/core/compute/pipeline-transcription/src/types/stage.ts:42:73`

This declares a widened `any` type (`resolvedReferents: { surface: string; referent: string; ref?: Ref.Ref<any> }[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1256 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:45`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1257 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:50`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1258 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:55`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1259 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:81`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1260 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:86`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1261 no-casts `packages/core/compute/pipeline/src/Pipeline.ts:48:91`

This declares a widened `any` type (`} = Function.dual(2, (source: Stream.Stream<any, any, any>, options: RunOptions<any, any, any>) =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1262 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:137:67`

This uses the non-null assertion `!` (`await using db2 = await peer.openDatabase(spaceKey, db.rootUrl!, {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1263 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:157:68`

This uses the non-null assertion `!` (`await using db2 = await peer.openDatabase(spaceKey, db1.rootUrl!, {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1264 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:202:66`

This line casts to `any` with `as any` (`const outer = (await db.query(Filter.id(outerId)).first()) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1265 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:220:27`

This uses the non-null assertion `!` (`rootUrl = db.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1266 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:230:66`

This line casts to `any` with `as any` (`const outer = (await db.query(Filter.id(outerId)).first()) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1267 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:250:88`

This line casts to `any` with `as any` (`const object = db.add(Obj.make(TestSchema.Expando, { type: db.makeRef(typeDXN) })) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1268 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:278:85`

This line casts to `any` with `as any` (`const object = db.add(Obj.make(TestSchema.Expando, { fn: db.makeRef(keyDXN) })) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1269 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:305:69`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1270 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:327:72`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey1, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1271 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:339:72`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey2, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1272 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:376:82`

This uses the non-null assertion `!` (`await using db2 = await asyncTimeout(peer2.openDatabase(spaceKey, db1.rootUrl!), 1_000);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1273 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:425:28`

This uses the non-null assertion `!` (`rootUrl = db1.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1274 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:445:29`

This uses the non-null assertion `!` (`return state.peers!.length;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1275 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:452:29`

This uses the non-null assertion `!` (`return state.peers![0].differentDocuments + state.peers![0].missingOnRemote + state.peers![0].missingOnLocal;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1276 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:452:66`

This uses the non-null assertion `!` (`return state.peers![0].differentDocuments + state.peers![0].missingOnRemote + state.peers![0].missingOnLocal;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1277 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:452:100`

This uses the non-null assertion `!` (`return state.peers![0].differentDocuments + state.peers![0].missingOnRemote + state.peers![0].missingOnLocal;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1278 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:468:69`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1279 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:488:21`

This declares a definite-assignment assertion with `!` (`let relationId!: EntityId;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1280 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:627:29`

This uses the non-null assertion `!` (`rootUrl = db.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1281 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:635:40`

This uses the non-null assertion `!` (`schemaDxn = Type.getURI(stored)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1282 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:640:47`

This uses the non-null assertion `!` (`expect(Type.getURI(Obj.getType(object)!)).to.eq(Type.getURI(stored));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1283 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:660:72`

This uses the non-null assertion `!` (`expect(getTypeAnnotation(Type.getSchema(Obj.getType(objects[0])!))).to.include({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1284 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:674:58`

This uses the non-null assertion `!` (`const objects = await db.query(Filter.type(schema!)).run();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1285 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:676:75`

This uses the non-null assertion `!` (`expect(getTypeAnnotation(Type.getSchema(Entity.getType(objects[0])!))).to.include({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1286 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:687:16`

This declares a definite-assignment assertion with `!` (`let typeURI!: URI.URI;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1287 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:694:36`

This uses the non-null assertion `!` (`typeURI = Type.getURI(schema)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1288 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:707:47`

This uses the non-null assertion `!` (`expect(Type.getTypename(Obj.getType(obj)!)).toEqual(Type.getTypename(TestSchema.Person));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1289 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:759:34`

This line casts to `any` with `as any` (`expect(() => db.add(plainObj as any)).toThrow(TypeError);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1290 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:764:50`

This line casts to `any` with `as any` (`await expect(db.appendToFeed(feed, [plainObj as any])).rejects.toThrow(TypeError);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1291 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:796:69`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1292 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:79:66`

This line casts to `any` with `as any` (`const child = (await db.query(Filter.id(childId)).first()) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1293 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:85:22`

This line casts to `any` with `as any` (`expect((parent as any).name).to.eq('Parent');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1294 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:86:22`

This line casts to `any` with `as any` (`expect((parent as any).id).to.eq(parentId);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1295 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:46:33`

This declares a widened `any` type (`[Obj.Meta]?: { tags?: Ref.Ref<any>[]; key?: string; version?: string };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1296 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:902:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1297 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:905:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1298 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:922:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1299 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:925:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1300 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:944:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1301 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:947:47`

This uses the non-null assertion `!` (`.from(Scope.feed(Feed.getFeedUri(feed)!))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1302 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:964:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1303 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:967:41`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1304 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1134:29`

This declares a widened `any` type (`Obj.update(obj, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1305 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1192:37`

This declares a widened `any` type (`Obj.update(object, (object: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1306 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1236:29`

This declares a widened `any` type (`Obj.update(obj, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1307 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1242:43`

This uses the non-null assertion `!` (`expect(after).toBeGreaterThan(before!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1308 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1252:29`

This declares a widened `any` type (`Obj.update(obj, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1309 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1285:34`

This uses the non-null assertion `!` (`expect(Obj.getMeta(reloaded!).createdAt).toBe(createdAt);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1310 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1338:45`

This uses the non-null assertion `!` (`const t1 = Obj.getMeta(obj1).createdAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1311 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1339:45`

This uses the non-null assertion `!` (`const t2 = Obj.getMeta(obj2).createdAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1312 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1340:45`

This uses the non-null assertion `!` (`const t3 = Obj.getMeta(obj3).createdAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1313 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1359:31`

This declares a widened `any` type (`Obj.update(obj2, (obj2: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1314 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1364:31`

This declares a widened `any` type (`Obj.update(obj1, (obj1: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1315 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1370:47`

This uses the non-null assertion `!` (`const upd1 = Obj.getMeta(obj1).updatedAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1316 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1371:47`

This uses the non-null assertion `!` (`const upd2 = Obj.getMeta(obj2).updatedAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1317 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1372:47`

This uses the non-null assertion `!` (`const upd3 = Obj.getMeta(obj3).updatedAt!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1318 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1408:116`

This uses the non-null assertion `!` (`.query(Query.select(Filter.type(DXN.make('com.example.type.person'))).from(Scope.feed(Feed.getFeedUri(feed)!)))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1319 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1415:45`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1320 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1424:45`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1321 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1444:45`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1322 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1502:48`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed1)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1323 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1516:48`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed2)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1324 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1530:48`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed1)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1325 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1531:48`

This uses the non-null assertion `!` (`Scope.feed(Feed.getFeedUri(feed2)!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1326 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1653:35`

This uses the non-null assertion `!` (`expect(Feed.getFeedUri(feed)!).toMatch(/^echo:\/\//);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1327 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1665:47`

This uses the non-null assertion `!` (`const uriString = Obj.getURI(traceResult!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1328 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1685:47`

This uses the non-null assertion `!` (`const uriString = Obj.getURI(traceResult!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1329 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1744:36`

This declares a widened `any` type (`expect(feedResults.map((obj: any) => obj.title).sort()).toEqual(['Feed Task 1', 'Feed Task 2']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1330 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1761:29`

This line casts to `any` with `as any` (`expect((feedResult[0] as any).title).toBe('Feed Task');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1331 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1809:36`

This declares a widened `any` type (`expect(feedResults.map((obj: any) => obj.title).sort()).toEqual(['Feed Task 1', 'Feed Task 2']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1332 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1906:18`

This uses the non-null assertion `!` (`doc.links![obj1.id] = 'automerge:4hjTgo9zLNsfRTJiLcpPY8P4smy';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1333 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1937:58`

This uses the non-null assertion `!` (`const obj1DocHandle = getObjectCore(obj1).docHandle!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1334 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1938:61`

This uses the non-null assertion `!` (`const anotherDocHandle = getObjectCore(obj2).docHandle!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1335 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1942:20`

This uses the non-null assertion `!` (`doc.objects![obj1.id] = obj1DocHandle.doc()!.objects![obj1.id];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1336 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1942:52`

This uses the non-null assertion `!` (`doc.objects![obj1.id] = obj1DocHandle.doc()!.objects![obj1.id];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1337 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1942:61`

This uses the non-null assertion `!` (`doc.objects![obj1.id] = obj1DocHandle.doc()!.objects![obj1.id];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1338 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1945:18`

This uses the non-null assertion `!` (`doc.links![obj1.id] = new A.RawString(anotherDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1339 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1945:67`

This uses the non-null assertion `!` (`doc.links![obj1.id] = new A.RawString(anotherDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1340 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1949:73`

This uses the non-null assertion `!` (`assertion = { objectId: obj2.id, documentUrl: anotherDocHandle.url! };`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1341 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1960:78`

This uses the non-null assertion `!` (`const object = queryResult.find((obj) => obj.id === assertion.objectId)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1342 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1961:45`

This uses the non-null assertion `!` (`expect(getObjectCore(object).docHandle!.url).to.eq(assertion.documentUrl);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1343 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1999:23`

This uses the non-null assertion `!` (`obj2Core.docHandle!.delete(); // Deleted handle access throws an exception.`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1344 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2007:86`

This line casts to `any` with `as any` (`const folder = db.add(Obj.make(TestSchema.Expando, { name: 'folder', objects: [] as any[] }));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1345 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2010:43`

This line casts to `any` with `as any` (`folder.objects.push(Ref.make(object as any));`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1346 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2263:44`

This uses the non-null assertion `!` (`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1347 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2263:67`

This uses the non-null assertion `!` (`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1348 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2349:44`

This uses the non-null assertion `!` (`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1349 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2349:67`

This uses the non-null assertion `!` (`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1350 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2528:40`

This declares a widened `any` type (`const titles = objects.map((obj: any) => obj.title).sort();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1351 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2818:114`

This uses the non-null assertion `!` (`Query.select(Filter.text('TypeScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1352 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2828:114`

This uses the non-null assertion `!` (`.query(Query.select(Filter.text('React', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1353 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2838:114`

This uses the non-null assertion `!` (`Query.select(Filter.text('JavaScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1354 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2888:112`

This uses the non-null assertion `!` (`Query.select(Filter.text('TypeScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1355 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2917:27`

This uses the non-null assertion `!` (`expect(entry.match!.rank).toBeDefined();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1356 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2918:34`

This uses the non-null assertion `!` (`expect(typeof entry.match!.rank).toBe('number');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1357 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2919:27`

This uses the non-null assertion `!` (`expect(entry.match!.rank).toBeGreaterThan(0);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1358 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2940:32`

This uses the non-null assertion `!` (`expect(entries[i].match!.rank).toBeGreaterThanOrEqual(entries[i + 1].match!.rank);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1359 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2940:83`

This uses the non-null assertion `!` (`expect(entries[i].match!.rank).toBeGreaterThanOrEqual(entries[i + 1].match!.rank);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1360 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2944:31`

This uses the non-null assertion `!` (`expect(entries[0].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1361 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2964:32`

This uses the non-null assertion `!` (`expect(entries[i].match!.rank).toBeLessThanOrEqual(entries[i + 1].match!.rank);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1362 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2964:80`

This uses the non-null assertion `!` (`expect(entries[i].match!.rank).toBeLessThanOrEqual(entries[i + 1].match!.rank);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1363 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2968:48`

This uses the non-null assertion `!` (`expect(entries[entries.length - 1].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1364 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2987:27`

This uses the non-null assertion `!` (`expect(entry.match!.rank).toBe(1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1365 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3011:31`

This uses the non-null assertion `!` (`expect(entries[0].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1366 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3012:31`

This uses the non-null assertion `!` (`expect(entries[1].result!.title).toBe('TypeScript TypeScript TypeScript');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1367 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3072:34`

This declares a widened `any` type (`Obj.update(objects[0], (o: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1368 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3160:60`

This uses the non-null assertion `!` (`const names = query.results.map((obj) => obj.name!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1369 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3260:63`

This uses the non-null assertion `!` (`updates.push([...query.results.map((obj) => obj.name!)].sort());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1370 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3357:26`

This line casts to `any` with `as any` (`expect((results[0] as any).value).toBe(42);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1371 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3372:44`

This line casts to `any` with `as any` (`const values = results.map((r) => (r as any).value).sort();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1372 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3387:28`

This line casts to `any` with `as any` (`expect((dbResults[0] as any).value).toBe(99);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1373 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3393:48`

This line casts to `any` with `as any` (`const values = bothResults.map((r) => (r as any).value).sort((a: number, b: number) => a - b);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1374 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3475:37`

This declares a widened `any` type (`const names = objects.map((o: any) => o.name).sort();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1375 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3564:48`

This declares a widened `any` type (`const taskResults = objects.filter((obj: any) => obj.title === 'Grandchild task');`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1376 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3592:34`

This declares a widened `any` type (`expect(objects.every((obj: any) => Obj.getTypename(obj) !== 'org.dxos.type.feed')).toBe(true);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1377 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3639:32`

This declares a widened `any` type (`expect(objects.map((obj: any) => obj.title).sort()).toEqual(['A', 'B']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1378 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3680:37`

This declares a widened `any` type (`const names = objects.map((o: any) => o.name).sort();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1379 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3748:76`

This declares a widened `any` type (`const assertQuery = async (db: EchoDatabase, filter: Filter.Any, expected: any[]) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1380 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3753:28`

This declares a widened `any` type (`const sortById = (objects: any[]) => objects.sort((a, b) => a.id.localeCompare(b.id));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1381 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:48:24`

This line casts to `any` with `as any` (`.map((o) => (o as any).value)`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1382 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:64:36`

This line casts to `any` with `as any` (`expect((local.get(original.id) as any).value).toBe(999);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1383 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:78:23`

This line casts to `any` with `as any` (`expect((result[0] as any).value).toBe(42);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1384 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:87:44`

This line casts to `any` with `as any` (`return registry.list().map((o) => (o as any).value);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1385 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:169:41`

This line casts to `any` with `as any` (`expect(expandoResults.map((o) => (o as any).value).sort()).toEqual([1, 2]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1386 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:185:26`

This line casts to `any` with `as any` (`expect((q.results[0] as any).value).toBe(10);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1387 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:37:85`

This line casts to `any` with `as any` (`expect(() => Obj.make(Organization.Organization, { status: 'invalid-status' } as any)).toThrow();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1388 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:41:70`

This line casts to `any` with `as any` (`expect(() => Obj.make(Organization.Organization, { name: 123 } as any)).toThrow();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1389 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:46:96`

This line casts to `any` with `as any` (`expect(() => Obj.make(Organization.Organization, { name: 'Acme', unknownField: 'extra' } as any)).toThrow(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1390 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:55:16`

This line casts to `any` with `as any` (`(org as any).unknownField = 'extra';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1391 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:62:92`

This line casts to `any` with `as any` (`expect(() => db.add(Obj.make(Organization.Organization, { status: 'invalid-status' } as any))).toThrow();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1392 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:68:43`

This line casts to `any` with `as any` (`expect(() => Obj.make(Task.Task, {} as any)).toThrow(/title/i);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1393 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:249:45`

This declares a widened `any` type (`const typeRow = data.objects.find((o: any) => o['@meta']?.key === typename);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1394 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:269:37`

This uses the non-null assertion `!` (`expect(Type.isType(roundTrip!)).to.be.true;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1395 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:270:42`

This uses the non-null assertion `!` (`expect(Type.getTypename(roundTrip!)).to.eq(typename);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1396 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:276:48`

This line casts to `any` with `as any` (`expect(() => Type.getTypename(schema as any)).not.to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1397 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:278:66`

This line casts to `any` with `as any` (`expect(entities.some((schema) => Type.getTypename(schema as any) === typename)).to.be.true;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1398 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:318:70`

This uses the non-null assertion `!` (`const main = objects.find((object) => object.title === 'Main task')!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1399 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:36:10`

This declares a definite-assignment assertion with `!` (`#schema!: Schema.Schema.AnyNoContext;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1400 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:37:9`

This declares a definite-assignment assertion with `!` (`#value!: unknown;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1401 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:48:18`

This declares a widened `any` type (`get(key: keyof any): unknown {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1402 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:50:25`

This line casts to `any` with `as any` (`return (this.#value as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1403 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:53:18`

This declares a widened `any` type (`set(key: keyof any, value: unknown): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1404 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:55:18`

This line casts to `any` with `as any` (`(this.#value as any)[key] = value;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1405 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:61:18`

This declares a widened `any` type (`get(key: keyof any): unknown;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1406 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:62:18`

This declares a widened `any` type (`set(key: keyof any, value: unknown): void;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1407 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:73:39`

This uses the non-null assertion `!` (`return livePrototypeCache.get(ast)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1408 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:90:56`

This declares a widened `any` type (`: function set(this: LiveProto, value: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1409 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:98:35`

This line casts to `any` with `as any` (`const proto: LiveProto = {} as any; // [Cell] will be defined in the object factory.`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1410 no-casts `packages/core/echo/echo-client/src/automerge/doc-handle-proxy.ts:154:62`

This declares a widened `any` type (`change(fn: (doc: A.Doc<T>) => void, opts?: A.ChangeOptions<any>): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1411 no-casts `packages/core/echo/echo-client/src/automerge/doc-handle-proxy.ts:167:80`

This declares a widened `any` type (`changeAt(heads: A.Heads, fn: (doc: A.Doc<T>) => void, opts?: A.ChangeOptions<any>): A.Heads | undefined {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1412 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:41:96`

This uses the non-null assertion `!` (`const hostHandle = await host.loadDoc<{ text: string }>(Context.default(), clientHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1413 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:50:31`

This declares a widened `any` type (`clientHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1414 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:64:31`

This declares a widened `any` type (`hostHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1415 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:68:32`

This uses the non-null assertion `!` (`expect(clientHandle.doc()!.text).to.equal(text);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1416 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:102:61`

This uses the non-null assertion `!` (`const handle2 = repo2.find<{ text: string }>(handle1.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1417 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:112:28`

This declares a widened `any` type (`handle2.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1418 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:134:29`

This uses the non-null assertion `!` (`url = clientHandle.url!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1419 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:135:33`

This declares a widened `any` type (`clientHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-1420 no-sleep-in-test `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:233`

`await sleep(200); // Wait for the object to be saved without flush.` (also line 270) waits an arbitrary period for the auto-save to complete before closing and later reopening to verify persistence, with no completion signal awaited. This is the classic sleep-to-synchronize anti-pattern; await an explicit save/flush signal instead.

# ERROR 50877ea571-1421 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:234:29`

This uses the non-null assertion `!` (`url = clientHandle.url!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1422 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:269:29`

This uses the non-null assertion `!` (`url = clientHandle.url!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1423 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:297:106`

This uses the non-null assertion `!` (`const hostHandle = await host.loadDoc<{ client: number; host: number }>(Context.default(), handle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1424 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:302:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1425 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:306:31`

This declares a widened `any` type (`hostHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1426 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:337:95`

This uses the non-null assertion `!` (`const hostHandle = await host.loadDoc<{ text: string }>(Context.default(), cloneHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1427 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:358:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1428 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:373:90`

This uses the non-null assertion `!` (`const loaded = await host.loadDoc<{ text: string }>(Context.default(), handle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1429 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:417:59`

This uses the non-null assertion `!` (`const foundHandle = repo2.find<DocStruct>(handle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1430 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:422:53`

This uses the non-null assertion `!` (`handles2.push(repo2.find<DocStruct>(handle.url!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1431 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:427:59`

This uses the non-null assertion `!` (`const foundHandle = repo1.find<DocStruct>(handle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1432 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:432:53`

This uses the non-null assertion `!` (`handles1.push(repo1.find<DocStruct>(handle.url!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1433 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:39:51`

This declares a widened `any` type (`private _handles: Record<string, DocHandleProxy<any>> = {};`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1434 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:99:48`

This declares a widened `any` type (`get handles(): Record<string, DocHandleProxy<any>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1435 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:281:25`

This uses the non-null assertion `!` (`this._sendUpdatesJob!.trigger();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1436 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:291:51`

This uses the non-null assertion `!` (`this._pendingUpdateIds.add(handle.documentId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1437 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.test.ts:183:31`

This line uses the double-cast escape hatch `as unknown as T` (`loaded = { id: objectId } as unknown as Entity.Unknown;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1438 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:223:27`

This declares a widened `any` type (`} catch (err: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1439 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:259:25`

This declares a widened `any` type (`} catch (err: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1440 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:337:19`

This declares a widened `any` type (`} catch (err: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1441 no-casts `packages/core/echo/echo-client/src/core-db/branching.ts:36:41`

This declares a widened `any` type (`export const forkDump = (sourceDoc: Doc<any>, atHeads?: Heads): Uint8Array => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1442 no-casts `packages/core/echo/echo-client/src/core-db/branching.ts:45:29`

This uses the non-null assertion `!` (`const hash = queue.pop()!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1443 no-casts `packages/core/echo/echo-client/src/core-db/branching.ts:55:23`

This declares a widened `any` type (`let forked = A.init<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1444 no-casts `packages/core/echo/echo-client/src/core-db/doc-semaphore.ts:11:34`

This declares a widened `any` type (`const beingChanged = new WeakSet<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1445 no-casts `packages/core/echo/echo-client/src/core-db/doc-semaphore.ts:13:44`

This declares a widened `any` type (`export const docChangeSemaphore = (handle: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1446 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:69:55`

This uses the non-null assertion `!` (`const textHandle = handles.linkedDocHandles[0]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1447 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:74:41`

This declares a widened `any` type (`textHandle.change((newDocument: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1448 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:79:47`

This uses the non-null assertion `!` (`const text = db.getObjectById(object.id)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1449 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:93:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1450 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:95:31`

This line casts to `any` with `as any` (`expect((retrievedObject as any).title).to.eq(newObject.title);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1451 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:104:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1452 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:113:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1453 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:120:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1454 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:137:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1455 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:147:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1456 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:168:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1457 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:174:68`

This declares a widened `any` type (`getObjectDocHandle(db.getObjectById(ids[0])).change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1458 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:179:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1459 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:181:40`

This line casts to `any` with `as any` (`expect((db.getObjectById(ids[0]) as any).content).to.eq(contents[0]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1460 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:183:25`

This declares a widened `any` type (`const dbObject: any = db.getObjectById(id);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1461 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:201:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1462 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:204:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1463 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:215:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1464 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:218:40`

This declares a widened `any` type (`oldRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1465 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:225:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1466 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:236:43`

This declares a widened `any` type (`Obj.update(rootObject, (rootObject: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1467 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:238:24`

This declares a widened `any` type (`.flatMap((v: any[]) => v)`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1468 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:239:26`

This declares a widened `any` type (`.forEach((obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1469 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:248:40`

This declares a widened `any` type (`newRootDocHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1470 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:264:49`

This uses the non-null assertion `!` (`await db.setSpaceRoot(newRootDocHandle.url!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1471 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:293:33`

This uses the non-null assertion `!` (`const rootUrl = db.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1472 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:304:24`

This line casts to `any` with `as any` (`expect((object as any).title).to.eq('first object');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1473 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:385:34`

This declares a widened `any` type (`const getObjectDocHandle = (obj: any) => getObjectCore(obj).docHandle!;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1474 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:385:70`

This uses the non-null assertion `!` (`const getObjectDocHandle = (obj: any) => getObjectCore(obj).docHandle!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1475 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:407:50`

This uses the non-null assertion `!` (`return peer2.openDatabase(spaceKey, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1476 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:423:15`

This declares a widened `any` type (`const data: any = { ...object };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1477 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:425:29`

This declares a widened `any` type (`docHandle.change((newDoc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1478 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:168:27`

This declares a definite-assignment assertion with `!` (`private _updateScheduler!: UpdateScheduler;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1479 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:263:46`

This uses the non-null assertion `!` (`const entries = await this._branchStore!.load();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1480 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:292:36`

This uses the non-null assertion `!` (`.then(() => this._branchStore!.save(entries))`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1481 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:454:37`

This declares a widened `any` type (`let inactivityTimeoutTimer: any | undefined;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1482 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:476:113`

This uses the non-null assertion `!` (`this._objects.get(objectToLoad.id) && !this._areDepsSatisfied(this._objects.get(objectToLoad.id)!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1483 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:485:91`

This uses the non-null assertion `!` (`result[objectToLoad.resultIndex] = this.getObjectCoreById(objectToLoad.id)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1484 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:564:25`

This uses the non-null assertion `!` (`delete doc.links![objectId];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1485 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:573:74`

This uses the non-null assertion `!` (`const toUnlink = objects.filter((o) => o?.isDeleted()).map((o) => o!.id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1486 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:606:23`

This uses the non-null assertion `!` (`newStruct.system!.type = EncodedReference.fromURI(type);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1487 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:800:46`

This declares a widened `any` type (`getLoadedDocumentHandles(): DocHandleProxy<any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1488 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1099:33`

This uses the non-null assertion `!` (`const core = queue.shift()!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1489 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1492:42`

This uses the non-null assertion `!` (`objectsToRebind.get(spaceRootUrl)!.objectIds.push(object.id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1490 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1494:63`

This uses the non-null assertion `!` (`const newObjectDocUrl = linkedObjectIds.get(object.id)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1491 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1662:40`

This uses the non-null assertion `!` (`request.stateChanged.on(this._ctx!, () => this._scheduleThrottledUpdate([core.id]));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1492 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:227:29`

This declares a widened `any` type (`change(changeFn: ChangeFn<any>, options?: A.ChangeOptions<any>): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1493 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:227:61`

This declares a widened `any` type (`change(changeFn: ChangeFn<any>, options?: A.ChangeOptions<any>): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1494 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:233:37`

This uses the non-null assertion `!` (`this.doc = A.change(this.doc!, options, changeFn);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1495 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:235:37`

This uses the non-null assertion `!` (`this.doc = A.change(this.doc!, changeFn);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1496 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:250:45`

This declares a widened `any` type (`changeAt(heads: Heads, callback: ChangeFn<any>, options?: ChangeOptions<any>): Heads | undefined {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1497 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:250:75`

This declares a widened `any` type (`changeAt(heads: Heads, callback: ChangeFn<any>, options?: ChangeOptions<any>): Heads | undefined {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1498 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:257:57`

This uses the non-null assertion `!` (`const { newDoc, newHeads } = A.changeAt(this.doc!, heads, options, callback);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1499 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:261:57`

This uses the non-null assertion `!` (`const { newDoc, newHeads } = A.changeAt(this.doc!, heads, callback);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1500 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:318:19`

This declares a widened `any` type (`} catch (err: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1501 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:358:21`

This declares a widened `any` type (`const values: any = value.map((val) => this.encode(val));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1502 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:363:70`

This declares a widened `any` type (`return Object.fromEntries(entries.map(([key, value]): [string, any] => [key, this.encode(value)]));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1503 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:376:17`

This declares a widened `any` type (`decode(value: any): DecodedAutomergePrimaryValue {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1504 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:394:84`

This declares a widened `any` type (`return Object.fromEntries(Object.entries(value).map(([key, value]): [string, any] => [key, this.decode(value)]));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1505 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:419:22`

This line casts to `any` with `as any` (`value = (value as any)?.[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1506 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:425:45`

This declares a widened `any` type (`private _setRaw(path: Doc.KeyPath, value: any): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1507 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:451:20`

This declares a widened `any` type (`const value: any = getDeep(doc, fullPath.slice(0, fullPath.length - 1));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1508 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:457:54`

This line casts to `any` with `as any` (`return (this._getRaw([SYSTEM_NAMESPACE, 'kind']) as any) ?? EntityKind.Object;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1509 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:554:39`

This line casts to `any` with `as any` (`for (const hash of A.getHeads(doc as any)) {`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1510 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:555:43`

This line casts to `any` with `as any` (`const decoded = A.inspectChange(doc as any, hash);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1511 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:566:46`

This line uses the double-cast escape hatch `as unknown as T` (`return this.getDecoded([META_NAMESPACE]) as unknown as EntityMeta;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# WARN 50877ea571-1512 no-sleep-in-test `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:93`

`await sleep(200); // Let the link change round-trip and the loader discover both links.` recurs identically at lines 168, 231, 282, 352, 398. Each waits a fixed 200ms hoping the document-loader has processed a just-written change before proceeding, instead of using `waitForCondition`/a query subscription on the loader's actual state.

# ERROR 50877ea571-1513 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:114:19`

This uses the non-null assertion `!` (`expect(partial!.id).toEqual(mainObjectId);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1514 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:287:42`

This uses the non-null assertion `!` (`expect(db.areStrongDepsSatisfied(core!)).toBe(false);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1515 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:316:20`

This uses the non-null assertion `!` (`expect(resolved!.id).toEqual(object.id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1516 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:413:19`

This uses the non-null assertion `!` (`newDoc.links![depObjectId] = new A.RawString(depDocHandle.url);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1517 no-casts `packages/core/echo/echo-client/src/core-db/types.ts:131:9`

This declares a widened `any` type (`data: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1518 no-casts `packages/core/echo/echo-client/src/core-db/util.ts:16:68`

This declares a widened `any` type (`for (const { path, value } of event.patches as (Patch & { value: any })[]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1519 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:43:40`

This declares a widened `any` type (`Obj.update(binding.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1520 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:83:41`

This declares a widened `any` type (`Obj.update(binding1.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1521 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:86:41`

This declares a widened `any` type (`Obj.update(binding2.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1522 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:117:31`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1523 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:120:40`

This declares a widened `any` type (`Obj.update(binding.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1524 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:145:40`

This declares a widened `any` type (`Obj.update(binding.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1525 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:172:37`

This declares a widened `any` type (`Obj.update(writer.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1526 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:180:37`

This declares a widened `any` type (`Obj.update(writer.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1527 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:195:38`

This declares a widened `any` type (`Obj.update(binding.object, (obj: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1528 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:54:66`

This uses the non-null assertion `!` (`const registry = db._entityManager.getBranchRegistry(root.id)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1529 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:80:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1530 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:83:31`

This declares a widened `any` type (`Obj.update(child, (child: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1531 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:105:31`

This declares a widened `any` type (`Obj.update(child, (child: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1532 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:122:31`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1533 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:132:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1534 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:137:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1535 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:156:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1536 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:159:31`

This declares a widened `any` type (`Obj.update(child, (child: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1537 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:181:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1538 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:184:31`

This declares a widened `any` type (`Obj.update(child, (child: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1539 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:219:19`

This declares a widened `any` type (`const inline: any = db.add(Obj.make(TestSchema.Expando, { title: 'inline' }), { placeIn: 'root-doc' });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1540 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:228:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1541 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:245:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1542 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:250:9`

This uses the non-null assertion `!` (`core!.changeAt(baseHeads, (doc: any) => {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1543 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:250:37`

This declares a widened `any` type (`core!.changeAt(baseHeads, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1544 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:289:19`

This declares a widened `any` type (`const child1: any = db1.add(Obj.make(TestSchema.Expando, { content: 'child-v0' }));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1545 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:290:18`

This declares a widened `any` type (`const root1: any = db1.add(Obj.make(TestSchema.Expando, { title: 'root-v0', child: Ref.make(child1) }));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1546 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:295:31`

This declares a widened `any` type (`Obj.update(root1, (root1: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1547 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:298:33`

This declares a widened `any` type (`Obj.update(child1, (child1: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1548 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:304:69`

This uses the non-null assertion `!` (`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1549 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:336:18`

This declares a widened `any` type (`const child: any = db.add(Obj.make(TestSchema.Expando, { content: 'child-v0' }));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1550 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:337:17`

This declares a widened `any` type (`const root: any = db.add(Obj.make(TestSchema.Expando, { title: 'root-v0', child: Ref.make(child) }));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1551 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:344:29`

This declares a widened `any` type (`Obj.update(root, (root: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1552 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:347:31`

This declares a widened `any` type (`Obj.update(child, (child: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1553 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:59:14`

This line casts to `any` with `as any` (`(clone as any)[symbolInternals as any].linkCache!.set(ref.id, ref);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1554 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:59:38`

This line casts to `any` with `as any` (`(clone as any)[symbolInternals as any].linkCache!.set(ref.id, ref);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1555 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:59:55`

This uses the non-null assertion `!` (`(clone as any)[symbolInternals as any].linkCache!.set(ref.id, ref);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1556 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:79:26`

This declares a widened `any` type (`coreClone.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1557 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:85:16`

This line casts to `any` with `as any` (`return proxy as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1558 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:88:42`

This declares a widened `any` type (`const getObjectDoc = (core: ObjectCore): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1559 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:89:41`

This uses the non-null assertion `!` (`let value = core.doc ?? core.docHandle!.doc();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1560 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:91:20`

This line casts to `any` with `as any` (`value = (value as any)?.[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1561 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:20:61`

This declares a widened `any` type (`export const getHeader = (tag: string, id: string, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1562 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:31:30`

This declares a widened `any` type (`const formatValue = (object: any, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1563 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:31:44`

This declares a widened `any` type (`const formatValue = (object: any, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1564 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:41:34`

This declares a widened `any` type (`export const getBody = (objData: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1565 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:18:40`

This line casts to `any` with `as any` (`[symbolInternals]: ObjectCore = null as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1566 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:19:36`

This line casts to `any` with `as any` (`[symbolPath]: Doc.KeyPath = null as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1567 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:20:36`

This line casts to `any` with `as any` (`[symbolNamespace]: string = null as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1568 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:21:47`

This line casts to `any` with `as any` (`[symbolHandler]: EchoReactiveHandler = null as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1569 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:33:44`

This declares a widened `any` type (`const fn = function (this: EchoArray<any>, ...args: any[]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1570 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:33:59`

This declares a widened `any` type (`const fn = function (this: EchoArray<any>, ...args: any[]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1571 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:34:19`

This declares a definite-assignment assertion with `!` (`let result!: any;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1572 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:34:22`

This declares a widened `any` type (`let result!: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1573 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:37:30`

This line casts to `any` with `as any` (`result = ((handler as any)[handlerMethodName] as Function).apply(handler, [this, this[symbolPath], ...args]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1574 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:44:64`

This line casts to `any` with `as any` (`expect(() => createObject(Obj.make(invalidSchema, { id: 42 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1575 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:132:73`

This line casts to `any` with `as any` (`}).pipe(EchoObjectSchema(DXN.make('com.example.type.test', '0.1.0'))) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1576 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:138:52`

This uses the non-null assertion `!` (`expect(prepareAstForCompare(Type.getSchema(type!).ast)).to.deep.eq(`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1577 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:147:86`

This uses the non-null assertion `!` (`expect(prepareAstForCompare(Type.getSchema(Obj.getType(obj.nested as Obj.Unknown)!).ast)).to.deep.eq(`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1578 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:210:52`

This line uses the double-cast escape hatch `as unknown as T` (`expect(() => graph.registry.add([NonEchoSchema as unknown as Type.AnyEntity])).to.throw();`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1579 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:402:22`

This declares a widened `any` type (`const objData: any = (obj as any).toJSON();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1580 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:402:33`

This line casts to `any` with `as any` (`const objData: any = (obj as any).toJSON();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1581 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:416:20`

This declares a widened `any` type (`const objData: any = Obj.toJSON(obj as any);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1582 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:416:41`

This line casts to `any` with `as any` (`const objData: any = Obj.toJSON(obj as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1583 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:451:90`

This declares a widened `any` type (`for (const value of [object.field, object.nested.deep.field, ...object.array.map((o: any) => o.field)]) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1584 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:494:77`

This uses the non-null assertion `!` (`expect(obj.reference['/']).to.eq(EID.make({ entityId: obj1.reference!.target!.id }));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1585 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:494:85`

This uses the non-null assertion `!` (`expect(obj.reference['/']).to.eq(EID.make({ entityId: obj1.reference!.target!.id }));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1586 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:570:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1587 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:570:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1588 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:571:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1589 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:571:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1590 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:587:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1591 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:587:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1592 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:588:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1593 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:588:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1594 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:606:22`

This uses the non-null assertion `!` (`contact.tasks!.push(Ref.make(task1));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1595 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:607:22`

This uses the non-null assertion `!` (`contact.tasks!.push(Ref.make(task2));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1596 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:614:27`

This uses the non-null assertion `!` (`expect(contact.tasks![0].target).to.eq(task1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1597 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:615:27`

This uses the non-null assertion `!` (`expect(contact.tasks![1].target).to.eq(task2);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1598 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:616:28`

This uses the non-null assertion `!` (`expect(task2.previous!.target).to.eq(task1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1599 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:633:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1600 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:633:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1601 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:634:39`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1602 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:634:43`

This uses the non-null assertion `!` (`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1603 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:684:37`

This line casts to `any` with `as any` (`expect(() => Obj.isDeleted({} as any)).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1604 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:708:35`

This line casts to `any` with `as any` (`expect(() => Obj.getMeta({} as any)).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1605 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:717:74`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => Obj.getMeta(obj).keys.push(1 as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1606 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:839:40`

This uses the non-null assertion `!` (`expect(Obj.getMeta(obj.objects[0]!.target!).keys).to.deep.eq([key]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1607 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:839:48`

This uses the non-null assertion `!` (`expect(Obj.getMeta(obj.objects[0]!.target!).keys).to.deep.eq([key]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1608 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:1038:47`

This uses the non-null assertion `!` (`expect(dbObj.queue.uri).to.include(queueId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1609 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:1063:12`

This line casts to `any` with `as any` (`(obj as any).id = '123';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1610 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:98:35`

This declares a widened `any` type (`_proxyMap = new WeakMap<object, any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1611 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:102:24`

This line casts to `any` with `as any` (`invariant(!(target as any)[symbolIsProxy]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1612 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:109:26`

This line casts to `any` with `as any` (`delete (target as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1613 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:167:61`

This declares a widened `any` type (`get(target: ProxyTarget, prop: string | symbol, receiver: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1614 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:167:67`

This declares a widened `any` type (`get(target: ProxyTarget, prop: string | symbol, receiver: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1615 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:217:58`

This declares a widened `any` type (`set(target: ProxyTarget, prop: string | symbol, value: any, receiver: any): boolean {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1616 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:217:73`

This declares a widened `any` type (`set(target: ProxyTarget, prop: string | symbol, value: any, receiver: any): boolean {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1617 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:293:22`

This line casts to `any` with `as any` (`return array as any as ProxyTarget;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1618 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:348:73`

This declares a widened `any` type (`private _validateValue(target: ProxyTarget, path: Doc.KeyPath, value: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1619 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:348:79`

This declares a widened `any` type (`private _validateValue(target: ProxyTarget, path: Doc.KeyPath, value: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1620 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:377:62`

This declares a widened `any` type (`private _handleLinksAssignment(target: ProxyTarget, value: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1621 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:377:68`

This declares a widened `any` type (`private _handleLinksAssignment(target: ProxyTarget, value: any): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1622 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:429:63`

This declares a widened `any` type (`arrayPush(target: ProxyTarget, path: Doc.KeyPath, ...items: any[]): number {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1623 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:438:53`

This declares a widened `any` type (`arrayPop(target: ProxyTarget, path: Doc.KeyPath): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1624 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:442:22`

This declares a widened `any` type (`let returnValue: any | undefined;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1625 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:443:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1626 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:452:55`

This declares a widened `any` type (`arrayShift(target: ProxyTarget, path: Doc.KeyPath): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1627 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:456:22`

This declares a widened `any` type (`let returnValue: any | undefined;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1628 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:457:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1629 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:466:66`

This declares a widened `any` type (`arrayUnshift(target: ProxyTarget, path: Doc.KeyPath, ...items: any[]): number {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1630 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:473:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1631 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:483:102`

This declares a widened `any` type (`arraySplice(target: ProxyTarget, path: Doc.KeyPath, start: number, deleteCount?: number, ...items: any[]): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1632 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:483:110`

This declares a widened `any` type (`arraySplice(target: ProxyTarget, path: Doc.KeyPath, start: number, deleteCount?: number, ...items: any[]): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1633 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:490:26`

This declares a widened `any` type (`let deletedElements: any[] | undefined;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1634 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:491:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1635 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:505:70`

This declares a widened `any` type (`arraySort(target: ProxyTarget, path: Doc.KeyPath, compareFn?: (v1: any, v2: any) => number): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1636 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:505:79`

This declares a widened `any` type (`arraySort(target: ProxyTarget, path: Doc.KeyPath, compareFn?: (v1: any, v2: any) => number): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1637 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:505:96`

This declares a widened `any` type (`arraySort(target: ProxyTarget, path: Doc.KeyPath, compareFn?: (v1: any, v2: any) => number): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1638 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:509:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1639 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:516:32`

This declares a widened `any` type (`return target as EchoArray<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1640 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:519:57`

This declares a widened `any` type (`arrayReverse(target: ProxyTarget, path: Doc.KeyPath): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1641 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:523:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1642 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:530:32`

This declares a widened `any` type (`return target as EchoArray<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1643 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:536:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1644 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:548:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1645 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:589:41`

This declares a widened `any` type (`createRef(target: ProxyTarget, proxy: any): URI.URI {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1646 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:598:37`

This line casts to `any` with `as any` (`const selfUri = (otherEchoObj as any)[SelfURIId];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1647 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:682:42`

This declares a widened `any` type (`target[symbolInternals].change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1648 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:691:76`

This declares a widened `any` type (`private _validateForArray(target: ProxyTarget, path: Doc.KeyPath, items: any[], start: number) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1649 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:698:55`

This declares a widened `any` type (`private _encodeForArray(target: ProxyTarget, items: any[] | undefined): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1650 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:698:75`

This declares a widened `any` type (`private _encodeForArray(target: ProxyTarget, items: any[] | undefined): any[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1651 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:712:24`

This declares a widened `any` type (`inspectFn: (value: any, options?: InspectOptionsStylized) => string,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1652 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:732:70`

This declares a widened `any` type (`export const throwIfCustomClass = (prop: Doc.KeyPath[number], value: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-1653 no-compat-shims `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:745`

`getObjectCore` and `isRootDataObject` (line 757) are re-exported here with the comment "Re-export from echo-object-utils for backward compatibility" even though both are actually implemented in `./echo-object-utils.ts`. Several call sites (`text.ts`, `clone.ts`, `subscription.ts`, `edit-history.ts`, `branching.ts`, `echo-handler.test.ts`, `edit-history.test.ts`) still import `getObjectCore` from `./echo-handler` instead of the real module. Per `no-compat-shims`, update those call sites to import directly from `./echo-object-utils` and delete the two re-export lines.

# ERROR 50877ea571-1654 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:754:48`

This uses the non-null assertion `!` (`return getDeep(core.getDoc(), core.mountPath)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1655 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:763:35`

This declares a widened `any` type (`const isEchoObjectField = (value: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1656 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:774:39`

This declares a widened `any` type (`const isDetachedObjectField = (value: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1657 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:797:10`

This declares a widened `any` type (`value: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1658 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:808:43`

This declares a widened `any` type (`export const isTypedObjectProxy = (value: any): value is any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1659 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:808:58`

This declares a widened `any` type (`export const isTypedObjectProxy = (value: any): value is any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1660 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:832:32`

This line uses the double-cast escape hatch `as unknown as T` (`const type = Obj.getType(obj as unknown as Obj.Unknown);`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1661 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:844:64`

This line uses the double-cast escape hatch `as unknown as T` (`const meta = getProxyTarget<EntityMeta>(Entity.getMeta(obj as unknown as Entity.Unknown));`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1662 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:864:103`

This uses the non-null assertion `!` (`Object.defineProperty(target, symbol, Object.getOwnPropertyDescriptor(previousState, symbol)!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1663 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:908:90`

This line casts to `any` with `as any` (`const target = createRecordTarget(createInstanceState(core, DATA_NAMESPACE, []), obj as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1664 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:934:29`

This line casts to `any` with `as any` (`const seededMeta = (obj as any)[MetaId] as EntityMeta | undefined;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1665 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:939:18`

This line uses the double-cast escape hatch `as unknown as T` (`return proxy as unknown as CreateObjectReturn<T>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1666 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:972:31`

This line casts to `any` with `as any` (`const parentValue = (target as any)[ParentId];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1667 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:978:20`

This line casts to `any` with `as any` (`delete (target as any)[ParentId];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1668 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1002:78`

This line casts to `any` with `as any` (`const obj = createProxy<ProxyTarget>(target, EchoReactiveHandler.instance) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1669 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1036:31`

This line casts to `any` with `as any` (`const sourceRef = (target as any)[RelationSourceId];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1670 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1037:31`

This line casts to `any` with `as any` (`const targetRef = (target as any)[RelationTargetId];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1671 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1053:39`

This declares a widened `any` type (`const validateInitialProps = (target: any, seen: Set<object> = new Set()) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1672 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1068:25`

This line casts to `any` with `as any` (`} else if ((value as any) instanceof Uint8Array) {`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1673 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1107:62`

This declares a widened `any` type (`const refToEncodedReference = (target: ProxyTarget, ref: Ref<any>): EncodedReference => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1674 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-object-utils.ts:17:23`

This line casts to `any` with `as any` (`const target = (obj as any as ProxyTarget)[symbolInternals];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1675 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-object-utils.ts:40:37`

This declares a widened `any` type (`export const isEchoObject = (value: any): value is Obj.Unknown => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1676 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:137:16`

This declares a widened `any` type (`const value: any = target[symbolInternals].getDecoded(fullPath);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1677 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:141:50`

This declares a widened `any` type (`export const getReified = (target: ProxyTarget): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1678 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:244:20`

This line casts to `any` with `as any` (`return (schema as any)[SchemaMetaSymbol].typename;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1679 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:254:80`

This declares a widened `any` type (`const resolveStrongDepFromWorkingSet = (database: EchoDatabase, uri: URI.URI): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1680 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:267:68`

This declares a widened `any` type (`const resolveEndpoint = (core: ObjectCore, ref: EncodedReference): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1681 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:272:49`

This declares a widened `any` type (`export const getParent = (target: ProxyTarget): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1682 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:277:55`

This declares a widened `any` type (`export const setParent = (target: ProxyTarget, value: any): void => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1683 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:288:57`

This declares a widened `any` type (`export const getRelationSource = (target: ProxyTarget): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1684 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:294:57`

This declares a widened `any` type (`export const getRelationTarget = (target: ProxyTarget): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1685 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:304:55`

This declares a widened `any` type (`const getSchemaKind = (target: ProxyTarget, receiver: any): EntityKind | undefined => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1686 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:307:44`

This line casts to `any` with `as any` (`const jsonSchemaEntityKind = (receiver as any).jsonSchema?.entityKind;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1687 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:319:65`

This declares a widened `any` type (`const getStaticTypeSchemaSlot = (target: ProxyTarget, receiver: any): Schema.Schema.AnyNoContext | undefined => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1688 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:360:57`

This line casts to `any` with `as any` (`return createProxy(metaTarget, getProxyHandler(self)) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1689 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:368:65`

This declares a widened `any` type (`export const handleStoredSchema = (target: ProxyTarget, object: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1690 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:368:71`

This declares a widened `any` type (`export const handleStoredSchema = (target: ProxyTarget, object: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1691 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:376:83`

This declares a widened `any` type (`export const lookupRef = (target: ProxyTarget, encodedRef: EncodedReference): Ref<any> | undefined => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1692 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:413:45`

This line uses the double-cast escape hatch `as unknown as T` (`[ATTR_META]: compactMeta(getMeta(self)) as unknown as EntityMetaJSON,`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1693 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:451:23`

This declares a widened `any` type (`header: (config?: any) => getHeader(typename ?? 'EchoObjectSchema', target[symbolInternals].id, config),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1694 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:552:21`

This declares a widened `any` type (`get [ParentId](): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1695 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:556:25`

This declares a widened `any` type (`set [ParentId](value: any) {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1696 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:562:45`

This declares a widened `any` type (`get [ChangeId](): (callback: (mutableObj: any) => void) => void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1697 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:570:36`

This declares a widened `any` type (`return (callback: (mutableObj: any) => void) => executeChange(core, target, receiver, callback);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1698 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:583:29`

This declares a widened `any` type (`get [RelationSourceId](): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1699 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:587:29`

This declares a widened `any` type (`get [RelationTargetId](): any {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1700 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:698:22`

This line casts to `any` with `as any` (`delete (target as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1701 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:705:10`

This declares a widened `any` type (`value: any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1702 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-proxy-target.ts:63:20`

This declares a widened `any` type (`} & ({ [key: keyof any]: any } | EchoArray<any>);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1703 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-proxy-target.ts:63:26`

This declares a widened `any` type (`} & ({ [key: keyof any]: any } | EchoArray<any>);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1704 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-proxy-target.ts:63:44`

This declares a widened `any` type (`} & ({ [key: keyof any]: any } | EchoArray<any>);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1705 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.test.ts:79:22`

This line casts to `any` with `as any` (`expect((snapshot as any).description).toBe('aaa');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1706 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:22:60`

This declares a widened `any` type (`export const getEditHistory = (object: Obj.Unknown): State<any>[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1707 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:27:43`

This declares a widened `any` type (`const changes = A.getHistory(doc as Doc<any>);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1708 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:67:42`

This declares a widened `any` type (`const doc = objectCore.getDoc() as Doc<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1709 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:71:28`

This declares a widened `any` type (`let accumulator = A.init<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1710 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:132:38`

This declares a widened `any` type (`const snapshot = A.view(doc as Doc<any>, version);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1711 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:148:5`

This line casts to `any` with `as any` (`} as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1712 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:161:56`

This declares a widened `any` type (`const historical = A.view(objectCore.getDoc() as Doc<any>, version);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1713 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:91:26`

This uses the non-null assertion `!` (`expect(obj.nested!.field).to.eq('bar');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1714 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:92:26`

This uses the non-null assertion `!` (`expect(obj.nested!).to.deep.eq(plainObject);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1715 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:95:21`

This uses the non-null assertion `!` (`obj.nested!.field = 'baz';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1716 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:97:26`

This uses the non-null assertion `!` (`expect(obj.nested!.field).to.eq('baz');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1717 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:118:26`

This uses the non-null assertion `!` (`obj.stringArray![0] = '4';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1718 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:124:23`

This declares a widened `any` type (`const circle: any = { field: 'circle' };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1719 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:126:39`

This uses the non-null assertion `!` (`expect(obj.nestedNullableArray![0]).to.deep.eq(circle);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1720 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:133:23`

This declares a widened `any` type (`const square: any = { field: 'square' };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1721 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:140:35`

This uses the non-null assertion `!` (`(obj.nestedNullableArray![2] as any).field = 'rectangle';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1722 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:140:40`

This line casts to `any` with `as any` (`(obj.nestedNullableArray![2] as any).field = 'rectangle';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1723 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:142:40`

This uses the non-null assertion `!` (`expect((obj.nestedNullableArray![2] as any).field).to.eq('rectangle');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1724 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:142:45`

This line casts to `any` with `as any` (`expect((obj.nestedNullableArray![2] as any).field).to.eq('rectangle');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1725 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:152:63`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.string = 1 as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1726 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:153:74`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.nested = { field: 1 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1727 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:155:58`

This uses the non-null assertion `!` (`expect(() => Obj.update(obj, (obj) => (obj.nested!.field = 1 as any))).to.throw();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1728 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:155:70`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.nested!.field = 1 as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1729 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:156:82`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.push({ field: 1 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1730 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:157:85`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.unshift({ field: 1 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1731 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:158:63`

This uses the non-null assertion `!` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0] = { field: 1 } as any))).to.throw();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1732 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:158:83`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0] = { field: 1 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1733 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:159:63`

This uses the non-null assertion `!` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0].field = 1 as any))).to.throw();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1734 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:159:78`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0].field = 1 as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1735 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:161:90`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.splice(1, 0, { field: 1 } as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1736 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:162:63`

This uses the non-null assertion `!` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![1].field = 1 as any))).to.throw();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1737 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:162:78`

This line casts to `any` with `as any` (`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![1].field = 1 as any))).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1738 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:177:31`

This uses the non-null assertion `!` (`expect(obj.nestedArray![0].field).to.eq('bar');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1739 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:180:26`

This uses the non-null assertion `!` (`obj.nestedArray![0].field = 'baz';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1740 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:182:31`

This uses the non-null assertion `!` (`expect(obj.nestedArray![0].field).to.eq('baz');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1741 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:185:26`

This uses the non-null assertion `!` (`obj.nestedArray![1].field = 'bar';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1742 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:187:31`

This uses the non-null assertion `!` (`expect(obj.nestedArray![1].field).to.eq('bar');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1743 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:199:37`

This uses the non-null assertion `!` (`expect(obj.twoDimNumberArray![0][0]).to.eq(1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1744 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:202:32`

This uses the non-null assertion `!` (`obj.twoDimNumberArray![0][0] = 4;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1745 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:204:37`

This uses the non-null assertion `!` (`expect(obj.twoDimNumberArray![0][0]).to.eq(4);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1746 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:237:26`

This uses the non-null assertion `!` (`expect(obj.nested!.field).to.eq('bar');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1747 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:243:21`

This uses the non-null assertion `!` (`obj.nested!.field = 'baz';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1748 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:246:26`

This uses the non-null assertion `!` (`expect(obj.nested!.field).to.eq('baz');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1749 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:299:68`

This line casts to `any` with `as any` (`expect({ ...obj }).to.deep.eq({ ...TEST_OBJECT, id: (obj as any).id });`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1750 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:313:53`

This line casts to `any` with `as any` (`expect(actual).to.deep.contain({ id: (obj as any).id, ...expected });`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1751 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:319:69`

This line casts to `any` with `as any` (`const expected = objectsHaveId ? { ...TEST_OBJECT, id: (obj as any).id } : TEST_OBJECT;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1752 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:326:69`

This line casts to `any` with `as any` (`const expected = objectsHaveId ? { ...TEST_OBJECT, id: (obj as any).id } : TEST_OBJECT;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1753 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:401:23`

This uses the non-null assertion `!` (`obj.nested!.field = 'baz';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1754 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:412:28`

This uses the non-null assertion `!` (`obj.stringArray![0] = '42';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1755 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:423:28`

This uses the non-null assertion `!` (`obj.nestedArray![0].field = 'baz';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1756 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:434:34`

This uses the non-null assertion `!` (`obj.twoDimNumberArray![0][0] = 4;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1757 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:461:42`

This uses the non-null assertion `!` (`return { array: obj.stringArray!, parent: obj };`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1758 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:469:31`

This uses the non-null assertion `!` (`parent.stringArray![0] = '2';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1759 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:481:31`

This uses the non-null assertion `!` (`parent.stringArray!.push('4');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1760 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:492:31`

This uses the non-null assertion `!` (`parent.stringArray!.length = 2;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1761 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:503:31`

This uses the non-null assertion `!` (`parent.stringArray!.push('4');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1762 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:515:39`

This uses the non-null assertion `!` (`value = parent.stringArray!.pop();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1763 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:528:39`

This uses the non-null assertion `!` (`value = parent.stringArray!.shift();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1764 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:541:43`

This uses the non-null assertion `!` (`newLength = parent.stringArray!.unshift('0');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1765 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:554:41`

This uses the non-null assertion `!` (`removed = parent.stringArray!.splice(1, 1, '4');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1766 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:567:45`

This uses the non-null assertion `!` (`returnValue = parent.stringArray!.sort();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1767 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:586:45`

This uses the non-null assertion `!` (`returnValue = parent.stringArray!.reverse();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1768 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:617:46`

This uses the non-null assertion `!` (`const array = obj.twoDimNumberArray!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1769 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.test.ts:21:40`

This line casts to `any` with `as any` (`return Obj.make(schema, props) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1770 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.test.ts:42:47`

This line casts to `any` with `as any` (`const object = Obj.make(schema, props as any) as TestSchema.Example;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1771 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.test.ts:299:38`

This declares a widened `any` type (`const createUpdateCounter = (object: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1772 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:10:25`

This declares a widened `any` type (`export type Selection = any[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1773 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:17:17`

This declares a widened `any` type (`selected: Set<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1774 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:22:12`

This declares a widened `any` type (`updated: any[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1775 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:23:10`

This declares a widened `any` type (`added: any[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1776 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:24:12`

This declares a widened `any` type (`removed: any[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1777 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:39:33`

This declares a widened `any` type (`const subscriptions = new Map<any, CleanupFn>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1778 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.ts:78:23`

This declares a widened `any` type (`selected: new Set<any>(),`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1779 no-casts `packages/core/echo/echo-client/src/echo-handler/version.ts:23:33`

This declares a widened `any` type (`export const getVersion = (obj: any): ObjectVersion => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1780 no-casts `packages/core/echo/echo-client/src/feed/feed-handle.ts:565:43`

This uses the non-null assertion `!` (`clearTimeout(this._pollingInterval!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1781 no-casts `packages/core/echo/echo-client/src/feed/feed-handle.ts:601:36`

This declares a widened `any` type (`const isSqliteNotOpenError = (err: any) => err.cause?.message?.includes('The database connection is not open');`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1782 no-casts `packages/core/echo/echo-client/src/feed/feed-object-core.ts:248:30`

This line uses the double-cast escape hatch `as unknown as T` (`const mutable = target as unknown as Record<string, unknown>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1783 no-casts `packages/core/echo/echo-client/src/feed/feed-object-core.ts:249:35`

This line uses the double-cast escape hatch `as unknown as T` (`const sourceRecord = source as unknown as Record<string, unknown>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1784 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:115:33`

This declares a widened `any` type (`expect(results.map((item: any) => item.name).sort()).toEqual(['alice', 'bob']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1785 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:399:36`

This uses the non-null assertion `!` (`const ref = container.objects![0];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1786 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:401:67`

This line casts to `any` with `as any` (`expect(EID.getEntityId(EID.parse(ref.uri))).toBe((queuePost as any).id);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1787 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:409:24`

This line casts to `any` with `as any` (`expect((resolved as any).name).toBe('alice');`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1788 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:433:37`

This uses the non-null assertion `!` (`postRefUri = container.objects![0]!.uri;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1789 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:433:41`

This uses the non-null assertion `!` (`postRefUri = container.objects![0]!.uri;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1790 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:440:68`

This uses the non-null assertion `!` (`await using db2 = await peer.openDatabase(spaceKey, db1.rootUrl!, { client: client2 });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1791 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:444:38`

This uses the non-null assertion `!` (`const postRef = container.objects![0]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1792 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:444:42`

This uses the non-null assertion `!` (`const postRef = container.objects![0]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1793 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:450:48`

This uses the non-null assertion `!` (`const postId = EID.getEntityId(postRef.uri)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1794 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:470:14`

This declares a definite-assignment assertion with `!` (`let feedA!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1795 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:471:14`

This declares a definite-assignment assertion with `!` (`let feedB!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1796 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:490:19`

This declares a definite-assignment assertion with `!` (`let dataFeed!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1797 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:491:20`

This declares a definite-assignment assertion with `!` (`let traceFeed!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1798 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:524:20`

This declares a definite-assignment assertion with `!` (`let traceFeed!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1799 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:551:20`

This declares a definite-assignment assertion with `!` (`let traceFeed!: Feed.Feed;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-1800 no-casts `packages/core/echo/echo-client/src/proxy-db/add-type.test.ts:55:34`

This line casts to `any` with `as any` (`expect(() => db.add(TestType as any)).to.throw();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1801 no-casts `packages/core/echo/echo-client/src/proxy-db/add-type.test.ts:62:48`

This uses the non-null assertion `!` (`expect(Type.getTypename(Obj.getType(object)!)).to.eq(Type.getTypename(TestType));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1802 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:73:27`

This uses the non-null assertion `!` (`rootUrl = db.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# WARN 50877ea571-1803 no-sleep-in-test `packages/core/echo/echo-client/src/proxy-db/database.test.ts:78`

`await sleep(500); // Wait for the object to be saved.` waits an arbitrary period for the object to persist to storage before closing the peer and reopening to check it round-tripped, rather than awaiting an explicit save/flush completion.

# ERROR 50877ea571-1804 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:197:27`

This uses the non-null assertion `!` (`rootUrl = db.rootUrl!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1805 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:235:44`

This uses the non-null assertion `!` (`expect(Type.getSchema(Obj.getType(task)!).ast).to.eq(Type.getSchema(TestSchema.Task).ast);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1806 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:262:31`

This uses the non-null assertion `!` (`expect(container.records![0].type).to.eq(TestSchema.RecordType.WORK);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1807 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:274:26`

This uses the non-null assertion `!` (`container.objects!.push(Ref.make(Obj.make(TestSchema.Expando, { foo: 100 })));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1808 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:275:26`

This uses the non-null assertion `!` (`container.objects!.push(Ref.make(Obj.make(TestSchema.Expando, { bar: 200 })));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1809 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:283:46`

This uses the non-null assertion `!` (`const target1 = await container.objects![0].load();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1810 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:284:46`

This uses the non-null assertion `!` (`const target2 = await container.objects![1].load();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1811 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:288:23`

This line casts to `any` with `as any` (`expect((target1 as any).foo).to.equal(100);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1812 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:289:23`

This line casts to `any` with `as any` (`expect((target2 as any).bar).to.equal(200);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1813 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:301:26`

This uses the non-null assertion `!` (`container.objects!.push(Ref.make(Obj.make(TestSchema.Task, {})));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1814 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:302:26`

This uses the non-null assertion `!` (`container.objects!.push(Ref.make(Obj.make(TestSchema.Person, {})));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1815 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:310:47`

This uses the non-null assertion `!` (`expect(Obj.getTypename(container.objects![0].target!)).to.equal(Type.getTypename(TestSchema.Task));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1816 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:310:58`

This uses the non-null assertion `!` (`expect(Obj.getTypename(container.objects![0].target!)).to.equal(Type.getTypename(TestSchema.Task));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1817 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:311:47`

This uses the non-null assertion `!` (`expect(Obj.getTypename(container.objects![1].target!)).to.equal(Type.getTypename(TestSchema.Person));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1818 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:311:58`

This uses the non-null assertion `!` (`expect(Obj.getTypename(container.objects![1].target!)).to.equal(Type.getTypename(TestSchema.Person));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1819 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:384:41`

This uses the non-null assertion `!` (`expect(Obj.getTypename(task.subTasks![0].target!)).to.eq('com.example.type.task');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1820 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:384:52`

This uses the non-null assertion `!` (`expect(Obj.getTypename(task.subTasks![0].target!)).to.eq('com.example.type.task');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1821 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:385:51`

This uses the non-null assertion `!` (`expect(JSON.parse(JSON.stringify(task.subTasks![0].target))['@type']).to.eq('dxn:com.example.type.task:0.1.0');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1822 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:391:39`

This line casts to `any` with `as any` (`const version1 = Obj.version(task as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1823 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:395:39`

This line casts to `any` with `as any` (`const version2 = Obj.version(task as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1824 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:403:39`

This line casts to `any` with `as any` (`const version3 = Obj.version(task as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1825 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:416:45`

This uses the non-null assertion `!` (`range(3).forEach(() => root.subTasks!.push(Ref.make(newTask())));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1826 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:417:22`

This uses the non-null assertion `!` (`root.subTasks!.push(Ref.make(newTask()), Ref.make(newTask()));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1827 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:421:27`

This uses the non-null assertion `!` (`expect(root.subTasks!.length).to.eq(5);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1828 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:425:32`

This uses the non-null assertion `!` (`const ids = root.subTasks!.map((task: any) => task.target!.id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1829 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:425:45`

This declares a widened `any` type (`const ids = root.subTasks!.map((task: any) => task.target!.id);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1830 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:425:64`

This uses the non-null assertion `!` (`const ids = root.subTasks!.map((task: any) => task.target!.id);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1831 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:426:20`

This uses the non-null assertion `!` (`root.subTasks!.forEach((task: any, i: number) => expect(task.target!.id).to.eq(ids[i]));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1832 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:426:37`

This declares a widened `any` type (`root.subTasks!.forEach((task: any, i: number) => expect(task.target!.id).to.eq(ids[i]));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1833 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:426:74`

This uses the non-null assertion `!` (`root.subTasks!.forEach((task: any, i: number) => expect(task.target!.id).to.eq(ids[i]));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1834 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:427:38`

This uses the non-null assertion `!` (`expect(Array.from(root.subTasks!.values())).to.have.length(5);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1835 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:436:27`

This uses the non-null assertion `!` (`expect(root.subTasks!.length).to.eq(3);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1836 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:447:22`

This uses the non-null assertion `!` (`root.subTasks!.splice(0, 2, Ref.make(newTask()));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1837 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:456:21`

This uses the non-null assertion `!` (`root.records!.push({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1838 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:464:92`

This uses the non-null assertion `!` (`const queriedContainer = (await db.query(Filter.type(TestSchema.Container)).run())[0]!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1839 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:465:38`

This uses the non-null assertion `!` (`expect(queriedContainer.records!.length).to.equal(1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1840 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:466:38`

This uses the non-null assertion `!` (`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1841 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:466:42`

This uses the non-null assertion `!` (`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1842 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:466:52`

This uses the non-null assertion `!` (`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1843 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:466:56`

This uses the non-null assertion `!` (`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1844 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:466:64`

This uses the non-null assertion `!` (`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1845 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:473:21`

This uses the non-null assertion `!` (`root.records!.push({ title: 'one' });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1846 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:485:21`

This uses the non-null assertion `!` (`root.records!.push({ title: 'two' });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1847 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:706:37`

This declares a widened `any` type (`const expectObjects = (echoObjects: any[], expectedObjects: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1848 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:706:61`

This declares a widened `any` type (`const expectObjects = (echoObjects: any[], expectedObjects: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1849 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:710:40`

This declares a widened `any` type (`const mapEchoToPlainJsObject = (array: any[]): any[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1850 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:710:48`

This declares a widened `any` type (`const mapEchoToPlainJsObject = (array: any[]): any[] => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1851 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:123:46`

This declares a widened `any` type (`getLoadedDocumentHandles(): DocHandleProxy<any>[];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1852 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:459:37`

This declares a widened `any` type (`makeRef<T extends AnyProperties = any>(uri: URI.URI): Ref.Ref<T> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1853 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:522:20`

This line uses the double-cast escape hatch `as unknown as T` (`return match as unknown as T;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1854 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:525:44`

This line uses the double-cast escape hatch `as unknown as T` (`return this._addPersistentSchema(type) as unknown as T;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1855 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:721:74`

This line casts to `any` with `as any` (`const output = (await migration.transform(object, { db: this })) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1856 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:727:24`

This line casts to `any` with `as any` (`delete (output as any).id;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1857 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:732:27`

This line casts to `any` with `as any` (`meta: metaPatch as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1858 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:850:46`

This declares a widened `any` type (`getLoadedDocumentHandles(): DocHandleProxy<any>[] {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1859 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:995:52`

This declares a widened `any` type (`async _loadObjectById(objectId: string, options: any = {}): Promise<Entity.Unknown | undefined> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1860 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:1016:43`

This declares a widened `any` type (`private readonly _rootProxies = new Map<any, Entity.Unknown>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1861 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:1020:50`

This declares a widened `any` type (`const createSchemaNotRegisteredError = (schema?: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1862 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:33:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1863 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:34:19`

This declares a widened `any` type (`const loaded: any = await restartedDb.query(Filter.id(object.id)).first();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1864 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:56:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1865 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:57:19`

This declares a widened `any` type (`const loaded: any = await restartedDb.query(Filter.id(object.id)).first();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1866 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:59:81`

This line casts to `any` with `as any` (`const [foo, bar] = await loadObjectReferences(loaded, (o) => [o.foo, o.bar] as any[]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1867 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:79:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1868 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:80:19`

This declares a widened `any` type (`const loaded: any = await restartedDb.query(Filter.id(object.id)).first();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1869 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:81:32`

This line casts to `any` with `as any` (`expect((loaded.nestedArray as any[]).every((v) => v == null)).to.be.true;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1870 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:82:81`

This line casts to `any` with `as any` (`const loadedArray = await loadObjectReferences(loaded, (o) => o.nestedArray as any[]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1871 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:111:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1872 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:112:19`

This declares a widened `any` type (`const loaded: any[] = await Promise.all(objects.map((o) => restartedDb.query(Filter.id(o.id)).first()));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1873 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:113:82`

This line casts to `any` with `as any` (`const loadedArrays = await loadObjectReferences(loaded, (o) => o.nestedArray as any[]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1874 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:133:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1875 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:134:19`

This declares a widened `any` type (`const loaded: any = await restartedDb.query(Filter.id(object.id)).first();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1876 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:152:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1877 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:153:19`

This declares a widened `any` type (`const loaded: any = await restartedDb.query(Filter.id(object.id)).first();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1878 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:186:78`

This uses the non-null assertion `!` (`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1879 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:188:59`

This uses the non-null assertion `!` (`const loadedNested = await loadObjectReferences(loaded!, (o) => o.nested.map((n: any) => n.target));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1880 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:188:86`

This declares a widened `any` type (`const loadedNested = await loadObjectReferences(loaded!, (o) => o.nested.map((n: any) => n.target));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1881 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:17:50`

This line casts to `any` with `as any` (`return getObjectCore(obj).getDecoded(['data']) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1882 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:43:36`

This line casts to `any` with `as any` (`const core = getObjectCore(obj as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1883 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:51:35`

This line casts to `any` with `as any` (`? () => (valueAccessor(obj) as any[]).every((v) => v != null)`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1884 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:65:59`

This line casts to `any` with `as any` (`return (Array.isArray(objOrArray) ? result : result[0]) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1885 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:66:86`

This uses the non-null assertion `!` (`const validator = Schema.validateSync(Type.getSchema(instanceWithSchemaRef.schema!.target!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1886 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:66:94`

This uses the non-null assertion `!` (`const validator = Schema.validateSync(Type.getSchema(instanceWithSchemaRef.schema!.target!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1887 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:80:57`

This uses the non-null assertion `!` (`expect(Type.getTypename(instanceWithSchemaRef.schema!.target!)).to.eq(Type.getTypename(GeneratedSchema));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1888 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:80:65`

This uses the non-null assertion `!` (`expect(Type.getTypename(instanceWithSchemaRef.schema!.target!)).to.eq(Type.getTypename(GeneratedSchema));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1889 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:93:40`

This uses the non-null assertion `!` (`instanceWithSchemaRef.schemaArray!.push(Ref.make(schema));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1890 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:95:62`

This uses the non-null assertion `!` (`expect(Type.getTypename(instanceWithSchemaRef.schemaArray![0].target!)).to.eq(Type.getTypename(GeneratedSchema));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1891 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:95:73`

This uses the non-null assertion `!` (`expect(Type.getTypename(instanceWithSchemaRef.schemaArray![0].target!)).to.eq(Type.getTypename(GeneratedSchema));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1892 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:120:46`

This uses the non-null assertion `!` (`expect(Type.getSchema(Obj.getType(object)!).ast).to.deep.eq(Type.getSchema(schema).ast);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1893 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:132:36`

This uses the non-null assertion `!` (`const uri = Type.getURI(schema)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1894 no-casts `packages/core/echo/echo-client/src/proxy-db/relations.test.ts:69:31`

This uses the non-null assertion `!` (`await testBuilder.lastPeer!.reload();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1895 no-casts `packages/core/echo/echo-client/src/proxy-db/relations.test.ts:71:44`

This uses the non-null assertion `!` (`const db = await testBuilder.lastPeer!.openLastDatabase();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1896 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:238:24`

This uses the non-null assertion `!` (`if (this._results!.find((result) => result.id === objectId)) {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1897 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:243:57`

This uses the non-null assertion `!` (`const trivial = isSimpleSelectionQuery(this._query!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1898 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:300:55`

This uses the non-null assertion `!` (`this._results = this._computeResults(this._query!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1899 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:303:25`

This uses the non-null assertion `!` (`return this._results!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1900 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:369:77`

This uses the non-null assertion `!` (`? { key: item.groupKey, count: groupCounts?.get(serializedGroupKey!) ?? 1 }`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1901 no-casts `packages/core/echo/echo-client/src/query/query-result-cache.ts:33:72`

This declares a widened `any` type (`readonly #byKey = new WeakDictionary<string, QueryResult.QueryResult<any>>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1902 no-casts `packages/core/echo/echo-client/src/query/query-result-cache.ts:38:72`

This declares a widened `any` type (`getOrCreate(query: Query.Any, factory: () => QueryResult.QueryResult<any>): QueryResult.QueryResult<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1903 no-casts `packages/core/echo/echo-client/src/query/query-result-cache.ts:38:103`

This declares a widened `any` type (`getOrCreate(query: Query.Any, factory: () => QueryResult.QueryResult<any>): QueryResult.QueryResult<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1904 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:61:29`

This uses the non-null assertion `!` (`return this._resultCache!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1905 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:67:29`

This uses the non-null assertion `!` (`return this._objectCache!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1906 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:113:29`

This uses the non-null assertion `!` (`return this._objectCache!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1907 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:123:29`

This uses the non-null assertion `!` (`return this._resultCache!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1908 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:194:29`

This line uses the double-cast escape hatch `as unknown as T` (`this._objectCache as unknown as GroupResult[] | undefined,`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1909 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:195:29`

This line uses the double-cast escape hatch `as unknown as T` (`presented.objects as unknown as GroupResult[],`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1910 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:224:25`

This line uses the double-cast escape hatch `as unknown as T` (`objects: groups as unknown as T[],`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1911 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:225:31`

This line uses the double-cast escape hatch `as unknown as T` (`entries: groupEntries as unknown as QueryResult.EntityEntry<T>[],`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1912 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:238:24`

This declares a widened `any` type (`.filter((object: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1913 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:331:33`

This uses the non-null assertion `!` (`members.get(serializedKey)!.push(entry.result);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1914 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:336:52`

This uses the non-null assertion `!` (`const groupMembers = members.get(serializedKey)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1915 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:338:61`

This uses the non-null assertion `!` (`const record: GroupResult = { ...keys.get(serializedKey)! };`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1916 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:343:100`

This uses the non-null assertion `!` (`record[aggregate.name] = _computeAggregate(aggregate, groupMembers, counts.get(serializedKey)!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1917 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:95:24`

This uses the non-null assertion `!` (`const ids = results!.map((item) => item.objectId);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1918 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:165:24`

This uses the non-null assertion `!` (`const ids = results!.map((item) => item.objectId);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1919 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:188:56`

This uses the non-null assertion `!` (`const names = personResults.map((item) => item.core!.getObjectStructure().data['name']);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1920 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:319:45`

This uses the non-null assertion `!` (`const first = firstIndexByAge.get(age)!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1921 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:460:62`

This declares a widened `any` type (`const planAndExecute = (db: DatabaseImpl, query: Query.Query<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1922 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:119:74`

This declares a widened `any` type (`private _query(query: Query.Any | Filter.Any): QueryResult.QueryResult<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1923 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:121:89`

This declares a widened `any` type (`return this.#queryResultCache.getOrCreate(normalized, () => new RegistryQueryResult<any>(this, normalized));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1924 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:297:61`

This uses the non-null assertion `!` (`return this.runSyncEntries().map((entry) => entry.result!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1925 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:305:24`

This line uses the double-cast escape hatch `as unknown as T` (`result: entity as unknown as T,`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1926 no-casts `packages/core/echo/echo-client/src/serializer.ts:48:41`

This line casts to `any` with `as any` (`return this.exportObject(object as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1927 no-casts `packages/core/echo/echo-client/src/testing/test-database-layer.ts:75:41`

This uses the non-null assertion `!` (`yield* Effect.promise(() => db!.flush());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1928 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:23:32`

This declares a widened `any` type (`export const getHeader = (obj: any /* TypedObject */, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1929 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:23:64`

This declares a widened `any` type (`export const getHeader = (obj: any /* TypedObject */, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1930 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:34:30`

This declares a widened `any` type (`const formatValue = (object: any, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1931 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:34:44`

This declares a widened `any` type (`const formatValue = (object: any, config?: any): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1932 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:44:30`

This declares a widened `any` type (`export const getBody = (obj: any /* TypedObject */): JsonML => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1933 no-casts `packages/core/echo/echo-client/src/util/migrate-document.ts:13:48`

This declares a widened `any` type (`export const migrateDocument = <T>(source: Doc<any>, targetData: T): Doc<T> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1934 no-casts `packages/core/echo/echo-client/src/util/migrate-document.ts:19:32`

This declares a widened `any` type (`const coalesce = (applyTo: any, targetData: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1935 no-casts `packages/core/echo/echo-client/src/util/migrate-document.ts:19:49`

This declares a widened `any` type (`const coalesce = (applyTo: any, targetData: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1936 no-casts `packages/core/echo/echo-doc/src/record.ts:17:31`

This declares a widened `any` type (`export const encode = (value: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1937 no-casts `packages/core/echo/echo-doc/src/record.ts:17:37`

This declares a widened `any` type (`export const encode = (value: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1938 no-casts `packages/core/echo/echo-doc/src/record.ts:47:31`

This declares a widened `any` type (`export const decode = (value: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1939 no-casts `packages/core/echo/echo-doc/src/record.ts:47:37`

This declares a widened `any` type (`export const decode = (value: any): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1940 no-casts `packages/core/echo/echo-doc/src/record.ts:80:30`

This declares a widened `any` type (`export const getDeep = (obj: any, path: Doc.KeyPath, init = false): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1941 no-casts `packages/core/echo/echo-doc/src/record.ts:80:69`

This declares a widened `any` type (`export const getDeep = (obj: any, path: Doc.KeyPath, init = false): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1942 no-casts `packages/core/echo/echo-doc/src/store-adapter.test.ts:24:32`

This declares a widened `any` type (`accessor.handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1943 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:51:20`

This declares a widened `any` type (`encode?: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1944 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:51:28`

This declares a widened `any` type (`encode?: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1945 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:53:20`

This declares a widened `any` type (`decode?: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1946 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:53:28`

This declares a widened `any` type (`decode?: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1947 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:64:28`

This declares a widened `any` type (`#accessor?: Doc.Accessor<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1948 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:68:29`

This declares a widened `any` type (`readonly #encode: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1949 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:68:37`

This declares a widened `any` type (`readonly #encode: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1950 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:69:29`

This declares a widened `any` type (`readonly #decode: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1951 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:69:37`

This declares a widened `any` type (`readonly #decode: (value: any) => any;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1952 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:89:31`

This declares a widened `any` type (`open(accessor: Doc.Accessor<any>): () => void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1953 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:208:36`

This uses the non-null assertion `!` (`const accessor = this.#accessor!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1954 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:218:72`

This uses the non-null assertion `!` (`return batch.deleted ? elements?.filter((element) => !batch.deleted!.includes(element.id)) : elements;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1955 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:62:49`

This uses the non-null assertion `!` (`schemaId.push(Obj.getTypeURI(organization)!.toString());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1956 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:69:49`

This uses the non-null assertion `!` (`schemaId.push(Obj.getTypeURI(organization)!.toString());`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1957 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:84:81`

This uses the non-null assertion `!` (`const beforeChangesCount = A.getAllChanges(getObjectCore(document).docHandle!.doc()).length;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1958 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:91:75`

This uses the non-null assertion `!` (`const changesCount = A.getAllChanges(getObjectCore(document).docHandle!.doc()).length;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1959 no-casts `packages/core/echo/echo-generator/src/generator.ts:45:64`

This declares a widened `any` type (`async createObject({ types }: { types?: T[] } = {}): Promise<any> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1960 no-casts `packages/core/echo/echo-generator/src/generator.ts:59:20`

This declares a widened `any` type (`const results: any[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1961 no-casts `packages/core/echo/echo-generator/src/generator.ts:64:46`

This line casts to `any` with `as any` (`const tasks = Object.entries<number>(map as any)`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1962 no-casts `packages/core/echo/echo-generator/src/generator.ts:137:54`

This uses the non-null assertion `!` (`const type = Type.getTypename(Obj.getType(object)!) as T;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1963 no-casts `packages/core/echo/echo-generator/src/generator.ts:140:26`

This uses the non-null assertion `!` (`await this._mutations![type](object, params);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1964 no-casts `packages/core/echo/echo-generator/src/types.ts:8:58`

This declares a widened `any` type (`export type TestObject = { id: string } & Record<string, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1965 no-casts `packages/core/echo/echo-generator/src/types.ts:12:82`

This declares a widened `any` type (`export type TestObjectProvider<T extends string = string> = (type: T) => Promise<any[]>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1966 no-casts `packages/core/echo/echo-generator/src/types.ts:16:52`

This declares a widened `any` type (`(provider: TestObjectProvider<T> | undefined) => any`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1967 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:36:41`

This declares a widened `any` type (`const handle = await host.createDoc<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1968 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:37:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1969 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:41:24`

This uses the non-null assertion `!` (`expect(handle.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1970 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:48:41`

This declares a widened `any` type (`const handle = await host.createDoc<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1971 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:49:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1972 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:58:41`

This declares a widened `any` type (`const handle2 = await host2.loadDoc<any>(Context.default(), url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1973 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:61:25`

This uses the non-null assertion `!` (`expect(handle2.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1974 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:89:48`

This uses the non-null assertion `!` (`const expectedHeads = getHeads(handle.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1975 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:110:95`

This uses the non-null assertion `!` (`const expectedHeads: (Heads | undefined)[] = handles.map((handle) => getHeads(handle.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1976 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:148:26`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('Hello from Subduction');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1977 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:178:24`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('Hello from Subduction');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1978 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:210:24`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('Hello from Subduction');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1979 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:289:28`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('authorized');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1980 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:395:15`

This line casts to `any` with `as any` (`((host1 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1981 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:395:29`

This line casts to `any` with `as any` (`((host1 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1982 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:398:15`

This line casts to `any` with `as any` (`((host2 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1983 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:398:29`

This line casts to `any` with `as any` (`((host2 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-1984 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:464:25`

This declares a widened `any` type (`h1.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1985 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:467:25`

This declares a widened `any` type (`h2.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-1986 no-sleep-in-test `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:795`

The locally-defined `waitForSubductionSave = async () => { await sleep(150); }` helper (used across many tests in this file) blindly waits 150ms for an async document save to complete instead of awaiting an actual save-completion signal (e.g. a flush promise or save event). Per the rule, prefer a `Trigger`/`waitForCondition` over a fixed-delay guess for this kind of positive completion wait.

# ERROR 50877ea571-1987 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:27:41`

This declares a widened `any` type (`const handle = await host.createDoc<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1988 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:28:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1989 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:32:24`

This uses the non-null assertion `!` (`expect(handle.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1990 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:82:41`

This declares a widened `any` type (`const handle = await host.createDoc<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1991 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:83:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1992 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:95:41`

This declares a widened `any` type (`const handle2 = await host2.loadDoc<any>(Context.default(), url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-1993 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:98:25`

This uses the non-null assertion `!` (`expect(handle2.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1994 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:130:50`

This uses the non-null assertion `!` (`const expectedHeads = A.getHeads(handle.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1995 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:152:97`

This uses the non-null assertion `!` (`const expectedHeads: (Heads | undefined)[] = handles.map((handle) => A.getHeads(handle.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1996 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:199:24`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1997 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:207:27`

This uses the non-null assertion `!` (`expect(localOnly.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-1998 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:246:28`

This line uses the double-cast escape hatch `as unknown as T` (`const adapter = (host2 as unknown as { _echoNetworkAdapter: EchoNetworkAdapter })._echoNetworkAdapter;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-1999 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:256:24`

This uses the non-null assertion `!` (`expect(loaded.doc()!.text).toEqual('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2000 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:168:16`

This declares a definite-assignment assertion with `!` (`private _repo!: Repo;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2001 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:169:19`

This declares a definite-assignment assertion with `!` (`private _storage!: SqliteStorageAdapter;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2002 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:180:18`

This declares a definite-assignment assertion with `!` (`private _peerId!: PeerId;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2003 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:252:35`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2004 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:342:92`

This line casts to `any` with `as any` (`((e: PeerCandidatePayload) => !updatingAuthScope && this._onPeerConnected(e.peerId)) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2005 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:346:98`

This line casts to `any` with `as any` (`((e: PeerDisconnectedPayload) => !updatingAuthScope && this._onPeerDisconnected(e.peerId)) as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2006 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:445:56`

This declares a widened `any` type (`get handles(): Readonly<Record<DocumentId, DocHandle<any>>> {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2007 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:481:35`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2008 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:546:64`

This uses the non-null assertion `!` (`return bufferToArray(Buffer.concat(chunks.map((c) => c.data!)));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2009 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:576:37`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2010 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:587:35`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2011 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:599:35`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2012 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:625:49`

This uses the non-null assertion `!` (`await waitForHeads(handle, entry.heads!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2013 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:640:29`

This declares a widened `any` type (`let handle: DocHandle<any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2014 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:816:52`

This line casts to `any` with `as any` (`const handle = this._repo.getHandle(documentId as any);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2015 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:904:42`

This uses the non-null assertion `!` (`result.push(getHeads(handle.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2016 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:974:19`

This uses the non-null assertion `!` (`result.peers!.push({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2017 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:1118:33`

This uses the non-null assertion `!` (`this._sharePolicyChangedTask!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2018 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:1298:40`

This declares a widened `any` type (`const changeIsPresentInDoc = (doc: Doc<any>, changeHash: string): boolean => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2019 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:168:27`

This uses the non-null assertion `!` (`holder.import(aBytes!, { docId: docA.documentId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2020 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:169:27`

This uses the non-null assertion `!` (`holder.import(bBytes!, { docId: docB.documentId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2021 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:264:27`

This uses the non-null assertion `!` (`holder.import(aBytes!, { docId: docA.documentId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2022 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:265:27`

This uses the non-null assertion `!` (`holder.import(bBytes!, { docId: docB.documentId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2023 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:743:35`

This line casts to `any` with `as any` (`requestor: (c.args[0] as any).toString(),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2024 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:744:32`

This line casts to `any` with `as any` (`author: (c.args[1] as any).toString(),`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2025 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:952:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2026 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:90:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2027 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:111:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2028 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:147:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2029 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:182:43`

This declares a widened `any` type (`const hostHandle = await peer1.find<any>(url as AutomergeUrl);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2030 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:185:46`

This declares a widened `any` type (`const peer2Handle = await findInStates<any>(peer2, hostHandle.url, FIND_STATES);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2031 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:196:21`

This declares a widened `any` type (`repo.import<any>(blob, { docId });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2032 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:204:41`

This declares a widened `any` type (`const sendDoc = async (doc: A.Doc<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2033 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:210:45`

This declares a widened `any` type (`clientDoc = A.change(clientDoc, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2034 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:215:44`

This declares a widened `any` type (`const serverHandle = await repo.find<any>(documentId);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2035 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:216:32`

This uses the non-null assertion `!` (`expect(serverHandle.doc()!.field).to.deep.equal(value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2036 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:224:41`

This declares a widened `any` type (`const reloaded = await repo2.find<any>(documentId);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2037 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:225:28`

This uses the non-null assertion `!` (`expect(reloaded.doc()!.field).to.deep.equal(value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2038 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:233:36`

This declares a widened `any` type (`const handleA = repoA.create<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2039 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:234:42`

This declares a widened `any` type (`const handleB = await findInStates<any>(repoB, handleA.url, FIND_STATES);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2040 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:237:28`

This declares a widened `any` type (`handleA.update((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2041 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:238:36`

This declares a widened `any` type (`return A.change(doc, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2042 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:244:27`

This uses the non-null assertion `!` (`expect(handleA.doc()!.text).to.equal(text);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2043 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:309:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2044 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:375:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2045 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:383:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2046 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:409:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2047 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:427:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2048 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:454:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2049 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:575:59`

This uses the non-null assertion `!` (`const initialHostHeads = getHeads(hostHandle.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2050 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:577:35`

This declares a widened `any` type (`clientHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2051 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:583:41`

This uses the non-null assertion `!` (`expect(getHeads(hostHandle.doc()!)).to.deep.equal(initialHostHeads);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2052 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:656:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2053 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:693:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2054 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:110:39`

This uses the non-null assertion `!` (`valueDuringChange = handle.doc()!.field;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2055 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:113:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2056 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:131:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2057 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:141:49`

This declares a widened `any` type (`const newDoc = change(copy, 'change', (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2058 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:213:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2059 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:217:46`

This declares a widened `any` type (`expect((await asyncTimeout(client.find<any>(handle.url), 1000))!.doc().text).to.equal(text);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2060 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:217:70`

This uses the non-null assertion `!` (`expect((await asyncTimeout(client.find<any>(handle.url), 1000))!.doc().text).to.equal(text);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2061 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:235:32`

This declares a widened `any` type (`firstHandle.change((doc: any) => (doc.text = 'Hello world'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2062 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:242:52`

This declares a widened `any` type (`const firstDocOnClient = await client.find<any>(firstHandle.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2063 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:244:38`

This uses the non-null assertion `!` (`expect(firstDocOnClient.doc()!.text).to.equal('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2064 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:248:33`

This declares a widened `any` type (`secondHandle.change((doc: any) => (doc.text = 'Hello world'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2065 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:253:53`

This declares a widened `any` type (`const secondDocOnClient = await client.find<any>(secondHandle.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2066 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:255:39`

This uses the non-null assertion `!` (`expect(secondDocOnClient.doc()!.text).to.equal('Hello world');`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2067 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:269:46`

This declares a widened `any` type (`const docOnClient = await findInStates<any>(client, handle.url, FIND_STATES);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2068 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:272:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2069 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:285:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2070 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:300:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2071 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:322:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2072 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:346:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2073 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:364:25`

This declares a widened `any` type (`docA.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2074 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:378:45`

This uses the non-null assertion `!` (`adapter1.peerCandidate(adapter2.peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2075 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:380:45`

This uses the non-null assertion `!` (`adapter2.peerCandidate(adapter1.peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2076 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:408:43`

This declares a widened `any` type (`const hostHandle = await peer1.find<any>(url as AutomergeUrl);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2077 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:412:47`

This uses the non-null assertion `!` (`await expect.poll(() => hostHandle.doc()!.text).not.toBeUndefined();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2078 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:430:52`

This uses the non-null assertion `!` (`let syncedHeads = getHeads(serverHandle.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2079 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:431:46`

This uses the non-null assertion `!` (`receiveByClient(save(serverHandle.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2080 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:445:35`

This declares a widened `any` type (`serverHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2081 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:453:35`

This declares a widened `any` type (`serverHandle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2082 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:462:84`

This declares a widened `any` type (`const receiveByServer = (blob: Uint8Array, docId: DocumentId) => repo.import<any>(blob, { docId });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2083 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:469:41`

This declares a widened `any` type (`const sendDoc = async (doc: A.Doc<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2084 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:477:47`

This declares a widened `any` type (`clientDoc = A.change(clientDoc, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2085 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:482:46`

This declares a widened `any` type (`const serverHandle = await repo.find<any>(documentId);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2086 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:483:34`

This uses the non-null assertion `!` (`expect(serverHandle.doc()!.field).to.deep.equal(value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2087 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:492:21`

This declares a widened `any` type (`repo.import<any>(blob, { docId });`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2088 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:503:41`

This declares a widened `any` type (`const sendDoc = async (doc: A.Doc<any>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2089 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:511:47`

This declares a widened `any` type (`clientDoc = A.change(clientDoc, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2090 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:516:46`

This declares a widened `any` type (`const serverHandle = await repo.find<any>(documentId);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2091 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:517:34`

This uses the non-null assertion `!` (`expect(serverHandle.doc()!.field).to.deep.equal(value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# WARN 50877ea571-2092 no-sleep-in-test `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:520`

`await sleep(100); // Re-open repo` waits an arbitrary period hoping the document auto-save has flushed to disk before reopening the repo and asserting the value round-tripped. Use a proper save-completion signal (e.g. await the underlying flush) instead of a fixed sleep.

# ERROR 50877ea571-2093 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:525:46`

This declares a widened `any` type (`const serverHandle = await repo.find<any>(documentId);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2094 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:526:34`

This uses the non-null assertion `!` (`expect(serverHandle.doc()!.field).to.deep.equal(value);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2095 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:535:36`

This declares a widened `any` type (`const handleA = repoA.create<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2096 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:536:40`

This declares a widened `any` type (`const handleB = await repoB.find<any>(handleA.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2097 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:539:28`

This declares a widened `any` type (`handleA.update((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2098 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:540:36`

This declares a widened `any` type (`return A.change(doc, (doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2099 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:545:27`

This uses the non-null assertion `!` (`expect(handleA.doc()!.text).to.equal(text);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2100 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:549:27`

This uses the non-null assertion `!` (`expect(handleB.doc()!.text).to.equal(text);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2101 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:597:27`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2102 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:610:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2103 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:622:41`

This uses the non-null assertion `!` (`const blob = A.save(original.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2104 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:627:29`

This declares a widened `any` type (`imported.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2105 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:643:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2106 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:670:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-2107 no-sleep-in-test `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:674`

`// No explicit flush - rely on auto-save. await sleep(200);` before closing the repo is the same auto-save-completion guess as line 520 — a real-timer wait standing in for a proper completion signal, which is exactly what the rule flags as flaky.

# ERROR 50877ea571-2108 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:704:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2109 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:710:56`

This declares a widened `any` type (`const docOnPeer2 = await peer2.repo.find<any>(handle.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2110 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:711:38`

This uses the non-null assertion `!` (`return docOnPeer2.doc()!.text;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2111 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:723:29`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2112 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:726:50`

This declares a widened `any` type (`const docOnPeer2 = await peer2.repo.find<any>(handle.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2113 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:729:32`

This uses the non-null assertion `!` (`expect(docOnPeer2.doc()!.offlineText).to.be.undefined;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2114 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:734:50`

This declares a widened `any` type (`const docOnPeer2 = await peer2.repo.find<any>(handle.url);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2115 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:737:32`

This uses the non-null assertion `!` (`expect(docOnPeer2.doc()!.offlineText).to.eq(offlineText);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2116 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:742:31`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2117 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:747:32`

This uses the non-null assertion `!` (`expect(docOnPeer2.doc()!.onlineText).to.eq(onlineText);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2118 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:760:33`

This declares a widened `any` type (`document.change((doc: any) => (doc.text = 'hello ' + idx));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2119 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:780:48`

This declares a widened `any` type (`const shouldFindDoc = await findInStates<any>(peer2.repo, docInRemoteCollection.url, ['ready']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2120 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:804:31`

This declares a widened `any` type (`document.change((doc: any) => (doc.text = 'hello'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2121 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:823:41`

This declares a widened `any` type (`const loaded = await findInStates<any>(peer2.repo, document.url, ['ready']);`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2122 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:845:31`

This declares a widened `any` type (`document.change((doc: any) => (doc.text = 'hello'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2123 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:880:27`

This declares a widened `any` type (`handle.change((doc: any) => (doc.text = 'hello'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2124 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:887:27`

This uses the non-null assertion `!` (`return doc.doc()!.text;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2125 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:898:29`

This declares a widened `any` type (`handle.change((doc: any) => (doc.text = 'hello'));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2126 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:899:35`

This uses the non-null assertion `!` (`blob = A.save(handle.doc()!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2127 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:920:29`

This uses the non-null assertion `!` (`return doc.doc()!.text;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2128 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:937:54`

This declares a widened `any` type (`createDocCallback: (repo: Repo) => Promise<A.Doc<any>[]>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2129 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:994:43`

This uses the non-null assertion `!` (`pair[0].peerCandidate(pair[1].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2130 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:995:43`

This uses the non-null assertion `!` (`pair[1].peerCandidate(pair[0].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2131 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1006:44`

This uses the non-null assertion `!` (`pair[0].peerDisconnected(pair[1].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2132 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1007:44`

This uses the non-null assertion `!` (`pair[1].peerDisconnected(pair[0].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2133 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1008:41`

This uses the non-null assertion `!` (`pair[0].peerCandidate(pair[1].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2134 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1009:41`

This uses the non-null assertion `!` (`pair[1].peerCandidate(pair[0].peerId!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2135 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1013:55`

This declares a widened `any` type (`const docChangeAfterAction = async (handle: DocHandle<any>, action: () => Promise<void>) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-2136 no-sleep-in-test `packages/core/echo/echo-host/src/automerge/collection-synchronizer.test.ts:106`

`peer.setLocalCollectionState(collectionId, STATE_1); await sleep(10);` (repeated at line 145) waits a fixed 10ms hoping the internal microtask-scheduled broadcast has run before asserting on `sentStates`/`sentTo`. There is no completion signal awaited; use `waitForCondition`/`expect.poll` on the observed side effect instead of a blind sleep.

# ERROR 50877ea571-2137 no-casts `packages/core/echo/echo-host/src/automerge/echo-data-monitor.ts:119:52`

This line casts to `any` with `as any` (`const values: (typeof value)[] = (timeSeries as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2138 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:30:34`

This declares a widened `any` type (`const peerInfo = new Trigger<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2139 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:40:41`

This declares a widened `any` type (`const messageReceived = new Trigger<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2140 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:52:40`

This declares a widened `any` type (`const onDisconnected = new Trigger<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2141 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:67:40`

This declares a widened `any` type (`const onDisconnected = new Trigger<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2142 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:79:40`

This declares a widened `any` type (`const onDisconnected = new Trigger<any>();`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2143 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:358:6`

This line casts to `any` with `as any` (`}) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2144 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:361:13`

This line casts to `any` with `as any` (`(metadata as any)?.dxos_peerSource === 'EchoNetworkAdapter';`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2145 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:122:47`

This uses the non-null assertion `!` (`expect(range.map((c) => Buffer.from(c.data!).toString())).toEqual(['one', 'two']);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2146 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:141:28`

This line casts to `any` with `as any` (`const docId = 'abc123' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2147 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:150:52`

This line casts to `any` with `as any` (`const result = await store.getHeads(['missing' as any]);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2148 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:161:27`

This line casts to `any` with `as any` (`const docId1 = 'doc1' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2149 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:162:27`

This line casts to `any` with `as any` (`const docId2 = 'doc2' as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2150 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:165:16`

This declares a widened `any` type (`const all: any[] = [];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2151 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:102:82`

This uses the non-null assertion `!` (`expect(result.cursors[0].cursor).toBe(headsCodec.encode(getHeads(handle.doc()!)));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2152 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:118:69`

This uses the non-null assertion `!` (`const doc1HeadsBefore = headsCodec.encode(getHeads(handle1.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2153 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:119:63`

This uses the non-null assertion `!` (`const doc2Heads = headsCodec.encode(getHeads(handle2.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2154 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:123:18`

This uses the non-null assertion `!` (`doc.objects!['obj-1'].data.title = 'Doc 1 Updated';`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2155 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:155:65`

This uses the non-null assertion `!` (`const currentHeads = headsCodec.encode(getHeads(handle.doc()!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2156 no-casts `packages/core/echo/echo-host/src/db-host/automerge-metrics.ts:18:46`

This declares a widened `any` type (`export const measureDocMetrics = (doc: A.Doc<any>): DocMetrics => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2157 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.test.ts:80:25`

This declares a widened `any` type (`handle.change((doc: any) => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# WARN 50877ea571-2158 no-sleep-in-test `packages/core/echo/echo-host/src/db-host/documents-synchronizer.test.ts:111`

`// Wait for the changes to be processed. await sleep(100);` waits a fixed delay hoping document changes have propagated through the synchronizer; the following assertion (`counter` `greaterThanOrEqual(0)`) is trivially always true, so this sleep doesn't even reliably exercise a race — it should be replaced by a proper completion wait tied to `sendUpdates` actually firing.

# WARN 50877ea571-2159 no-sleep-in-test `packages/core/echo/echo-host/src/db-host/documents-synchronizer.test.ts:142`

`// Wait for auto-save (no explicit flush). await sleep(500);` before closing the host/synchronizer waits an arbitrary period for the auto-save to complete rather than awaiting an explicit completion signal — the same anti-pattern as the `automerge-repo.test.ts` auto-save waits.

# ERROR 50877ea571-2160 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:100:37`

This uses the non-null assertion `!` (`this._sendUpdatesJob!.trigger();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2161 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:165:31`

This uses the non-null assertion `!` (`await this._sendUpdatesJob!.join();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2162 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:201:27`

This uses the non-null assertion `!` (`this._sendUpdatesJob!.trigger();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2163 no-casts `packages/core/echo/echo-host/src/db-host/echo-host.ts:109:25`

This declares a definite-assignment assertion with `!` (`private _updateIndexes!: DeferredTask;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2164 no-casts `packages/core/echo/echo-host/src/db-host/echo-host.ts:537:28`

This uses the non-null assertion `!` (`this._updateIndexes!.schedule();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2165 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:50:39`

This uses the non-null assertion `!` (`expect(JSON.parse(result.objects![0])).toMatchObject(object1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2166 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:51:39`

This uses the non-null assertion `!` (`expect(JSON.parse(result.objects![1])).toMatchObject(object2);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2167 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:57:60`

This declares a widened `any` type (`const runtime = Effect.succeed(yield* Effect.runtime<any>());`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2168 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:84:39`

This uses the non-null assertion `!` (`expect(JSON.parse(result.objects![1])).toMatchObject({ 'id': object1Id, '@deleted': true });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2169 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:90:60`

This declares a widened `any` type (`const runtime = Effect.succeed(yield* Effect.runtime<any>());`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2170 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:111:38`

This uses the non-null assertion `!` (`expect(JSON.parse(page1.objects![0])).toMatchObject(items[0]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2171 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:112:38`

This uses the non-null assertion `!` (`expect(JSON.parse(page1.objects![4])).toMatchObject(items[4]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2172 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:121:34`

This uses the non-null assertion `!` (`after: page1.nextCursor!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2173 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:125:38`

This uses the non-null assertion `!` (`expect(JSON.parse(page2.objects![0])).toMatchObject(items[5]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2174 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:126:38`

This uses the non-null assertion `!` (`expect(JSON.parse(page2.objects![4])).toMatchObject(items[9]);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2175 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:132:60`

This declares a widened `any` type (`const runtime = Effect.succeed(yield* Effect.runtime<any>());`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2176 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:157:37`

This uses the non-null assertion `!` (`expect(JSON.parse(head.objects![0])).toMatchObject({ id: objectId, data: 'test' });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2177 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:158:37`

This uses the non-null assertion `!` (`expect(JSON.parse(head.objects![1])).toMatchObject({ 'id': objectId, '@deleted': true });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2178 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:63:31`

This uses the non-null assertion `!` (`spaceId: spaceId! as SpaceId,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2179 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:99:29`

This uses the non-null assertion `!` (`feedId: feedId!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2180 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:124:39`

This uses the non-null assertion `!` (`const messages = objectIds!.map((id) => ({`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2181 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:126:29`

This uses the non-null assertion `!` (`feedId: feedId!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2182 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:45:37`

This declares a widened `any` type (`const makeExecutor = (query: { ast: any }): QueryExecutor =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2183 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:47:21`

This line casts to `any` with `as any` (`indexEngine: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2184 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:48:17`

This line casts to `any` with `as any` (`runtime: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2185 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:49:23`

This line casts to `any` with `as any` (`automergeHost: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2186 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:50:27`

This line casts to `any` with `as any` (`spaceStateManager: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2187 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:53:28`

This line casts to `any` with `as any` (`reactivity: 'reactive' as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2188 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:87:18`

This uses the non-null assertion `!` (`expect(result!.spaceIds?.has(spaceId)).toBe(true);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2189 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:89:18`

This uses the non-null assertion `!` (`expect(result!.typenames?.has(PERSON_TYPENAME)).toBe(true);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2190 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:90:18`

This uses the non-null assertion `!` (`expect(result!.typenames?.has(PERSON_DXN)).toBe(false);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2191 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:91:18`

This uses the non-null assertion `!` (`expect(result!.objectIds?.has(objectId)).toBe(true);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2192 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:93:18`

This uses the non-null assertion `!` (`expect(result!.queueIds).toBeUndefined();`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2193 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:108:18`

This uses the non-null assertion `!` (`expect(result!.typenames).toEqual(new Set([PERSON_TYPENAME, ORG_TYPENAME]));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2194 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:307:23`

This line casts to `any` with `as any` (`indexEngine: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2195 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:308:19`

This line casts to `any` with `as any` (`runtime: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2196 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:309:25`

This line casts to `any` with `as any` (`automergeHost: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2197 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:310:29`

This line casts to `any` with `as any` (`spaceStateManager: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2198 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:316:30`

This line casts to `any` with `as any` (`reactivity: 'reactive' as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2199 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:422:23`

This line casts to `any` with `as any` (`indexEngine: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2200 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:423:19`

This line casts to `any` with `as any` (`runtime: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2201 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:424:25`

This line casts to `any` with `as any` (`automergeHost: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2202 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:425:29`

This line casts to `any` with `as any` (`spaceStateManager: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2203 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:428:30`

This line casts to `any` with `as any` (`reactivity: 'reactive' as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2204 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:441:23`

This line casts to `any` with `as any` (`indexEngine: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2205 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:442:19`

This line casts to `any` with `as any` (`runtime: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2206 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:443:25`

This line casts to `any` with `as any` (`automergeHost: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2207 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:444:29`

This line casts to `any` with `as any` (`spaceStateManager: {} as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2208 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:447:30`

This line casts to `any` with `as any` (`reactivity: 'reactive' as any,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2209 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:103:45`

This uses the non-null assertion `!` (`await this._openConnection(this._ctx!, spaceId);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2210 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:178:20`

This uses the non-null assertion `!` (`this._ctx!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2211 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:192:53`

This uses the non-null assertion `!` (`await this._openConnection(replicatorCtx!, spaceId, reconnects + 1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2212 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:237:36`

This declares a definite-assignment assertion with `!` (`private _readableStreamController!: ReadableStreamDefaultController<AutomergeProtocolMessage>;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2213 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:365:48`

This uses the non-null assertion `!` (`const payload = cbor.decode(message.payload!.value) as AutomergeProtocolMessage;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2214 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:372:42`

This uses the non-null assertion `!` (`payload.senderId = this._remotePeerId! as PeerId;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2215 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:414:14`

This line casts to `any` with `as any` (`(message as any).targetId = this._targetServiceId as PeerId;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2216 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.test.ts:68:84`

This line casts to `any` with `as any` (`const currentConnectionId = () => (openConnections[openConnections.length - 1] as any)._connectionId as string;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2217 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:298:20`

This uses the non-null assertion `!` (`this._ctx!,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2218 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:365:36`

This declares a definite-assignment assertion with `!` (`private _readableStreamController!: ReadableStreamDefaultController<SubductionProtocolMessage>;`), telling the compiler to skip checking that the field/variable is initialized before use. Per the no-casts rule, initialize it at declaration or in the constructor, or give it a type that honestly reflects it may be undefined until set, instead of asserting past the check.

# ERROR 50877ea571-2219 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:485:44`

This uses the non-null assertion `!` (`payload = cbor.decode(message.payload!.value) as SubductionProtocolMessageEnveloped;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2220 no-casts `packages/core/echo/echo-host/src/filter/filter-match.ts:148:30`

This line casts to `any` with `as any` (`const value = (obj as any)[key];`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2221 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:502:99`

This uses the non-null assertion `!` (`const serializeItemGroupKey = (item: QueryItem): string => GroupBy.serializeGroupKey(item.groupKey!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2222 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:737:53`

This line casts to `any` with `as any` (`throw new Error(`Unknown step type: ${(step as any)._tag}`);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2223 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:991:66`

This line casts to `any` with `as any` (`throw new Error(`Unknown selector type: ${(step.selector as any)._tag}`);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2224 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1468:68`

This line casts to `any` with `as any` (`throw new Error(`Unknown traversal type: ${(step.traversal as any)._tag}`);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2225 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1586:112`

This uses the non-null assertion `!` (`const partitioned = GroupBy.partitionByGroupKey(withKeys, (item) => GroupBy.serializeGroupKey(item.groupKey!));`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2226 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1589:56`

This uses the non-null assertion `!` (`(item) => GroupBy.serializeGroupKey(item.groupKey!),`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2227 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1520:26`

This line casts to `any` with `as any` (`expect((selectStep as any).limit).toBeUndefined();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2228 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1522:25`

This line casts to `any` with `as any` (`expect((orderStep as any).limit).toBeUndefined();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2229 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1937:24`

This line casts to `any` with `as any` (`expect((selectStep as any).limit).toBeUndefined();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2230 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1939:89`

This line casts to `any` with `as any` (`const orderWithLimit = plan.steps.some((step) => step._tag === 'OrderStep' && (step as any).limit === 10);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2231 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2007:26`

This line casts to `any` with `as any` (`expect((selectStep as any).limit).toBeUndefined();`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2232 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2009:91`

This line casts to `any` with `as any` (`const orderWithLimit = plan.steps.some((step) => step._tag === 'OrderStep' && (step as any).limit === 10);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2233 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2019:25`

This line casts to `any` with `as any` (`expect((orderStep as any).limit).toBe(10);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2234 no-casts `packages/core/echo/echo-host/src/query/query-planner.ts:102:54`

This line casts to `any` with `as any` (`message: `Unsupported query type: ${(query as any).type}`,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2235 no-casts `packages/core/echo/echo-host/src/query/query-planner.ts:477:56`

This line casts to `any` with `as any` (`message: `Unsupported filter type: ${(filter as any).type}`,`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2236 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:65:58`

This uses the non-null assertion `!` (`log('create connection', { from: replicator.context!.peerId, to: otherReplicator.context!.peerId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2237 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:65:95`

This uses the non-null assertion `!` (`log('create connection', { from: replicator.context!.peerId, to: otherReplicator.context!.peerId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2238 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:67:27`

This uses the non-null assertion `!` (`replicator.context!.peerId,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2239 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:68:32`

This uses the non-null assertion `!` (`otherReplicator.context!.peerId,`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2240 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:72:31`

This uses the non-null assertion `!` (`await replicator.context!.onConnectionOpen(connection1);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2241 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:73:36`

This uses the non-null assertion `!` (`await otherReplicator.context!.onConnectionOpen(connection2);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2242 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:79:31`

This uses the non-null assertion `!` (`await replicator.context!.onConnectionClosed(connection);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2243 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:80:33`

This uses the non-null assertion `!` (`await connection.otherSide!.owningReplicator!.removeConnection(connection.otherSide!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2244 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:80:51`

This uses the non-null assertion `!` (`await connection.otherSide!.owningReplicator!.removeConnection(connection.otherSide!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2245 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:80:90`

This uses the non-null assertion `!` (`await connection.otherSide!.owningReplicator!.removeConnection(connection.otherSide!);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2246 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:168:45`

This uses the non-null assertion `!` (`log('disconnect', { peerId: this.context!.peerId });`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2247 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:176:17`

This uses the non-null assertion `!` (`this.context!.onConnectionOpen(connection);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2248 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:181:17`

This uses the non-null assertion `!` (`this.context!.onConnectionClosed(connection);`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2249 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:225:12`

This uses the non-null assertion `!` (`params[1]!.onSyncMessage = () => {`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2250 no-casts `packages/core/echo/echo-host/src/util.ts:16:33`

This uses the non-null assertion `!` (`const obj = spaceDoc.objects![id];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2251 no-casts `packages/core/echo/echo-panproto/src/Lens.test.ts:127:84`

This line casts to `any` with `as any` (`expect(() => Lens.make('org.dxos.test.lens.bad', Task, GtdTask, { done: 'nope' as any })).to.throw(`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2252 no-casts `packages/core/echo/echo-panproto/src/Lens.test.ts:202:57`

This line casts to `any` with `as any` (`expect(() => Lens.put(task, makeLens(), { nope: 1 } as any)).to.throw(/not a property of the target/);`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2253 no-casts `packages/core/echo/echo-panproto/src/Lens.ts:41:22`

This declares a widened `any` type (`export type Lens<S = any, T = any> = LensShape<S, T>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2254 no-casts `packages/core/echo/echo-panproto/src/Lens.ts:41:31`

This declares a widened `any` type (`export type Lens<S = any, T = any> = LensShape<S, T>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2255 no-casts `packages/core/echo/echo-panproto/src/Lens.ts:47:16`

This declares a widened `any` type (`codec: Codec<any, V> | string,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2256 no-casts `packages/core/echo/echo-panproto/src/Lens.ts:48:51`

This declares a widened `any` type (`): { kind: 'converted'; property: P; codec: Codec<any, V> | string } => ({`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2257 no-casts `packages/core/echo/echo-panproto/src/lens/codecs.ts:52:49`

This line uses the double-cast escape hatch `as unknown as T` (`decode: (value) => forward[value] ?? (value as unknown as B),`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-2258 no-casts `packages/core/echo/echo-panproto/src/lens/codecs.ts:53:50`

This line uses the double-cast escape hatch `as unknown as T` (`encode: (value) => backward[value] ?? (value as unknown as A),`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-2259 no-casts `packages/core/echo/echo-panproto/src/lens/live.ts:43:48`

This declares a widened `any` type (`export const of = <S, T extends Record<string, any>>(obj: Obj.Unknown, lens: Lens<S, T>): Obj.OfShape<T> => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2260 no-casts `packages/core/echo/echo-panproto/src/lens/live.ts:50:37`

This declares a widened `any` type (`const change = (callback: (draft: any) => void): void => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2261 no-casts `packages/core/echo/echo-panproto/src/lens/live.ts:137:6`

This line uses the double-cast escape hatch `as unknown as T` (`}) as unknown as Obj.OfShape<T>;`), which routes an incompatible type through `unknown` to defeat structural checking. Per the no-casts rule, fix the type at its source instead: correct the producing function's return type, add a proper type guard, or adjust the target type so the value type-checks directly.

# ERROR 50877ea571-2262 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:22:23`

This declares a widened `any` type (`export type Codec<A = any, B = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2263 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:22:32`

This declares a widened `any` type (`export type Codec<A = any, B = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2264 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:33:25`

This declares a widened `any` type (`export type Derived<S = any, V = any, K extends keyof S = keyof S> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2265 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:33:34`

This declares a widened `any` type (`export type Derived<S = any, V = any, K extends keyof S = keyof S> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2266 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:45:27`

This declares a widened `any` type (`export type Converted<S = any, V = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2267 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:45:36`

This declares a widened `any` type (`export type Converted<S = any, V = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2268 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:49:25`

This declares a widened `any` type (`readonly codec: Codec<any, NonNullable<V>> | string;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2269 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:53:26`

This declares a widened `any` type (`export type ReadOnly<S = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2270 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:59:30`

This declares a widened `any` type (`export type MappingEntry<S = any, V = any> = (keyof S & string) | Converted<S, V> | ReadOnly<S> | Derived<S, V, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2271 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:59:39`

This declares a widened `any` type (`export type MappingEntry<S = any, V = any> = (keyof S & string) | Converted<S, V> | ReadOnly<S> | Derived<S, V, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2272 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:59:113`

This declares a widened `any` type (`export type MappingEntry<S = any, V = any> = (keyof S & string) | Converted<S, V> | ReadOnly<S> | Derived<S, V, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2273 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:65:25`

This declares a widened `any` type (`export type Mapping<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2274 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:65:34`

This declares a widened `any` type (`export type Mapping<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2275 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:137:22`

This declares a widened `any` type (`export type Lens<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2276 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:137:31`

This declares a widened `any` type (`export type Lens<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2277 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:151:28`

This declares a widened `any` type (`export type AnyLens = Lens<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2278 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:151:33`

This declares a widened `any` type (`export type AnyLens = Lens<any, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2279 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:154:30`

This declares a widened `any` type (`export type CodedMapping<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2280 no-casts `packages/core/echo/echo-panproto/src/lens/types.ts:154:39`

This declares a widened `any` type (`export type CodedMapping<S = any, T = any> = {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2281 no-casts `packages/core/echo/echo-panproto/src/react.ts:32:52`

This declares a widened `any` type (`<S extends Obj.Unknown, T extends Record<string, any>>(obj: S, lens: Lens.Lens<any, T>): [T, LensUpdateCallback<T>];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2282 no-casts `packages/core/echo/echo-panproto/src/react.ts:32:82`

This declares a widened `any` type (`<S extends Obj.Unknown, T extends Record<string, any>>(obj: S, lens: Lens.Lens<any, T>): [T, LensUpdateCallback<T>];`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2283 no-casts `packages/core/echo/echo-panproto/src/react.ts:34:52`

This declares a widened `any` type (`<S extends Obj.Unknown, T extends Record<string, any>>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2284 no-casts `packages/core/echo/echo-panproto/src/react.ts:36:21`

This declares a widened `any` type (`lens: Lens.Lens<any, T>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2285 no-casts `packages/core/echo/echo-panproto/src/react.ts:45:52`

This declares a widened `any` type (`<S extends Obj.Unknown, T extends Record<string, any>, K extends keyof T & string>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2286 no-casts `packages/core/echo/echo-panproto/src/react.ts:47:21`

This declares a widened `any` type (`lens: Lens.Lens<any, T>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2287 no-casts `packages/core/echo/echo-panproto/src/react.ts:50:55`

This declares a widened `any` type (`} = (<S extends Obj.Unknown, T extends Record<string, any>, K extends keyof T & string>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2288 no-casts `packages/core/echo/echo-panproto/src/react.ts:52:19`

This declares a widened `any` type (`lens: Lens.Lens<any, T>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2289 no-casts `packages/core/echo/echo-panproto/src/react.ts:54:4`

This declares a widened `any` type (`): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2290 no-casts `packages/core/echo/echo-panproto/src/react.ts:67:47`

This declares a widened `any` type (`Obj.update(Lens.of(obj, lens), (view: any) => (updateOrValue as (view: Obj.Mutable<T>) => void)(view));`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2291 no-casts `packages/core/echo/echo-panproto/src/react.ts:81:4`

This line casts to `any` with `as any` (`}) as any;`), discarding type checking on the expression for the rest of its use. Per the no-casts rule, fix the type at its source — narrow the source's type, add an explicit interface/type guard, or thread the correct generic — instead of escaping through `any`.

# ERROR 50877ea571-2292 no-casts `packages/core/echo/echo-panproto/src/react.ts:89:78`

This declares a widened `any` type (`export const useLensValue = <S extends Obj.Unknown, T extends Record<string, any>, K extends keyof T & string>(`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2293 no-casts `packages/core/echo/echo-panproto/src/react.ts:91:19`

This declares a widened `any` type (`lens: Lens.Lens<any, T>,`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2294 no-casts `packages/core/echo/echo-panproto/src/react.ts:93:4`

This declares a widened `any` type (`): any => {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2295 no-casts `packages/core/echo/echo-protocol/src/document-structure.ts:207:24`

This declares a widened `any` type (`data: Record<string, any>;`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2296 no-casts `packages/core/echo/echo-protocol/src/document-structure.ts:366:43`

This declares a widened `any` type (`annotations?: { readonly [key: string]: any };`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2297 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:58:48`

This declares a widened `any` type (`static #setQueuePosition(obj: Record<string, any>, position: number): void {`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2298 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:60:19`

This uses the non-null assertion `!` (`obj[ATTR_META]!.keys ??= [];`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2299 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:61:32`

This uses the non-null assertion `!` (`const keys = obj[ATTR_META]!.keys!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2300 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:61:38`

This uses the non-null assertion `!` (`const keys = obj[ATTR_META]!.keys!;`) to silence a possible null/undefined value. Per the no-casts rule, fix the type at its source instead — add a real null/undefined check, supply a default, or use an invariant/assertion helper that actually narrows the type.

# ERROR 50877ea571-2301 no-casts `packages/core/echo/echo-protocol/src/reference.ts:18:43`

This declares a widened `any` type (`export const isEncodedReference = (value: any): value is EncodedReference =>`), defeating the type checker at this signature/declaration/generic. Per the no-casts rule, replace `any` with a precise type, a generic parameter, or `unknown` narrowed at the call site.

# ERROR 50877ea571-2302 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:374`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const props: Record<string, any> = {};`

# ERROR 50877ea571-2303 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:396`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _parseObjectProperty(cursor: TreeCursor, input: string): { key: string | null; value: any } {`

# ERROR 50877ea571-2304 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:398`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let value: any = null;`

# ERROR 50877ea571-2305 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:426`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let value: any = null;`

# ERROR 50877ea571-2306 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:478`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [key] = Object.entries(this._tags!).find(([, value]) => value.label.toLowerCase() === str) ?? [];`

# ERROR 50877ea571-2307 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:485`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _parseValue(cursor: TreeCursor, input: string): any {`

# ERROR 50877ea571-2308 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:506`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const props: Record<string, any> = {};`

# ERROR 50877ea571-2309 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:523`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const array: any[] = [];`

# ERROR 50877ea571-2310 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:50`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const natural = (direction: QueryAST.OrderDirection = 'asc'): Order$.Order<any> =>`

# ERROR 50877ea571-2311 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:91`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) > (filter.value as any);`

# ERROR 50877ea571-2312 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:93`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) >= (filter.value as any);`

# ERROR 50877ea571-2313 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:95`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) < (filter.value as any);`

# ERROR 50877ea571-2314 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:97`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) <= (filter.value as any);`

# ERROR 50877ea571-2315 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:108`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!_filterMatchValueLocal(vf, (value as any)[key])) {`

# ERROR 50877ea571-2316 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:122`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) >= filter.from && (value as any) <= filter.to;`

# ERROR 50877ea571-2317 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:134`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const _filterMatchEntityLocal = (filter: QueryAST.Filter, entity: any): boolean => {`

# ERROR 50877ea571-2318 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:165`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `throw new Error(`Filter type '${(filter as any).type}' is not supported in toPredicate.`);`

# ERROR 50877ea571-2319 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:232`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `static type(schema: string, props?: Filter$.Props<unknown>): Filter$.Filter<any>;`

# ERROR 50877ea571-2320 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:455`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `static 'toPredicate' = ((entityOrFilter: any, filter?: any): any => {`

# ERROR 50877ea571-2321 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:457`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (entity: any) => _filterMatchEntityLocal(entityOrFilter.ast, entity);`

# ERROR 50877ea571-2322 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:476`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const propsFilterToAst = (predicates: Filter$.Props<any>): Pick<QueryAST.FilterObject, 'id' | 'props'> => {`

# ERROR 50877ea571-2323 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:497`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const processPredicate = (predicate: any): QueryAST.Filter => {`

# ERROR 50877ea571-2324 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:526`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private static 'projectionVariance': Query$.Projection<any>[typeof ProjectionTypeId] =`

# ERROR 50877ea571-2325 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:527`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `{} as Query$.Projection<any>[typeof ProjectionTypeId];`

# ERROR 50877ea571-2326 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:544`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `select(filter: Filter$.Any | Filter$.Props<any>): Query$.Any {`

# ERROR 50877ea571-2327 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:560`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `project(property: string): Query$.Projection<any> {`

# ERROR 50877ea571-2328 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:569`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `static type(schema: string, predicates?: Filter$.Props<unknown>): Query$.Query<any>;`

# ERROR 50877ea571-2329 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:600`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `static project(query: Query$.Any, property: string): Query$.Projection<any> {`

# ERROR 50877ea571-2330 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:604`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `static from(...args: any[]): Query$.Any {`

# ERROR 50877ea571-2331 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:610`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (wrapper.from as (...args: any[]) => Query$.Any)(...args);`

# ERROR 50877ea571-2332 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:613`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `from(...args: any[]): Query$.Any {`

# ERROR 50877ea571-2333 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:674`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `relation?: Type$.Relation<any, any, any, any> | string,`

# ERROR 50877ea571-2334 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:691`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `relation?: Type$.Relation<any, any, any, any> | string,`

# ERROR 50877ea571-2335 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:799`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const isRef = (obj: any): obj is Ref.Ref<any> => {`

# ERROR 50877ea571-2336 no-casts `packages/core/echo/echo-react/src/useObject.ts:120`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (<T extends Obj.Unknown, K extends keyof T>(objOrRef: T | Ref.Ref<T> | undefined, property?: K): any => {`

# ERROR 50877ea571-2337 no-casts `packages/core/echo/echo-react/src/useObject.ts:131`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Obj.update(obj, (obj: any) => {`

# ERROR 50877ea571-2338 no-casts `packages/core/echo/echo-react/src/useObject.ts:156`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2339 no-casts `packages/core/echo/echo-react/src/useObject.ts:176`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return useAtomValue(atom as any);`

# ERROR 50877ea571-2340 no-casts `packages/core/echo/echo-react/src/useQuery.ts:41`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): any[] => {`

# ERROR 50877ea571-2341 no-casts `packages/core/echo/echo-react/src/useQuery.ts:66`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const objects = useSyncExternalStore<any[]>(subscribe, getObjects);`

# ERROR 50877ea571-2342 no-casts `packages/core/echo/echo-solid/src/useObject.ts:139`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): [Accessor<any>, ObjectUpdateCallback<T> | ObjectPropUpdateCallback<T[K]>] {`

# ERROR 50877ea571-2343 no-casts `packages/core/echo/echo-solid/src/useObject.ts:188`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Obj.update(obj, (obj: any) => {`

# ERROR 50877ea571-2344 no-casts `packages/core/echo/echo-solid/src/useObject.ts:242`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `setValue(() => currentValue as unknown as T);`

# ERROR 50877ea571-2345 no-casts `packages/core/echo/echo-solid/src/useObject.ts:247`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `setValue(() => registry.get(atom) as unknown as T);`

# ERROR 50877ea571-2346 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let db: any;`

# ERROR 50877ea571-2347 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any[] | undefined;`

# ERROR 50877ea571-2348 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:42`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any[] | undefined;`

# ERROR 50877ea571-2349 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:63`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2350 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:84`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2351 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:121`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2352 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:157`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2353 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:182`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2354 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:207`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2355 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:208`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let dbAccessor: any = db;`

# ERROR 50877ea571-2356 no-casts `packages/core/echo/echo-solid/src/useQuery.test.tsx:244`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let objectsAccessor: (() => any[]) | undefined;`

# ERROR 50877ea571-2357 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let db: any;`

# ERROR 50877ea571-2358 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any;`

# ERROR 50877ea571-2359 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:42`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any;`

# ERROR 50877ea571-2360 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:54`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any;`

# ERROR 50877ea571-2361 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:87`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2362 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:93`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2363 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:126`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2364 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:132`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2365 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:139`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let dbAccessor: any = db;`

# ERROR 50877ea571-2366 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:154`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2367 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:188`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2368 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:194`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2369 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:201`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);`

# ERROR 50877ea571-2370 no-casts `packages/core/echo/echo/src/Aggregate.ts:44`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type Any = Aggregate<any, any>;`

# ERROR 50877ea571-2371 no-casts `packages/core/echo/echo/src/Aggregate.ts:47`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type ValueOf<A> = A extends Aggregate<any, infer V> ? V : never;`

# ERROR 50877ea571-2372 no-casts `packages/core/echo/echo/src/Aggregate.ts:55`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private static 'variance': Aggregate<any, any>[AggregateTypeId] = {} as Aggregate<any, any>[AggregateTypeId];`

# ERROR 50877ea571-2373 no-casts `packages/core/echo/echo/src/Annotation.test.ts:340`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const result = ColorAnnotation.getFromAst(nameProperty!.type);`

# ERROR 50877ea571-2374 no-casts `packages/core/echo/echo/src/Database.ts:380`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(ref: URI.URI | Ref<any>): Effect.Effect<Entity.Unknown, never, Service>;`

# ERROR 50877ea571-2375 no-casts `packages/core/echo/echo/src/Database.ts:383`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `ref: URI.URI | Ref<any>,`

# ERROR 50877ea571-2376 no-casts `packages/core/echo/echo/src/Database.ts:387`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `ref: URI.URI | Ref<any>,`

# ERROR 50877ea571-2377 no-casts `packages/core/echo/echo/src/Database.ts:408`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(!schema || isInstanceOf(schema as any, object), 'Object type mismatch.');`

# ERROR 50877ea571-2378 no-casts `packages/core/echo/echo/src/Database.ts:409`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return object as any;`

# ERROR 50877ea571-2379 no-casts `packages/core/echo/echo/src/Database.ts:410`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}).pipe(Effect.withSpan('Database.resolve'))) as any;`

# ERROR 50877ea571-2380 no-casts `packages/core/echo/echo/src/Database.ts:493`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Effect.map(({ db }) => db.query(queryOrFilter as any) as QueryResult.QueryResult<any>),`

# ERROR 50877ea571-2381 no-casts `packages/core/echo/echo/src/Entity.ts:79`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as Schema.Schema<Unknown>;`

# ERROR 50877ea571-2382 no-casts `packages/core/echo/echo/src/Entity.ts:132`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2383 no-casts `packages/core/echo/echo/src/Entity.ts:142`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any)[SnapshotKindId] !== undefined;`

# ERROR 50877ea571-2384 no-casts `packages/core/echo/echo/src/Entity.ts:151`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Meta: unique symbol = internal.MetaId as any;`

# ERROR 50877ea571-2385 no-casts `packages/core/echo/echo/src/Entity.ts:228`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getDatabase = (entity: Unknown | Snapshot): any | undefined => internal.getDatabase(entity);`

# ERROR 50877ea571-2386 no-casts `packages/core/echo/echo/src/Filter.test.ts:81`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((combined.ast as any).filters).toHaveLength(2);`

# ERROR 50877ea571-2387 no-casts `packages/core/echo/echo/src/Filter.test.ts:82`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((combined.ast as any).filters[1]).toEqual({`

# ERROR 50877ea571-2388 no-casts `packages/core/echo/echo/src/Filter.ts:39`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type Any = Filter<any>;`

# ERROR 50877ea571-2389 no-casts `packages/core/echo/echo/src/Filter.ts:121`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<S extends internal.UnknownTypeSchema<any, any>>(`

# ERROR 50877ea571-2390 no-casts `packages/core/echo/echo/src/Filter.ts:129`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(uri: URI.URI, props?: Props<unknown>): Filter<any>;`

# ERROR 50877ea571-2391 no-casts `packages/core/echo/echo/src/Filter.ts:134`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (input: Type$.AnyEntity | Schema.Schema.AnyNoContext | URI.URI, props?: Props<unknown>): any => {`

# ERROR 50877ea571-2392 no-casts `packages/core/echo/echo/src/Filter.ts:452`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const propsFilterToAst = (predicates: Props<any>): Pick<QueryAST.FilterObject, 'id' | 'props'> => {`

# ERROR 50877ea571-2393 no-casts `packages/core/echo/echo/src/Filter.ts:474`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const processPredicate = (predicate: any): QueryAST.Filter => {`

# ERROR 50877ea571-2394 no-casts `packages/core/echo/echo/src/Filter.ts:483`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(predicate: any) => !Ref.isRef(predicate) && typeof predicate === 'object' && predicate !== null,`

# ERROR 50877ea571-2395 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.test.ts:140`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} as unknown as TestEchoSchema;`

# ERROR 50877ea571-2396 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:181`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const type = (obj as any)?.[TypeId];`

# ERROR 50877ea571-2397 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:195`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setTypename = (obj: any, typename: URI.URI): void => {`

# ERROR 50877ea571-2398 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:217`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const type = (obj as any)[TypeId];`

# ERROR 50877ea571-2399 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:484`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `args?: any[];`

# ERROR 50877ea571-2400 no-casts `packages/core/echo/echo/src/internal/Annotation/atoms.ts:51`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Annotation.Annotation<Record<string, any>>,`

# ERROR 50877ea571-2401 no-casts `packages/core/echo/echo/src/internal/Annotation/atoms.ts:53`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `]): Atom.Atom<any> => {`

# ERROR 50877ea571-2402 no-casts `packages/core/echo/echo/src/internal/Annotation/atoms.ts:105`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Data.tuple(target, annotation as Annotation.Annotation<Record<string, any>>, key),`

# ERROR 50877ea571-2403 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:119`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `proxy: any,`

# ERROR 50877ea571-2404 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:120`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `callback: (proxy: any) => void,`

# ERROR 50877ea571-2405 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:130`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(eventTarget as any)[EventId]?.emit();`

# ERROR 50877ea571-2406 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:134`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(ownerTarget as any)[EventId]?.emit();`

# ERROR 50877ea571-2407 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:15`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const tagRef = (id: string): Ref.Ref<any> => Ref.fromURI(URI.make(`echo://BBBBBBBBBBBBBBBBBBBBBBBBBB/${id}`));`

# ERROR 50877ea571-2408 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:63`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => ((meta as any).keys = [])).toThrow(/outside of Obj.update/);`

# ERROR 50877ea571-2409 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:64`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => ((meta as any).tags = [tagRef('tag')])).toThrow(/outside of Obj.update/);`

# ERROR 50877ea571-2410 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:268`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.address!.state = 'NY';`

# ERROR 50877ea571-2411 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:282`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.address!.coordinates!.lat = 51.5074;`

# ERROR 50877ea571-2412 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:283`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.address!.coordinates!.lng = -0.1278;`

# ERROR 50877ea571-2413 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:298`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.address!.city = 'LA';`

# ERROR 50877ea571-2414 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:310`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.address!.coordinates!.lat = 0;`

# ERROR 50877ea571-2415 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:356`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.fields!.push({ label: 'tag2', value: 'val2' });`

# ERROR 50877ea571-2416 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:360`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(obj.fields![1].label).toBe('tag2');`

# ERROR 50877ea571-2417 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:372`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let popped: any;`

# ERROR 50877ea571-2418 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:374`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `popped = obj.fields!.pop();`

# ERROR 50877ea571-2419 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:392`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.fields!.splice(1, 1, { label: 'x', value: 'x' });`

# ERROR 50877ea571-2420 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:396`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(obj.fields![1].label).toBe('x');`

# ERROR 50877ea571-2421 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:407`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.fields!.push({ label: 'tag2', value: 'val2' });`

# ERROR 50877ea571-2422 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:419`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.fields!.pop();`

# ERROR 50877ea571-2423 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:431`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.fields!.splice(0, 1);`

# ERROR 50877ea571-2424 no-casts `packages/core/echo/echo/src/internal/common/proxy/define-hidden-property.ts:8`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const defineHiddenProperty = (object: any, key: string | symbol, value: any) => {`

# ERROR 50877ea571-2425 no-casts `packages/core/echo/echo/src/internal/common/proxy/event-batch.ts:28`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(target as any)[EventId]?.emit();`

# ERROR 50877ea571-2426 no-casts `packages/core/echo/echo/src/internal/common/proxy/event-batch.ts:42`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(target as any)[EventId]?.emit();`

# ERROR 50877ea571-2427 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:51`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const objData: any = (obj as any)[objectData];`

# ERROR 50877ea571-2428 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:66`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(obj.classInstance!.field).to.eq('value');`

# ERROR 50877ea571-2429 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:71`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.classInstance!.field = 'baz';`

# ERROR 50877ea571-2430 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:72`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(obj.classInstance!.field).to.eq('baz');`

# ERROR 50877ea571-2431 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:112`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj.classInstance!.field = 'baz';`

# ERROR 50877ea571-2432 no-casts `packages/core/echo/echo/src/internal/common/proxy/json-serializer.ts:26`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const attachTypedJsonSerializer = (obj: any) => {`

# ERROR 50877ea571-2433 no-casts `packages/core/echo/echo/src/internal/common/proxy/json-serializer.ts:43`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const typedJsonSerializer = function (this: any) {`

# ERROR 50877ea571-2434 no-casts `packages/core/echo/echo/src/internal/common/proxy/json-serializer.ts:45`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const result: any = {`

# ERROR 50877ea571-2435 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:55`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return createReactiveObject<T>(Object.assign({}, obj) as T, meta, schema as Schema.Schema<T, any>, typeSource);`

# ERROR 50877ea571-2436 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:73`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const parent = (obj as any)[ParentId];`

# ERROR 50877ea571-2437 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:75`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (obj as any)[ParentId];`

# ERROR 50877ea571-2438 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:94`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const metaProxy = (obj as any)[MetaId];`

# ERROR 50877ea571-2439 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:118`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const schema = (obj as any)[SchemaId] as Schema.Schema<T, any> | undefined;`

# ERROR 50877ea571-2440 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:125`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const meta = (obj as any)[MetaId];`

# ERROR 50877ea571-2441 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:142`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const setIdOnTarget = (target: any) => {`

# ERROR 50877ea571-2442 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:167`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `defineHiddenProperty(obj, MetaId, createProxy(fullMeta, TypedReactiveHandler.instance as any));`

# ERROR 50877ea571-2443 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:26`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getRawTarget = (value: any): any => {`

# ERROR 50877ea571-2444 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:41`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any)?.[EchoOwner];`

# ERROR 50877ea571-2445 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:65`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `value: any,`

# ERROR 50877ea571-2446 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:143`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `value: any,`

# ERROR 50877ea571-2447 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:144`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `visitor: (actualValue: any) => boolean,`

# ERROR 50877ea571-2448 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:185`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const wouldCreateCycle = (targetRoot: object, value: any): boolean =>`

# ERROR 50877ea571-2449 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:192`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const hasForeignOwner = (value: any, target: object): boolean =>`

# ERROR 50877ea571-2450 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:24`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const isProxy = (value: unknown) => !!(value as any)?.[symbolIsProxy];`

# ERROR 50877ea571-2451 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const isReactiveRecord = (value: any): boolean => {`

# ERROR 50877ea571-2452 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:35`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const isValidProxyTarget = (value: any): value is object => {`

# ERROR 50877ea571-2453 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:49`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getProxySlot = <T extends object>(proxy: any): ProxyHandlerSlot<T> => {`

# ERROR 50877ea571-2454 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const value = (proxy as any)[symbolIsProxy];`

# ERROR 50877ea571-2455 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:55`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getProxyTarget = <T extends object>(proxy: any): T => {`

# ERROR 50877ea571-2456 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:59`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getProxyHandler = <T extends object>(proxy: any): ReactiveHandler<T> => {`

# ERROR 50877ea571-2457 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:85`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(getProxySlot(obj).target as any).id = id;`

# ERROR 50877ea571-2458 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:139`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get(target: T, prop: string | symbol, receiver: any): any {`

# ERROR 50877ea571-2459 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:152`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const TRAPS: (keyof ProxyHandler<any>)[] = [`

# ERROR 50877ea571-2460 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:175`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `value: function (this: ProxyHandlerSlot<any>, ...args: any[]) {`

# ERROR 50877ea571-2461 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive-array.ts:16`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const checkArrayMutationAllowed = (arr: any, method: string): void => {`

# ERROR 50877ea571-2462 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive-array.ts:58`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `value: function (this: ReactiveArray<any>, ...args: any[]) {`

# ERROR 50877ea571-2463 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive-array.ts:62`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result!: any;`

# ERROR 50877ea571-2464 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.test.ts:37`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const queueShaped: any = {`

# ERROR 50877ea571-2465 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.test.ts:49`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const unsubscribe = Obj.subscribe(value as any, () => {});`

# ERROR 50877ea571-2466 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const target = getProxyTarget(obj as any);`

# ERROR 50877ea571-2467 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:28`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (target as any)[EventId].on(callback);`

# ERROR 50877ea571-2468 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:41`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `: T extends { [RefTypeId]: any }`

# ERROR 50877ea571-2469 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:76`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const changeFn = (obj as any)[ChangeId];`

# ERROR 50877ea571-2470 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:152`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `public static getTargetPropertySchema(target: any, prop: string | symbol): Schema.Schema.AnyNoContext {`

# ERROR 50877ea571-2471 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:153`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const schema: Schema.Schema.AnyNoContext | undefined = (target as any)[SchemaId];`

# ERROR 50877ea571-2472 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:320`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const next: any = ast.types.find((t) => (predicate != null && predicate(t)) || SchemaAST.isSuspend(t));`

# ERROR 50877ea571-2473 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:408`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const checkIdNotPresentOnSchema = (schema: Schema.Schema<any, any, any>) => {`

# ERROR 50877ea571-2474 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:49`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Obj.make(Bar, { fooRef: { id: '1', field } as any })).to.throw();`

# ERROR 50877ea571-2475 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Obj.make(Bar, { fooRef: undefined as any })).to.throw(); // Unresolved reference.`

# ERROR 50877ea571-2476 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:93`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((object.value as any).name).to.eq('Robert Smith');`

# ERROR 50877ea571-2477 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:102`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const unsubscribe = subscribe(object as any, () => {`

# ERROR 50877ea571-2478 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:258`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const unsubscribe = subscribe(obj as any, () => {`

# ERROR 50877ea571-2479 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:277`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const unsubscribe = subscribe(obj as any, () => {`

# ERROR 50877ea571-2480 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:57`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `[ParentId]?: any;`

# ERROR 50877ea571-2481 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:63`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} & ({ [key: keyof any]: any } | any[]);`

# ERROR 50877ea571-2482 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const copy = new ReactiveArray<any>();`

# ERROR 50877ea571-2483 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:113`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const copy = new ReactiveArray<any>();`

# ERROR 50877ea571-2484 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:121`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const copy: Record<string, any> = {};`

# ERROR 50877ea571-2485 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:124`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `copy[key] = deepCopy((actualValue as any)[key], visited);`

# ERROR 50877ea571-2486 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:134`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const copyHiddenProperties = (source: any, target: any): void => {`

# ERROR 50877ea571-2487 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:206`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (callback: (obj: any) => void) => executeChange(target, target, this, callback);`

# ERROR 50877ea571-2488 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:215`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const jsonSchema = (target as any).jsonSchema;`

# ERROR 50877ea571-2489 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:267`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const descriptor = Object.getOwnPropertyDescriptor(target, symbol)!;`

# ERROR 50877ea571-2490 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:276`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (target as any)[symbol];`

# ERROR 50877ea571-2491 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:287`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `public static readonly instance: ReactiveHandler<any> = new TypedReactiveHandler();`

# ERROR 50877ea571-2492 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:289`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `readonly _proxyMap = new WeakMap<object, any>();`

# ERROR 50877ea571-2493 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:319`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((target as any)[symbolIsProxy]) {`

# ERROR 50877ea571-2494 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:322`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let value = (target as any)[key];`

# ERROR 50877ea571-2495 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:329`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(target as any)[key] = value;`

# ERROR 50877ea571-2496 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:356`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get(target: ProxyTarget, prop: string | symbol, receiver: any): any {`

# ERROR 50877ea571-2497 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:384`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `set(target: ProxyTarget, prop: string | symbol, value: any, receiver: any): boolean {`

# ERROR 50877ea571-2498 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:516`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `value: any,`

# ERROR 50877ea571-2499 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:517`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): { echoRoot: object; preparedValue: any } {`

# ERROR 50877ea571-2500 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:566`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _validateValue(target: any, prop: string | symbol, value: any) {`

# ERROR 50877ea571-2501 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:583`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `inspectFn: (value: any, options?: InspectOptionsStylized) => string,`

# ERROR 50877ea571-2502 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:599`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const toJSON = (target: ProxyTarget): any => {`

# ERROR 50877ea571-2503 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:627`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `obj: any,`

# ERROR 50877ea571-2504 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:671`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let elementSchema: Schema.Schema<any>;`

# ERROR 50877ea571-2505 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:685`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const prepareTypedTarget = <T>(target: T, schema: Schema.Schema<T, any>, typeSource?: TypeSource) => {`

# ERROR 50877ea571-2506 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:695`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const validateAndReactifyTarget = <T>(target: T, schema: Schema.Schema<T, any>) => {`

# ERROR 50877ea571-2507 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:712`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const prepareDecodedTypedTarget = <T>(target: T, schema: Schema.Schema<T, any>) => {`

# ERROR 50877ea571-2508 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:717`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const makeArraysReactive = (target: any) => {`

# ERROR 50877ea571-2509 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:70`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR 50877ea571-2510 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:78`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(object.meta!.test.value).to.eq(300);`

# ERROR 50877ea571-2511 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:88`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR 50877ea571-2512 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:100`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR 50877ea571-2513 no-casts `packages/core/echo/echo/src/internal/common/types/meta.ts:95`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const metadata = (obj as any)[MetaId];`

# ERROR 50877ea571-2514 no-casts `packages/core/echo/echo/src/internal/common/types/projection.ts:39`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const variance: Projection<any>[ProjectionTypeId] = {} as Projection<any>[ProjectionTypeId];`

# ERROR 50877ea571-2515 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (obj as any)[SchemaId];`

# ERROR 50877ea571-2516 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:57`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setSchema = (obj: any, schema: Schema.Schema.AnyNoContext): void => {`

# ERROR 50877ea571-2517 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:78`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (obj as any)[TypeEntityId];`

# ERROR 50877ea571-2518 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:84`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setType = (obj: any, type: unknown): void => {`

# ERROR 50877ea571-2519 no-casts `packages/core/echo/echo/src/internal/Entity/api.ts:37`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const key = (entity as any)[MetaId]?.key;`

# ERROR 50877ea571-2520 no-casts `packages/core/echo/echo/src/internal/Entity/api.ts:74`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getDatabase = (entity: AnyEntity): any | undefined => {`

# ERROR 50877ea571-2521 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:179`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const version = DXN.getVersion(TypeMetaSchemaDXN)!;`

# ERROR 50877ea571-2522 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:245`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const entity = makeObject(persistentEntitySchema, { id } as any, meta);`

# ERROR 50877ea571-2523 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:247`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const target = getProxyTarget(entity)!;`

# ERROR 50877ea571-2524 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:274`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return entity as unknown as EchoTypeSchema<Self, {}, K, Fields>;`

# ERROR 50877ea571-2525 no-casts `packages/core/echo/echo/src/internal/Entity/guard.ts:16`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any)[KindId] !== undefined;`

# ERROR 50877ea571-2526 no-casts `packages/core/echo/echo/src/internal/Entity/guard.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any)[SnapshotKindId] !== undefined;`

# ERROR 50877ea571-2527 no-casts `packages/core/echo/echo/src/internal/Entity/object.ts:49`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fields = ((self as any).fields ?? {}) as Fields;`

# ERROR 50877ea571-2528 no-casts `packages/core/echo/echo/src/internal/Entity/object.ts:85`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return constructor as unknown as Type.ObjClass<Self, Schema.Schema.Type<_Schema>, {}>;`

# ERROR 50877ea571-2529 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:52`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type RelationSourceTargetRefs<Source = any, Target = any> = {`

# ERROR 50877ea571-2530 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:72`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type RelationEndpoint = Type.AnyObj | UnknownTypeSchema<any, EntityKind.Object>;`

# ERROR 50877ea571-2531 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:79`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `S extends UnknownTypeSchema<infer A, any> ? A : S extends Type.AnyObj ? Type.InstanceType<S> : unknown;`

# ERROR 50877ea571-2532 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:139`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fields = ((self as any).fields ?? {}) as Fields;`

# ERROR 50877ea571-2533 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:206`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return constructor as unknown as Type.RelationClass<unknown, unknown, unknown, unknown, {}>;`

# ERROR 50877ea571-2534 no-casts `packages/core/echo/echo/src/internal/Entity/type-kind.ts:52`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fields = ((self as any).fields ?? {}) as Fields;`

# ERROR 50877ea571-2535 no-casts `packages/core/echo/echo/src/internal/Entity/type-uri.ts:63`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `object: any,`

# ERROR 50877ea571-2536 no-casts `packages/core/echo/echo/src/internal/Entity/type-uri.ts:77`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const type = (object as any)[TypeId];`

# ERROR 50877ea571-2537 no-casts `packages/core/echo/echo/src/internal/Entity/util.ts:18`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getObjectEchoUri = (object: any): EID.EID | undefined => {`

# ERROR 50877ea571-2538 no-casts `packages/core/echo/echo/src/internal/Entity/version.ts:47`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const ver = (entity as any)[ObjectVersionId];`

# ERROR 50877ea571-2539 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:90`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const structuralMatch = (filterObj: any, targetObj: any, strict = true): boolean => {`

# ERROR 50877ea571-2540 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:145`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const compareValue = filter.value as any;`

# ERROR 50877ea571-2541 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:158`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) > compareValue;`

# ERROR 50877ea571-2542 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:160`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) >= compareValue;`

# ERROR 50877ea571-2543 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:162`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) < compareValue;`

# ERROR 50877ea571-2544 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:164`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) <= compareValue;`

# ERROR 50877ea571-2545 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:176`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const nestedValue = (value as any)[key];`

# ERROR 50877ea571-2546 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:205`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any) >= filter.from && (value as any) <= filter.to;`

# ERROR 50877ea571-2547 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:252`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!filterMatchValue(valueFilter, (json as any)[key])) {`

# ERROR 50877ea571-2548 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:278`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const tags = rawTags.map((tag: any) => (typeof tag?.encode === 'function' ? tag.encode() : tag));`

# ERROR 50877ea571-2549 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:38`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const prop = jsonSchema.properties!['active' as const];`

# ERROR 50877ea571-2550 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:46`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const prop = jsonSchema.properties!['email' as const];`

# ERROR 50877ea571-2551 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:56`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const prop = jsonSchema.properties!['salary' as const];`

# ERROR 50877ea571-2552 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:68`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const prop = jsonSchema.properties!['birthday' as const];`

# ERROR 50877ea571-2553 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:24`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((schema as any).exclusiveMaximum === true) {`

# ERROR 50877ea571-2554 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (schema as any).exclusiveMaximum;`

# ERROR 50877ea571-2555 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:27`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} else if ((schema as any).exclusiveMaximum === false) {`

# ERROR 50877ea571-2556 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:28`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (schema as any).exclusiveMaximum;`

# ERROR 50877ea571-2557 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:31`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((schema as any).exclusiveMinimum === true) {`

# ERROR 50877ea571-2558 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:33`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (schema as any).exclusiveMinimum;`

# ERROR 50877ea571-2559 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:34`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} else if ((schema as any).exclusiveMinimum === false) {`

# ERROR 50877ea571-2560 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:35`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (schema as any).exclusiveMinimum;`

# ERROR 50877ea571-2561 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:41`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `delete (schema as any)[key];`

# ERROR 50877ea571-2562 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:97`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const maybeGoOnArray = (value: any) => {`

# ERROR 50877ea571-2563 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:107`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const goOnRecord = (record: Record<string, any>) => {`

# ERROR 50877ea571-2564 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:50`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getNormalizedEchoAnnotations(jsonSchema.properties!.name!)!.meta![EXAMPLE_NAMESPACE]).to.deep.eq(meta);`

# ERROR 50877ea571-2565 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:66`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const nested = jsonSchema.properties!.name;`

# ERROR 50877ea571-2566 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:103`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expectReferenceAnnotation((jsonSchema.properties!.name as any).items);`

# ERROR 50877ea571-2567 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:118`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expectReferenceAnnotation(jsonSchema.properties!.name);`

# ERROR 50877ea571-2568 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:125`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(getNormalizedEchoAnnotations(jsonSchema.properties!.name!)).to.be.undefined;`

# ERROR 50877ea571-2569 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:166`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Object.keys(schema.properties!)).toEqual([`

# ERROR 50877ea571-2570 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:262`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Object.keys(jsonSchema.properties!).length).toBeGreaterThan(0);`

# ERROR 50877ea571-2571 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:306`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(Schema.asserts(JsonSchemaType) as any)(jsonSchema);`

# ERROR 50877ea571-2572 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:572`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(jsonSchema2.properties!.name.description).to.eq('Name');`

# ERROR 50877ea571-2573 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:255`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `result = Schema.Union(...root.enum!.map((e) => Schema.Literal(e)));`

# ERROR 50877ea571-2574 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:257`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `result = Schema.Union(...root.oneOf!.map((v) => toEffectSchema(v, defs)));`

# ERROR 50877ea571-2575 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:259`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `result = Schema.Union(...root.anyOf!.map((v) => toEffectSchema(v, defs)));`

# ERROR 50877ea571-2576 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:261`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (root.allOf!.length === 1) {`

# ERROR 50877ea571-2577 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:262`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `result = toEffectSchema(root.allOf![0], defs);`

# ERROR 50877ea571-2578 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:311`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const refSegments = root.$ref!.split('/');`

# ERROR 50877ea571-2579 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:330`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const echoRefinement: JsonSchemaEchoAnnotations = (root as any)[ECHO_ANNOTATIONS_NS_DEPRECATED_KEY];`

# ERROR 50877ea571-2580 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:342`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(fields as any)[key] = root.required?.includes(key)`

# ERROR 50877ea571-2581 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:349`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `fields = orderKeys(fields, root.propertyOrder as any);`

# ERROR 50877ea571-2582 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:352`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let schema: Schema.Schema<any, any, unknown>;`

# ERROR 50877ea571-2583 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:377`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return schema.annotations(annotations) as any;`

# ERROR 50877ea571-2584 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:381`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const echoRefinement: JsonSchemaEchoAnnotations = (root as any)[ECHO_ANNOTATIONS_NS_DEPRECATED_KEY];`

# ERROR 50877ea571-2585 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:383`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((echoRefinement as any)?.reference != null) {`

# ERROR 50877ea571-2586 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:387`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(echoRefinement as any).reference.typename,`

# ERROR 50877ea571-2587 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:388`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(echoRefinement as any).reference.version,`

# ERROR 50877ea571-2588 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:396`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const refToEffectSchema = (root: any): Schema.Schema.AnyNoContext => {`

# ERROR 50877ea571-2589 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:418`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const annotations_toJsonSchemaFields = (annotations: SchemaAST.Annotations): Record<symbol, any> => {`

# ERROR 50877ea571-2590 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:419`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const schemaFields: Record<string, any> = {};`

# ERROR 50877ea571-2591 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:424`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `echoAnnotations[key as keyof JsonSchemaEchoAnnotations] = annotations[annotationId] as any;`

# ERROR 50877ea571-2592 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:495`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const annotations: Types.Mutable<Schema.Annotations.Schema<any>> = {};`

# ERROR 50877ea571-2593 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:526`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `annotations[annotationId] = (schema as any)[key];`

# ERROR 50877ea571-2594 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:26`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const isRef = (obj: unknown): obj is Ref.Ref<any> =>`

# ERROR 50877ea571-2595 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:31`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const db = getDatabase(snapshot as any);`

# ERROR 50877ea571-2596 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:35`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const obj = db.getObjectById((snapshot as any).id);`

# ERROR 50877ea571-2597 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:47`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get.setSelf(getSnapshot(obj) as unknown as Obj.Snapshot<T>);`

# ERROR 50877ea571-2598 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:52`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return getSnapshot(obj) as unknown as Obj.Snapshot<T>;`

# ERROR 50877ea571-2599 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:70`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get.setSelf(isDeleted(target) ? undefined : (getSnapshot(target) as unknown as Obj.Snapshot<T>));`

# ERROR 50877ea571-2600 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:74`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return isDeleted(target) ? undefined : (getSnapshot(target) as unknown as Obj.Snapshot<T>);`

# ERROR 50877ea571-2601 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:166`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get.setSelf(getSnapshot(entity) as unknown as Entity.Snapshot);`

# ERROR 50877ea571-2602 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:171`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return getSnapshot(entity) as unknown as Entity.Snapshot;`

# ERROR 50877ea571-2603 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:182`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get.setSelf(getSnapshot(relation) as unknown as Relation.Snapshot<T>);`

# ERROR 50877ea571-2604 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:187`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return getSnapshot(relation) as unknown as Relation.Snapshot<T>;`

# ERROR 50877ea571-2605 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:199`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (objOrRef: Obj.Unknown | Ref.Ref<any>): Atom.Atom<any> => {`

# ERROR 50877ea571-2606 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:201`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return refFamily(objOrRef as any);`

# ERROR 50877ea571-2607 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:206`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return objectFamily(obj as any);`

# ERROR 50877ea571-2608 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:217`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (objOrRef: Obj.Unknown | Ref.Ref<any>, key: any): Atom.Atom<any> => {`

# ERROR 50877ea571-2609 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:219`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return refPropertyFamily(objOrRef as Ref.Ref<any>)(key);`

# ERROR 50877ea571-2610 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:234`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (objOrRef: Obj.Unknown | Ref.Ref<any>): Atom.Atom<any> => {`

# ERROR 50877ea571-2611 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:236`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return refWithReactiveFamily(objOrRef as Ref.Ref<any>);`

# ERROR 50877ea571-2612 no-casts `packages/core/echo/echo/src/internal/Obj/clone.ts:66`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const props: any = deepMapValues(data, mapRef);`

# ERROR 50877ea571-2613 no-casts `packages/core/echo/echo/src/internal/Obj/common.ts:46`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): Schema.SchemaClass<any> => {`

# ERROR 50877ea571-2614 no-casts `packages/core/echo/echo/src/internal/Obj/common.ts:68`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} as any;`

# ERROR 50877ea571-2615 no-casts `packages/core/echo/echo/src/internal/Obj/common.ts:72`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `_A: (_: any) => _,`

# ERROR 50877ea571-2616 no-casts `packages/core/echo/echo/src/internal/Obj/common.ts:73`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `_I: (_: any) => _,`

# ERROR 50877ea571-2617 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:44`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contact as any)['@type']).toBeUndefined();`

# ERROR 50877ea571-2618 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:79`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `[Relation.Source]: person1 as any,`

# ERROR 50877ea571-2619 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:80`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `[Relation.Target]: person2 as any,`

# ERROR 50877ea571-2620 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.ts:71`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (input: any, props: any): any => {`

# ERROR 50877ea571-2621 no-casts `packages/core/echo/echo/src/internal/Obj/deleted.ts:12`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((obj as any)[ObjectDeletedId] === undefined) {`

# ERROR 50877ea571-2622 no-casts `packages/core/echo/echo/src/internal/Obj/deleted.ts:18`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (obj as any)[ObjectDeletedId] ?? false;`

# ERROR 50877ea571-2623 no-casts `packages/core/echo/echo/src/internal/Obj/inspect.ts:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const attachedTypedObjectInspector = (obj: any) => {`

# ERROR 50877ea571-2624 no-casts `packages/core/echo/echo/src/internal/Obj/inspect.ts:44`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `[ATTR_META]: (this as any)[MetaId], // TODO(dmaretskyi): Couldn't use getMeta since that throw's if the object has no meta.`

# ERROR 50877ea571-2625 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:67`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contactFromJson as any)[TypeId]).toEqual(Type.getURI(TestSchema.Person));`

# ERROR 50877ea571-2626 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:68`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contactFromJson as any)[KindId]).toBe(EntityKind.Object);`

# ERROR 50877ea571-2627 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:69`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contactFromJson as any)[RelationSourceId]).toBeUndefined();`

# ERROR 50877ea571-2628 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:70`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contactFromJson as any)[RelationTargetId]).toBeUndefined();`

# ERROR 50877ea571-2629 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:71`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((contactFromJson as any)[MetaId]).toEqual({`

# ERROR 50877ea571-2630 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:88`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(taskFromJson.assignee!.uri).toEqual(EID.make({ entityId: contact.id }));`

# ERROR 50877ea571-2631 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:89`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(taskFromJson.assignee!.target).toEqual(contact);`

# ERROR 50877ea571-2632 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:90`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(await taskFromJson.assignee!.load()).toEqual(contact);`

# ERROR 50877ea571-2633 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:91`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((taskFromJson as any)[TypeId]).toEqual(Type.getURI(TestSchema.Task));`

# ERROR 50877ea571-2634 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:92`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((taskFromJson as any)[KindId]).toBe(EntityKind.Object);`

# ERROR 50877ea571-2635 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:93`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((taskFromJson as any)[RelationSourceId]).toBeUndefined();`

# ERROR 50877ea571-2636 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:94`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((taskFromJson as any)[RelationTargetId]).toBeUndefined();`

# ERROR 50877ea571-2637 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:95`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((taskFromJson as any)[MetaId]).toEqual({ keys: [], tags: [], annotations: {} });`

# ERROR 50877ea571-2638 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:102`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const contactFromJson: any = await objectFromJSON(contactJson);`

# ERROR 50877ea571-2639 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:114`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const json = objectToJSON(expando) as any;`

# ERROR 50877ea571-2640 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:118`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fromJson = (await objectFromJSON(json)) as any;`

# ERROR 50877ea571-2641 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:120`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(fromJson[MetaId].tags.map((ref: any) => ref.uri)).toEqual(['echo:/TAGLEGACY']);`

# ERROR 50877ea571-2642 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:132`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((expandoFromJson as any)[ATTR_TYPE]).toBeUndefined();`

# ERROR 50877ea571-2643 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:143`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((expandoFromJson as any)[ATTR_TYPE]).toBeUndefined();`

# ERROR 50877ea571-2644 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:159`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const typeJson = objectToJSON(typeEntity as any);`

# ERROR 50877ea571-2645 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:161`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const reconstructed = (await objectFromJSON(typeJson, { refResolver })) as any;`

# ERROR 50877ea571-2646 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:250`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const relationJson = objectToJSON(relation as any);`

# ERROR 50877ea571-2647 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:258`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const live = (await decodeLive(relationJson, { refResolver })) as any;`

# ERROR 50877ea571-2648 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:276`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const live: any = await decodeLive(contactJson);`

# ERROR 50877ea571-2649 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:288`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const contactJson: any = objectToJSON(contact);`

# ERROR 50877ea571-2650 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:313`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `live.tasks!.push(Ref.make(task2));`

# WARN 50877ea571-2651 no-compat-shims `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:47`

`attachTypedJsonSerializer` is re-exported here "for backward compatibility," but the real implementation lives in `../common/proxy/json-serializer.ts` (imported at the top of this file from `'../common/proxy'`). `create-object.ts` still imports it from `./json-serializer` instead of `../common/proxy`. Per `no-compat-shims`, point `create-object.ts` at the real location and drop the re-export.

# ERROR 50877ea571-2652 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:51`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `T extends Ref<any>`

# ERROR 50877ea571-2653 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:102`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let obj: any;`

# ERROR 50877ea571-2654 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:184`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_TYPE] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2655 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:185`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_META] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2656 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:186`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_DELETED] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2657 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:187`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_SELF_URI] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2658 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:188`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_SELF_URI_LEGACY] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2659 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:189`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_RELATION_SOURCE] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2660 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:190`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant((obj as any)[ATTR_RELATION_TARGET] === undefined, 'Invalid object model');`

# ERROR 50877ea571-2661 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:199`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const normalizeMeta = (meta: any): any => {`

# ERROR 50877ea571-2662 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:231`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const restoreUint8Arrays = (data: unknown): any =>`

# ERROR 50877ea571-2663 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:249`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = jsonData as any;`

# ERROR 50877ea571-2664 no-casts `packages/core/echo/echo/src/internal/Obj/schema-validator.test.ts:53`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `target: any;`

# ERROR 50877ea571-2665 no-casts `packages/core/echo/echo/src/internal/Obj/schema-validator.test.ts:55`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `valueToAssign: any;`

# ERROR 50877ea571-2666 no-casts `packages/core/echo/echo/src/internal/Obj/schema-validator.test.ts:178`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const target: any = { array: [1, 2, null], object: { field: 3 } };`

# WARN 50877ea571-2667 no-compat-shims `packages/core/echo/echo/src/internal/Obj/schema-validator.ts:5`

This entire file is a one-line compat shim: `export { SchemaValidator, checkIdNotPresentOnSchema } from '../common/proxy';`, explicitly labeled "Re-export from proxy for backward compatibility." `Obj/index.ts` still does `export * from './schema-validator'` and `schema-validator.test.ts` imports from this local shim rather than `../common/proxy` directly. Per `no-compat-shims`, update both call sites to the real `common/proxy` location and delete this file.

# ERROR 50877ea571-2668 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.test.ts:115`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let result: any;`

# ERROR 50877ea571-2669 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:19`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setValue = (obj: Mutable<any>, path: readonly (string | number)[], value: any): void => {`

# ERROR 50877ea571-2670 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:115`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getDefaultValueForType = (ast: SchemaAST.AST | undefined): any => {`

# ERROR 50877ea571-2671 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:140`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const createObjectWithDefaults = (ast: SchemaAST.AST | undefined): any => {`

# ERROR 50877ea571-2672 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:146`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const obj: any = {};`

# ERROR 50877ea571-2673 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:32`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `source: any,`

# ERROR 50877ea571-2674 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:33`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `target: any,`

# ERROR 50877ea571-2675 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:74`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2676 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:78`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `snapshot[SnapshotKindId] = (obj as any)[KindId];`

# ERROR 50877ea571-2677 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:83`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const source = obj as any;`

# ERROR 50877ea571-2678 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:99`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `copySymbolProperty(source, snapshot, MetaId, (meta: any) => ({`

# ERROR 50877ea571-2679 no-casts `packages/core/echo/echo/src/internal/Query/query-result-effect.ts:32`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} as any;`

# ERROR 50877ea571-2680 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `unsubscribeTarget = subscribe(target as any, () => {`

# ERROR 50877ea571-2681 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:27`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const deleted = !!(target as any)[ObjectDeletedId];`

# ERROR 50877ea571-2682 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:30`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const deleted = !!(target as any)[ObjectDeletedId];`

# ERROR 50877ea571-2683 no-casts `packages/core/echo/echo/src/internal/Ref/ref.test.ts:56`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `'/': getObjectEchoUri(task)!.toString(),`

# ERROR 50877ea571-2684 no-casts `packages/core/echo/echo/src/internal/Ref/ref.test.ts:75`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `'tasks': [{ '/': getObjectEchoUri(task)!.toString() }],`

# ERROR 50877ea571-2685 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:79`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `typename: (ast.annotations[ReferenceAnnotationId] as any).typename,`

# ERROR 50877ea571-2686 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:80`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `version: (ast.annotations[ReferenceAnnotationId] as any).version,`

# ERROR 50877ea571-2687 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:85`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const RefTypeId: unique symbol = Symbol.for('@dxos/echo/internal/Ref') as any;`

# ERROR 50877ea571-2688 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:106`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<S extends Type.AnyEntity | UnknownTypeSchema<any, any> = Type.AnyEntity>(`

# ERROR 50877ea571-2689 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:113`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `: S extends UnknownTypeSchema<infer A, any>`

# ERROR 50877ea571-2690 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:121`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `isRef: (obj: unknown) => obj is Ref<any>;`

# ERROR 50877ea571-2691 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:126`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `hasEntityId: (id: EntityId) => (ref: Ref<any>) => boolean;`

# ERROR 50877ea571-2692 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:131`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `isRefSchema: (schema: Schema.Schema<any, any>) => schema is RefSchema<any>;`

# ERROR 50877ea571-2693 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:148`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `fromURI: (uri: URI.URI) => Ref<any>;`

# ERROR 50877ea571-2694 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:154`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Ref: RefFn = (input: any): RefSchema<any> => {`

# ERROR 50877ea571-2695 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:263`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Ref.isRef = (obj: any): obj is Ref<any> => {`

# ERROR 50877ea571-2696 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:267`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Ref.hasEntityId = (id: EntityId) => (ref: Ref<any>) => {`

# ERROR 50877ea571-2697 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:272`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Ref.isRefSchema = (schema: Schema.Schema<any, any>): schema is RefSchema<any> => {`

# ERROR 50877ea571-2698 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:293`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Ref.fromURI = (uri: URI.URI): Ref<any> => {`

# ERROR 50877ea571-2699 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:314`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): Schema.SchemaClass<Ref<any>, EncodedReference> => {`

# ERROR 50877ea571-2700 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:322`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `$ref: echoUri ?? DXN.make(typename!),`

# ERROR 50877ea571-2701 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:328`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const refSchema = Schema.declare<Ref<any>, EncodedReference, []>(`

# ERROR 50877ea571-2702 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:335`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return EncodedReference.fromURI((value as Ref<any>).uri);`

# ERROR 50877ea571-2703 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:660`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setRefResolver = (ref: Ref<any>, resolver: RefResolver) => {`

# ERROR 50877ea571-2704 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:668`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getRefSavedTarget = (ref: Ref<any>): AnyProperties | undefined => {`

# ERROR 50877ea571-2705 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:674`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const refVariance: Ref<any>[typeof RefTypeId] = {`

# ERROR 50877ea571-2706 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:675`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `_T: null as any,`

# ERROR 50877ea571-2707 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:678`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const refFromEncodedReference = (encodedReference: EncodedReference, resolver?: RefResolver): Ref<any> => {`

# ERROR 50877ea571-2708 no-casts `packages/core/echo/echo/src/internal/Type/compose.ts:25`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const propSchema = source.properties![prop]; // TODO(dmaretskyi): Find by json-path instead.`

# ERROR 50877ea571-2709 no-casts `packages/core/echo/echo/src/internal/Type/compose.ts:28`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const resultProp = result.properties[prop] as Record<string, any>;`

# ERROR 50877ea571-2710 no-casts `packages/core/echo/echo/src/internal/Type/manipulation.ts:18`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as any as Schema.Schema.AnyNoContext;`

# ERROR 50877ea571-2711 no-casts `packages/core/echo/echo/src/Json.test.ts:23`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} as unknown as Database.Database;`

# ERROR 50877ea571-2712 no-casts `packages/core/echo/echo/src/Json.test.ts:124`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const node: any = { name: 'self' };`

# ERROR 50877ea571-2713 no-casts `packages/core/echo/echo/src/Json.test.ts:171`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const out = JSON.parse(safeStringify(document, replacer, 0)!);`

# ERROR 50877ea571-2714 no-casts `packages/core/echo/echo/src/Json.ts:16`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type JsonReplacer = (key: string, value: any) => any;`

# ERROR 50877ea571-2715 no-casts `packages/core/echo/echo/src/Json.ts:30`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const toJson = (obj: Obj.Any): unknown => (typeof (obj as any).toJSON === 'function' ? (obj as any).toJSON() : obj);`

# ERROR 50877ea571-2716 no-casts `packages/core/echo/echo/src/Json.ts:56`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return function (this: any, key: string, value: any) {`

# ERROR 50877ea571-2717 no-casts `packages/core/echo/echo/src/Migration.ts:76`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onMigration?: (params: OnMigrateProps<any, any>) => Promise<void>;`

# ERROR 50877ea571-2718 no-casts `packages/core/echo/echo/src/Migration.ts:111`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `transform: options.transform as any,`

# ERROR 50877ea571-2719 no-casts `packages/core/echo/echo/src/Migration.ts:112`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onMigration: options.onMigration as any,`

# ERROR 50877ea571-2720 no-casts `packages/core/echo/echo/src/Obj.test.ts:45`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((snapshot as any)[Entity.KindId]).toBeUndefined();`

# ERROR 50877ea571-2721 no-casts `packages/core/echo/echo/src/Obj.test.ts:240`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Obj.getKeys(plain as any, SOURCE)).toThrow(/EntityMeta not found/);`

# ERROR 50877ea571-2722 no-casts `packages/core/echo/echo/src/Obj.test.ts:440`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `person.address!.city = 'New York';`

# ERROR 50877ea571-2723 no-casts `packages/core/echo/echo/src/Obj.test.ts:579`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `type HasIndexSignature = Obj.Any extends { [key: string]: any } ? true : false;`

# ERROR 50877ea571-2724 no-casts `packages/core/echo/echo/src/Obj.ts:86`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as internal.UnknownTypeSchema<Unknown, typeof Entity.Kind.Object>;`

# ERROR 50877ea571-2725 no-casts `packages/core/echo/echo/src/Obj.ts:174`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export function make(input: Type.AnyObj, props: any): OfShape<any> {`

# ERROR 50877ea571-2726 no-casts `packages/core/echo/echo/src/Obj.ts:194`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const filterUndefined: any = Object.fromEntries(Object.entries(props).filter(([_, v]) => v !== undefined));`

# ERROR 50877ea571-2727 no-casts `packages/core/echo/echo/src/Obj.ts:198`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const value = (props as any)[sym];`

# ERROR 50877ea571-2728 no-casts `packages/core/echo/echo/src/Obj.ts:225`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return typeof obj === 'object' && obj !== null && (obj as any)[Entity.SnapshotKindId] === Entity.Kind.Object;`

# ERROR 50877ea571-2729 no-casts `packages/core/echo/echo/src/Obj.ts:247`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getSnapshot: <T extends Unknown>(obj: T) => Snapshot<T> = objInternal.getSnapshot as any;`

# ERROR 50877ea571-2730 no-casts `packages/core/echo/echo/src/Obj.ts:410`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getValue = (obj: Unknown | Snapshot, path: readonly (string | number)[]): any => {`

# ERROR 50877ea571-2731 no-casts `packages/core/echo/echo/src/Obj.ts:440`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setValue: (obj: Mutable<Unknown>, path: readonly (string | number)[], value: any) => void =`

# ERROR 50877ea571-2732 no-casts `packages/core/echo/echo/src/Obj.ts:441`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `objInternal.setValue as any;`

# ERROR 50877ea571-2733 no-casts `packages/core/echo/echo/src/Obj.ts:489`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2734 no-casts `packages/core/echo/echo/src/Obj.ts:519`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2735 no-casts `packages/core/echo/echo/src/Obj.ts:653`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = Function.dual(2, (entity: Unknown | Snapshot, source?: string): ForeignKey[] => internal.getKeys(entity, source!));`

# ERROR 50877ea571-2736 no-casts `packages/core/echo/echo/src/Obj.ts:749`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Parent: unique symbol = internal.ParentId as any;`

# ERROR 50877ea571-2737 no-casts `packages/core/echo/echo/src/Obj.ts:924`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) => Promise<Unknown> = objInternal.objectFromJSON as any;`

# ERROR 50877ea571-2738 no-casts `packages/core/echo/echo/src/Order.ts:18`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type Any = Order<any>;`

# ERROR 50877ea571-2739 no-casts `packages/core/echo/echo/src/Order.ts:20`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `class OrderClass implements Order<any> {`

# ERROR 50877ea571-2740 no-casts `packages/core/echo/echo/src/Order.ts:21`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private static 'variance': Order<any>[OrderTypeId] = {} as Order<any>[OrderTypeId];`

# ERROR 50877ea571-2741 no-casts `packages/core/echo/echo/src/Order.ts:36`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const natural = (direction: QueryAST.OrderDirection = 'asc'): Order<any> =>`

# ERROR 50877ea571-2742 no-casts `packages/core/echo/echo/src/Query.test.ts:614`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(person as any)).toThrow(TypeError);`

# ERROR 50877ea571-2743 no-casts `packages/core/echo/echo/src/Query.test.ts:615`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(person as any)).toThrow(`

# ERROR 50877ea571-2744 no-casts `packages/core/echo/echo/src/Query.test.ts:621`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(undefined as any)).toThrow(TypeError);`

# ERROR 50877ea571-2745 no-casts `packages/core/echo/echo/src/Query.test.ts:622`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(undefined as any)).toThrow(`

# ERROR 50877ea571-2746 no-casts `packages/core/echo/echo/src/Query.test.ts:628`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(null as any)).toThrow(TypeError);`

# ERROR 50877ea571-2747 no-casts `packages/core/echo/echo/src/Query.test.ts:629`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(() => Query.select(Filter.type(TestSchema.Person)).from(null as any)).toThrow(`

# ERROR 50877ea571-2748 no-casts `packages/core/echo/echo/src/Query.test.ts:1129`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const _isAssignable: Obj.Unknown = null as any as Filter.Type<typeof filter>;`

# ERROR 50877ea571-2749 no-casts `packages/core/echo/echo/src/Query.ts:106`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `referencedBy(): Query<any>;`

# ERROR 50877ea571-2750 no-casts `packages/core/echo/echo/src/Query.ts:146`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `parent(): Query<any>;`

# ERROR 50877ea571-2751 no-casts `packages/core/echo/echo/src/Query.ts:152`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `children(): Query<any>;`

# ERROR 50877ea571-2752 no-casts `packages/core/echo/echo/src/Query.ts:196`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `aggregate<const A extends Record<string, Aggregate.Aggregate<T, any>>>(`

# ERROR 50877ea571-2753 no-casts `packages/core/echo/echo/src/Query.ts:303`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type Any = Query<any>;`

# ERROR 50877ea571-2754 no-casts `packages/core/echo/echo/src/Query.ts:326`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `select(filter: Filter.Any | Filter.Props<any>): Any {`

# ERROR 50877ea571-2755 no-casts `packages/core/echo/echo/src/Query.ts:342`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `project(property: string): Projection<any> {`

# ERROR 50877ea571-2756 no-casts `packages/core/echo/echo/src/Query.ts:641`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<S extends internal.UnknownTypeSchema<any, any>>(`

# ERROR 50877ea571-2757 no-casts `packages/core/echo/echo/src/Query.ts:649`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(uri: URI.URI, predicates?: Filter.Props<unknown>): Query<any>;`

# ERROR 50877ea571-2758 no-casts `packages/core/echo/echo/src/Ref.ts:66`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<T extends Type$.Type<any>>(type: T): RefSchema<Type$.InstanceType<T>>;`

# ERROR 50877ea571-2759 no-casts `packages/core/echo/echo/src/Ref.ts:71`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<S extends internal.UnknownTypeSchema<any, any>>(schema: S): RefSchema<Schema.Schema.Type<S> & Obj.Unknown>;`

# ERROR 50877ea571-2760 no-casts `packages/core/echo/echo/src/Ref.ts:72`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = refInternal.Ref as any;`

# ERROR 50877ea571-2761 no-casts `packages/core/echo/echo/src/Ref.ts:110`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const fromURI = (uri: URI.URI): refInternal.Ref<any> => refInternal.Ref.fromURI(uri);`

# ERROR 50877ea571-2762 no-casts `packages/core/echo/echo/src/Registry.ts:154`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (yield* Effect.promise(() => registry.query(queryOrFilter as any).run())) as any;`

# ERROR 50877ea571-2763 no-casts `packages/core/echo/echo/src/Relation.test.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((snapshot as any)[Entity.KindId]).toBeUndefined();`

# ERROR 50877ea571-2764 no-casts `packages/core/echo/echo/src/Relation.ts:78`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as internal.UnknownTypeSchema<Unknown, typeof Entity.Kind.Relation>;`

# ERROR 50877ea571-2765 no-casts `packages/core/echo/echo/src/Relation.ts:112`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Source: unique symbol = entityInternal.RelationSourceId as any;`

# ERROR 50877ea571-2766 no-casts `packages/core/echo/echo/src/Relation.ts:115`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Target: unique symbol = entityInternal.RelationTargetId as any;`

# ERROR 50877ea571-2767 no-casts `packages/core/echo/echo/src/Relation.ts:131`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `type MakePropsInternal<T extends Endpoints<any, any>> = {`

# ERROR 50877ea571-2768 no-casts `packages/core/echo/echo/src/Relation.ts:170`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `meta = props[internal.MetaId] as any;`

# ERROR 50877ea571-2769 no-casts `packages/core/echo/echo/src/Relation.ts:177`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(props as any)[internal.RelationSourceDXNId] = sourceDXN;`

# ERROR 50877ea571-2770 no-casts `packages/core/echo/echo/src/Relation.ts:178`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(props as any)[internal.RelationTargetDXNId] = targetDXN;`

# ERROR 50877ea571-2771 no-casts `packages/core/echo/echo/src/Relation.ts:182`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return internal.makeObject(schema as any, props as any, meta, type as any) as any;`

# ERROR 50877ea571-2772 no-casts `packages/core/echo/echo/src/Relation.ts:207`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2773 no-casts `packages/core/echo/echo/src/Relation.ts:222`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const kind = (value as any)[Entity.KindId] ?? (value as any)[Entity.SnapshotKindId];`

# ERROR 50877ea571-2774 no-casts `packages/core/echo/echo/src/Relation.ts:230`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (value as any)[Entity.SnapshotKindId] === internal.EntityKind.Relation;`

# ERROR 50877ea571-2775 no-casts `packages/core/echo/echo/src/Relation.ts:352`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getSnapshot: <T extends Unknown>(rel: T) => Snapshot<T> = internal.getSnapshot as any;`

# ERROR 50877ea571-2776 no-casts `packages/core/echo/echo/src/Relation.ts:376`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const getValue = (rel: Unknown | Snapshot, path: readonly (string | number)[]): any => {`

# ERROR 50877ea571-2777 no-casts `packages/core/echo/echo/src/Relation.ts:387`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const setValue: (rel: Mutable<Unknown>, path: readonly (string | number)[], value: any) => void =`

# ERROR 50877ea571-2778 no-casts `packages/core/echo/echo/src/Relation.ts:388`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `internal.setValue as any;`

# ERROR 50877ea571-2779 no-casts `packages/core/echo/echo/src/testing/api.test.ts:46`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const x: any = {};`

# ERROR 50877ea571-2780 no-casts `packages/core/echo/echo/src/testing/api.test.ts:87`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const any: Entity.Unknown = null as any;`

# ERROR 50877ea571-2781 no-casts `packages/core/echo/echo/src/testing/util.ts:27`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `getStaticTypeSchema(TypeSchema) as any,`

# ERROR 50877ea571-2782 no-casts `packages/core/echo/echo/src/testing/util.ts:31`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as Type.Type;`

# ERROR 50877ea571-2783 no-casts `packages/core/echo/echo/src/testing/util.ts:37`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const prepareAstForCompare = (obj: SchemaAST.AST): any =>`

# ERROR 50877ea571-2784 no-casts `packages/core/echo/echo/src/testing/util.ts:38`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `deepMapValues(obj, (value: any, recurse: any) => {`

# ERROR 50877ea571-2785 no-casts `packages/core/echo/echo/src/testing/util.ts:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `for (const sym of Object.getOwnPropertySymbols(clone as any)) {`

# ERROR 50877ea571-2786 no-casts `packages/core/echo/echo/src/Type.test.ts:81`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity as any)[Entity.KindId]).toBe(Entity.Kind.Type);`

# ERROR 50877ea571-2787 no-casts `packages/core/echo/echo/src/Type.test.ts:126`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity as any)[Entity.KindId]).toBe(Entity.Kind.Type);`

# ERROR 50877ea571-2788 no-casts `packages/core/echo/echo/src/Type.test.ts:128`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity.jsonSchema as any).relationSource?.$ref).toBe(Type.getURI(TestSchema.Person).toString());`

# ERROR 50877ea571-2789 no-casts `packages/core/echo/echo/src/Type.test.ts:129`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity.jsonSchema as any).relationTarget?.$ref).toBe(Type.getURI(TestSchema.Organization).toString());`

# ERROR 50877ea571-2790 no-casts `packages/core/echo/echo/src/Type.test.ts:139`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity.jsonSchema as any).relationSource?.$ref).toBe(unknownURI);`

# ERROR 50877ea571-2791 no-casts `packages/core/echo/echo/src/Type.test.ts:140`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((entity.jsonSchema as any).relationTarget?.$ref).toBe(unknownURI);`

# ERROR 50877ea571-2792 no-casts `packages/core/echo/echo/src/Type.test.ts:187`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((TestSchema.Person as any)['~@dxos/echo/SchemaKind']).toBe(Entity.Kind.Object);`

# ERROR 50877ea571-2793 no-casts `packages/core/echo/echo/src/Type.test.ts:192`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((TestSchema.HasManager as any)['~@dxos/echo/SchemaKind']).toBe(Entity.Kind.Relation);`

# ERROR 50877ea571-2794 no-casts `packages/core/echo/echo/src/Type.ts:144`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} = (dxn, options) => (schema) => internal.makeObjectType(dxn, schema, options) as any;`

# ERROR 50877ea571-2795 no-casts `packages/core/echo/echo/src/Type.ts:156`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Type: Type<typeInternal.TypeSchema> = typeInternal.TypeSchema as any;`

# ERROR 50877ea571-2796 no-casts `packages/core/echo/echo/src/Type.ts:193`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `internal.getStaticTypeSchema(typeInternal.TypeSchema) as any,`

# ERROR 50877ea571-2797 no-casts `packages/core/echo/echo/src/Type.ts:194`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `data as any,`

# ERROR 50877ea571-2798 no-casts `packages/core/echo/echo/src/Type.ts:201`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as Type<typeInternal.TypeSchema>;`

# ERROR 50877ea571-2799 no-casts `packages/core/echo/echo/src/Type.ts:214`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `source: AnyObj | internal.UnknownTypeSchema<any, typeof EntityModule.Kind.Object>;`

# ERROR 50877ea571-2800 no-casts `packages/core/echo/echo/src/Type.ts:215`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `target: AnyObj | internal.UnknownTypeSchema<any, typeof EntityModule.Kind.Object>;`

# ERROR 50877ea571-2801 no-casts `packages/core/echo/echo/src/Type.ts:232`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `internal.getStaticTypeSchema(typeInternal.TypeSchema) as any,`

# ERROR 50877ea571-2802 no-casts `packages/core/echo/echo/src/Type.ts:233`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `{ ...rest, jsonSchema: enrichedJsonSchema } as any,`

# ERROR 50877ea571-2803 no-casts `packages/core/echo/echo/src/Type.ts:240`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `) as unknown as Type<typeInternal.TypeSchema>;`

# ERROR 50877ea571-2804 no-casts `packages/core/echo/echo/src/Type.ts:305`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `source: Obj<SourceInstance, any> | internal.UnknownTypeSchema<SourceInstance, typeof EntityModule.Kind.Object>;`

# ERROR 50877ea571-2805 no-casts `packages/core/echo/echo/src/Type.ts:306`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `target: Obj<TargetInstance, any> | internal.UnknownTypeSchema<TargetInstance, typeof EntityModule.Kind.Object>;`

# ERROR 50877ea571-2806 no-casts `packages/core/echo/echo/src/Type.ts:330`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2807 no-casts `packages/core/echo/echo/src/Type.ts:399`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type AnyRef = Schema.Schema<internal.Ref<any>, EncodedReference>;`

# ERROR 50877ea571-2808 no-casts `packages/core/echo/echo/src/Type.ts:647`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `T extends RelationClass<any, infer Props, infer Source, infer Target, any>`

# ERROR 50877ea571-2809 no-casts `packages/core/echo/echo/src/Type.ts:649`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `: T extends Relation<infer Props, infer Source, infer Target, any>`

# ERROR 50877ea571-2810 no-casts `packages/core/echo/echo/src/Type.ts:651`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `: T extends ObjClass<any, infer A, any>`

# ERROR 50877ea571-2811 no-casts `packages/core/echo/echo/src/Type.ts:653`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `: T extends Obj<infer A, any>`

# ERROR 50877ea571-2812 no-casts `packages/core/echo/feed/src/feed-store.test.ts:231`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(result2.blocks[0].insertionId!).toBeGreaterThan(result1.blocks[0].insertionId!);`

# ERROR 50877ea571-2813 no-casts `packages/core/echo/feed/src/feed-store.test.ts:289`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(block1.insertionId).toBeLessThan(block2.insertionId!);`

# ERROR 50877ea571-2814 no-casts `packages/core/echo/feed/src/feed-store.test.ts:290`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(block2.insertionId).toBeLessThan(block3.insertionId!);`

# ERROR 50877ea571-2815 no-casts `packages/core/echo/feed/src/feed-store.test.ts:304`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `cursor: invalidCursor as any,`

# ERROR 50877ea571-2816 no-casts `packages/core/echo/feed/src/feed-store.test.ts:310`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const cause: any = result.cause;`

# ERROR 50877ea571-2817 no-casts `packages/core/echo/feed/src/feed-store.test.ts:339`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `cursor: validCursor as any,`

# ERROR 50877ea571-2818 no-casts `packages/core/echo/feed/src/feed-store.test.ts:400`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(blocks[0].position).toBeLessThan(blocks[1].position!);`

# ERROR 50877ea571-2819 no-casts `packages/core/echo/feed/src/feed-store.test.ts:401`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(blocks[1].position).toBeLessThan(blocks[2].position!);`

# ERROR 50877ea571-2820 no-casts `packages/core/echo/feed/src/feed-store.ts:153`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const feed = { spaceId, feedId: block.feedId!, feedNamespace };`

# ERROR 50877ea571-2821 no-casts `packages/core/echo/feed/src/feed-store.ts:157`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const blockId = blockNaturalKey(block.feedId!, block.actorId, block.sequence);`

# ERROR 50877ea571-2822 no-casts `packages/core/echo/feed/src/feed-store.ts:175`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const blockId = blockNaturalKey(row.feedId!, row.actorId, row.sequence);`

# ERROR 50877ea571-2823 no-casts `packages/core/echo/feed/src/feed-store.ts:195`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `{ encryptionKeyId: row.encryptionKeyId!, iv: new Uint8Array(row.iv!), ciphertext: data },`

# ERROR 50877ea571-2824 no-casts `packages/core/echo/feed/src/feed-store.ts:196`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `{ feed: { spaceId, feedId: row.feedId!, feedNamespace }, blockId },`

# ERROR 50877ea571-2825 no-casts `packages/core/echo/feed/src/feed-store.ts:366`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(feedId) => this.#ensureFeed(request.spaceId!, feedId),`

# ERROR 50877ea571-2826 no-casts `packages/core/echo/feed/src/feed-store.ts:559`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const key = block.feedId!;`

# ERROR 50877ea571-2827 no-casts `packages/core/echo/feed/src/feed-store.ts:570`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const id = yield* this.#ensureFeed(request.spaceId!, feedId, request.feedNamespace);`

# ERROR 50877ea571-2828 no-casts `packages/core/echo/feed/src/feed-store.ts:597`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const key = block.feedId!;`

# ERROR 50877ea571-2829 no-casts `packages/core/echo/feed/src/feed-store.ts:598`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const feedPrivateId = feedPrivateIds.get(key)!;`

# ERROR 50877ea571-2830 no-casts `packages/core/echo/feed/src/feed-store.ts:626`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `positions.push(positionToInsert!);`

# ERROR 50877ea571-2831 no-casts `packages/core/echo/feed/src/feed-store.ts:627`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `maxPositions.set(request.feedNamespace, positionToInsert!);`

# ERROR 50877ea571-2832 no-casts `packages/core/echo/feed/src/feed-store.ts:652`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `positions.push(positionToInsert!);`

# ERROR 50877ea571-2833 no-casts `packages/core/echo/feed/src/feed-store.ts:653`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `maxPositions.set(request.feedNamespace, positionToInsert!);`

# ERROR 50877ea571-2834 no-casts `packages/core/echo/feed/src/feed-store.ts:705`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const feedPrivateId = feedPrivateIds.get(key)!;`

# ERROR 50877ea571-2835 no-casts `packages/core/echo/feed/src/feed-store.ts:768`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `blocksBySpaceNamespace.get(spaceNamespaceKey)!.blocks.push(block);`

# ERROR 50877ea571-2836 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:659`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const recordIds = objects.map((o) => o.recordId!);`

# ERROR 50877ea571-2837 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:664`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((snapshotMap.get(objects[0].recordId!) as any).value).toBe('alpha');`

# ERROR 50877ea571-2838 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:665`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((snapshotMap.get(objects[1].recordId!) as any).value).toBe('beta');`

# ERROR 50877ea571-2839 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:695`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const snapshots = yield* index.querySnapshotsJSON([object.recordId!, staleId]);`

# ERROR 50877ea571-2840 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:698`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(snapshots[0].recordId).toBe(object.recordId!);`

# ERROR 50877ea571-2841 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:699`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((snapshots[0].snapshot as any).value).toBe('present');`

# ERROR 50877ea571-2842 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:728`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const recordIds = objects.map((o) => o.recordId!);`

# ERROR 50877ea571-2843 no-casts `packages/core/echo/index-core/src/indexes/reverse-ref-index.ts:125`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const refs = extractReferences(data as unknown as Record<string, unknown>);`

# ERROR 50877ea571-2844 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:51`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const result = await verifyChain(chain, issuer, signingKey!);`

# ERROR 50877ea571-2845 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:75`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.value = await signer.sign(signingKey ?? issuer, signedPayload);`

# ERROR 50877ea571-2846 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:77`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain = chain;`

# ERROR 50877ea571-2847 no-casts `packages/core/halo/credentials/src/credentials/signing.ts:39`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const normalizedAssertion: Record<string, any> = { ...originalAssertion };`

# ERROR 50877ea571-2848 no-casts `packages/core/halo/credentials/src/credentials/signing.ts:62`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const canonicalStringify = (obj: any): string =>`

# ERROR 50877ea571-2849 no-casts `packages/core/halo/credentials/src/credentials/signing.ts:71`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `replacer: function (this: any, key: any, value: any) {`

# ERROR 50877ea571-2850 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:57`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.value[0]++;`

# ERROR 50877ea571-2851 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:110`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.nonce = undefined;`

# ERROR 50877ea571-2852 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:136`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.nonce = PublicKey.random().asUint8Array();`

# ERROR 50877ea571-2853 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:241`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain = undefined;`

# ERROR 50877ea571-2854 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:283`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain!.credential.proof!.value = randomBytes(`

# ERROR 50877ea571-2855 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:284`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain!.credential.proof!.value.length,`

# ERROR 50877ea571-2856 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:325`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain!.credential = await createCredential({`

# ERROR 50877ea571-2857 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:376`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain!.credential = await createCredential({`

# ERROR 50877ea571-2858 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:491`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential.proof!.chain!.credential.proof!.chain!.credential = await createCredential({`

# ERROR 50877ea571-2859 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:21`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!credential.issuer.equals(credential.proof!.signer)) {`

# ERROR 50877ea571-2860 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:22`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!credential.proof!.chain) {`

# ERROR 50877ea571-2861 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:29`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const result = await verifyChain(credential.proof!.chain, credential.issuer, credential.proof!.signer);`

# ERROR 50877ea571-2862 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:48`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (credential.proof!.type !== SIGNATURE_TYPE_ED25519) {`

# ERROR 50877ea571-2863 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:51`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `errors: [`Invalid signature type: ${credential.proof!.type}`],`

# ERROR 50877ea571-2864 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:56`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!(await verifySignature(credential.proof!.signer, signData, credential.proof!.value))) {`

# ERROR 50877ea571-2865 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:19`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _root = { id: -1, parents: [], children: [] } as any as ChainVertex<A>;`

# ERROR 50877ea571-2866 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:24`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _sentinel = { id: -2, parents: [], children: [] } as any as ChainVertex<A>;`

# ERROR 50877ea571-2867 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:48`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return this._sentinel.parents.map((v) => v.credential!.id!);`

# ERROR 50877ea571-2868 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:63`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._vertexByCredentialId.set(credential.id!, newVertex);`

# ERROR 50877ea571-2869 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:131`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const path = paths.pop()!;`

# ERROR 50877ea571-2870 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:212`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `path.credentials.add(headCredential.id!);`

# ERROR 50877ea571-2871 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:258`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const newState = this._stateHandler.createState(subjectVertex.credential!, subjectVertex.assertion);`

# ERROR 50877ea571-2872 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:290`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const isPointInEveryPath = paths.every((p) => p.credentials.has(headCredential.id!));`

# ERROR 50877ea571-2873 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:313`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let it = path.forkPoint!;`

# ERROR 50877ea571-2874 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:317`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `it = it!.forkPoint!;`

# ERROR 50877ea571-2875 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:356`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `result.chosenPath![forkPoint.head.id].push(...(path.chosenPath![forkPoint.head.id] ?? []));`

# ERROR 50877ea571-2876 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:368`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const vertex = branch.state.get(subject)!;`

# ERROR 50877ea571-2877 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:379`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `stateOverrides: subjectToBranch.mapValues((v, key) => v.state.get(key)!),`

# ERROR 50877ea571-2878 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:400`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const candidateVertex = candidate.state.get(modifiedSubject)!;`

# ERROR 50877ea571-2879 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:401`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const currentVertex = existing.state.get(modifiedSubject)!;`

# ERROR 50877ea571-2880 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:411`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const candidateCredential = candidateVertex.credential!;`

# ERROR 50877ea571-2881 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:412`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const currentCredential = currentVertex.credential!;`

# ERROR 50877ea571-2882 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:414`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (existing.credentials.has(candidateCredential.id!) !== candidate.credentials.has(currentCredential.id!)) {`

# ERROR 50877ea571-2883 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:419`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return candidate.credentials.has(currentCredential.id!);`

# ERROR 50877ea571-2884 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:556`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return Object.fromEntries(Object.entries(path.chosenPath!).map(([k, vs]) => [k, vs.map((v) => v.id)]));`

# ERROR 50877ea571-2885 no-casts `packages/core/halo/credentials/src/invitations.ts:10`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `id: any;`

# ERROR 50877ea571-2886 no-casts `packages/core/halo/credentials/src/invitations.ts:11`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `authNonce: any;`

# ERROR 50877ea571-2887 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:187`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `credential: { ...chain.credential, proof: { ...chain.credential.proof, signer: PublicKey.random() } as any },`

# ERROR 50877ea571-2888 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:223`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(await verifyPresentationSignature(presentation, presentation.proofs![0])).to.deep.equal({ kind: 'pass' });`

# ERROR 50877ea571-2889 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:252`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await verifyPresentationSignature(presentation, { ...presentation.proofs![0], creationDate: new Date(0) }),`

# ERROR 50877ea571-2890 no-casts `packages/core/halo/credentials/src/state-machine/invitation-state-machine.test.ts:135`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return message.credential!.credential;`

# ERROR 50877ea571-2891 no-casts `packages/core/halo/credentials/src/state-machine/invitation-state-machine.test.ts:144`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `'credentialId': invitation.id!,`

# ERROR 50877ea571-2892 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:538`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const credential = feedMessage[0].credential!.credential;`

# ERROR 50877ea571-2893 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:540`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return feedMessage[0].credential!.credential.id!;`

# ERROR 50877ea571-2894 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:558`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const credential = feedMessage[0].credential!.credential;`

# ERROR 50877ea571-2895 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:560`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return feedMessage[0].credential!.credential.id!;`

# ERROR 50877ea571-2896 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:104`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return credential!.issuer.equals(this._spaceKey);`

# ERROR 50877ea571-2897 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:126`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const memberId = update.credential!.subject.id!;`

# ERROR 50877ea571-2898 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:147`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return Object.entries(SpaceMember.Role).find(([_, value]) => value === role)![0];`

# ERROR 50877ea571-2899 no-casts `packages/core/halo/credentials/src/state-machine/space-state-machine.ts:67`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _credentialProcessors: CredentialConsumer<any>[] = [];`

# ERROR 50877ea571-2900 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:50`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `body?: any;`

# ERROR 50877ea571-2901 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:158`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-2902 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:165`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `throw processingError!;`

# ERROR 50877ea571-2903 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:221`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-2904 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:228`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `throw processingError!;`

# ERROR 50877ea571-2905 no-casts `packages/core/mesh/edge-client/src/edge-ai-http-client.ts:199`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return send(request.body.body as any);`

# ERROR 50877ea571-2906 no-casts `packages/core/mesh/edge-client/src/edge-client.ts:47`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get info(): any;`

# ERROR 50877ea571-2907 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:35`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR 50877ea571-2908 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:57`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(String(targetCall![0])).toBe('https://edge.example.com/ai/generate/anthropic/v1/messages?beta=true');`

# ERROR 50877ea571-2909 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:58`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(targetCall![1]?.method).toBe('POST');`

# ERROR 50877ea571-2910 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:74`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR 50877ea571-2911 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:92`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(String(putCall![0])).toBe('https://edge.example.com/api/file/abc123');`

# ERROR 50877ea571-2912 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:93`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(putCall![1]?.method).toBe('POST');`

# ERROR 50877ea571-2913 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:94`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(putCall![1]?.body).toBe(bytes);`

# ERROR 50877ea571-2914 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:95`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((putCall![1]?.headers as Record<string, string>)['Content-Type']).toBe('application/octet-stream');`

# ERROR 50877ea571-2915 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:105`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (input: any, init?: RequestInit) => {`

# ERROR 50877ea571-2916 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:131`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (input: any) => {`

# ERROR 50877ea571-2917 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:153`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (_input: any, init?: RequestInit) => {`

# ERROR 50877ea571-2918 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:164`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetchMock = vi.fn(async (input: any, init?: RequestInit) => {`

# ERROR 50877ea571-2919 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:417`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `public async listFunctions(ctx: Context, args?: EdgeHttpCallArgs): Promise<any> {`

# ERROR 50877ea571-2920 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:432`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `): Promise<any> {`

# ERROR 50877ea571-2921 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:457`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `input: any,`

# ERROR 50877ea571-2922 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:655`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `HttpClient.execute(HttpClientRequest.make(_args.method as any)(url.toString())),`

# ERROR 50877ea571-2923 no-casts `packages/core/mesh/edge-client/src/edge-ws-muxer.ts:41`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _sendTimeout: any | undefined;`

# ERROR 50877ea571-2924 no-casts `packages/core/mesh/edge-client/src/protocol.ts:28`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `toJson(message: Message): any {`

# ERROR 50877ea571-2925 no-casts `packages/core/mesh/edge-client/src/protocol.ts:98`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const toUint8Array = async (data: any): Promise<Uint8Array> => {`

# ERROR 50877ea571-2926 no-casts `packages/core/mesh/edge-client/src/service/edge-service.test.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const fetch = (async (input: any, init: any) => {`

# ERROR 50877ea571-2927 no-casts `packages/core/mesh/edge-client/src/service/edge-service.test.ts:33`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (await handler(captured)) as unknown as Response;`

# ERROR 50877ea571-2928 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:22`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `messageHandler?: (payload: any) => Promise<Uint8Array | undefined>;`

# ERROR 50877ea571-2929 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:34`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const messageSink: any[] = [];`

# ERROR 50877ea571-2930 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:35`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const messageSourceLog: any[] = [];`

# ERROR 50877ea571-2931 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:37`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const sendResponseMessage = createResponseSender(() => connection!.muxer);`

# ERROR 50877ea571-2932 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:43`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `ws.on('message', async (data: any) => {`

# ERROR 50877ea571-2933 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:83`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return connection!.muxer.send(msg);`

# ERROR 50877ea571-2934 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:87`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `connection!.ws.close(1011);`

# ERROR 50877ea571-2935 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (_: any, callback: (admit: boolean) => void) => {`

# ERROR 50877ea571-2936 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:109`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const recipient = request.source!;`

# ERROR 50877ea571-2937 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:116`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `serviceId: request.serviceId!,`

# ERROR 50877ea571-2938 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:125`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `? params.payloadDecoder(request.payload!.value!)`

# ERROR 50877ea571-2939 no-casts `packages/core/mesh/messaging/src/messenger.ts:112`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(!this._onAckCallbacks.has(reliablePayload.messageId!));`

# ERROR 50877ea571-2940 no-casts `packages/core/mesh/messaging/src/messenger.ts:141`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._onAckCallbacks.delete(reliablePayload.messageId!);`

# ERROR 50877ea571-2941 no-casts `packages/core/mesh/messaging/src/messenger.ts:156`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._onAckCallbacks.delete(reliablePayload.messageId!);`

# ERROR 50877ea571-2942 no-casts `packages/core/mesh/messaging/src/messenger.ts:220`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `listeners!.delete(onMessage);`

# ERROR 50877ea571-2943 no-casts `packages/core/mesh/messaging/src/messenger.ts:281`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (this._receivedMessages.has(reliablePayload.messageId!)) {`

# ERROR 50877ea571-2944 no-casts `packages/core/mesh/messaging/src/messenger.ts:285`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._receivedMessages.add(reliablePayload.messageId!);`

# ERROR 50877ea571-2945 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:43`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const connection = mesh.createConnection({ peerKey: peer.peerKey!, identityDid: peer.identityDid! });`

# ERROR 50877ea571-2946 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:44`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const manager = new EdgeSignalManager({ edgeConnection: connection as any });`

# ERROR 50877ea571-2947 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:115`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await (subscriber.manager as any)._rejoinAllSwarms();`

# ERROR 50877ea571-2948 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.ts:287`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { joinedPeers: oldPeers } = this._swarmPeers.get(topic)!;`

# ERROR 50877ea571-2949 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.ts:313`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._swarmPeers.get(topic)!.joinedPeers = newPeers;`

# ERROR 50877ea571-2950 no-casts `packages/core/mesh/messaging/src/signal-manager/memory-signal-manager.ts:121`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._context.swarms.get(topic)!.add(peer);`

# ERROR 50877ea571-2951 no-casts `packages/core/mesh/messaging/src/signal-manager/memory-signal-manager.ts:153`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._context.swarms.get(topic)!.delete(peer);`

# ERROR 50877ea571-2952 no-casts `packages/core/mesh/messaging/src/testing/test-edge-mesh.ts:135`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `public readonly statusChanged: EdgeConnection['statusChanged'] = new Event<any>();`

# ERROR 50877ea571-2953 no-casts `packages/core/mesh/messaging/src/testing/test-edge-mesh.ts:147`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `get info(): any {`

# ERROR 50877ea571-2954 no-casts `packages/core/mesh/network-manager/src/connection-log.ts:64`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `info.connections!.push(connectionInfo);`

# ERROR 50877ea571-2955 no-casts `packages/core/mesh/network-manager/src/connection-log.ts:71`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `connectionInfo.events!.push({`

# ERROR 50877ea571-2956 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:26`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onSignal = (async () => {}) as any,`

# ERROR 50877ea571-2957 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:102`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onSignal: (async () => {}) as any,`

# ERROR 50877ea571-2958 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:107`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onSignal: (async () => {}) as any,`

# ERROR 50877ea571-2959 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:209`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onSignal: (async () => {}) as any,`

# ERROR 50877ea571-2960 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:218`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onSignal: (async () => {}) as any,`

# ERROR 50877ea571-2961 no-casts `packages/core/mesh/network-manager/src/swarm/connection-limiter.ts:68`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._waitingPromises.get(sessionId)!.reject(new CancelledError());`

# ERROR 50877ea571-2962 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:277`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2963 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:284`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2964 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:328`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2965 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:334`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2966 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:152`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2967 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:197`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2968 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:210`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2969 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:227`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2970 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:319`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._connectionCtx!,`

# ERROR 50877ea571-2971 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.test.ts:112`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await asyncTimeout(connectionDisplaced!, 1000);`

# ERROR 50877ea571-2972 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.test.ts:138`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `void peer1.swarm._peers.get(peer2.peer)!.connection!.close();`

# ERROR 50877ea571-2973 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:27`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getClassName = (obj: any) => Object.getPrototypeOf(obj).constructor.name;`

# ERROR 50877ea571-2974 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:293`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (!peer!.advertizing) {`

# ERROR 50877ea571-2975 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:354`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2976 no-casts `packages/core/mesh/network-manager/src/testing/test-builder.ts:66`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _proxy?: ProtoRpcPeer<any>;`

# ERROR 50877ea571-2977 no-casts `packages/core/mesh/network-manager/src/testing/test-builder.ts:67`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _service?: ProtoRpcPeer<any>;`

# ERROR 50877ea571-2978 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:129`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `void swarm1.protocol.connections.get(swarm2.peer.peerId)!.closeConnection(new Error('test error'));`

# ERROR 50877ea571-2979 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:152`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `?.disconnected.waitFor(({ peerKey }) => peer1.peerId.equals(peerKey!));`

# ERROR 50877ea571-2980 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:164`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `.poll(() => !!peer2._networkManager.getSwarm(topic)!._peers.get({ peerKey: peer1.peerId.toHex() })?.advertizing, {`

# ERROR 50877ea571-2981 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:190`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const swarmsAllPeersConnected: Promise<any>[] = [];`

# ERROR 50877ea571-2982 no-casts `packages/core/mesh/network-manager/src/transport/memory-transport.ts:196`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const toError = (err: any): Error => (err instanceof Error ? err : new Error(String(err)));`

# ERROR 50877ea571-2983 no-casts `packages/core/mesh/network-manager/src/transport/tcp/tcp-transport.ts:55`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { port } = this._server!.address() as AddressInfo;`

# ERROR 50877ea571-2984 no-casts `packages/core/mesh/network-manager/src/transport/tcp/tcp-transport.ts:134`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this.options.stream.pipe(this._socket!).pipe(this.options.stream);`

# ERROR 50877ea571-2985 no-casts `packages/core/mesh/network-manager/src/transport/transport.ts:83`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `rawStats?: any;`

# ERROR 50877ea571-2986 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:58`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-2987 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:70`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `connection.onnegotiationneeded?.(null as any);`

# ERROR 50877ea571-2988 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:79`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return typeof (globalThis as any).RTCPeerConnection === 'undefined'`

# ERROR 50877ea571-2989 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:145`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-2990 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:169`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onicecandidateerror: (event: any) => {`

# ERROR 50877ea571-2991 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:473`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `reject: (reason?: any) => void;`

# WARN 50877ea571-2992 no-sleep-in-test `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:47`

`void transport.close(); ...; await sleep(10); expect(controller.channel.wasClosed()).to.be.true;` fires `transport.close()` without awaiting it and instead sleeps 10ms hoping the async close has propagated to the channel before checking `wasClosed()`. Await the close (or a closed event/`Trigger`) instead of guessing a sufficient delay.

# ERROR 50877ea571-2993 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:116`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const deliveredMessages: any[] = [];`

# ERROR 50877ea571-2994 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:119`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `write: (chunk: any, _: BufferEncoding, callback: (error?: Error | null) => void) => {`

# ERROR 50877ea571-2995 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:124`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const options = { topic: 'test', stream } as any as TransportOptions;`

# ERROR 50877ea571-2996 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:153`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(channel as any).onmessage({ data: message });`

# ERROR 50877ea571-2997 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:177`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} as any as RtcPeerConnection,`

# ERROR 50877ea571-2998 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:151`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private async _handleChannelWrite(chunk: any, callback: PendingStreamFlushedCallback): Promise<void> {`

# ERROR 50877ea571-2999 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:166`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3000 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:185`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-3001 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:364`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `write: (chunk: any, _: BufferEncoding, callback: () => void) => {`

# ERROR 50877ea571-3002 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:369`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let sendSignal: any | undefined;`

# ERROR 50877ea571-3003 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:374`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `sendSignalFromTransport: async (signal: any) => {`

# ERROR 50877ea571-3004 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:404`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `public async onSignal(signal: any): Promise<void> {`

# ERROR 50877ea571-3005 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:409`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return {} as any;`

# ERROR 50877ea571-3006 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:55`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-3007 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:99`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(err: any) => {`

# ERROR 50877ea571-3008 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:127`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3009 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:133`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3010 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:168`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-3011 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:213`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private _raiseIfOpen(error: any): void {`

# ERROR 50877ea571-3012 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-service.ts:173`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-3013 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-service.ts:178`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (error: any) {`

# ERROR 50877ea571-3014 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-stats.ts:45`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getRtcConnectionStats = async (connection: RTCPeerConnection, channelTopic?: string): Promise<any> => {`

# ERROR 50877ea571-3015 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-stats.ts:48`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const statsEntries: [string, any][] = Array.from((stats as any).entries());`

# ERROR 50877ea571-3016 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-stats.ts:65`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `raw: Object.fromEntries(stats as any),`

# ERROR 50877ea571-3017 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:130`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const deliverSignal = (channel: RtcTransportChannel, signal: any) => {`

# ERROR 50877ea571-3018 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:145`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const signalBuffer: any[] = [];`

# ERROR 50877ea571-3019 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:146`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const sendSignal = async (signal: any) => {`

# ERROR 50877ea571-3020 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:196`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(channel as any).open = async () => {`

# ERROR 50877ea571-3021 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:211`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const text = Buffer.from(msg.value!).toString();`

# ERROR 50877ea571-3022 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:264`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3023 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:358`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(msg.value!).toEqual(encodeMessage('request'));`

# ERROR 50877ea571-3024 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:413`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).closed).toEqual(true);`

# ERROR 50877ea571-3025 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:414`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).error).toBeInstanceOf(Error);`

# ERROR 50877ea571-3026 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:415`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).error.message).toEqual('Test error');`

# WARN 50877ea571-3027 no-sleep-in-test `packages/core/mesh/rpc/src/rpc.test.ts:441`

`await stream.close(); await sleep(1); expect(closeCalled).toEqual(true);` waits 1ms hoping the remote peer's stream-close notification has round-tripped before checking `closeCalled`. Use a `Trigger`/`waitForCondition` tied to the close actually being observed instead of a fixed sleep.

# ERROR 50877ea571-3028 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:453`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(msg.value!).toEqual(encodeMessage('request'));`

# ERROR 50877ea571-3029 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:500`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).closed).toEqual(true);`

# ERROR 50877ea571-3030 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:501`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).error).toBeInstanceOf(Error);`

# ERROR 50877ea571-3031 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:502`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect((msgs[0] as any).error.message).toEqual('Test error');`

# ERROR 50877ea571-3032 no-casts `packages/core/mesh/rpc/src/rpc.ts:115`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private readonly _localStreams = new Map<number, Stream<any>>();`

# ERROR 50877ea571-3033 no-casts `packages/core/mesh/rpc/src/rpc.ts:156`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3034 no-casts `packages/core/mesh/rpc/src/rpc.ts:159`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `}) as any;`

# ERROR 50877ea571-3035 no-casts `packages/core/mesh/rpc/src/rpc.ts:213`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3036 no-casts `packages/core/mesh/rpc/src/rpc.ts:219`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3037 no-casts `packages/core/mesh/rpc/src/rpc.ts:317`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const item = this._outgoingRequests.get(responseId)!;`

# ERROR 50877ea571-3038 no-casts `packages/core/mesh/rpc/src/rpc.ts:410`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `timeout === 0 ? responseReceived : asyncTimeout<any>(responseReceived, timeout ?? DEFAULT_TIMEOUT);`

# ERROR 50877ea571-3039 no-casts `packages/core/mesh/rpc/src/rpc.ts:585`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3040 no-casts `packages/core/mesh/rpc/src/service.test.ts:93`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3041 no-casts `packages/core/mesh/rpc/src/service.test.ts:604`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `testCall: async (req: any, options?: RequestOptions) => {`

# ERROR 50877ea571-3042 no-casts `packages/core/mesh/rpc/src/service.test.ts:628`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const received = receivedCtx!.getAttribute(TRACE_SPAN_ATTRIBUTE);`

# ERROR 50877ea571-3043 no-casts `packages/core/mesh/rpc/src/service.test.ts:643`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `testCall: async (req: any, options?: RequestOptions) => {`

# ERROR 50877ea571-3044 no-casts `packages/core/mesh/rpc/src/service.test.ts:681`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `testCall: (_req: any, options?: RequestOptions) => {`

# ERROR 50877ea571-3045 no-casts `packages/core/mesh/rpc/src/service.test.ts:708`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const received = receivedCtx!.getAttribute(TRACE_SPAN_ATTRIBUTE);`

# ERROR 50877ea571-3046 no-casts `packages/core/mesh/rpc/src/service.ts:23`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type ServiceTypesOf<Bundle extends ServiceBundle<any>> =`

# ERROR 50877ea571-3047 no-casts `packages/core/mesh/rpc/src/service.ts:91`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const exposedRpcs: Record<string, ServiceHandler<any>> = {};`

# ERROR 50877ea571-3048 no-casts `packages/core/mesh/rpc/src/service.ts:231`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const rpc: Record<string, ServiceHandler<any>> = {};`

# ERROR 50877ea571-3049 no-casts `packages/core/mesh/rpc/src/service.ts:235`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `rpc[serviceFqn] = services[serviceName].createServer(handlers[serviceName] as any);`

# ERROR 50877ea571-3050 no-casts `packages/core/mesh/teleport-e2e/src/teleport.test.ts:19`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `connection1.teleport!.addExtension('example.testing.rpc', extension1);`

# ERROR 50877ea571-3051 no-casts `packages/core/mesh/teleport-e2e/src/teleport.test.ts:21`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `connection2.teleport!.addExtension('example.testing.rpc', extension2);`

# ERROR 50877ea571-3052 no-casts `packages/core/mesh/teleport-extension-automerge-replicator/src/automerge-replicator.test.ts:40`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(deliveredMessage!.payload[0]).to.eq(42);`

# ERROR 50877ea571-3053 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:103`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `postMessage(channel: string, payload: any): void {`

# ERROR 50877ea571-3054 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:131`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._listeners.get(channel)!.add(callback);`

# ERROR 50877ea571-3055 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:135`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._listeners.get(channel)!.delete(callback);`

# ERROR 50877ea571-3056 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:142`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._listeners.get(message.channelId)!.forEach((callback) => {`

# ERROR 50877ea571-3057 no-casts `packages/core/mesh/teleport-extension-gossip/src/testing.ts:39`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const extension = this.gossip.createExtension({ remotePeerId: connection.teleport!.remotePeerId });`

# ERROR 50877ea571-3058 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-store.test.ts:18`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Uint8Array.from(meta1.bitfield!)).toEqual(new Uint8Array([0b10000000]));`

# ERROR 50877ea571-3059 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-store.test.ts:22`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Uint8Array.from(meta2!.id)).toEqual(meta1.id);`

# ERROR 50877ea571-3060 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-store.test.ts:45`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Uint8Array.from(newMeta.bitfield!)).toEqual(new Uint8Array([0b11000000]));`

# ERROR 50877ea571-3061 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-store.test.ts:61`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Uint8Array.from(meta1.bitfield!)).toEqual(new Uint8Array([0b10000000]));`

# ERROR 50877ea571-3062 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-store.test.ts:71`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `expect(Uint8Array.from(meta2.bitfield!)).toEqual(new Uint8Array([0b11000000]));`

# ERROR 50877ea571-3063 no-casts `packages/core/mesh/teleport-extension-object-sync/src/blob-sync.ts:128`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._downloadRequests.get(blobChunk.id)!.want.bitfield = BitField.invert(meta.bitfield);`

# ERROR 50877ea571-3064 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:36`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `private readonly _feeds = new ComplexMap<PublicKey, FeedWrapper<any>>(PublicKey.hash);`

# ERROR 50877ea571-3065 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:48`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (this._extensionContext!.initiator === false) {`

# ERROR 50877ea571-3066 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:49`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await this._rpc!.rpc.ReplicatorService.updateFeeds({`

# ERROR 50877ea571-3067 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:56`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} else if (this._extensionContext!.initiator === true) {`

# ERROR 50877ea571-3068 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:86`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `addFeed(feed: FeedWrapper<any>): void {`

# ERROR 50877ea571-3069 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:109`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(this._extensionContext!.initiator === true, 'Invalid call');`

# ERROR 50877ea571-3070 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:114`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR 50877ea571-3071 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:124`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR 50877ea571-3072 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:192`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(this._extensionContext!.initiator === true, 'Invalid call');`

# ERROR 50877ea571-3073 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:194`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { streamTag } = await this._rpc!.rpc.ReplicatorService.startReplication({ info: feedInfo });`

# ERROR 50877ea571-3074 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:208`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR 50877ea571-3075 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:224`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const networkStream = await this._extensionContext!.createStream(streamTag, {`

# ERROR 50877ea571-3076 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:285`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `networkStream.pipe(replicationStream as any).pipe(networkStream);`

# ERROR 50877ea571-3077 no-casts `packages/core/mesh/teleport-extension-replicator/src/stress.test.ts:24`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `readonly feedStore: FeedStore<any>;`

# ERROR 50877ea571-3078 no-casts `packages/core/mesh/teleport-extension-replicator/src/stress.test.ts:69`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const expectedLength = model.feeds.get(feedKey)!;`

# ERROR 50877ea571-3079 no-casts `packages/core/mesh/teleport/src/control-extension.ts:27`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onTimeout: (err: any) => void;`

# ERROR 50877ea571-3080 no-casts `packages/core/mesh/teleport/src/control-extension.ts:122`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# WARN 50877ea571-3081 no-sleep-in-test `packages/core/mesh/teleport/src/muxing/balancer.test.ts:101`

`await sleep(20); expect(stream.writeCalls).to.equal(1); expect(balancer.buffersCount).to.toBeGreaterThan(0);` waits a fixed 20ms hoping the balancer has buffered chunks before asserting positively on it, while the very next test in the same file correctly uses `expect.poll(...)` for an equivalent wait. Use `expect.poll`/`waitForCondition` here too instead of a blind sleep.

# ERROR 50877ea571-3082 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:89`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (chunk.length < dataLength!) {`

# ERROR 50877ea571-3083 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:92`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `msgLength: dataLength!,`

# ERROR 50877ea571-3084 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:98`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const channelBuffer = this._receiveBuffers.get(channelId)!;`

# ERROR 50877ea571-3085 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:129`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const sendBuffer = this._sendBuffers.get(channelId)!;`

# ERROR 50877ea571-3086 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:186`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# WARN 50877ea571-3087 no-sleep-in-test `packages/core/mesh/teleport/src/muxing/framer.test.ts:104`

`await sleep(2); // Must be longer the pipe's flush interval` then asserts `framesReceived.length` equals `framesSent.length`. The comment itself acknowledges this is timing-dependent on the pipe's flush interval; replace with a wait on an actual "all frames delivered" signal (e.g. `waitForCondition`) rather than assuming 2ms is long enough.

# ERROR 50877ea571-3088 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:117`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `while (offset < this._buffer!.length) {`

# ERROR 50877ea571-3089 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:118`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const frame = decodeFrame(this._buffer!, offset);`

# ERROR 50877ea571-3090 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:129`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if (offset < this._buffer!.length) {`

# ERROR 50877ea571-3091 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:131`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._buffer = this._buffer!.subarray(offset);`

# ERROR 50877ea571-3092 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:193`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3093 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:255`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3094 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:285`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `).catch(async (err: any) => {`

# ERROR 50877ea571-3095 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:321`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `).catch(async (err: any) => {`

# ERROR 50877ea571-3096 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:385`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `channelId: channel.remoteId!,`

# ERROR 50877ea571-3097 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:412`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3098 no-casts `packages/core/mesh/teleport/src/teleport.ts:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3099 no-casts `packages/core/mesh/teleport/src/teleport.ts:180`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3100 no-casts `packages/core/mesh/teleport/src/teleport.ts:209`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3101 no-casts `packages/core/mesh/teleport/src/testing/test-extension-with-streams.ts:41`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const networkStream = await this.extensionContext!.createStream(streamTag, {`

# ERROR 50877ea571-3102 no-casts `packages/core/mesh/teleport/src/testing/test-extension-with-streams.ts:108`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const stream = this._streams.get(streamTag)!;`

# ERROR 50877ea571-3103 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:35`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._socket!.send(msg);`

# ERROR 50877ea571-3104 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:38`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `this._socket!.onmessage = async (msg: WebSocket.MessageEvent) => {`

# ERROR 50877ea571-3105 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:42`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `cb(msg.data as any);`

# ERROR 50877ea571-3106 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:64`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await this._rpc!.open();`

# ERROR 50877ea571-3107 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:68`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3108 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:100`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return this._rpc!.rpc;`

# ERROR 50877ea571-3109 no-casts `packages/core/mesh/websocket-rpc/src/e2e.node.test.ts:27`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `testCall: async (request: any) => {`

# ERROR 50877ea571-3110 no-casts `packages/core/mesh/websocket-rpc/src/server.ts:56`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `cb(msg.data as any);`

# ERROR 50877ea571-3111 no-casts `packages/core/protocols/scripts/gen-service-rpcs.ts:48`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const requestType = method.resolvedRequestType!.fullName.replace(/^\./, '');`

# ERROR 50877ea571-3112 no-casts `packages/core/protocols/scripts/gen-service-rpcs.ts:49`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const responseType = method.resolvedResponseType!.fullName.replace(/^\./, '');`

# ERROR 50877ea571-3113 no-casts `packages/core/protocols/src/edge/edge.ts:56`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export type EdgeErrorData = { type: string } & Record<string, any>;`

# ERROR 50877ea571-3114 no-casts `packages/core/protocols/src/edge/edge.ts:188`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `output?: any;`

# ERROR 50877ea571-3115 no-casts `packages/core/protocols/src/edge/edge.ts:452`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `data: Record<SpaceId, { diagnostics?: any & { redFlags: string[] }; fetchError?: string }>;`

# ERROR 50877ea571-3116 no-casts `packages/core/protocols/src/edge/edge.ts:510`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `name: 'name' in err ? err.name : (err as any).code || 'Error',`

# ERROR 50877ea571-3117 no-casts `packages/core/protocols/src/errors/encoding.ts:8`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const encodeError = (err: any): SerializedErrorProto => {`

# ERROR 50877ea571-3118 no-casts `packages/core/protocols/src/errors/helpers.ts:15`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const errorRegistry = new Map<string, (message?: string, context?: any) => Error>();`

# ERROR 50877ea571-3119 no-casts `packages/core/protocols/src/errors/helpers.ts:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const registerError = (code: string, make: (message?: string, context?: any) => Error) => {`

# ERROR 50877ea571-3120 no-casts `packages/core/protocols/src/errors/helpers.ts:22`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const registerErrorNoArgs = (code: string, Constructor: { new (options?: any): Error }) => {`

# ERROR 50877ea571-3121 no-casts `packages/core/protocols/src/errors/helpers.ts:28`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Constructor: { new (options?: { message?: string; context?: any }): Error },`

# ERROR 50877ea571-3122 no-casts `packages/core/protocols/src/errors/helpers.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `registerError(code, (message?: string, context?: any) => new Constructor({ message, context }));`

# ERROR 50877ea571-3123 no-casts `packages/core/protocols/src/errors/helpers.ts:33`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const errorFromCode = (code?: string, message?: string, context?: any) => {`

# ERROR 50877ea571-3124 no-casts `packages/core/protocols/src/FeedProtocol.ts:315`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Object.values(WellKnownNamespaces).includes(namespace as any);`

# ERROR 50877ea571-3125 no-casts `packages/core/protocols/src/messenger.ts:21`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `postMessage(channel: string, message: any): Promise<void>;`

# ERROR 50877ea571-3126 no-casts `packages/core/protocols/src/proto/substitutions.ts:12`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `decode: (value: any) => PublicKey.from(value.data),`

# ERROR 50877ea571-3127 no-casts `packages/core/protocols/src/proto/substitutions.ts:18`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `decode: (value: any) => PublicKey.from(new Uint8Array(value.data)).asBuffer(),`

# ERROR 50877ea571-3128 no-casts `packages/core/protocols/src/proto/substitutions.ts:26`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `decode: (vector: any) =>`

# ERROR 50877ea571-3129 no-casts `packages/core/protocols/src/proto/substitutions.ts:29`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `.filter((frame: any) => frame.feedKey != null && frame.seq != null)`

# ERROR 50877ea571-3130 no-casts `packages/core/protocols/src/proto/substitutions.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `.map((frame: any) => [PublicKey.from(frame.feedKey), frame.seq]),`

# ERROR 50877ea571-3131 no-casts `packages/core/protocols/src/rpc-bridge.ts:29`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const source = handlers as unknown as Record<string, (...args: any[]) => unknown>;`

# ERROR 50877ea571-3132 no-casts `packages/core/protocols/src/rpc-bridge.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const normalized: Record<string, (...args: any[]) => unknown> = {};`

# ERROR 50877ea571-3133 no-casts `packages/core/protocols/src/rpc-bridge.ts:41`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `Effect.provide(group.toLayer(normalized as unknown as RpcGroup.HandlersFrom<Rpcs>)),`

# ERROR 50877ea571-3134 no-casts `packages/devtools/cli-util/src/testing/test-console.ts:11`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `function logToString(...args: any[]): string {`

# ERROR 50877ea571-3135 no-casts `packages/devtools/cli-util/src/testing/test-console.ts:31`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(...args: readonly any[]) => {`

# ERROR 50877ea571-3136 no-casts `packages/devtools/cli-util/src/testing/test-console.ts:37`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `log: (...args: readonly any[]) => Effect.sync(() => pusher('log')(...args)),`

# ERROR 50877ea571-3137 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:16`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `readonly entries: ReadonlyArray<{ key: string; value: Doc.Doc<any>; isNested?: boolean }>;`

# ERROR 50877ea571-3138 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:22`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `readonly entries: Array<{ key: string; value: Doc.Doc<any>; isNested?: boolean }> = [];`

# ERROR 50877ea571-3139 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:64`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let valueDoc: Doc.Doc<any>;`

# ERROR 50877ea571-3140 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:67`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `valueDoc = value as unknown as Doc.Doc<any>;`

# ERROR 50877ea571-3141 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:139`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(parent.entries as Array<{ key: string; value: Doc.Doc<any>; isNested?: boolean }>).push({`

# ERROR 50877ea571-3142 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:161`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return nestImpl(parent, key, builder!);`

# ERROR 50877ea571-3143 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:240`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return nestedOptionImpl(builder, key, value!);`

# ERROR 50877ea571-3144 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:250`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const whenImpl = (builder: FormBuilder, condition: any, ...ops: ((b: FormBuilder) => FormBuilder)[]): FormBuilder => {`

# ERROR 50877ea571-3145 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:262`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export function when(builder: FormBuilder, condition: any, ...ops: ((b: FormBuilder) => FormBuilder)[]): FormBuilder;`

# ERROR 50877ea571-3146 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:264`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `condition: any,`

# ERROR 50877ea571-3147 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:269`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `conditionOrOp?: any | ((b: FormBuilder) => FormBuilder),`

# ERROR 50877ea571-3148 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:318`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return eachImpl(builder, items, fn!);`

# ERROR 50877ea571-3149 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:330`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const build = (builder: FormBuilder): Doc.Doc<any> => {`

# ERROR 50877ea571-3150 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:332`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const entryLines: Doc.Doc<any>[] = [];`

# ERROR 50877ea571-3151 no-casts `packages/devtools/cli-util/src/util/printer.ts:11`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const print = (doc: Doc.Doc<any>) => AnsiDoc.render(doc, { style: 'pretty' });`

# ERROR 50877ea571-3152 no-casts `packages/devtools/cli-util/src/util/printer.ts:16`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const printList = (items: Array<Doc.Doc<any>>) =>`

# ERROR 50877ea571-3153 no-casts `packages/devtools/cli-util/src/util/space-format.ts:162`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `epoch: any;`

# ERROR 50877ea571-3154 no-casts `packages/devtools/cli/src/bin.ts:47`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `let leaksTracker: any;`

# ERROR 50877ea571-3155 no-casts `packages/devtools/cli/src/commands/admin/identity/index.ts:11`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const identity: Command.Command<any, any, any, any> = Command.make('identity').pipe(`

# ERROR 50877ea571-3156 no-casts `packages/devtools/cli/src/commands/admin/space/index.ts:12`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const space: Command.Command<any, any, any, any> = Command.make('space').pipe(`

# ERROR 50877ea571-3157 no-casts `packages/devtools/cli/src/commands/chat/components/Chat.tsx:144`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3158 no-casts `packages/devtools/cli/src/commands/chat/components/Markdown.tsx:108`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const children = getChildren(props.node.parent!);`

# ERROR 50877ea571-3159 no-casts `packages/devtools/cli/src/commands/chat/components/Markdown.tsx:261`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const createJsonBlock = (content: any) => {`

# ERROR 50877ea571-3160 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:42`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const typename = Obj.getTypename(obj as any);`

# ERROR 50877ea571-3161 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:46`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const id = (obj as any).id as string | undefined;`

# ERROR 50877ea571-3162 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const meta = (obj as any).__meta;`

# ERROR 50877ea571-3163 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:90`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `.filter((obj: any) => Obj.getTypename(obj) === 'org.dxos.type.message')`

# ERROR 50877ea571-3164 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:91`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `.filter((obj: any) => obj?.parentId === sessionFeedId || obj?.feed === sessionFeedId);`

# ERROR 50877ea571-3165 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:143`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const live = (space.db as any).getObjectById?.(id);`

# ERROR 50877ea571-3166 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:166`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `console.log(await renderText(space, (session as any).feed?.id ?? ''));`

# ERROR 50877ea571-3167 no-casts `packages/devtools/cli/src/commands/function/deploy/echo.ts:130`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `() => functionObject.source!.load() as Promise<Script.Script>,`

# ERROR 50877ea571-3168 no-casts `packages/devtools/cli/src/commands/function/deploy/echo.ts:132`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const source = (yield* Effect.tryPromise(() => script.source!.load())) as Text.Text;`

# ERROR 50877ea571-3169 no-casts `packages/devtools/cli/src/commands/hub/accounts/index.ts:10`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const account: Command.Command<any, any, any, any> = Command.make('account').pipe(`

# ERROR 50877ea571-3170 no-casts `packages/devtools/cli/src/commands/hub/codes/index.ts:11`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const code: Command.Command<any, any, any, any> = Command.make('code').pipe(`

# ERROR 50877ea571-3171 no-casts `packages/devtools/cli/src/commands/hub/user/list/list.ts:46`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return yield* Console.log((result as any).entries?.length + ' waitlist entries');`

# ERROR 50877ea571-3172 no-casts `packages/devtools/cli/src/commands/hub/util.ts:59`

Per the no-casts rule, this line has an `as unknown as T` double-cast escape hatch that forces an unrelated type instead of correcting the source type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const envelope = result as unknown as EdgeEnvelope<T>;`

# ERROR 50877ea571-3173 no-casts `packages/devtools/cli/src/commands/plugin-defs.ts:41`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `ObservabilityPlugin({} as any),`

# ERROR 50877ea571-3174 no-casts `packages/devtools/cli/src/commands/reset.ts:29`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3175 no-casts `packages/devtools/cli/src/commands/reset.ts:44`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3176 no-casts `packages/devtools/cli/src/components/App.tsx:167`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `fallback={(err: any) => {`

# ERROR 50877ea571-3177 no-casts `packages/devtools/cli/src/components/Table.tsx:31`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getId = props.getId ?? ((row: any) => row.id);`

# ERROR 50877ea571-3178 no-casts `packages/devtools/cli/src/components/Table.tsx:143`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getId = props.getId ?? ((row: any) => row.id);`

# ERROR 50877ea571-3179 no-casts `packages/devtools/cli/src/util/runtime.ts:65`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `invoke: (op: any, ...args: any[]) => {`

# ERROR 50877ea571-3180 no-casts `packages/devtools/cli/src/util/runtime.ts:66`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handler = handlers.find((h: any) => h.meta.key === op.meta.key);`

# ERROR 50877ea571-3181 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:130`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const wrapConsole = (variant: 'warn' | 'error', original: (...args: any[]) => void) => {`

# ERROR 50877ea571-3182 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:131`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return (...args: any[]) => {`

# ERROR 50877ea571-3183 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:143`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type; and a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(process.stderr as any).write = (chunk: any, ...rest: any[]): boolean => {`

# ERROR 50877ea571-3184 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:146`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `typeof maybeCallback === 'function' ? (maybeCallback as any) : undefined;`

# ERROR 50877ea571-3185 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:158`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(process.stderr as any).write = originalWrite;`

# ERROR 50877ea571-3186 no-casts `packages/devtools/devtools-extension/src/background.ts:20`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const messageListener = (message: any) => {`

# ERROR 50877ea571-3187 no-casts `packages/devtools/devtools-extension/src/background.ts:40`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const messageListener = (message: any) => {`

# ERROR 50877ea571-3188 no-casts `packages/devtools/devtools-extension/src/content.ts:12`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `port.onMessage.addListener((message: any) => {`

# ERROR 50877ea571-3189 no-casts `packages/devtools/devtools-extension/src/panel.ts:39`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `port.onMessage.addListener((message: any) => {`

# ERROR 50877ea571-3190 no-casts `packages/devtools/devtools-extension/src/sandbox.tsx:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handler = (event: MessageEvent<any>) => {`

# ERROR 50877ea571-3191 no-casts `packages/devtools/devtools-extension/src/sandbox.tsx:82`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const init = () => createRoot(document.getElementById('root')!).render(<App />);`

# ERROR 50877ea571-3192 no-casts `packages/devtools/devtools/scripts/proxy.ts:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `socket.on('message', (data: any) => {`

# ERROR 50877ea571-3193 no-casts `packages/devtools/devtools/scripts/proxy.ts:40`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `socket.on('message', (data: any) => {`

# ERROR 50877ea571-3194 no-casts `packages/devtools/devtools/src/components/JsonView.tsx:22`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(key: any, value: any) => {`

# ERROR 50877ea571-3195 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `data: Array<{ id: string; [key: string]: any }>;`

# ERROR 50877ea571-3196 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:18`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `detailsTransform?: (data: any) => MaybePromise<any>;`

# ERROR 50877ea571-3197 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:30`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [selected, setSelected] = useState<any | undefined>(undefined);`

# ERROR 50877ea571-3198 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:31`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [transformedData, setTransformedData] = useState<any>(undefined);`

# ERROR 50877ea571-3199 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:35`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(row: any) => {`

# ERROR 50877ea571-3200 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:12`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `object: any;`

# ERROR 50877ea571-3201 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:31`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `stylesheet: any;`

# ERROR 50877ea571-3202 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:32`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `useInlineStyles: any;`

# ERROR 50877ea571-3203 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:43`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `onNavigate?.(URI.make((node.children![0].value as string).slice(1, -1)));`

# ERROR 50877ea571-3204 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `tagName?: keyof JSX.IntrinsicElements | ComponentType<any> | undefined;`

# ERROR 50877ea571-3205 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:95`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `properties?: { className: any[]; [key: string]: any };`

# ERROR 50877ea571-3206 no-casts `packages/devtools/devtools/src/components/performance/panels/RawQueriesPanel.tsx:26`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `<td className='p-1 w-[80px] text-right'>{queries.get(key)!.toLocaleString()}</td>`

# ERROR 50877ea571-3207 no-casts `packages/devtools/devtools/src/components/PropertiesTable.tsx:20`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `format: (value: any) => ReactNode;`

# ERROR 50877ea571-3208 no-casts `packages/devtools/devtools/src/components/PropertiesTable.tsx:91`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `object: Record<string, any>;`

# ERROR 50877ea571-3209 no-casts `packages/devtools/devtools/src/components/PropertiesTable.tsx:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const format = (key: string, value: any) => {`

# ERROR 50877ea571-3210 no-casts `packages/devtools/devtools/src/components/Tree.tsx:18`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const Node = ({ data }: ThemedClassName<{ data?: any; root?: boolean }>) => {`

# ERROR 50877ea571-3211 no-casts `packages/devtools/devtools/src/components/Tree.tsx:42`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const KeyValue = ({ classNames, label, data }: ThemedClassName<{ label: string; data?: any }>) => {`

# ERROR 50877ea571-3212 no-casts `packages/devtools/devtools/src/components/Tree.tsx:70`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const Scalar = ({ classNames, value }: ThemedClassName<{ value: any }>) => {`

# ERROR 50877ea571-3213 no-casts `packages/devtools/devtools/src/hooks/useProxiedClient.tsx:35`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handler = (event: MessageEvent<any>) => {`

# ERROR 50877ea571-3214 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:37`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `filter: any;`

# ERROR 50877ea571-3215 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:38`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `metrics: any;`

# ERROR 50877ea571-3216 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:110`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const memory: MemoryInfo = (window.performance as any).memory;`

# ERROR 50877ea571-3217 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:148`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const stream = client.services.services.NetworkService!.queryStatus();`

# ERROR 50877ea571-3218 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:163`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const stream = client.services.services.EdgeAgentService!.queryEdgeStatus();`

# ERROR 50877ea571-3219 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:191`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `export const removeEmpty = (obj: any): any => {`

# ERROR 50877ea571-3220 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:21`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `} catch (err: any) {`

# ERROR 50877ea571-3221 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:28`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({});`

# ERROR 50877ea571-3222 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:32`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({ record });`

# ERROR 50877ea571-3223 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:36`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({ reset: true });`

# ERROR 50877ea571-3224 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:50`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `if ((window as any).chrome) {`

# ERROR 50877ea571-3225 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:17`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const hasMatchRecursive = (object: any, pattern: RegExp): boolean => {`

# ERROR 50877ea571-3226 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:43`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const getStoredObject = (doc: DatabaseDirectory | undefined): any => {`

# ERROR 50877ea571-3227 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:87`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `id: handle.documentId!,`

# ERROR 50877ea571-3228 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:88`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `documentId: handle.documentId!,`

# ERROR 50877ea571-3229 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:90`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `type: getStoredObjectType({ documentId: handle.documentId!, accessor: () => doc, id: handle.documentId! }),`

# ERROR 50877ea571-3230 no-casts `packages/devtools/devtools/src/panels/echo/FeedsPanel/FeedsPanel.tsx:137`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const issuerKeys = credential ? { identity: credential.issuer, device: credential.proof!.signer } : undefined;`

# ERROR 50877ea571-3231 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:30`

Per the no-casts rule, this line has an `as any` cast, which suppresses the type checker instead of fixing the underlying type. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `match ||= !!String((item as any).title ?? '').match(matcher);`

# ERROR 50877ea571-3232 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:42`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const mapHistoryRow = (item: AmState<any>): HistoryRow => {`

# ERROR 50877ea571-3233 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:59`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [selectedVersionObject, setSelectedVersionObject] = useState<any | null>(null);`

# ERROR 50877ea571-3234 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:115`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `version: Obj.getType(item) ? Type.getVersion(Obj.getType(item)!) : undefined,`

# ERROR 50877ea571-3235 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:122`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handleObjectRowClicked = useCallback((row: any) => {`

# ERROR 50877ea571-3236 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:155`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `setSelectedVersionObject(checkoutVersion(selected!, [version.hash]));`

# ERROR 50877ea571-3237 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:161`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `(row: any) => {`

# ERROR 50877ea571-3238 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:25`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const objects: any[] = [];`

# ERROR 50877ea571-3239 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:26`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [selected, setSelected] = useState<any>();`

# ERROR 50877ea571-3240 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:27`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const [selectedVersionObject, setSelectedVersionObject] = useState<any | null>(null);`

# ERROR 50877ea571-3241 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:38`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `return objects.map((item: any) => ({`

# ERROR 50877ea571-3242 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:45`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handleRowClicked = (row: any) => {`

# ERROR 50877ea571-3243 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:83`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `object: any;`

# ERROR 50877ea571-3244 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:93`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `stylesheet: any;`

# ERROR 50877ea571-3245 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:94`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `useInlineStyles: any;`

# ERROR 50877ea571-3246 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:113`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `tagName?: keyof JSX.IntrinsicElements | ComponentType<any> | undefined;`

# ERROR 50877ea571-3247 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:114`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `properties?: { className: any[]; [key: string]: any };`

# ERROR 50877ea571-3248 no-casts `packages/devtools/devtools/src/panels/echo/SchemaPanel/SchemaPanel.tsx:118`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handleObjectRowClicked = useCallback((row: any) => {`

# ERROR 50877ea571-3249 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/FeedTable.tsx:56`

Per the no-casts rule, this line has a widened `any` in a type signature/generic that lets callers bypass type checking. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const handleRowClick = (row: any) => {`

# ERROR 50877ea571-3250 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:38`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `const state = space!.state.get();`

# ERROR 50877ea571-3251 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:40`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await space!.open();`

# ERROR 50877ea571-3252 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:42`

Per the no-casts rule, this line has a non-null assertion (`!`) that asserts away a possible `null`/`undefined` without a real check. Fix the type at its source (narrow/derive the real type, add a proper guard, or thread the correct generic) instead of casting around the checker. Line: `await space!.close();`

# ERROR 50877ea571-3253 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:68:9`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `(row: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3254 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:83:65`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const space = spaces.find((space) => space.id === spaceId)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3255 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:95:65`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const space = spaces.find((space) => space.id === spaceId)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3256 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:105:65`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const space = spaces.find((space) => space.id === spaceId)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3257 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:158:50`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const handleRowAction = (actionId: string, item: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3258 no-casts `packages/devtools/devtools/src/panels/edge/EdgeDashboardPanel/EdgeDashboardPanel.tsx:29:25`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const formatData = (data: any) =>`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3259 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/ExceptionPanel.tsx:48:37`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const context = log.context as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3260 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/InvocationTraceContainer.tsx:145:44`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const handleRowClick = useCallback((row: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3261 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/LogPanel.tsx:62:29`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const safeStringify = (value: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3262 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:34:31`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `properties?: { className: any[]; [key: string]: any };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3263 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:35:16`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `children?: any[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3264 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:37:15`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `stylesheet: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3265 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:38:20`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `useInlineStyles: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3266 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:105:10`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `data?: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3267 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:130:19`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `let response: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3268 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:148:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3269 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:280:8`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `data?: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3270 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:52:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `return async (data: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3271 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:67:19`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3272 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:115:15`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3273 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:122:15`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const result: any = {};`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3274 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:66:83`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `radius: (node: GraphLayoutNode<NetworkGraphNode>) => (isMe(node.data!) ? 24 : 16),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3275 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:95:28`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `node.data!.member?.identity.profile?.displayName ??`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3276 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:96:28`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `node.data!.member?.identity.identityKey.truncate();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3277 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:98:39`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const peer = node.data!.peer?.peerId?.truncate();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3278 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:66:29`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `(response.swarmEvent!.peerAvailable && PublicKey.from(response.swarmEvent!.peerAvailable.peer).toString()) ||`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3279 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:67:29`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `(response.swarmEvent!.peerLeft && PublicKey.from(response.swarmEvent!.peerLeft.peer).toString()),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3280 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:68:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `since: response.swarmEvent!.peerAvailable?.since ?? new Date(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3281 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:94:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `author: PublicKey.from(response.message!.author).toString(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3282 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:95:49`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `recipient: PublicKey.from(response.message!.recipient).toString(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3283 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:96:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `message: response.message!.payload.messageId,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3284 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:97:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `topic: response.message!.payload?.payload?.topic,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3285 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:121:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `author: PublicKey.from(response.message!.author).toString(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3286 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:122:49`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `recipient: PublicKey.from(response.message!.recipient).toString(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3287 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:123:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `message: response.message!.payload.messageId,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3288 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:130:93`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const getView = (id: ViewType): View<SignalResponse> => views.find((type) => type.id === id)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3289 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:23:48`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `connectionStarted: server.connectionStarted!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3290 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:24:44`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `lastStateChange: server.lastStateChange!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3291 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:25:36`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `reconnectIn: server.reconnectIn!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3292 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:26:22`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `host: server.host!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3293 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:27:24`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `state: server.state!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3294 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:83:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const status = servers!.map(getSignalStatus);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3295 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:59:24`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `preprocessor?: (entry: any) => SerializedLogEntry;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3296 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:63:16`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `private _logs: any[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3297 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:110:38`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const zapPreprocessor = (entry: any): SerializedLogEntry => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3298 no-casts `packages/e2e/blade-runner/src/analysys/plot.ts:17:70`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const image = await chartJSNodeCanvas.renderToBuffer(configuration as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3299 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:17:29`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const decodeVal = (val: any): any => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3300 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:26:20`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const decoded: any = decodeVal(val);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3301 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:52:35`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `messages.get(data.message)!.sent = entry.timestamp;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3302 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:55:35`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `messages.get(data.message)!.received = entry.timestamp;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3303 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:63:39`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `.map((message) => message.received! - message.sent!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3304 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:220:61`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const discoverStats = timings.seen.get(expectedPeer)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3305 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:221:48`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `discoverLag.push(discoverStats.discover! - expectedPeerTimings.join);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3306 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:224:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `notifyLag.push(discoverStats.notify! - expectedPeerTimings.join!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3307 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:286:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `peers.get(data.peerId)!.iterations++;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3308 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:291:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `peers.get(data.peerId)!.errors++;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3309 no-casts `packages/e2e/blade-runner/src/analysys/traces.ts:51:88`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._traces[event.name].duration = event.ts - this._traces[event.name].begin!.ts;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3310 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:70:25`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `setReplicant(replicant: any): void {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3311 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:129:62`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const result = dataValues.map((value) => JSON.parse(value!));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3312 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:138:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `if (parseInt(value!) === count) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3313 no-casts `packages/e2e/blade-runner/src/env/replicant-registry.ts:21:44`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `return this._replicantClasses.get(name)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3314 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-handle.ts:37:30`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `value: async (...args: any[]) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3315 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-handle.ts:54:30`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const getAllProperties = (obj: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3316 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-server.ts:14:10`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `handler: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3317 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-server.ts:30:41`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const response = await (handler as any)[method](...args);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3318 no-casts `packages/e2e/blade-runner/src/env/scheduler-env.ts:142:62`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const result = dataValues.map((value) => JSON.parse(value!));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3319 no-casts `packages/e2e/blade-runner/src/env/scheduler-env.ts:154:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `if (parseInt(value!) === count) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3320 no-casts `packages/e2e/blade-runner/src/main.ts:54:66`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const params: RunProps = JSON.parse(process.env.DX_RUN_PARAMS!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3321 no-casts `packages/e2e/blade-runner/src/main.ts:60:19`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if ((globalThis as any).DX_RUN_PARAMS) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3322 no-casts `packages/e2e/blade-runner/src/main.ts:62:32`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const params = (globalThis as any).DX_RUN_PARAMS;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3323 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:25:7`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `spec: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3324 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:26:8`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `stats: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3325 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:27:10`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `results: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3326 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:41:24`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if (typeof (globalThis as any).dxgravity_env !== 'undefined') {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3327 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:42:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `process.env = (globalThis as any).dxgravity_env;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3328 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:106:13`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `let result: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3329 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:131:12`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `let stats: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3330 no-casts `packages/e2e/blade-runner/src/plan/run-replicant.ts:51:17`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(window as any).dx_runner_log?.(config, entry);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3331 no-casts `packages/e2e/blade-runner/src/plan/run-replicant.ts:62:13`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(window as any).dx_runner_done?.(code);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3332 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:126:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `bobReplicator.context!.peerId,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3333 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:127:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `aliceReplicator.context!.peerId,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3334 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:129:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `aliceReplicator.context!.onConnectionOpen(aliceConnection);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3335 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:130:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `bobReplicator.context!.onConnectionOpen(bobConnection);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3336 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:133:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `aliceReplicator.context!.onConnectionClosed(aliceConnection!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3337 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:134:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `bobReplicator.context!.onConnectionClosed(bobConnection!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3338 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:146:67`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await using db2 = await bob.openDatabase(spaceKey, db1.rootUrl!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3339 no-casts `packages/e2e/blade-runner/src/redis/util.ts:91:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `encode: (value: any): Any => ({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3340 no-casts `packages/e2e/blade-runner/src/redis/util.ts:95:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `decode: (value: Any): any => JSON.parse(Buffer.from(value.value).toString()),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3341 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.node.test.ts:67:13`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `} as unknown as ReplicantProps,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3342 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.ts:19:36`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `super(options.disconnectTimeout!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3343 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.ts:23:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const stream: any = WebSocketStream(this.options.address);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3344 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:82:17`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._repo!.create<DocStruct>({ text: random.string.hexadecimal({ length: mutationSize }) }),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3345 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:100:21`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._repo!.flush();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3346 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:116:42`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const handle = await this._repo!.find<DocStruct>(id);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3347 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:62:36`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._db = await this._testPeer!.createDatabase(PublicKey.fromHex(spaceKey));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3348 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:68:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `rootUrl: this._db.rootUrl!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3349 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:74:36`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._db = await this._testPeer!.openDatabase(PublicKey.fromHex(spaceKey), rootUrl);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3350 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:101:15`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._db!.add(doc);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3351 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:55:56`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._client.services.services.QueryService!.setConfig(indexing, { timeout: 20_000 });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3352 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:88:77`

Line uses `as any` and the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._client.services.services.EdgeAgentService!.createAgent(null as any, { timeout: 10_000 });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3353 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:111:40`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const response = await this._client!.edge.createSpace({ agentKey });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3354 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:116:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `condition: () => this._client!.spaces.get(response.spaceId as SpaceId),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3355 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:136:29`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `client: this._client!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3356 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:137:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `ownerUri: this._identity!.did,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3357 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:173:31`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const space = this._client!.spaces.get(spaceId);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3358 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:96:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `for (const topic of topics!) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3359 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:100:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await sleep(discoverTimeout!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3360 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:102:35`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await Promise.all(topics!.map((topic) => cancelWithContext(context, peer.leaveTopic(PublicKey.from(topic)))));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3361 no-casts `packages/e2e/blade-runner/src/replicants/storage-replicant.ts:101:68`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `filesToSave.find((f) => f.key.join() === file.key.join())!.data.equals(Buffer.from(file.data!)),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3362 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:60:31`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._edgeConnection!.send(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3363 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:75:31`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._edgeConnection!.send(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3364 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:109:63`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const payload = JSON.parse(Buffer.from(message.payload!.value).toString());`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3365 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:126:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._edgeConnection!.send(message);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3366 no-casts `packages/e2e/blade-runner/src/run-test-signal.ts:23:18`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `_onError?: (err: any) => void,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3367 no-casts `packages/e2e/blade-runner/src/spec/edge-sync.ts:96:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `configEnabledEdgeSync.runtime!.client!.edgeFeatures!.subductionReplicator = true;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3368 no-casts `packages/e2e/blade-runner/src/spec/edge-sync.ts:99:35`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `configDisabledEdgeSync.runtime!.client!.edgeFeatures!.subductionReplicator = false;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3369 no-casts `packages/e2e/blade-runner/src/spec/transport.ts:312:28`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `concurrentInits![idx]++;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3370 no-casts `packages/e2e/blade-runner/src/test-builder.ts:46:19`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `onError?: (err: any) => void,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3371 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:17:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `[data: string]: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3372 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:23:8`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `args?: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3373 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:24:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `[data: string]: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3374 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:137:21`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._controller!.enqueue(JSON.stringify(event));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3375 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:142:23`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._controller!.close();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# WARN 50877ea571-3376 no-sleep-in-test `packages/e2e/blade-runner/src/tracing/readable-muxer.node.test.ts:30`

`for (const stream of readableStreams) { muxer.pushStream(stream); await sleep(10); }` relies on a fixed 10ms gap between `pushStream` calls to keep the interleaving deterministic for the later `toDeepEqual` assertion. This is a timing-dependent synchronization; use a completion signal from `pushStream`/the muxer (or restructure so ordering doesn't depend on wall-clock spacing) instead of `sleep`.

# ERROR 50877ea571-3377 no-casts `packages/e2e/proto-guard/src/generate-snapshot.ts:79:33`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `Obj.update(object2, (object2: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3378 no-casts `packages/e2e/proto-guard/src/snapshots-registry.ts:51:62`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `Object.keys(snapshotToRemove).every((key) => (snapshot as any)[key] !== (snapshotToRemove as any)[key]),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3379 no-casts `packages/experimental/discord-bot/src/bot.ts:89:25`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3380 no-casts `packages/experimental/discord-worker/src/register-commands.ts:60:37`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `console.log(`Registered ${(result as any[]).length} commands.`);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3381 no-casts `packages/plugins/plugin-assistant/src/components/ChatPrompt/ChatOptions.stories.tsx:72:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: ChatOptions as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3382 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/registry.tsx:86:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `?.map((option: any) => option._tag === 'option' && getXmlTextChild(option.children))`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3383 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:43:35`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `updateWidget(_id: string, _value: any) {}`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3384 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:299:41`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const updates: { id: string; value: any }[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3385 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:301:48`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `override updateWidget(id: string, value: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3386 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/widgets/ToolWidget.tsx:105:61`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `type ToolPanelItem = { title: string; icon?: string; content: any };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3387 no-casts `packages/plugins/plugin-assistant/src/components/Toolbox/Toolbox.stories.tsx:28:22`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: Toolbox as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3388 no-casts `packages/plugins/plugin-assistant/src/containers/AgentArticle/AgentArticle.stories.tsx:82:68`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(space.db, random as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3389 no-casts `packages/plugins/plugin-assistant/src/containers/TracePanel/TracePanel.stories.tsx:135:42`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `const subAgentMessages = subAgentFixture as unknown as Trace.Message[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3390 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:841:44`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const event = span.events[eventIndex]!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3391 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:852:44`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const event = span.events[eventIndex]!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3392 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:887:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `pipe(...args: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3393 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:1039:40`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const commit = this.#commits[index]!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3394 no-casts `packages/plugins/plugin-assistant/src/execution-graph/pending-block-status.test.ts:114:59`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `({ meta: { pid: 'pid-1' }, isEphemeral: true, events }) as unknown as Trace.Message;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3395 no-casts `packages/plugins/plugin-assistant/src/execution-graph/remote-snapshot.test.ts:69:56`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const parentIndex = idToMainIndex.get(parentId)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3396 no-casts `packages/plugins/plugin-assistant/src/execution-graph/sub-agent-delegation.test.ts:15:35`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `const messages = (subAgentFixture as unknown as Trace.Message[])`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3397 no-casts `packages/plugins/plugin-assistant/src/processor/processor.node.test.ts:35:65`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `const managedRuntime = ManagedRuntime.make(Layer.empty) as unknown as Capabilities.ProcessManagerRuntime;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3398 no-casts `packages/plugins/plugin-assistant/src/processor/processor.node.test.ts:36:90`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const processor = new AiChatProcessor(session, managedRuntime, feed, Layer.empty as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3399 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:26:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return 'recording' as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3400 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:28:28`

Line uses `as any` and a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `add: (obj: any) => obj as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3401 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:29:36`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `addType: async () => undefined as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3402 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:38:88`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `queryFeed: () => ({ subscribe: () => () => {}, results: [], run: async () => [] }) as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3403 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:39:84`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `query: () => ({ subscribe: () => () => {}, results: [], run: async () => [] }) as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3404 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:43:30`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `makeRef: () => undefined as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3405 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:45:5`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `} as unknown as Database.Database;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3406 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:66:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `createMessageGenerator()[2]!.pipe(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3407 no-casts `packages/plugins/plugin-assistant/src/testing/test-services.ts:9:56`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `const ServiceTypeSchema = AssistantService.ServiceType as unknown as Type.Obj<AssistantService.ServiceType>;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# WARN 50877ea571-3408 no-compat-shims `packages/plugins/plugin-assistant/src/types/Assistant.ts:7`

`Chat` is re-exported from `@dxos/assistant-toolkit` with the comment "Re-export Chat schema for backward compatibility," and many files in this package (`ChatThread.tsx`, `registry.tsx`, `ChatOptions.tsx`, `ChatArticle.tsx`, `ChatDialog.tsx`, `AssistantSettings.tsx`, `usePresets.ts`, `useChatProcessor.ts`, `capabilities/settings.ts`, `capabilities/AssistantSurfaces.tsx`) still reach `Chat` through this `Assistant` namespace shim, while other files in the same package (`ChatOptions.stories.tsx`, `ChatPrompt.tsx`, `AssistantOperation.ts`, `processor.ts`) already import `Chat` directly from `@dxos/assistant-toolkit`. Per `no-compat-shims`, finish the migration by importing `Chat` from `@dxos/assistant-toolkit` at all remaining call sites and drop the re-export.

# ERROR 50877ea571-3409 no-casts `packages/plugins/plugin-assistant/src/types/AssistantService.ts:72:3`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3410 no-casts `packages/plugins/plugin-attention/src/capabilities/attention.ts:27:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer ??= {};`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3411 no-casts `packages/plugins/plugin-attention/src/capabilities/attention.ts:29:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer.attention = {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3412 workspace-deps `packages/plugins/plugin-blogger/package.json:152`

`peerDependencies` lists `@dxos/react-ui` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps — a `*` peer reads as out-of-range on any bump and cascades a spurious major. Change to `"@dxos/react-ui": "workspace:^"`.

# ERROR 50877ea571-3413 workspace-deps `packages/plugins/plugin-blogger/package.json:153`

`peerDependencies` lists `@dxos/ui-theme` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/ui-theme": "workspace:^"`.

# ERROR 50877ea571-3414 no-casts `packages/plugins/plugin-bookmarks/src/operations/summarize.ts:75:13`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3415 workspace-deps `packages/plugins/plugin-brain/package.json:164`

`peerDependencies` lists `@dxos/react-ui` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/react-ui": "workspace:^"`.

# ERROR 50877ea571-3416 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:58:9`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3417 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:78:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = mailbox.feed.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3418 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:110:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = mailbox.feed.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3419 no-casts `packages/plugins/plugin-brain/src/operations/operations.test.ts:62:9`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3420 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:78:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3421 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:79:19`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(routine!.spec?.kind).toBe('runnable');`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3422 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:80:19`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(routine!.spec?.kind === 'runnable' && routine!.spec.runnable.uri.toString()).toBe(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3423 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:85:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const trigger = await routine!.triggers[0].tryLoad();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3424 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:127:27`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.activities![key] = { lamportTimestamp, payload };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3425 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:268:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._reconcileSwarmStateTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3426 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:288:41`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `return codec.decode(peer.state!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3427 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:308:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `lastActivity.lamportTimestamp!.version! > (this._state.activities![key]?.lamportTimestamp?.version ?? 0)`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3428 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:310:31`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.activities![key] = lastActivity;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3429 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:319:50`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `async () => this._reconcileSwarmStateTask!.schedule(),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3430 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:337:41`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `.sort((a, b) => a.lamportTimestamp!.id!.localeCompare(b.lamportTimestamp!.id!));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3431 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:130:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.videoStream!.addTrack(this._state.videoTrack);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3432 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:163:27`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await this._state.peer!.open();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3433 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:164:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3434 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:185:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.videoStream!.removeTrack(this._state.videoTrack);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3435 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:188:28`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.videoStream!.addTrack(this._state.videoTrack);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3436 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:192:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3437 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:198:56`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.videoTrack && this._state.videoStream!.removeTrack(this._state.videoTrack);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3438 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:201:56`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._state.videoTrack && this._state.videoStream!.addTrack(this._state.videoTrack);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3439 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:206:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3440 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:220:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3441 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:221:71`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._speakingMonitor = new SpeakingMonitor(this._state.audioTrack!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3442 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:239:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3443 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:248:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3444 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:256:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pushTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3445 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:271:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._pullTracksTask!.schedule();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3446 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:367:43`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const track = await this._state.peer!.pullTrack({ trackData: { ...trackData, mid: undefined }, ctx });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3447 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:411:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `track: await this._state.peer!.pushTrack({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3448 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:39:11`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `body: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3449 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:47:12`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `json?: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3450 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:311:52`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const transceiver = this.session.peerConnection!.addTransceiver(track, { direction: 'sendonly' });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3451 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:316:50`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `peerConnection: this.session.peerConnection!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3452 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:318:40`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `sessionId: this.session.sessionId!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3453 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:366:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `acc.set(track.trackName!, {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3454 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:404:59`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const trackInfo = trackMap.get(trackData.trackName!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3455 no-casts `packages/plugins/plugin-calls/src/calls/util/task-scheduling.ts:21:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (error: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3456 no-casts `packages/plugins/plugin-calls/src/components/Participant/ParticipantGrid.tsx:13:51`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const getId = (user: UserState): string => user.id!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3457 no-casts `packages/plugins/plugin-calls/src/components/Participant/ParticipantGrid.tsx:41:13`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any, // TODO(burdon): Remove cast.`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3458 no-casts `packages/plugins/plugin-calls/src/components/ResponsiveGrid/ResponsiveGrid.stories.tsx:93:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: ResponsiveGrid as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3459 no-casts `packages/plugins/plugin-calls/src/components/ResponsiveGrid/ResponsiveGrid.tsx:54:62`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const defaultGetId: ResponsiveGridProps<any>['getId'] = (item: any) => item.id;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3460 no-casts `packages/plugins/plugin-calls/src/containers/CallDebugPanel/CallDebugPanel.tsx:145:50`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `'*self* ' + (self?.name ?? truncateKey(self!.id, 8)),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3461 no-casts `packages/plugins/plugin-calls/src/hooks/useVideoStreamTrack.tsx:21:44`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const stream = (videoElement.current as any).captureStream();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3462 workspace-deps `packages/plugins/plugin-chess-com/package.json:151`

`peerDependencies` lists `@dxos/react-ui` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/react-ui": "workspace:^"`.

# ERROR 50877ea571-3463 workspace-deps `packages/plugins/plugin-chess-com/package.json:152`

`peerDependencies` lists `@dxos/ui-theme` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/ui-theme": "workspace:^"`.

# ERROR 50877ea571-3464 no-casts `packages/plugins/plugin-chess-com/src/operations/sync-games.test.ts:107:64`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const variant = yield* Database.load(gamesAfterFirst[0]!.variant);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3465 no-casts `packages/plugins/plugin-client/src/capabilities/migrations.ts:23:19`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `(_migrations: any[]) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3466 no-casts `packages/plugins/plugin-client/src/capabilities/migrations.ts:33:43`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `void Promise.all(spaces.map((space: any) => space.internal.db.runMigrations(migrations))).catch((err) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3467 no-casts `packages/plugins/plugin-client/src/commands/halo/create/create.test.ts:22:92`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const layer = Layer.merge(TestLayer, fromPlugins([ClientPlugin({}), ObservabilityPlugin({} as any)]));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3468 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:50:42`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `storageConfig = config.values.runtime!.client!.storage!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3469 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:60:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `if (yield* fs.exists(storageConfig.dataRoot!)) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3470 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:61:78`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `log.error('data directory already exists', { path: storageConfig.dataRoot! });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3471 no-casts `packages/plugins/plugin-client/src/containers/AccountContainer/AccountContainer.tsx:47:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3472 no-casts `packages/plugins/plugin-client/src/containers/RecoveryCodeDialog/RecoveryCodeDialog.stories.tsx:36:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: RecoveryCodeDialog as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3473 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:60:5`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3474 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:66:5`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3475 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:72:5`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3476 no-casts `packages/plugins/plugin-client/src/operations/create-agent.ts:20:50`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `client.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 10_000 }),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3477 no-casts `packages/plugins/plugin-client/src/operations/create-passkey.ts:69:70`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `recoveryKey: PublicKey.from(new Uint8Array((credential as any).response.getPublicKey())),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3478 no-casts `packages/plugins/plugin-client/src/operations/create-passkey.ts:70:38`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `algorithm: (credential as any).response.getPublicKeyAlgorithm() === -7 ? 'ES256' : 'ED25519',`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3479 no-casts `packages/plugins/plugin-client/src/operations/create-passkey.ts:78:49`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `client.services.services.IdentityService!.createRecoveryCredential({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3480 no-casts `packages/plugins/plugin-client/src/operations/create-recovery-code.ts:22:49`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `client.services.services.IdentityService!.createRecoveryCredential({}),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3481 no-casts `packages/plugins/plugin-client/src/operations/redeem-token.ts:20:49`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `client.services.services.IdentityService!.recoverIdentity(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3482 no-casts `packages/plugins/plugin-code/src/components/FileTree/FileTree.tsx:111:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `{sortNodes(node.children!.values()).map((child) => (`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3483 no-casts `packages/plugins/plugin-commerce/src/types/Search.test.ts:43:29`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = search.feed!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3484 no-casts `packages/plugins/plugin-conductor/src/containers/CanvasArticle/CanvasArticle.tsx:74:40`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `graphMonitor={graphMonitor as any}`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3485 no-casts `packages/plugins/plugin-conductor/src/containers/CanvasArticle/CanvasArticle.tsx:109:86`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `processManagerRuntime.runPromiseExit(effect.pipe(Effect.provide(spaceLayer)) as any) as Promise<`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3486 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-coordinator/create-single-cursor.test.ts:59:38`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3487 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-coordinator/reconcile-cursors.test.ts:64:38`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3488 no-casts `packages/plugins/plugin-connector/src/capabilities/create-object.ts:48:31`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `object: undefined as unknown as Obj.Unknown,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3489 no-casts `packages/plugins/plugin-connector/src/commands/connector/oauth-flow.ts:196:15`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `(edgeClient as unknown as { _authHeader?: string })._authHeader;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3490 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:75:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3491 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:76:70`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const routineInstructions = await Routine.instructionsRef(routine!)?.tryLoad();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3492 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:83:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const trigger = await routine!.triggers[0].tryLoad();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3493 no-casts `packages/plugins/plugin-crx/src/page-actions.test.ts:48:25`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `getTarget: () => ({}) as any, // Stub database; never dereferenced by the handler.`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3494 no-casts `packages/plugins/plugin-debug/src/components/SchemaTable/SchemaTable.tsx:11:8`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `types: any[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3495 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/ObjectGenerator.tsx:21:42`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator: ValueGenerator = random as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3496 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:56:55`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `schemas: [CanvasBoard.CanvasBoard, Trigger.Trigger] as any[],`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3497 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:268:59`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `functionTrigger = triggerShape.functionTrigger!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3498 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:622:59`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `functionTrigger = triggerShape.functionTrigger!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3499 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:735:51`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `functionTrigger = triggerShape.functionTrigger!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3500 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:739:37`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `initSpec(functionTrigger.spec as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3501 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:789:80`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const inputNode = computeModel.nodes.find((node) => node.type === NODE_INPUT)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3502 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:792:60`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `functionTrigger.runnable = Ref.make(computeModel.root) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3503 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.stories.tsx:34:42`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `logStore: { export: async () => '' } as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3504 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:87:47`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const url = client.config.values.runtime!.services!.ipfs!.gateway + '/' + info.cid;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3505 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:111:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3506 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:258:110`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const updateConfig = (config: ConfigProto, setConfig: (newConfig: ConfigProto) => void, path: string[], value: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3507 no-casts `packages/plugins/plugin-debug/src/containers/DebugStatus/DebugStatus.tsx:63:33`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const errorListener = (event: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3508 no-casts `packages/plugins/plugin-deck/src/containers/DeckLayout/DeckLayout.stories.tsx:161:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const subject = (data as any)?.subject;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3509 no-casts `packages/plugins/plugin-deck/src/containers/DeckLayout/DeckLayout.stories.tsx:162:38`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const attendableId = (data as any)?.attendableId as string | undefined;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3510 no-casts `packages/plugins/plugin-deck/src/containers/DeckSettings/DeckSettings.tsx:16:32`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const isSocket = !!(globalThis as any).__args;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3511 no-casts `packages/plugins/plugin-deck/src/containers/Sidebar/ComplementarySidebar.tsx:151:12`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `subject: any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3512 no-casts `packages/plugins/plugin-deck/src/types/DeckSchema.ts:68:35`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const isLayoutMode = (value: any): value is LayoutMode => Schema.is(LayoutMode)(value);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3513 workspace-deps `packages/plugins/plugin-devtools/package.json:134`

`peerDependencies` lists `@dxos/react-ui` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/react-ui": "workspace:^"`.

# ERROR 50877ea571-3514 workspace-deps `packages/plugins/plugin-devtools/package.json:135`

`peerDependencies` lists `@dxos/ui-theme` as `workspace:*`, but the `workspace-deps` rule requires `workspace:^` for in-repo peer deps. Change to `"@dxos/ui-theme": "workspace:^"`.

# ERROR 50877ea571-3515 no-casts `packages/plugins/plugin-devtools/src/DevtoolsPlugin.tsx:41:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer ??= {};`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3516 no-casts `packages/plugins/plugin-devtools/src/DevtoolsPlugin.tsx:44:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer.changeStorageVersionInMetadata = async (version: number) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3517 no-casts `packages/plugins/plugin-devtools/src/DevtoolsPlugin.tsx:47:36`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const client: Client = (window as any).dxos.client;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3518 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:38:6`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as MessageResponse;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3519 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:96:57`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const config: Type.Config = { channels: [channelId!], descendThreads, seed: { maxDays } };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3520 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:104:60`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const layer = Layer.mergeAll(discordSourceLayer(token!), coreLayer, aiLayer);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3521 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:167:56`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `}).pipe(Effect.provide(discordSourceLayer(token!))),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3522 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:58:27`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect((result.entries![0] as LogRecord).m).toBe('rpc thing');`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3523 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:90:26`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(result.entries![0]).toEqual({ t: baseRows[0].t, m: 'one' });`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3524 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:120:51`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const byKey = Object.fromEntries(result.groups!.map((group) => [group.key, group.count]));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3525 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:158:26`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(result.entries![0]).toBe(formatPrettyLine(record));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3526 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:159:26`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(result.entries![0]).toBe('2026-01-01T00:00:01.000Z I a.ts:7 Foo hi {"k":1} ');`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3527 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.ts:310:9`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `} as unknown as Parameters<typeof shouldLog>[0];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3528 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.ts:326:52`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const key = getGroupKey(record, input.groupBy!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3529 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.stories.tsx:13:47`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const items = CitiesData.features.map((feature: any) => ({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3530 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.tsx:20:9`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `items?: any[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3531 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.tsx:49:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `containerRef.current!.append(plot);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3532 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:21:47`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const items = CitiesData.features.map((feature: any) => ({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3533 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:35:40`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const land = feature(CountriesData as any, CountriesData.objects.land as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3534 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:65:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `containerRef.current!.append(drawing);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3535 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.tsx:20:9`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `items?: any[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3536 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.tsx:28:47`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const land = topojson.feature(CountriesData as any, CountriesData.objects.land as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3537 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.stories.tsx:26:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator = random as any as ValueGenerator;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3538 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:57:27`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `.nodeLabel((node: any) => (node.type === 'schema' ? node.data.typename : (node.data.label ?? node.id)))`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3539 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:58:33`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `.nodeAutoColorBy((node: any) => (node.type === 'schema' ? 'schema' : node.data.typename))`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3540 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:61:33`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `.linkAutoColorBy((link: any) => link.type);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3541 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:81:27`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `.onNodeClick((node: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3542 no-casts `packages/plugins/plugin-explorer/src/components/Graph/ForceGraph.stories.tsx:30:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator = random as any as ValueGenerator;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3543 no-casts `packages/plugins/plugin-explorer/src/components/Lattice/Lattice.stories.tsx:27:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator = random as any as ValueGenerator;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3544 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:23:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator = random as any as ValueGenerator;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3545 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:50:38`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `data: buildOrgHierarchy(orgs as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3546 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:51:47`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `edges: connectionsToEdges(connections as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3547 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:95:19`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: Tree as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3548 no-casts `packages/plugins/plugin-explorer/src/components/Visualization/variants.ts:156:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const relation = (edge.data as any)?.object as Obj.Unknown | undefined;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3549 no-casts `packages/plugins/plugin-explorer/src/components/Visualization/variants.ts:163:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const property = (edge.data as any)?.property as string | undefined;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3550 no-casts `packages/plugins/plugin-explorer/src/containers/ExplorerArticle/ExplorerArticle.stories.tsx:25:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator = random as any as ValueGenerator;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3551 no-casts `packages/plugins/plugin-explorer/src/containers/ExplorerArticle/ExplorerArticle.stories.tsx:38:57`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return <ExplorerArticle role='article' subject={graph as any} attendableId={graph.id} variant={variant} />;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3552 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:78:34`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const org = organizations[i] as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3553 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:80:24`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `buckets.get(sector)!.push({`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3554 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:103:50`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const source = Relation.getSource(relation as any) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3555 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:104:50`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const target = Relation.getTarget(relation as any) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3556 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:111:25`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `kind: (relation as any).kind,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3557 no-casts `packages/plugins/plugin-explorer/src/util/plot.ts:7:34`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export type Accessor<T> = (object: any) => T;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3558 no-casts `packages/plugins/plugin-file/src/extensions/image.tsx:137:65`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const matched = await space.db.query(Filter.id(echoUri!)).first();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3559 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:31:92`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const manager = { acquire: () => ({ connection: fakeConnection, release: () => {} }) } as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3560 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:39:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3561 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:60:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3562 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:67:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3563 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:84:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const acquireCalls: any[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3564 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:87:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `acquire: (params: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3565 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:91:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3566 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:100:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3567 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:115:98`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const manager = { acquire: () => ({ connection: makeFakeConnection(), release: () => {} }) } as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3568 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:121:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3569 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:134:92`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const manager = { acquire: () => ({ connection: fakeConnection, release: () => {} }) } as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3570 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:140:7`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3571 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:55:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const connections: any[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3572 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:77:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const connections: any[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3573 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:96:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const connections: any[] = [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3574 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:170:5`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3575 no-casts `packages/plugins/plugin-freeq/src/services/IrcConnection.test.ts:67:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const requestedCaps = capReq!.slice('CAP REQ :'.length).split(' ');`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3576 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:83:23`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `type Listener = (event: any) => void;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3577 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:102:27`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `emit(type: string, event: any = {}): void {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3578 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:117:84`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const transport = makeWebSocketTransport('wss://irc.freeq.at/irc', FakeWebSocket as any);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3579 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:40:49`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `createVariant: () => Effect.sync(() => ({}) as unknown as Obj.Any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3580 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:49:49`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `createVariant: () => Effect.sync(() => ({}) as unknown as Obj.Any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3581 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:64:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `target={{} as any}`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3582 no-casts `packages/plugins/plugin-github/src/services/github-api.ts:452:11`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `input as unknown as Record<string, unknown>,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3583 no-casts `packages/plugins/plugin-github/src/services/github-api.ts:472:11`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `input as unknown as Record<string, unknown>,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3584 no-casts `packages/plugins/plugin-graph/src/graph.ts:74:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer ??= {};`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3585 no-casts `packages/plugins/plugin-graph/src/graph.ts:75:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).composer.graph = graph;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3586 no-casts `packages/plugins/plugin-ibkr/src/components/ReportSections/ReportSections.tsx:26:7`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `rows: any[];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3587 no-casts `packages/plugins/plugin-illustrator/src/capabilities/migrations.test.ts:32:28`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const addLegacySketch = (db: any, { name, schema = 'tldraw.com/2' }: { name?: string; schema?: string } = {}) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3588 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:58:20`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `identify: (record: any) => Identity | undefined;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3589 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:64:21`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `translate: (record: any, delta: Scene.Point) => void;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3590 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:166:66`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const nextIndex = (content: ContentMap, isCounted: (record: any) => boolean = () => true): number => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3591 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:64:67`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const [element] = objects.find((object) => object.id === id)!.elements;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3592 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:68:65`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const frame = objects.find((object) => object.id === 'CORE')!.elements[0];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3593 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:75:73`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const y = (id: string) => objects.find((object) => object.id === id)!.origin!.y;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3594 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:87:88`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const edges = objectsOf(compile(FLOWCHART)).find((object) => object.id === 'edges')!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3595 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:97:64`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const core = objects.find((object) => object.id === 'CORE')!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3596 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:101:62`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const node = objects.find((object) => object.id === id)!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3597 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:102:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(node.origin!.x).toBeGreaterThanOrEqual(core.origin!.x);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3598 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:103:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(node.origin!.y).toBeGreaterThanOrEqual(core.origin!.y);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3599 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:104:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(node.origin!.x).toBeLessThanOrEqual(core.origin!.x + frame.w);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3600 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:105:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(node.origin!.y).toBeLessThanOrEqual(core.origin!.y + frame.h);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3601 no-casts `packages/plugins/plugin-inbox/src/apis/jmap/jmap-api.test.ts:39:55`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `type MockRequest = { url: string; method: string; body: any };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3602 no-casts `packages/plugins/plugin-inbox/src/capabilities/app-graph-builder.ts:153:93`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `...(mailboxSnapshot.filters?.map(({ name, filter }: { name: string; filter: any }) =>`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3603 no-casts `packages/plugins/plugin-inbox/src/capabilities/app-graph-builder.ts:184:75`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const index = mailbox.filters.findIndex((f: any) => f.name === name);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3604 no-casts `packages/plugins/plugin-inbox/src/components/EditMessage/EditMessage.stories.tsx:20:42`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator: ValueGenerator = random as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3605 no-casts `packages/plugins/plugin-inbox/src/components/EditMessage/EditMessage.stories.tsx:86:27`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `onSend: async (message: any) => console.log(message),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3606 no-casts `packages/plugins/plugin-inbox/src/components/Event/Event.stories.tsx:21:42`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator: ValueGenerator = random as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3607 no-casts `packages/plugins/plugin-inbox/src/containers/CalendarArticle/CalendarArticle.stories.tsx:59:72`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = yield* Effect.tryPromise(() => calendar.feed!.tryLoad());`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3608 no-casts `packages/plugins/plugin-inbox/src/containers/MailboxArticle/mailbox-search.test.ts:94:35`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const subquery = (query.ast as any).filter.props.threadId.subquery;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3609 no-casts `packages/plugins/plugin-inbox/src/containers/MailboxArticle/mailbox-search.test.ts:111:35`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const subquery = (query.ast as any).filter.props.threadId.subquery;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3610 no-casts `packages/plugins/plugin-inbox/src/operations/add-mailbox.ts:19:35`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const target = input.target as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3611 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:87:53`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const messageEchoId = EID.tryParse((message as any)['@uri']);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3612 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:103:34`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = mailbox.feed!.target as Feed.Feed;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3613 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:133:13`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3614 no-casts `packages/plugins/plugin-inbox/src/operations/extractor/summarize-extractor.test.ts:90:93`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `generateText: () => Effect.succeed([{ type: 'text', text: MOCK_SUMMARY }] as const) as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3615 no-casts `packages/plugins/plugin-inbox/src/operations/extractor/summarize-extractor.test.ts:91:40`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `streamText: () => Stream.empty as any,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3616 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:44:51`

Line uses `as any` and the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const json = Entity.toJSON(result!.message) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3617 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:59:51`

Line uses `as any` and the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const json = Entity.toJSON(result!.message) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3618 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:94:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const properties = result!.message.properties;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3619 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:122:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const textBlocks = result!.message.blocks.filter(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3620 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:154:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const textBlocks = result!.message.blocks.filter(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3621 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:183:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const textBlocks = result!.message.blocks.filter(`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3622 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:207:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const properties = result!.message.properties!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3623 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:210:40`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Mailbox.isReplyable(result!.message)).toBe(false);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3624 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:219:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const properties = result!.message.properties!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3625 no-casts `packages/plugins/plugin-inbox/src/operations/mail/google/sync/mapper.test.ts:222:40`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Mailbox.isReplyable(result!.message)).toBe(true);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# WARN 50877ea571-3626 no-sleep-in-test `packages/plugins/plugin-inbox/src/operations/mail/google/sync/sync-e2e.test.ts:268`

`observeInvocations` runs `while (true) { ...await space.db.query(...).run()...; await sleep(1_000); }`, busy-polling the database every second to detect new invocation trace events. Since this queries an ECHO database, prefer an ECHO query subscription (as the rule suggests) over a sleep-based poll loop.

# ERROR 50877ea571-3627 no-casts `packages/plugins/plugin-inbox/src/testing/email-processor.ts:89:15`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const result: any =`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3628 no-casts `packages/plugins/plugin-inbox/src/testing/gmail-fixtures.test.ts:60:62`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const full = yield* api.getMessage('me', page1.messages![0].id);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3629 no-casts `packages/plugins/plugin-inbox/src/testing/gmail-fixtures.test.ts:61:42`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(full.id).toBe(page1.messages![0].id);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3630 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:9:24`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const EntityId: any; // typeof import('@dxos/functions-sdk').EntityId;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3631 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:11:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const create: any; // typeof import('@dxos/functions-sdk').create;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3632 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:13:30`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const defineFunction: any; // typeof import('@dxos/functions-sdk').defineFunction;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3633 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:15:17`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const S: any; // typeof import('@dxos/functions-sdk').S;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3634 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:17:22`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const Filter: any; // typeof import('@dxos/functions-sdk').Filter;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3635 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:19:31`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const FunctionsClient: any; // typeof import('@dxos/functions-sdk').FunctionsClient;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3636 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:21:35`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `export const createClientFromEnv: any; //  typeof import('@dxos/functions-sdk').createClientFromEnv;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3637 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:34:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = mailbox.feed!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3638 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:45:42`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect([...TagIndex.bind(mailbox.tags!.target!).objects(tagUri)]).toEqual([message.id]);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3639 no-casts `packages/plugins/plugin-inbox/src/types/SystemTags.test.ts:36:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = mailbox.feed!.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3640 no-casts `packages/plugins/plugin-inbox/src/types/SystemTags.test.ts:50:52`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const starred = SystemTags.tagAtom(mailbox.tags!.target!, starredUri);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3641 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.test.ts:28:6`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as Message.Message;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3642 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:21:24`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const matchesAst = (ast: any, message: Message.Message, tags: string[]): boolean => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3643 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:27:66`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `return Array.isArray(ast.filters) && ast.filters.every((sub: any) => matchesAst(sub, message, tags));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3644 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:29:65`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `return Array.isArray(ast.filters) && ast.filters.some((sub: any) => matchesAst(sub, message, tags));`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3645 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:66:30`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const matchesPredicate = (ast: any, value: any, message: Message.Message, tags: string[]): boolean => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3646 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:109:28`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const matchesValue = (value: any, query: any): boolean => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3647 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:125:21`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const getPath = (obj: any, path: string): any => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3648 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:171:32`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if (block && typeof (block as any).text === 'string') {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3649 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:172:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `haystacks.push((block as any).text);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3650 no-casts `packages/plugins/plugin-inbox/src/util/util.test.ts:226:7`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `} as unknown as Message.Message;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3651 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanArticle/KanbanArticle.tsx:146:27`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `out.push(target as unknown as Obj.Unknown);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3652 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanProperties/KanbanProperties.tsx:55:62`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `view.projection.pivotFieldId = values.columnFieldId!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3653 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanProperties/KanbanProperties.tsx:90:111`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const settingsSchema = (isView ? KanbanSchema.KanbanViewSettingsSchema : KanbanSchema.KanbanSettingsSchema) as any;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3654 no-casts `packages/plugins/plugin-kanban/src/hooks/useEchoChangeCallback.ts:21:27`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `Obj.update(item, (item: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3655 no-casts `packages/plugins/plugin-kanban/src/hooks/useItemsProjection.ts:25:26`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const fieldProjection: any = {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3656 no-casts `packages/plugins/plugin-kanban/src/hooks/useItemsProjection.ts:42:17`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `return stub as unknown as ProjectionModel;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3657 no-casts `packages/plugins/plugin-kanban/src/util/arrangement.test.ts:104:33`

Line uses the `as unknown as` double-cast escape hatch, which suppresses the type checker per the no-casts rule: `columns: { a: { ids: {} as unknown as string[] }, b: { ids: ['ok'] } },`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3658 no-casts `packages/plugins/plugin-linear/src/services/linear-api.ts:235:26`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `selectConnection: (data: any) => {`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3659 no-casts `packages/plugins/plugin-magazine/src/containers/MagazineArticle/MagazineArticle.stories.tsx:139:71`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const echoFeed = yield* Effect.promise(() => subscriptionFeed.feed!.load());`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3660 no-casts `packages/plugins/plugin-magazine/src/operations/sources/rss.ts:100:14`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `const items: any[] = (isAtom ? channel.entry : channel.item) ?? [];`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3661 no-casts `packages/plugins/plugin-magazine/src/operations/sources/rss.ts:106:30`

Line uses a widened `any` in the type signature, which suppresses the type checker per the no-casts rule: `? item.link.find((l: any) => l['@_rel'] === 'alternate')?.['@_href']`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3662 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:36:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const postFeed = subscription.feed.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3663 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:47:52`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `return { db, subscription, queuePost: queuePost! };`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3664 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:98:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const postFeed = subscription.feed.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3665 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:111:22`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `post: queuePost!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3666 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:118:77`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const found = await Subscription.findPostContent(subscription, queuePost!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3667 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:120:38`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(EID.equals(EID.parse(found!.post.uri), EID.parse(Obj.getURI(queuePost!)))).toBe(true);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3668 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:139:46`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const postFeed = subscription.feed.target!;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3669 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:152:22`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `post: queuePost!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3670 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:157:22`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `post: queuePost!,`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3671 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:163:79`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const query = Subscription.queryPostContentForPost(subscription, queuePost!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3672 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:166:41`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const entries = await db.query(query!).run();`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3673 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:169:78`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const latest = await Subscription.findPostContent(subscription, queuePost!);`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3674 no-casts `packages/plugins/plugin-map/src/capabilities/app-graph-builder.ts:31:50`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const presentationRef = (node.properties as any).presentation;`. Fix the type at its source (narrow the parameter/return type, add a proper guard, or thread the real type through) instead of casting/asserting past it.

# ERROR 50877ea571-3675 no-casts `packages/plugins/plugin-map/src/types/MapCapabilities.ts:70`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `useMarkers: (subject: any, options: { attendableId?: string }) => MarkerSet;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3676 no-casts `packages/plugins/plugin-markdown/src/components/MarkdownEditor/MarkdownEditorContent.tsx:178`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const composer = (window as any).composer;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3677 no-casts `packages/plugins/plugin-markdown/src/containers/MarkdownArticle/MarkdownArticle.stories.tsx:46`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const generator: ValueGenerator = random as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3678 no-casts `packages/plugins/plugin-markdown/src/hooks/useLinkQuery.ts:71`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const name = (block ? query!.slice(1) : (query ?? '')).toLowerCase();`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3679 no-casts `packages/plugins/plugin-markdown/src/testing.ts:24`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const testToolkit = Toolkit.empty as Toolkit.Toolkit<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3680 no-casts `packages/plugins/plugin-markdown/src/util.tsx:16`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const isEditorModel = (data: any): data is { id: string; text: string } =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3681 no-casts `packages/plugins/plugin-markdown/src/util.tsx:24`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type MarkdownProperties = Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3682 no-casts `packages/plugins/plugin-meeting/src/stories/EventCall.stories.tsx:117`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const feed = yield* Effect.tryPromise(() => calendar.feed!.tryLoad());`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3683 no-casts `packages/plugins/plugin-mermaid/src/extensions/mermaid-extension.ts:190`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `div.innerHTML = svg!;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3684 no-casts `packages/plugins/plugin-mermaid/src/extensions/mermaid-extension.ts:215`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3685 no-casts `packages/plugins/plugin-native-filesystem/src/capabilities/app-graph-builder.ts:287`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `): Node.NodeArg<any> | null => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3686 no-casts `packages/plugins/plugin-native/src/capabilities/spotlight-listener.ts:15`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `payload?: Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3687 no-casts `packages/plugins/plugin-native/src/capabilities/spotlight-listener.ts:35`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `await invokePromise(LayoutOperation.Open, payload as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3688 no-casts `packages/plugins/plugin-native/src/capabilities/spotlight-listener.ts:38`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `await invokePromise(LayoutOperation.SwitchWorkspace, payload as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3689 no-casts `packages/plugins/plugin-navtree/src/components/NavTreeItem/NavTreeItemAction.stories.tsx:24`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} satisfies Node.NodeArg<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3690 no-casts `packages/plugins/plugin-navtree/src/components/NavTreeItem/NavTreeItemAction.stories.tsx:40`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `}) satisfies Node.NodeArg<any>,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3691 no-casts `packages/plugins/plugin-navtree/src/components/NavTreeItem/NavTreeItemAction.tsx:125`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const baseLabel = toLocalizedString(monolithicAction ? monolithicAction.properties!.label : props.label, t);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3692 no-casts `packages/plugins/plugin-navtree/src/components/Sidebar/L0Menu.tsx:193`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `itemElement.current!.closest('[data-drag-autoscroll]')?.setAttribute('data-drag-autoscroll', 'active');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3693 no-casts `packages/plugins/plugin-navtree/src/components/Sidebar/L0Menu.tsx:196`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `itemElement.current!.closest('[data-drag-autoscroll]')?.setAttribute('data-drag-autoscroll', 'idle');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3694 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:88`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const [openItems, setOpenItems] = useState<ItemMap>({ [root.children![0].id]: true });`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3695 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:125`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `(parent!.children ??= []).push({`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3696 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:130`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `setOpenItems((open) => ({ ...open, [parent!.id]: true }));`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3697 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:216`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const [openItems] = useState<ItemMap>({ [root.children![0].id]: true });`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3698 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:17`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type TestItem = { id: string; type: string } & Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3699 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:21`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `createData: () => any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3700 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:29`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `type ObjectFactoryMap = { [type: string]: ObjectFactory<any> };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3701 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:95`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `return Object.values(this.factories).map((f) => f.schema!);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3702 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:175`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} satisfies Node.NodeArg<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3703 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:196`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} satisfies Node.NodeArg<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3704 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:198`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} satisfies Node.NodeArg<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3705 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:11`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const removeItem = (tree: Node.NodeArg<any>, source: TreeData) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3706 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:13`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const index = parent.nodes!.findIndex((node: Node.NodeArg<any>) => node.id === source.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3707 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:13`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const index = parent.nodes!.findIndex((node: Node.NodeArg<any>) => node.id === source.id);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3708 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:14`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const item = parent.nodes![index];`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3709 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:15`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `parent.nodes!.splice(index, 1);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3710 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:19`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const getNode = (tree: Node.NodeArg<any>, path: string[]) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3711 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:22`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `node = node.nodes!.find((n: Node.NodeArg<any>) => n.id === part)!;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3712 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:22`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `node = node.nodes!.find((n: Node.NodeArg<any>) => n.id === part)!;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3713 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:34`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `state: Node.NodeArg<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3714 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:43`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const index = parent.nodes!.findIndex(({ id }) => id === target.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3715 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:44`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `parent.nodes!.splice(index, 0, item);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3716 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:51`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const index = parent.nodes!.findIndex(({ id }) => id === target.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3717 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:52`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `parent.nodes!.splice(index + 1, 0, item);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3718 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:59`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `parent.nodes!.push(item);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3719 no-casts `packages/plugins/plugin-observability/src/ObservabilityPlugin.test.ts:19`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `plugins: [ObservabilityPlugin({} as any)],`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3720 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:276`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await appendToFeed(space, mailbox.feed.target!, messages);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3721 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:277`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `await appendToFeed(space, calendar.feed.target!, events);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3722 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1360`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const store = new Store<TLRecord, any>({`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3723 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1361`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `schema: tlSchema as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3724 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1362`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `props: { defaultName: '', assets: { upload: async () => '', resolve: () => '' }, onMount: () => {} } as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3725 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1367`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `PageRecordType.create({ id: pageId as any, name: pageName, index: 'a1' as IndexKey }),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3726 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1425`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as TLRecord;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3727 no-casts `packages/plugins/plugin-onboarding/src/containers/WelcomeContainer/Welcome/Welcome.stories.tsx:38`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: Welcome as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3728 no-casts `packages/plugins/plugin-onboarding/src/credentials/codec.ts:10`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `isKey: (value: any): value is PublicKey => value instanceof PublicKey,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3729 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:16`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `isKey(value: any): value is KeyType;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3730 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `encode(obj: any): string {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3731 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:36`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private _decodeRecursive(obj: any): any {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3732 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:51`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private _prepareForEncoding(obj: any): any {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3733 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:66`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private _recurse(obj: any, handler: (obj: object) => any | null): any {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3734 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:81`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const result: any = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3735 no-casts `packages/plugins/plugin-onboarding/src/credentials/util.ts:135`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `let envelope: { success: boolean; message?: string; data?: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3736 no-casts `packages/plugins/plugin-onboarding/src/credentials/util.ts:142`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `` const error: any = new Error(`Account redemption failed: ${envelope.message ?? 'unknown error'}`); ``. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3737 no-casts `packages/plugins/plugin-onboarding/src/onboarding-manager.ts:324`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3738 no-casts `packages/plugins/plugin-onboarding/src/onboarding-manager.ts:344`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `let envelope: { success: boolean; message?: string; data?: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3739 no-casts `packages/plugins/plugin-onboarding/src/onboarding-manager.ts:346`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `envelope = (await response.json()) as { success: boolean; message?: string; data?: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3740 no-casts `packages/plugins/plugin-onboarding/src/onboarding-manager.ts:357`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `` const error: any = new Error(`Account redemption failed: ${envelope.message ?? 'unknown error'}`); ``. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3741 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:43`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(globalThis as any).ethereum as Eip1193Provider | undefined;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3742 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:120`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `domain: message.domain as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3743 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:121`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `types: message.types as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3744 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:122`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `primaryType: message.primaryType as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3745 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:123`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `message: message.message as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3746 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:135`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return await publicClient.readContract(args as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3747 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.stories.tsx:126`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(space.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3748 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.stories.tsx:196`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(space.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3749 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:87`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `items: columns as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3750 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:89`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `get: (data) => data as unknown as Obj.Unknown,`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3751 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:90`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `make: (object) => object as unknown as Pipeline.Column,`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3752 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:123`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `status: random.helpers.arrayElement(['todo', 'in-progress', 'done']) as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3753 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:124`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `priority: random.helpers.arrayElement(['low', 'medium', 'high']) as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3754 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:184`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `status: random.helpers.arrayElement(['todo', 'in-progress', 'done']) as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3755 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:185`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `priority: random.helpers.arrayElement(['low', 'medium', 'high']) as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3756 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:191`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(defaultSpace.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3757 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineProperties/PipelineProperties.stories.tsx:127`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(defaultSpace.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3758 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:32`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `remarkPlugins={[[remarkFrontmatter, 'yaml'], remarkParseFrontmatter as any]}`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3759 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:34`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `rehypePlugins={[[rehypeAddClasses, classes], rehypeHighlight as any, slideLayout]}`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3760 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:50`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(tree: any, file: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3761 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:95`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const ImageWrapper = ({ node: _, ...props }: { node: any }) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3762 no-casts `packages/plugins/plugin-presenter/src/useExitPresenter.ts:16`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const useExitPresenter = (object: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3763 no-casts `packages/plugins/plugin-preview/src/capabilities/preview-popover.ts:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const resolvedLabel = Obj.getLabel(object as any, { fallback: 'typename' });`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3764 no-casts `packages/plugins/plugin-preview/src/capabilities/preview-popover.ts:117`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `DX_ANCHOR_ACTIVATE as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3765 no-casts `packages/plugins/plugin-preview/src/cards/ExpandoCard.tsx:43`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const fieldSchema = schemaForValue((subject as any)[key]);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3766 no-casts `packages/plugins/plugin-preview/src/cards/ExpandoCard.tsx:52`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(values: any, { changed }: { changed: Record<string, boolean> }) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3767 no-casts `packages/plugins/plugin-preview/src/cards/FormCard.tsx:80`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return properties.some((prop) => (subject as any)?.[prop.name] != null);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3768 no-casts `packages/plugins/plugin-preview/src/cards/FormCard.tsx:89`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const value = Obj.getValue(values as any, parts);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3769 no-casts `packages/plugins/plugin-preview/src/cards/TaskCard.tsx:39`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(statusProperty!.type as any).types[0],`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3770 no-casts `packages/plugins/plugin-preview/src/cards/TaskCard.tsx:39`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `(statusProperty!.type as any).types[0],`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3771 no-casts `packages/plugins/plugin-preview/src/cards/TaskCard.tsx:43`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const options = (statusMeta as any).value.singleSelect.options as { id: string; title: string; color: string }[];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3772 no-casts `packages/plugins/plugin-preview/src/stories/Card.stories.tsx:100`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const _FormTableEmpty: StoryObj<typeof DefaultStory<any>> = {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3773 no-casts `packages/plugins/plugin-preview/src/stories/fixtures.ts:16`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const omitImage = ({ image: _, ...rest }: any) => rest;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3774 no-casts `packages/plugins/plugin-preview/src/stories/fixtures.ts:67`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `export const createUnknown = (): Obj.Any => ({}) as unknown as Obj.Any;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3775 workspace-deps `packages/plugins/plugin-progress/package.json:86`

`peerDependencies` pins `@dxos/react-ui` and `@dxos/ui-theme` (line 86-87) to `workspace:*`, which reads as out-of-range on any bump and cascades a spurious major per the `workspace-deps` rule. Change both to `workspace:^`.

# WARN 50877ea571-3776 no-compat-shims `packages/plugins/plugin-progress/src/plugin.ts:5`

`plugin.ts` is a dead re-export shim (`export * from './ProgressPlugin'`) left behind after the plugin definition moved into `ProgressPlugin.ts`. Unlike every sibling plugin's `plugin.ts` (which uses the `Plugin.lazy(meta, () => import('#plugin'))` pattern), this one has no lazy wrapper and nothing in the repo imports `./plugin` — `index.ts` already re-exports directly from `./ProgressPlugin`, and the `#plugin` subpath in `package.json` points straight at `./src/ProgressPlugin.ts`. Per the rule, delete `plugin.ts` (or update it to follow the standard `Plugin.lazy` pattern used by every other plugin) instead of leaving the old-location re-export in place.

# ERROR 50877ea571-3777 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:78`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3778 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:81`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const routineInstructionsRef = Routine.instructionsRef(routine!);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3779 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:89`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(routine!.triggers).toHaveLength(1);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3780 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:90`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const trigger = await routine!.triggers[0].tryLoad();`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3781 no-casts `packages/plugins/plugin-registry/src/commands/plugin/index.ts:11`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const plugin: Command.Command<any, any, any, any> = Command.make('plugin').pipe(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3782 no-casts `packages/plugins/plugin-registry/src/commands/registry/index.ts:27`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const registry: Command.Command<any, any, any, any> = Command.make('registry').pipe(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3783 no-casts `packages/plugins/plugin-registry/src/commands/registry/util.ts:287`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `authHeader: (client.edge.http as unknown as { _authHeader?: string })._authHeader,`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3784 no-casts `packages/plugins/plugin-registry/src/components/PluginList/PluginList.stories.tsx:41`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `tags: random.helpers.uniqueArray(RegistryTagType.literals as any, Math.floor(Math.random() * 3)),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3785 no-casts `packages/plugins/plugin-review/src/capabilities/agent-runner.ts:126`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const content = (text as unknown as { content: string }).content ?? '';`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3786 no-casts `packages/plugins/plugin-review/src/capabilities/agent-runner.ts:226`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const original = (target.text as unknown as { content: string }).content ?? '';`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3787 no-casts `packages/plugins/plugin-review/src/containers/CommentsArticle/CommentsArticle.tsx:58`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const value = (subject as unknown as Record<string, unknown>)[key];`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3788 no-casts `packages/plugins/plugin-review/src/should-trigger-agent.test.ts:19`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as Message.Message;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3789 no-casts `packages/plugins/plugin-review/src/should-trigger-agent.test.ts:26`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as Thread.Thread;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3790 no-casts `packages/plugins/plugin-review/src/stories/DocumentVersioning.stories.tsx:300`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const text = yield* Effect.promise(() => currentDoc!.content.load());`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3791 no-casts `packages/plugins/plugin-review/src/stories/DocumentVersioning.stories.tsx:301`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `seedComments(defaultSpace, currentDoc!, text, [COMMENT_ANCHOR]);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3792 no-casts `packages/plugins/plugin-review/src/util/author-hue.ts:13`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `data?: { readonly [key: string]: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3793 no-casts `packages/plugins/plugin-review/src/util/message.ts:24`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `data?: { readonly [key: string]: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3794 no-casts `packages/plugins/plugin-routine/src/capabilities/create-object.test.ts:15`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(blank!.label).toBe('Blank');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3795 no-casts `packages/plugins/plugin-routine/src/capabilities/create-object.test.ts:16`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(typeof blank!.scaffold).toBe('function');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3796 no-casts `packages/plugins/plugin-routine/src/commands/trigger/util.ts:112`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `defaults?: Record<string, any> | undefined,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3797 no-casts `packages/plugins/plugin-routine/src/commands/trigger/util.ts:201`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const inputObj: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3798 no-casts `packages/plugins/plugin-routine/src/commands/trigger/util.ts:374`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const feedRef = (obj as any).feed as Ref.Ref<any> | undefined;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3799 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateEditor.stories.tsx:64`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: TemplateEditor as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3800 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `schema?: Schema.Schema<any, any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3801 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:185`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const inputIndex = draft.inputs!.findIndex((i) => i?.name === next.name);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3802 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:187`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `draft.inputs![inputIndex].name = name;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3803 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:190`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `draft.inputs!.push({ name, kind: 'value' });`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3804 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:197`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const inputIndex = draft.inputs!.findIndex((i) => i?.name === input.name);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3805 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:199`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `draft.inputs!.splice(inputIndex, 1);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3806 no-casts `packages/plugins/plugin-routine/src/components/TriggerEditor/TriggerEditor.stories.tsx:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(window as any)[DEBUG_SYMBOL] = { automation };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3807 no-casts `packages/plugins/plugin-routine/src/components/TriggerEditor/TriggerEditor.stories.tsx:71`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const automation = (window as any)[DEBUG_SYMBOL]?.automation as Routine.Routine | undefined;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3808 no-casts `packages/plugins/plugin-script/src/capabilities/compiler.ts:44`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `` paths: Object.fromEntries(runtimeModules.map((mod: any) => [mod.moduleName, [`./src/${mod.filename}`]])), ``. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3809 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:23`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `data?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3810 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:62`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `data?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3811 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:102`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3812 no-casts `packages/plugins/plugin-script/src/components/TypescriptEditor/TypescriptEditor.tsx:96`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `` tsFacet.of({ env: env as any, path: `./src/${id}.ts` }), ``. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3813 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.stories.tsx:84`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `subject: undefined as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3814 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:50`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const [queryValues, setQueryValues] = useState<Record<string, any>>({});`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3815 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:66`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `Obj.update(notebook!, (obj) => {`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3816 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:217`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `input: Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3817 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:230`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `onSuccess: (result: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3818 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:27`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const formatted = await format(script.source.target!.content, {`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3819 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `Obj.update(script.source.target!, (obj) => {`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3820 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:36`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3821 no-casts `packages/plugins/plugin-script/src/hooks/useCreateAndDeployScriptTemplates.ts:43`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `initialTemplateId: template.id as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3822 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:21`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private _valuesByCellIdAtom = Atom.make<Record<string, any>>({});`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3823 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:22`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private _valuesByNameAtom = Atom.make<Record<string, any>>({});`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3824 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:46`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `get values(): Atom.Atom<Record<string, any>> {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3825 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:53`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `getValues(): Record<string, any> {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3826 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:60`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `get valuesByName(): Atom.Atom<Record<string, any>> {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3827 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:67`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `getValuesByName(): Record<string, any> {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3828 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:74`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `getValue(cellId: string): any {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3829 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:97`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const valuesByName: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3830 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:98`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const valuesByCellId: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3831 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:155`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `private async eval(source: string, values: Record<string, any>) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3832 no-casts `packages/plugins/plugin-script/src/notebook/eval.ts:10`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const evalScript = async (code: string, deps: Record<string, any> = {}) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3833 no-casts `packages/plugins/plugin-script/src/notebook/eval.ts:104`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `declare var console: { log(...args: any[]): void };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3834 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `value?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3835 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:138`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `let value: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3836 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:219`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `type: typeChecker.typeToString(typeChecker.getTypeOfSymbolAtLocation(param, param.valueDeclaration!)),`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3837 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:294`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `let value: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3838 no-casts `packages/plugins/plugin-script/src/skills/functions/deploy.ts:36`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const buildResult = yield* Effect.promise(() => bundleFunction({ source: script.source!.target!.content }));`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3839 no-casts `packages/plugins/plugin-script/src/templates/commentary.ts:227`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3840 no-casts `packages/plugins/plugin-script/src/templates/data-generator.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `}: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3841 no-casts `packages/plugins/plugin-script/src/templates/data-generator.ts:27`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const result: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3842 no-casts `packages/plugins/plugin-script/src/templates/data-generator.ts:39`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const objectResult: any = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3843 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:60`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `handler: ({ data: { channelId, queueId, after = DEFAULT_AFTER, pageSize = 5 }, context: { space } }: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3844 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:64`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3845 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:68`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3846 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:83`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const messages = yield* rest.getChannelMessages(channelId, options).pipe((res: any) => res.json);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3847 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:85`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.map((message: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3848 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:98`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3849 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:100`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `yield* Ref.update(newMessages, (n: any) => n + queueMessages.length);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3850 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:101`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `yield* Ref.update(lastMessage, (m: any) => queueMessages.at(-1));`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3851 no-casts `packages/plugins/plugin-script/src/templates/email.ts:9`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export default async ({ data: { bodyText }, context: { space } }: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3852 no-casts `packages/plugins/plugin-script/src/templates/forex-effect.ts:33`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `` const json: any = yield* HttpClientRequest.get(`https://free.ratesdb.com/v1/rates?from=${from}&to=${to}`).pipe( ``. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3853 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:47`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `handler: ({ context: { space }, data: { mailboxId, userId, after, pageSize } }: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3854 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:51`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3855 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:61`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `Effect.flatMap((res: any) => res.json),`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3856 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:74`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3857 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:78`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3858 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:94`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const from = messageDetails.payload.headers.find((h: any) => h.name === 'From');`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3859 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:99`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `messageDetails.payload.parts?.find((p: any) => p.mimeType === 'text/plain')?.body.data;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3860 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:106`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const subject = messageDetails.payload.headers.find((h: any) => h.name === 'Subject')?.value;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3861 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:123`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `yield* Ref.update(newMessages, (n: any) => [object, ...n]);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3862 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:133`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `Stream.flatMap((batch: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3863 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:136`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `catch: (e: any) => e,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3864 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:147`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const getUrl = (userId: string, messageId?: string, params?: Record<string, any>) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3865 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:17`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const isScriptDeployed = ({ script, fn }: { script: Script.Script; fn: any }): boolean => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3866 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:54`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `source: script.source!.target!.content,`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3867 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:76`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `} catch (err: any) {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3868 no-casts `packages/plugins/plugin-script/src/util/functions.ts:23`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `fn: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3869 no-casts `packages/plugins/plugin-script/src/util/functions.ts:41`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `meta: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3870 no-casts `packages/plugins/plugin-search/src/containers/SearchArticle/SearchArticle.stories.tsx:57`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(defaultSpace.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3871 no-casts `packages/plugins/plugin-search/src/containers/SearchDialog/SearchDialog.stories.tsx:63`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(defaultSpace.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3872 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:58`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const label = Obj.getLabel(object as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3873 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:79`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const getStringProperty = (object: any, keys: string[]): string | undefined => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3874 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:111`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const value = (object as any)[key];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3875 no-casts `packages/plugins/plugin-search/src/search/exa.ts:184`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const sanitizeObjects = (entries: { data: any; schema: Type.AnyObj }[]) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3876 no-casts `packages/plugins/plugin-sequencer/src/audio/ScorePlayer.ts:194`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `return this.#master ? ((this.#master as unknown as { input: AudioNode }).input ?? undefined) : undefined;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3877 no-casts `packages/plugins/plugin-sequencer/src/components/SequenceGrid/SequenceGrid.tsx:566`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `atoms={atoms as any}`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3878 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.stories.tsx:93`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `applyLeadSheetToScore(score as unknown as MutableScore, document);`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3879 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:86`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const score = snapshot as unknown as Score.Score;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3880 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:116`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3881 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:124`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3882 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:138`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3883 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:157`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3884 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:173`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3885 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:183`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore & { loopStart?: number; loopEnd?: number };`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3886 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:234`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3887 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:251`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3888 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:289`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const mutable = subject as unknown as MutableScore;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3889 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:317`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const player = playerRef.current!;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3890 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:322`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const player = playerRef.current!;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3891 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:328`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const player = playerRef.current!;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3892 no-casts `packages/plugins/plugin-sequencer/src/operations/write.ts:22`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `applyLeadSheetToScore(resolved as unknown as MutableScore, document);`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3893 no-casts `packages/plugins/plugin-sheet/src/components/ComputeGraph/compute-graph.stories.tsx:30`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const [result, setResult] = useState<any>();`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3894 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:141`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `model.setValue(parseCellIndex(editing!.index), value);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3895 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:292`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `index: contextMenuOpen![contextMenuAxis] + (operation === 'insert-before' ? 0 : 1),`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3896 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:298`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `axisIndex: model.sheet[contextMenuAxis === 'row' ? 'rows' : 'columns'][contextMenuOpen![contextMenuAxis]],`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3897 no-casts `packages/plugins/plugin-sheet/src/components/SheetToolbar/align.ts:94`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `} else if (model.sheet.ranges![index].value === nextRangeEntity.value) {`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3898 no-casts `packages/plugins/plugin-sheet/src/components/SheetToolbar/SheetToolbar.stories.tsx:22`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: SheetToolbar as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3899 no-casts `packages/plugins/plugin-sheet/src/playwright/sheet-manager.ts:67`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `sourcePosition: { x: startBox!.width / 2, y: startBox!.height / 2 },`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3900 no-casts `packages/plugins/plugin-sheet/src/playwright/sheet-manager.ts:68`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `targetPosition: { x: endBox!.width / 2, y: endBox!.height / 2 },`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3901 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:17`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const getTauriWindow = (): any => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3902 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:18`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const tauri = (globalThis as any).__TAURI__;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3903 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const getTauriCore = (): any => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3904 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:26`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const tauri = (globalThis as any).__TAURI__;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3905 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:141`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `window.visualViewport ? addEventListener(window.visualViewport, 'scroll' as any, resetScroll) : () => {},`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3906 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:144`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `addEventListener(document, 'scroll', detectContainerScroll as EventListener, { capture: true } as any),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3907 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:312`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `'keyboard' as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3908 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:116`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3909 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:117`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `render: (args: any) => <DefaultStory onAction={args.onAction} />,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3910 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:151`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `args: {} as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3911 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:162`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `args: {} as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3912 no-casts `packages/plugins/plugin-simple-layout/src/hooks/actions.ts:38`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `get: (atom: Atom.Atom<any>) => any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3913 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:17`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `content?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3914 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `dialogOverlayStyle?: Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3915 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `dialogContent?: { component: string; props?: any } | null;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3916 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:34`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `popoverContent?: { component: string; props?: any } | { subject: any } | null;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3917 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/collections.ts:149`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.map((ref: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3918 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/collections.ts:216`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.map((ref: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3919 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/database.ts:314`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const snapshot = get(Entity.atom(mutableSchema as unknown as Entity.Unknown));`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3920 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:60`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3921 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:78`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3922 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:97`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3923 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:112`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, userSchemas);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3924 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:127`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `Annotation.setDictionary(annotations as any, MigrationVersionAnnotation, version);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3925 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:136`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `({ state: { get: () => state }, properties: makeFakeProperties(version) }) as unknown as Space;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3926 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/spaces.ts:239`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const order: string[] = (spacesOrderSnapshot as any)?.order ?? [];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3927 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/spaces.ts:357`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `Obj.update(spacesOrder, (spacesOrder: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3928 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:175`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const legacyRef = (space.properties as any)[Type.getTypename(Collection.Collection)];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3929 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:183`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `` const legacyVersion = (space.properties as any)[`${Migrations.namespace}.version`]; ``. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3930 no-casts `packages/plugins/plugin-space/src/containers/CreateObjectDialog/CreateObjectDialog.stories.tsx:30`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `] as unknown as Space[];`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3931 no-casts `packages/plugins/plugin-space/src/containers/CreateObjectDialog/CreateObjectDialog.stories.tsx:64`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `createObject: () => Effect.succeed({ id: 'mock-id', subject: [], object: {} as any }),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3932 no-casts `packages/plugins/plugin-space/src/containers/DefaultProperties/DefaultProperties.tsx:49`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `createObject: async (values: any, db: Database.Database): Promise<Obj.Unknown> => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3933 no-casts `packages/plugins/plugin-space/src/containers/RecordArticle/RecordArticle.stories.tsx:62`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const factory = createObjectFactory(space.db, random as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3934 no-casts `packages/plugins/plugin-space/src/containers/TypeArticle/useDuplicates.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `spec: IdentitySpec<any> | undefined;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3935 no-casts `packages/plugins/plugin-space/src/containers/TypeArticle/useDuplicates.ts:111`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `spec: IdentitySpec<any> | undefined,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3936 no-casts `packages/plugins/plugin-space/src/operations/add-object.ts:20`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const target = input.target as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3937 no-casts `packages/plugins/plugin-space/src/operations/delete-field.ts:18`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const view = input.view as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3938 no-casts `packages/plugins/plugin-space/src/operations/restore-field.ts:18`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const view = input.view as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3939 no-casts `packages/plugins/plugin-space/src/operations/snapshot.ts:15`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const db = input.db as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3940 no-casts `packages/plugins/plugin-space/src/types/SpaceCapabilities.ts:126`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const IdentitySpec = Capability.make<import('@dxos/extractor').IdentitySpec<any>>()(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3941 no-casts `packages/plugins/plugin-space/src/types/SpaceCapabilities.ts:133`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `initialFormValues?: Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3942 no-casts `packages/plugins/plugin-space/src/types/SpaceCapabilities.ts:134`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `onCreateObject: (data: Record<string, any>) => void | Promise<void>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3943 no-casts `packages/plugins/plugin-space/src/types/SpaceSchema.ts:147`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `props: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3944 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:113`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const objId = (obj as any).id as string;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3945 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:241`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if (obj && (obj as any).id === id) {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3946 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:420`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const objId = (obj as any).id as string;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3947 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:477`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const toolCtx = (toolManagerRef.current as any)?._ctx;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3948 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:577`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `id: (obj as any).id as string,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3949 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/import-export.ts:47`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const objId = (object as any).id as string | undefined;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3950 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:136`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if (obj && (obj as any).id === selectedObjectId && (obj as any).color) {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3951 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:138`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `updateEditorState({ hue: (obj as any).color });`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3952 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:156`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `if (obj && (obj as any).id === selectedObjectId) {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3953 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/add-object.ts:55`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const objId = (object as any).id as string | undefined;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3954 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/delete-objects.ts:30`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3955 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/join-objects.ts:62`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3956 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/join-objects.ts:77`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const newObjId = (newObject as any).id as string;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3957 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/subtract-objects.ts:62`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3958 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/subtract-objects.ts:77`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const newObjId = (newObject as any).id as string;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3959 no-casts `packages/plugins/plugin-status-bar/src/components/StatusBar/StatusBar.stories.tsx:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: StatusBar as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3960 workspace-deps `packages/plugins/plugin-studio/package.json:201`

`peerDependencies` pins `@dxos/react-ui` to `workspace:*`, violating the `workspace-deps` rule (peers must use the caret form `workspace:^` to avoid cascading a spurious major on any bump). Change it to `workspace:^`.

# ERROR 50877ea571-3961 no-casts `packages/plugins/plugin-support/src/containers/FeedbackPanel/FeedbackPanel.stories.tsx:30`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `captureUserFeedback: async (form: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3962 no-casts `packages/plugins/plugin-support/src/containers/FeedbackPanel/FeedbackPanel.stories.tsx:36`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as Observability.Observability;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3963 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:68`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(_row: number, objects: any[]) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3964 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:102`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(actionId: string, data: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3965 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:117`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(schema: Type.AnyEntity, values: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3966 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:160`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const { props } = model.projection!.getFieldProjection(field.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-3967 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:192`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(row: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3968 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:260`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.map((order) => Order.property<any>(order.property, order.direction));`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3969 no-casts `packages/plugins/plugin-tasks/src/types/Journal.ts:69`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `db: { add: (obj: any) => any },`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3970 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:23`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `dialogOverlayStyle?: Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3971 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `dialogContent?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3972 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:34`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `popoverContent?: any;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3973 no-casts `packages/plugins/plugin-thread/src/containers/ThreadArticle/ThreadArticle.stories.tsx:55`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: ThreadComponent.Root as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3974 no-casts `packages/plugins/plugin-thread/src/hooks/useStatus.ts:23`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `setProcessing((prev: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3975 no-casts `packages/plugins/plugin-thread/src/util.ts:18`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `data?: { readonly [key: string]: any };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3976 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:193`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `actions: (_editor: Editor, actions: any, _helpers: any) => ({`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3977 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:205`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `tools: (_editor: Editor, tools: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3978 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:229`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `Toolbar: (props: any) => (`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3979 no-casts `packages/plugins/plugin-tldraw/src/migrations/migrate.ts:15`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type MigrateCanvas = (current: SerializedStore<TLRecord>, version?: number) => Promise<SerializedStore<any>>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3980 no-casts `packages/plugins/plugin-tldraw/src/model/builder.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const isShape = (record: any) => record?.typeName === 'shape';`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3981 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:16`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `type ShapeRecord = Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3982 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:171`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.sort((a: any, b: any) => Number(String(a.index).slice(1)) - Number(String(b.index).slice(1)))`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3983 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:172`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `.map((point: any) => ({ x: point.x, y: point.y }));`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3984 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:25`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const shapes = Object.values(content).filter((record: any) => record.typeName === 'shape');`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3985 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const bindings = Object.values(content).filter((record: any) => record.typeName === 'binding');`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3986 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `expect((bindings[0] as any).toId).to.eq('shape:a');`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3987 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:38`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const arrow: any = content['shape:arrow'];`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3988 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:39`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const line: any = content['shape:line'];`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3989 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:43`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `expect(Object.values(content).filter((record: any) => record.typeName === 'binding')).to.have.length(0);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3990 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:101`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `for (const record of Object.values(content) as any[]) {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3991 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:107`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const content: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3992 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:152`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const content: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3993 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:173`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const content: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3994 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:195`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const content: Record<string, any> = {};`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# WARN 50877ea571-3995 no-compat-shims `packages/plugins/plugin-transcription/src/normalization/normalization.ts:24`

`NormalizationInput` and `NormalizationOutput` (lines 23-27) are `@deprecated` type aliases pointing at `SentenceNormalizationInputType`/`SentenceNormalizationOutput`, which now live in `../types/TranscriptOperation`. No call site anywhere in `plugin-transcription` (or elsewhere in the repo) still references either alias — they are a leftover compatibility re-export from the rename with no remaining consumers. Per the `no-compat-shims` rule, delete both aliases now that the move to `TranscriptOperation` is complete, rather than leaving the old names behind "for compatibility".

# ERROR 50877ea571-3996 no-casts `packages/plugins/plugin-transcription/src/operations/summarize.ts:62`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-3997 no-casts `packages/plugins/plugin-transcription/src/stories/Pipeline.stories.tsx:131`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const STAGE_FACTORY: Record<StageId, () => Stage<any, any>> = {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3998 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:22`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const enableQueryIndexes = (services: { QueryService?: any }) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-3999 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:25`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `services.QueryService!.setConfig({`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4000 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:35`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `yield* Effect.promise(() => services.QueryService!.reindex());`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4001 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:16`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(env.backends.onnx as any).wasm.numThreads = 1;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4002 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:17`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(env.backends.onnx as any).provider = 'webgpu';`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4003 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:18`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `(env.backends.onnx as any).webgpu = {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4004 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:89`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `progress_callback: (progress: any) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4005 no-casts `packages/plugins/plugin-transformer/src/testing/pipeline.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `protected abstract generateText(prompt: string): Promise<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4006 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:137`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `invoke: () => Effect.succeed(undefined as any),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4007 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:139`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `invokePromise: () => Promise.resolve({ data: undefined as any }),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4008 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:256`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const fetchLists = trelloApi.fetchLists as unknown as ReturnType<typeof vi.fn>;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4009 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:51`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `db: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4010 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:69`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const makeKanban = (db: any) =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4011 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:126`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4012 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:243`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `(localItem as unknown as Record<string, unknown>).description = 'local edit';`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4013 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:258`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const item = localItem as unknown as Record<string, unknown>;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4014 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:283`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `(localItem as unknown as Record<string, unknown>).description = 'local edit';`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4015 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:297`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const item = localItem as unknown as Record<string, unknown>;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4016 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:361`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `db: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4017 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:415`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4018 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:465`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4019 no-casts `packages/plugins/plugin-trip/src/capabilities/app-graph-builder.ts:66`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const target = Ref.isRef(ref) ? ref.target : (ref as unknown as Segment.Segment | undefined);`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4020 no-casts `packages/plugins/plugin-trip/src/containers/SegmentArticle/SegmentArticle.tsx:42`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const value = Obj.getValue(values as any, parts);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4021 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:305`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const updatedSegment = second.updated!.find((obj) => Obj.instanceOf(Segment.Segment, obj)) as Segment.Segment;`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4022 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:364`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4023 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:394`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4024 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:418`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4025 no-casts `packages/plugins/plugin-video/src/components/Transcript/Transcript.stories.tsx:43`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `component: Transcript as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4026 no-casts `packages/plugins/plugin-video/src/operations/summarize.ts:73`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `} as any),`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4027 no-casts `packages/plugins/plugin-voxel/src/components/VoxelEditor/VoxelEditor.tsx:251`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const controlsRef = useRef<any>(null);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4028 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:50`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const nextVoxels = lifeRef.current!.tick(currentVoxels);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4029 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:51`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `updateVoxels(Voxel.toVoxelMap(nextVoxels) as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4030 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:67`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return { [key]: { hue: voxel.hue } } as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4031 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:101`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `return Voxel.toVoxelMap(newVoxels) as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4032 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:116`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `const pattern = lifeRef.current!.randomPattern();`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4033 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:117`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `updateVoxels(Voxel.toVoxelMap(pattern) as any);`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4034 no-casts `packages/plugins/plugin-voxel/src/types/Voxel.ts:102`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `voxels: voxels ? (toVoxelMap(voxels) as any) : {},`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4035 no-casts `packages/plugins/plugin-voxel/src/types/VoxelOperation.ts:111`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `Schema.Union(...MODEL_TYPES.map((type) => Schema.Literal(type))) as unknown as Schema.Schema<ModelType>`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4036 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:27`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const space = { id: spaceId } as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4037 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:28`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const client = { spaces: { get: (id: SpaceId) => (id === spaceId ? space : undefined) } } as unknown as Client;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4038 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:29`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const blockstore = {} as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4039 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:63`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const directory = {} as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4040 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:64`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const forest = {} as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4041 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:85`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `vi.mocked(loadWnfs).mockResolvedValueOnce({ directory: {} as any, forest: {} as any });`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4042 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:95`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const directory = {} as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4043 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:96`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const forest = {} as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4044 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:33`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const propsAny = space.properties as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4045 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:36`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `delete (obj as any)['wnfs_access_key'];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4046 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:37`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `delete (obj as any)['wnfs_private_forest_cid'];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4047 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:44`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `delete (properties as any)['wnfs'];`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4048 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:49`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const cacheKey = wnfsState?.privateForestCid as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4049 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:59`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `?.privateForestCid as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4050 no-casts `packages/plugins/plugin-zen/src/components/Editor/Editor.tsx:22`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(values: any, { changed }: { changed: Record<string, boolean> }) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4051 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:181`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._leftOscillator!.frequency.setTargetAtTime(baseFrequency, now, rampTime);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4052 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:182`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._rightOscillator!.frequency.setTargetAtTime(baseFrequency + beatFrequency, now, rampTime);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4053 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:183`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._masterGain!.gain.setTargetAtTime(volume, now, rampTime);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4054 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:223`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `this._noiseGain.connect(this._masterGain!);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4055 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:236`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `transports.set(id, transport!);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4056 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:240`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `if (transport!.sessionId) {`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4057 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:241`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `transports.delete(transport!.sessionId);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4058 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:90`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: Array<{ name: string }> };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4059 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:101`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: Array<{ name: string }> };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4060 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:110`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4061 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:122`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: null; note: string };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4062 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:132`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: Array<{ name: string }> };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4063 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:143`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: Array<{ ref: string }> };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4064 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:152`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as {`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4065 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:164`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: { source?: string } | null };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4066 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:176`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: unknown[] };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4067 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:189`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const payload = parseToolText(result as any) as { data: Array<unknown>; truncated?: string };`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4068 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:199`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `})) as any,`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4069 no-casts `packages/reflect/introspect-mcp/src/tools/tools.ts:90`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `inputSchema: Schema.Struct<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4070 no-casts `packages/reflect/introspect-mcp/src/tools/tools.ts:106`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `): Record<string, ToolDefinition<any>> => ({`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4071 no-casts `packages/reflect/introspect-tools/src/metadata.ts:66`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `inputSchema: Schema.Struct<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4072 no-casts `packages/reflect/introspect-tools/src/metadata.ts:75`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `outputSchema: Schema.Struct<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4073 no-casts `packages/reflect/introspect-tools/src/metadata.ts:209`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const inputSchemaToZod = (struct: Schema.Struct<any>): Record<string, z.ZodTypeAny> => effectFieldsToZod(struct);`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4074 private-new-packages `packages/reflect/introspect/src/__fixtures__/packages/pkg-b/package.json:4`

This fixture package (`@fixture/pkg-b`, not present on the npm registry — confirmed new/unpublished) explicitly sets `"private": false`, unlike its sibling fixture `pkg-a` which correctly sets `"private": true`. Per the `private-new-packages` rule, every newly-added package must set `"private": true` until a trusted publisher exists; change `"private": false` to `"private": true` here.

# ERROR 50877ea571-4075 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(code!.package).toBe('@dxos/plugin-code');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4076 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:32`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Array.isArray(code!.tags)).toBe(true);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4077 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:33`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(Array.isArray(code!.dependsOn)).toBe(true);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4078 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(code!.dependsOn).toEqual(expect.arrayContaining(['org.dxos.plugin.graph', 'org.dxos.plugin.space']));`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4079 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:43`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(assistant!.tags).toEqual([]);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4080 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:73`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(chess!.description).toMatch(/Full-featured chess game/);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4081 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:75`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(chess!.tags).toEqual(['game']);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4082 no-casts `packages/reflect/introspect/src/__sanity__/real-monorepo.test.ts:30`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(introspectPkg!.entryPoints[0]).toContain('src/index.ts');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4083 no-casts `packages/reflect/introspect/src/__sanity__/real-monorepo.test.ts:37`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.kind).toBe('function');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4084 no-casts `packages/reflect/introspect/src/indexer/symbols.ts:213`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const initializer = (decl as unknown as { getInitializer?: () => Node | undefined }).getInitializer?.();`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4085 no-casts `packages/reflect/introspect/src/indexer/symbols.ts:244`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const candidate = cursor as unknown as { getJsDocs?: () => JSDoc[] };`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4086 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:58`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.workspaceDependencies).toEqual(['@dxos/echo-react', '@fixture/pkg-a']);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4087 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:59`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.externalDependencies).toEqual(['react']);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4088 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:60`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.entryPoints).toEqual(['src/index.ts']);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4089 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:71`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskMatch!.summary).toContain('Task item');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4090 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:78`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(make!.kind).toBe('function');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4091 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:79`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(make!.summary).toContain('factory');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4092 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:124`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.signature).toContain('Task');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4093 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:125`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.location.file).toContain('pkg-a/src/Task.ts');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4094 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:126`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.location.line).toBeGreaterThan(0);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4095 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:127`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.summary).toContain('Task item');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4096 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:128`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toBeUndefined();`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4097 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:133`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('Schema.Struct');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4098 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:134`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('Type.makeObject');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4099 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:135`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('com.example.type.Task');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4100 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:137`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain("description: 'Short summary of the task.'");`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4101 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:139`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain("LabelAnnotation.set(['title'])");`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4102 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:146`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('export class Task');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4103 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:147`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('Type.makeObject');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4104 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:153`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.source).toContain('useObject');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4105 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:154`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(detail!.location.file).toContain('pkg-b/src/TaskCard/TaskCard.tsx');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4106 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:167`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskFactory!.applies).toContain('Task');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4107 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:168`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskFactory!.host.kind).toBe('symbol');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4108 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:169`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskFactory!.host.symbol).toBe('make');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4109 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:170`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskFactory!.host.file).toContain('pkg-a/src/Task.ts');`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4110 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:171`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `expect(taskFactory!.uses).toEqual(['{@link Task}']);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4111 no-casts `packages/sdk/app-framework/src/cli/cli.ts:23`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `type SubCommands = [Command.Command<any, any, any, any>, ...Array<Command.Command<any, any, any, any>>];`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4112 no-casts `packages/sdk/app-framework/src/cli/cli.ts:26`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `rootCommand: Command.Command<any, any, any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4113 no-casts `packages/sdk/app-framework/src/common/capabilities.ts:81`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type AnyCommand = Command$.Command<any, any, any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4114 no-casts `packages/sdk/app-framework/src/common/capabilities.ts:93`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const Layer = Capability$.make<Layer$.Layer<any, any, never>>()('org.dxos.app-framework.capability.layer');`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4115 no-casts `packages/sdk/app-framework/src/common/capabilities.ts:230`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type ManagedRuntime = ManagedRuntime$.ManagedRuntime<any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4116 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:23`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `expect(() => Role.make('org.dxos.test.role.bad-name' as any)).toThrow();`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4117 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:27`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `expect(() => Role.make('article' as any)).toThrow();`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4118 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:31`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `expect(() => Role.make('org.article' as any)).toThrow();`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4119 no-casts `packages/sdk/app-framework/src/core/capability.ts:104`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `capability: MultiTag<T, any> | InterfaceDef<T>,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4120 no-casts `packages/sdk/app-framework/src/core/capability.ts:166`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type Implementation<I extends InterfaceDef<any>> = I extends InterfaceDef<infer T> ? T : never;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4121 no-casts `packages/sdk/app-framework/src/core/capability.ts:228`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export interface Tag<T, S extends string = any>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4122 no-casts `packages/sdk/app-framework/src/core/capability.ts:236`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export interface MultiTag<T, S extends string = any>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4123 no-casts `packages/sdk/app-framework/src/core/capability.ts:241`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type AnyTag = Tag<any, any> | MultiTag<any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4124 no-casts `packages/sdk/app-framework/src/core/capability.ts:264`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `}) as unknown as A extends 'multi' ? MultiTag<T, S> : Tag<T, S>;`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4125 no-casts `packages/sdk/app-framework/src/core/capability.ts:287`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `: buildTag<unknown, string, 'multi'>(identifier, 'multi')) as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4126 no-casts `packages/sdk/app-framework/src/core/capability.ts:302`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `: buildTag<unknown, string, 'single'>(identifier, 'single')) as any;`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4127 no-casts `packages/sdk/app-framework/src/core/capability.ts:320`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export type Any = Capability<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4128 no-casts `packages/sdk/app-framework/src/core/capability.ts:372`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `C extends Tag<any, infer S>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4129 no-casts `packages/sdk/app-framework/src/core/capability.ts:374`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `: C extends MultiTag<any, infer S>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4130 no-casts `packages/sdk/app-framework/src/core/capability.ts:388`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `<C extends Tag<any, any>>(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4131 no-casts `packages/sdk/app-framework/src/core/capability.ts:390`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `implementation: C extends Tag<infer T, any> ? T : never,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4132 no-casts `packages/sdk/app-framework/src/core/capability.ts:392`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `<C extends MultiTag<any, any>>(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4133 no-casts `packages/sdk/app-framework/src/core/capability.ts:394`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `implementation: C extends MultiTag<infer T, any> ? T : never,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4134 no-casts `packages/sdk/app-framework/src/core/capability.ts:399`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `[ContributionTypeId]: capability as unknown as IdentifierOf<C>,`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4135 no-casts `packages/sdk/app-framework/src/core/capability.ts:407`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `export const contributeAll = <C extends MultiTag<any, any>>(`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4136 no-casts `packages/sdk/app-framework/src/core/capability.ts:409`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `implementations: C extends MultiTag<infer T, any> ? readonly T[] : never,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4137 no-casts `packages/sdk/app-framework/src/core/capability.ts:412`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `[ContributionTypeId]: capability as unknown as IdentifierOf<C>,`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4138 no-casts `packages/sdk/app-framework/src/core/capability.ts:487`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `(options: Options): Effect.Effect<any, Error, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4139 no-casts `packages/sdk/app-framework/src/core/capability.ts:530`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const lazyFn = (options: Options): Effect.Effect<any, Error, any> =>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4140 no-casts `packages/sdk/app-framework/src/core/capability.ts:545`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const props = spec.props ? spec.props(options) : (options as unknown as Props);`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4141 no-casts `packages/sdk/app-framework/src/core/capability.ts:575`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const body = (options: Options): Effect.Effect<any, Error, any> => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4142 no-casts `packages/sdk/app-framework/src/core/capability.ts:578`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const props = spec.props ? spec.props(options) : (options as unknown as Props);`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4143 no-casts `packages/sdk/app-framework/src/core/plugin-manager/module-loader.ts:168`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const pending: Deferred.Deferred<any, Error>[] = [];`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4144 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:44`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const atomCounter = (registry: Registry.Registry, atom: Atom.Atom<any>) => {`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4145 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1619`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `assert.isFalse(Plugin.isLazy(registered!));`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4146 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1620`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `assert.strictEqual(registered!.modules.length, 1);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4147 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1722`

Line uses the non-null assertion `!`, which suppresses the type checker per the no-casts rule: `assert.isDefined(errorMessage!.error);`. Fix the type at its source (narrow with a guard, provide a default, or make the field non-optional) instead of asserting past it.

# ERROR 50877ea571-4148 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1763`

Line uses `as any`, which suppresses the type checker per the no-casts rule: `const BadFactory = Object.assign(() => ({ not: 'a plugin' }) as any, { meta: lazyMeta });`. Fix the type at its source (narrow the value's type, add a type guard, or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4149 no-casts `packages/sdk/app-framework/src/core/plugin.ts:139`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `props?: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4150 no-casts `packages/sdk/app-framework/src/core/plugin.ts:157`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `readonly activate: (props?: any) => Effect.Effect<any, any, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4151 no-casts `packages/sdk/app-framework/src/core/plugin.ts:180`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `? Opts['activate'] extends (props?: any) => Effect.Effect<infer A, infer E, infer R>`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4152 no-casts `packages/sdk/app-framework/src/core/plugin.ts:212`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `readonly activate: (props?: any) => Effect.Effect<any, Error, any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4153 no-casts `packages/sdk/app-framework/src/core/plugin.ts:404`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `moduleOrOptionsOrBuilder: Capability.Module<any> | ModuleEntry | ((options: T) => ModuleEntry) | PluginBuilder<T>,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4154 no-casts `packages/sdk/app-framework/src/core/plugin.ts:410`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `const module = moduleOrOptionsOrBuilder as Capability.Module<any>;`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4155 no-casts `packages/sdk/app-framework/src/core/plugin.ts:464`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `module: ModuleEntry | ((options: any) => ModuleEntry),`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4156 no-casts `packages/sdk/app-framework/src/core/plugin.ts:465`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `options?: any,`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4157 no-casts `packages/sdk/app-framework/src/core/plugin.ts:536`

Line has a widened `any` in a type signature/generic, which suppresses the type checker per the no-casts rule: `type LazyPayload = { loader: LazyLoader<any>; options: unknown };`. Replace `any` with the precise type, a bounded generic, or `unknown` narrowed at the call site.

# ERROR 50877ea571-4158 no-casts `packages/sdk/app-framework/src/core/plugin.ts:620`

Line uses the double-cast `as unknown as T` escape hatch, which suppresses the type checker per the no-casts rule: `const { loader, options } = (plugin as unknown as { [LazyTag]: LazyPayload })[LazyTag];`. Fix the type at its source (narrow the value's real type or thread the correct type through) instead of casting past it.

# ERROR 50877ea571-4159 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/history-tracker.test.ts:18`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;`

# ERROR 50877ea571-4160 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:11`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `operation: Operation.Definition<any, any>;`

# ERROR 50877ea571-4161 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:12`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `input: any;`

# ERROR 50877ea571-4162 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:13`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `output: any;`

# ERROR 50877ea571-4163 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:14`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `inverse: Operation.Definition<any, any>;`

# ERROR 50877ea571-4164 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:15`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `inverseInput: any;`

# ERROR 50877ea571-4165 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:15`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type InputOf<T> = T extends Operation.Definition<infer I, any> ? I : never;`

# ERROR 50877ea571-4166 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:20`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type OutputOf<T> = T extends Operation.Definition<any, infer O> ? O : never;`

# ERROR 50877ea571-4167 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:26`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type MessageProvider<Op extends Operation.Definition<any, any>> =`

# ERROR 50877ea571-4168 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:38`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `Op extends Operation.Definition<any, any> = Operation.Definition<any, any>,`

# ERROR 50877ea571-4169 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `Inv extends Operation.Definition<any, any> = Operation.Definition<any, any>,`

# ERROR 50877ea571-4170 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:66`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `Op extends Operation.Definition<any, any>,`

# ERROR 50877ea571-4171 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:67`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `Inv extends Operation.Definition<any, any>,`

# ERROR 50877ea571-4172 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:116`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const make = <Op extends Operation.Definition<any, any>, Inv extends Operation.Definition<any, any>>(`

# ERROR 50877ea571-4173 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-mapping.ts:123`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const resolveMessage = <Op extends Operation.Definition<any, any>>(`

# ERROR 50877ea571-4174 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:17`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `inverse: Operation.Definition<any, any>;`

# ERROR 50877ea571-4175 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:19`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `deriveContext: (input: any, output: any) => any | undefined;`

# ERROR 50877ea571-4176 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:21`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `message?: UndoMapping.MessageProvider<Operation.Definition<any, any>>;`

# ERROR 50877ea571-4177 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `lookup: (operation: Operation.Definition<any, any>) => UndoMappingResult | undefined;`

# ERROR 50877ea571-4178 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const lookup = (operation: Operation.Definition<any, any>): UndoMappingResult | undefined => {`

# ERROR 50877ea571-4179 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:178`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const managedRuntime = ManagedRuntime.make(runtimeLayer as Layer.Layer<any, any, never>);`

# ERROR 50877ea571-4180 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:187`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `runPromise: (effect, options) => managedRuntime.runPromise(effect as Effect.Effect<any, any, any>, options),`

# ERROR 50877ea571-4181 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:189`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `managedRuntime.runPromiseExit(effect as Effect.Effect<any, any, any>, options),`

# ERROR 50877ea571-4182 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:190`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `runFork: (effect, options) => managedRuntime.runFork(effect as Effect.Effect<any, any, any>, options),`

# ERROR 50877ea571-4183 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:191`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `runSync: (effect) => managedRuntime.runSync(effect as Effect.Effect<any, any, any>),`

# ERROR 50877ea571-4184 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:216`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `Effect.flatMap(ProcessManager.ProcessOperationInvoker.Service, Effect.succeed) as unknown as Effect.Effect<`

# ERROR 50877ea571-4185 no-casts `packages/sdk/app-framework/src/testing/harness.ts:232`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const result = await invoker.invokePromise(op as any, ...(args as [any]));`

# ERROR 50877ea571-4186 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:75`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `spyFn?: (impl: (...args: any[]) => any) => any,`

# ERROR 50877ea571-4187 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:81`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const record = (op: Operation.Definition<any, any>, input?: unknown) => {`

# ERROR 50877ea571-4188 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:103`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return delegate()!.invocations;`

# ERROR 50877ea571-4189 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:106`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return delegate()!.pendingFollowups;`

# ERROR 50877ea571-4190 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:109`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return delegate()!.awaitFollowups;`

# ERROR 50877ea571-4191 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:111`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `invoke: (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {`

# ERROR 50877ea571-4192 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:114`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return Effect.succeed(undefined as any);`

# ERROR 50877ea571-4193 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:116`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return (delegate()!.invoke as any)(op, input, ...rest);`

# ERROR 50877ea571-4194 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:116`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return (delegate()!.invoke as any)(op, input, ...rest);`

# ERROR 50877ea571-4195 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:118`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `invokePromise: async (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {`

# ERROR 50877ea571-4196 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:123`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return (delegate()!.invokePromise as any)(op, input, ...rest);`

# ERROR 50877ea571-4197 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:123`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return (delegate()!.invokePromise as any)(op, input, ...rest);`

# ERROR 50877ea571-4198 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:125`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `schedule: (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {`

# ERROR 50877ea571-4199 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:130`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return (delegate()!.schedule as any)(op, input, ...rest);`

# ERROR 50877ea571-4200 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:130`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return (delegate()!.schedule as any)(op, input, ...rest);`

# ERROR 50877ea571-4201 no-casts `packages/sdk/app-framework/src/testing/react.tsx:40`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type RenderSurfaceProps<TToken extends Role.Role<any>> = {`

# ERROR 50877ea571-4202 no-casts `packages/sdk/app-framework/src/testing/react.tsx:44`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `fallback?: FC<{ error: Error; data?: any }>;`

# ERROR 50877ea571-4203 no-casts `packages/sdk/app-framework/src/testing/react.tsx:51`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const renderSurface = <TToken extends Role.Role<any>>(`

# ERROR 50877ea571-4204 no-casts `packages/sdk/app-framework/src/testing/react.tsx:59`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `<SurfaceComponent type={type} data={data as any} limit={limit} fallback={fallback} placeholder={placeholder} />,`

# ERROR 50877ea571-4205 no-casts `packages/sdk/app-framework/src/testing/withPluginManager.stories.tsx:64`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any).__ERROR_BOUNDARY_ERRORS__ = [];`

# ERROR 50877ea571-4206 no-casts `packages/sdk/app-framework/src/testing/withPluginManager.tsx:100`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const options = typeof init === 'function' ? init(context as any) : init;`

# ERROR 50877ea571-4207 no-casts `packages/sdk/app-framework/src/ui/components/PluginManager/PluginManagerContext.stories.tsx:29`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const counter = (capabilities as any)[0];`

# ERROR 50877ea571-4208 no-casts `packages/sdk/app-framework/src/ui/components/PluginManager/PluginManagerContext.stories.tsx:70`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const counter = (capabilities as any)[0];`

# ERROR 50877ea571-4209 no-casts `packages/sdk/app-framework/src/ui/components/Surface/index.ts:45`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type TypedProps<TToken extends Role.Role<any>> = SurfaceTypedProps<TToken>;`

# ERROR 50877ea571-4210 no-casts `packages/sdk/app-framework/src/ui/components/Surface/index.ts:48`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ComponentProps<T extends Record<string, any> = Record<string, any>> = SurfaceComponentPropsType<T>;`

# ERROR 50877ea571-4211 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.test.tsx:189`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const IsAvailableProbe = ({ role, testId }: { role: Role.Role<any>; testId: string }) => {`

# ERROR 50877ea571-4212 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:74`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data?: Record<string, any>;`

# ERROR 50877ea571-4213 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:77`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `[key: string]: any;`

# ERROR 50877ea571-4214 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:113`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const previousPropsRef = useRef<Record<string, any>>({});`

# ERROR 50877ea571-4215 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:138`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data?: Record<string, any>;`

# ERROR 50877ea571-4216 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:140`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `fallback?: FC<{ error: Error; data?: any }>;`

# ERROR 50877ea571-4217 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:142`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `[key: string]: any;`

# ERROR 50877ea571-4218 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:223`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `}: TypedProps<Role.Role<any>>) => {`

# ERROR 50877ea571-4219 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:291`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `) as (<TToken extends Role.Role<any>>(props: TypedProps<TToken>) => React.ReactNode) &`

# ERROR 50877ea571-4220 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:292`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `NamedExoticComponent<TypedProps<Role.Role<any>>>;`

# ERROR 50877ea571-4221 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:343`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `type IsSurfaceAvailable = <TToken extends Role.Role<any>>(args: { type: TToken; data?: Role.Data<TToken> }) => boolean;`

# ERROR 50877ea571-4222 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:356`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(args: { type: Role.Role<any>; data?: Props['data'] }) => {`

# ERROR 50877ea571-4223 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceDebug.tsx:373`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const node = element as any;`

# ERROR 50877ea571-4224 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:16`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const filter: Filter<Record<string, any>> = {`

# ERROR 50877ea571-4225 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const token = Role.make<Record<string, any>>('org.dxos.test.role.article');`

# ERROR 50877ea571-4226 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:32`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'ok' }, 'org.dxos.test.role.article')).toBe(true);`

# ERROR 50877ea571-4227 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:33`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'no' }, 'org.dxos.test.role.article')).toBe(false);`

# ERROR 50877ea571-4228 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:37`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const tokenA = Role.make<Record<string, any>>('org.dxos.test.role.article');`

# ERROR 50877ea571-4229 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:38`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const tokenB = Role.make<Record<string, any>>('org.dxos.test.role.section');`

# ERROR 50877ea571-4230 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const filter: Filter<Record<string, any>> = {`

# ERROR 50877ea571-4231 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:41`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `{ role: tokenA.role, guard: (data) => (data as any).subject === 'a' },`

# ERROR 50877ea571-4232 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:42`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `{ role: tokenB.role, guard: (data) => (data as any).subject === 's' },`

# ERROR 50877ea571-4233 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:48`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'a' }, tokenA.role)).toBe(true);`

# ERROR 50877ea571-4234 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:49`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'a' }, tokenB.role)).toBe(false);`

# ERROR 50877ea571-4235 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:50`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 's' }, tokenB.role)).toBe(true);`

# ERROR 50877ea571-4236 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:51`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 's' }, tokenA.role)).toBe(false);`

# ERROR 50877ea571-4237 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:53`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'a' })).toBe(true);`

# ERROR 50877ea571-4238 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:54`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(def.filter!({ subject: 'unknown' })).toBe(false);`

# ERROR 50877ea571-4239 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:58`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const token = Role.make<Record<string, any>>('org.dxos.test.role.r');`

# ERROR 50877ea571-4240 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:59`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const filter: Filter<Record<string, any>> = { bindings: [{ role: token.role, guard: () => true }] };`

# ERROR 50877ea571-4241 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:65`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const token = Role.make<Record<string, any>>('org.dxos.test.role.article');`

# ERROR 50877ea571-4242 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:72`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const token = Role.make<Record<string, any>>('org.dxos.test.role.article');`

# ERROR 50877ea571-4243 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:36`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const isFilter = (value: unknown): value is Filter<any> =>`

# ERROR 50877ea571-4244 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:69`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type Props<T extends Record<string, any> = Record<string, unknown>> = {`

# ERROR 50877ea571-4245 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:74`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `fallback?: FC<{ error: Error; data?: any }>;`

# ERROR 50877ea571-4246 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:89`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type TypedProps<TToken extends Role.Role<any>> = {`

# ERROR 50877ea571-4247 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:90`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `fallback?: FC<{ error: Error; data?: any }>;`

# ERROR 50877ea571-4248 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:101`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `[K in keyof Record<string, any>]: K extends 'fallback' | 'placeholder' | 'id' | 'type' | 'data' | 'limit'`

# ERROR 50877ea571-4249 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:103`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `: any;`

# ERROR 50877ea571-4250 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:111`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type CoreProps<T extends Record<string, any> = Record<string, unknown>> = PropsWithChildren<{`

# ERROR 50877ea571-4251 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:134`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ComponentProps<T extends Record<string, any> = Record<string, any>> = {`

# ERROR 50877ea571-4252 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:146`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `Record<string, any>;`

# ERROR 50877ea571-4253 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:151`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ComponentFunction<T extends Record<string, any> = Record<string, any>> = (`

# ERROR 50877ea571-4254 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:165`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ReactDefinition<T extends Record<string, any> = any> = Readonly<{`

# ERROR 50877ea571-4255 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:170`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `component: ComponentType<any>;`

# ERROR 50877ea571-4256 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:172`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `props?: (props: ComponentProps<T>) => Record<string, any>;`

# ERROR 50877ea571-4257 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:182`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type WebComponentDefinition<T extends Record<string, any> = any> = Readonly<{`

# ERROR 50877ea571-4258 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:198`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type Definition<T extends Record<string, any> = any> = ReactDefinition<T> | WebComponentDefinition<T>;`

# ERROR 50877ea571-4259 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:204`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `T extends Record<string, any> = any,`

# ERROR 50877ea571-4260 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:205`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `P extends Record<string, any> = ComponentProps<T>,`

# ERROR 50877ea571-4261 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:237`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type TypedWebComponentDefinition<T extends Record<string, any> = any> = Readonly<{`

# ERROR 50877ea571-4262 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:244`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const expandBindings = <T extends Record<string, any>>(`

# ERROR 50877ea571-4263 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:277`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function create<T extends Record<string, any> = any, P extends Record<string, any> = ComponentProps<T>>(`

# ERROR 50877ea571-4264 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:280`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function create<T extends Record<string, any> = any, P extends Record<string, any> = ComponentProps<T>>(`

# ERROR 50877ea571-4265 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:291`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function createWeb<T extends Record<string, any> = any>(`

# ERROR 50877ea571-4266 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:294`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function createWeb<T extends Record<string, any> = any>(`

# ERROR 50877ea571-4267 no-casts `packages/sdk/app-framework/src/ui/hooks/useApp.tsx:212`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(globalThis as any).composer?.profiler?.dump();`

# ERROR 50877ea571-4268 no-casts `packages/sdk/app-framework/src/ui/hooks/useProcessManagerRuntime.ts:38`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const useSpaceCallback = <const Tags extends readonly Context.Tag<any, any>[], T>(`

# ERROR 50877ea571-4269 no-casts `packages/sdk/app-framework/src/ui/hooks/useProcessManagerRuntime.ts:41`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `fn: () => Effect.Effect<T, any, Context.Tag.Identifier<Tags[number]> | Capabilities.ProcessManagerRuntimeServices>,`

# ERROR 50877ea571-4270 no-casts `packages/sdk/app-framework/src/ui/hooks/useProcessManagerRuntime.ts:53`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return runtime.runPromise(fn().pipe(Effect.provide(layer)) as Effect.Effect<T, any, any>);`

# ERROR 50877ea571-4271 no-casts `packages/sdk/app-framework/src/ui/hooks/useProcessManagerRuntime.ts:62`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const useSpaceService = <T extends Context.Tag<any, any>>(`

# ERROR 50877ea571-4272 no-casts `packages/sdk/app-framework/src/ui/hooks/useProcessManagerRuntime.ts:73`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return runtime.runPromiseExit(effect as Effect.Effect<Context.Tag.Service<T>, any, any>);`

# ERROR 50877ea571-4273 no-casts `packages/sdk/app-graph/src/atoms.ts:9`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const observableFamily = Atom.family((observable: MulticastObservable<any>) => {`

# ERROR 50877ea571-4274 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:489`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const parent = (child?: Node.NodeArg<any>): Node.NodeArg<any> => ({`

# ERROR 50877ea571-4275 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:496`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const inlineChild = (overrides?: Partial<Node.NodeArg<any>>): Node.NodeArg<any> => ({`

# ERROR 50877ea571-4276 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:506`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const nodesAtom = Atom.make<Node.NodeArg<any>[]>([]);`

# ERROR 50877ea571-4277 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:614`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const withInlineAction = (actionId = 'delete'): Node.NodeArg<any> => ({`

# ERROR 50877ea571-4278 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:647`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const withFilterChild = (): Node.NodeArg<any> => ({`

# ERROR 50877ea571-4279 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:675`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const filterChild: Node.NodeArg<any> = {`

# ERROR 50877ea571-4280 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:697`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const makeMultiParent = (childIds: string[]): Node.NodeArg<any> => ({`

# ERROR 50877ea571-4281 no-casts `packages/sdk/app-graph/src/graph-builder.ts:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ConnectorExtension = (node: Atom.Atom<Option.Option<Node.Node>>) => Atom.Atom<Node.NodeArg<any>[]>;`

# ERROR 50877ea571-4282 no-casts `packages/sdk/app-graph/src/graph-builder.ts:50`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `) => Atom.Atom<Omit<Node.NodeArg<Node.ActionData<any>>, 'type' | 'nodes' | 'edges'>[]>;`

# ERROR 50877ea571-4283 no-casts `packages/sdk/app-graph/src/graph-builder.ts:65`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ResolverExtension = (id: string) => Atom.Atom<Node.NodeArg<any> | null>;`

# ERROR 50877ea571-4284 no-casts `packages/sdk/app-graph/src/graph-builder.ts:78`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `connector?: (node: Atom.Atom<Option.Option<Node.Node>>) => Atom.Atom<Node.NodeArg<any>[]>;`

# ERROR 50877ea571-4285 no-casts `packages/sdk/app-graph/src/graph-builder.ts:201`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type BuilderNode<TData = any> = Node.Node<TData, { urlSegment?: string } & Record<string, any>>;`

# ERROR 50877ea571-4286 no-casts `packages/sdk/app-graph/src/graph-builder.ts:209`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `node: Node.NodeArg<any>,`

# ERROR 50877ea571-4287 no-casts `packages/sdk/app-graph/src/graph-builder.ts:212`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): Node.NodeArg<any> => {`

# ERROR 50877ea571-4288 no-casts `packages/sdk/app-graph/src/graph-builder.ts:286`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `nodes: Node.NodeArg<any>[];`

# ERROR 50877ea571-4289 no-casts `packages/sdk/app-graph/src/graph-builder.ts:295`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `readonly _connectorPreviousArgs = new Map<string, Node.NodeArg<any>[]>();`

# ERROR 50877ea571-4290 no-casts `packages/sdk/app-graph/src/graph-builder.ts:320`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `_constructNode: (node: Node.NodeArg<any>) => Option.Option<Node.Node>;`

# ERROR 50877ea571-4291 no-casts `packages/sdk/app-graph/src/graph-builder.ts:340`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `_constructNode: (node: Node.NodeArg<any>) => Option.Option<Node.Node>;`

# ERROR 50877ea571-4292 no-casts `packages/sdk/app-graph/src/graph-builder.ts:361`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _recordProvenance(node: Node.NodeArg<any>, extensionId: string): void {`

# ERROR 50877ea571-4293 no-casts `packages/sdk/app-graph/src/graph-builder.ts:369`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _applyConnectorUpdate(key: string, nodes: Node.NodeArg<any>[], previous: string[]): void {`

# ERROR 50877ea571-4294 no-casts `packages/sdk/app-graph/src/graph-builder.ts:423`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private readonly _resolvers = Atom.family<string, Atom.Atom<Option.Option<Node.NodeArg<any>>>>((id) => {`

# ERROR 50877ea571-4295 no-casts `packages/sdk/app-graph/src/graph-builder.ts:438`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private readonly _connectors = Atom.family<string, Atom.Atom<{ extensionId: string; node: Node.NodeArg<any> }[]>>(`

# ERROR 50877ea571-4296 no-casts `packages/sdk/app-graph/src/graph-builder.ts:459`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const entries: { extensionId: string; node: Node.NodeArg<any> }[] = [];`

# ERROR 50877ea571-4297 no-casts `packages/sdk/app-graph/src/graph-builder.ts:653`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return removeExtensionImpl(builder, id!);`

# ERROR 50877ea571-4298 no-casts `packages/sdk/app-graph/src/graph-builder.ts:929`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `) => Effect.Effect<Omit<Node.NodeArg<Node.ActionData<any>, any>, 'type'>[], never, R>;`

# ERROR 50877ea571-4299 no-casts `packages/sdk/app-graph/src/graph-builder.ts:936`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `resolver?: (id: string, get: Atom.Context) => Effect.Effect<Node.NodeArg<any, any> | null, never, R>;`

# ERROR 50877ea571-4300 no-casts `packages/sdk/app-graph/src/graph-builder.ts:937`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `connector?: (matched: TMatched, get: Atom.Context) => Effect.Effect<Node.NodeArg<any, any>[], never, R>;`

# ERROR 50877ea571-4301 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1038`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `factory: (data: TData, get: Atom.Context) => Node.NodeArg<any>[],`

# ERROR 50877ea571-4302 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1059`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `factory: (data: TData, get: Atom.Context) => Effect.Effect<Node.NodeArg<any>[], never, R>,`

# ERROR 50877ea571-4303 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1084`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `) => Effect.Effect<Omit<Node.NodeArg<Node.ActionData<any>>, 'type'>[], never, R>;`

# ERROR 50877ea571-4304 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1089`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `connector?: (object: Type.InstanceType<T>, get: Atom.Context) => Effect.Effect<Node.NodeArg<any>[], never, R>;`

# ERROR 50877ea571-4305 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1125`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(nodes: Node.NodeArg<any>[]): Node.NodeArg<any>[] =>`

# ERROR 50877ea571-4306 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1142`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const collectAllInlineIds = (nodes: Node.NodeArg<any>[]): string[] =>`

# ERROR 50877ea571-4307 no-casts `packages/sdk/app-graph/src/graph.test.ts:455`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `let json: any;`

# ERROR 50877ea571-4308 no-casts `packages/sdk/app-graph/src/graph.ts:92`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `json(id?: string): Atom.Atom<any>;`

# ERROR 50877ea571-4309 no-casts `packages/sdk/app-graph/src/graph.ts:210`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `readonly _json = Atom.family<string, Atom.Atom<any>>((id) => {`

# ERROR 50877ea571-4310 no-casts `packages/sdk/app-graph/src/graph.ts:212`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const toJSON = (node: Node.Node, seen: string[] = []): any => {`

# ERROR 50877ea571-4311 no-casts `packages/sdk/app-graph/src/graph.ts:214`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const obj: Record<string, any> = {`

# ERROR 50877ea571-4312 no-casts `packages/sdk/app-graph/src/graph.ts:257`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `json(id = Node.RootId): Atom.Atom<any> {`

# ERROR 50877ea571-4313 no-casts `packages/sdk/app-graph/src/graph.ts:282`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `_constructNode(node: Node.NodeArg<any>): Option.Option<Node.Node> {`

# ERROR 50877ea571-4314 no-casts `packages/sdk/app-graph/src/graph.ts:297`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `return graph as unknown as GraphImpl;`

# ERROR 50877ea571-4315 no-casts `packages/sdk/app-graph/src/graph.ts:311`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const jsonImpl = (graph: BaseGraph, id = Node.RootId): Atom.Atom<any> => {`

# ERROR 50877ea571-4316 no-casts `packages/sdk/app-graph/src/graph.ts:380`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return getNodeImpl(graph, id!);`

# ERROR 50877ea571-4317 no-casts `packages/sdk/app-graph/src/graph.ts:410`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return getNodeOrThrowImpl(graph, id!);`

# ERROR 50877ea571-4318 no-casts `packages/sdk/app-graph/src/graph.ts:479`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return getActionsImpl(graph, id!);`

# ERROR 50877ea571-4319 no-casts `packages/sdk/app-graph/src/graph.ts:504`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return getEdgesImpl(graph, id!);`

# ERROR 50877ea571-4320 no-casts `packages/sdk/app-graph/src/graph.ts:606`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return getPathImpl(graph, params!);`

# ERROR 50877ea571-4321 no-casts `packages/sdk/app-graph/src/graph.ts:700`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return initializeImpl(graph, id!);`

# ERROR 50877ea571-4322 no-casts `packages/sdk/app-graph/src/graph.ts:863`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return sortEdgesImpl(graph, id, relation, order!);`

# ERROR 50877ea571-4323 no-casts `packages/sdk/app-graph/src/graph.ts:870`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const addNodesImpl = <T extends WritableGraph>(graph: T, nodes: Node.NodeArg<any, Record<string, any>>[]): T => {`

# ERROR 50877ea571-4324 no-casts `packages/sdk/app-graph/src/graph.ts:880`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function addNodes<T extends WritableGraph>(graph: T, nodes: Node.NodeArg<any, Record<string, any>>[]): T;`

# ERROR 50877ea571-4325 no-casts `packages/sdk/app-graph/src/graph.ts:881`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function addNodes(nodes: Node.NodeArg<any, Record<string, any>>[]): <T extends WritableGraph>(graph: T) => T;`

# ERROR 50877ea571-4326 no-casts `packages/sdk/app-graph/src/graph.ts:883`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `graphOrNodes: T | Node.NodeArg<any, Record<string, any>>[],`

# ERROR 50877ea571-4327 no-casts `packages/sdk/app-graph/src/graph.ts:884`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `nodes?: Node.NodeArg<any, Record<string, any>>[],`

# ERROR 50877ea571-4328 no-casts `packages/sdk/app-graph/src/graph.ts:888`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const nodes = graphOrNodes as Node.NodeArg<any, Record<string, any>>[];`

# ERROR 50877ea571-4329 no-casts `packages/sdk/app-graph/src/graph.ts:900`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const addNodeImpl = <T extends WritableGraph>(graph: T, nodeArg: Node.NodeArg<any, Record<string, any>>): T => {`

# ERROR 50877ea571-4330 no-casts `packages/sdk/app-graph/src/graph.ts:912`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} = nodeArg as Node.NodeArg<any> & {`

# ERROR 50877ea571-4331 no-casts `packages/sdk/app-graph/src/graph.ts:996`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function addNode<T extends WritableGraph>(graph: T, nodeArg: Node.NodeArg<any, Record<string, any>>): T;`

# ERROR 50877ea571-4332 no-casts `packages/sdk/app-graph/src/graph.ts:997`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function addNode(nodeArg: Node.NodeArg<any, Record<string, any>>): <T extends WritableGraph>(graph: T) => T;`

# ERROR 50877ea571-4333 no-casts `packages/sdk/app-graph/src/graph.ts:999`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `graphOrNodeArg: T | Node.NodeArg<any, Record<string, any>>,`

# ERROR 50877ea571-4334 no-casts `packages/sdk/app-graph/src/graph.ts:1000`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `nodeArg?: Node.NodeArg<any, Record<string, any>>,`

# ERROR 50877ea571-4335 no-casts `packages/sdk/app-graph/src/graph.ts:1004`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const nodeArg = graphOrNodeArg as Node.NodeArg<any, Record<string, any>>;`

# ERROR 50877ea571-4336 no-casts `packages/sdk/app-graph/src/node-matcher.ts:182`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(...matchers: NodeMatcher<any>[]): NodeMatcher<any>;`

# ERROR 50877ea571-4337 no-casts `packages/sdk/app-graph/src/node-matcher.ts:184`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(...matchers: NodeMatcher<any>[]): NodeMatcher<any> =>`

# ERROR 50877ea571-4338 no-casts `packages/sdk/app-graph/src/node-matcher.ts:186`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `let first: Option.Option<any> = Option.none();`

# ERROR 50877ea571-4339 no-casts `packages/sdk/app-graph/src/node-matcher.ts:220`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(...matchers: NodeMatcher<any>[]): NodeMatcher<any>;`

# ERROR 50877ea571-4340 no-casts `packages/sdk/app-graph/src/node-matcher.ts:222`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(...matchers: NodeMatcher<any>[]): NodeMatcher<any> =>`

# ERROR 50877ea571-4341 no-casts `packages/sdk/app-graph/src/node-matcher.ts:317`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(matcher: NodeMatcher<any>): NodeMatcher<unknown> =>`

# ERROR 50877ea571-4342 no-casts `packages/sdk/app-graph/src/node.ts:35`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type Node<TData = any, TProperties extends Record<string, any> = Record<string, any>> = Readonly<{`

# ERROR 50877ea571-4343 no-casts `packages/sdk/app-graph/src/node.ts:67`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type NodeFilter<TData = any, TProperties extends Record<string, any> = Record<string, any>> = (`

# ERROR 50877ea571-4344 no-casts `packages/sdk/app-graph/src/node.ts:68`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `node: Node<unknown, Record<string, any>>,`

# ERROR 50877ea571-4345 no-casts `packages/sdk/app-graph/src/node.ts:91`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type NodeArg<TData, TProperties extends Record<string, any> = Record<string, any>> = MakeOptional<`

# ERROR 50877ea571-4346 no-casts `packages/sdk/app-graph/src/node.ts:100`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `actions?: NodeArg<ActionData<any> | typeof actionGroupSymbol>[];`

# ERROR 50877ea571-4347 no-casts `packages/sdk/app-graph/src/node.ts:127`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ActionData<R = never> = (params?: InvokeProps) => Effect.Effect<any, Error, R>;`

# ERROR 50877ea571-4348 no-casts `packages/sdk/app-graph/src/node.ts:133`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ActionContext = Context.Context<any>;`

# ERROR 50877ea571-4349 no-casts `packages/sdk/app-graph/src/node.ts:135`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type Action<TProperties extends Record<string, any> = Record<string, any>> = Readonly<`

# ERROR 50877ea571-4350 no-casts `packages/sdk/app-graph/src/node.ts:148`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ActionGroup<TProperties extends Record<string, any> = Record<string, any>> = Readonly<`

# ERROR 50877ea571-4351 no-casts `packages/sdk/app-graph/src/node.ts:179`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const make = <TData = any, TProperties extends Record<string, any> = Record<string, any>>(`

# ERROR 50877ea571-4352 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:59`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `.filter((space: any) => get(CreateAtom.fromObservable(space.state)) === SpaceState.SPACE_READY)`

# ERROR 50877ea571-4353 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:107`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any).graph = graph;`

# ERROR 50877ea571-4354 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:225`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `<Select.Root value={action?.toString()} onValueChange={(action) => setAction(action as unknown as Action)}>`

# ERROR 50877ea571-4355 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:19`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Node: FC<{ data?: any; root?: boolean }> = ({ data, root }) => {`

# ERROR 50877ea571-4356 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const KeyValue: FC<{ label: string; data?: any; className?: string }> = ({ label, data, className }) => {`

# ERROR 50877ea571-4357 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:62`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const Scalar: FC<{ value: any }> = ({ value }) => {`

# ERROR 50877ea571-4358 no-casts `packages/sdk/app-graph/src/util.ts:58`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const nodeArgsUnchanged = (prev: Node.NodeArg<any>[], next: Node.NodeArg<any>[]): boolean => {`

# ERROR 50877ea571-4359 no-casts `packages/sdk/app-solid/src/useCapabilities.test.tsx:24`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `} as unknown as PluginManager.PluginManager;`

# ERROR 50877ea571-4360 no-casts `packages/sdk/app-solid/src/useCapabilities.test.tsx:65`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `} as unknown as PluginManager.PluginManager;`

# ERROR 50877ea571-4361 no-casts `packages/sdk/app-solid/src/usePluginManager.test.tsx:20`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `} as unknown as PluginManager.PluginManager;`

# ERROR 50877ea571-4362 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:164`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `atom: Atom.Writable<any>;`

# ERROR 50877ea571-4363 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:259`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `getText: (obj: any, anchor: string) => string | undefined;`

# ERROR 50877ea571-4364 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:270`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `getTextContent: (object: any) => Promise<string | undefined>;`

# ERROR 50877ea571-4365 no-casts `packages/sdk/app-toolkit/src/app-framework/progress-trace-sink.test.ts:26`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `}) as unknown as Trace.Message;`

# ERROR 50877ea571-4366 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function createFactory<TArgs extends any[], T>(`

# ERROR 50877ea571-4367 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:47`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export function createFactory<TArgs extends any[], T>(`

# ERROR 50877ea571-4368 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:55`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return cache.get(key)!;`

# ERROR 50877ea571-4369 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:65`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(key: string, ns: string, extra?: Record<string, any>): [string, Record<string, any>] => [key, { ns, ...extra }],`

# ERROR 50877ea571-4370 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:66`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(key: string, ns: string, extra?: Record<string, any>) => '${key}\0${ns}${extra ? '\0${JSON.stringify(extra)}' : ''}',`

# ERROR 50877ea571-4371 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:240`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const refValue = (object as any)?.[iconFromRefProp];`

# ERROR 50877ea571-4372 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:332`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const makeDeckCompanion = <TData = any>({`

# ERROR 50877ea571-4373 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:545`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `actions: Node.NodeArg<Node.ActionData<any>>[];`

# ERROR 50877ea571-4374 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:569`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): Node.NodeArg<Node.ActionData<any>> => ({`

# ERROR 50877ea571-4375 no-casts `packages/sdk/app-toolkit/src/app-graph/TypeSection.ts:88`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `createObject?: (space: Space) => Effect.Effect<any, any, any>;`

# ERROR 50877ea571-4376 no-casts `packages/sdk/app-toolkit/src/app-graph/TypeSection.ts:113`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const filter = Filter.type(type as any) as Filter.Any;`

# ERROR 50877ea571-4377 no-casts `packages/sdk/app-toolkit/src/app/NativePasskey.ts:59`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const platform = ((navigator as any).userAgentData?.platform || navigator.platform)?.toLowerCase();`

# ERROR 50877ea571-4378 no-casts `packages/sdk/app-toolkit/src/echo/AppSpace.test.ts:57`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const makeSpace = (tags: string[]): Space => ({ tags, properties: {} }) as unknown as Space;`

# ERROR 50877ea571-4379 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:127`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const filter = SurfaceInternals.makeFilter(AppSurface.Article, (data: any) => data.custom === true);`

# ERROR 50877ea571-4380 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:182`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `SurfaceInternals.makeFilter(AppSurface.Article, (data: any) => data.extra === true),`

# ERROR 50877ea571-4381 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:310`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(definition.filter!({ subject: objectA, attendableId: 'id' }, 'org.dxos.role.article')).toBe(true);`

# ERROR 50877ea571-4382 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:325`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(definition.filter!({ subject: objectA, attendableId: 'id' }, 'org.dxos.role.article')).toBe(true);`

# ERROR 50877ea571-4383 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:326`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(definition.filter!({ subject: objectA }, 'org.dxos.role.article')).toBe(false);`

# ERROR 50877ea571-4384 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:327`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(definition.filter!({ subject: objectA, attendableId: 'id' }, 'org.dxos.role.section')).toBe(true);`

# ERROR 50877ea571-4385 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:328`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(definition.filter!({ subject: objectA }, 'org.dxos.role.section')).toBe(false);`

# ERROR 50877ea571-4386 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:23`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `type UnionToIntersection<U> = (U extends any ? (arg: U) => void : never) extends (arg: infer I) => void ? I : never;`

# ERROR 50877ea571-4387 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:37`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const oneOf = <TFilters extends ReadonlyArray<Surface.Filter<any>>>(`

# ERROR 50877ea571-4388 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:49`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const allOf = <TFilters extends ReadonlyArray<Surface.Filter<any>>>(`

# ERROR 50877ea571-4389 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:92`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<{ subject?: any }>, S extends Type.AnyEntity>(`

# ERROR 50877ea571-4390 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:97`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<{ subject?: any }>, S extends Type.AnyEntity[]>(`

# ERROR 50877ea571-4391 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:103`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `token: Role.Role<any>,`

# ERROR 50877ea571-4392 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:105`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `predicate?: (data: any) => boolean,`

# ERROR 50877ea571-4393 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:106`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): Surface.Filter<any> => {`

# ERROR 50877ea571-4394 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:135`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const literal = <TToken extends Role.Role<{ subject?: any }>, T extends string | null>(`

# ERROR 50877ea571-4395 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:160`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<{ subject?: any }>, T>(`

# ERROR 50877ea571-4396 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:164`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<{ subject?: any }>>(`

# ERROR 50877ea571-4397 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:168`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} = (token: Role.Role<any>, check: (value: unknown) => boolean): Surface.Filter<any> => {`

# ERROR 50877ea571-4398 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:182`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const snapshot = <TToken extends Role.Role<{ subject?: any }>, S extends Type.Obj<any>>(`

# ERROR 50877ea571-4399 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:203`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<any>>(token: TToken): Surface.Filter<{ companionTo: Obj.Any }>;`

# ERROR 50877ea571-4400 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:204`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<any>, S extends Type.AnyEntity>(`

# ERROR 50877ea571-4401 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:208`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<any>, T extends string>(token: TToken, value: T): Surface.Filter<{ companionTo: T }>;`

# ERROR 50877ea571-4402 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:209`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `<TToken extends Role.Role<any>, T>(`

# ERROR 50877ea571-4403 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:214`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `token: Role.Role<any>,`

# ERROR 50877ea571-4404 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:216`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): Surface.Filter<any> => {`

# ERROR 50877ea571-4405 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:246`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Article: Role.Role<ArticleData<any>> = Role.make('org.dxos.role.article');`

# ERROR 50877ea571-4406 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:252`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `properties?: Record<string, any>; // TODO(burdon): What is this for?`

# ERROR 50877ea571-4407 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:339`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const settings = (token: Role.Role<any>, prefix?: string): Surface.Filter<SettingsData> => {`

# ERROR 50877ea571-4408 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:356`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Section: Role.Role<SectionData<any>> = Role.make('org.dxos.role.section');`

# ERROR 50877ea571-4409 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:359`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Slide: Role.Role<SectionData<any>> = Role.make('org.dxos.role.slide');`

# ERROR 50877ea571-4410 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:362`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Tabpanel: Role.Role<ArticleData<any>> = Role.make('org.dxos.role.tabpanel');`

# ERROR 50877ea571-4411 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:372`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Related: Role.Role<{ attendableId?: string; subject: any }> = Role.make('org.dxos.role.related');`

# ERROR 50877ea571-4412 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:413`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const ObjectProperties: Role.Role<ObjectPropertiesData<any>> = Role.make('org.dxos.role.objectProperties');`

# ERROR 50877ea571-4413 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:433`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const CardContent: Role.Role<CardData<any>> = Role.make('org.dxos.role.cardContent');`

# ERROR 50877ea571-4414 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:482`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type DialogData<Component extends string = string, ComponentProps = any> = {`

# ERROR 50877ea571-4415 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:513`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const component = <ComponentProps = any, Component extends string = string>(`

# ERROR 50877ea571-4416 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:595`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const deckCompanion = (variant: string): Role.Role<{ subject?: any }> => {`

# ERROR 50877ea571-4417 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:601`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return Role.make('org.dxos.role.deckCompanion.${variant}' as any);`

# ERROR 50877ea571-4418 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:147`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(hostInvitation!.identityKey).not.to.exist;`

# ERROR 50877ea571-4419 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:148`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation?.identityKey).to.deep.eq(client1.halo.identity.get()!.identityKey);`

# ERROR 50877ea571-4420 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:149`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation?.identityKey).to.deep.eq(client2.halo.identity.get()!.identityKey);`

# ERROR 50877ea571-4421 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:238`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const guestSpace = client2.spaces.get(guestInvitation!.spaceKey!);`

# ERROR 50877ea571-4422 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:249`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `members.sort((m1, m2) => (m1.identity.identityKey.equals(client1.halo.identity.get()!.identityKey) ? -1 : 1));`

# ERROR 50877ea571-4423 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:259`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `did: client1.halo.identity.get()!.did,`

# ERROR 50877ea571-4424 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:260`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `identityKey: client1.halo.identity.get()!.identityKey,`

# ERROR 50877ea571-4425 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:273`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `did: client2.halo.identity.get()!.did,`

# ERROR 50877ea571-4426 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:274`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `identityKey: client2.halo.identity.get()!.identityKey,`

# ERROR 50877ea571-4427 no-casts `packages/sdk/client-e2e/src/client.test.ts:201`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = client2.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4428 no-casts `packages/sdk/client-e2e/src/client.test.ts:226`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `.poll(() => thread1.messages[0].target!.blocks[0].content?.target?.content, { timeout: 1_000 })`

# ERROR 50877ea571-4429 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:154`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const contact = contacts.find((c) => c.identityKey.equals(client.halo.identity.get()!.identityKey));`

# ERROR 50877ea571-4430 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:156`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return contact!;`

# ERROR 50877ea571-4431 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:165`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return client.spaces.get().find((s) => s.key.equals(spaceKey))!;`

# ERROR 50877ea571-4432 no-casts `packages/sdk/client-e2e/src/halo-credentials.node.test.ts:52`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `ids: credentials.map(({ id }) => id!),`

# ERROR 50877ea571-4433 no-casts `packages/sdk/client-e2e/src/halo-credentials.node.test.ts:57`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(presentation.proofs![0].nonce).to.deep.equal(nonce);`

# ERROR 50877ea571-4434 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:26`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');`

# ERROR 50877ea571-4435 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:40`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');`

# ERROR 50877ea571-4436 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:52`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');`

# ERROR 50877ea571-4437 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:55`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user-updated');`

# ERROR 50877ea571-4438 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:177`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client2.halo.identity.get()!.profile?.displayName).to.equal('test-user-updated');`

# ERROR 50877ea571-4439 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:90`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `object: (result as any).toJSON(),`

# ERROR 50877ea571-4440 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:97`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `objects.every((object) => indexResults.some((entry) => objectContains(entry.result!, object))) &&`

# ERROR 50877ea571-4441 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:98`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `indexResults.every((entry) => objects.some((object) => objectContains(entry.result!, object)))`

# ERROR 50877ea571-4442 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:156`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = client.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4443 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:187`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = client.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4444 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:318`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const objectContains = (container: any, content: any): boolean => {`

# ERROR 50877ea571-4445 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:322`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `isEqual: Object.entries(content).every(([key, value]) => key === 'id' || isEqual((container as any)[key], value)),`

# ERROR 50877ea571-4446 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:329`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(typeof value === 'object' && typeof (container as any)[key] === 'object'`

# ERROR 50877ea571-4447 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:330`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `? objectContains((container as any)[key], value)`

# ERROR 50877ea571-4448 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:331`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `: isEqual((container as any)[key], value)),`

# ERROR 50877ea571-4449 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:62`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation!.target).to.eq(hostInvitation!.target);`

# WARN 50877ea571-4450 no-sleep-in-test `packages/sdk/client-e2e/src/invitations.test.ts:63`

After confirming `hostInvitation.state`/`guestInvitation.state` are `SUCCESS`, `await sleep(20)` waits an arbitrary period before asserting on downstream derived state (`dataSpaceManager.spaces`). Prefer `waitForCondition`/`expect.poll` on that derived state instead of a fixed delay.

# ERROR 50877ea571-4451 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:65`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `switch (hostInvitation!.kind) {`

# ERROR 50877ea571-4452 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:67`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation!.spaceKey).to.exist;`

# ERROR 50877ea571-4453 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:68`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(hostInvitation!.spaceKey).to.deep.eq(guestInvitation!.spaceKey);`

# ERROR 50877ea571-4454 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:70`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(host.dataSpaceManager!.spaces.get(hostInvitation!.spaceKey!)).to.exist;`

# ERROR 50877ea571-4455 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:71`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guest.dataSpaceManager!.spaces.get(guestInvitation!.spaceKey!)).to.exist;`

# ERROR 50877ea571-4456 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:75`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(hostInvitation!.identityKey).not.to.exist;`

# ERROR 50877ea571-4457 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:76`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation!.identityKey).to.deep.eq(host.identityManager.identity!.identityKey);`

# ERROR 50877ea571-4458 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:77`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(guestInvitation!.identityKey).to.deep.eq(guest.identityManager.identity!.identityKey);`

# ERROR 50877ea571-4459 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:80`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect.poll(() => host.identityManager.identity!.authorizedDeviceKeys.size).toEqual(2);`

# ERROR 50877ea571-4460 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:81`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect.poll(() => guest.identityManager.identity!.authorizedDeviceKeys.size).toEqual(2);`

# ERROR 50877ea571-4461 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:320`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `space = await host.dataSpaceManager!.createSpace(Context.default());`

# ERROR 50877ea571-4462 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:636`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `() => [(host.services as any).host.context, (guest.services as any).host.context],`

# ERROR 50877ea571-4463 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:666`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `() => [(host.services as any).host.context, (guest.services as any).host.context],`

# ERROR 50877ea571-4464 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:704`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const service = makeServicesFromRpc(rpc, Runtime.defaultRuntime).InvitationsService!;`

# ERROR 50877ea571-4465 no-casts `packages/sdk/client-e2e/src/lazy-space-loading.test.ts:85`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const client2State = members.find((m) => m.identity.identityKey.equals(client2.halo.identity.get()!.identityKey));`

# ERROR 50877ea571-4466 no-casts `packages/sdk/client-e2e/src/lazy-space-loading.test.ts:150`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return client.spaces.get().find((s) => s.id === space.id)!;`

# ERROR 50877ea571-4467 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:131`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const echoSpace = host.spaces.get().find((s) => s.key.equals(space.key))!;`

# ERROR 50877ea571-4468 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:133`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `memberKey: target.halo.identity.get()!.identityKey,`

# ERROR 50877ea571-4469 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:142`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return space.members.get().find((m) => m.identity.identityKey.equals(client.halo.identity.get()!.identityKey));`

# ERROR 50877ea571-4470 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:174`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return client.spaces.get().find((s) => s.key.equals(space.key))!;`

# ERROR 50877ea571-4471 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:48`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await waitForSpace(client2, guestInvitation!.spaceKey!, { ready: true });`

# ERROR 50877ea571-4472 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:76`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(fred, space.key!, { ready: true });`

# ERROR 50877ea571-4473 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:105`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(fred, space.key!, { ready: true });`

# ERROR 50877ea571-4474 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:111`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(charlie, space.key!, { ready: true });`

# ERROR 50877ea571-4475 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:126`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(genesisCredential!.subject.assertion.membershipPolicy).toEqual(MembershipPolicy.LOCKED);`

# ERROR 50877ea571-4476 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:134`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const invitationStream = peer.services.services.InvitationsService!.queryInvitations();`

# ERROR 50877ea571-4477 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:34`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await waitForSpace(client2, guestInvitation!.spaceKey!, { ready: true });`

# ERROR 50877ea571-4478 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:65`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(fred, space.key!, { ready: true });`

# ERROR 50877ea571-4479 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:94`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(fred, space.key!, { ready: true });`

# ERROR 50877ea571-4480 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:100`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSpace(charlie, space.key!, { ready: true });`

# ERROR 50877ea571-4481 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:115`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(genesisCredential!.subject.assertion.membershipPolicy).toEqual(MembershipPolicy.LOCKED);`

# ERROR 50877ea571-4482 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:123`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const invitationStream = peer.services.services.InvitationsService!.queryInvitations();`

# ERROR 50877ea571-4483 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:128`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = await waitForSpace(client2, guestInvitation!.spaceKey!, {`

# ERROR 50877ea571-4484 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:173`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const dataSpace1 = services1.host!.context.dataSpaceManager?.spaces.get(space1.key);`

# ERROR 50877ea571-4485 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:174`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const feedKey = dataSpace1!.inner.dataFeedKey;`

# ERROR 50877ea571-4486 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:175`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const feed1 = services1.host!.context.feedStore.getFeed(feedKey!)!;`

# ERROR 50877ea571-4487 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:198`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const feed2 = services2.host!.context.feedStore.getFeed(feedKey!)!;`

# ERROR 50877ea571-4488 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:265`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Obj.update(space.db.getObjectById(id)!, (obj) => {`

# ERROR 50877ea571-4489 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:309`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Obj.update(space2.db.getObjectById(obj.id)!, (obj) => {`

# ERROR 50877ea571-4490 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:352`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `expect((space2.db.getObjectById(obj.id) as any).data).to.equal('test-reactive');`

# ERROR 50877ea571-4491 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:379`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `Obj.update(hostDocument.content as any, (c: any) => {`

# ERROR 50877ea571-4492 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:386`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const reloadedSpace = host.spaces.get().find((s) => s.id === hostSpace.id)!;`

# ERROR 50877ea571-4493 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:502`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const getTypename = (obj: any) => {`

# ERROR 50877ea571-4494 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:553`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const guestRoot = guestSpace.db.getObjectById(hostRoot.id)!;`

# ERROR 50877ea571-4495 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:678`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const feedEntry = parsed.feeds.find((f: any) => f.feedObjectId === feedObj.id);`

# ERROR 50877ea571-4496 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:725`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `expect(messages.map((m: any) => m.name).sort()).toEqual(['msg-1', 'msg-2']);`

# ERROR 50877ea571-4497 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:793`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `.query(Query.select(Filter.everything()).from(Scope.feed(feedURI!)))`

# ERROR 50877ea571-4498 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:829`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `exported.feeds![0].messages.map((msg) => Obj.fromJSON(msg, { refResolver })),`

# ERROR 50877ea571-4499 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:835`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `.query(Query.select(Filter.everything()).from(Scope.feed(targetFeedURI!)))`

# ERROR 50877ea571-4500 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:910`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return space.db.getObjectById<TestSchema.DocumentType>(documentId)!.content.target!.content;`

# ERROR 50877ea571-4501 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:320`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `) as unknown as Effect.Effect<ClientServicesRpc, never, Scope.Scope>;`

# ERROR 50877ea571-4502 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:328`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const rpcRecord = rpc as unknown as Record<string, Record<string, (...args: any[]) => unknown>>;`

# ERROR 50877ea571-4503 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:348`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const rpcRecord = rpc as unknown as Record<string, Record<string, (...args: any[]) => unknown>>;`

# ERROR 50877ea571-4504 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:376`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const rpc: Record<string, Record<string, (...args: any[]) => unknown>> = {};`

# ERROR 50877ea571-4505 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:390`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `return rpc as unknown as ClientServicesRpc;`

# ERROR 50877ea571-4506 no-casts `packages/sdk/client-services/src/packlets/devtools/feeds.ts:45`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `feedMap.get(feed.key)!.owner = findFeedOwner(spaceManager, feed.key);`

# ERROR 50877ea571-4507 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:26`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4508 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:85`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `: { peerLeft: { peer: PublicKey.from(swarmEvent.peerLeft!.peer.peerKey).asUint8Array() } },`

# ERROR 50877ea571-4509 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:106`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4510 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:20`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const spaces: Space[] = [...context.spaceManager!.spaces.values()];`

# ERROR 50877ea571-4511 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:36`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `controlFeed: space.controlFeedKey!, // TODO(dmaretskyi): Those keys may be missing.`

# ERROR 50877ea571-4512 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:37`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `dataFeed: space.dataFeedKey!,`

# ERROR 50877ea571-4513 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:45`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `unsubscribe = context.dataSpaceManager!.updated.on(() => update());`

# ERROR 50877ea571-4514 no-casts `packages/sdk/client-services/src/packlets/diagnostics/browser-diagnostics-broadcast.ts:25`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `payload?: any;`

# ERROR 50877ea571-4515 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics-collector.ts:15`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `broadcastDiagnosticsRequest(): any;`

# ERROR 50877ea571-4516 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics-collector.ts:30`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): Promise<any> {`

# ERROR 50877ea571-4517 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:119`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `(await getFirstStreamValue(clientServices.DevicesService!.queryDevices(), {`

# ERROR 50877ea571-4518 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:135`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `(await getFirstStreamValue(clientServices.DevtoolsHost!.subscribeToFeeds({}), {`

# ERROR 50877ea571-4519 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:142`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const status = await getFirstStreamValue(clientServices.NetworkService!.queryStatus(), {`

# ERROR 50877ea571-4520 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:215`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `for await (const filename of (dir as any)?.keys()) {`

# ERROR 50877ea571-4521 no-casts `packages/sdk/client-services/src/packlets/identity/authenticator.ts:65`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `if (!credential.proof!.nonce || !Buffer.from(nonce).equals(credential.proof!.nonce)) {`

# ERROR 50877ea571-4522 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:91`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(peer2.identityManager.identity!.identityKey).to.deep.eq(identity1.identityKey);`

# ERROR 50877ea571-4523 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:92`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(peer2.identityManager.identity!.deviceKey).to.deep.eq(identity1.deviceKey);`

# ERROR 50877ea571-4524 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:195`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `deviceProfile!.label = 'updated profile';`

# ERROR 50877ea571-4525 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:196`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await peer.identityManager.updateDeviceProfile(deviceProfile!);`

# ERROR 50877ea571-4526 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.ts:313`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `identityKey: this._identity!.identityKey,`

# ERROR 50877ea571-4527 no-casts `packages/sdk/client-services/src/packlets/identity/identity-recovery-manager.ts:111`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4528 no-casts `packages/sdk/client-services/src/packlets/identity/identity-recovery-manager.ts:211`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4529 no-casts `packages/sdk/client-services/src/packlets/identity/identity.ts:246`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _onFeedAdded = async (feed: FeedWrapper<any>) => {`

# ERROR 50877ea571-4530 no-casts `packages/sdk/client-services/src/packlets/identity/identity.ts:247`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._edgeFeedReplicator!.addFeed(feed);`

# ERROR 50877ea571-4531 no-casts `packages/sdk/client-services/src/packlets/invitations/edge-invitation-handler.ts:166`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4532 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-guest-extenstion.ts:110`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4533 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-host-extension.ts:223`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4534 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-host-extension.ts:255`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4535 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:26`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `error(lockHolder: FlowLockHolder | null, error: any): boolean;`

# ERROR 50877ea571-4536 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:64`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `error: (lockHolder: FlowLockHolder | null, error: any): boolean => {`

# ERROR 50877ea571-4537 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:78`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const logStateUpdate = (invitation: Invitation, actor: any, newState: Invitation.State, error?: Error) => {`

# WARN 50877ea571-4538 no-sleep-in-test `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:56`

`await performAuth(guest, invitation); await sleep(15); expect(guest.ctx.disposed).to.be.true;` waits a fixed real delay hoping context disposal has completed before asserting it positively. The same `sleep(N); expect(...ctx.disposed).to.be...)` shape recurs throughout the file (e.g. lines 132, 184, 201, 223, 246, 312, 319); use `waitForCondition`/`expect.poll` on `ctx.disposed` instead of guessing a sufficient delay.

# ERROR 50877ea571-4539 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:146`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `codeInput.wake(invitation.authCode!);`

# ERROR 50877ea571-4540 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:238`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `authCodeInput2.wake(invitation.authCode!);`

# ERROR 50877ea571-4541 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:317`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const newHost = await createPeer(invitation.spaceKey!);`

# ERROR 50877ea571-4542 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:327`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `authCodeInput2.wake(invitation.authCode!);`

# ERROR 50877ea571-4543 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:357`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any;`

# ERROR 50877ea571-4544 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:164`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4545 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:193`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4546 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:431`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4547 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:28`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await peer.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4548 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:29`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(peer.dataSpaceManager!.spaces.has(space.key)).to.be.true;`

# ERROR 50877ea571-4549 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:37`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await peer.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4550 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:38`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(peer.dataSpaceManager!.spaces.has(space.key)).to.be.true;`

# ERROR 50877ea571-4551 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:47`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await host.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4552 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:53`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = host.dataSpaceManager!.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4553 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:54`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = guest.dataSpaceManager!.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4554 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:71`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await host.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4555 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:77`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = host.dataSpaceManager!.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4556 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:78`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = guest.dataSpaceManager!.spaces.get(spaceKey)!;`

# ERROR 50877ea571-4557 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:100`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await host.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4558 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:137`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = host.dataSpaceManager!.spaces.get(invitation1!.spaceKey!)!;`

# ERROR 50877ea571-4559 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:138`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = guest.dataSpaceManager!.spaces.get(invitation2!.spaceKey!)!;`

# ERROR 50877ea571-4560 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:158`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await host.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4561 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:184`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await host.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4562 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.ts:116`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `? invitation.guestKeypair!.publicKey`

# ERROR 50877ea571-4563 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.ts:123`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return credential.credential.credential.id!;`

# ERROR 50877ea571-4564 no-casts `packages/sdk/client-services/src/packlets/locks/browser.ts:56`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _onMessage(event: MessageEvent<any>): void {`

# ERROR 50877ea571-4565 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:215`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4566 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:227`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4567 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:269`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4568 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:133`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4569 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:148`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4570 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:333`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4571 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:75`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `public pipeline!: Pipeline;`

# ERROR 50877ea571-4572 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:76`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `public feed!: FeedWrapper<FeedMessage>;`

# ERROR 50877ea571-4573 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:78`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `public writePromise: Promise<any> = Promise.resolve();`

# ERROR 50877ea571-4574 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:115`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const promise = this.pipeline.writer!.write(message);`

# ERROR 50877ea571-4575 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:144`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const agent = real.agents.get(this.agent)!;`

# ERROR 50877ea571-4576 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:161`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const targets: any = {};`

# ERROR 50877ea571-4577 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:180`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const tf: Timeframe = real.agents.values().next().value!.pipeline.state.timeframe;`

# ERROR 50877ea571-4578 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:223`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const agent = real.agents.get(this.agent)!;`

# ERROR 50877ea571-4579 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.test.ts:49`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await pipeline.writer!.write({});`

# WARN 50877ea571-4580 no-sleep-in-test `packages/sdk/client-services/src/packlets/pipeline/pipeline.test.ts:131`

`await pipeline.start(); await sleep(1000); await pipeline.pause(); await pipeline.setCursor(...); await pipeline.unpause();` relies on a fixed 1s window for a specific number of blocks to have been consumed before pausing, to exercise a "cursor change mid-stream" scenario. Whether the test actually hits that scenario is timing-dependent; gate the pause on an observed progress count (e.g. via `processedEvent`) instead of a blind sleep.

# ERROR 50877ea571-4581 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:227`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private readonly _downloads = new ComplexMap<FeedWrapper<FeedMessage>, any>((value) => PublicKey.hash(value.key));`

# ERROR 50877ea571-4582 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:254`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return this._feedSetIterator!.feeds;`

# ERROR 50877ea571-4583 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:289`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._feedSetIterator!.open();`

# ERROR 50877ea571-4584 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:410`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `handle = feed.download({ start: seq + 1, linear: true }, (err: any, data: any) => {`

# ERROR 50877ea571-4585 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:136`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space = await proxy.SpacesService!.createSpace({ membershipPolicy: MembershipPolicy.INVITE });`

# ERROR 50877ea571-4586 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:150`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const statuses = await PbStream.consumeData(proxy.SystemService!.queryStatus({}));`

# ERROR 50877ea571-4587 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:162`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect(PbStream.consumeData(proxy.SystemService!.queryStatus({}))).rejects.toThrow('stream failed');`

# ERROR 50877ea571-4588 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:172`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const error = await proxy.SystemService!.getConfig().then(`

# ERROR 50877ea571-4589 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:182`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect(proxy.SystemService!.getConfig()).rejects.toThrow(`

# ERROR 50877ea571-4590 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:203`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const request = proxy.SystemService!.getConfig();`

# ERROR 50877ea571-4591 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:219`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect(proxy.SystemService!.getConfig(undefined, { timeout: 100 })).rejects.toThrow(TimeoutError);`

# ERROR 50877ea571-4592 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:55`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `statusChanged: new Event<any>(),`

# ERROR 50877ea571-4593 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:73`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const decoded = cborDecode(routerMessage.payload!.value) as ProtocolMessage;`

# ERROR 50877ea571-4594 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:130`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `edgeClient: edgeClient as any,`

# ERROR 50877ea571-4595 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.ts:157`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `try: () => cborXdecode(msg.payload!.value),`

# ERROR 50877ea571-4596 no-casts `packages/sdk/client-services/src/packlets/services/platform.ts:8`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if ((process as any).browser) {`

# ERROR 50877ea571-4597 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:23`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await device1.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4598 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:24`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await device2.dataSpaceManager!.waitUntilSpaceReady(space1!.key);`

# ERROR 50877ea571-4599 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:25`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = await device2.dataSpaceManager!.spaces.get(space1.key);`

# ERROR 50877ea571-4600 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:26`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space2!.inner.controlPipeline.state.waitUntilTimeframe(space1.inner.controlPipeline.state.timeframe);`

# ERROR 50877ea571-4601 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:39`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space1 = await identity2.dataSpaceManager!.createSpace(new Context());`

# ERROR 50877ea571-4602 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:48`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await device2.dataSpaceManager!.waitUntilSpaceReady(space1!.key);`

# ERROR 50877ea571-4603 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:49`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = await device2.dataSpaceManager!.spaces.get(space1.key);`

# ERROR 50877ea571-4604 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:50`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space2!.inner.controlPipeline.state.waitUntilTimeframe(space1.inner.controlPipeline.state.timeframe);`

# ERROR 50877ea571-4605 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:59`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await services.IdentityService!.createIdentity({});`

# ERROR 50877ea571-4606 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:60`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { spaceKey } = await services.SpacesService!.createSpace({ membershipPolicy: MembershipPolicy.INVITE });`

# ERROR 50877ea571-4607 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:62`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const stream = services.SpacesService!.queryCredentials({ spaceKey });`

# ERROR 50877ea571-4608 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:79`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await services.IdentityService!.createIdentity({});`

# ERROR 50877ea571-4609 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:83`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `issuer: host.context.identityManager.identity!.deviceKey,`

# ERROR 50877ea571-4610 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:88`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `services.IdentityService!.queryIdentity()!.subscribe(({ identity }) => {`

# ERROR 50877ea571-4611 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:99`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const credentials = services.SpacesService!.queryCredentials({ spaceKey: await haloSpace.wait() });`

# ERROR 50877ea571-4612 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:117`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await services.IdentityService!.createIdentity({});`

# ERROR 50877ea571-4613 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:121`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `issuer: host.context.identityManager.identity!.deviceKey,`

# ERROR 50877ea571-4614 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:126`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const presentation = await services.IdentityService!.signPresentation({`

# ERROR 50877ea571-4615 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:212`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `#feedStore?: FeedStore<any>;`

# ERROR 50877ea571-4616 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:276`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return await createDiagnostics(services, this, this.#config!);`

# ERROR 50877ea571-4617 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:346`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `get feedStore(): FeedStore<any> {`

# ERROR 50877ea571-4618 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:440`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `createIceProvider(this.#config!.get('runtime.services.iceProviders')!),`

# ERROR 50877ea571-4619 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:567`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this.#keyring!,`

# ERROR 50877ea571-4620 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:568`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `() => this.#identityManager!.identity ?? failUndefined(),`

# ERROR 50877ea571-4621 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:748`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._setNetworkIdentity({ deviceCredential: params.authorizedDeviceCredential!, identity });`

# ERROR 50877ea571-4622 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:771`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#dataSpaceManager!.open(ctx);`

# ERROR 50877ea571-4623 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:774`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#edgeAgentManager!.open(ctx);`

# ERROR 50877ea571-4624 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:781`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this.#dataSpaceManager!,`

# ERROR 50877ea571-4625 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:782`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this.#signingContextProvider!(),`

# ERROR 50877ea571-4626 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:783`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this.#keyring!,`

# ERROR 50877ea571-4627 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:789`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this.#deviceSpaceSync!.setIdentity(identity);`

# ERROR 50877ea571-4628 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:790`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#deviceSpaceSync!.open?.(ctx);`

# ERROR 50877ea571-4629 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:843`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await RuntimeProvider.runPromise(this.#runtime)(this.#storageMigrate!);`

# ERROR 50877ea571-4630 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:852`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#identityManager!.open(ctx);`

# ERROR 50877ea571-4631 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:853`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `log('identityManager opened', { hasIdentity: !!this.#identityManager!.identity });`

# ERROR 50877ea571-4632 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:856`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._setNetworkIdentity({ identity: this.#identityManager!.identity });`

# ERROR 50877ea571-4633 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:862`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#signalManager!.open(ctx);`

# ERROR 50877ea571-4634 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:865`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#networkManager!.open();`

# ERROR 50877ea571-4635 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:870`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#echoHost!.addReplicator(ctx, this.#meshReplicator);`

# ERROR 50877ea571-4636 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:874`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#echoHost!.addReplicator(ctx, this.#echoEdgeReplicator);`

# ERROR 50877ea571-4637 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:878`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#metadataStore!.load();`

# ERROR 50877ea571-4638 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:881`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#spaceManager!.open();`

# ERROR 50877ea571-4639 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:883`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `if (this.#identityManager!.identity) {`

# ERROR 50877ea571-4640 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:885`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this.#identityManager!.identity.joinNetwork(ctx);`

# ERROR 50877ea571-4641 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:897`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const loadedInvitations = await this.#invitationsManager!.loadPersistentInvitations(ctx);`

# ERROR 50877ea571-4642 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.test.ts:44`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const cyclic: any = new Error('boom');`

# ERROR 50877ea571-4643 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:30`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `for (let cur: any = err, depth = 0; cur != null && depth < 5; cur = cur.cause, depth++) {`

# ERROR 50877ea571-4644 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:253`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `write(offset: number, data: Buffer, cb: Callback<any>): void {`

# ERROR 50877ea571-4645 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:291`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `del(offset: number, size: number, cb: Callback<any>): void {`

# ERROR 50877ea571-4646 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:394`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return files.get(fullPath)!;`

# ERROR 50877ea571-4647 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:73`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const parent = (obj as any)[ATTR_PARENT];`

# ERROR 50877ea571-4648 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:78`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const relationSource = (obj as any)[ATTR_RELATION_SOURCE];`

# ERROR 50877ea571-4649 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:79`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const relationTarget = (obj as any)[ATTR_RELATION_TARGET];`

# ERROR 50877ea571-4650 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:95`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if ((obj as any)[ATTR_DELETED]) {`

# ERROR 50877ea571-4651 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:99`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const meta = (obj as any)[ATTR_META];`

# ERROR 50877ea571-4652 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-writer.ts:159`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `[ATTR_TYPE]: (structure.system?.type?.['/'] ?? '') as any,`

# ERROR 50877ea571-4653 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-writer.ts:211`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const namespace = (obj as any).namespace === 'trace' ? 'trace' : 'data';`

# ERROR 50877ea571-4654 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive-reader.ts:76`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `feedBlocksByFeedId.get(feedId)!.set(chunkIndex, blocks);`

# ERROR 50877ea571-4655 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive-reader.ts:85`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `allBlocks.push(...blockChunks.get(chunkIndex)!);`

# ERROR 50877ea571-4656 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:373`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const directory = buildDatabaseDirectoryFromObjects(objects as any);`

# ERROR 50877ea571-4657 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:375`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const structure = directory.objects![id];`

# ERROR 50877ea571-4658 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:421`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any;`

# ERROR 50877ea571-4659 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:435`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any;`

# ERROR 50877ea571-4660 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:449`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `expect((obj['@meta'] as any).annotations).toEqual({`

# ERROR 50877ea571-4661 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:464`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const directory = buildDatabaseDirectoryFromObjects(objects as any);`

# ERROR 50877ea571-4662 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:465`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const structure = directory.objects![id];`

# ERROR 50877ea571-4663 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:484`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const directory = buildDatabaseDirectoryFromObjects(objects as any);`

# ERROR 50877ea571-4664 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:485`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const structure = directory.objects![id];`

# ERROR 50877ea571-4665 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:504`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const directory = buildDatabaseDirectoryFromObjects(objects as any);`

# ERROR 50877ea571-4666 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:505`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const structure = directory.objects![id];`

# ERROR 50877ea571-4667 no-casts `packages/sdk/client-services/src/packlets/space/admission-discovery-extension.ts:49`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4668 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.test.ts:90`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await controlPipeline.pipeline.writer!.write({`

# ERROR 50877ea571-4669 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.test.ts:114`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await controlPipeline.pipeline.writer!.write({`

# ERROR 50877ea571-4670 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:88`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4671 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:167`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4672 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:216`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4673 no-casts `packages/sdk/client-services/src/packlets/space/space-manager.ts:174`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4674 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:32`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4675 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:60`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4676 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:93`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `space2.controlFeedKey!,`

# ERROR 50877ea571-4677 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:94`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `space2.dataFeedKey!,`

# ERROR 50877ea571-4678 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:109`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space1.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4679 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:112`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space2.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4680 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:138`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space1.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4681 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:147`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space2.controlPipeline.state!.waitUntilTimeframe(space2.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4682 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:167`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4683 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:178`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);`

# ERROR 50877ea571-4684 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:366`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const newDoc = await this._echoHost.createDoc(data as any as DatabaseDirectory, {`

# ERROR 50877ea571-4685 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:371`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `if (newDoc.doc().access !== undefined && newDoc.doc().access!.spaceKey !== spaceKey.toHex()) {`

# ERROR 50877ea571-4686 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:373`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `doc.access!.spaceKey = spaceKey.toHex();`

# ERROR 50877ea571-4687 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:658`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4688 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:300`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async postMessage(channel: string, message: any): Promise<void> {`

# ERROR 50877ea571-4689 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:491`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._metadataStore.setWritableFeedKeys(this.key, this.inner.controlFeedKey!, this.inner.dataFeedKey!);`

# ERROR 50877ea571-4690 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:658`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _onFeedAdded = async (feed: FeedWrapper<any>) => {`

# ERROR 50877ea571-4691 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:659`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._edgeFeedReplicator!.addFeed(feed);`

# ERROR 50877ea571-4692 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.test.ts:110`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `sendSpy.mockImplementationOnce(async (_ctx: any, request: any) => {`

# ERROR 50877ea571-4693 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.test.ts:200`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `messageHandler: async (message: any) => {`

# ERROR 50877ea571-4694 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:36`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private readonly _feeds = new ComplexMap<PublicKey, FeedWrapper<any>>(PublicKey.hash);`

# ERROR 50877ea571-4695 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:75`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const payload = decodeCbor(message.payload!.value) as ProtocolMessage;`

# ERROR 50877ea571-4696 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:120`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async addFeed(feed: FeedWrapper<any>): Promise<void> {`

# ERROR 50877ea571-4697 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:133`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private async _replicateFeed(ctx: Context, feed: FeedWrapper<any>): Promise<void> {`

# ERROR 50877ea571-4698 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:140`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `Event.wrap(feed.core as any, 'append').on(ctx, async () => {`

# ERROR 50877ea571-4699 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:198`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._sendMessage(this._connectionCtx!, {`

# ERROR 50877ea571-4700 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:206`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._pushBlocks(this._connectionCtx!, feed, message.length, feed.length);`

# ERROR 50877ea571-4701 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:233`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private async _pushBlocks(ctx: Context, feed: FeedWrapper<any>, from: number, to: number): Promise<void> {`

# ERROR 50877ea571-4702 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:259`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private async _integrateBlocks(feed: FeedWrapper<any>, blocks: FeedBlock[]): Promise<void> {`

# ERROR 50877ea571-4703 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:277`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private async _pushBlocksIfNeeded(feed: FeedWrapper<any>): Promise<void> {`

# ERROR 50877ea571-4704 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:285`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const remoteLength = this._remoteLength.get(feed.key)!;`

# ERROR 50877ea571-4705 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:287`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await this._pushBlocks(this._connectionCtx!, feed, remoteLength, feed.length);`

# ERROR 50877ea571-4706 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:294`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `onError: async (err: any) => {`

# ERROR 50877ea571-4707 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:91`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private _activeEdgePollingIntervalHandle: any | undefined = undefined;`

# ERROR 50877ea571-4708 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:173`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const allNotarized = Promise.all(credentials.map((credential) => this._waitUntilProcessed(credential.id!)));`

# ERROR 50877ea571-4709 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:219`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `credentials: credentials.filter((credential) => !this._processedCredentials.has(credential.id!)),`

# ERROR 50877ea571-4710 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:224`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4711 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:256`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4712 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:326`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4713 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:395`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const handleEdgeError = (error: any) => {`

# ERROR 50877ea571-4714 no-casts `packages/sdk/client-services/src/packlets/testing/invitation-utils.ts:99`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `invariant(hostInvitation.swarmKey!.equals(guestInvitation.swarmKey!));`

# ERROR 50877ea571-4715 no-casts `packages/sdk/client-services/src/packlets/testing/test-builder.ts:131`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `feedStore?: FeedStore<any>;`

# ERROR 50877ea571-4716 no-casts `packages/sdk/client-services/src/packlets/testing/test-builder.ts:237`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return new SpaceInvitationProtocol(this.dataSpaceManager, this.identity!, this.keyring, invitation.spaceKey!);`

# ERROR 50877ea571-4717 no-casts `packages/sdk/client-services/src/packlets/worker/worker-runtime.ts:105`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const signalMetadataTags: any = { runtime: 'worker-runtime' };`

# ERROR 50877ea571-4718 no-casts `packages/sdk/client-services/src/packlets/worker/worker-runtime.ts:221`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4719 no-casts `packages/sdk/client-services/src/packlets/worker/worker-session.ts:145`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4720 no-casts `packages/sdk/client/src/client/client.ts:297`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async diagnostics(options: JsonKeyOptions = {}): Promise<any> {`

# ERROR 50877ea571-4721 no-casts `packages/sdk/client/src/client/client.ts:306`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async repair(): Promise<any> {`

# ERROR 50877ea571-4722 no-casts `packages/sdk/client/src/client/client.ts:308`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const repairSummary: any = {};`

# ERROR 50877ea571-4723 no-casts `packages/sdk/client/src/client/client.ts:315`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(space as any)._data.pipeline.currentEpoch?.subject.assertion.automergeRoot.slice('automerge:'.length),`

# ERROR 50877ea571-4724 no-casts `packages/sdk/client/src/client/client.ts:322`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `for await (const filename of (dir as any)?.keys()) {`

# ERROR 50877ea571-4725 no-casts `packages/sdk/client/src/client/client.ts:476`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const edgeUrl = this._config!.get('runtime.services.edge.url');`

# ERROR 50877ea571-4726 no-casts `packages/sdk/client/src/devtools/debug-port-controller.ts:138`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `dxos: (globalThis as any).__DXOS__,`

# ERROR 50877ea571-4727 no-casts `packages/sdk/client/src/devtools/debug-port-controller.ts:139`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `composer: (globalThis as any).composer,`

# ERROR 50877ea571-4728 no-casts `packages/sdk/client/src/devtools/devtools.ts:47`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `get?(id: string): Promise<any>;`

# ERROR 50877ea571-4729 no-casts `packages/sdk/client/src/devtools/devtools.ts:79`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `joinTables: any;`

# ERROR 50877ea571-4730 no-casts `packages/sdk/client/src/devtools/devtools.ts:302`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `((globalThis as any).__DXOS__ as DevtoolsHook) = hook;`

# ERROR 50877ea571-4731 no-casts `packages/sdk/client/src/devtools/devtools.ts:320`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `delete (globalThis as any).__DXOS__;`

# ERROR 50877ea571-4732 no-casts `packages/sdk/client/src/devtools/devtools.ts:321`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `delete (globalThis as any).dxos;`

# ERROR 50877ea571-4733 no-casts `packages/sdk/client/src/echo/space-list.ts:41`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `private _ctx!: Context;`

# ERROR 50877ea571-4734 no-casts `packages/sdk/client/src/echo/space-list.ts:181`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await spaceProxy!._processSpaceUpdate(this._ctx, space);`

# ERROR 50877ea571-4735 no-casts `packages/sdk/client/src/echo/space-proxy.ts:83`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const getSpace = (object?: any): Space | undefined => {`

# ERROR 50877ea571-4736 no-casts `packages/sdk/client/src/echo/space-proxy.ts:140`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `private readonly _db!: DatabaseImpl;`

# ERROR 50877ea571-4737 no-casts `packages/sdk/client/src/echo/space-proxy.ts:141`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `private readonly _internal!: SpaceInternal;`

# ERROR 50877ea571-4738 no-casts `packages/sdk/client/src/echo/space-proxy.ts:406`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._membersUpdate.emit(space.members!);`

# ERROR 50877ea571-4739 no-casts `packages/sdk/client/src/echo/space-proxy.ts:552`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async postMessage(channel: string, message: any): Promise<void> {`

# ERROR 50877ea571-4740 no-casts `packages/sdk/client/src/edge/client-edge-api.ts:58`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return new QueryResultImpl(queryContext as any, normalizedQuery) as QueryResult.QueryResult<Query.Type<Q>>;`

# ERROR 50877ea571-4741 no-casts `packages/sdk/client/src/edge/client-edge-api.ts:77`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export class RemoteEdgeQueryContext<T extends Entity.Unknown = Entity.Unknown> implements QueryContext<any, T> {`

# ERROR 50877ea571-4742 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:20`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const edgeClient = { putBlob } as unknown as EdgeHttpClient;`

# ERROR 50877ea571-4743 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:34`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const edgeClient = { getBlob } as unknown as EdgeHttpClient;`

# ERROR 50877ea571-4744 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:46`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const edgeClient = { getBlob } as unknown as EdgeHttpClient;`

# ERROR 50877ea571-4745 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:57`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const edgeClient = { hasBlob } as unknown as EdgeHttpClient;`

# ERROR 50877ea571-4746 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:69`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const edgeClient = { getBlobUrl } as unknown as EdgeHttpClient;`

# ERROR 50877ea571-4747 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:161`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._serviceProvider.rpc.SpacesService.queryCredentials({ spaceKey: identity.spaceKey! }),`

# ERROR 50877ea571-4748 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:316`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `if (ids && !ids.some((id) => id.equals(credential.id!))) {`

# ERROR 50877ea571-4749 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:335`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const invitation = this._invitationProxy!.share(options);`

# ERROR 50877ea571-4750 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:353`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return this._invitationProxy!.join(invitation, deviceProfileWithDefaults);`

# ERROR 50877ea571-4751 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:368`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `spaceKey: identity.spaceKey!,`

# ERROR 50877ea571-4752 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:383`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const credentialsToPresent = credentials.filter((credential) => ids.some((id) => id.equals(credential.id!)));`

# ERROR 50877ea571-4753 no-casts `packages/sdk/client/src/invitations/invitations-proxy.ts:51`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `private _ctx!: Context;`

# ERROR 50877ea571-4754 no-casts `packages/sdk/client/src/invitations/invitations-proxy.ts:246`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const invitationValue = (invitation as any)[key];`

# ERROR 50877ea571-4755 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `init(authToken?: any): boolean;`

# ERROR 50877ea571-4756 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:90`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `init(authToken?: any): boolean {`

# ERROR 50877ea571-4757 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:108`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `_checkAuthorization(authToken?: any): boolean {`

# ERROR 50877ea571-4758 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:125`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `_checkAuthCookie(authToken: any): boolean {`

# ERROR 50877ea571-4759 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:193`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { deviceKey } = this._halo.device!;`

# ERROR 50877ea571-4760 no-casts `packages/sdk/client/src/services/dedicated/dedicated-worker-client-services.test.ts:72`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const space2 = client2.spaces.get(space.id)!;`

# ERROR 50877ea571-4761 no-casts `packages/sdk/client/src/services/dedicated/dedicated-worker-client-services.test.ts:174`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(client2.halo.identity.get()!.profile?.displayName).toEqual(updatedDisplayName);`

# ERROR 50877ea571-4762 no-casts `packages/sdk/client/src/services/fake-agent-hosting-provider.ts:30`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `public init(authToken: any): boolean {`

# ERROR 50877ea571-4763 no-casts `packages/sdk/client/src/services/local-client-services.ts:136`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `signalMetadataTags: any = {`

# ERROR 50877ea571-4764 no-casts `packages/sdk/client/src/services/local-client-services.ts:155`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if ((globalThis as any).__args) {`

# ERROR 50877ea571-4765 no-casts `packages/sdk/client/src/services/local-client-services.ts:265`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `makeInProcessClientServicesRpc(() => this._host!.services).pipe(Scope.extend(this._serviceScope)),`

# ERROR 50877ea571-4766 no-casts `packages/sdk/client/src/services/shell-manager.ts:83`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `iframe!.setAttribute('style', shellStyles);`

# ERROR 50877ea571-4767 no-casts `packages/sdk/client/src/services/shell-manager.ts:84`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `iframe!.setAttribute('name', 'dxos-shell');`

# ERROR 50877ea571-4768 no-casts `packages/sdk/client/src/services/shell-manager.ts:85`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `iframe!.setAttribute('data-testid', 'dxos-shell');`

# ERROR 50877ea571-4769 no-casts `packages/sdk/client/src/services/shell-manager.ts:87`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `iframe!.style.display = display === ShellDisplay.NONE ? 'none' : '';`

# ERROR 50877ea571-4770 no-casts `packages/sdk/client/src/services/shell-manager.ts:89`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `iframe!.blur();`

# ERROR 50877ea571-4771 no-casts `packages/sdk/client/src/testing/test-builder.ts:173`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._ctx.onDispose(() => this._workerFactory!.close());`

# ERROR 50877ea571-4772 no-casts `packages/sdk/client/src/testing/test-builder.ts:177`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `createWorker: () => this._workerFactory!.make(),`

# ERROR 50877ea571-4773 no-casts `packages/sdk/client/src/testing/test-builder.ts:178`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `createCoordinator: () => this._coordinator!,`

# ERROR 50877ea571-4774 no-casts `packages/sdk/client/src/testing/test-builder.ts:199`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `createIceProvider(this.config.get('runtime.services.iceProviders')!),`

# ERROR 50877ea571-4775 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:70`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await clientA.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 10_000 });`

# WARN 50877ea571-4776 no-sleep-in-test `packages/sdk/client/test/e2e/edge-recovery.test.ts:72`

`await sleep(15_000);` ("waiting for HALO feed sync", also `sleep(1_000)` at line 89 and `sleep(5_000)` at line 113 "waiting for spaces to appear") waits a fixed real-time duration hoping asynchronous replication has completed, with no completion signal awaited. The same file's own `waitForSync` helper (below) shows the preferred alternative — poll actual sync state until a real condition is met (with a timeout) rather than sleeping a guessed duration.

# ERROR 50877ea571-4777 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:75`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { recoveryCode } = await clientA.services.services.IdentityService!.createRecoveryCredential({});`

# ERROR 50877ea571-4778 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:108`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await clientB.halo.recoverIdentity({ recoveryCode: recoveryCode! });`

# ERROR 50877ea571-4779 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:126`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `recoveredSpace!.waitUntilReady(),`

# ERROR 50877ea571-4780 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:131`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `log.info('clientB space ready', { spaceId: recoveredSpace!.id });`

# ERROR 50877ea571-4781 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:134`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await waitForSync(recoveredSpace!.db);`

# ERROR 50877ea571-4782 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:137`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const objects = await recoveredSpace!.db.query(Filter.type(TestSchema.Expando)).run();`

# ERROR 50877ea571-4783 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:140`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `objects.map((obj: any) => ({ name: obj.name, counter: obj.counter })),`

# ERROR 50877ea571-4784 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:144`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `expect(objects.some((obj: any) => obj.name === 'test-object-1' && obj.counter === 42)).toBe(true);`

# ERROR 50877ea571-4785 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:145`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `expect(objects.some((obj: any) => obj.name === 'test-object-2' && obj.counter === 99)).toBe(true);`

# ERROR 50877ea571-4786 no-casts `packages/sdk/client/test/e2e/sync.test.ts:56`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { status } = (await Stream.first(client.services.services.EdgeAgentService!.queryEdgeStatus())) ?? {};`

# ERROR 50877ea571-4787 no-casts `packages/sdk/client/test/e2e/sync.test.ts:85`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `space = client.spaces.get()[0]!;`

# ERROR 50877ea571-4788 no-casts `packages/sdk/config/src/config.test.ts:117`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any,`

# ERROR 50877ea571-4789 no-casts `packages/sdk/config/src/config.test.ts:148`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any,`

# ERROR 50877ea571-4790 no-casts `packages/sdk/config/src/config.test.ts:149`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `defaults as any,`

# ERROR 50877ea571-4791 no-casts `packages/sdk/config/src/config.ts:31`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const mapFromKeyValues = (spec: MappingSpec, values: Record<string, any>) => {`

# ERROR 50877ea571-4792 no-casts `packages/sdk/config/src/config.ts:76`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const mapToKeyValues = (spec: MappingSpec, values: any) => {`

# ERROR 50877ea571-4793 no-casts `packages/sdk/config/src/config.ts:77`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const config: Record<string, any> = {};`

# ERROR 50877ea571-4794 no-casts `packages/sdk/config/src/config.ts:120`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `private readonly _config: any;`

# ERROR 50877ea571-4795 no-casts `packages/sdk/config/src/config.ts:154`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `find<T = any>(path: string, test: object): T | undefined {`

# ERROR 50877ea571-4796 no-casts `packages/sdk/config/src/loaders/index.ts:19`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const maybeLoadFile = (file: string): any => {`

# ERROR 50877ea571-4797 no-casts `packages/sdk/config/src/loaders/index.ts:22`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4798 no-casts `packages/sdk/config/src/plugin/definitions.ts:41`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `content = parse(readFileSync(value, 'utf-8')) as any;`

# ERROR 50877ea571-4799 no-casts `packages/sdk/config/src/plugin/definitions.ts:69`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4800 no-casts `packages/sdk/migrations/src/migration-builder.ts:69`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `migrate: (objectStructure: EntityStructure) => MaybePromise<{ type: Type.AnyEntity; props: any }>,`

# ERROR 50877ea571-4801 no-casts `packages/sdk/migrations/src/migration-builder.ts:88`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `type: EncodedReference.fromURI(getSchemaURI(schema)!),`

# ERROR 50877ea571-4802 no-casts `packages/sdk/migrations/src/migration-builder.ts:102`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._addHandleToFlushList(newHandle.documentId!);`

# ERROR 50877ea571-4803 no-casts `packages/sdk/migrations/src/migration-builder.ts:105`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async addObject(type: Type.AnyEntity, props: any): Promise<string> {`

# ERROR 50877ea571-4804 no-casts `packages/sdk/migrations/src/migration-builder.ts:137`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const materialized = toJS(oldHandle.doc()!) as DatabaseDirectory;`

# ERROR 50877ea571-4805 no-casts `packages/sdk/migrations/src/migration-builder.ts:144`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._addHandleToFlushList(newHandle.documentId!);`

# ERROR 50877ea571-4806 no-casts `packages/sdk/migrations/src/migration-builder.ts:162`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._addHandleToFlushList(this._newRoot.documentId!);`

# ERROR 50877ea571-4807 no-casts `packages/sdk/migrations/src/migration-builder.ts:212`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._addHandleToFlushList(this._newRoot.documentId!);`

# ERROR 50877ea571-4808 no-casts `packages/sdk/migrations/src/migration-builder.ts:222`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `props: any;`

# ERROR 50877ea571-4809 no-casts `packages/sdk/migrations/src/migration-builder.ts:230`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `core.setType(EncodedReference.fromURI(getSchemaURI(schema)!));`

# ERROR 50877ea571-4810 no-casts `packages/sdk/migrations/src/migration-builder.ts:239`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._newLinks[core.id] = newHandle.url!;`

# ERROR 50877ea571-4811 no-casts `packages/sdk/migrations/src/migration-builder.ts:240`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `this._addHandleToFlushList(newHandle.documentId!);`

# ERROR 50877ea571-4812 no-casts `packages/sdk/migrations/src/migrations.test.ts:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const objects = await space.db.query(Filter.props<any>({ namespace: 'test' })).run();`

# ERROR 50877ea571-4813 no-casts `packages/sdk/migrations/src/migrations.test.ts:41`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const objects = await space.db.query(Filter.props<any>({ namespace: 'test' })).run();`

# ERROR 50877ea571-4814 no-casts `packages/sdk/observability/src/extensions/otel/extension.ts:253`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if (typeof (globalThis as any).ServiceWorkerGlobalScope !== 'undefined') {`

# ERROR 50877ea571-4815 no-casts `packages/sdk/observability/src/extensions/otel/extension.ts:256`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if (typeof (globalThis as any).SharedWorkerGlobalScope !== 'undefined') {`

# ERROR 50877ea571-4816 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:58`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `gauge(name: string, value: number, tags?: any): void {`

# ERROR 50877ea571-4817 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:64`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `increment(name: string, value?: number, tags?: any): void {`

# ERROR 50877ea571-4818 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:70`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `distribution(name: string, value: number, tags?: any): void {`

# ERROR 50877ea571-4819 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:87`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return Object.entries(data.tags).reduce<{ [key: string]: any }>((obj, [key, value]) => {`

# ERROR 50877ea571-4820 no-casts `packages/sdk/observability/src/providers/client-observability.ts:31`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `clientServices.IdentityService!.queryIdentity().subscribe((idqr) => {`

# ERROR 50877ea571-4821 no-casts `packages/sdk/observability/src/providers/client-observability.ts:41`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `clientServices.DevicesService!.queryDevices().subscribe((dqr) => {`

# ERROR 50877ea571-4822 no-casts `packages/sdk/observability/src/providers/client-observability.ts:106`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `clientServices.NetworkService!.queryStatus().subscribe((networkStatus) => {`

# ERROR 50877ea571-4823 no-casts `packages/sdk/observability/src/providers/client-observability.ts:123`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const platform = yield* Effect.promise(() => clientServices.SystemService!.getPlatform());`

# ERROR 50877ea571-4824 no-casts `packages/sdk/observability/src/providers/client-observability.ts:138`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const memory = (window.performance as any).memory;`

# ERROR 50877ea571-4825 no-casts `packages/sdk/observability/test/e2e/tracing-invitation.test.ts:119`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await host.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 15_000 });`

# ERROR 50877ea571-4826 no-casts `packages/sdk/react-client/src/client/ClientProvider.test.tsx:81`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const wrapper = ({ children }: any) => <ClientProvider client={client}>{children}</ClientProvider>;`

# ERROR 50877ea571-4827 no-casts `packages/sdk/react-client/src/echo/useSpaces.test.tsx:43`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { result } = renderHook(() => useSpace(space!.id), { wrapper });`

# ERROR 50877ea571-4828 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:30`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `await client.services.services.EdgeAgentService.createAgent(null as any, { timeout: 10_000 });`

# ERROR 50877ea571-4829 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:63`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const recoveryKey = PublicKey.from(new Uint8Array((credential as any).response.getPublicKey()));`

# ERROR 50877ea571-4830 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:64`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const algorithm = (credential as any).response.getPublicKeyAlgorithm() === -7 ? 'ES256' : 'ED25519';`

# ERROR 50877ea571-4831 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:92`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const lookupKey = PublicKey.from(new Uint8Array((credential as any).response.userHandle));`

# ERROR 50877ea571-4832 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:98`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `signature: Buffer.from((credential as any).response.signature),`

# ERROR 50877ea571-4833 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:99`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `clientDataJson: Buffer.from((credential as any).response.clientDataJSON),`

# ERROR 50877ea571-4834 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:100`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `authenticatorData: Buffer.from((credential as any).response.authenticatorData),`

# ERROR 50877ea571-4835 no-casts `packages/sdk/react-client/src/testing/ClientRepeater.stories.tsx:26`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const JsonPanel = ({ value }: { value: any }) => <JsonHighlighter data={value} />;`

# ERROR 50877ea571-4836 no-casts `packages/sdk/react-client/src/testing/util.tsx:34`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return ({ children }: any) => <ClientContext.Provider value={{ client }}>{children}</ClientContext.Provider>;`

# ERROR 50877ea571-4837 no-casts `packages/sdk/react-client/src/testing/util.tsx:38`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return ({ children }: any) => <ClientProvider client={client}>{children}</ClientProvider>;`

# ERROR 50877ea571-4838 no-casts `packages/sdk/schema/src/annotations/factory.ts:24`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type FactoryFn = (values: any) => unknown;`

# ERROR 50877ea571-4839 no-casts `packages/sdk/schema/src/annotations/view.ts:77`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return yield* Effect.promise(() => holder.load!() as Promise<View.View>);`

# ERROR 50877ea571-4840 no-casts `packages/sdk/schema/src/experimental/json-schema.test.ts:118`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const typeString = String((schema as any).type ?? '');`

# ERROR 50877ea571-4841 no-casts `packages/sdk/schema/src/graph/graph.ts:35`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const target = (object as any)[prop.name]?.target;`

# ERROR 50877ea571-4842 no-casts `packages/sdk/schema/src/projection/projection.test.ts:253`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const initialSchemaProps = { ...mutable.jsonSchema.properties! };`

# ERROR 50877ea571-4843 no-casts `packages/sdk/schema/src/projection/projection.test.ts:259`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(mutable.jsonSchema.properties!.email).to.be.undefined;`

# ERROR 50877ea571-4844 no-casts `packages/sdk/schema/src/projection/projection.test.ts:260`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(mutable.jsonSchema.properties!.name).to.deep.equal(initialSchemaProps.name);`

# ERROR 50877ea571-4845 no-casts `packages/sdk/schema/src/projection/projection.test.ts:712`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const emailId = projectionModel.getFields().find((f) => f.path === 'email')!.id;`

# ERROR 50877ea571-4846 no-casts `packages/sdk/schema/src/projection/projection.test.ts:805`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `draft.jsonSchema.properties!.status = { type: 'string' };`

# ERROR 50877ea571-4847 no-casts `packages/sdk/schema/src/projection/projection.test.ts:963`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const { field, props } = projection.getFieldProjection(fieldId!);`

# ERROR 50877ea571-4848 no-casts `packages/sdk/schema/src/stories/ECHO.stories.tsx:25`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const generator = random as any as ValueGenerator;`

# ERROR 50877ea571-4849 no-casts `packages/sdk/schema/src/testing/generator.ts:21`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export type ValueGenerator<T = any> = Record<string, () => T>;`

# ERROR 50877ea571-4850 no-casts `packages/sdk/schema/src/testing/generator.ts:42`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async (specs: TypeSpec[]): Promise<any[]> => {`

# ERROR 50877ea571-4851 no-casts `packages/sdk/schema/src/testing/generator.ts:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const result: any[] = [];`

# ERROR 50877ea571-4852 no-casts `packages/sdk/schema/src/testing/generator.ts:69`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data?: Record<string, any>;`

# ERROR 50877ea571-4853 no-casts `packages/sdk/schema/src/testing/generator.ts:85`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `async (specs: RelationSpec[]): Promise<any[]> => {`

# ERROR 50877ea571-4854 no-casts `packages/sdk/schema/src/testing/generator.ts:86`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const result: any[] = [];`

# ERROR 50877ea571-4855 no-casts `packages/sdk/schema/src/testing/generator.ts:117`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `type as unknown as Type.AnyObj,`

# ERROR 50877ea571-4856 no-casts `packages/sdk/schema/src/testing/generator.ts:122`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any);`

# ERROR 50877ea571-4857 no-casts `packages/sdk/schema/src/testing/generator.ts:123`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `result.push(addToDatabase(db)(Relation.make(type as any, props as any)));`

# ERROR 50877ea571-4858 no-casts `packages/sdk/schema/src/testing/generator.ts:144`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `if ((obj as any)[name] === undefined && name !== 'id') {`

# ERROR 50877ea571-4859 no-casts `packages/sdk/schema/src/testing/generator.ts:145`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(obj as any)[name] = createValue(generator, schema, property, force);`

# ERROR 50877ea571-4860 no-casts `packages/sdk/schema/src/testing/generator.ts:161`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `): any | undefined => {`

# ERROR 50877ea571-4861 no-casts `packages/sdk/schema/src/testing/generator.ts:176`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const fn = getDeep<(...args: any[]) => any>(generator, generatorName.split('.'));`

# ERROR 50877ea571-4862 no-casts `packages/sdk/schema/src/testing/generator.ts:205`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const refsToSet: Array<{ name: PropertyKey; ref: any }> = [];`

# ERROR 50877ea571-4863 no-casts `packages/sdk/schema/src/testing/generator.ts:228`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `Obj.update(obj as any, (mutableObj: any) => {`

# ERROR 50877ea571-4864 no-casts `packages/sdk/schema/src/testing/generator.ts:240`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return (data: Entity.Properties<Type.InstanceType<S>>) => Obj.make<S>(type, data as any);`

# ERROR 50877ea571-4865 no-casts `packages/sdk/schema/src/testing/generator.ts:245`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return <T extends AnyProperties>(obj: T): Obj.OfShape<T> => db.add(obj as any) as any;`

# ERROR 50877ea571-4866 no-casts `packages/sdk/schema/src/testing/generator.ts:248`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const logObject = (message: string) => (obj: any) => log.info(message, { obj });`

# ERROR 50877ea571-4867 no-casts `packages/sdk/schema/src/util/deprecated.ts:66`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const mapSchemaToFields = (schema: Schema.Schema<any, any>): SchemaFieldDescription[] => {`

# ERROR 50877ea571-4868 no-casts `packages/sdk/schema/src/util/formatting.ts:12`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `value: any;`

# ERROR 50877ea571-4869 no-casts `packages/sdk/schema/src/util/schema.ts:83`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const fields: any = Object.fromEntries(`

# ERROR 50877ea571-4870 no-casts `packages/sdk/schema/src/util/schema.ts:96`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const jsonProp = schema.jsonSchema.properties![prop.name] as Mutable<JsonSchemaType>;`

# ERROR 50877ea571-4871 no-casts `packages/sdk/schema/src/util/validate.ts:12`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const validateSchema = <T>(schema: Schema.Schema<T>, values: any): ValidationError[] | undefined => {`

# ERROR 50877ea571-4872 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.stories.tsx:23`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Default = (props: any) => {`

# ERROR 50877ea571-4873 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.tsx:36`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `member.identity.identityKey.equals(client.halo.identity.get()!.identityKey) ? -1 : 1,`

# ERROR 50877ea571-4874 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.tsx:39`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `(member) => !member.identity.identityKey.equals(client.halo.identity.get()!.identityKey),`

# ERROR 50877ea571-4875 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useEdgeAgentsHandlers.ts:74`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `await service.createAgent(null as any, { timeout: 7_500 });`

# ERROR 50877ea571-4876 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useEdgeAgentsHandlers.ts:81`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4877 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4878 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:62`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4879 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:80`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4880 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:94`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `onError?: (error: any) => void,`

# ERROR 50877ea571-4881 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:137`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(error: any) => {`

# ERROR 50877ea571-4882 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:153`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const config: StateNodeConfig<JoinMachineContext, any, InvitationEvent> = {`

# ERROR 50877ea571-4883 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:176`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `src: (context) => context[Kind.toLowerCase() as Lowercase<Kind>].invitationSubscribable!,`

# ERROR 50877ea571-4884 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:14`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const getComponentNames = (module: any): string[] =>`

# ERROR 50877ea571-4885 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:18`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const getComponents = (module: any): FC[] => {`

# ERROR 50877ea571-4886 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const Default = (props: any) => {`

# ERROR 50877ea571-4887 no-casts `packages/sdk/shell/src/panels/SpacePanel/SpacePanelProps.ts:18`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `send: (event: SingleOrArray<Event<any>>) => void;`

# ERROR 50877ea571-4888 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:35`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any)['peer${id}CreateSpaceInvitation'] = (options?: Partial<Invitation>) => {`

# ERROR 50877ea571-4889 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:126`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any)['peer${id}CreateHaloInvitation'] = (options?: Partial<Invitation>) => {`

# ERROR 50877ea571-4890 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:204`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `<IdentityListItem identity={identity} presence={networkStatus as unknown as SpaceMember.PresenceState} />`

# ERROR 50877ea571-4891 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:121`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return displayName!;`

# ERROR 50877ea571-4892 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:166`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return spaceName!;`

# ERROR 50877ea571-4893 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:295`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any)['peer${id}CreateSpaceInvitation'](options);`

# ERROR 50877ea571-4894 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:298`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any)['peer${id}CreateHaloInvitation'](options);`

# ERROR 50877ea571-4895 no-casts `packages/sdk/types/src/testing/generator.test.ts:42`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any as ValueGenerator;`

# ERROR 50877ea571-4896 no-casts `packages/sdk/types/src/testing/generator.test.ts:51`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `objects: objects.map((obj: any) => stripUndefined({ name: obj.name, employer: obj.employer?.name })),`

# ERROR 50877ea571-4897 no-casts `packages/sdk/versioning/src/branch.test.ts:43`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const activeSuggestions = (doc: any, creator: string) =>`

# ERROR 50877ea571-4898 no-casts `packages/sdk/versioning/src/branch.test.ts:45`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(branch: any) => branch.status === 'active' && branch.kind === 'suggestion' && branch.creator === creator,`

# ERROR 50877ea571-4899 no-casts `packages/sdk/worker-framework/src/Client.ts:47`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `config?: Record<string, any>;`

# ERROR 50877ea571-4900 no-casts `packages/sdk/worker-framework/src/Client.ts:97`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `readonly #config: Record<string, any> | undefined;`

# ERROR 50877ea571-4901 no-casts `packages/sdk/worker-framework/src/Client.ts:227`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4902 no-casts `packages/sdk/worker-framework/src/Client.ts:345`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4903 no-casts `packages/sdk/worker-framework/src/Client.ts:373`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4904 no-casts `packages/sdk/worker-framework/src/Client.ts:408`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4905 no-casts `packages/sdk/worker-framework/src/Client.ts:429`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `readonly #config: Record<string, any> | undefined;`

# ERROR 50877ea571-4906 no-casts `packages/sdk/worker-framework/src/Client.ts:433`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `#worker!: WorkerProtocol.WorkerOrPort;`

# ERROR 50877ea571-4907 no-casts `packages/sdk/worker-framework/src/Client.ts:434`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `#livenessLockKey!: string;`

# ERROR 50877ea571-4908 no-casts `packages/sdk/worker-framework/src/Client.ts:439`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `config: Record<string, any> | undefined,`

# ERROR 50877ea571-4909 no-casts `packages/sdk/worker-framework/src/Coordinator/coordinator-worker.test.ts:41`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `onConnect({ ports: [channel.port1] } as unknown as MessageEvent);`

# ERROR 50877ea571-4910 no-casts `packages/sdk/worker-framework/src/internal/locks.ts:22`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return AbortSignal.any(signals);`

# ERROR 50877ea571-4911 no-casts `packages/sdk/worker-framework/src/internal/locks.ts:59`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (error: any) {`

# ERROR 50877ea571-4912 no-casts `packages/sdk/worker-framework/src/Worker.ts:58`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `config: Record<string, any> | undefined;`

# ERROR 50877ea571-4913 no-casts `packages/sdk/worker-framework/src/Worker.ts:64`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const scope = self as unknown as WorkerProtocol.WorkerEndpoint & { close(): void };`

# ERROR 50877ea571-4914 no-casts `packages/sdk/worker-framework/src/Worker.ts:127`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `} catch (err: any) {`

# ERROR 50877ea571-4915 no-casts `packages/stories/stories-assistant/src/stories/Sketch.stories.tsx:110`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const records = Object.values(canvas.content ?? {}) as any[];`

# ERROR 50877ea571-4916 no-casts `packages/stories/stories-assistant/src/testing/decorators.tsx:112`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `types?: any[];`

# ERROR 50877ea571-4917 no-casts `packages/stories/stories-assistant/src/testing/decorators.tsx:407`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `}) as any;`

# ERROR 50877ea571-4918 no-casts `packages/stories/stories-assistant/src/testing/decorators.tsx:429`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `.map(([source, token]) => Obj.make(AccessToken.AccessToken, { source, token: token! }));`

# ERROR 50877ea571-4919 no-casts `packages/stories/stories-brain/src/test/model-ladder.bench.test.ts:80`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const reference = variants.find((variant) => variant.name.includes(LADDER_REFERENCE)) ?? variants.at(-1)!;`

# ERROR 50877ea571-4920 no-casts `packages/stories/stories-brain/src/test/model-ladder.bench.test.ts:124`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const referenceTags = ladder.find((entry) => entry.model === reference.name)!.runs.map((run) => run.output);`

# ERROR 50877ea571-4921 no-casts `packages/stories/stories-brain/src/testing/harness/internal/eval-report.ts:144`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const wins = pairs.filter((pair) => pair!.winner === 'candidate').length;`

# ERROR 50877ea571-4922 no-casts `packages/stories/stories-inbox/src/stories/ExtractMessageList.stories.tsx:70`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `generateObject: (options: any) =>`

# ERROR 50877ea571-4923 no-casts `packages/stories/stories-inbox/src/stories/ExtractMessageList.stories.tsx:73`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any),`

# ERROR 50877ea571-4924 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:31`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const feed = mailbox.feed!.target!;`

# ERROR 50877ea571-4925 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:61`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `expect(serialized.map((message: any) => message.properties?.subject).sort()).toEqual(['First', 'Second']);`

# ERROR 50877ea571-4926 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:68`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const next = await mailbox.feed!.tryLoad();`

# ERROR 50877ea571-4927 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:70`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(next!.id).not.toBe(original.id);`

# ERROR 50877ea571-4928 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:74`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),`

# ERROR 50877ea571-4929 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:79`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const originalIds = new Set(serialized.map((entry: any) => entry.id));`

# ERROR 50877ea571-4930 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:95`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const next = await mailbox.feed!.tryLoad();`

# ERROR 50877ea571-4931 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:96`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `expect(next!.id).not.toBe(original.id);`

# ERROR 50877ea571-4932 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:98`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),`

# ERROR 50877ea571-4933 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:131`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const next = await mailbox.feed!.tryLoad();`

# ERROR 50877ea571-4934 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:133`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),`

# ERROR 50877ea571-4935 no-casts `packages/stories/stories-inbox/src/testing/archive.ts:42`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const reconstructMessage = (json: any): Message.Message =>`

# ERROR 50877ea571-4936 no-casts `packages/stories/stories-lens/src/components.tsx:52`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const changedOnly = <T extends object>(values: T, { changed }: FormUpdateMeta<any>): Partial<T> => {`

# ERROR 50877ea571-4937 no-casts `packages/stories/stories-lens/src/components.tsx:88`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(values: object, meta: FormUpdateMeta<any>) => {`

# ERROR 50877ea571-4938 no-casts `packages/stories/stories-lens/src/components.tsx:138`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `(values: object, meta: FormUpdateMeta<any>) => {`

# ERROR 50877ea571-4939 no-casts `packages/stories/stories-lens/src/stories/ObjectLens.stories.tsx:70`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const onCreateSpace = async ({ space }: { space: { db: { add: (obj: any) => any } } }) => {`

# ERROR 50877ea571-4940 no-casts `packages/stories/stories-lens/src/stories/ObjectLens.stories.tsx:117`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `canvasElement.querySelector<T>('[data-testid="${testId}"]')!;`

# ERROR 50877ea571-4941 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:51`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `)!;`

# ERROR 50877ea571-4942 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:107`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const onCreateSpace = async ({ space }: { space: { db: { add: (obj: any) => any } } }) => {`

# ERROR 50877ea571-4943 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:152`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `canvasElement.querySelector<T>('[data-testid="${testId}"]')!;`

# ERROR 50877ea571-4944 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:171`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const strong = editor.querySelector<HTMLElement>('strong')!;`

# ERROR 50877ea571-4945 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:172`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const em = editor.querySelector<HTMLElement>('em')!;`

# ERROR 50877ea571-4946 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:180`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const paragraph = editor.querySelector<HTMLElement>('p')!;`

# ERROR 50877ea571-4947 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:181`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const heading1 = editor.querySelector<HTMLElement>('h1')!;`

# ERROR 50877ea571-4948 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:182`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const heading2 = editor.querySelector<HTMLElement>('h2')!;`

# ERROR 50877ea571-4949 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:199`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const list = editor.querySelector<HTMLElement>('ul')!;`

# ERROR 50877ea571-4950 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:202`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await expect(getComputedStyle(list.querySelector('li')!).display).toBe('list-item');`

# ERROR 50877ea571-4951 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:208`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `await userEvent.click(editor.querySelector<HTMLElement>('h1')!);`

# ERROR 50877ea571-4952 no-casts `packages/stories/storybook-testing/src/Cell.test.ts:10`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const object = { id: 'obj-1' } as any;`

# ERROR 50877ea571-4953 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:12`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const Token = Role.make<Record<string, any>>('org.dxos.test.token');`

# ERROR 50877ea571-4954 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:19`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const object = { id: 'obj-1' } as any;`

# ERROR 50877ea571-4955 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:29`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const object = { id: 'obj-1' } as any;`

# ERROR 50877ea571-4956 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:35`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `token?: Role.Role<any>;`

# ERROR 50877ea571-4957 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:37`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data?: Record<string, any>;`

# ERROR 50877ea571-4958 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:46`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `| Role.Role<any>`

# ERROR 50877ea571-4959 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:47`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `| { type: Role.Role<any>; data?: Record<string, any>; id?: string }`

# ERROR 50877ea571-4960 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:64`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `type: Role.Role<any>;`

# ERROR 50877ea571-4961 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:66`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data?: Record<string, any>;`

# ERROR 50877ea571-4962 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:72`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `type: Role.Role<any>;`

# ERROR 50877ea571-4963 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:74`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `data: Record<string, any>;`

# ERROR 50877ea571-4964 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:144`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const BindingDebug = ({ role, data }: { role: string; data: Record<string, any> }) => (`

# ERROR 50877ea571-4965 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:162`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const SurfaceCell = ({ type, data }: { type: Role.Role<any>; data: Record<string, any> }) => {`

# ERROR 50877ea571-4966 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:31`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const Composer = forwardRef<SVGSVGElement, any>((props, ref) => {`

# ERROR 50877ea571-4967 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:218`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `.delay((_: any, i: any) => i * 200)`

# ERROR 50877ea571-4968 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:237`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `arc().innerRadius(innerRadius).outerRadius(outerRadius).startAngle(startAngle).endAngle(endAngle) as any,`

# ERROR 50877ea571-4969 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:245`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `.attrTween('transform', (() => interpolateString('rotate(0)', 'rotate(360)')) as any)`

# ERROR 50877ea571-4970 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:252`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `}) as any);`

# ERROR 50877ea571-4971 no-casts `packages/ui/brand/src/components/experimental/Logo.stories.tsx:229`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const d = arc<any, any>()`

# ERROR 50877ea571-4972 no-casts `packages/ui/brand/src/components/experimental/rive.stories.tsx:16`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `let t: any;`

# ERROR 50877ea571-4973 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:95`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `document.querySelector('dx-grid')!.addEventListener('dx-grid-cells-select', (event) => {`

# ERROR 50877ea571-4974 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:96`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `(window as any).DX_GRID_EVENT = event;`

# ERROR 50877ea571-4975 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:103`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return (window as any).DX_GRID_EVENT;`

# ERROR 50877ea571-4976 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:106`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return ((window as any).DX_GRID_EVENT = undefined);`

# ERROR 50877ea571-4977 no-casts `packages/ui/lit-ui/src/dx-icon/dx-icon.ts:21`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const getRegistry = (): IconRegistry | undefined => (globalThis as unknown as RegistryHost)[REGISTRY_GLOBAL];`

# ERROR 50877ea571-4978 no-casts `packages/ui/react-primitives/react-error-boundary/src/ErrorBoundary.tsx:29`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `resetKeys?: any[];`

# ERROR 50877ea571-4979 no-casts `packages/ui/react-primitives/react-error-boundary/src/ErrorBoundary.tsx:105`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `const win = window as any;`

# ERROR 50877ea571-4980 no-casts `packages/ui/react-primitives/react-hooks/src/useAsyncState.ts:12`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `deps: any[] = [],`

# ERROR 50877ea571-4981 no-casts `packages/ui/react-primitives/react-hooks/src/useControlledState.ts:36`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `function isFunction(value: unknown): value is (...args: any[]) => any {`

# ERROR 50877ea571-4982 no-casts `packages/ui/react-primitives/react-hooks/src/useDebugDeps.ts:19`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const diff: Record<number, { previous: any; current: any }> = {};`

# ERROR 50877ea571-4983 no-casts `packages/ui/react-primitives/react-hooks/src/useId.ts:12`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `const Alea: PrngFactory = alea as unknown as PrngFactory;`

# ERROR 50877ea571-4984 no-casts `packages/ui/react-primitives/react-hooks/src/useTimeout.ts:7`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const useTimeout = (callback?: () => Promise<void>, delay = 0, deps: any[] = []) => {`

# ERROR 50877ea571-4985 no-casts `packages/ui/react-primitives/react-hooks/src/useTimeout.ts:26`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `deps: any[] = [],`

# ERROR 50877ea571-4986 no-casts `packages/ui/react-primitives/react-hooks/src/useTransitions.ts:7`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const isFunction = <T>(functionToCheck: any): functionToCheck is (value: T) => boolean => {`

# ERROR 50877ea571-4987 no-casts `packages/ui/react-ui-attention/src/components/AttentionGlyph/AttentionGlyph.stories.tsx:32`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `component: AttentionGlyph as any,`

# ERROR 50877ea571-4988 no-casts `packages/ui/react-ui-attention/src/components/AttentionProvider/attention-context.ts:21`

This line uses the `as unknown as T` double-cast escape hatch, which per the no-casts rule forces an unrelated type instead of correcting the source type. Fix the type at its source (a proper interface, generic, or mapping function) rather than casting through `unknown`: `attention: undefined as unknown as AttentionManager,`

# ERROR 50877ea571-4989 no-casts `packages/ui/react-ui-audio/src/hooks/useAudioStream.ts:58`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `audioContextRef.current = (options.source as any).context;`

# ERROR 50877ea571-4990 no-casts `packages/ui/react-ui-audio/src/hooks/useAudioStream.ts:59`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `analyserRef.current = audioContextRef.current!.createAnalyser();`

# ERROR 50877ea571-4991 no-casts `packages/ui/react-ui-canvas-compute/src/compute.stories.tsx:199`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `component: Editor.Root as any,`

# ERROR 50877ea571-4992 no-casts `packages/ui/react-ui-canvas-compute/src/compute.stories.tsx:233`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `} as any),`

# ERROR 50877ea571-4993 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:68`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `value: any;`

# ERROR 50877ea571-4994 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:90`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `| { type: 'compute-input'; nodeId: string; property: string; value: any }`

# ERROR 50877ea571-4995 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:91`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `| { type: 'compute-output'; nodeId: string; property: string; value: any }`

# ERROR 50877ea571-4996 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:92`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `| { type: 'custom'; nodeId: string; event: any };`

# ERROR 50877ea571-4997 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:239`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `setOutput(nodeId: string, property: string, value: any): void {`

# ERROR 50877ea571-4998 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:341`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const tasks: Effect.Effect<unknown, any, ComputeServices>[] = [];`

# ERROR 50877ea571-4999 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:434`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return { type: 'compute-input', ...(payload as { nodeId: string; property: string; value: any }) };`

# ERROR 50877ea571-5000 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:436`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return { type: 'compute-output', ...(payload as { nodeId: string; property: string; value: any }) };`

# ERROR 50877ea571-5001 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:438`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `return { type: 'custom', ...(payload as { nodeId: string; event: any }) };`

# ERROR 50877ea571-5002 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:447`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const computeValueBag = (bag: ValueBag<any>): Effect.Effect<Record<string, RuntimeValue>, never, never> => {`

# ERROR 50877ea571-5003 no-casts `packages/ui/react-ui-canvas-compute/src/graph/createComputeGraph.test.ts:25`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const circuits: [string, () => any][] = [`

# ERROR 50877ea571-5004 no-casts `packages/ui/react-ui-canvas-compute/src/graph/createComputeGraph.test.ts:53`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const incoming = model.edges.filter((edge) => edge.target === gpt!.id);`

# ERROR 50877ea571-5005 no-casts `packages/ui/react-ui-canvas-compute/src/graph/createComputeGraph.test.ts:54`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `const outgoing = model.edges.filter((edge) => edge.source === gpt!.id);`

# ERROR 50877ea571-5006 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:21`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `setOutput: (property: string, value: any) => void;`

# ERROR 50877ea571-5007 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:54`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `return controller.evalNode(shape.node!);`

# ERROR 50877ea571-5008 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:69`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `node: controller.getComputeNode(shape.node!),`

# ERROR 50877ea571-5009 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:75`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `inputs: controller.getInputs(shape.node!),`

# ERROR 50877ea571-5010 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:76`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `outputs: controller.getOutputs(shape.node!),`

# ERROR 50877ea571-5011 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:77`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `setOutput: (property: string, value: any) => {`

# ERROR 50877ea571-5012 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:78`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `controller.setOutput(shape.node!, property, value);`

# ERROR 50877ea571-5013 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useGraphMonitor.ts:117`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `functionTrigger.runnable = Ref.make(graph.root) as any;`

# ERROR 50877ea571-5014 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useGraphMonitor.ts:130`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `db.remove(trigger.functionTrigger!.target!);`

# ERROR 50877ea571-5015 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:21`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const getHeight = (input: Schema.Schema<any>) => {`

# ERROR 50877ea571-5016 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `input: Schema.Schema<any> = VoidInput,`

# ERROR 50877ea571-5017 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:29`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `output: Schema.Schema<any> = VoidOutput,`

# ERROR 50877ea571-5018 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Constant.tsx:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const inferType = (value: any): string | undefined => {`

# ERROR 50877ea571-5019 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/defs.ts:27`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `return parts ? (parts.slice(1) as any) : [undefined, id];`

# ERROR 50877ea571-5020 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Feed.tsx:39`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const FeedItem = ({ classNames, item }: ThemedClassName<{ item: any }>) => {`

# ERROR 50877ea571-5021 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Thread.tsx:15`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const items: any[] = [];`

# ERROR 50877ea571-5022 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Thread.tsx:36`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const ThreadItem = ({ classNames, item }: ThemedClassName<{ item: any }>) => {`

# ERROR 50877ea571-5023 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/trigger-def.ts:36`

This line uses an `as any` cast, which per the no-casts rule suppresses the type checker instead of fixing the underlying type at its source. Replace it with the concrete type, a type guard, or a narrower generic: `export const TriggerShape: Schema.Schema<TriggerShape> = TriggerShapeSchema as any;`

# ERROR 50877ea571-5024 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/trigger-spec.ts:36`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `const kindToSchema: Record<Trigger.Kind, Schema.Schema<any>> = {`

# ERROR 50877ea571-5025 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Trigger.tsx:39`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `Obj.update(functionTrigger!, (obj) => {`

# ERROR 50877ea571-5026 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Trigger.tsx:56`

This line uses the non-null assertion `!`, which per the no-casts rule asserts away a possible `null`/`undefined` without a real check. Replace it with a guard, optional chaining, or a type that reflects when the value is actually guaranteed to exist: `outputSchema={getOutputSchema(functionTrigger.spec!.kind!)}`

# ERROR 50877ea571-5027 no-casts `packages/ui/react-ui-canvas-editor/src/components/Canvas/Shape.tsx:28`

This line has a widened `any` in a type position (parameter, return type, field, or generic argument), which per the no-casts rule lets values bypass type checking instead of fixing the type at its source. Replace `any` with the concrete type, a bounded generic, or `unknown` plus a narrowing check: `export const ShapeComponent = (props: ShapeComponentProps<any>) => {`

# ERROR 50877ea571-5028 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:26`

`random` is cast `as any` to satisfy the `ValueGenerator` type it doesn't structurally match; per the no-casts rule, give `random` (or the `ValueGenerator` type it's assigned to) a compatible signature instead of casting through `any`.

# ERROR 50877ea571-5029 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:106`

The Storybook `component` field casts `Editor.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Editor.Root`'s prop types (or the `Meta<typeof Editor.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5030 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useActionHandler.ts:216`

`const g = overlayRef.current!.querySelector<SVGGElement>(` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 216.

# ERROR 50877ea571-5031 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:99`

This file uses the non-null assertion `!` 2 times (lines 99, 100) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const find = (id: string) => simulation.nodes().find((d: any) => d.id === id)!;` at line 99; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5032 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:230`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5033 no-casts `packages/ui/react-ui-canvas-editor/src/layout/bullets.ts:26`

`const edge = graph.getEdge(el.getAttribute(DATA_SHAPE_ID)!);` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 26.

# ERROR 50877ea571-5034 no-casts `packages/ui/react-ui-canvas-editor/src/layout/layout.ts:70`

`node.data` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `node.data`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5035 no-casts `packages/ui/react-ui-canvas-editor/src/shapes/defs.ts:23`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5036 no-casts `packages/ui/react-ui-canvas-editor/src/testing/DragTest.tsx:56`

This file uses the non-null assertion `!` 3 times (lines 56, 60, 63) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `// const pos = getInputPoint(draggingRef.current!.parentElement!, location.current.input);` at line 56; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5037 no-casts `packages/ui/react-ui-canvas-editor/src/testing/useSelection.ts:26`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5038 no-casts `packages/ui/react-ui-canvas-editor/src/testing/useSelection.ts:27`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5039 no-casts `packages/ui/react-ui-canvas-editor/src/types/model.ts:23`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5040 no-casts `packages/ui/react-ui-canvas/src/components/CellGrid/CellGrid.stories.tsx:175`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5041 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:28`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5042 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:30`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5043 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:40`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5044 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:41`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5045 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:42`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5046 no-casts `packages/ui/react-ui-components/src/components/QueryEditor/query-extension.ts:237`

`const label: string = this._identifier.split(/\W/).at(-1)!;` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 237.

# ERROR 50877ea571-5047 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:23`

This file uses the non-null assertion `!` 15 times (lines 23, 32, 35, 53, 129, 140, 147, 158, 163, 178, 194, 194, 203, 209, 217) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const lanes = commits.map((commit) => laneById.get(commit.id)!);` at line 23; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5048 workspace-deps `packages/ui/react-ui-dashboard/package.json:46`

`peerDependencies` pins `@dxos/react-ui` and `@dxos/ui-theme` (line 46-47) to `workspace:*`, violating the `workspace-deps` rule. Change both to `workspace:^`.

# ERROR 50877ea571-5049 no-casts `packages/ui/react-ui-dashboard/src/util.test.ts:21`

This file uses the non-null assertion `!` 2 times (lines 21, 49) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const last = calendar.cells.at(-1)!;` at line 21; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5050 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:22`

This file uses the non-null assertion `!` 23 times (lines 22, 23, 29, 30, 35, 38, 40, 41, 45, 46, 47, 48, 54, 55, 56, 57, 66, 67, 71, 72, 73, 81, 83) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `expect(nodes.get('X')!.origin!.y).toBeLessThan(nodes.get('CORE')!.origin!.y);` at line 22; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5051 no-casts `packages/ui/react-ui-editor/src/components/EditorMenuProvider/EditorMenuProvider.tsx:104`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5052 no-casts `packages/ui/react-ui-editor/src/components/EditorPreviewProvider/EditorPreviewProvider.tsx:55`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5053 no-casts `packages/ui/react-ui-editor/src/components/EditorPreviewProvider/EditorPreviewProvider.tsx:64`

The double cast `as unknown as RefObject<HTMLButtonElement>` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5054 no-casts `packages/ui/react-ui-editor/src/stories/Popover.stories.tsx:31`

`random` is cast `as any` to satisfy the `ValueGenerator` type it doesn't structurally match; per the no-casts rule, give `random` (or the `ValueGenerator` type it's assigned to) a compatible signature instead of casting through `any`.

# ERROR 50877ea571-5055 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:99`

This file uses the non-null assertion `!` 7 times (lines 99, 100, 121, 240, 300, 520, 525) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `Grey: (b) => d3.interpolateGreys(0.25 + d3.mean(b.last ?? [0])!),` at line 99; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5056 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:149`

This file uses the non-null assertion `!` 12 times (lines 149, 567, 568, 569, 607, 615, 646, 647, 706, 722, 723, 978) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const uniformName = gl.getActiveUniform(program, i)!.name;` at line 149; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5057 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:46`

`helper.material` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `helper.material`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5058 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:47`

`helper.material` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `helper.material`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5059 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:48`

`helper.material` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `helper.material`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5060 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:283`

`kube.init(contentRef.current!).start();` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 283.

# ERROR 50877ea571-5061 no-casts `packages/ui/react-ui-form/src/annotations.ts:22`

This file widens 2 parameter/return types to `any` (lines 22, 51) instead of typing them — e.g. `readonly load: (values: any) => Effect.Effect<readonly OptionsLookupEntry[], unknown>;` at line 22 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5062 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.stories.tsx:59`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5063 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.stories.tsx:81`

The Storybook `component` field casts `FieldEditor` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `FieldEditor`'s prop types (or the `Meta<typeof FieldEditor>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5064 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.test.tsx:37`

This file uses the non-null assertion `!` 2 times (lines 37, 47) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `fireEvent.click(option!);` at line 37; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5065 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.test.tsx:55`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5066 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/ArrayField/ArrayField.tsx:40`

`const isOrderedItem = (item: any): item is OrderedItem =>` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5067 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.stories.tsx:65`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5068 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.stories.tsx:162`

`const editorContent = editorRoot.querySelector<HTMLElement>('.cm-content')!;` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 162.

# ERROR 50877ea571-5069 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/find-ref-option.ts:14`

`const isRefSnapshot = (val: any): val is { '/': string } => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5070 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/find-ref-option.ts:15`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5071 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:116`

`(values: any, { isValid, changed }: { isValid: boolean; changed: Record<SchemaEx.JsonPath, boolean> }) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5072 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:139`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5073 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/RefField.stories.tsx:48`

`onCreate={(_type, props: any) => space.db.add(Obj.make(Organization, { name: 'New organization', ...props }))}` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5074 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/RefField.tsx:124`

`async (values: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5075 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/SelectOptionField/SelectOptionField.stories.tsx:53`

The Storybook `component` field casts `SelectOptionField` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `SelectOptionField`'s prop types (or the `Meta<typeof SelectOptionField>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5076 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/FormField.tsx:277`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5077 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:200`

`omitId(AnnotatedFlight) as unknown as Schema.Schema<any>` is the double-cast escape hatch the no-casts rule names, and it also bakes in an extra `any` — per the rule, type `omitId`'s return generically so the result is already a `Schema.Schema<T>` for the concrete `T`, instead of forcing it through `unknown`.

# ERROR 50877ea571-5078 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:243`

`omitId(Type.getSchema(Journey)) as unknown as Schema.Schema<any>` is the double-cast escape hatch the no-casts rule names, and it also bakes in an extra `any`; per the rule, type `omitId`/`getSchema` generically so the result is already the concrete `Schema.Schema<T>` instead of forcing it through `unknown`.

# ERROR 50877ea571-5079 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:293`

`omitId(Type.getSchema(Flight)) as unknown as Schema.Schema<any>` is the double-cast escape hatch the no-casts rule names, and it also bakes in an extra `any`; per the rule, type `omitId`/`getSchema` generically so the result is already the concrete `Schema.Schema<T>` instead of forcing it through `unknown`.

# ERROR 50877ea571-5080 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:29`

This file uses the non-null assertion `!` 11 times (lines 29, 30, 31, 37, 38, 39, 41, 48, 49, 51, 52) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `expect(resolved!.segments).toEqual(['number']);` at line 29; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5081 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:41`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5082 no-casts `packages/ui/react-ui-form/src/components/ObjectForm/ObjectForm.tsx:48`

`const handleCreate = useCallback((type: Type.AnyEntity, values: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5083 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.stories.tsx:66`

`(values: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5084 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.tsx:29`

This file widens 2 parameter/return types to `any` (lines 29, 59) instead of typing them — e.g. `onCreate?: (values: any) => unknown | Promise<unknown>;` at line 29 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5085 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:67`

`FactoryAnnotation.set(((values: any) =>` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5086 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:100`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5087 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:122`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5088 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:140`

The Storybook `component` field casts `ObjectProperties` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `ObjectProperties`'s prop types (or the `Meta<typeof ObjectProperties>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5089 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:183`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5090 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:19`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5091 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:21`

`return debug!;` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 21.

# ERROR 50877ea571-5092 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:39`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5093 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:53`

`created` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `created`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5094 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:63`

The query result is cast to `any[]` instead of the actual object type, discarding all element type-checking for the rest of the block; per the no-casts rule, type the query/`.run()` result with the concrete schema type (or a generic parameter) instead of widening to `any[]`.

# ERROR 50877ea571-5095 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:64`

This file widens 2 parameter/return types to `any` (lines 64, 88) instead of typing them — e.g. `const created = authors.find((author: any) => author.name === 'Ada Lovelace');` at line 64 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5096 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:70`

The value is reassigned through `as any`, discarding its type for everything downstream; per the no-casts rule, type the variable correctly at its declaration (or the function it's assigned from) instead of widening it to `any`.

# ERROR 50877ea571-5097 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:72`

`created` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `created`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5098 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:87`

The query result is cast to `any[]` instead of the actual object type, discarding all element type-checking for the rest of the block; per the no-casts rule, type the query/`.run()` result with the concrete schema type (or a generic parameter) instead of widening to `any[]`.

# ERROR 50877ea571-5099 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:91`

`created` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `created`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5100 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:96`

The value is reassigned through `as any`, discarding its type for everything downstream; per the no-casts rule, type the variable correctly at its declaration (or the function it's assigned from) instead of widening it to `any`.

# ERROR 50877ea571-5101 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:98`

`created` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `created`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5102 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:68`

`(type: Type.AnyEntity, values: any): Obj.Unknown => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5103 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:107`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5104 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:124`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5105 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:111`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5106 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:112`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5107 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:126`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5108 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.tsx:48`

`ObjectTreeImpl as unknown as <T>(...) => ReactElement | null` re-introduces the generic `T` that `composable()` erases, using the double-cast escape hatch the no-casts rule names; the accompanying comment explains why, but per the rule the fix belongs at the source — give `composable()`/`ObjectTreeImpl` a signature that preserves the generic (or a typed helper for this pattern) instead of restoring it with a cast through `unknown` at every call site that needs it.

# ERROR 50877ea571-5109 no-casts `packages/ui/react-ui-form/src/components/RefEditor/RefEditor.stories.tsx:22`

`random` is cast `as any` to satisfy the `ValueGenerator` type it doesn't structurally match; per the no-casts rule, give `random` (or the `ValueGenerator` type it's assigned to) a compatible signature instead of casting through `any`.

# ERROR 50877ea571-5110 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.stories.tsx:120`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5111 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:20`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5112 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:77`

This file uses the non-null assertion `!` 9 times (lines 77, 78, 83, 89, 125, 132, 138, 175, 208) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `expect(schemaProperties!.new_property).toBeDefined();` at line 77; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5113 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:81`

This file widens 6 parameter/return types to `any` (lines 81, 87, 129, 136, 178, 183) instead of typing them — e.g. `const newPropertyField = debugObjects.view.projection.fields.find((field: any) => field.path === 'new_property');` at line 81 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5114 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.tsx:180`

`(values: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5115 no-casts `packages/ui/react-ui-form/src/hooks/useFormContext.ts:85`

`onValueChange: (ast: SchemaAST.AST, value: any) => onValueChange(stablePath, ast, value),` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5116 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:270`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5117 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:284`

The value is reassigned through `as any`, discarding its type for everything downstream; per the no-casts rule, type the variable correctly at its declaration (or the function it's assigned from) instead of widening it to `any`.

# ERROR 50877ea571-5118 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:378`

`const mergeAtPath = (obj: any, path: readonly (string | number)[], value: any): any => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5119 no-casts `packages/ui/react-ui-form/src/types.ts:136`

`onCreate?: (schema: Type.AnyEntity, values: any) => Obj.Unknown | Promise<Obj.Unknown> | undefined | void;` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5120 no-casts `packages/ui/react-ui-form/src/util/omit.ts:29`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5121 no-casts `packages/ui/react-ui-form/src/util/omit.ts:60`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5122 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:104`

This file uses the non-null assertion `!` 6 times (lines 104, 105, 110, 112, 114, 116) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `)!;` at line 104; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5123 no-casts `packages/ui/react-ui-gameboard/src/components/Chessboard/Chessboard.tsx:79`

This file uses the non-null assertion `!` 3 times (lines 79, 80, 144) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const square = getSquareLocation(gridRef.current!, location)!;` at line 79; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5124 no-casts `packages/ui/react-ui-geo/src/components/Globe/Globe.stories.tsx:159`

The double cast `as any as T` is the exact escape hatch the no-casts rule calls out — routing through `any` defeats the type checker entirely; replace it with a direct, correctly-typed conversion or a runtime check/type guard that narrows to `T`.

# ERROR 50877ea571-5125 no-casts `packages/ui/react-ui-geo/src/components/Globe/Globe.stories.tsx:165`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5126 no-casts `packages/ui/react-ui-geo/src/components/Map/Map.stories.tsx:59`

The Storybook `component` field casts `Map.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Map.Root`'s prop types (or the `Meta<typeof Map.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5127 no-casts `packages/ui/react-ui-geo/src/util/render.ts:119`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5128 no-casts `packages/ui/react-ui-geo/src/util/render.ts:124`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5129 no-casts `packages/ui/react-ui-geo/src/util/render.ts:145`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5130 no-casts `packages/ui/react-ui-geo/src/util/render.ts:241`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5131 no-casts `packages/ui/react-ui-graph/src/components/SVG/Zoom.tsx:23`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5132 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:131`

This file uses the non-null assertion `!` 2 times (lines 131, 132) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `source.outgoing!.push([source, target, edge]);` at line 131; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5133 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:283`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5134 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:205`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5135 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:207`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5136 no-casts `packages/ui/react-ui-graph/src/fx/fx.stories.tsx:84`

The double cast `as unknown as SVGGElement` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5137 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:256`

`d.data.node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `d.data.node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5138 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:257`

`d.data.node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `d.data.node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5139 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:297`

`parentStub` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `parentStub`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5140 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:298`

`parentStub` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `parentStub`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5141 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:300`

`node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5142 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:328`

This file uses the non-null assertion `!` 2 times (lines 328, 329) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `source: sourceNode!,` at line 328; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5143 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-lattice-projector.ts:52`

`n.data` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `n.data`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5144 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:39`

`edge.data` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `edge.data`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5145 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:46`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5146 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:57`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5147 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:165`

This file uses the non-null assertion `!` 5 times (lines 165, 165, 167, 191, 208) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const placed = new Set<string>([focusId!]);` at line 165; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5148 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:243`

`focusNode` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `focusNode`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5149 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:248`

`d.data.node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `d.data.node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5150 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:139`

This file uses the non-null assertion `!` 6 times (lines 139, 139, 140, 140, 168, 169) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const visited = new Set<string>([focusId!]);` at line 139; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5151 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:356`

This file uses the non-null assertion `!` 2 times (lines 356, 730) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `options.renderNode!(shapeGroup, d);` at line 356; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5152 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:487`

The double cast `as unknown as D3Selection` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5153 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:600`

The double cast `as unknown as D3Selection` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5154 no-casts `packages/ui/react-ui-graph/src/graph/renderer/labels.ts:27`

`node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5155 no-casts `packages/ui/react-ui-graph/src/graph/renderer/labels.ts:28`

`node` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `node`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5156 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:56`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5157 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:87`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5158 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:115`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5159 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:160`

`const root = select(this._ref.current!);` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 160.

# ERROR 50877ea571-5160 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:172`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5161 no-casts `packages/ui/react-ui-graph/src/hooks/useSvgContext.ts:41`

This file uses the non-null assertion `!` 5 times (lines 41, 49, 61, 82, 83) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `return this._ref.current!;` at line 41; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5162 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:42`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5163 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:65`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5164 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:72`

This file uses the non-null assertion `!` 2 times (lines 72, 111) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `this._options?.onDblClick!(this);` at line 72; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5165 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:91`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5166 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:111`

The d3 selection/transition is cast `as any` to bridge mismatched generic type parameters instead of aligning them; per the no-casts rule, parameterize the `Selection`/`Transition`/`drag` generics correctly (or introduce a small typed wrapper) so the call type-checks without the cast.

# ERROR 50877ea571-5167 no-casts `packages/ui/react-ui-introspect/src/components/ToolForm/ToolForm.tsx:99`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5168 no-casts `packages/ui/react-ui-introspect/src/components/ToolForm/ToolForm.tsx:100`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5169 no-casts `packages/ui/react-ui-list/src/aspects/useListSelection.test.ts:22`

An empty/placeholder object is cast `as any` to satisfy a handler's parameter type instead of constructing a value of the real type; per the no-casts rule, build a minimally valid typed stub (or type the handler parameter as optional/partial) instead of casting through `any`.

# ERROR 50877ea571-5170 no-casts `packages/ui/react-ui-list/src/aspects/useListSelection.test.ts:29`

An empty/placeholder object is cast `as any` to satisfy a handler's parameter type instead of constructing a value of the real type; per the no-casts rule, build a minimally valid typed stub (or type the handler parameter as optional/partial) instead of casting through `any`.

# ERROR 50877ea571-5171 no-casts `packages/ui/react-ui-list/src/aspects/useListSelection.test.ts:64`

An empty/placeholder object is cast `as any` to satisfy a handler's parameter type instead of constructing a value of the real type; per the no-casts rule, build a minimally valid typed stub (or type the handler parameter as optional/partial) instead of casting through `any`.

# ERROR 50877ea571-5172 no-casts `packages/ui/react-ui-list/src/aspects/useListSelection.test.ts:84`

An empty/placeholder object is cast `as any` to satisfy a handler's parameter type instead of constructing a value of the real type; per the no-casts rule, build a minimally valid typed stub (or type the handler parameter as optional/partial) instead of casting through `any`.

# ERROR 50877ea571-5173 no-casts `packages/ui/react-ui-list/src/aspects/useListSelection.test.ts:97`

An empty/placeholder object is cast `as any` to satisfy a handler's parameter type instead of constructing a value of the real type; per the no-casts rule, build a minimally valid typed stub (or type the handler parameter as optional/partial) instead of casting through `any`.

# ERROR 50877ea571-5174 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionRoot.tsx:29`

`item` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `item`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5175 no-casts `packages/ui/react-ui-list/src/components/Combobox/Combobox.stories.tsx:49`

The Storybook `component` field casts `Combobox.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Combobox.Root`'s prop types (or the `Meta<typeof Combobox.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5176 no-casts `packages/ui/react-ui-list/src/components/Listbox/Listbox.tsx:202`

The double cast `as unknown as ForwardedRef<HTMLOListElement>` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5177 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedList.stories.tsx:25`

This file widens 2 parameter/return types to `any` (lines 25, 266) instead of typing them — e.g. `const isItem = (value: any): value is Item => !!value && typeof value === 'object' && typeof value.id === 'string';` at line 25 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5178 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListRoot.tsx:31`

`isItem?: (item: any) => boolean;` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5179 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListRoot.tsx:47`

`item` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `item`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5180 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:50`

This file uses the non-null assertion `!` 8 times (lines 50, 52, 59, 80, 81, 88, 89, 96) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const index = parent.items!.findIndex(({ id }) => id === source.id);` at line 50; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5181 no-casts `packages/ui/react-ui-list/src/components/Tree/Tree.stories.tsx:88`

This file uses the non-null assertion `!` 3 times (lines 88, 100, 101) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const id = pathKey.split('~').pop()!;` at line 88; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5182 no-casts `packages/ui/react-ui-list/src/components/Treegrid/Treegrid.stories.tsx:77`

This file uses the non-null assertion `!` 2 times (lines 77, 91) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const { node, path, parentOf } = stack.pop()!;` at line 77; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5183 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/create-controller.ts:123`

This file widens 2 parameter/return types to `any` (lines 123, 160) instead of typing them — e.g. `setContext: (context: any) => {` at line 123 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5184 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/MarkdownStream.tsx:74`

`setContext: (context: any) => void;` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5185 no-casts `packages/ui/react-ui-masonry/src/Masonry.tsx:87`

`Tile={Tile!}` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 87.

# ERROR 50877ea571-5186 no-casts `packages/ui/react-ui-mcp/src/ToolForm.tsx:41`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5187 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:107`

This file uses the non-null assertion `!` 7 times (lines 107, 109, 110, 227, 259, 260, 261) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `expect(separatorNode!.properties).toEqual({ variant: 'gap' });` at line 107; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5188 no-casts `packages/ui/react-ui-menu/src/components/action-label.ts:19`

`return '${toLocalizedString(action.properties!.label, t)}${shortcut ? ' (${keySymbols(shortcut).join('')})' : ''}';` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 19.

# ERROR 50877ea571-5189 no-casts `packages/ui/react-ui-menu/src/components/ActionLabel.tsx:24`

`<span className='grow truncate'>{toLocalizedString(action.properties!.label, t)}</span>` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 24.

# ERROR 50877ea571-5190 no-casts `packages/ui/react-ui-mosaic/src/components/Board/Column.tsx:241`

`get: (refOrObj) => (Ref.isRef(refOrObj) ? refOrObj.target! : refOrObj),` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 241.

# ERROR 50877ea571-5191 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:248`

This file uses the non-null assertion `!` 4 times (lines 248, 249, 288, 291) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `? (index) => (index % 2 === 0 ? 'ph-${index}' : getId(visibleItems![Math.floor(index / 2)]))` at line 248; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5192 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:371`

The component/value is cast to `any` to attach an internal marker property (`COMPOSABLE`/`dxos.composable`) that isn't declared on its type; per the no-casts rule, declare the marker on the component's type (e.g. via an intersection type or a typed helper) instead of casting through `any`.

# ERROR 50877ea571-5193 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.test.tsx:34`

The double cast `as unknown as Virtualizer<any, any>` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5194 workspace-deps `packages/ui/react-ui-rdf/package.json:44`

`peerDependencies` pins `@dxos/react-ui` and `@dxos/ui-theme` (line 44-45) to `workspace:*`, violating the `workspace-deps` rule. Change both to `workspace:^`.

# ERROR 50877ea571-5195 no-casts `packages/ui/react-ui-search/src/components/SearchList/hooks/useSearchListResults.ts:62`

`item` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `item`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5196 no-casts `packages/ui/react-ui-search/src/components/SearchList/SearchList.stories.tsx:500`

The Storybook `component` field casts `SearchList.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `SearchList.Root`'s prop types (or the `Meta<typeof SearchList.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5197 no-casts `packages/ui/react-ui-syntax-highlighter/src/JsonHighlighter/JsonHighlighter.tsx:12`

`export type JsonReplacer = CreateReplacerProps | ((key: string, value: any) => any);` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5198 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:42`

`const createChildren = (root: any, d = 0) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5199 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:77`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5200 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:98`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5201 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.stories.tsx:111`

`const handleRowClicked = (row: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5202 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.tsx:29`

This file widens 3 parameter/return types to `any` (lines 29, 30, 67) instead of typing them — e.g. `onRowClick?: (row: any) => void;` at line 29 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5203 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:32`

`random` is cast `as any` to satisfy the `ValueGenerator` type it doesn't structurally match; per the no-casts rule, give `random` (or the `ValueGenerator` type it's assigned to) a compatible signature instead of casting through `any`.

# ERROR 50877ea571-5204 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:71`

The double cast `as unknown as TableRow[]` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5205 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:92`

`(schema: Type.AnyEntity, values: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5206 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:110`

`(row: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5207 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:182`

`view.projection.fields.find((field) => field.path === 'name')!,` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 182.

# ERROR 50877ea571-5208 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:231`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5209 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:272`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5210 no-casts `packages/ui/react-ui-table/src/components/Table/TableContent.tsx:56`

`onRowClick?: (row: any) => void;` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5211 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:24`

`export type OnCreateHandler = (schema: Type.AnyEntity, values: any) => Parameters<typeof Ref.make>[0];` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5212 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:155`

`label: SchemaEx.getValue(obj, fieldProjection.field.referencePath!) || obj.id.toString(),` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 155.

# ERROR 50877ea571-5213 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/TableCellEditor.tsx:130`

`async (value: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5214 no-casts `packages/ui/react-ui-table/src/hooks/useAddRow.ts:23`

`(data?: any): InsertRowResult => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5215 no-casts `packages/ui/react-ui-table/src/model/modal-controller.ts:20`

The double cast `as unknown as HTMLElement` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5216 no-casts `packages/ui/react-ui-table/src/model/table-model.test.ts:160`

This file uses the non-null assertion `!` 3 times (lines 160, 163, 192) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const titleFieldId = first.projection.getFields().find((field) => field.path === 'title')!.id;` at line 160; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5217 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:50`

The double cast `as unknown as Table.Table` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5218 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:130`

This file widens 6 parameter/return types to `any` (lines 130, 147, 779, 835, 876, 1072) instead of typing them — e.g. `onInsertRow?: (data?: any) => InsertRowResult;` at line 130 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5219 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:173`

`private _selection!: SelectionModel<T>;` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 173.

# ERROR 50877ea571-5220 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:645`

The double cast `as any as T` is the exact escape hatch the no-casts rule calls out — routing through `any` defeats the type checker entirely; replace it with a direct, correctly-typed conversion or a runtime check/type guard that narrows to `T`.

# ERROR 50877ea571-5221 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:816`

The double cast `as unknown as Obj.Unknown` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5222 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:819`

The double cast `as unknown as Obj.Unknown` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5223 no-casts `packages/ui/react-ui-table/src/model/table-presentation.ts:171`

`const getLabel = (value: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5224 no-casts `packages/ui/react-ui-table/src/testing/useTestTableModel.ts:64`

`const handleCellUpdate = useCallback((cell: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5225 no-casts `packages/ui/react-ui-table/src/util/dynamic-table.ts:52`

This file uses the non-null assertion `!` 2 times (lines 52, 98) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `return { typename: Type.getTypename(resolved)!, type: resolved };` at line 52; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5226 no-casts `packages/ui/react-ui-table/src/util/schema.ts:21`

`schema` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `schema`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5227 no-casts `packages/ui/react-ui-table/src/util/sort.ts:11`

`export const getSortValue = (value: any, format: Format.TypeFormat): any => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5228 no-casts `packages/ui/react-ui-thread/src/testing.ts:13`

`random` is cast `as any` to satisfy the `ValueGenerator` type it doesn't structurally match; per the no-casts rule, give `random` (or the `ValueGenerator` type it's assigned to) a compatible signature instead of casting through `any`.

# ERROR 50877ea571-5229 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:26`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5230 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:27`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5231 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:39`

This file widens 2 parameter/return types to `any` (lines 39, 43) instead of typing them — e.g. `onresult: (event: any) => {` at line 39 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5232 no-casts `packages/ui/react-ui/src/components/Breadcrumb/Breadcrumb.stories.tsx:44`

The Storybook `component` field casts `Breadcrumb.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Breadcrumb.Root`'s prop types (or the `Meta<typeof Breadcrumb.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5233 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:137`

The Storybook `render` field casts `DefaultStory` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `DefaultStory`'s prop types (or the `Meta<typeof DefaultStory>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5234 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:150`

The Storybook `render` field casts `DensitiesStory` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `DensitiesStory`'s prop types (or the `Meta<typeof DensitiesStory>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5235 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:158`

The Storybook `render` field casts `DensitiesStory` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `DensitiesStory`'s prop types (or the `Meta<typeof DensitiesStory>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5236 no-casts `packages/ui/react-ui/src/components/Dialog/AlertDialog.stories.tsx:51`

The Storybook `component` field casts `AlertDialog.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `AlertDialog.Root`'s prop types (or the `Meta<typeof AlertDialog.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5237 no-casts `packages/ui/react-ui/src/components/Dialog/AlertDialog.stories.tsx:52`

The Storybook `render` field casts `DefaultStory` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `DefaultStory`'s prop types (or the `Meta<typeof DefaultStory>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5238 no-casts `packages/ui/react-ui/src/components/Dialog/Dialog.stories.tsx:103`

The Storybook `component` field casts `Dialog` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Dialog`'s prop types (or the `Meta<typeof Dialog>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5239 no-casts `packages/ui/react-ui/src/components/ErrorFallback/ErrorFallback.stories.tsx:39`

`window` is cast to `any` to attach or read an ad-hoc property that isn't on the `Window` type; per the no-casts rule, add a typed `declare global { interface Window { ... } }` augmentation for the property instead of casting the whole object away.

# ERROR 50877ea571-5240 no-casts `packages/ui/react-ui/src/components/Input/Input.stories.tsx:88`

The Storybook `component` field casts `Input.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Input.Root`'s prop types (or the `Meta<typeof Input.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5241 no-casts `packages/ui/react-ui/src/components/Input/Input.stories.tsx:265`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5242 no-casts `packages/ui/react-ui/src/components/Menu/ContextMenu.stories.tsx:95`

The Storybook `component` field casts `ContextMenu.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `ContextMenu.Root`'s prop types (or the `Meta<typeof ContextMenu.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5243 no-casts `packages/ui/react-ui/src/components/Message/Message.stories.tsx:46`

The Storybook `component` field casts `Message.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Message.Root`'s prop types (or the `Meta<typeof Message.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5244 no-casts `packages/ui/react-ui/src/components/Toast/Toast.stories.tsx:59`

The Storybook `component` field casts `Toast` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Toast`'s prop types (or the `Meta<typeof Toast>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5245 no-casts `packages/ui/react-ui/src/components/Toolbar/Toolbar.stories.tsx:72`

The Storybook `component` field casts `Toolbar` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Toolbar`'s prop types (or the `Meta<typeof Toolbar>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5246 no-casts `packages/ui/react-ui/src/components/Tooltip/Tooltip.stories.tsx:35`

The Storybook `component` field casts `Tooltip` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Tooltip`'s prop types (or the `Meta<typeof Tooltip>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5247 no-casts `packages/ui/react-ui/src/primitives/ThemeProvider/icon-registry.ts:63`

The double cast `as unknown as RegistryHost` is exactly the escape hatch the no-casts rule names — routing through `unknown` defeats structural type-checking entirely; per the rule, replace it with a type that's actually compatible (adjust the source/target types or add a runtime narrowing check) instead of forcing the conversion through `unknown`.

# ERROR 50877ea571-5248 no-casts `packages/ui/react-ui/src/util/slots.ts:84`

The component/value is cast to `any` to attach an internal marker property (`COMPOSABLE`/`dxos.composable`) that isn't declared on its type; per the no-casts rule, declare the marker on the component's type (e.g. via an intersection type or a typed helper) instead of casting through `any`.

# ERROR 50877ea571-5249 no-casts `packages/ui/react-ui/src/util/slots.ts:87`

`child.type` is cast to `any` to reach or assign an untyped property instead of typing it; per the no-casts rule, extend `child.type`'s declared type to include the property (or narrow with a type guard) so the access is checked by the compiler.

# ERROR 50877ea571-5250 no-casts `packages/ui/react-ui/src/util/slots.ts:107`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5251 no-casts `packages/ui/react-ui/src/util/slots.ts:108`

The component/value is cast to `any` to attach an internal marker property (`COMPOSABLE`/`dxos.composable`) that isn't declared on its type; per the no-casts rule, declare the marker on the component's type (e.g. via an intersection type or a typed helper) instead of casting through `any`.

# ERROR 50877ea571-5252 no-casts `packages/ui/react-ui/src/util/slots.ts:135`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5253 no-casts `packages/ui/react-ui/src/util/slots.ts:136`

The component/value is cast to `any` to attach an internal marker property (`COMPOSABLE`/`dxos.composable`) that isn't declared on its type; per the no-casts rule, declare the marker on the component's type (e.g. via an intersection type or a typed helper) instead of casting through `any`.

# ERROR 50877ea571-5254 no-casts `packages/ui/solid-ui-geo/src/components/Globe/Globe.solid-stories.tsx:150`

The double cast `as any as T` is the exact escape hatch the no-casts rule calls out — routing through `any` defeats the type checker entirely; replace it with a direct, correctly-typed conversion or a runtime check/type guard that narrows to `T`.

# ERROR 50877ea571-5255 no-casts `packages/ui/solid-ui-geo/src/components/Globe/Globe.solid-stories.tsx:158`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5256 no-casts `packages/ui/solid-ui-geo/src/components/Globe/Globe.solid-stories.tsx:158`

`? createTrip(airports()!, routes, (dots()?.objects.dots as any)?.geometries[0].coordinates)` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 158.

# ERROR 50877ea571-5257 no-casts `packages/ui/solid-ui-geo/src/components/Map/Map.solid-stories.tsx:28`

The Storybook `component` field casts `Map.Root` to `any` to paper over a prop/type mismatch with the `Meta`/`StoryObj` generic instead of fixing it; per the no-casts rule, align `Map.Root`'s prop types (or the `Meta<typeof Map.Root>` generic argument) so the assignment type-checks without the cast.

# ERROR 50877ea571-5258 no-casts `packages/ui/solid-ui-geo/src/hooks/useSpinner.ts:30`

This file uses the non-null assertion `!` 2 times (lines 30, 42) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `let lastRotation = controller!.projection.rotate();` at line 30; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5259 no-casts `packages/ui/solid-ui-geo/src/util/render.ts:82`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5260 no-casts `packages/ui/ui-editor/src/extensions/decoration/replacer.test.ts:31`

`dispatch: (transaction: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5261 no-casts `packages/ui/ui-editor/src/extensions/decoration/replacer.test.ts:34`

This cast to `any` silences the type checker instead of fixing the underlying type mismatch; per the no-casts rule, type the value precisely at its source (or add a narrowing check) so the compiler verifies this expression.

# ERROR 50877ea571-5262 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:213`

This file uses the non-null assertion `!` 7 times (lines 213, 248, 324, 325, 326, 327, 328) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `this._node!.style.transform = 'translate(${shakeX}px,${shakeY}px)';` at line 213; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5263 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/decorate.ts:317`

This file uses the non-null assertion `!` 3 times (lines 317, 321, 573) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `const mark = node.node.firstChild!;` at line 317; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5264 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:520`

This file uses the non-null assertion `!` 7 times (lines 520, 526, 620, 623, 716, 844, 903) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `if (sNode.firstChild!.to === from) {` at line 520; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5265 no-casts `packages/ui/ui-editor/src/extensions/language/xml/stub.ts:23`

`updated(id: string, widgetState: any): void;` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5266 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:320`

`updated: (id: string, widgetState: any) => {` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5267 no-casts `packages/ui/ui-editor/src/extensions/review/comments.ts:515`

This file uses the non-null assertion `!` 2 times (lines 515, 516) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `if (text.eq(tracked!.text)) {` at line 515; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5268 no-casts `packages/ui/ui-editor/src/extensions/review/diff.test.ts:150`

`const applied = current.slice(0, splice!.from) + splice!.insert + current.slice(splice!.from + splice!.del);` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 150.

# ERROR 50877ea571-5269 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:78`

This file uses the non-null assertion `!` 2 times (lines 78, 123) to bypass possibly-`null`/`undefined` types instead of narrowing them — e.g. `return traverse<T>(itemOrCb, maybeCb!);` at line 78; per the no-casts rule, replace each with a proper null check, a default/fallback value, or a typed accessor/helper that can't return `null`/`undefined`, rather than asserting past the type checker.

# ERROR 50877ea571-5270 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:179`

This file widens 2 parameter/return types to `any` (lines 179, 180) instead of typing them — e.g. `const format = (value: any) =>` at line 179 — silencing the type checker for everything that flows through them; per the no-casts rule, give each its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.

# ERROR 50877ea571-5271 no-casts `packages/ui/ui-editor/src/util/dom.ts:30`

`return textRange(dom as Text, 0, dom.nodeValue!.length).getClientRects();` uses the non-null assertion `!` to bypass a possibly-`null`/`undefined` type instead of narrowing it; per the no-casts rule, replace it with a proper null check, a default value, or a typed accessor that can't return `null`/`undefined` at line 30.

# ERROR 50877ea571-5272 no-casts `packages/ui/ui-editor/src/util/dom.ts:34`

The double cast `as any as T` is the exact escape hatch the no-casts rule calls out — routing through `any` defeats the type checker entirely; replace it with a direct, correctly-typed conversion or a runtime check/type guard that narrows to `T`.

# ERROR 50877ea571-5273 no-casts `packages/ui/ui-types/src/translations.ts:10`

`export const isLabel = (o: any): o is Label =>` widens a parameter/return type to `any` instead of typing it, silencing the type checker for everything that flows through it; per the no-casts rule, give it its real type (a generic parameter, a union, or the concrete domain type) instead of `any`.
