---
branch: claude/structured-review-issues-ao3cdn
commit: cc9b81fcad6d1e3dbf78ffcd53b93718d34b1a8d
base: full
mode: default
createdAt: 2026-08-18T08:21:19.219Z
isFinalized: true
groups: 20
rules: [harness-script-hygiene, no-casts, no-compat-shims, no-echo-internal-in-sdk, no-sleep-in-test, private-new-packages]
reviewId: cc9b81fc
---

_4844 error(s), 37 warning(s)._

# WARN cc9b81fc-1 harness-script-hygiene `.agents/skills/agentic-review/lib/resolution.mjs:12:14`

`STATUSES` is exported but never imported anywhere in the repo, and the module's own validation logic hardcodes the same three values as a literal in `LINE_RE`/`LEGACY_LINE_RE` instead of deriving from it — an unused export per the harness-script-hygiene rule. Either wire the regexes (and any external consumer) to read from `STATUSES`, or delete the binding.

# WARN cc9b81fc-2 harness-script-hygiene `.agents/skills/agentic-review/lib/store.mjs:21:1`

`export { RESOLUTION_FILE } from './resolution.mjs';` is a dead re-export: both consumers (`scripts/finalize.mjs`, `scripts/unresolved.mjs`) import `RESOLUTION_FILE` directly from `../lib/resolution.mjs` rather than through `store.mjs`, so this line has no importer — an unused export per the harness-script-hygiene rule. Remove the re-export, or switch the two scripts to import it through `store.mjs` if that indirection is wanted.

# ERROR cc9b81fc-3 no-casts `packages/apps/composer-app/src/main.tsx:114:32`

`const importMeta = import.meta as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-4 no-casts `packages/apps/composer-app/src/main.tsx:238:9`

`(window as any).composer = { profiler, otel };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-5 no-casts `packages/apps/composer-app/src/main.tsx:600:45`

Non-null assertion `!` in `const root = document.getElementById('root')!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-6 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:189:28`

`const eventKeyOf = (event: any): string =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-7 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:191:34`

`inventory = modules.map((module: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-8 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:201:43`

`requires: (spec.requires ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}`),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-9 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:202:43`

`provides: (spec.provides ?? []).map((tag: any) => `${String(tag.identifier)}#${String(tag.arity)}`),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-10 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:294:60`

`const modules = (data.snapshot?.modules ?? []).map((entry: any) => ({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-11 no-casts `packages/apps/composer-app/src/playwright/harness-helpers.ts:318:73`

`slowestModules: (data.snapshot?.modules ?? []).slice(0, 10).map((entry: any) => ({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-12 no-casts `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:240:7`

`args: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-13 no-casts `packages/apps/composer-app/src/playwright/plugins/inbox-http-mock.ts:242:4`

`): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-14 no-casts `packages/apps/composer-app/src/vite/trace-boot-leak.ts:44:15`

`handler(this: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-15 no-casts `packages/apps/composer-app/src/vite/trace-boot-leak.ts:62:30`

Non-null assertion `!` in `const current = queue.shift()!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-16 no-casts `packages/apps/composer-app/vite.config.ts:393:28`

Non-null assertion `!` in `const url = new URL(req.url!, `http://${req.headers.host}`);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-17 no-casts `packages/apps/composer-app/vite.config.ts:639:36`

`function chunkFileNames(chunkInfo: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-18 no-casts `packages/apps/composer-app/vite.config.ts:641:15`

`let segments: any[] = chunkInfo.facadeModuleId.split('/').reverse().slice(1);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-19 no-casts `packages/apps/composer-crx/src/background.ts:92:7`

`(msg: any, sender: browser.Runtime.MessageSender): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-20 no-casts `packages/apps/composer-crx/src/background.ts:105:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-21 no-casts `packages/apps/composer-crx/src/background.ts:121:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-22 no-casts `packages/apps/composer-crx/src/background.ts:156:57`

`browser.contextMenus.onClicked.addListener(async (info: any, tab: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-23 no-casts `packages/apps/composer-crx/src/content.ts:63:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-24 no-casts `packages/apps/composer-crx/src/content.ts:75:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-25 no-casts `packages/apps/composer-crx/src/content.ts:219:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-26 no-casts `packages/apps/composer-crx/src/content.ts:232:45`

`browser.runtime.onMessage.addListener((msg: any): undefined | Promise<unknown> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-27 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:93:14`

Non-null assertion `!` in `expect(result!.actionId).toBe('a-1');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-28 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:94:14`

Non-null assertion `!` in `expect(result!.snapshot.source.url).toBe('https://example.com/');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-29 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:95:14`

Non-null assertion `!` in `expect(result!.snapshot.source.title).toBe('Example');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-30 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:96:14`

Non-null assertion `!` in `expect(result!.snapshot.selection?.text).toBe('hello');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-31 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:97:14`

Non-null assertion `!` in `expect(result!.snapshot.selection?.html).toBe('<p>hello</p>');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-32 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:98:14`

Non-null assertion `!` in `expect(result!.snapshot.hints?.ogTitle).toBe('OG Title');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-33 no-casts `packages/apps/composer-crx/src/core/actions/deliver.test.ts:99:14`

Non-null assertion `!` in `expect(result!.snapshot.hints?.h1).toBe('Heading');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-34 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:50:42`

`expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-35 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:51:42`

`expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions[0].id).toBe('action-1');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-36 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:72:42`

`expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-37 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:152:42`

`expect((stored[PAGE_ACTIONS_STORAGE_KEY] as any).actions).toHaveLength(1);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-38 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:169:21`

`expect((messages[0] as any).message.type).toBe(PAGE_ACTIONS_LIST_MESSAGE_TYPE);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-39 no-casts `packages/apps/composer-crx/src/core/actions/registry.test.ts:170:21`

`expect((messages[0] as any).tabId).toBe(5);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-40 no-casts `packages/apps/composer-crx/src/core/extractors/snapshot.test.ts:39:21`

Non-null assertion `!` in `expect(snapshot.html!.length).toBe(MAX_HTML_LENGTH);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-41 no-casts `packages/apps/testbench-app/src/components/Error.tsx:14:22`

`const stack = (error as any)?.stack;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-42 no-casts `packages/apps/testbench-app/src/components/ItemList.tsx:64:57`

`const setValue = (object: Obj.Any, prop: string, value: any) =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-43 no-casts `packages/apps/testbench-app/src/components/ItemList.tsx:83:18`

`checked={(object as any)[property]}` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-44 no-casts `packages/apps/testbench-app/src/components/status/ErrorIndicator.tsx:17:31`

`const errorListener = (event: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-45 no-casts `packages/apps/todomvc/src/components/TodoFooter.tsx:17:19`

`onClearCompleted: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-46 no-casts `packages/apps/todomvc/src/components/TodoItem.tsx:60:14`

`const input: any = event.target;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-47 no-casts `packages/apps/todomvc/src/components/Todos.tsx:54:17`

Non-null assertion `!` in `inputRef.current!.value = '';` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-48 no-casts `packages/common/async/src/chain.ts:5:28`

`type Transform = (...args: any) => Promise<any>;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-49 no-casts `packages/common/async/src/debounce.test.ts:40:15`

`let lastArgs: any[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-50 no-casts `packages/common/async/src/debounce.test.ts:41:31`

`const fn = throttle((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-51 no-casts `packages/common/async/src/debounce.test.ts:94:15`

`let lastArgs: any[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-52 no-casts `packages/common/async/src/debounce.test.ts:95:31`

`const fn = debounce((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-53 no-casts `packages/common/async/src/debounce.ts:5:27`

`type Callback = (...args: any[]) => void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-54 no-casts `packages/common/async/src/debounce.ts:17:19`

`return ((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-55 no-casts `packages/common/async/src/debounce.ts:44:19`

`return ((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-56 no-casts `packages/common/async/src/debounce.ts:62:19`

`return ((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-57 no-casts `packages/common/async/src/debounce.ts:84:19`

`return ((...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-58 no-casts `packages/common/async/src/errors.ts:9:30`

`export const toError = (err: any) => (err === undefined || typeof err === 'string' ? new Error(err) : err);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-59 no-casts `packages/common/async/src/errors.ts:20:14`

`onError(err: any): void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-60 no-casts `packages/common/async/src/errors.ts:23:83`

`export const observableError = (observable: ObservableProvider<AsyncEvents>, err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-61 no-casts `packages/common/async/src/event-emitter.ts:13:89`

`export const onEvent = (eventEmitter: EventEmitter, eventName: string, callback: (args: any) => void) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-62 no-casts `packages/common/async/src/event-emitter.ts:34:15`

`test?: (args: any) => boolean,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-63 no-casts `packages/common/async/src/events.ts:21:31`

`on(event: string, cb: (data?: any) => void): void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-64 no-casts `packages/common/async/src/events.ts:22:32`

`off(event: string, cb: (data?: any) => void): void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-65 no-casts `packages/common/async/src/events.ts:73:24`

`const onEvent = (data: any) => event.emit(data);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-66 no-casts `packages/common/async/src/events.ts:131:10`

`on(_ctx: any, _callback?: EventCallback<T>, options?: ListenerOptions): CleanupFn {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-67 no-casts `packages/common/async/src/events.ts:168:12`

`once(_ctx: any, _callback?: (data: T) => void): CleanupFn {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-68 no-casts `packages/common/async/src/events.ts:304:13`

`return this as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-69 no-casts `packages/common/async/src/events.ts:466:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-70 no-casts `packages/common/async/src/events.ts:481:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-71 no-casts `packages/common/async/src/mutex.test.ts:131:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-72 no-casts `packages/common/async/src/mutex.test.ts:139:13`

Non-null assertion `!` in `expect(error!.stack!.includes('throwsError')).to.be.true;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-73 no-casts `packages/common/async/src/mutex.test.ts:140:13`

Non-null assertion `!` in `expect(error!.stack!.includes('callmutex')).to.be.true;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-74 no-casts `packages/common/async/src/mutex.ts:108:61`

`const enableWarning = !FORCE_DISABLE_WARNING && (globalThis as any).mochaExecutor;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-75 no-casts `packages/common/async/src/mutex.ts:116:9`

`target: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-76 no-casts `packages/common/async/src/mutex.ts:118:47`

`descriptor: TypedPropertyDescriptor<(...args: any) => Promise<any>>,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-77 no-casts `packages/common/async/src/mutex.ts:120:32`

Non-null assertion `!` in `const method = descriptor.value!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-78 no-casts `packages/common/async/src/mutex.ts:121:60`

`descriptor.value = async function synchronizedMethod(this: any & LockableClass, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-79 no-casts `packages/common/async/src/observable.test.ts:148:46`

`const concat = observable.losslessConcat((a: any, b: any) => a.concat(...b), MulticastObservable.of([4, 5, 6]));` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-80 no-casts `packages/common/async/src/persistent-lifecycle.ts:123:34`

Non-null assertion `!` in `await cancelWithContext(this._ctx!, sleep(this._restartAfter));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-81 no-casts `packages/common/async/src/persistent-lifecycle.ts:154:18`

Non-null assertion `!` in `this._restartTask!.schedule();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-82 no-casts `packages/common/async/src/push-iterable.ts:23:15`

`throw(value?: any): void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-83 no-casts `packages/common/async/src/push-iterable.ts:30:104`

`const buf: ({ kind: 'next'; value: T } | { kind: 'return'; value: TReturn } | { kind: 'throw'; value?: any })[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-84 no-casts `packages/common/async/src/push-iterable.ts:63:17`

`throw: (value?: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-85 no-casts `packages/common/async/src/stream-to-array.ts:24:10`

`let arr: any[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-86 no-casts `packages/common/async/src/stream-to-array.ts:26:22`

`const onData = (doc: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-87 no-casts `packages/common/async/src/stream-to-array.ts:30:21`

`const onEnd = (err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# WARN cc9b81fc-88 no-sleep-in-test `packages/common/async/src/task-scheduling.test.ts:39`

`await sleep(2)` gates `expect(called).to.be.false` right after disposing the context — sleep used as the negative-assertion synchronization mechanism. Prefer awaiting the task's own completion/cancellation signal.

# ERROR cc9b81fc-89 no-casts `packages/common/async/src/task-scheduling.ts:181:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-90 no-casts `packages/common/async/src/task-scheduling.ts:189:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-91 no-casts `packages/common/async/src/test-stream.ts:32:24`

`override _write(chunk: any, encoding: BufferEncoding, callback: (error?: Error | null) => void): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-92 no-casts `packages/common/async/src/testing.ts:17:65`

`export const waitForCondition = <FunctionType extends (...args: any) => any>({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-93 no-casts `packages/common/async/src/testing.ts:42:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-94 no-casts `packages/common/async/src/track-leaks.ts:44:10`

`(target: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-95 no-casts `packages/common/async/src/track-leaks.ts:56:48`

`target.prototype[open] = async function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-96 no-casts `packages/common/async/src/track-leaks.ts:68:49`

`target.prototype[close] = async function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-97 no-casts `packages/common/async/src/track-leaks.ts:91:9`

`(global as any).dxDumpLeaks = dumpLeaks;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-98 no-casts `packages/common/codec-protobuf/src/buffer-patch.ts:14:13`

`encode: (x: any) => arrayToBuffer(codec.encode(x)),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-99 no-casts `packages/common/codec-protobuf/src/codec.ts:91:17`

`fromObject(obj: any): T {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-100 no-casts `packages/common/codec-protobuf/src/codec.ts:98:17`

`addJson(schema: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-101 no-casts `packages/common/codec-protobuf/src/codec.ts:102:56`

`encodeToJson(value: T, options: EncodingOptions = {}): any {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-102 no-casts `packages/common/codec-protobuf/src/codec.ts:107:22`

`decodeFromJson(data: any, options: EncodingOptions = {}): T {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-103 no-casts `packages/common/codec-protobuf/src/common.ts:18:17`

`decode: (value: any, context: TypeMapperContext, schema: Schema<any>, options: EncodingOptions) => T;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-104 no-casts `packages/common/codec-protobuf/src/compression.ts:7:38`

`export const compressSchema = (data: any): any => compress(data);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-105 no-casts `packages/common/codec-protobuf/src/compression.ts:9:40`

`export const decompressSchema = (data: any): any => decompress(data);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-106 no-casts `packages/common/codec-protobuf/src/mapping.ts:20:34`

`export type TypeMapper = (value: any, context: TypeMapperContext, ...extraArgs: any) => any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-107 no-casts `packages/common/codec-protobuf/src/mapping.ts:42:35`

`export type FieldMapper = (value: any, typeName: string) => Promise<any>;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-108 no-casts `packages/common/codec-protobuf/src/mapping.ts:44:83`

`export const mapMessage = async (type: protobufjs.Type, mapper: FieldMapper, obj: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-109 no-casts `packages/common/codec-protobuf/src/mapping.ts:45:12`

`const res: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-110 no-casts `packages/common/codec-protobuf/src/mapping.ts:56:78`

`const mapField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-111 no-casts `packages/common/codec-protobuf/src/mapping.ts:60:44`

`return await Promise.all(value.map((value: any) => mapScalarField(field, mapper, value)));` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-112 no-casts `packages/common/codec-protobuf/src/mapping.ts:69:84`

`const mapScalarField = async (field: protobufjs.Field, mapper: FieldMapper, value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-113 no-casts `packages/common/codec-protobuf/src/mapping.ts:90:30`

`const res: Record<K, U> = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-114 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:9:8`

`value: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-115 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:35:28`

`export const ref = (value: any): Ref => ({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-116 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:40:23`

`const isRef = (value: any): value is Ref => value[Ref] === true;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-117 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:72:49`

`gen: (c: (parts: TemplateStringsArray, ...args: any[]) => void) => void,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-118 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:74:15`

`): ((...args: any[]) => any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-119 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/codegen.ts:80:29`

`const preprocessArg = (arg: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-120 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:12:28`

`export type Mapper = (obj: any, extraArgs: any[]) => any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-121 no-casts `packages/common/codec-protobuf/src/precompiled-mapping/create-message-mapper.ts:24:27`

`cache[type.fullName] = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-122 no-casts `packages/common/codec-protobuf/src/sanitizer.ts:18:48`

`export const sanitize = (type: pb.Type, value: any, path: string, context: SanitizeContext) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-123 no-casts `packages/common/codec-protobuf/src/sanitizer.ts:46:45`

`const sanitizeEnum = (type: pb.Enum, value: any, path: string, context: SanitizeContext): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-124 no-casts `packages/common/codec-protobuf/src/schema.ts:15:9`

`schema: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-125 no-casts `packages/common/codec-protobuf/src/schema.ts:107:17`

`addJson(schema: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-126 no-casts `packages/common/codec-protobuf/src/service.ts:69:7`

`(this as any)[methodName] = (request: unknown, requestOptions?: RequestOptions) => {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-127 no-casts `packages/common/codec-protobuf/src/service.ts:75:37`

Non-null assertion `!` in `type_url: method.resolvedRequestType!.fullName,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-128 no-casts `packages/common/codec-protobuf/src/service.ts:79:68`

Non-null assertion `!` in `return Stream.map(stream, (data) => responseCodec.decode(data.value!, encodingOptions));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-129 no-casts `packages/common/codec-protobuf/src/service.ts:82:7`

`(this as any)[methodName] = async (request: unknown, requestOptions?: RequestOptions) => {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-130 no-casts `packages/common/codec-protobuf/src/service.ts:88:37`

Non-null assertion `!` in `type_url: method.resolvedRequestType!.fullName,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-131 no-casts `packages/common/codec-protobuf/src/service.ts:97:29`

`Object.defineProperty((this as any)[methodName], 'name', {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-132 no-casts `packages/common/codec-protobuf/src/service.ts:126:57`

Non-null assertion `!` in `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-133 no-casts `packages/common/codec-protobuf/src/service.ts:132:38`

Non-null assertion `!` in `type_url: method.resolvedResponseType!.fullName,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-134 no-casts `packages/common/codec-protobuf/src/service.ts:147:57`

Non-null assertion `!` in `const requestDecoded = requestCodec.decode(request.value!, this._encodingOptions);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-135 no-casts `packages/common/codec-protobuf/src/service.ts:153:38`

Non-null assertion `!` in `type_url: method.resolvedResponseType!.fullName,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-136 no-casts `packages/common/codec-protobuf/src/service.ts:161:17`

`return (handler as any).bind(service);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-137 no-casts `packages/common/codec-protobuf/src/stream.ts:231:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-138 no-casts `packages/common/codec-protobuf/src/stream.ts:251:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-139 no-casts `packages/common/codec-protobuf/src/stream.ts:262:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-140 no-casts `packages/common/codec-protobuf/src/stream.ts:284:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-141 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:17:4`

`): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-142 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:33:14`

`return value as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-143 no-casts `packages/common/codec-protobuf/src/substitutions/any.ts:46:8`

`value: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-144 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:7:41`

`const encodeStructValue = (structValue: any, visitedObjects: WeakSet<any>): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-145 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:43:68`

`const encodeStruct = (struct: Struct, visitedObjects = new WeakSet<any>()): any => ({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-146 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:49:41`

`const decodeStructValue = (structValue: any): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-147 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:75:31`

`const decodeStruct = (struct: any): Struct =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-148 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:80:26`

`encode: (value: Struct): any => encodeStruct(value),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-149 no-casts `packages/common/codec-protobuf/src/substitutions/struct.ts:81:17`

`decode: (value: any): Struct => decodeStruct(value),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-150 no-casts `packages/common/codec-protobuf/src/substitutions/timestamp.ts:7:24`

`encode: (value: Date): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-151 no-casts `packages/common/codec-protobuf/src/substitutions/timestamp.ts:15:17`

`decode: (value: any): Date => new Date(parseInt(value.seconds ?? '0') * 1000 + (value.nanos ?? 0) / 1e6),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-152 no-casts `packages/common/context/src/context.test.ts:44:19`

`expect((undefined as any) instanceof Context).toBeFalsy();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-153 no-casts `packages/common/context/src/context.ts:130:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-154 no-casts `packages/common/context/src/context.ts:195:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-155 no-casts `packages/common/context/src/context.ts:260:28`

`getAttribute(key: string): any {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-156 no-casts `packages/common/context/src/resource.ts:92:21`

`} catch (doubleErr: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-157 no-casts `packages/common/context/src/resource.ts:188:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-158 no-casts `packages/common/debug/src/devtools-formatter.ts:28:19`

`header: (config?: any) => JsonML | null;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-159 no-casts `packages/common/debug/src/devtools-formatter.ts:29:21`

`hasBody?: (config?: any) => boolean;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-160 no-casts `packages/common/debug/src/devtools-formatter.ts:30:18`

`body?: (config?: any) => JsonML | null;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-161 no-casts `packages/common/debug/src/devtools-formatter.ts:44:10`

`((window as any).devtoolsFormatters ??= []).push({` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-162 no-casts `packages/common/debug/src/devtools-formatter.ts:45:17`

`header: (value: any, config: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-163 no-casts `packages/common/debug/src/devtools-formatter.ts:56:18`

`hasBody: (value: any, config: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-164 no-casts `packages/common/debug/src/devtools-formatter.ts:64:15`

`body: (value: any, config: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-165 no-casts `packages/common/debug/src/equality.ts:8:25`

`[equalsSymbol]: (other: any) => boolean;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-166 no-casts `packages/common/debug/src/equality.ts:14:36`

`export const isEquatable = (value: any): value is Equatable => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-167 no-casts `packages/common/debug/src/equality.ts:18:50`

`export const isEqual = (value: Equatable, other: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-168 no-casts `packages/common/debug/src/equality.ts:25:42`

`export const loadashEqualityFn = (value: any, other: any): boolean | undefined => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-169 no-casts `packages/common/debug/src/exposed-modules.ts:19:52`

`export const exposeModule = (name: string, module: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-170 no-casts `packages/common/debug/src/inspect.ts:22:36`

`export const inspectObject = (obj: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-171 no-casts `packages/common/debug/src/log-method.ts:8:9`

`target: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-172 no-casts `packages/common/debug/src/log-method.ts:10:47`

`descriptor: TypedPropertyDescriptor<(...args: any) => any>,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-173 no-casts `packages/common/debug/src/log-method.ts:12:32`

Non-null assertion `!` in `const method = descriptor.value!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-174 no-casts `packages/common/debug/src/log-method.ts:13:36`

`descriptor.value = function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-175 no-casts `packages/common/debug/src/log-method.ts:18:20`

`result.catch((err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-176 no-casts `packages/common/debug/src/log-method.ts:23:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-177 no-casts `packages/common/debug/src/snoop.ts:19:24`

Non-null assertion `!` in `const stack = err.stack!.split('\n');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-178 no-casts `packages/common/debug/src/snoop.ts:44:47`

`in(label: string, level: SnoopLevel, ...args: any[]): string {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-179 no-casts `packages/common/debug/src/snoop.ts:48:47`

`out(label: string, level: SnoopLevel, result: any): string {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-180 no-casts `packages/common/debug/src/snoop.ts:52:9`

`sync(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-181 no-casts `packages/common/debug/src/snoop.ts:54:18`

`return (...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-182 no-casts `packages/common/debug/src/snoop.ts:62:10`

`async(f: any, label?: string, level: SnoopLevel = SnoopLevel.VERBOSE) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-183 no-casts `packages/common/debug/src/snoop.ts:64:24`

`return async (...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-184 no-casts `packages/common/debug/src/strings.ts:13:34`

`export const truncateKey = (key: any, length = 8) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-185 no-casts `packages/common/debug/src/timeout-warning.ts:46:17`

`return (target: any, propertyName: string, descriptor: TypedPropertyDescriptor<(...args: any) => any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-186 no-casts `packages/common/debug/src/timeout-warning.ts:47:32`

Non-null assertion `!` in `const method = descriptor.value!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-187 no-casts `packages/common/debug/src/timeout-warning.ts:48:36`

`descriptor.value = function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-188 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:43:15`

Non-null assertion `!` in `return options!.mode === 'promise' ? promise.then(flattenExit) : promise;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-189 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSet.ts:47:57`

`registry.set(atom, typeof value === 'function' ? (value as any)(registry.get(atom)) : value);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-190 no-casts `packages/common/effect-atom-solid/src/hooks/useAtomSuspense.ts:55:45`

`} else if (next._tag === 'Initial' || (next as any).waiting) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-191 no-casts `packages/common/effect-atom-solid/src/registry.ts:36:11`

`children: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-192 no-casts `packages/common/effect-zod/src/effect-to-zod.ts:52:32`

`out[name] = propToZod(prop.ast as unknown as AnyAst);` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-193 no-casts `packages/common/effect/src/dynamic-runtime.test.ts:284:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-194 no-casts `packages/common/effect/src/dynamic-runtime.test.ts:308:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-195 no-casts `packages/common/effect/src/internal/async-task-tagging.ts:27:24`

`const trace = (console as any).createTask(name);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-196 no-casts `packages/common/effect/src/internal/async-task-tagging.ts:28:7`

`(span as any)[runInTask] = (f: any) => trace.run(f);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-197 no-casts `packages/common/effect/src/internal/errors.ts:23:34`

`const prettyErrorStack = (error: any, appendStacks: string[] = []): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-198 no-casts `packages/common/effect/src/internal/errors.ts:151:17`

Non-null assertion `!` in `return err.stack!.split('\n').slice(1);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-199 no-casts `packages/common/effect/src/internal/errors.ts:233:15`

`} = (...args: any[]): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-200 no-casts `packages/common/effect/src/internal/json-path.ts:114:34`

`export const getField = (object: any, path: JsonPath): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-201 no-casts `packages/common/effect/src/internal/json-path.ts:122:69`

`export const getValue = <T extends object>(obj: T, path: JsonPath): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-202 no-casts `packages/common/effect/src/internal/json-path.ts:129:75`

`export const setValue = <T extends object>(obj: T, path: JsonPath, value: any): T => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-203 no-casts `packages/common/effect/src/sanity.test.ts:68:16`

`.catch((error: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-204 no-casts `packages/common/errors/src/errors.test.ts:27:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-205 no-casts `packages/common/errors/src/errors.test.ts:30:31`

Non-null assertion `!` in `const stackLines = error.stack!.split('\n');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-206 no-casts `packages/common/feed-store/src/feed-factory.ts:35:33`

`onwrite?: (index: number, data: any, peer: null, cb: (err: Error | null) => void) => void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-207 no-casts `packages/common/feed-store/src/feed-queue.browser.test.ts:31:41`

Non-null assertion `!` in `await localBuilder._properties.generator!.writeBlocks(feed.createFeedWriter(), { count: 10 });` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-208 no-casts `packages/common/feed-store/src/feed-queue.ts:99:15`

`write: (data: any, next: () => void) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-209 no-casts `packages/common/feed-store/src/feed-store.node.test.ts:15:38`

Non-null assertion `!` in `const feedKey = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-210 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:57:34`

Non-null assertion `!` in `const key = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-211 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:71:34`

Non-null assertion `!` in `const key = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-212 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:89:34`

Non-null assertion `!` in `const key = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-213 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:99:3`

`} as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-214 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:144:35`

Non-null assertion `!` in `const key1 = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-215 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:198:35`

Non-null assertion `!` in `const key1 = await builder.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-216 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:255:36`

Non-null assertion `!` in `const key1 = await builder1.keyring!.createKey();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-217 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:263:33`

`await feed1.append(`block-${i}` as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-218 no-casts `packages/common/feed-store/src/feed-wrapper.test.ts:301:45`

`await source.append(random.lorem.sentence() as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-219 no-casts `packages/common/feed-store/src/feed-wrapper.ts:74:17`

`transform(data: any, cb: (err?: Error | null, data?: any) => void) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-220 no-casts `packages/common/feed-store/src/feed-wrapper.ts:87:34`

`readStream.pipe(transform, (err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-221 no-casts `packages/common/feed-store/src/feed-wrapper.ts:168:13`

`on(...args: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-222 no-casts `packages/common/feed-store/src/feed-wrapper.ts:169:25`

`return (this._hypercore as any).on(...args);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-223 no-casts `packages/common/feed-store/src/feed-wrapper.ts:172:14`

`off(...args: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-224 no-casts `packages/common/feed-store/src/feed-wrapper.ts:173:25`

`return (this._hypercore as any).off(...args);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-225 no-casts `packages/common/feed-store/src/feed-wrapper.ts:177:61`

`return promisify(this._hypercore.open.bind(this._hypercore) as any)(...args);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-226 no-casts `packages/common/feed-store/src/feed-wrapper.ts:181:62`

`return promisify(this._hypercore.close.bind(this._hypercore) as any)(...args);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-227 no-casts `packages/common/feed-store/src/feed-wrapper.ts:202:60`

`return promisify(this._hypercore.get.bind(this._hypercore) as any)(index, options);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-228 no-casts `packages/common/feed-store/src/feed-wrapper.ts:206:14`

`append(data: any | any[]): Promise<number> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-229 no-casts `packages/common/feed-store/src/feed-wrapper.ts:233:32`

`proof(index: number, options?: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-230 no-casts `packages/common/feed-store/src/feed-wrapper.ts:242:35`

`return promisify((this._hypercore as any)._putBuffer.bind(this._hypercore) as any)(index, data, proof, peer);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-231 no-casts `packages/common/feed-store/src/feed-wrapper.ts:258:49`

`valueEncoding: { decode: (x: Uint8Array) => x } as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-232 no-casts `packages/common/feed-store/src/feed-wrapper.ts:268:49`

`valueEncoding: { decode: (x: Uint8Array) => x } as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-233 no-casts `packages/common/feed-store/src/feed-wrapper.ts:307:24`

Non-null assertion `!` in `if (this._feed.bitfield!.total(this._cursor, this._cursor + this._batch) === this._batch) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-234 no-casts `packages/common/feed-store/src/testing/test-generator.ts:20:15`

`encode: (obj: any) => Buffer.from(JSON.stringify(obj)),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-235 no-casts `packages/common/graph/src/Graph.ts:25:49`

`export type Any = Specialize<BaseNode, { data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-236 no-casts `packages/common/graph/src/Graph.ts:44:49`

`export type Any = Specialize<BaseEdge, { data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-237 no-casts `packages/common/hypercore/src/hypercore-protocol.test.ts:94:18`

`onmessage: (msg: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-238 no-casts `packages/common/hypercore/src/hypercore-protocol.test.ts:104:18`

`onmessage: (msg: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-239 no-casts `packages/common/hypercore/src/hypercore.test.ts:52:14`

`const block: any = await get(0);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-240 no-casts `packages/common/hypercore/src/iterator.ts:22:55`

`return new Readable({ objectMode: true }).wrap(stream as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-241 no-casts `packages/common/hypercore/src/replicate.test.ts:351:23`

`const onClose = (err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-242 no-casts `packages/common/hypercore/src/streams.test.ts:28:15`

`write: (data: any, next: () => void) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-243 no-casts `packages/common/hypercore/src/util.ts:7:25`

`export const py = (obj: any, fn: Function) => promisify(fn.bind(obj));` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-244 no-casts `packages/common/invariant/src/meta.ts:25:4`

`S: any | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-245 no-casts `packages/common/invariant/src/meta.ts:31:26`

`C?: (fn: Function, args: any[]) => void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-246 no-casts `packages/common/keyboard/src/keyboard.test.ts:30:3`

`} as unknown as KeyboardEvent);` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-247 no-casts `packages/common/keyboard/src/keyboard.ts:13:8`

`data?: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-248 no-casts `packages/common/keyboard/src/keyboard.ts:21:8`

`data?: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-249 no-casts `packages/common/keyboard/src/keyboard.ts:136:31`

`const tagName = (event.target as any)?.tagName;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-250 no-casts `packages/common/keyboard/src/keyboard.ts:138:64`

`tagName === 'INPUT' || tagName === 'TEXTAREA' || (event.target as any)?.getAttribute('contenteditable');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-251 no-casts `packages/common/keys/src/DXN.ts:222:3`

`) as unknown as Schema.Codec<DXN, DXN>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-252 no-casts `packages/common/keys/src/EID.ts:166:3`

`) as unknown as Schema.Codec<EID, EID>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-253 no-casts `packages/common/keys/src/public-key.ts:32:35`

`const isLikeArrayBuffer = (value: any): value is ArrayBuffer =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-254 no-casts `packages/common/keys/src/public-key.ts:85:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-255 no-casts `packages/common/keys/src/public-key.ts:126:27`

`static isPublicKey(value: any): value is PublicKey {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-256 no-casts `packages/common/keys/src/public-key.ts:133:36`

`static assertValidPublicKey(value: any): asserts value is PublicKey {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-257 no-casts `packages/common/keys/src/public-key.ts:265:61`

Non-null assertion `!` in `return `PublicKey(${printControlCode(inspectFn.colors[color]![0])}${this.truncate()}${printControlCode(` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-258 no-casts `packages/common/keys/src/public-key.ts:266:23`

Non-null assertion `!` in `inspectFn.colors.reset![0],` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-259 no-casts `packages/common/keys/src/public-key.ts:319:23`

`[equalsSymbol](other: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-260 no-casts `packages/common/keys/src/URI.ts:34:3`

`) as unknown as Schema.Codec<URI, URI>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-261 no-casts `packages/common/lock-file/src/sys.ts:70:26`

Non-null assertion `!` in `const errno = this._koffi!.errno();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-262 no-casts `packages/common/log-store-idb/src/idb-log-store.browser.test.ts:23:3`

`} as unknown as LogEntry;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-263 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:333:27`

`const locks = (globalThis as any).navigator?.locks as LockManager | undefined;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-264 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:367:39`

Non-null assertion `!` in `byteLength: byteLengthUtf8(rows[index]!.lines),` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-265 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:384:17`

`if ((globalThis as any).document?.visibilityState === 'hidden') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-266 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:503:31`

Non-null assertion `!` in `parts.push('\n', chunks[index]!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-267 no-casts `packages/common/log-store-idb/src/idb-log-store.ts:528:28`

Non-null assertion `!` in `const chunk = chunks[index]!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-268 no-casts `packages/common/log/src/context.ts:224:25`

`const c = (entryContext as any).context;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-269 no-casts `packages/common/log/src/context.ts:280:23`

`mergeInto((rawContext as any).context);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-270 no-casts `packages/common/log/src/context.ts:286:24`

`mergeInto((entry.error as any).context);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-271 no-casts `packages/common/log/src/context.ts:340:28`

`const ctxErr = (rawContext as any).error ?? (rawContext as any).err;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-272 no-casts `packages/common/log/src/decorators.ts:16:10`

`(target: any, propertyKey: string | symbol, descriptor: PropertyDescriptor) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-273 no-casts `packages/common/log/src/decorators.ts:17:32`

Non-null assertion `!` in `const method = descriptor.value!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-274 no-casts `packages/common/log/src/decorators.ts:19:36`

`descriptor.value = function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-275 no-casts `packages/common/log/src/decorators.ts:24:9`

`S: this as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-276 no-casts `packages/common/log/src/decorators.ts:27:38`

`const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-277 no-casts `packages/common/log/src/decorators.ts:49:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-278 no-casts `packages/common/log/src/decorators.ts:59:22`

`<F extends (...args: any[]) => any>(` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-279 no-casts `packages/common/log/src/decorators.ts:64:37`

`const decoratedFn = function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-280 no-casts `packages/common/log/src/decorators.ts:70:38`

`const formattedArgs = args.map((arg: any) => inspect(arg, false, 1, true)).join(', ');` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-281 no-casts `packages/common/log/src/decorators.ts:79:54`

`transformedResult = result.then(opts.transformOutput as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-282 no-casts `packages/common/log/src/decorators.ts:101:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-283 no-casts `packages/common/log/src/decorators.ts:110:26`

`const isThenable = (obj: any): obj is Promise<unknown> => obj && typeof obj.then === 'function';` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-284 no-casts `packages/common/log/src/environment.test.ts:199:89`

`const scope = makeWorkerScope(FakeServiceWorkerGlobalScope, 'ServiceWorkerGlobalScope') as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-285 no-casts `packages/common/log/src/environment.ts:18:31`

`const cryptoRef = (globalThis as any).crypto as Crypto | undefined;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-286 no-casts `packages/common/log/src/environment.ts:58:21`

`const ctor = (scope as any)?.[ctorName];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-287 no-casts `packages/common/log/src/environment.ts:108:40`

`const computeEnvironmentName = (scope: any): string => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-288 no-casts `packages/common/log/src/experimental/ownership.ts:16:18`

`public instance: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-289 no-casts `packages/common/log/src/experimental/ownership.ts:19:16`

`public constr: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-290 no-casts `packages/common/log/src/experimental/ownership.ts:28:13`

`const info: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-291 no-casts `packages/common/log/src/experimental/ownership.ts:44:55`

`function decorateMethodWeakReturnOwnership(prototype: any, key: string) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-292 no-casts `packages/common/log/src/experimental/ownership.ts:46:37`

`prototype[key] = function (...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-293 no-casts `packages/common/log/src/experimental/ownership.ts:50:18`

`res.then((value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-294 no-casts `packages/common/log/src/experimental/ownership.ts:65:58`

`export function ownershipClass<T extends { new (...args: any[]): {} }>(constr: T) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-295 no-casts `packages/common/log/src/experimental/ownership.ts:73:22`

`constructor(...args: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-296 no-casts `packages/common/log/src/experimental/ownership.ts:74:38`

`const currentCausality = (globalThis as any)[kCurrentOwnershipScope];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-297 no-casts `packages/common/log/src/experimental/ownership.ts:75:13`

`(globalThis as any)[kCurrentOwnershipScope] = new OwnershipScope(constr, currentCausality);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-298 no-casts `packages/common/log/src/experimental/ownership.ts:77:7`

`(this as any)[kOwnershipScope] = (globalThis as any)[kCurrentOwnershipScope];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-299 no-casts `packages/common/log/src/experimental/ownership.ts:78:7`

`(this as any)[kOwnershipScope].instance = this;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-300 no-casts `packages/common/log/src/experimental/ownership.ts:79:13`

`(globalThis as any)[kCurrentOwnershipScope] = currentCausality;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-301 no-casts `packages/common/log/src/experimental/ownership.ts:84:35`

`export const debugInfo = (target: any, propertyKey: string, descriptor: PropertyDescriptor) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-302 no-casts `packages/common/log/src/experimental/ownership.ts:89:51`

`export const getCurrentOwnershipScope = (thisRef: any) => thisRef;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-303 no-casts `packages/common/log/src/jsonl.test.ts:116:12`

Non-null assertion `!` in `expect(line!.indexOf('\n')).toBe(-1);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-304 no-casts `packages/common/log/src/jsonl.test.ts:118:31`

Non-null assertion `!` in `const record = JSON.parse(line!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-305 no-casts `packages/common/log/src/log-buffer.test.ts:107:16`

Non-null assertion `!` in `expect(record.c!.length).toBe(500);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-306 no-casts `packages/common/log/src/log-buffer.test.ts:129:27`

Non-null assertion `!` in `expect(JSON.parse(record.c!).self).toBe('[object Object]');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-307 no-casts `packages/common/log/src/log.test.ts:15:28`

`private readonly context?: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-308 no-casts `packages/common/log/src/log.test.ts:77:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-309 no-casts `packages/common/log/src/log.test.ts:85:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-310 no-casts `packages/common/log/src/log.test.ts:93:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-311 no-casts `packages/common/log/src/log.ts:83:32`

`function: <F extends (...args: any[]) => any>(` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-312 no-casts `packages/common/log/src/log.ts:207:38`

`export const log: Log = ((globalThis as any).DX_LOG ??= createLog());` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-313 no-casts `packages/common/log/src/log.ts:215:31`

`export const debug = (label?: any, args?: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-314 no-casts `packages/common/log/src/log.ts:227:55`

Non-null assertion `!` in `const getFormattedStackTrace = () => new Error().stack!.split('\n').slice(3).join('\n');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-315 no-casts `packages/common/log/src/meta.ts:41:6`

`'S': any | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-316 no-casts `packages/common/log/src/meta.ts:47:28`

`'C'?: (fn: Function, args: any[]) => void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-317 no-casts `packages/common/log/src/options.ts:59:14`

Non-null assertion `!` in `file: process!.env.LOG_CONFIG,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-318 no-casts `packages/common/log/src/options.ts:60:16`

Non-null assertion `!` in `filter: process!.env.LOG_FILTER,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-319 no-casts `packages/common/log/src/options.ts:61:19`

Non-null assertion `!` in `processor: process!.env.LOG_PROCESSOR,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-320 no-casts `packages/common/log/src/platform/browser/index.ts:14:17`

`if ((globalThis as any).localStorage_dxlog) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-321 no-casts `packages/common/log/src/platform/browser/index.ts:15:21`

`dxlog = (globalThis as any).localStorage_dxlog;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-322 no-casts `packages/common/log/src/processors/browser-processor.ts:76:15`

`const levels: any = {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-323 no-casts `packages/common/log/src/processors/browser-processor.ts:121:15`

`const levels: any = {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-324 no-casts `packages/common/log/src/processors/browser-processor.ts:144:35`

`const unwrapEffectError = (error: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-325 no-casts `packages/common/log/src/processors/console-processor.ts:35:11`

`context?: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-326 no-casts `packages/common/log/src/processors/console-processor.ts:37:9`

`scope?: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-327 no-casts `packages/common/log/src/processors/file-processor.ts:67:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-328 no-casts `packages/common/log/src/processors/file-processor.ts:90:19`

Non-null assertion `!` in `return logFilePath!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-329 no-casts `packages/common/log/src/scope.ts:19:33`

`export const logInfo = (target: any, propertyKey: string, descriptor?: PropertyDescriptor) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-330 no-casts `packages/common/log/src/scope.ts:28:47`

`export const gatherLogInfoFromScope = (scope: any): Record<string, any> => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-331 no-casts `packages/common/log/src/scope.ts:40:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-332 no-casts `packages/common/merkle-search-tree/src/forest.ts:96:15`

Non-null assertion `!` in `invariant(node!.level > 0);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-333 no-casts `packages/common/merkle-search-tree/src/forest.ts:97:28`

Non-null assertion `!` in `node = this.#nodes.get(node!.children[node!.items.length]);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-334 no-casts `packages/common/merkle-search-tree/src/forest.ts:194:79`

Non-null assertion `!` in `return await this.merge(root, newTree, async (key, value1, value2) => (value2 !== null ? value2 : value1!));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-335 no-casts `packages/common/merkle-search-tree/src/forest.ts:305:39`

Non-null assertion `!` in `const key = (item1?.key ?? item2?.key)!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-336 no-casts `packages/common/protobuf-compiler/src/main.ts:24:71`

`parser.add_argument('-v', '--version', { action: 'version', version } as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-337 no-casts `packages/common/protobuf-compiler/src/parser/resolver.ts:19:24`

`return function (this: any, origin, target) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-338 no-casts `packages/common/protobuf-compiler/src/protobuf-json.ts:9:14`

`[K: string]: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-339 no-casts `packages/common/protobuf-compiler/src/protobuf-json.ts:29:61`

`export const serializeSchemaToJson = (root: protobuf.Root): any =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-340 no-casts `packages/common/protobuf-compiler/src/util.ts:5:36`

`export const getFlags = (enumType: any, flags: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-341 no-casts `packages/common/protobuf-compiler/test/any.ts:7:16`

`[key: string]: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-342 no-casts `packages/common/protobuf-compiler/test/proto/substitutions.ts:15:17`

`decode: (value: any) => new MyKey(value.data),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-343 no-casts `packages/common/protobuf-compiler/test/proto/substitutions.ts:19:17`

`decode: (value: any): MyInterfaceSubstitution => value,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-344 no-casts `packages/common/protobuf-compiler/test/types.test.ts:67:54`

`const encoded = codec.encode({ badname: 'badvalue' } as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-345 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:37:19`

Non-null assertion `!` in `database.getdb(res!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-346 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:70:7`

`(file as any).destroyed = true;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-347 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:71:7`

`(file as any).unlinked = true;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-348 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:72:7`

`(file as any).closed = true;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-349 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:78:7`

`(file as any).deletable = true;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-350 no-casts `packages/common/random-access-storage/src/browser/idb-storage.ts:96:21`

Non-null assertion `!` in `reject(request.error!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-351 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:55:33`

`for await (const entry of (root as any).keys()) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-352 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:89:56`

`getOrCreateFile(path: string, filename: string, opts?: any): File {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-353 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:115:34`

`await file.destroy().catch((err: any) => log.warn(err));` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-354 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:123:48`

`for await (const filename of await (this._root as any).keys()) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-355 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:124:17`

Line combines two casts the no-casts rule bans: the non-null assertion `this._root!` forces past a possibly-unset field, and `(err: any)` widens the catch parameter to `any`. Narrow `_root`'s type so it is checked instead of asserted, and give `err` a concrete type (e.g. `unknown` with a type guard, or the specific error type) instead of `any`.

# ERROR cc9b81fc-356 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:155:35`

`for await (const entry of (handle as any).values()) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-357 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:172:25`

Non-null assertion `!` in `await recurse(this._root!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-358 no-casts `packages/common/random-access-storage/src/browser/web-fs.ts:252:3`

`} as any as RandomAccessStorage;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-359 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:65:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-360 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:81:66`

`protected getOrCreateFile(path: string, filename: string, opts?: any): File {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-361 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:114:71`

`protected abstract _createFile(path: string, filename: string, opts?: any): RandomAccessStorage;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-362 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:134:90`

`Array.from((await this._getFiles(path)).values()).map((file) => file.close().catch((err: any) => log.catch(err))),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-363 no-casts `packages/common/random-access-storage/src/common/abstract-storage.ts:149:14`

`.catch((err: any) => log.error(err.message));` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-364 no-casts `packages/common/random-access-storage/src/common/directory.ts:14:58`

`getOrCreateFile: (path: string, filename: string, opts?: any) => File;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-365 no-casts `packages/common/random-access-storage/src/common/directory.ts:27:76`

`private readonly _getOrCreateFile: (path: string, filename: string, opts?: any) => File;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-366 no-casts `packages/common/random-access-storage/src/common/directory.ts:67:42`

`getOrCreateFile(filename: string, opts?: any): File {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-367 no-casts `packages/common/random-access-storage/src/common/file.ts:48:29`

`const pifyFields = (object: any, type: StorageType, fields: string[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-368 no-casts `packages/common/random-access-storage/src/common/file.ts:55:33`

`object[field] = async (...args: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-369 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:26:10`

`(newFile as any).closed = false;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-370 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:39:34`

Non-null assertion `!` in `return cb(err, arrayToBuffer(data!));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-371 no-casts `packages/common/random-access-storage/src/common/memory-storage.ts:50:20`

`const size = (file as any).length;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-372 no-casts `packages/common/random-access-storage/src/node/node-storage.ts:44:70`

`protected override _createFile(path: string, filename: string, opts: any = {}): RandomAccessStorage {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-373 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:175:26`

`sqlite3.vfs_register(vfs as any, false);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-374 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:214:38`

`sqlite3.bind_collection(stmt, params as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-375 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:220:36`

Non-null assertion `!` in `for (let index = 0; index < columns!.length; index++) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-376 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:221:12`

Non-null assertion `!` in `obj[columns![index]] = row[index];` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-377 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:262:38`

`sqlite3.bind_collection(stmt, params as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-378 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:267:36`

Non-null assertion `!` in `for (let index = 0; index < columns!.length; index++) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-379 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:268:12`

Non-null assertion `!` in `obj[columns![index]] = row[index];` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-380 no-casts `packages/common/sql-sqlite/src/internal/opfs-client.ts:355:3`

`) as unknown as WasmSqliteClient.SqliteClient;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-381 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:70:26`

`sqlite3.vfs_register(vfs as any, false);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-382 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:97:27`

`const onMessage = (event: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-383 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:158:38`

`sqlite3.bind_collection(stmt, params as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-384 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:188:13`

`} catch (e: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-385 no-casts `packages/common/sql-sqlite/src/OpfsWorker.ts:193:36`

Non-null assertion `!` in `options.port.postMessage([messageId!, message, undefined]);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-386 no-casts `packages/common/sql-sqlite/src/testing/00-opfs-pool-sync.browser.test.ts:89:12`

Non-null assertion `!` in `expect(dxos!.payloadBytes).toBeGreaterThanOrEqual(source.byteLength);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-387 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:81:45`

`console.log({ reg: sqlite3.vfs_register(vfs as any, false) });` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-388 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:95:31`

`sqlite3.update_hook(db, (_op: any, _db: any, table: any, rowid: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-389 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:110:38`

`sqlite3.bind_collection(stmt, params as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-390 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:116:28`

Non-null assertion `!` in `for (let i = 0; i < columns!.length; i++) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-391 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:117:12`

Non-null assertion `!` in `obj[columns![i]] = row[i];` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-392 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:148:38`

`sqlite3.bind_collection(stmt, params as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-393 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:153:28`

Non-null assertion `!` in `for (let i = 0; i < columns!.length; i++) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-394 no-casts `packages/common/sql-sqlite/src/testing/sqlite-effect-idb.browser.test.ts:154:12`

Non-null assertion `!` in `obj[columns![i]] = row[i];` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-395 no-casts `packages/common/sql-sqlite/src/testing/sqlite-idb.browser.test.ts:35:10`

`let vfs: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-396 no-casts `packages/common/storybook-utils/src/stories/test/Test.stories.tsx:32:39`

`play: async ({ args, canvasElement }: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-397 no-casts `packages/common/test-utils/src/playwright.ts:50:31`

Non-null assertion `!` in `const packageDir = packageJson!.split('/').slice(0, -1).join('/');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-398 no-casts `packages/common/timeframe/src/timeframe.ts:106:23`

`[equalsSymbol](other: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-399 no-casts `packages/common/tracing/src/api.ts:32:10`

`(target: any, propertyKey: string, descriptor: TypedPropertyDescriptor<(...args: any) => any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-400 no-casts `packages/common/tracing/src/api.ts:33:32`

Non-null assertion `!` in `const method = descriptor.value!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-401 no-casts `packages/common/tracing/src/api.ts:35:42`

`descriptor.value = async function (this: any, ...args: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-402 no-casts `packages/common/tracing/src/api.ts:88:11`

`instance: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-403 no-casts `packages/common/tracing/src/api.ts:166:26`

`const addLink = (parent: any, child: any, opts: AddLinkOptions = {}) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-404 no-casts `packages/common/tracing/src/diagnostic.ts:29:7`

`data: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-405 no-casts `packages/common/tracing/src/diagnostic.ts:94:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-406 no-casts `packages/common/tracing/src/diagnostics-channel.ts:71:54`

`if (this._serveChannel && typeof (this._serveChannel as any).unref === 'function') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-407 no-casts `packages/common/tracing/src/diagnostics-channel.ts:72:21`

`(this._serveChannel as any).unref();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-408 no-casts `packages/common/tracing/src/diagnostics-channel.ts:73:22`

`(this._clientChannel as any).unref();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-409 no-casts `packages/common/tracing/src/diagnostics-channel.ts:83:19`

Non-null assertion `!` in `this._serveChannel!.postMessage({` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-410 no-casts `packages/common/tracing/src/diagnostics-channel.ts:99:19`

Non-null assertion `!` in `this._serveChannel!.postMessage({` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-411 no-casts `packages/common/tracing/src/diagnostics-channel.ts:110:45`

Non-null assertion `!` in `this._ctx.onDispose(() => this._serveChannel!.removeEventListener('message', listener));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-412 no-casts `packages/common/tracing/src/trace-processor.ts:69:17`

`addLink(parent: any, child: any, opts: AddLinkOptions): void {}` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-413 no-casts `packages/common/tracing/src/trace-processor.ts:72:61`

`export const TRACE_PROCESSOR: TraceProcessor = ((globalThis as any).TRACE_PROCESSOR ??= new TraceProcessor());` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-414 no-casts `packages/common/tracing/src/tracing.test.ts:81:12`

Non-null assertion `!` in `expect(span!.options.parentContext?.traceparent).toBe(parentTrace.traceparent);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-415 no-casts `packages/common/tracing/src/tracing.test.ts:84:76`

Non-null assertion `!` in `expect(childCtx?.getAttribute(TRACE_SPAN_ATTRIBUTE)?.traceparent).toBe(span!.spanContext.traceparent);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-416 no-casts `packages/common/tracing/src/tracing.test.ts:86:12`

Non-null assertion `!` in `expect(span!.ended).toBe(false);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-417 no-casts `packages/common/tracing/src/tracing.test.ts:88:12`

Non-null assertion `!` in `expect(span!.ended).toBe(true);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-418 no-casts `packages/common/tracing/src/tracing.test.ts:159:16`

Non-null assertion `!` in `expect(workSpan!.ended).toBe(true);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-419 no-casts `packages/common/tracing/src/tracing.test.ts:183:17`

Non-null assertion `!` in `expect(childSpan!.options.parentContext?.traceparent).toBe(parentSpan!.spanContext.traceparent);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-420 no-casts `packages/common/tracing/src/tracing.test.ts:209:32`

Non-null assertion `!` in `await svc.laterWork(capturedCtx!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-421 no-casts `packages/common/tracing/src/tracing.test.ts:215:17`

Non-null assertion `!` in `expect(laterSpan!.options.parentContext?.traceparent).toBe(setupSpan!.spanContext.traceparent);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-422 no-casts `packages/common/tracing/src/tracing.test.ts:236:16`

Non-null assertion `!` in `expect(failSpan!.error).toBe(testError);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-423 no-casts `packages/common/tracing/src/tracing.test.ts:237:16`

Non-null assertion `!` in `expect(failSpan!.ended).toBe(true);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-424 no-casts `packages/common/tracing/src/tracing.test.ts:261:16`

Non-null assertion `!` in `expect(longSpan!.ended).toBe(false);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-425 no-casts `packages/common/tracing/src/tracing.test.ts:266:16`

Non-null assertion `!` in `expect(longSpan!.ended).toBe(true);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-426 no-casts `packages/common/tracing/src/tracing.test.ts:288:16`

Non-null assertion `!` in `expect(workSpan!.options.startTime).toBeTypeOf('number');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-427 no-casts `packages/common/tracing/src/tracing.test.ts:289:16`

Non-null assertion `!` in `expect(workSpan!.options.startTime).toBeGreaterThanOrEqual(beforeStart);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-428 no-casts `packages/common/tracing/src/tracing.test.ts:290:16`

Non-null assertion `!` in `expect(workSpan!.options.startTime).toBeLessThanOrEqual(afterEnd);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-429 no-casts `packages/common/tracing/src/tracing.test.ts:292:16`

Non-null assertion `!` in `expect(workSpan!.endTime).toBeTypeOf('number');` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-430 no-casts `packages/common/tracing/src/tracing.test.ts:293:16`

Non-null assertion `!` in `expect(workSpan!.endTime).toBeGreaterThanOrEqual(workSpan!.options.startTime!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-431 no-casts `packages/common/tracing/src/tracing.test.ts:294:16`

Non-null assertion `!` in `expect(workSpan!.endTime).toBeLessThanOrEqual(afterEnd);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-432 no-casts `packages/common/util/src/binder.ts:11:35`

`export const createBinder = (obj: any) => ({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-433 no-casts `packages/common/util/src/callback-collection.ts:8:53`

`export class CallbackCollection<F extends (...args: any[]) => Promise<any>> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-434 no-casts `packages/common/util/src/callback.ts:16:43`

`export class Callback<T extends (...args: any[]) => any> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-435 no-casts `packages/common/util/src/callback.ts:45:36`

`type Obj = { [i: string | symbol]: any };` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-436 no-casts `packages/common/util/src/callback.ts:52:18`

`return (...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-437 no-casts `packages/common/util/src/complex.ts:65:75`

`forEach(callbackfn: (value: T, value2: T, set: Set<T>) => void, thisArg?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-438 no-casts `packages/common/util/src/complex.ts:192:75`

`forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-439 no-casts `packages/common/util/src/complex.ts:226:35`

Non-null assertion `!` in `return this._values.get(primitive)!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-440 no-casts `packages/common/util/src/complex.ts:242:35`

Non-null assertion `!` in `return this._values.get(primitive)!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-441 no-casts `packages/common/util/src/deep.ts:11:33`

`export const setDeep = <T>(obj: any, path: readonly (string | number)[], value: T): T => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-442 no-casts `packages/common/util/src/deep.ts:32:33`

`export const getDeep = <T>(obj: any, path: readonly (string | number)[]): T | undefined => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-443 no-casts `packages/common/util/src/defer-function.ts:20:22`

`<T extends (...args: any[]) => any>(fnProvider: () => T) =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-444 no-casts `packages/common/util/src/defer.ts:28:13`

`if ((result as any) instanceof Promise) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-445 no-casts `packages/common/util/src/di-key.ts:32:15`

`new (...args: any[]): T;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-446 no-casts `packages/common/util/src/di-key.ts:46:15`

`new (...args: any[]): T;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-447 no-casts `packages/common/util/src/di-key.ts:69:11`

`return id as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-448 no-casts `packages/common/util/src/di-key.ts:96:12`

`return (id as any)[symbolSingleton];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-449 no-casts `packages/common/util/src/entry.ts:32:13`

`return this as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-450 no-casts `packages/common/util/src/instance-id.ts:9:39`

`const instanceContexts = ((globalThis as any)[symbol] ??= new WeakMap<` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-451 no-casts `packages/common/util/src/instance-id.ts:23:58`

`export const getPrototypeSpecificInstanceId = (instance: any): number => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-452 no-casts `packages/common/util/src/instance-id.ts:25:49`

`const instanceCtx = defaultMap(instanceContexts as any, prototype, () => ({` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-453 no-casts `packages/common/util/src/instance-id.ts:39:40`

`export const getDebugName = (instance: any): string => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-454 no-casts `packages/common/util/src/json.ts:20:50`

`export const jsonReplacer = (key: string, value: any): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-455 no-casts `packages/common/util/src/json.ts:41:32`

`export const jsonify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-456 no-casts `packages/common/util/src/json.ts:60:12`

`const res: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-457 no-casts `packages/common/util/src/json.ts:79:35`

`export const jsonlogify = (value: any, depth = 0, visitedObjects = new WeakSet<any>()): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-458 no-casts `packages/common/util/src/json.ts:100:12`

`const res: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-459 no-casts `packages/common/util/src/json.ts:121:22`

`(key: string, value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-460 no-casts `packages/common/util/src/map-values.ts:18:8`

`value: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-461 no-casts `packages/common/util/src/map-values.ts:19:13`

`fn: (value: any, recurse: (value: any) => any, key: string | number | undefined) => any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-462 no-casts `packages/common/util/src/map-values.ts:20:4`

`): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-463 no-casts `packages/common/util/src/map-values.ts:28:31`

`private readonly _fn: (value: any, recurse: (value: any) => any, key: string | number | undefined) => any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-464 no-casts `packages/common/util/src/map-values.ts:31:12`

`map(value: any): any {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-465 no-casts `packages/common/util/src/map-values.ts:35:21`

`private _map(value: any, key: string | number | undefined): any {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-466 no-casts `packages/common/util/src/map-values.ts:43:28`

`private _recurse = (value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-467 no-casts `packages/common/util/src/map-values.ts:59:12`

`const res: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-468 no-casts `packages/common/util/src/map-values.ts:77:8`

`value: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-469 no-casts `packages/common/util/src/map-values.ts:78:13`

`fn: (value: any, recurse: (value: any) => Promise<any>, key: string | number | undefined) => Promise<any>,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-470 no-casts `packages/common/util/src/map-values.ts:88:8`

`value: any,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-471 no-casts `packages/common/util/src/map-values.ts:89:18`

`recurse: (value: any) => Promise<any>,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-472 no-casts `packages/common/util/src/map-values.ts:94:12`

`map(value: any): Promise<any> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-473 no-casts `packages/common/util/src/map-values.ts:98:21`

`private _map(value: any, key: string | number | undefined): Promise<any> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-474 no-casts `packages/common/util/src/map-values.ts:106:34`

`private _recurse = async (value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-475 no-casts `packages/common/util/src/map-values.ts:122:12`

`const res: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-476 no-casts `packages/common/util/src/object.ts:8:65`

`export const keys = <R>(obj: R): (keyof R)[] => Object.keys(obj as any) as (keyof R)[];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-477 no-casts `packages/common/util/src/object.ts:10:83`

`export const entries = <R>(obj: R): [keyof R, R[keyof R]][] => Object.entries(obj as any) as [keyof R, any][];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-478 no-casts `packages/common/util/src/pick.ts:26:36`

`export const pickBy = (obj: Record<any, any>, predicate: (value: any) => boolean) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-479 no-casts `packages/common/util/src/platform.ts:8:40`

`export const isBun = () => (globalThis as any).Bun !== undefined;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-480 no-casts `packages/common/util/src/platform.ts:10:44`

`export const isTauri = () => !!(globalThis as any).__TAURI__;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-481 no-casts `packages/common/util/src/platform.ts:28:55`

`})(navigator.userAgent || navigator.vendor || (window as any).opera);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-482 no-casts `packages/common/util/src/platform.ts:45:55`

`})(navigator.userAgent || navigator.vendor || (window as any).opera);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-483 no-casts `packages/common/util/src/platform.ts:76:30`

`const platform = ((navigator as any).userAgentData?.platform || navigator.platform)?.toLowerCase();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-484 no-casts `packages/common/util/src/reducers.ts:59:18`

Non-null assertion `!` in `.sort((a, b) => a! - b!) as number[];` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-485 no-casts `packages/common/util/src/remove-undefined-keys.ts:7:13`

`if ((object as any)[prop] === undefined) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-486 no-casts `packages/common/util/src/remove-undefined-keys.ts:8:16`

`delete (object as any)[prop];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-487 no-casts `packages/common/util/src/remove-undefined-keys.ts:15:46`

`type Test = string[] | ((key: string, value: any) => boolean);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-488 no-casts `packages/common/util/src/remove-undefined-keys.ts:20:40`

`export const removeProperties = (root: any, condition: Test): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-489 no-casts `packages/common/util/src/safe-await.ts:22:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-490 no-casts `packages/common/util/src/safe-instanceof.test.ts:37:19`

`expect((undefined as any) instanceof Tagged).to.eq(false);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-491 no-casts `packages/common/util/src/safe-instanceof.ts:20:10`

`(target: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-492 no-casts `packages/common/util/src/safe-instanceof.ts:24:19`

`value: (instance: any) => instance?.[instanceTag] === tag,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-493 no-casts `packages/common/util/src/safe-instanceof.ts:28:19`

`value: (instance: any) => instance?.[instanceTag] === tag,` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-494 no-casts `packages/common/util/src/safe-stringify.ts:10:54`

`export type StringifyReplacer = (key: string, value: any) => typeof SKIP | any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-495 no-casts `packages/common/util/src/safe-stringify.ts:15:36`

`export function safeStringify(obj: any, filter: StringifyReplacer | undefined = defaultFilter, indent = 2) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-496 no-casts `packages/common/util/src/safe-stringify.ts:19:25`

`function replacer(this: any, key: string, value: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-497 no-casts `packages/common/util/src/safe-stringify.ts:66:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-498 no-casts `packages/common/util/src/safe-stringify.ts:73:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-499 no-casts `packages/common/util/src/safe-stringify.ts:100:24`

`return function (this: any, key: string, value: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-500 no-casts `packages/common/util/src/sort.ts:12:5`

`(a: any, b: any) =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-501 no-casts `packages/common/util/src/string.ts:27:66`

`export function inline(strings: TemplateStringsArray, ...values: any[]): string {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-502 no-casts `packages/common/util/src/string.ts:35:64`

`export function trim(strings: TemplateStringsArray, ...values: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-503 no-casts `packages/common/util/src/tracer.ts:14:9`

`value?: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-504 no-casts `packages/common/util/src/tracer.ts:62:26`

`emit(id: string, value?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-505 no-casts `packages/common/util/src/tracer.ts:66:26`

`mark(id: string, value?: any): { start: number; end: () => void } {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-506 no-casts `packages/common/util/src/tracer.ts:78:42`

`private _createEvent(id: string, value?: any): Event {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-507 no-casts `packages/common/util/src/tree.ts:20:14`

Non-null assertion `!` in `node.children!.forEach((child: TreeNode, i) => {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-508 no-casts `packages/common/util/src/types.ts:13:49`

`export type GuardedType<T> = T extends ((value: any) => value is infer R) ? R : never;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-509 no-casts `packages/common/util/src/types.ts:106:61`

`export const stripUndefined = <T extends { [index: string]: any }>(obj: T): T => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-510 no-casts `packages/common/util/src/types.ts:128:9`

`(sorted as any)[key] = (obj as any)[key];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-511 no-casts `packages/common/util/src/uint8array.ts:69:22`

`const inner = (value as any)['/'];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-512 no-casts `packages/common/util/src/weak.ts:108:58`

Non-null assertion `!` in `return this._internal.has(key) && this._internal.get(key)!.deref() !== undefined;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-513 no-casts `packages/common/util/src/weak.ts:130:75`

`forEach(callbackfn: (value: V, key: K, map: Map<K, V>) => void, thisArg?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-514 no-casts `packages/common/web-context-react/src/consumer.test.tsx:23:44`

`const Consumer = ({ options }: { options?: any }) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-515 no-casts `packages/common/web-context-react/src/consumer.test.tsx:66:17`

`const event = e as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# WARN cc9b81fc-516 no-sleep-in-test `packages/common/web-context-solid/src/consumer.test.tsx:103`

`await new Promise((resolve) => setTimeout(resolve, 50))` is the sole mechanism used to let "the consumer should NOT update" settle before asserting. Replace with a deterministic hook into the Solid reactivity system (e.g. an effect callback or `waitFor`) rather than a fixed-time sleep.

# ERROR cc9b81fc-517 no-casts `packages/common/web-context-solid/src/consumer.test.tsx:124:17`

`const event = e as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-518 no-casts `packages/common/web-context-solid/src/solid-element.tsx:83:25`

`return wrappedComponent as unknown as ComponentType<P>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-519 no-casts `packages/core/compute/agent-runtime/src/assistant-session-tests/xml-response.test.ts:56:51`

`const combined = textBlocks.map((block) => (block as any).text ?? '').join('\n');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-520 no-casts `packages/core/compute/agent-runtime/src/testing/assistant-test-layer.ts:329:3`

`) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-521 no-casts `packages/core/compute/ai/src/AiPreprocessor.ts:726:20`

`filtered.push(next as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-522 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:84:4`

`}) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-523 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:116:24`

`} = function (...args: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-524 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:122:3`

`} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-525 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:128:50`

`const provided = Layer.provide(self.layer, layer as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-526 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:129:26`

`return make(self.toolkit as any, provided as any) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-527 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:155:14`

`return empty as unknown as OpaqueToolkit<` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-528 no-casts `packages/core/compute/ai/src/OpaqueToolkit.ts:164:3`

`) as unknown as OpaqueToolkit<` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-529 no-casts `packages/core/compute/ai/src/resolvers/anthropic/AnthropicResolver.ts:39:21`

`({ type: 'adaptive' as any } as const)` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-530 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.test.ts:101:90`

`const captureRequestBody = (apiFormat: ChatCompletionsAdapter.ApiFormat, capture: (body: any) => void) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-531 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.test.ts:152:11`

`let body: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-532 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.test.ts:157:43`

`const args = body.messages.find((message: any) => message.role === 'assistant').tool_calls[0].function.arguments;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-533 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.test.ts:166:11`

`let body: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-534 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.test.ts:171:43`

`const args = body.messages.find((message: any) => message.role === 'assistant').tool_calls[0].function.arguments;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-535 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:426:39`

`description: Tool.getDescription(tool as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-536 no-casts `packages/core/compute/ai/src/resolvers/ChatCompletionsAdapter.ts:427:37`

`parameters: Tool.getJsonSchema(tool as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-537 no-casts `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:154:11`

`let body: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-538 no-casts `packages/core/compute/ai/src/resolvers/ollama/OllamaAdmin.test.ts:164:11`

`let body: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-539 no-casts `packages/core/compute/ai/src/testing/effect-ai.test.ts:192:95`

`Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as any } })),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-540 no-casts `packages/core/compute/ai/src/testing/effect-ai.test.ts:257:95`

`Effect.provide(AnthropicLanguageModel.model('claude-opus-4-6', { thinking: { type: 'adaptive' as any } })),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-541 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:419:31`

`toolChoice: params.toolChoice as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-542 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:476:31`

`toolChoice: params.toolChoice as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-543 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:529:39`

`description: Tool.getDescription(tool as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-544 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:530:38`

`inputSchema: Tool.getJsonSchema(tool as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-545 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:684:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-546 no-casts `packages/core/compute/ai/src/testing/model-fixture/LanguageModelFixture.ts:762:15`

`} catch (err: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-547 no-casts `packages/core/compute/ai/src/testing/process-messages.ts:58:65`

`const tk: Toolkit.Toolkit<{}> = opts.toolkit ?? (Toolkit.make() as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-548 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:28:3`

`} as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-549 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:125:16`

`expect((result as any).result).toBeUndefined();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-550 no-casts `packages/core/compute/ai/src/testing/tools.test.ts:126:16`

`expect((result as any).error).toMatch(/SyntaxError/);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-551 no-casts `packages/core/compute/ai/src/tools/call.ts:42:56`

`const toolResult = yield* toolkit.handle(toolCall.name as any, input as any).pipe(` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-552 no-casts `packages/core/compute/ai/src/tools/tool-execution-service.ts:27:98`

`toolkit.of(Record.map(toolkit.tools, (tool, name) => Effect.fail(new AiToolNotFoundError(name))) as any) as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-553 no-casts `packages/core/compute/ai/src/util/printer.ts:18:32`

`export type Logger = (...data: any[]) => void;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-554 no-casts `packages/core/compute/ai/src/util/printer.ts:53:22`

`private log(...data: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-555 no-casts `packages/core/compute/ai/src/util/printer.ts:100:62`

`const input = Schema.decodeUnknownSync(tool.parametersSchema as any)(JSON.parse(content.input));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-556 no-casts `packages/core/compute/ai/src/util/printer.ts:130:60`

`const result = Schema.decodeUnknownSync(tool.successSchema as any)(` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-557 no-casts `packages/core/compute/ai/src/util/sse.ts:197:56`

`export function readableStreamAsyncIterable<T>(stream: any): AsyncIterableIterator<T> {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-558 no-casts `packages/core/compute/ai/src/util/sse.ts:243:14`

`textDecoder: any; // TextDecoder found in browsers; not typed to avoid pulling in either "dom" or "node" types.` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-559 no-casts `packages/core/compute/ai/src/util/sse.ts:276:26`

Non-null assertion `!` in `this.buffer.push(lines[0]!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-560 no-casts `packages/core/compute/ai/src/util/sse.ts:323:8`

`(bytes as any).constructor.name` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-561 no-casts `packages/core/compute/assistant-toolkit/src/skills/agent/operations/get-context.ts:57:4`

`}) as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-562 no-casts `packages/core/compute/assistant-toolkit/src/skills/database/operations/relation-delete.ts:18:20`

`db.remove(relation as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-563 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:140:29`

`message.blocks.some((block: any) => block._tag === 'text' && block.text.trim().length > 0),` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-564 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:209:17`

`.filter((block: any) => block._tag === 'text')` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-565 no-casts `packages/core/compute/assistant-toolkit/src/skills/discord/operations/fetch-messages.ts:210:14`

`.map((block: any) => block.text)` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-566 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:83:96`

Non-null assertion `!` in `const client = yield* HttpClient.HttpClient.pipe(Effect.map(withAuthorization(credential.apiKey!)));` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-567 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:94:13`

`const json: any = yield* response.json;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-568 no-casts `packages/core/compute/assistant-toolkit/src/skills/linear/operations/sync-issues.ts:95:44`

`const tasks = (json.data.team.issues.edges as any[]).map((edge: any) =>` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-569 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:35:21`

`if (!Ref.isRef((obj as any)[key])) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-570 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:38:31`

`const ref: Ref.Unknown = (obj as any)[key];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-571 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:46:6`

`(obj as any)[key] = Ref.make(target);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-572 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:80:16`

`typeof (newObj as any)[key] !== 'string' &&` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-573 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:81:16`

`typeof (newObj as any)[key] !== 'number' &&` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-574 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:82:16`

`typeof (newObj as any)[key] !== 'boolean' &&` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-575 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:83:20`

`!Ref.isRef((newObj as any)[key])` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-576 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:88:11`

`(existing as any)[key] = (newObj as any)[key];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-577 no-casts `packages/core/compute/assistant-toolkit/src/sync/sync.ts:98:18`

`delete (existing as any)[key];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-578 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.test.ts:51:39`

`const companies = entities.filter((e: any) => e.text === 'SpaceX' || e.text === 'Tesla');` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-579 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.test.ts:53:24`

`companies.forEach((company: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-580 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:75:42`

Non-null assertion `!` in `const lastTokenGroup = tokenGroups.at(-1)!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-581 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:76:40`

Non-null assertion `!` in `if (canBeCombined(lastTokenGroup.at(-1)!, token)) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-582 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:88:20`

Non-null assertion `!` in `entity: group.at(0)!.entity.split('-')[1],` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-583 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:90:19`

Non-null assertion `!` in `index: group.at(0)!.index,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-584 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:91:18`

Non-null assertion `!` in `word: group.at(0)!.word,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-585 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:92:19`

Non-null assertion `!` in `start: group.at(0)!.start,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-586 no-casts `packages/core/compute/assistant/src/extraction/extraction-ner-function/named-entity-recognition.ts:93:18`

Non-null assertion `!` in `end: group.at(-1)!.end,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-587 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:168:26`

Non-null assertion `!` in `role: message.sender.role!,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-588 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:173:26`

Non-null assertion `!` in `role: message.sender.role!,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-589 no-casts `packages/core/compute/assistant/src/request/AiRequest.ts:263:40`

`onPart: (part) => observer.onPart(part as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-590 no-casts `packages/core/compute/assistant/src/session/AiContext.ts:145:47`

Non-null assertion `!` in `await this._updateBindings(this.#bindingsQuery!.results);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-591 no-casts `packages/core/compute/assistant/src/session/AiContext.ts:351:38`

Non-null assertion `!` in `EID.getEntityId(EID.tryParse(obj.uri)!) === EID.getEntityId(EID.tryParse(ref.uri)!))` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-592 no-casts `packages/core/compute/assistant/src/session/Harness.ts:238:17`

`return host.rpc as unknown as RpcClient.RpcClient<HarnessControlRpcs>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-593 no-casts `packages/core/compute/assistant/src/session/toolkit.ts:46:63`

`return OpaqueToolkit.make(mergedToolkit, combinedHandlerLayer as any) as OpaqueToolkit.OpaqueToolkit;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-594 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:21:40`

`const decodeIn = (schema: Schema.Codec<any, any>, value: unknown): any[] => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-595 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:23:16`

`const decoded: any = Schema.decodeUnknownSync(Schema.Struct(fields))({ in: value });` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-596 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:52:16`

`const decoded: any = Schema.decodeUnknownSync(Schema.Struct(fields))({});` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-597 no-casts `packages/core/compute/assistant/src/tool-runtime/services.test.ts:153:16`

`const decoded: any = Schema.decodeUnknownSync(Schema.Struct(fields))({` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-598 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:77:72`

`return Effect.fn(`toolFunctionHandler ${tool.name}`)(function* (input: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-599 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:84:50`

`const { result } = yield* (toolkitHandler.handle as any)(tool.name, input);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-600 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:102:3`

`) as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-601 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:103:3`

`) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-602 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:181:25`

Non-null assertion `!` in `return toolCache.get(fn)!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-603 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:229:40`

`return node.map(statePropertyOpenness) as unknown as JsonSchema.JsonSchema;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-604 no-casts `packages/core/compute/assistant/src/tool-runtime/services.ts:331:22`

`return typeof (value as any).tools === 'object' && typeof (value as any).handle === 'function';` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-605 no-casts `packages/core/compute/assistant/src/util/execution-graph.ts:135:39`

`private sortObjectsByCreated(objects: any[]) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-606 no-casts `packages/core/compute/assistant/src/util/execution-graph.ts:138:43`

`const createdObjects: { idx: number; obj: any }[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-607 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:24:39`

`export type AsyncFunction = (...args: any) => Promise<InterpreterValue>;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-608 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:56:51`

`static createInvocationKey(name: string, ...args: any): string {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-609 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:124:7`

`args: any[],` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-610 no-casts `packages/core/compute/compute-hyperformula/src/functions/async-function.ts:180:62`

`return this.runFunction(ast.args, state, metadata, (...args: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-611 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:30:34`

`async (binding: string, ...args: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-612 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:58:12`

`let input: any;` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-613 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:62:12`

`input = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-614 no-casts `packages/core/compute/compute-hyperformula/src/functions/edge-function.ts:70:15`

`return result as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-615 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:31:14`

`invoke: (op: any, ...args: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-616 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:32:35`

`const handler = handlers.find((h: any) => h.meta.key === op.meta.key);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-617 no-casts `packages/core/compute/compute-hyperformula/src/testing/test-runtime.ts:51:3`

`} as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-618 no-casts `packages/core/compute/compute-hyperformula/src/types/types.test.ts:57:39`

`const setCell = (cell: string, value: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-619 no-casts `packages/core/compute/compute-hyperformula/src/types/types.ts:17:34`

`export const isFormula = (value: any): value is string => typeof value === 'string' && value.charAt(0) === '=';` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-620 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:19:29`

`fetch: async () => response as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-621 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:59:15`

`expect((error as any)?.context?.cacheKey).toBe('abc');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-622 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:60:15`

`expect((error as any)?.context?.status).toBe(500);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-623 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:77:15`

`expect((error as any)?.context?.type).toBe('overloaded_error');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-624 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.test.ts:78:15`

`expect((error as any)?.context?.status).toBe(529);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-625 no-casts `packages/core/compute/compute-runtime/src/functions-ai-http-client.ts:85:31`

`return send(request.body.body as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-626 no-casts `packages/core/compute/compute-runtime/src/functions-trace.ts:218:53`

Non-null assertion `!` in `duration: isInProgress ? now - start.timestamp : end!.timestamp - start.timestamp,` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-627 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:269:25`

`const process = 'pid-1' as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-628 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:326:86`

`const resolvedA = yield* resolveWithScope(resolver.resolve(ServiceA, { process: 'p1' as any }));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-629 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:331:81`

`const exit = yield* resolveWithScope(resolver.resolve(ServiceB, { process: 'p1' as any })).pipe(Effect.exit);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-630 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:349:93`

`const resolvedB = yield* resolveWithScope(resolver.resolve(ServiceB, { space, process: 'p2' as any }));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-631 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:413:33`

`const process = 'sync-triggers' as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-632 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:439:86`

`const conversation = 'echo://BBBBBBBBBBBBBBBBBBBBBBBBBB/01JTESTFEED0000000000000000' as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-633 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:441:68`

`resolver.resolve(ServiceB, { space, conversation, process: 'agent' as any }),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-634 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:486:27`

`const process = 'p-multi' as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-635 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:560:75`

`yield* resolveWithScope(resolver.resolve(ServiceC, { space, process: 'p1' as any }));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-636 no-casts `packages/core/compute/compute-runtime/src/LayerStack.test.ts:561:75`

`yield* resolveWithScope(resolver.resolve(ServiceC, { space, process: 'p2' as any }));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-637 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:39:82`

`return ServiceResolver.make((tag, context) => this.#resolveService(tag, context) as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-638 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:618:99`

`const baseLayer: Layer.Layer<unknown, unknown, unknown> = Layer.syncContext(() => this.#services) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-639 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:699:20`

Non-null assertion `!` in `const a = layers[i]!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-640 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:700:20`

Non-null assertion `!` in `const b = layers[j]!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-641 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:705:7`

Non-null assertion `!` in `adj[j]!.push(i);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-642 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:729:26`

Non-null assertion `!` in `for (const v of adj[next]!) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-643 no-casts `packages/core/compute/compute-runtime/src/LayerStack.ts:734:47`

Non-null assertion `!` in `this.#layers = sorted.map((idx) => layers[idx]!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-644 no-casts `packages/core/compute/compute-runtime/src/object-template.ts:54:3`

`} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-645 no-casts `packages/core/compute/compute-runtime/src/ProcessHandle.ts:388:34`

`const defWithSchema = definition as unknown as { input: Schema.Codec<I, unknown, never> };` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-646 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:198:51`

Non-null assertion `!` in `yield* Deferred.succeed(SlowChildGate.alarmStarted!, undefined);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-647 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:200:48`

Non-null assertion `!` in `yield* Deferred.await(SlowChildGate.alarmResume!);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-648 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:201:50`

Non-null assertion `!` in `yield* Ref.set(SlowChildGate.alarmHandlerFinished!, true);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-649 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:516:15`

Non-null assertion `!` in `expect(tree[0]!.pid).toEqual(handle.pid);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-650 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:517:15`

Non-null assertion `!` in `expect(tree[0]!.parentPid).toBeNull();` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-651 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:518:15`

Non-null assertion `!` in `expect(tree[0]!.state).toEqual(Process.State.HYBERNATING);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-652 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:878:58`

`input.override !== undefined ? { spaceId: input.override as any } : undefined,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-653 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:911:18`

`return dbService as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-654 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:1000:86`

`const conversation = 'echo://BBBBBBBBBBBBBBBBBBBBBBBBBB/01JTESTCONVERSATION00000000' as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-655 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:1465:50`

Non-null assertion `!` in `yield* Deferred.succeed(SlowChildGate.alarmResume!, undefined);` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# WARN cc9b81fc-656 no-sleep-in-test `packages/core/compute/compute-runtime/src/ProcessManager.test.ts:1512`

Three tests (also lines 1555, 1562, 1606) use a raw `yield* Effect.promise(() => new Promise<void>((resolve) => setTimeout(resolve, 50)))` to let a forked fiber reach its blocked state before shutting down the manager, even though the file otherwise runs on `TestClock` (see `TestClock.adjust` usage) and elsewhere uses `expect.poll`. Replace the unvirtualized real-time wait with an explicit signal from `onInput` (e.g. a `Deferred` resolved just before entering the blocking branch) or `expect.poll`.

# ERROR cc9b81fc-657 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:79:17`

`RpcGroup.make() as unknown as RpcGroup.RpcGroup<any>,` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-658 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:644:27`

`const defRaw = definition as unknown as { input: Schema.Codec<I, any, never> };` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-659 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:709:15`

`return handle as unknown as Handle<I, O, _Rpcs>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-660 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:760:24`

`submitOutput: (output: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-661 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:847:27`

`const defRaw = definition as unknown as { input: Schema.Codec<any, any, never> };` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-662 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:848:29`

`const encodeInput = (input: any): Effect.Effect<unknown> =>` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-663 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:910:17`

`return existing as unknown as Handle<I, O, Rpcs>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-664 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:935:15`

`return handle as unknown as Handle<I, O, Rpcs>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-665 no-casts `packages/core/compute/compute-runtime/src/ProcessManager.ts:947:15`

`return handle as unknown as Handle<I, O, Rpcs>;` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-666 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:208:10`

`...args: any[]` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-667 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:257:10`

`...args: any[]` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-668 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:327:10`

`...args: any[]` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-669 no-casts `packages/core/compute/compute-runtime/src/ProcessOperationInvoker.ts:343:28`

`invoke(op, input, options) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-670 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:107:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-671 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:115:13`

`let result: any = await func.handler(dataWithDecodedRefs);` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-672 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:141:17`

`} catch (error: any) {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-673 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:370:66`

`if (typeof value === 'object' && value !== null && typeof (value as any)['/'] === 'string') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-674 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:372:38`

`return refFromEncodedReference(value as any, resolver);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-675 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:383:53`

`const result: Record<string, unknown> = { ...(value as any) };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-676 no-casts `packages/core/compute/compute-runtime/src/protocol.ts:387:55`

`result[key] = decodeRefsFromSchema(prop.type, (result as any)[key], db);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-677 no-casts `packages/core/compute/compute-runtime/src/RemoteOperationInvoker.test.ts:16:90`

`invoke: <I, O>(_ctx: DxosContext, _deployedId: string, input: I) => Effect.succeed(input as unknown as O),` is the double-cast escape hatch (`as unknown as T`) the no-casts rule explicitly bans. Fix the type at its source — adjust the underlying declaration, generic bound, or add a real type guard — instead of routing through `unknown` to force an unrelated type.

# ERROR cc9b81fc-678 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.test.ts:16:25`

`resolve: (tag) => ((tag as any) === MyTag ? Option.some({ value: 'test' } as any) : Option.none()),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-679 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:46:16`

`(Function.flow as any)(...tags.map((tag) => Effect.provideServiceEffect(tag, ServiceRegistry.resolve(tag))));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-680 no-casts `packages/core/compute/compute-runtime/src/services/service-registry.ts:59:16`

`(Function.flow as any)(` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans. Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated.

# ERROR cc9b81fc-681 no-casts `packages/core/compute/compute-runtime/src/testing/layer.ts:78:31`

Non-null assertion `!` in `yield* Effect.promise(() => db!.flush());` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-682 no-casts `packages/core/compute/compute-runtime/src/trace-buffer.ts:34:15`

Non-null assertion `!` in `if (buf[index]!.id === id) {` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-683 no-casts `packages/core/compute/compute-runtime/src/trace-buffer.ts:42:23`

Non-null assertion `!` in `const item = buf[read]!;` bypasses the type checker's null/undefined check, which the no-casts rule bans. Fix the type at its source (narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted).

# ERROR cc9b81fc-684 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:8:102`

`export const createInvocationPayload = (trigger: Trigger.Trigger, event: TriggerEvent.TriggerEvent): any => {` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-685 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:13:16`

`const payload: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-686 no-casts `packages/core/compute/compute-runtime/src/triggers/input-builder.ts:21:24`

`let valueSubstitution: any = propertyPath.startsWith('trigger')` widens to `any` in a signature/type position, which the no-casts rule bans. Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-687 no-casts `packages/core/compute/compute-runtime/src/triggers/jsonata.test.ts:99`

`const expression = jsonata(trigger.input!.value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-688 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:59`

`return dbService as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-689 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:475`

`runnable: Ref.make(badFn) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-690 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:744`

`Obj.getMeta(trigger).keys.push({ source: LEGACY_KEY_FEED_CURSOR, id: cursor! });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-691 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1135`

`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-692 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1162`

`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-693 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1195`

`spec: Trigger.specSubscription(Query.select(Filter.everything()).from(Scope.feed(Feed.getFeedUri(feed)!))),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-694 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1226`

`spec: Trigger.specSubscription(Query.select(Filter.type(Task.Task)).from(Scope.feed(Feed.getFeedUri(feed)!))),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-695 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.test.ts:1321`

`runnable: Ref.make(badFn) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-696 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.ts:1145`

`const kind = spec!.kind as 'feed' | 'subscription';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-697 no-casts `packages/core/compute/compute-runtime/src/triggers/trigger-dispatcher.ts:1222`

`private _prepareInputData = (trigger: Trigger.Trigger, event: TriggerEvent.TriggerEvent): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-698 no-casts `packages/core/compute/compute/src/LayerSpec.ts:89`

`make: make as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-699 no-casts `packages/core/compute/compute/src/Operation.ts:185`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-700 no-casts `packages/core/compute/compute/src/Operation.ts:233`

`} as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-701 no-casts `packages/core/compute/compute/src/Operation.ts:285`

`})) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-702 no-casts `packages/core/compute/compute/src/Operation.ts:293`

`} as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-703 no-casts `packages/core/compute/compute/src/Operation.ts:828`

`invoke: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-704 no-casts `packages/core/compute/compute/src/Operation.ts:829`

`service.invoke(op, input, { ...options, ...invocationOptions })) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-705 no-casts `packages/core/compute/compute/src/Operation.ts:830`

`invokePromise: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-706 no-casts `packages/core/compute/compute/src/Operation.ts:831`

`service.invokePromise(op, input, { ...options, ...invocationOptions })) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-707 no-casts `packages/core/compute/compute/src/Operation.ts:832`

`schedule: ((op: Operation.Definition.Any, input: any, invocationOptions: InvokeOptions) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-708 no-casts `packages/core/compute/compute/src/Operation.ts:833`

`service.schedule(op, input, { ...options, ...invocationOptions })) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-709 no-casts `packages/core/compute/compute/src/Operation.ts:875`

`source: from.source as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-710 no-casts `packages/core/compute/compute/src/Process.ts:308`

`[ProcessTypeId]: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-711 no-casts `packages/core/compute/compute/src/ServiceResolver.ts:94`

`return service as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-712 no-casts `packages/core/compute/compute/src/Trace.test.ts:107`

`expect((decoded.events[0].data as any).progress.key).toBe('k1');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-713 no-casts `packages/core/compute/compute/src/types/Template.test.ts:51`

`expect(decode(kind as any)).toBe(kind);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-714 no-casts `packages/core/compute/compute/src/types/Template.test.ts:56`

`expect(() => Schema.decodeSync(Template.InputKind)('nonsense' as any)).toThrow();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-715 no-casts `packages/core/compute/compute/src/types/Template.test.ts:100`

`const handlersByKey: Record<string, (input: any) => Effect.Effect<any, any, any>> = {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-716 no-casts `packages/core/compute/compute/src/types/Template.test.ts:105`

`invoke: (op: any, input: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-717 no-casts `packages/core/compute/compute/src/types/Template.test.ts:114`

`} as unknown as Operation.OperationService);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-718 no-casts `packages/core/compute/compute/src/types/Template.ts:111`

`const result = yield* Operation.invoke(fn, undefined as any).pipe(Effect.orDie);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-719 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.test.ts:42`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-720 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:207`

`const compute = await computeNodeResolver!(node);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-721 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:242`

`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-722 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:252`

`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-723 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:262`

`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-724 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:289`

`this._topology!.nodes.find((node) => node.id === input.sources[0].nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-725 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:299`

`const sourceNode = this._topology!.nodes.find((node) => node.id === source.nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-726 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:401`

`const result = yield* this._computeCache.get(nodeId)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-727 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:409`

`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-728 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:509`

`recurse(this._topology!.nodes.find((node) => node.id === bound.nodeId) ?? failedInvariant());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-729 no-casts `packages/core/compute/conductor/src/compiler/fiber-compiler.ts:515`

`const node = this._topology!.nodes.find((node) => node.id === nodeId) ?? failedInvariant();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-730 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:43`

`export const isFalsy = (value: any) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-731 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:52`

`export const isTruthy = (value: any) => !isFalsy(value);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-732 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:119`

`exec: (_, node) => Effect.succeed(ValueBag.make({ [DEFAULT_OUTPUT]: node!.value })),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-733 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:226`

`const mappedItems = items.map((item: any) => ({` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-734 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:252`

`const { id: _id, '@type': _type, ...rest } = item as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-735 no-casts `packages/core/compute/conductor/src/nodes/registry.ts:342`

`Effect.gen(function* (): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-736 no-casts `packages/core/compute/conductor/src/nodes/template/json.ts:56`

`return props[p1] as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-737 no-casts `packages/core/compute/conductor/src/sequence/logger.ts:15`

`type ConsoleLogSignature = (message: string, arg?: any) => void;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-738 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:32`

`nodeResolver: async (node: ComputeNode) => this._nodes.get(node.type!)!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-739 no-casts `packages/core/compute/conductor/src/testing/test-runtime.ts:80`

`computeNodeResolver: async (node: ComputeNode) => workflow.getResolvedNode(node.id)!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-740 no-casts `packages/core/compute/conductor/src/tests/gpt.test.ts:32`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-741 no-casts `packages/core/compute/conductor/src/tests/streaming.test.ts:33`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-742 no-casts `packages/core/compute/conductor/src/types/compute.test.ts:26`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-743 no-casts `packages/core/compute/conductor/src/types/compute.ts:27`

`export const isNotExecuted = (value: any): value is NotExecuted => value.kind === 'not-executed';` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-744 no-casts `packages/core/compute/conductor/src/types/compute.ts:61`

`isValueBag: (value: any): value is ValueBag => value._tag === 'ValueBag',` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-745 no-casts `packages/core/compute/conductor/src/types/compute.ts:67`

`values: mapValues(values as any, (value) => (Effect.isEffect(value) ? value : Effect.succeed(value))) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-746 no-casts `packages/core/compute/conductor/src/util/ast.test.ts:26`

`const field = pickProperty(Schema.Struct({ foo: Schema.Number }), 'bar' as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-747 no-casts `packages/core/compute/conductor/src/util/stream.ts:9`

`const isStream = (value: any): value is Stream.Stream<any> =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-748 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:42`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-749 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:120`

`const result = yield* executeEffect(executable.exec!(makeInput({ num1: 2, num2: 3 })));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-750 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:183`

`const makeInput = (input: any) => ValueBag.make({ input });` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-751 no-casts `packages/core/compute/conductor/src/workflow/workflow.test.ts:259`

`type Transform = (input: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-752 no-casts `packages/core/compute/conductor/src/workflow/workflow.ts:128`

`return this._resolvedNodeById.get(node.id)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-753 no-casts `packages/core/compute/crawler/src/testing/index.ts:265`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-754 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.test.ts:94`

`Effect.flatMap((res: any) => res.json),` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-755 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.test.ts:100`

`return (res as any).data.rates[to].toString();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-756 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.ts:103`

`httpPlugin as any as Plugin,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-757 no-casts `packages/core/compute/edge-compute/src/bundler/bundler.ts:190`

`return Object.values(result.metafile!.outputs)[0].imports.map((entry): Import => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-758 no-casts `packages/core/compute/edge-compute/src/edge-client.ts:83`

`const functions: Operation.PersistentOperation[] = result.uploadedFunctions.flatMap((record: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-759 no-casts `packages/core/compute/edge-compute/src/FunctionsServiceClient.ts:135`

`return response.uploadedFunctions.flatMap((record: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-760 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:104`

`join(build.initialOptions.outdir!, 'metafile.json'),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-761 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:108`

`join(build.initialOptions.outdir!, 'manifest.json'),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-762 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:111`

`files: Object.keys(result.metafile!.outputs).map((path) => ({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-763 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:112`

`name: relative(build.initialOptions.outdir!, path),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-764 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:129`

`const assetPaths = Object.keys(result.metafile!.outputs).map((path) => relative(outdir, path));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-765 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:141`

`Object.entries(result.metafile!.outputs)` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-766 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:162`

`Array.map((_) => _.original!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-767 no-casts `packages/core/compute/edge-compute/src/native/bundler.ts:220`

`const postprocessSourceMap = (map: any, outDir: string, gitDir: string | null) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-768 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:55`

`sourceTypes: [Type.getTypename(Note)!],` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-769 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:57`

`targets: [{ type: Type.getTypename(Contact)!, identity: { fields: ['email'] } }],` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-770 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:88`

`} as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-771 no-casts `packages/core/compute/extractor/src/ExtractionTemplate.test.ts:105`

`[Type.getTypename(Contact)!]: ({ email }: { email: string }) =>` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-772 no-casts `packages/core/compute/extractor/src/Resolver.ts:37`

`resolve: (type, input: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-773 no-casts `packages/core/compute/extractor/src/testing/mock-ai.ts:30`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-774 no-casts `packages/core/compute/functions-runtime-cloudflare/src/functions-client.ts:87`

`const space = this._spaces.get(spaceId)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-775 no-casts `packages/core/compute/functions-runtime-cloudflare/src/functions-client.ts:93`

`export const createClientFromEnv = async (env: any): Promise<FunctionsClient> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-776 no-casts `packages/core/compute/functions-runtime-cloudflare/src/wrap-handler-for-cloudflare.ts:57`

`} catch (error: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-777 no-casts `packages/core/compute/functions-runtime-cloudflare/src/wrap-handler-for-cloudflare.ts:81`

`} = (await request.json()) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-778 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:102`

`trigger.input!.iterations = 100;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-779 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:156`

`trigger.input!.iterations = 1_000_000_000;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-780 no-casts `packages/core/compute/functions-testing/src/cpu-limit.test.ts:162`

`trigger.input!.iterations = 100;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-781 no-casts `packages/core/compute/link/src/Cursor.test.ts:344`

`expect([...Cursor.readTagHeads(cursor)!]).toEqual(['head-a']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-782 no-casts `packages/core/compute/link/src/Cursor.test.ts:348`

`expect([...Cursor.readTagHeads(cursor)!]).toEqual(['head-b', 'head-c']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-783 no-casts `packages/core/compute/link/src/Cursor.test.ts:358`

`expect([...Cursor.readTagHeads(cursor)!]).toEqual(['head-b']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-784 no-casts `packages/core/compute/link/src/Cursor.test.ts:362`

`expect([...Cursor.readTagHeads(cursor)!]).toEqual(['head-b']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-785 no-casts `packages/core/compute/link/src/Cursor.test.ts:373`

`expect([...Cursor.readTagHeads(cursor)!]).toEqual(['head-a']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-786 no-casts `packages/core/compute/link/src/Cursor.test.ts:433`

`expect([...state.foreignIndex!.keys()].sort()).toEqual(['id-10', 'id-30']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-787 no-casts `packages/core/compute/link/src/Cursor.test.ts:435`

`expect(state.foreignIndex!.get('id-10')).toBe(items[0].id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-788 no-casts `packages/core/compute/link/src/Cursor.test.ts:436`

`expect(state.foreignIndex!.get('id-30')).toBe(items[2].id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-789 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:96`

`arguments: args as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-790 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:103`

`) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-791 no-casts `packages/core/compute/mcp-client/src/McpToolkit.ts:172`

`const inner = error != null && typeof error === 'object' && 'error' in error ? (error as any).error : error;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-792 no-casts `packages/core/compute/mcp-server/src/internal/projection.ts:104`

`const isRefProperty = (property: any): boolean => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-793 no-casts `packages/core/compute/mcp-server/src/internal/projection.ts:135`

`export const tolerateStringifiedRefs = (fields: Fields, inputSchema: any): Fields => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-794 no-casts `packages/core/compute/mcp-server/src/Server.ts:169`

`const toolkit = Toolkit.make(...(tools as any[]));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-795 no-casts `packages/core/compute/mcp-server/src/Server.ts:178`

`return McpServer.toolkit(toolkit).pipe(Layer.provide(handlers)) as unknown as Layer.Layer<` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-796 no-casts `packages/core/compute/operation/src/invoker.test.ts:24`

`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-797 no-casts `packages/core/compute/operation/src/OperationInvoker.ts:134`

`dynamicRuntime = DynamicRuntime.make(this._managedRuntime!, services);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-798 no-casts `packages/core/compute/operation/src/scheduler.test.ts:19`

`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-799 no-casts `packages/core/compute/operation/src/scheduler.test.ts:48`

`const invokeFn = ((_op: any, input: { id: string }) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-800 no-casts `packages/core/compute/operation/src/scheduler.test.ts:51`

`return undefined as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-801 no-casts `packages/core/compute/operation/src/scheduler.test.ts:78`

`const invokeFn = ((_op: any, input: { id: string }) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-802 no-casts `packages/core/compute/operation/src/scheduler.test.ts:81`

`return undefined as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-803 no-casts `packages/core/compute/operation/src/scheduler.test.ts:106`

`const invokeFn = (() => Effect.succeed(undefined as any)) as Scheduler.InvokeFn;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-804 no-casts `packages/core/compute/operation/src/scheduler.test.ts:126`

`const invokeFn = ((_op: any, input: { id: string }) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-805 no-casts `packages/core/compute/operation/src/scheduler.test.ts:132`

`return undefined as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-806 no-casts `packages/core/compute/operation/src/scheduler.test.ts:156`

`const invokeFn = ((_op: any, input: { id: string }) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-807 no-casts `packages/core/compute/operation/src/scheduler.test.ts:162`

`return undefined as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-808 no-casts `packages/core/compute/pipeline-discord/src/stages/answer-questions.test.ts:124`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-809 no-casts `packages/core/compute/pipeline-email/src/stages/stats.test.ts:21`

`const ctx = Layer.succeed(EmailPipelineCtx, { db: {} as any, stats, summaries: [] });` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-810 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:23`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-811 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:34`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-812 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:50`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-813 no-casts `packages/core/compute/pipeline-rdf/src/testing/index.ts:68`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-814 no-casts `packages/core/compute/pipeline-transcription/src/asr/transcriber.ts:330`

`lastTimestamp: zeroTimestamp + filteredSegments.at(-1)!.end * 1_000,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-815 no-casts `packages/core/compute/pipeline-transcription/src/stages/correction-llm.test.ts:24`

`} as any),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-816 no-casts `packages/core/compute/pipeline-transcription/src/stages/correction-llm.test.ts:25`

`} as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-817 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:23`

`const write = await EffectEx.runPromise(stage.run({ window: [block] }, { model: stage.model! }));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-818 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:47`

`stage.run({ window: [block] }, { lookup: makeDatabaseLookup(db), model: stage.model! }),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-819 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:50`

`const update = write.blockUpdates![0];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-820 no-casts `packages/core/compute/pipeline-transcription/src/stages/extraction.test.ts:69`

`stage.run({ window: [block] }, { lookup: makeDatabaseLookup(db), model: stage.model! }),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-821 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:137`

`await using db2 = await peer.openDatabase(spaceKey, db.rootUrl!, {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-822 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:157`

`await using db2 = await peer.openDatabase(spaceKey, db1.rootUrl!, {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-823 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:202`

`const outer = (await db.query(Filter.id(outerId)).first()) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-824 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:220`

`rootUrl = db.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-825 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:230`

`const outer = (await db.query(Filter.id(outerId)).first()) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-826 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:250`

`const object = db.add(Obj.make(TestSchema.Expando, { type: db.makeRef(typeDXN) })) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-827 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:278`

`const object = db.add(Obj.make(TestSchema.Expando, { fn: db.makeRef(keyDXN) })) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-828 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:305`

`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-829 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:327`

`await using db2 = await peer2.openDatabase(spaceKey1, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-830 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:339`

`await using db2 = await peer2.openDatabase(spaceKey2, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-831 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:376`

`await using db2 = await asyncTimeout(peer2.openDatabase(spaceKey, db1.rootUrl!), 1_000);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-832 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:425`

`rootUrl = db1.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-833 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:445`

`return state.peers!.length;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-834 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:452`

`return state.peers![0].differentDocuments + state.peers![0].missingOnRemote + state.peers![0].missingOnLocal;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-835 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:468`

`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-836 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:488`

`let relationId!: EntityId;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-837 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:627`

`rootUrl = db.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-838 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:635`

`schemaDxn = Type.getURI(stored)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-839 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:640`

`expect(Type.getURI(Obj.getType(object)!)).to.eq(Type.getURI(stored));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-840 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:660`

`expect(getTypeAnnotation(Type.getSchema(Obj.getType(objects[0])!))).to.include({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-841 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:674`

`const objects = await db.query(Filter.type(schema!)).run();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-842 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:676`

`expect(getTypeAnnotation(Type.getSchema(Entity.getType(objects[0])!))).to.include({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-843 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:687`

`let typeURI!: URI.URI;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-844 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:694`

`typeURI = Type.getURI(schema)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-845 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:707`

`expect(Type.getTypename(Obj.getType(obj)!)).toEqual(Type.getTypename(TestSchema.Person));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-846 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:759`

`expect(() => db.add(plainObj as any)).toThrow(TypeError);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-847 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:764`

`await expect(db.appendToFeed(feed, [plainObj as any])).rejects.toThrow(TypeError);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-848 no-casts `packages/core/echo/echo-client-e2e/src/integration.test.ts:796`

`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-849 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:79`

`const child = (await db.query(Filter.id(childId)).first()) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-850 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:85`

`expect((parent as any).name).to.eq('Parent');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-851 no-casts `packages/core/echo/echo-client-e2e/src/parent.test.ts:86`

`expect((parent as any).id).to.eq(parentId);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-852 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:953`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-853 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:956`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-854 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:973`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-855 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:976`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-856 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:995`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-857 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:998`

`.from(Scope.feed(Feed.getFeedUri(feed)!))` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-858 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1015`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-859 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1018`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# WARN cc9b81fc-860 no-sleep-in-test `packages/core/echo/echo-client-e2e/src/query.test.ts:1180`

`while (Math.floor(Date.now() / 1000) < secondAfterFlush) { await sleep(50); }` is a busy-poll loop waiting for a wall-clock second boundary. Compute the remaining milliseconds to the boundary and `await sleep(...)` once instead of polling in a loop.

# ERROR cc9b81fc-861 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1185`

`Obj.update(obj, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-862 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1243`

`Obj.update(object, (object: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-863 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1287`

`Obj.update(obj, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-864 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1293`

`expect(after).toBeGreaterThan(before!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-865 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1303`

`Obj.update(obj, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-866 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1336`

`expect(Obj.getMeta(reloaded!).createdAt).toBe(createdAt);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-867 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1389`

`const t1 = Obj.getMeta(obj1).createdAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-868 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1390`

`const t2 = Obj.getMeta(obj2).createdAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-869 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1391`

`const t3 = Obj.getMeta(obj3).createdAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-870 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1410`

`Obj.update(obj2, (obj2: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-871 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1415`

`Obj.update(obj1, (obj1: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-872 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1421`

`const upd1 = Obj.getMeta(obj1).updatedAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-873 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1422`

`const upd2 = Obj.getMeta(obj2).updatedAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-874 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1423`

`const upd3 = Obj.getMeta(obj3).updatedAt!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-875 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1459`

`.query(Query.select(Filter.type(DXN.make('com.example.type.person'))).from(Scope.feed(Feed.getFeedUri(feed)!)))` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-876 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1466`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-877 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1475`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-878 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1495`

`Scope.feed(Feed.getFeedUri(feed)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-879 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1553`

`Scope.feed(Feed.getFeedUri(feed1)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-880 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1567`

`Scope.feed(Feed.getFeedUri(feed2)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-881 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1581`

`Scope.feed(Feed.getFeedUri(feed1)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-882 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1582`

`Scope.feed(Feed.getFeedUri(feed2)!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-883 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1704`

`expect(Feed.getFeedUri(feed)!).toMatch(/^echo:\/\//);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-884 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1716`

`const uriString = Obj.getURI(traceResult!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-885 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1736`

`const uriString = Obj.getURI(traceResult!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-886 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1795`

`expect(feedResults.map((obj: any) => obj.title).sort()).toEqual(['Feed Task 1', 'Feed Task 2']);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-887 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1812`

`expect((feedResult[0] as any).title).toBe('Feed Task');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-888 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1860`

`expect(feedResults.map((obj: any) => obj.title).sort()).toEqual(['Feed Task 1', 'Feed Task 2']);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-889 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1957`

`doc.links![obj1.id] = 'automerge:4hjTgo9zLNsfRTJiLcpPY8P4smy';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-890 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1988`

`const obj1DocHandle = getObjectCore(obj1).docHandle!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-891 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1989`

`const anotherDocHandle = getObjectCore(obj2).docHandle!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-892 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1993`

`doc.objects![obj1.id] = obj1DocHandle.doc()!.objects![obj1.id];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-893 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:1996`

`doc.links![obj1.id] = new A.RawString(anotherDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-894 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2000`

`assertion = { objectId: obj2.id, documentUrl: anotherDocHandle.url! };` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-895 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2011`

`const object = queryResult.find((obj) => obj.id === assertion.objectId)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-896 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2012`

`expect(getObjectCore(object).docHandle!.url).to.eq(assertion.documentUrl);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-897 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2050`

`obj2Core.docHandle!.delete(); // Deleted handle access throws an exception.` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-898 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2058`

`const folder = db.add(Obj.make(TestSchema.Expando, { name: 'folder', objects: [] as any[] }));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-899 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2061`

`folder.objects.push(Ref.make(object as any));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-900 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2314`

`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-901 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2400`

`expect(objects.sort((a, b) => a.title!.localeCompare(b.title!))).toMatchObject([` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-902 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2579`

`const titles = objects.map((obj: any) => obj.title).sort();` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-903 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2869`

`Query.select(Filter.text('TypeScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-904 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2879`

`.query(Query.select(Filter.text('React', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]))` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-905 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2889`

`Query.select(Filter.text('JavaScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-906 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2939`

`Query.select(Filter.text('TypeScript', { type: 'full-text' })).from([Scope.feed(Feed.getFeedUri(feed)!)]),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-907 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2968`

`expect(entry.match!.rank).toBeDefined();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-908 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2969`

`expect(typeof entry.match!.rank).toBe('number');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-909 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2970`

`expect(entry.match!.rank).toBeGreaterThan(0);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-910 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2991`

`expect(entries[i].match!.rank).toBeGreaterThanOrEqual(entries[i + 1].match!.rank);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-911 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:2995`

`expect(entries[0].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-912 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3015`

`expect(entries[i].match!.rank).toBeLessThanOrEqual(entries[i + 1].match!.rank);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-913 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3019`

`expect(entries[entries.length - 1].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-914 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3038`

`expect(entry.match!.rank).toBe(1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-915 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3062`

`expect(entries[0].result!.title).toBe('TypeScript TypeScript TypeScript TypeScript');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-916 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3063`

`expect(entries[1].result!.title).toBe('TypeScript TypeScript TypeScript');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-917 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3123`

`Obj.update(objects[0], (o: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-918 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3211`

`const names = query.results.map((obj) => obj.name!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-919 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3311`

`updates.push([...query.results.map((obj) => obj.name!)].sort());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-920 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3408`

`expect((results[0] as any).value).toBe(42);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-921 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3423`

`const values = results.map((r) => (r as any).value).sort();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-922 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3438`

`expect((dbResults[0] as any).value).toBe(99);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-923 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3444`

`const values = bothResults.map((r) => (r as any).value).sort((a: number, b: number) => a - b);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-924 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3526`

`const names = objects.map((o: any) => o.name).sort();` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-925 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3615`

`const taskResults = objects.filter((obj: any) => obj.title === 'Grandchild task');` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-926 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3643`

`expect(objects.every((obj: any) => Obj.getTypename(obj) !== 'org.dxos.type.feed')).toBe(true);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-927 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3690`

`expect(objects.map((obj: any) => obj.title).sort()).toEqual(['A', 'B']);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-928 no-casts `packages/core/echo/echo-client-e2e/src/query.test.ts:3731`

`const names = objects.map((o: any) => o.name).sort();` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-929 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:48`

`.map((o) => (o as any).value)` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-930 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:64`

`expect((local.get(original.id) as any).value).toBe(999);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-931 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:78`

`expect((result[0] as any).value).toBe(42);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-932 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:87`

`return registry.list().map((o) => (o as any).value);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-933 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:169`

`expect(expandoResults.map((o) => (o as any).value).sort()).toEqual([1, 2]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-934 no-casts `packages/core/echo/echo-client-e2e/src/registry.test.ts:185`

`expect((q.results[0] as any).value).toBe(10);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-935 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:37`

`expect(() => Obj.make(Organization.Organization, { status: 'invalid-status' } as any)).toThrow();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-936 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:41`

`expect(() => Obj.make(Organization.Organization, { name: 123 } as any)).toThrow();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-937 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:46`

`expect(() => Obj.make(Organization.Organization, { name: 'Acme', unknownField: 'extra' } as any)).toThrow(` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-938 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:55`

`(org as any).unknownField = 'extra';` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-939 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:62`

`expect(() => db.add(Obj.make(Organization.Organization, { status: 'invalid-status' } as any))).toThrow();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-940 no-casts `packages/core/echo/echo-client-e2e/src/schema-validation.test.ts:68`

`expect(() => Obj.make(Task.Task, {} as any)).toThrow(/title/i);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-941 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:249`

`const typeRow = data.objects.find((o: any) => o['@meta']?.key === typename);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-942 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:269`

`expect(Type.isType(roundTrip!)).to.be.true;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-943 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:270`

`expect(Type.getTypename(roundTrip!)).to.eq(typename);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-944 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:276`

`expect(() => Type.getTypename(schema as any)).not.to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-945 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:278`

`expect(entities.some((schema) => Type.getTypename(schema as any) === typename)).to.be.true;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-946 no-casts `packages/core/echo/echo-client-e2e/src/serializer.test.ts:318`

`const main = objects.find((object) => object.title === 'Main task')!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-947 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:36`

`#schema!: Schema.Codec<any, any>;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-948 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:37`

`#value!: unknown;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-949 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:50`

`return (this.#value as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-950 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:55`

`(this.#value as any)[key] = value;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-951 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:73`

`return livePrototypeCache.get(ast)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-952 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:90`

`: function set(this: LiveProto, value: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-953 no-casts `packages/core/echo/echo-client-e2e/src/static-typed-object.test.ts:98`

`const proto: LiveProto = {} as any; // [Cell] will be defined in the object factory.` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-954 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:41`

`const hostHandle = await host.loadDoc<{ text: string }>(Context.default(), clientHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-955 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:50`

`clientHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-956 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:64`

`hostHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-957 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:68`

`expect(clientHandle.doc()!.text).to.equal(text);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-958 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:102`

`const handle2 = repo2.find<{ text: string }>(handle1.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-959 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:112`

`handle2.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-960 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:134`

`url = clientHandle.url!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-961 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:135`

`clientHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-962 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:234`

`url = clientHandle.url!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-963 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:269`

`url = clientHandle.url!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-964 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:297`

`const hostHandle = await host.loadDoc<{ client: number; host: number }>(Context.default(), handle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-965 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:302`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-966 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:306`

`hostHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-967 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:337`

`const hostHandle = await host.loadDoc<{ text: string }>(Context.default(), cloneHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-968 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:358`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-969 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:373`

`const loaded = await host.loadDoc<{ text: string }>(Context.default(), handle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-970 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:417`

`const foundHandle = repo2.find<DocStruct>(handle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-971 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:422`

`handles2.push(repo2.find<DocStruct>(handle.url!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-972 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:427`

`const foundHandle = repo1.find<DocStruct>(handle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-973 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.test.ts:432`

`handles1.push(repo1.find<DocStruct>(handle.url!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-974 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:285`

`this._sendUpdatesJob!.trigger();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-975 no-casts `packages/core/echo/echo-client/src/automerge/repo-proxy.ts:295`

`this._pendingUpdateIds.add(handle.documentId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# WARN cc9b81fc-976 no-sleep-in-test `packages/core/echo/echo-client/src/client/index-query-source-provider.test.ts:176`

`await new Promise((resolve) => setTimeout(resolve, 10))` gates `expect(calls).toHaveLength(0)` — a negative assertion synchronized purely by a fixed 10ms delay. Use a deterministic wait (e.g. flush the relevant queue/observable) instead of sleep.

# ERROR cc9b81fc-977 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.test.ts:238`

`loaded = { id: objectId } as unknown as Entity.Unknown;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-978 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:241`

`} catch (err: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-979 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:277`

`} catch (err: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-980 no-casts `packages/core/echo/echo-client/src/client/index-query-source-provider.ts:355`

`} catch (err: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-981 no-casts `packages/core/echo/echo-client/src/core-db/branching.ts:45`

`const hash = queue.pop()!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-982 no-casts `packages/core/echo/echo-client/src/core-db/doc-semaphore.ts:13`

`export const docChangeSemaphore = (handle: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-983 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:69`

`const textHandle = handles.linkedDocHandles[0]!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-984 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:74`

`textHandle.change((newDocument: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-985 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:79`

`const text = db.getObjectById(object.id)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-986 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:93`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-987 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:95`

`expect((retrievedObject as any).title).to.eq(newObject.title);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-988 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:104`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-989 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:113`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-990 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:120`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-991 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:137`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-992 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:147`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-993 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:168`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-994 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:174`

`getObjectDocHandle(db.getObjectById(ids[0])).change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-995 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:179`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-996 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:181`

`expect((db.getObjectById(ids[0]) as any).content).to.eq(contents[0]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-997 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:201`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-998 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:204`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-999 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:215`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1000 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:218`

`oldRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1001 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:225`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1002 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:236`

`Obj.update(rootObject, (rootObject: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1003 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:239`

`.forEach((obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1004 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:248`

`newRootDocHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1005 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:264`

`await db.setSpaceRoot(newRootDocHandle.url!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1006 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:293`

`const rootUrl = db.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1007 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:304`

`expect((object as any).title).to.eq('first object');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1008 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:385`

`const getObjectDocHandle = (obj: any) => getObjectCore(obj).docHandle!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1009 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:385`

`const getObjectDocHandle = (obj: any) => getObjectCore(obj).docHandle!;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1010 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:407`

`return peer2.openDatabase(spaceKey, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1011 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.test.ts:425`

`docHandle.change((newDoc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1012 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:167`

`private _updateScheduler!: UpdateScheduler;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1013 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:262`

`const entries = await this._branchStore!.load();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1014 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:291`

`.then(() => this._branchStore!.save(entries))` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1015 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:475`

`this._objects.get(objectToLoad.id) && !this._areDepsSatisfied(this._objects.get(objectToLoad.id)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1016 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:484`

`result[objectToLoad.resultIndex] = this.getObjectCoreById(objectToLoad.id)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1017 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:563`

`delete doc.links![objectId];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1018 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:593`

`delete draft.objects![id];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1019 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:596`

`delete draft.links![id];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1020 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:599`

`delete draft.branches![id];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1021 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:619`

`const toUnlink = objects.filter((o) => o?.isDeleted()).map((o) => o!.id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1022 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:652`

`newStruct.system!.type = EncodedReference.fromURI(type);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1023 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1163`

`const core = queue.shift()!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1024 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1556`

`objectsToRebind.get(spaceRootUrl)!.objectIds.push(object.id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1025 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1558`

`const newObjectDocUrl = linkedObjectIds.get(object.id)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1026 no-casts `packages/core/echo/echo-client/src/core-db/entity-manager.ts:1751`

`request.stateChanged.on(this._ctx!, () => this._scheduleThrottledUpdate([core.id]));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1027 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:233`

`this.doc = A.change(this.doc!, options, changeFn);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1028 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:235`

`this.doc = A.change(this.doc!, changeFn);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1029 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:257`

`const { newDoc, newHeads } = A.changeAt(this.doc!, heads, options, callback);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1030 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:261`

`const { newDoc, newHeads } = A.changeAt(this.doc!, heads, callback);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1031 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:318`

`} catch (err: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1032 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:376`

`decode(value: any): DecodedAutomergePrimaryValue {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1033 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:419`

`value = (value as any)?.[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1034 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:425`

`private _setRaw(path: Doc.KeyPath, value: any): void {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1035 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:457`

`return (this._getRaw([SYSTEM_NAMESPACE, 'kind']) as any) ?? EntityKind.Object;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1036 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:554`

`for (const hash of A.getHeads(doc as any)) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1037 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:555`

`const decoded = A.inspectChange(doc as any, hash);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1038 no-casts `packages/core/echo/echo-client/src/core-db/object-core.ts:566`

`return this.getDecoded([META_NAMESPACE]) as unknown as EntityMeta;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1039 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:114`

`expect(partial!.id).toEqual(mainObjectId);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1040 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:287`

`expect(db.areStrongDepsSatisfied(core!)).toBe(false);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1041 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:316`

`expect(resolved!.id).toEqual(object.id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1042 no-casts `packages/core/echo/echo-client/src/core-db/strong-deps-stall.test.ts:413`

`newDoc.links![depObjectId] = new A.RawString(depDocHandle.url);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1043 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:43`

`Obj.update(binding.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1044 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:83`

`Obj.update(binding1.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1045 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:86`

`Obj.update(binding2.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1046 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:117`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1047 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:120`

`Obj.update(binding.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1048 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:145`

`Obj.update(binding.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1049 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:172`

`Obj.update(writer.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1050 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:180`

`Obj.update(writer.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1051 no-casts `packages/core/echo/echo-client/src/echo-handler/branch-binding.test.ts:195`

`Obj.update(binding.object, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1052 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:54`

`const registry = db._entityManager.getBranchRegistry(root.id)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1053 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:80`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1054 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:83`

`Obj.update(child, (child: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1055 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:105`

`Obj.update(child, (child: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1056 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:122`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1057 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:132`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1058 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:137`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1059 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:156`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1060 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:159`

`Obj.update(child, (child: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1061 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:181`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1062 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:184`

`Obj.update(child, (child: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1063 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:228`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1064 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:245`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1065 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:250`

`core!.changeAt(baseHeads, (doc: any) => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1066 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:250`

`core!.changeAt(baseHeads, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1067 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:295`

`Obj.update(root1, (root1: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1068 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:298`

`Obj.update(child1, (child1: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1069 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:304`

`await using db2 = await peer2.openDatabase(spaceKey, db1.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1070 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:344`

`Obj.update(root, (root: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1071 no-casts `packages/core/echo/echo-client/src/echo-handler/branching.test.ts:347`

`Obj.update(child, (child: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1072 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:59`

`(clone as any)[symbolInternals as any].linkCache!.set(ref.id, ref);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1073 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:59`

`(clone as any)[symbolInternals as any].linkCache!.set(ref.id, ref);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1074 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:79`

`coreClone.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1075 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:85`

`return proxy as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1076 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:88`

`const getObjectDoc = (core: ObjectCore): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1077 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:89`

`let value = core.doc ?? core.docHandle!.doc();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1078 no-casts `packages/core/echo/echo-client/src/echo-handler/clone.ts:91`

`value = (value as any)?.[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1079 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:20`

`export const getHeader = (tag: string, id: string, config?: any): JsonML => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1080 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:31`

`const formatValue = (object: any, config?: any): JsonML => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1081 no-casts `packages/core/echo/echo-client/src/echo-handler/devtools-formatter.ts:41`

`export const getBody = (objData: any): JsonML => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1082 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:18`

`[symbolInternals]: ObjectCore = null as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1083 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:19`

`[symbolPath]: Doc.KeyPath = null as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1084 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:20`

`[symbolNamespace]: string = null as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1085 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:21`

`[symbolHandler]: EchoReactiveHandler = null as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1086 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:34`

`let result!: any;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1087 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-array.ts:37`

`result = ((handler as any)[handlerMethodName] as Function).apply(handler, [this, this[symbolPath], ...args]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1088 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:44`

`expect(() => createObject(Obj.make(invalidSchema, { id: 42 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1089 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:134`

`}).pipe(EchoObjectSchema(DXN.make('com.example.type.test', '0.1.0'))) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1090 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:140`

`expect(prepareAstForCompare(Type.getSchema(type!).ast)).to.deep.eq(` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1091 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:150`

`expect(prepareAstForCompare(Type.getSchema(Obj.getType(obj.nested as Obj.Unknown)!).ast)).to.deep.eq(` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1092 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:213`

`expect(() => graph.registry.add([NonEchoSchema as unknown as Type.AnyEntity])).to.throw();` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1093 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:405`

`const objData: any = (obj as any).toJSON();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1094 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:419`

`const objData: any = Obj.toJSON(obj as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1095 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:454`

`for (const value of [object.field, object.nested.deep.field, ...object.array.map((o: any) => o.field)]) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1096 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:497`

`expect(obj.reference['/']).to.eq(EID.make({ entityId: obj1.reference!.target!.id }));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1097 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:573`

`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1098 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:574`

`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1099 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:590`

`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1100 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:591`

`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1101 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:609`

`contact.tasks!.push(Ref.make(task1));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1102 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:610`

`contact.tasks!.push(Ref.make(task2));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1103 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:617`

`expect(contact.tasks![0].target).to.eq(task1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1104 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:618`

`expect(contact.tasks![1].target).to.eq(task2);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1105 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:619`

`expect(task2.previous!.target).to.eq(task1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1106 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:636`

`expect(person.previousEmployment![0]!.target?.name).to.eq('DXOS');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1107 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:637`

`expect(person.previousEmployment![1]!.target?.name).to.eq('Braneframe');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1108 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:703`

`expect(() => Obj.isDeleted({} as any)).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1109 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:727`

`expect(() => Obj.getMeta({} as any)).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1110 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:736`

`expect(() => Obj.update(obj, (obj) => Obj.getMeta(obj).keys.push(1 as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1111 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:858`

`expect(Obj.getMeta(obj.objects[0]!.target!).keys).to.deep.eq([key]);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1112 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:1057`

`expect(dbObj.queue.uri).to.include(queueId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1113 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.test.ts:1082`

`(obj as any).id = '123';` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1114 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:103`

`invariant(!(target as any)[symbolIsProxy]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1115 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:110`

`delete (target as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1116 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:168`

`get(target: ProxyTarget, prop: string | symbol, receiver: any): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1117 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:218`

`set(target: ProxyTarget, prop: string | symbol, value: any, receiver: any): boolean {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1118 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:294`

`return array as any as ProxyTarget;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1119 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:349`

`private _validateValue(target: ProxyTarget, path: Doc.KeyPath, value: any): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1120 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:378`

`private _handleLinksAssignment(target: ProxyTarget, value: any): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1121 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:439`

`arrayPop(target: ProxyTarget, path: Doc.KeyPath): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1122 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:444`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1123 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:453`

`arrayShift(target: ProxyTarget, path: Doc.KeyPath): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1124 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:458`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1125 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:474`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1126 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:492`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1127 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:506`

`arraySort(target: ProxyTarget, path: Doc.KeyPath, compareFn?: (v1: any, v2: any) => number): any[] {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1128 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:510`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1129 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:524`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1130 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:537`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1131 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:549`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1132 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:590`

`createRef(target: ProxyTarget, proxy: any): URI.URI {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1133 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:599`

`const selfUri = (otherEchoObj as any)[SelfURIId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1134 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:683`

`target[symbolInternals].change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1135 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:713`

`inspectFn: (value: any, options?: InspectOptionsStylized) => string,` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1136 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:733`

`export const throwIfCustomClass = (prop: Doc.KeyPath[number], value: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1137 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:752`

`return getDeep(core.getDoc(), core.mountPath)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1138 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:758`

`const isEchoObjectField = (value: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1139 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:769`

`const isDetachedObjectField = (value: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1140 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:803`

`export const isTypedObjectProxy = (value: any): value is any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1141 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:827`

`const type = Obj.getType(obj as unknown as Obj.Unknown);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1142 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:839`

`const meta = getProxyTarget<EntityMeta>(Entity.getMeta(obj as unknown as Entity.Unknown));` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1143 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:859`

`Object.defineProperty(target, symbol, Object.getOwnPropertyDescriptor(previousState, symbol)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1144 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:903`

`const target = createRecordTarget(createInstanceState(core, DATA_NAMESPACE, []), obj as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1145 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:929`

`const seededMeta = (obj as any)[MetaId] as EntityMeta | undefined;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1146 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:934`

`return proxy as unknown as CreateObjectReturn<T>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1147 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:967`

`const parentValue = (target as any)[ParentId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1148 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:973`

`delete (target as any)[ParentId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1149 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:997`

`const obj = createProxy<ProxyTarget>(target, EchoReactiveHandler.instance) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1150 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1035`

`const sourceRef = (target as any)[RelationSourceId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1151 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1036`

`const targetRef = (target as any)[RelationTargetId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1152 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1052`

`const validateInitialProps = (target: any, seen: Set<object> = new Set()) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1153 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:1067`

`} else if ((value as any) instanceof Uint8Array) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1154 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-object-utils.ts:17`

`const target = (obj as any as ProxyTarget)[symbolInternals];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1155 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-object-utils.ts:40`

`export const isEchoObject = (value: any): value is Obj.Unknown => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1156 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:141`

`export const getReified = (target: ProxyTarget): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1157 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:244`

`return (schema as any)[SchemaMetaSymbol].typename;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1158 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:254`

`const resolveStrongDepFromWorkingSet = (database: EchoDatabase, uri: URI.URI): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1159 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:267`

`const resolveEndpoint = (core: ObjectCore, ref: EncodedReference): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1160 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:272`

`export const getParent = (target: ProxyTarget): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1161 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:277`

`export const setParent = (target: ProxyTarget, value: any): void => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1162 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:288`

`export const getRelationSource = (target: ProxyTarget): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1163 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:294`

`export const getRelationTarget = (target: ProxyTarget): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1164 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:304`

`const getSchemaKind = (target: ProxyTarget, receiver: any): EntityKind | undefined => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1165 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:307`

`const jsonSchemaEntityKind = (receiver as any).jsonSchema?.entityKind;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1166 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:319`

`const getStaticTypeSchemaSlot = (target: ProxyTarget, receiver: any): Schema.Codec<any, any> | undefined => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1167 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:360`

`return createProxy(metaTarget, getProxyHandler(self)) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1168 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:368`

`export const handleStoredSchema = (target: ProxyTarget, object: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1169 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:413`

`[ATTR_META]: compactMeta(getMeta(self)) as unknown as EntityMetaJSON,` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1170 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:451`

`header: (config?: any) => getHeader(typename ?? 'EchoObjectSchema', target[symbolInternals].id, config),` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1171 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:552`

`get [ParentId](): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1172 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:556`

`set [ParentId](value: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1173 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:562`

`get [ChangeId](): (callback: (mutableObj: any) => void) => void {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1174 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:570`

`return (callback: (mutableObj: any) => void) => executeChange(core, target, receiver, callback);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1175 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:583`

`get [RelationSourceId](): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1176 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:587`

`get [RelationTargetId](): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1177 no-casts `packages/core/echo/echo-client/src/echo-handler/echo-prototypes.ts:698`

`delete (target as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1178 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.test.ts:79`

`expect((snapshot as any).description).toBe('aaa');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1179 no-casts `packages/core/echo/echo-client/src/echo-handler/edit-history.ts:148`

`} as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1180 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:91`

`expect(obj.nested!.field).to.eq('bar');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1181 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:92`

`expect(obj.nested!).to.deep.eq(plainObject);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1182 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:95`

`obj.nested!.field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1183 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:97`

`expect(obj.nested!.field).to.eq('baz');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1184 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:118`

`obj.stringArray![0] = '4';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1185 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:126`

`expect(obj.nestedNullableArray![0]).to.deep.eq(circle);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1186 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:140`

`(obj.nestedNullableArray![2] as any).field = 'rectangle';` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1187 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:140`

`(obj.nestedNullableArray![2] as any).field = 'rectangle';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1188 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:142`

`expect((obj.nestedNullableArray![2] as any).field).to.eq('rectangle');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1189 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:142`

`expect((obj.nestedNullableArray![2] as any).field).to.eq('rectangle');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1190 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:152`

`expect(() => Obj.update(obj, (obj) => (obj.string = 1 as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1191 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:153`

`expect(() => Obj.update(obj, (obj) => (obj.nested = { field: 1 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1192 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:155`

`expect(() => Obj.update(obj, (obj) => (obj.nested!.field = 1 as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1193 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:155`

`expect(() => Obj.update(obj, (obj) => (obj.nested!.field = 1 as any))).to.throw();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1194 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:156`

`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.push({ field: 1 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1195 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:157`

`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.unshift({ field: 1 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1196 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:158`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0] = { field: 1 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1197 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:158`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0] = { field: 1 } as any))).to.throw();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1198 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:159`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0].field = 1 as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1199 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:159`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![0].field = 1 as any))).to.throw();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1200 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:161`

`expect(() => Obj.update(obj, (obj) => obj.nestedArray?.splice(1, 0, { field: 1 } as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1201 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:162`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![1].field = 1 as any))).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1202 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:162`

`expect(() => Obj.update(obj, (obj) => (obj.nestedArray![1].field = 1 as any))).to.throw();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1203 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:177`

`expect(obj.nestedArray![0].field).to.eq('bar');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1204 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:180`

`obj.nestedArray![0].field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1205 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:182`

`expect(obj.nestedArray![0].field).to.eq('baz');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1206 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:185`

`obj.nestedArray![1].field = 'bar';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1207 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:187`

`expect(obj.nestedArray![1].field).to.eq('bar');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1208 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:199`

`expect(obj.twoDimNumberArray![0][0]).to.eq(1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1209 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:202`

`obj.twoDimNumberArray![0][0] = 4;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1210 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:204`

`expect(obj.twoDimNumberArray![0][0]).to.eq(4);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1211 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:237`

`expect(obj.nested!.field).to.eq('bar');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1212 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:243`

`obj.nested!.field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1213 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:246`

`expect(obj.nested!.field).to.eq('baz');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1214 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:299`

`expect({ ...obj }).to.deep.eq({ ...TEST_OBJECT, id: (obj as any).id });` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1215 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:313`

`expect(actual).to.deep.contain({ id: (obj as any).id, ...expected });` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1216 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:319`

`const expected = objectsHaveId ? { ...TEST_OBJECT, id: (obj as any).id } : TEST_OBJECT;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1217 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:326`

`const expected = objectsHaveId ? { ...TEST_OBJECT, id: (obj as any).id } : TEST_OBJECT;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1218 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:401`

`obj.nested!.field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1219 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:412`

`obj.stringArray![0] = '42';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1220 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:423`

`obj.nestedArray![0].field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1221 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:434`

`obj.twoDimNumberArray![0][0] = 4;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1222 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:461`

`return { array: obj.stringArray!, parent: obj };` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1223 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:469`

`parent.stringArray![0] = '2';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1224 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:481`

`parent.stringArray!.push('4');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1225 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:492`

`parent.stringArray!.length = 2;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1226 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:503`

`parent.stringArray!.push('4');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1227 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:515`

`value = parent.stringArray!.pop();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1228 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:528`

`value = parent.stringArray!.shift();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1229 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:541`

`newLength = parent.stringArray!.unshift('0');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1230 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:554`

`removed = parent.stringArray!.splice(1, 1, '4');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1231 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:567`

`returnValue = parent.stringArray!.sort();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1232 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:586`

`returnValue = parent.stringArray!.reverse();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1233 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.blueprint-test.ts:617`

`const array = obj.twoDimNumberArray!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1234 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.test.ts:21`

`return Obj.make(schema, props) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1235 no-casts `packages/core/echo/echo-client/src/echo-handler/reactive-proxy.test.ts:42`

`const object = Obj.make(schema, props as any) as TestSchema.Example;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1236 no-casts `packages/core/echo/echo-client/src/echo-handler/subscription.test.ts:299`

`const createUpdateCounter = (object: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1237 no-casts `packages/core/echo/echo-client/src/echo-handler/version.ts:23`

`export const getVersion = (obj: any): ObjectVersion => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1238 no-casts `packages/core/echo/echo-client/src/feed/feed-handle.ts:675`

`const isSqliteNotOpenError = (err: any) => err.cause?.message?.includes('The database connection is not open');` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1239 no-casts `packages/core/echo/echo-client/src/feed/feed-object-core.ts:248`

`const mutable = target as unknown as Record<string, unknown>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1240 no-casts `packages/core/echo/echo-client/src/feed/feed-object-core.ts:249`

`const sourceRecord = source as unknown as Record<string, unknown>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1241 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:115`

`expect(results.map((item: any) => item.name).sort()).toEqual(['alice', 'bob']);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1242 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:399`

`const ref = container.objects![0];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1243 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:401`

`expect(EID.getEntityId(EID.parse(ref.uri))).toBe((queuePost as any).id);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1244 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:409`

`expect((resolved as any).name).toBe('alice');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1245 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:433`

`postRefUri = container.objects![0]!.uri;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1246 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:440`

`await using db2 = await peer.openDatabase(spaceKey, db1.rootUrl!, { client: client2 });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1247 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:444`

`const postRef = container.objects![0]!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1248 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:450`

`const postId = EID.getEntityId(postRef.uri)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1249 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:470`

`let feedA!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1250 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:471`

`let feedB!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1251 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:490`

`let dataFeed!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1252 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:491`

`let traceFeed!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1253 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:524`

`let traceFeed!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1254 no-casts `packages/core/echo/echo-client/src/feed/feed.test.ts:551`

`let traceFeed!: Feed.Feed;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1255 no-casts `packages/core/echo/echo-client/src/proxy-db/add-type.test.ts:55`

`expect(() => db.add(TestType as any)).to.throw();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1256 no-casts `packages/core/echo/echo-client/src/proxy-db/add-type.test.ts:62`

`expect(Type.getTypename(Obj.getType(object)!)).to.eq(Type.getTypename(TestType));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1257 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:71`

`rootUrl = db.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1258 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:195`

`rootUrl = db.rootUrl!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1259 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:233`

`expect(Type.getSchema(Obj.getType(task)!).ast).to.eq(Type.getSchema(TestSchema.Task).ast);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1260 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:260`

`expect(container.records![0].type).to.eq(TestSchema.RecordType.WORK);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1261 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:272`

`container.objects!.push(Ref.make(Obj.make(TestSchema.Expando, { foo: 100 })));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1262 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:273`

`container.objects!.push(Ref.make(Obj.make(TestSchema.Expando, { bar: 200 })));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1263 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:281`

`const target1 = await container.objects![0].load();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1264 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:282`

`const target2 = await container.objects![1].load();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1265 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:286`

`expect((target1 as any).foo).to.equal(100);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1266 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:287`

`expect((target2 as any).bar).to.equal(200);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1267 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:299`

`container.objects!.push(Ref.make(Obj.make(TestSchema.Task, {})));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1268 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:300`

`container.objects!.push(Ref.make(Obj.make(TestSchema.Person, {})));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1269 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:308`

`expect(Obj.getTypename(container.objects![0].target!)).to.equal(Type.getTypename(TestSchema.Task));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1270 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:309`

`expect(Obj.getTypename(container.objects![1].target!)).to.equal(Type.getTypename(TestSchema.Person));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1271 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:382`

`expect(Obj.getTypename(task.subTasks![0].target!)).to.eq('com.example.type.task');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1272 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:383`

`expect(JSON.parse(JSON.stringify(task.subTasks![0].target))['@type']).to.eq('dxn:com.example.type.task:0.1.0');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1273 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:389`

`const version1 = Obj.version(task as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1274 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:393`

`const version2 = Obj.version(task as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1275 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:401`

`const version3 = Obj.version(task as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1276 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:414`

`range(3).forEach(() => root.subTasks!.push(Ref.make(newTask())));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1277 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:415`

`root.subTasks!.push(Ref.make(newTask()), Ref.make(newTask()));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1278 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:419`

`expect(root.subTasks!.length).to.eq(5);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1279 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:423`

`const ids = root.subTasks!.map((task: any) => task.target!.id);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1280 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:423`

`const ids = root.subTasks!.map((task: any) => task.target!.id);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1281 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:424`

`root.subTasks!.forEach((task: any, i: number) => expect(task.target!.id).to.eq(ids[i]));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1282 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:424`

`root.subTasks!.forEach((task: any, i: number) => expect(task.target!.id).to.eq(ids[i]));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1283 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:425`

`expect(Array.from(root.subTasks!.values())).to.have.length(5);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1284 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:434`

`expect(root.subTasks!.length).to.eq(3);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1285 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:445`

`root.subTasks!.splice(0, 2, Ref.make(newTask()));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1286 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:454`

`root.records!.push({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1287 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:462`

`const queriedContainer = (await db.query(Filter.type(TestSchema.Container)).run())[0]!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1288 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:463`

`expect(queriedContainer.records!.length).to.equal(1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1289 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:464`

`expect(queriedContainer.records![0]!.contacts![0]!.target!.name).to.equal('tester');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1290 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:471`

`root.records!.push({ title: 'one' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1291 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:483`

`root.records!.push({ title: 'two' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1292 no-casts `packages/core/echo/echo-client/src/proxy-db/database.test.ts:694`

`const expectObjects = (echoObjects: any[], expectedObjects: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1293 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:542`

`return match as unknown as T;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1294 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:545`

`return this._addPersistentSchema(type) as unknown as T;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1295 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:741`

`const output = (await migration.transform(object, { db: this })) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1296 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:747`

`delete (output as any).id;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1297 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:752`

`meta: metaPatch as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1298 no-casts `packages/core/echo/echo-client/src/proxy-db/database.ts:1029`

`const createSchemaNotRegisteredError = (schema?: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1299 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:33`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1300 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:56`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1301 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:59`

`const [foo, bar] = await loadObjectReferences(loaded, (o) => [o.foo, o.bar] as any[]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1302 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:79`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1303 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:81`

`expect((loaded.nestedArray as any[]).every((v) => v == null)).to.be.true;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1304 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:82`

`const loadedArray = await loadObjectReferences(loaded, (o) => o.nestedArray as any[]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1305 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:111`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1306 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:113`

`const loadedArrays = await loadObjectReferences(loaded, (o) => o.nestedArray as any[]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1307 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:133`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1308 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:152`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1309 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:186`

`const restartedDb = await restartedPeer.openDatabase(spaceKey, db.rootUrl!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1310 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:188`

`const loadedNested = await loadObjectReferences(loaded!, (o) => o.nested.map((n: any) => n.target));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1311 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.test.ts:188`

`const loadedNested = await loadObjectReferences(loaded!, (o) => o.nested.map((n: any) => n.target));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1312 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:17`

`return getObjectCore(obj).getDecoded(['data']) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1313 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:43`

`const core = getObjectCore(obj as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1314 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:51`

`? () => (valueAccessor(obj) as any[]).every((v) => v != null)` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1315 no-casts `packages/core/echo/echo-client/src/proxy-db/load-object.ts:65`

`return (Array.isArray(objOrArray) ? result : result[0]) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1316 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:66`

`const validator = Schema.decodeSync(Schema.toType(Type.getSchema(instanceWithSchemaRef.schema!.target!)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1317 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:80`

`expect(Type.getTypename(instanceWithSchemaRef.schema!.target!)).to.eq(Type.getTypename(GeneratedSchema));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1318 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:93`

`instanceWithSchemaRef.schemaArray!.push(Ref.make(schema));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1319 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:95`

`expect(Type.getTypename(instanceWithSchemaRef.schemaArray![0].target!)).to.eq(Type.getTypename(GeneratedSchema));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1320 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:120`

`expect(Type.getSchema(Obj.getType(object)!).ast).to.deep.eq(Type.getSchema(schema).ast);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1321 no-casts `packages/core/echo/echo-client/src/proxy-db/mutable-schema.test.ts:132`

`const uri = Type.getURI(schema)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1322 no-casts `packages/core/echo/echo-client/src/proxy-db/relations.test.ts:69`

`await testBuilder.lastPeer!.reload();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1323 no-casts `packages/core/echo/echo-client/src/proxy-db/relations.test.ts:71`

`const db = await testBuilder.lastPeer!.openLastDatabase();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1324 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:238`

`if (this._results!.find((result) => result.id === objectId)) {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1325 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:243`

`const trivial = isSimpleSelectionQuery(this._query!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1326 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:300`

`this._results = this._computeResults(this._query!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1327 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:303`

`return this._results!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1328 no-casts `packages/core/echo/echo-client/src/query/graph-query-context.ts:369`

`? { key: item.groupKey, count: groupCounts?.get(serializedGroupKey!) ?? 1 }` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1329 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:61`

`return this._resultCache!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1330 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:67`

`return this._objectCache!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1331 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:113`

`return this._objectCache!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1332 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:123`

`return this._resultCache!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1333 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:194`

`this._objectCache as unknown as GroupResult[] | undefined,` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1334 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:195`

`presented.objects as unknown as GroupResult[],` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1335 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:231`

`objects: groups as unknown as T[],` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1336 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:232`

`entries: groupEntries as unknown as QueryResult.EntityEntry<T>[],` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1337 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:245`

`.filter((object: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1338 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:338`

`members.get(serializedKey)!.push(entry.result);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1339 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:343`

`const groupMembers = members.get(serializedKey)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1340 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:345`

`const record: GroupResult = { ...keys.get(serializedKey)! };` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1341 no-casts `packages/core/echo/echo-client/src/query/query-result.ts:350`

`record[aggregate.name] = _computeAggregate(aggregate, groupMembers, counts.get(serializedKey)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1342 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:95`

`const ids = results!.map((item) => item.objectId);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1343 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:165`

`const ids = results!.map((item) => item.objectId);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1344 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:188`

`const names = personResults.map((item) => item.core!.getObjectStructure().data['name']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1345 no-casts `packages/core/echo/echo-client/src/query/working-set-executor.test.ts:341`

`const first = firstIndexByAge.get(age)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1346 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:297`

`return this.runSyncEntries().map((entry) => entry.result!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1347 no-casts `packages/core/echo/echo-client/src/registry/registry.ts:304`

`result: entity as unknown as T,` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1348 no-casts `packages/core/echo/echo-client/src/serializer.ts:48`

`return this.exportObject(object as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1349 no-casts `packages/core/echo/echo-client/src/testing/test-database-layer.ts:75`

`yield* Effect.promise(() => db!.flush());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1350 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:23`

`export const getHeader = (obj: any /* TypedObject */, config?: any): JsonML => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1351 no-casts `packages/core/echo/echo-client/src/util/devtools-formatter.ts:34`

`const formatValue = (object: any, config?: any): JsonML => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1352 no-casts `packages/core/echo/echo-client/src/util/migrate-document.ts:19`

`const coalesce = (applyTo: any, targetData: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1353 no-casts `packages/core/echo/echo-doc/src/record.ts:17`

`export const encode = (value: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1354 no-casts `packages/core/echo/echo-doc/src/record.ts:47`

`export const decode = (value: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1355 no-casts `packages/core/echo/echo-doc/src/record.ts:80`

`export const getDeep = (obj: any, path: Doc.KeyPath, init = false): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1356 no-casts `packages/core/echo/echo-doc/src/store-adapter.test.ts:24`

`accessor.handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1357 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:51`

`encode?: (value: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1358 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:53`

`decode?: (value: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1359 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:68`

`readonly #encode: (value: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1360 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:69`

`readonly #decode: (value: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1361 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:208`

`const accessor = this.#accessor!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1362 no-casts `packages/core/echo/echo-doc/src/store-adapter.ts:218`

`return batch.deleted ? elements?.filter((element) => !batch.deleted!.includes(element.id)) : elements;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1363 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:62`

`schemaId.push(Obj.getTypeURI(organization)!.toString());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1364 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:69`

`schemaId.push(Obj.getTypeURI(organization)!.toString());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1365 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:84`

`const beforeChangesCount = A.getAllChanges(getObjectCore(document).docHandle!.doc()).length;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1366 no-casts `packages/core/echo/echo-generator/src/generator.test.ts:91`

`const changesCount = A.getAllChanges(getObjectCore(document).docHandle!.doc()).length;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1367 no-casts `packages/core/echo/echo-generator/src/generator.ts:64`

`const tasks = Object.entries<number>(map as any)` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1368 no-casts `packages/core/echo/echo-generator/src/generator.ts:137`

`const type = Type.getTypename(Obj.getType(object)!) as T;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1369 no-casts `packages/core/echo/echo-generator/src/generator.ts:140`

`await this._mutations![type](object, params);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1370 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:37`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1371 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:41`

`expect(handle.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1372 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:49`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1373 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:61`

`expect(handle2.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1374 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:89`

`const expectedHeads = getHeads(handle.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1375 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:110`

`const expectedHeads: (Heads | undefined)[] = handles.map((handle) => getHeads(handle.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1376 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:148`

`expect(loaded.doc()!.text).toEqual('Hello from Subduction');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1377 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:178`

`expect(loaded.doc()!.text).toEqual('Hello from Subduction');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1378 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:210`

`expect(loaded.doc()!.text).toEqual('Hello from Subduction');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1379 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:289`

`expect(loaded.doc()!.text).toEqual('authorized');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1380 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:395`

`((host1 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1381 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:398`

`((host2 as any)._repo as any).on('subduction-peer-bound', (b: SubductionPeerBinding) =>` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1382 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:464`

`h1.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1383 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host-subduction.test.ts:467`

`h2.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1384 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:28`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1385 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:32`

`expect(handle.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1386 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:83`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1387 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:98`

`expect(handle2.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1388 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:130`

`const expectedHeads = A.getHeads(handle.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1389 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:152`

`const expectedHeads: (Heads | undefined)[] = handles.map((handle) => A.getHeads(handle.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1390 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:199`

`expect(loaded.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1391 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:207`

`expect(localOnly.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1392 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:246`

`const adapter = (host2 as unknown as { _echoNetworkAdapter: EchoNetworkAdapter })._echoNetworkAdapter;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1393 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.test.ts:256`

`expect(loaded.doc()!.text).toEqual('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1394 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:167`

`private _repo!: Repo;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1395 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:168`

`private _storage!: SqliteStorageAdapter;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1396 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:179`

`private _peerId!: PeerId;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1397 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:251`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1398 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:341`

`((e: PeerCandidatePayload) => !updatingAuthScope && this._onPeerConnected(e.peerId)) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1399 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:345`

`((e: PeerDisconnectedPayload) => !updatingAuthScope && this._onPeerDisconnected(e.peerId)) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1400 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:484`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1401 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:549`

`return bufferToArray(Buffer.concat(chunks.map((c) => c.data!)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1402 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:621`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1403 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:632`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1404 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:644`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1405 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:670`

`await waitForHeads(handle, entry.heads!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1406 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:861`

`const handle = this._repo.getHandle(documentId as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1407 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:949`

`result.push(getHeads(handle.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1408 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:1019`

`result.peers!.push({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1409 no-casts `packages/core/echo/echo-host/src/automerge/automerge-host.ts:1163`

`this._sharePolicyChangedTask!.schedule();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1410 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:168`

`holder.import(aBytes!, { docId: docA.documentId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1411 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:169`

`holder.import(bBytes!, { docId: docB.documentId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1412 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:264`

`holder.import(aBytes!, { docId: docA.documentId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1413 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:265`

`holder.import(bBytes!, { docId: docB.documentId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1414 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:743`

`requestor: (c.args[0] as any).toString(),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1415 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:744`

`author: (c.args[1] as any).toString(),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1416 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction-policy.test.ts:952`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1417 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:90`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1418 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:111`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1419 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:147`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1420 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:210`

`clientDoc = A.change(clientDoc, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1421 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:216`

`expect(serverHandle.doc()!.field).to.deep.equal(value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1422 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:225`

`expect(reloaded.doc()!.field).to.deep.equal(value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1423 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:237`

`handleA.update((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1424 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:238`

`return A.change(doc, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1425 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:244`

`expect(handleA.doc()!.text).to.equal(text);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1426 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:309`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1427 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:375`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1428 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:383`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1429 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:409`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1430 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:427`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1431 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:454`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1432 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:575`

`const initialHostHeads = getHeads(hostHandle.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1433 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:577`

`clientHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1434 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:583`

`expect(getHeads(hostHandle.doc()!)).to.deep.equal(initialHostHeads);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1435 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:656`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1436 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo-subduction.test.ts:693`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1437 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:110`

`valueDuringChange = handle.doc()!.field;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1438 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:113`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1439 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:131`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1440 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:141`

`const newDoc = change(copy, 'change', (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1441 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:213`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1442 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:217`

`expect((await asyncTimeout(client.find<any>(handle.url), 1000))!.doc().text).to.equal(text);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1443 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:235`

`firstHandle.change((doc: any) => (doc.text = 'Hello world'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1444 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:244`

`expect(firstDocOnClient.doc()!.text).to.equal('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1445 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:248`

`secondHandle.change((doc: any) => (doc.text = 'Hello world'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1446 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:255`

`expect(secondDocOnClient.doc()!.text).to.equal('Hello world');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1447 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:272`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1448 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:285`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1449 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:300`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1450 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:322`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1451 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:346`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1452 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:364`

`docA.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1453 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:378`

`adapter1.peerCandidate(adapter2.peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1454 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:380`

`adapter2.peerCandidate(adapter1.peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1455 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:412`

`await expect.poll(() => hostHandle.doc()!.text).not.toBeUndefined();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1456 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:430`

`let syncedHeads = getHeads(serverHandle.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1457 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:431`

`receiveByClient(save(serverHandle.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1458 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:445`

`serverHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1459 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:453`

`serverHandle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1460 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:477`

`clientDoc = A.change(clientDoc, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1461 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:483`

`expect(serverHandle.doc()!.field).to.deep.equal(value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1462 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:511`

`clientDoc = A.change(clientDoc, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1463 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:517`

`expect(serverHandle.doc()!.field).to.deep.equal(value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1464 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:526`

`expect(serverHandle.doc()!.field).to.deep.equal(value);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1465 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:539`

`handleA.update((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1466 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:540`

`return A.change(doc, (doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1467 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:545`

`expect(handleA.doc()!.text).to.equal(text);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1468 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:549`

`expect(handleB.doc()!.text).to.equal(text);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1469 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:597`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1470 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:610`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1471 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:622`

`const blob = A.save(original.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1472 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:627`

`imported.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1473 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:643`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1474 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:670`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1475 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:704`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1476 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:711`

`return docOnPeer2.doc()!.text;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1477 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:723`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1478 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:729`

`expect(docOnPeer2.doc()!.offlineText).to.be.undefined;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1479 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:737`

`expect(docOnPeer2.doc()!.offlineText).to.eq(offlineText);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1480 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:742`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1481 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:747`

`expect(docOnPeer2.doc()!.onlineText).to.eq(onlineText);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1482 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:760`

`document.change((doc: any) => (doc.text = 'hello ' + idx));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1483 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:804`

`document.change((doc: any) => (doc.text = 'hello'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1484 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:845`

`document.change((doc: any) => (doc.text = 'hello'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1485 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:880`

`handle.change((doc: any) => (doc.text = 'hello'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1486 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:887`

`return doc.doc()!.text;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1487 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:898`

`handle.change((doc: any) => (doc.text = 'hello'));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1488 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:899`

`blob = A.save(handle.doc()!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1489 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:920`

`return doc.doc()!.text;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1490 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:994`

`pair[0].peerCandidate(pair[1].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1491 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:995`

`pair[1].peerCandidate(pair[0].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1492 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1006`

`pair[0].peerDisconnected(pair[1].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1493 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1007`

`pair[1].peerDisconnected(pair[0].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1494 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1008`

`pair[0].peerCandidate(pair[1].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1495 no-casts `packages/core/echo/echo-host/src/automerge/automerge-repo.test.ts:1009`

`pair[1].peerCandidate(pair[0].peerId!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1496 no-casts `packages/core/echo/echo-host/src/automerge/echo-data-monitor.ts:119`

`const values: (typeof value)[] = (timeSeries as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1497 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:43`

`await callbacks.onSyncMessage!(encodeSyncPayload(PAYLOAD));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1498 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:54`

`await callbacks.onClose!();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1499 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.test.ts:133`

`await callbacks.onStartReplication!({ id: peerId }, PublicKey.random());` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1500 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:229`

`return connection.connection.pushBundle!(ctx, bundle);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1501 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:237`

`return connection.connection.pullBundle!(ctx, docHeads);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1502 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:358`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1503 no-casts `packages/core/echo/echo-host/src/automerge/echo-network-adapter.ts:361`

`(metadata as any)?.dxos_peerSource === 'EchoNetworkAdapter';` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1504 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:122`

`expect(range.map((c) => Buffer.from(c.data!).toString())).toEqual(['one', 'two']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1505 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:163`

`expect(range.map((chunk) => Buffer.from(chunk.data!).toString())).toEqual(['exact', 'child']);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1506 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:195`

`const docId = 'abc123' as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1507 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:204`

`const result = await store.getHeads(['missing' as any]);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1508 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:215`

`const docId1 = 'doc1' as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1509 no-casts `packages/core/echo/echo-host/src/automerge/sqlite-storage-adapter.test.ts:216`

`const docId2 = 'doc2' as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1510 no-casts `packages/core/echo/echo-host/src/db-host/auto-reclaim.test.ts:76`

`rootHandle!.change((draft: DatabaseDirectory) => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1511 no-casts `packages/core/echo/echo-host/src/db-host/auto-reclaim.test.ts:90`

`rootHandle!.change((draft: DatabaseDirectory) => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1512 no-casts `packages/core/echo/echo-host/src/db-host/auto-reclaim.test.ts:91`

`delete draft.links![objectId];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1513 no-casts `packages/core/echo/echo-host/src/db-host/auto-reclaim.test.ts:99`

`parentHandle!.change((draft: DatabaseDirectory) => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1514 no-casts `packages/core/echo/echo-host/src/db-host/auto-reclaim.test.ts:146`

`await linkExisting(holder, 'obj-shared', sharedHandle!.url);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1515 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:102`

`expect(result.cursors[0].cursor).toBe(headsCodec.encode(getHeads(handle.doc()!)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1516 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:118`

`const doc1HeadsBefore = headsCodec.encode(getHeads(handle1.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1517 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:119`

`const doc2Heads = headsCodec.encode(getHeads(handle2.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1518 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:123`

`doc.objects!['obj-1'].data.title = 'Doc 1 Updated';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1519 no-casts `packages/core/echo/echo-host/src/db-host/automerge-data-source.test.ts:155`

`const currentHeads = headsCodec.encode(getHeads(handle.doc()!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1520 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.test.ts:80`

`handle.change((doc: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1521 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:100`

`this._sendUpdatesJob!.trigger();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1522 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:165`

`await this._sendUpdatesJob!.join();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1523 no-casts `packages/core/echo/echo-host/src/db-host/documents-synchronizer.ts:201`

`this._sendUpdatesJob!.trigger();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1524 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:51`

`expect(JSON.parse(result.objects![0])).toMatchObject(object1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1525 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:52`

`expect(JSON.parse(result.objects![1])).toMatchObject(object2);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1526 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:85`

`expect(JSON.parse(result.objects![1])).toMatchObject({ 'id': object1Id, '@deleted': true });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1527 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:112`

`expect(JSON.parse(page1.objects![0])).toMatchObject(items[0]);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1528 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:113`

`expect(JSON.parse(page1.objects![4])).toMatchObject(items[4]);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1529 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:122`

`after: page1.nextCursor!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1530 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:126`

`expect(JSON.parse(page2.objects![0])).toMatchObject(items[5]);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1531 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:127`

`expect(JSON.parse(page2.objects![4])).toMatchObject(items[9]);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1532 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:158`

`expect(JSON.parse(head.objects![0])).toMatchObject({ id: objectId, data: 'test' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1533 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:159`

`expect(JSON.parse(head.objects![1])).toMatchObject({ 'id': objectId, '@deleted': true });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1534 no-casts `packages/core/echo/echo-host/src/db-host/feed-service.test.ts:257`

`expect(JSON.parse(next.objects![0])).toMatchObject(object1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1535 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:77`

`spaceId: spaceId! as SpaceId,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1536 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:110`

`feedId: feedId!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1537 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:135`

`const messages = objectIds!.map((id) => ({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1538 no-casts `packages/core/echo/echo-host/src/db-host/local-feed-service.ts:137`

`feedId: feedId!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1539 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:47`

`indexEngine: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1540 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:48`

`runtime: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1541 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:49`

`automergeHost: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1542 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:50`

`spaceStateManager: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1543 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:53`

`reactivity: 'reactive' as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1544 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:87`

`expect(result!.spaceIds?.has(spaceId)).toBe(true);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1545 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:89`

`expect(result!.typenames?.has(PERSON_TYPENAME)).toBe(true);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1546 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:90`

`expect(result!.typenames?.has(PERSON_DXN)).toBe(false);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1547 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:91`

`expect(result!.objectIds?.has(objectId)).toBe(true);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1548 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:93`

`expect(result!.queueIds).toBeUndefined();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1549 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:108`

`expect(result!.typenames).toEqual(new Set([PERSON_TYPENAME, ORG_TYPENAME]));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1550 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:307`

`indexEngine: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1551 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:308`

`runtime: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1552 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:309`

`automergeHost: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1553 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:310`

`spaceStateManager: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1554 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:316`

`reactivity: 'reactive' as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1555 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:422`

`indexEngine: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1556 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:423`

`runtime: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1557 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:424`

`automergeHost: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1558 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:425`

`spaceStateManager: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1559 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:428`

`reactivity: 'reactive' as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1560 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:441`

`indexEngine: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1561 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:442`

`runtime: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1562 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:443`

`automergeHost: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1563 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:444`

`spaceStateManager: {} as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1564 no-casts `packages/core/echo/echo-host/src/db-host/query-invalidation.test.ts:447`

`reactivity: 'reactive' as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1565 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:103`

`await this._openConnection(this._ctx!, spaceId);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1566 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:178`

`this._ctx!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1567 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:192`

`await this._openConnection(replicatorCtx!, spaceId, reconnects + 1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1568 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:237`

`private _readableStreamController!: ReadableStreamDefaultController<AutomergeProtocolMessage>;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1569 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:365`

`const payload = cbor.decode(message.payload!.value) as AutomergeProtocolMessage;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1570 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:372`

`payload.senderId = this._remotePeerId! as PeerId;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1571 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-replicator.ts:414`

`(message as any).targetId = this._targetServiceId as PeerId;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1572 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.test.ts:68`

`const currentConnectionId = () => (openConnections[openConnections.length - 1] as any)._connectionId as string;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1573 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:298`

`this._ctx!,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1574 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:365`

`private _readableStreamController!: ReadableStreamDefaultController<SubductionProtocolMessage>;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1575 no-casts `packages/core/echo/echo-host/src/edge/echo-edge-subduction-replicator.ts:485`

`payload = cbor.decode(message.payload!.value) as SubductionProtocolMessageEnveloped;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1576 no-casts `packages/core/echo/echo-host/src/filter/filter-match.ts:148`

`const value = (obj as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1577 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:519`

`const serializeItemGroupKey = (item: QueryItem): string => GroupBy.serializeGroupKey(item.groupKey!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1578 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:754`

`throw new Error('Unknown step type: ${(step as any)._tag}');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1579 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1009`

`throw new Error('Unknown selector type: ${(step.selector as any)._tag}');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1580 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1486`

`throw new Error('Unknown traversal type: ${(step.traversal as any)._tag}');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1581 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1604`

`const partitioned = GroupBy.partitionByGroupKey(withKeys, (item) => GroupBy.serializeGroupKey(item.groupKey!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1582 no-casts `packages/core/echo/echo-host/src/query/query-executor.ts:1607`

`(item) => GroupBy.serializeGroupKey(item.groupKey!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1583 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1520`

`expect((selectStep as any).limit).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1584 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1522`

`expect((orderStep as any).limit).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1585 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1937`

`expect((selectStep as any).limit).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1586 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:1939`

`const orderWithLimit = plan.steps.some((step) => step._tag === 'OrderStep' && (step as any).limit === 10);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1587 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2007`

`expect((selectStep as any).limit).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1588 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2009`

`const orderWithLimit = plan.steps.some((step) => step._tag === 'OrderStep' && (step as any).limit === 10);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1589 no-casts `packages/core/echo/echo-host/src/query/query-planner.test.ts:2019`

`expect((orderStep as any).limit).toBe(10);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1590 no-casts `packages/core/echo/echo-host/src/query/query-planner.ts:102`

`message: 'Unsupported query type: ${(query as any).type}',` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1591 no-casts `packages/core/echo/echo-host/src/query/query-planner.ts:503`

`message: 'Unsupported filter type: ${(filter as any).type}',` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1592 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:65`

`log('create connection', { from: replicator.context!.peerId, to: otherReplicator.context!.peerId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1593 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:67`

`replicator.context!.peerId,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1594 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:68`

`otherReplicator.context!.peerId,` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1595 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:72`

`await replicator.context!.onConnectionOpen(connection1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1596 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:73`

`await otherReplicator.context!.onConnectionOpen(connection2);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1597 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:79`

`await replicator.context!.onConnectionClosed(connection);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1598 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:80`

`await connection.otherSide!.owningReplicator!.removeConnection(connection.otherSide!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1599 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:157`

`return this.shouldAdvertise ? (params) => this.shouldAdvertise!(params) : undefined;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1600 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:168`

`log('disconnect', { peerId: this.context!.peerId });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1601 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:176`

`this.context!.onConnectionOpen(connection);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1602 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:181`

`this.context!.onConnectionClosed(connection);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1603 no-casts `packages/core/echo/echo-host/src/testing/test-replicator.ts:225`

`params[1]!.onSyncMessage = () => {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1604 no-casts `packages/core/echo/echo-host/src/util.ts:16`

`const obj = spaceDoc.objects![id];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1605 no-casts `packages/core/echo/echo-panproto/src/Lens.test.ts:127`

`expect(() => Lens.make('org.dxos.test.lens.bad', Task, GtdTask, { done: 'nope' as any })).to.throw(` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1606 no-casts `packages/core/echo/echo-panproto/src/Lens.test.ts:202`

`expect(() => Lens.put(task, makeLens(), { nope: 1 } as any)).to.throw(/not a property of the target/);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1607 no-casts `packages/core/echo/echo-panproto/src/lens/codecs.ts:52`

`decode: (value) => forward[value] ?? (value as unknown as B),` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1608 no-casts `packages/core/echo/echo-panproto/src/lens/codecs.ts:53`

`encode: (value) => backward[value] ?? (value as unknown as A),` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1609 no-casts `packages/core/echo/echo-panproto/src/lens/live.ts:50`

`const change = (callback: (draft: any) => void): void => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1610 no-casts `packages/core/echo/echo-panproto/src/lens/live.ts:137`

`}) as unknown as Obj.OfShape<T>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1611 no-casts `packages/core/echo/echo-panproto/src/react.ts:54`

`): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1612 no-casts `packages/core/echo/echo-panproto/src/react.ts:67`

`Obj.update(Lens.of(obj, lens), (view: any) => (updateOrValue as (view: Obj.Mutable<T>) => void)(view));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1613 no-casts `packages/core/echo/echo-panproto/src/react.ts:81`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1614 no-casts `packages/core/echo/echo-panproto/src/react.ts:93`

`): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1615 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:60`

`obj[ATTR_META]!.keys ??= [];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1616 no-casts `packages/core/echo/echo-protocol/src/echo-feed-codec.ts:61`

`const keys = obj[ATTR_META]!.keys!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1617 no-casts `packages/core/echo/echo-protocol/src/reference.ts:18`

`export const isEncodedReference = (value: any): value is EncodedReference =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1618 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:478`

`const [key] = Object.entries(this._tags!).find(([, value]) => value.label.toLowerCase() === str) ?? [];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1619 no-casts `packages/core/echo/echo-query/src/parser/query-builder.ts:485`

`private _parseValue(cursor: TreeCursor, input: string): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1620 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:91`

`return (value as any) > (filter.value as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1621 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:93`

`return (value as any) >= (filter.value as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1622 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:95`

`return (value as any) < (filter.value as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1623 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:97`

`return (value as any) <= (filter.value as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1624 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:108`

`if (!_filterMatchValueLocal(vf, (value as any)[key])) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1625 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:122`

`return (value as any) >= filter.from && (value as any) <= filter.to;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1626 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:134`

`const _filterMatchEntityLocal = (filter: QueryAST.Filter, entity: any): boolean => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1627 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:165`

`throw new Error('Filter type '${(filter as any).type}' is not supported in toPredicate.');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1628 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:464`

`static 'toPredicate' = ((entityOrFilter: any, filter?: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1629 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:466`

`return (entity: any) => _filterMatchEntityLocal(entityOrFilter.ast, entity);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1630 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:506`

`const processPredicate = (predicate: any): QueryAST.Filter => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1631 no-casts `packages/core/echo/echo-query/src/query-lite/query-lite.ts:808`

`const isRef = (obj: any): obj is Ref.Ref<any> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# WARN cc9b81fc-1632 no-sleep-in-test `packages/core/echo/echo-react/src/useObject.test.tsx:111`

`await new Promise((resolve) => setTimeout(resolve, 50))` is used to "ensure no update happens" before asserting the value is unchanged — sleep as the negative-assertion synchronization mechanism. Prefer a deterministic flush/wait instead of the fixed delay.

# ERROR cc9b81fc-1633 no-casts `packages/core/echo/echo-react/src/useObject.ts:120`

`} = (<T extends Obj.Unknown, K extends keyof T>(objOrRef: T | Ref.Ref<T> | undefined, property?: K): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1634 no-casts `packages/core/echo/echo-react/src/useObject.ts:131`

`Obj.update(obj, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1635 no-casts `packages/core/echo/echo-react/src/useObject.ts:156`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1636 no-casts `packages/core/echo/echo-react/src/useObject.ts:176`

`return useAtomValue(atom as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# WARN cc9b81fc-1637 no-sleep-in-test `packages/core/echo/echo-react/src/usePagination.test.tsx:102`

Both assertions (lines 102, 260) rely on `await new Promise((resolve) => setTimeout(resolve, 50))` to "give any erroneous extra state updates a chance to land" before checking nothing changed. Replace the fixed sleep with a deterministic settle point (e.g. awaiting the hook's own update-batching promise or an act()-flush) per the rule.

# ERROR cc9b81fc-1638 no-casts `packages/core/echo/echo-solid/src/useObject.ts:188`

`Obj.update(obj, (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1639 no-casts `packages/core/echo/echo-solid/src/useObject.ts:242`

`setValue(() => currentValue as unknown as T);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1640 no-casts `packages/core/echo/echo-solid/src/useObject.ts:247`

`setValue(() => registry.get(atom) as unknown as T);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1641 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:87`

`expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1642 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:93`

`expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1643 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:126`

`expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1644 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:132`

`expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1645 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:154`

`expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1646 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:188`

`expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1647 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:194`

`expect(Type.getTypename(result!)).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1648 no-casts `packages/core/echo/echo-solid/src/useType.test.tsx:201`

`expect(getByTestId('typename').textContent).toBe(Type.getTypename(registeredSchema)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1649 no-casts `packages/core/echo/echo/src/Annotation.test.ts:340`

`const result = ColorAnnotation.getFromAst(nameProperty!.type);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1650 no-casts `packages/core/echo/echo/src/Database.ts:435`

`invariant(!schema || isInstanceOf(schema as any, object), 'Object type mismatch.');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1651 no-casts `packages/core/echo/echo/src/Database.ts:436`

`return object as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1652 no-casts `packages/core/echo/echo/src/Database.ts:437`

`}).pipe(Effect.withSpan('Database.resolve'))) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1653 no-casts `packages/core/echo/echo/src/Database.ts:543`

`Effect.map(({ db }) => db.query(queryOrFilter as any) as QueryResult.QueryResult<any>),` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1654 no-casts `packages/core/echo/echo/src/Entity.ts:77`

`]) as unknown as Schema.Codec<Unknown>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1655 no-casts `packages/core/echo/echo/src/Entity.ts:130`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1656 no-casts `packages/core/echo/echo/src/Entity.ts:140`

`return (value as any)[SnapshotKindId] !== undefined;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1657 no-casts `packages/core/echo/echo/src/Entity.ts:149`

`export const Meta: unique symbol = internal.MetaId as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1658 no-casts `packages/core/echo/echo/src/Filter.test.ts:81`

`expect((combined.ast as any).filters).toHaveLength(2);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1659 no-casts `packages/core/echo/echo/src/Filter.test.ts:82`

`expect((combined.ast as any).filters[1]).toEqual({` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1660 no-casts `packages/core/echo/echo/src/Filter.ts:135`

`} = (input: Type$.AnyEntity | Schema.Codec<any, any> | URI.URI, props?: Props<unknown>): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1661 no-casts `packages/core/echo/echo/src/Filter.ts:514`

`const processPredicate = (predicate: any): QueryAST.Filter => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1662 no-casts `packages/core/echo/echo/src/Filter.ts:523`

`(predicate: any) => !Ref.isRef(predicate) && typeof predicate === 'object' && predicate !== null,` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1663 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.test.ts:140`

`} as unknown as TestEchoSchema;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1664 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:175`

`const type = (obj as any)?.[TypeId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1665 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:189`

`export const setTypename = (obj: any, typename: URI.URI): void => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1666 no-casts `packages/core/echo/echo/src/internal/Annotation/annotations.ts:211`

`const type = (obj as any)[TypeId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1667 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:120`

`callback: (proxy: any) => void,` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1668 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:130`

`(eventTarget as any)[EventId]?.emit();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1669 no-casts `packages/core/echo/echo/src/internal/common/proxy/change-context.ts:134`

`(ownerTarget as any)[EventId]?.emit();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1670 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:63`

`expect(() => ((meta as any).keys = [])).toThrow(/outside of Obj.update/);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1671 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:64`

`expect(() => ((meta as any).tags = [tagRef('tag')])).toThrow(/outside of Obj.update/);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1672 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:268`

`obj.address!.state = 'NY';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1673 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:282`

`obj.address!.coordinates!.lat = 51.5074;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1674 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:283`

`obj.address!.coordinates!.lng = -0.1278;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1675 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:298`

`obj.address!.city = 'LA';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1676 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:310`

`obj.address!.coordinates!.lat = 0;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1677 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:356`

`obj.fields!.push({ label: 'tag2', value: 'val2' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1678 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:360`

`expect(obj.fields![1].label).toBe('tag2');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1679 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:374`

`popped = obj.fields!.pop();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1680 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:392`

`obj.fields!.splice(1, 1, { label: 'x', value: 'x' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1681 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:396`

`expect(obj.fields![1].label).toBe('x');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1682 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:407`

`obj.fields!.push({ label: 'tag2', value: 'val2' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1683 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:419`

`obj.fields!.pop();` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1684 no-casts `packages/core/echo/echo/src/internal/common/proxy/change.test.ts:431`

`obj.fields!.splice(0, 1);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1685 no-casts `packages/core/echo/echo/src/internal/common/proxy/define-hidden-property.ts:8`

`export const defineHiddenProperty = (object: any, key: string | symbol, value: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1686 no-casts `packages/core/echo/echo/src/internal/common/proxy/event-batch.ts:28`

`(target as any)[EventId]?.emit();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1687 no-casts `packages/core/echo/echo/src/internal/common/proxy/event-batch.ts:42`

`(target as any)[EventId]?.emit();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1688 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:51`

`const objData: any = (obj as any)[objectData];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1689 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:66`

`expect(obj.classInstance!.field).to.eq('value');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1690 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:71`

`obj.classInstance!.field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1691 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:72`

`expect(obj.classInstance!.field).to.eq('baz');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1692 no-casts `packages/core/echo/echo/src/internal/common/proxy/handler.test.ts:112`

`obj.classInstance!.field = 'baz';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1693 no-casts `packages/core/echo/echo/src/internal/common/proxy/json-serializer.ts:26`

`export const attachTypedJsonSerializer = (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1694 no-casts `packages/core/echo/echo/src/internal/common/proxy/json-serializer.ts:43`

`export const typedJsonSerializer = function (this: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1695 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:76`

`const parent = (obj as any)[ParentId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1696 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:78`

`delete (obj as any)[ParentId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1697 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:97`

`const metaProxy = (obj as any)[MetaId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1698 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:121`

`const schema = (obj as any)[SchemaId] as Schema.Codec<T, any> | undefined;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1699 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:128`

`const meta = (obj as any)[MetaId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1700 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:145`

`const setIdOnTarget = (target: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1701 no-casts `packages/core/echo/echo/src/internal/common/proxy/make-object.ts:170`

`defineHiddenProperty(obj, MetaId, createProxy(fullMeta, TypedReactiveHandler.instance as any));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1702 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:26`

`export const getRawTarget = (value: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1703 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:41`

`return (value as any)?.[EchoOwner];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1704 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:144`

`visitor: (actualValue: any) => boolean,` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1705 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:185`

`export const wouldCreateCycle = (targetRoot: object, value: any): boolean =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1706 no-casts `packages/core/echo/echo/src/internal/common/proxy/ownership.ts:192`

`export const hasForeignOwner = (value: any, target: object): boolean =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1707 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:24`

`export const isProxy = (value: unknown) => !!(value as any)?.[symbolIsProxy];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1708 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:30`

`export const isReactiveRecord = (value: any): boolean => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1709 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:35`

`export const isValidProxyTarget = (value: any): value is object => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1710 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:49`

`export const getProxySlot = <T extends object>(proxy: any): ProxyHandlerSlot<T> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1711 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:50`

`const value = (proxy as any)[symbolIsProxy];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1712 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:55`

`export const getProxyTarget = <T extends object>(proxy: any): T => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1713 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:59`

`export const getProxyHandler = <T extends object>(proxy: any): ReactiveHandler<T> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1714 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:85`

`(getProxySlot(obj).target as any).id = id;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1715 no-casts `packages/core/echo/echo/src/internal/common/proxy/proxy-utils.ts:139`

`get(target: T, prop: string | symbol, receiver: any): any {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1716 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive-array.ts:16`

`const checkArrayMutationAllowed = (arr: any, method: string): void => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1717 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive-array.ts:62`

`let result!: any;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1718 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.test.ts:49`

`const unsubscribe = Obj.subscribe(value as any, () => {});` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1719 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:26`

`const target = getProxyTarget(obj as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1720 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:28`

`return (target as any)[EventId].on(callback);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1721 no-casts `packages/core/echo/echo/src/internal/common/proxy/reactive.ts:76`

`const changeFn = (obj as any)[ChangeId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1722 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:177`

`public static isOptionalProperty(target: any, prop: string | symbol): boolean {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1723 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:178`

`const schema: Schema.Top | undefined = (target as any)[SchemaId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1724 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:186`

`public static getTargetPropertySchema(target: any, prop: string | symbol): Schema.Top {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1725 no-casts `packages/core/echo/echo/src/internal/common/proxy/schema-validator.ts:187`

`const schema: Schema.Top | undefined = (target as any)[SchemaId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1726 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:49`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect(() => Obj.make(Bar, { fooRef: { id: '1', field } as any })).to.throw();`

# ERROR cc9b81fc-1727 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:50`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect(() => Obj.make(Bar, { fooRef: undefined as any })).to.throw(); // Unresolved reference.`

# ERROR cc9b81fc-1728 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:95`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((object.value as any).name).to.eq('Robert Smith');`

# ERROR cc9b81fc-1729 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:104`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const unsubscribe = subscribe(object as any, () => {`

# ERROR cc9b81fc-1730 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:260`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const unsubscribe = subscribe(obj as any, () => {`

# ERROR cc9b81fc-1731 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.test.ts:279`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const unsubscribe = subscribe(obj as any, () => {`

# ERROR cc9b81fc-1732 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:60`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `[ParentId]?: any;`

# ERROR cc9b81fc-1733 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:66`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} & ({ [key: keyof any]: any } | any[]);`

# ERROR cc9b81fc-1734 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:127`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `copy[key] = deepCopy((actualValue as any)[key], visited);`

# ERROR cc9b81fc-1735 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:137`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const copyHiddenProperties = (source: any, target: any): void => {`

# ERROR cc9b81fc-1736 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:209`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `return (callback: (obj: any) => void) => executeChange(target, target, this, callback);`

# ERROR cc9b81fc-1737 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:237`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const jsonSchema = (target as any).jsonSchema;`

# ERROR cc9b81fc-1738 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:289`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const descriptor = Object.getOwnPropertyDescriptor(target, symbol)!;`

# ERROR cc9b81fc-1739 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:298`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `delete (target as any)[symbol];`

# ERROR cc9b81fc-1740 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:341`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `if ((target as any)[symbolIsProxy]) {`

# ERROR cc9b81fc-1741 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:344`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `let value = (target as any)[key];`

# ERROR cc9b81fc-1742 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:351`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(target as any)[key] = value;`

# ERROR cc9b81fc-1743 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:378`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `get(target: ProxyTarget, prop: string | symbol, receiver: any): any {`

# ERROR cc9b81fc-1744 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:406`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `set(target: ProxyTarget, prop: string | symbol, value: any, receiver: any): boolean {`

# ERROR cc9b81fc-1745 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:538`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `value: any,`

# ERROR cc9b81fc-1746 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:539`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `): { echoRoot: object; preparedValue: any } {`

# ERROR cc9b81fc-1747 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:588`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `private _validateValue(target: any, prop: string | symbol, value: any) {`

# ERROR cc9b81fc-1748 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:609`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `inspectFn: (value: any, options?: InspectOptionsStylized) => string,`

# ERROR cc9b81fc-1749 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:625`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const toJSON = (target: ProxyTarget): any => {`

# ERROR cc9b81fc-1750 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:652`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const setSchemaProperties = (obj: any, schema: Schema.Top, typeSource?: TypeSource, skipOwnStamp = false) => {`

# ERROR cc9b81fc-1751 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-handler.ts:738`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const makeArraysReactive = (target: any) => {`

# ERROR cc9b81fc-1752 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:65`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR cc9b81fc-1753 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:73`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(object.meta!.test.value).to.eq(300);`

# ERROR cc9b81fc-1754 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:83`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR cc9b81fc-1755 no-casts `packages/core/echo/echo/src/internal/common/proxy/typed-object.test.ts:95`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(object.meta!.test).to.eq(100);`

# ERROR cc9b81fc-1756 no-casts `packages/core/echo/echo/src/internal/common/types/meta.ts:95`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const metadata = (obj as any)[MetaId];`

# ERROR cc9b81fc-1757 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:50`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return (obj as any)[SchemaId];`

# ERROR cc9b81fc-1758 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:57`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const setSchema = (obj: any, schema: Schema.Codec<any, any>): void => {`

# ERROR cc9b81fc-1759 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:78`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return (obj as any)[TypeEntityId];`

# ERROR cc9b81fc-1760 no-casts `packages/core/echo/echo/src/internal/common/types/typename.ts:84`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const setType = (obj: any, type: unknown): void => {`

# ERROR cc9b81fc-1761 no-casts `packages/core/echo/echo/src/internal/Entity/api.ts:37`

`const key = (entity as any)[MetaId]?.key;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1762 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:180`

`const version = DXN.getVersion(TypeMetaSchemaDXN)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1763 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:252`

`const entity = makeObject(persistentEntitySchema, { id } as any, meta);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1764 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:254`

`const target = getProxyTarget(entity)!;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1765 no-casts `packages/core/echo/echo/src/internal/Entity/entity.ts:281`

`return entity as unknown as EchoTypeSchema<Self, {}, K, Fields>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1766 no-casts `packages/core/echo/echo/src/internal/Entity/guard.ts:16`

`return (value as any)[KindId] !== undefined;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1767 no-casts `packages/core/echo/echo/src/internal/Entity/guard.ts:26`

`return (value as any)[SnapshotKindId] !== undefined;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1768 no-casts `packages/core/echo/echo/src/internal/Entity/object.ts:51`

`const fields = ((self as any).fields ?? {}) as Fields;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1769 no-casts `packages/core/echo/echo/src/internal/Entity/object.ts:95`

`return constructor as unknown as Type.ObjClass<Self, Schema.Schema.Type<_Schema>, {}>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1770 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:139`

`const fields = ((self as any).fields ?? {}) as Fields;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1771 no-casts `packages/core/echo/echo/src/internal/Entity/relation.ts:214`

`return constructor as unknown as Type.RelationClass<unknown, unknown, unknown, unknown, {}>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1772 no-casts `packages/core/echo/echo/src/internal/Entity/type-kind.ts:52`

`const fields = ((self as any).fields ?? {}) as Fields;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1773 no-casts `packages/core/echo/echo/src/internal/Entity/type-uri.ts:77`

`const type = (object as any)[TypeId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1774 no-casts `packages/core/echo/echo/src/internal/Entity/util.ts:18`

`export const getObjectEchoUri = (object: any): EID.EID | undefined => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1775 no-casts `packages/core/echo/echo/src/internal/Entity/version.ts:47`

`const ver = (entity as any)[ObjectVersionId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1776 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:90`

`const structuralMatch = (filterObj: any, targetObj: any, strict = true): boolean => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1777 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:145`

`const compareValue = filter.value as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1778 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:158`

`return (value as any) > compareValue;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1779 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:160`

`return (value as any) >= compareValue;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1780 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:162`

`return (value as any) < compareValue;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1781 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:164`

`return (value as any) <= compareValue;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1782 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:176`

`const nestedValue = (value as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1783 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:205`

`return (value as any) >= filter.from && (value as any) <= filter.to;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1784 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:252`

`if (!filterMatchValue(valueFilter, (json as any)[key])) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1785 no-casts `packages/core/echo/echo/src/internal/Filter/match.ts:278`

`const tags = rawTags.map((tag: any) => (typeof tag?.encode === 'function' ? tag.encode() : tag));` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1786 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:38`

`const prop = jsonSchema.properties!['active' as const];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1787 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:46`

`const prop = jsonSchema.properties!['email' as const];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1788 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:56`

`const prop = jsonSchema.properties!['salary' as const];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1789 no-casts `packages/core/echo/echo/src/internal/Format/format.test.ts:68`

`const prop = jsonSchema.properties!['birthday' as const];` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1790 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:24`

`if ((schema as any).exclusiveMaximum === true) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1791 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:26`

`delete (schema as any).exclusiveMaximum;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1792 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:27`

`} else if ((schema as any).exclusiveMaximum === false) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1793 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:28`

`delete (schema as any).exclusiveMaximum;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1794 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:31`

`if ((schema as any).exclusiveMinimum === true) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1795 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:33`

`delete (schema as any).exclusiveMinimum;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1796 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:34`

`} else if ((schema as any).exclusiveMinimum === false) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1797 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:35`

`delete (schema as any).exclusiveMinimum;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1798 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:41`

`delete (schema as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1799 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-normalize.ts:97`

`const maybeGoOnArray = (value: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1800 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:46`

`expect(properties!.age).toMatchObject({ type: 'integer', minimum: 0, maximum: 150 });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1801 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:47`

`expect(properties!.age).not.toHaveProperty('allOf');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1802 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:50`

`expect(properties!.email).toMatchObject({ type: 'string', format: 'email' });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1803 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:51`

`expect(properties!.email).not.toHaveProperty('allOf');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1804 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:52`

`expect(typeof properties!.email.description).toBe('string');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1805 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:54`

`expect(properties!.name).toMatchObject({` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1806 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:65`

`expect(jsonSchema.properties!.email).not.toHaveProperty('anyOf');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1807 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:66`

`expect(jsonSchema.properties!.age).not.toHaveProperty('anyOf');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1808 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:73`

`expect(properties!.kind).toMatchObject({ type: 'string', enum: ['personal', 'work'] });` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1809 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-shape.test.ts:97`

`expect(typeof properties![key].description, key).toBe('string');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1810 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-v3.test.ts:50`

`const declared = Object.keys((document as any).properties ?? {});` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1811 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-v3.test.ts:51`

`const required: string[] = (document as any).required ?? [];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1812 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-v3.test.ts:63`

`expect((annotations as any)[TypeAnnotationId]?.typename).toBe(typename);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1813 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema-v3.test.ts:115`

`const annotations = SchemaAST.resolveAnnotations(data!.type) ?? {};` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1814 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:49`

`expect(getNormalizedEchoAnnotations(jsonSchema.properties!.name!)!.meta![EXAMPLE_NAMESPACE]).to.deep.eq(meta);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1815 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:65`

`const nested = jsonSchema.properties!.name;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1816 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:102`

`expectReferenceAnnotation((jsonSchema.properties!.name as any).items);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1817 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:102`

`expectReferenceAnnotation((jsonSchema.properties!.name as any).items);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1818 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:117`

`expectReferenceAnnotation(jsonSchema.properties!.name);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1819 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:124`

`expect(getNormalizedEchoAnnotations(jsonSchema.properties!.name!)).to.be.undefined;` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1820 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:165`

`expect(Object.keys(schema.properties!)).toEqual([` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1821 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:257`

`expect(Object.keys(jsonSchema.properties!).length).toBeGreaterThan(0);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1822 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:319`

`expect(SchemaAST.getAnnotation(contact!.type, SchemaAST.TitleAnnotationId)).to.eq('Custom Title');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1823 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.test.ts:561`

`expect(jsonSchema2.properties!.name.description).to.eq('Name');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1824 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:301`

`result = Schema.Union(root.enum!.map((e) => Schema.Literal(e)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1825 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:303`

`result = Schema.Union(root.oneOf!.map((v) => toEffectSchema(v, defs)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1826 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:305`

`result = Schema.Union(root.anyOf!.map((v) => toEffectSchema(v, defs)));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1827 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:307`

`if (root.allOf!.length === 1) {` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1828 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:308`

`result = toEffectSchema(root.allOf![0], defs);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1829 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:358`

`const refSegments = root.$ref!.split('/');` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1830 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:380`

`const echoRefinement: JsonSchemaEchoAnnotations = (root as any)[ECHO_ANNOTATIONS_NS_DEPRECATED_KEY];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1831 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:394`

`(fields as any)[key] = root.required?.includes(key)` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1832 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:401`

`fields = orderKeys(fields, root.propertyOrder as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1833 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:445`

`const echoRefinement: JsonSchemaEchoAnnotations = (root as any)[ECHO_ANNOTATIONS_NS_DEPRECATED_KEY];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1834 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:447`

`if ((echoRefinement as any)?.reference != null) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1835 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:451`

`(echoRefinement as any).reference.typename,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1836 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:452`

`(echoRefinement as any).reference.version,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1837 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:460`

`const refToEffectSchema = (root: any): Schema.Codec<any, any> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1838 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:488`

`echoAnnotations[key as keyof JsonSchemaEchoAnnotations] = annotations[annotationId] as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1839 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:593`

`annotations[annotationId] = (schema as any)[key];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1840 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:637`

`(branch: any) =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1841 no-casts `packages/core/echo/echo/src/internal/JsonSchema/json-schema.ts:730`

`const inlineAllOfDeep = (node: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1842 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:31`

`const db = getDatabase(snapshot as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1843 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:35`

`const obj = db.getObjectById((snapshot as any).id);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1844 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:47`

`get.setSelf(getSnapshot(obj) as unknown as Obj.Snapshot<T>);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1845 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:52`

`return getSnapshot(obj) as unknown as Obj.Snapshot<T>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1846 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:70`

`get.setSelf(isDeleted(target) ? undefined : (getSnapshot(target) as unknown as Obj.Snapshot<T>));` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1847 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:74`

`return isDeleted(target) ? undefined : (getSnapshot(target) as unknown as Obj.Snapshot<T>);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1848 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:167`

`get.setSelf(getSnapshot(entity) as unknown as Entity.Snapshot);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1849 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:172`

`return getSnapshot(entity) as unknown as Entity.Snapshot;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1850 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:183`

`get.setSelf(getSnapshot(relation) as unknown as Relation.Snapshot<T>);` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1851 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:188`

`return getSnapshot(relation) as unknown as Relation.Snapshot<T>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1852 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:202`

`return refFamily(objOrRef as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1853 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:207`

`return objectFamily(obj as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1854 no-casts `packages/core/echo/echo/src/internal/Obj/atoms.ts:218`

`} = (objOrRef: Obj.Unknown | Ref.Ref<any>, key: any): Atom.Atom<any> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1855 no-casts `packages/core/echo/echo/src/internal/Obj/common.ts:66`

`} as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1856 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:42`

`expect((contact as any)['@type']).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1857 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:77`

`[Relation.Source]: person1 as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1858 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.test.ts:78`

`[Relation.Target]: person2 as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1859 no-casts `packages/core/echo/echo/src/internal/Obj/create-object.ts:70`

`} = (input: any, props: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1860 no-casts `packages/core/echo/echo/src/internal/Obj/deleted.ts:12`

`if ((obj as any)[ObjectDeletedId] === undefined) {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1861 no-casts `packages/core/echo/echo/src/internal/Obj/deleted.ts:18`

`return (obj as any)[ObjectDeletedId] ?? false;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1862 no-casts `packages/core/echo/echo/src/internal/Obj/inspect.ts:17`

`export const attachedTypedObjectInspector = (obj: any) => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1863 no-casts `packages/core/echo/echo/src/internal/Obj/inspect.ts:44`

`[ATTR_META]: (this as any)[MetaId], // TODO(dmaretskyi): Couldn't use getMeta since that throw's if the object has no meta.` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1864 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:67`

`expect((contactFromJson as any)[TypeId]).toEqual(Type.getURI(TestSchema.Person));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1865 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:68`

`expect((contactFromJson as any)[KindId]).toBe(EntityKind.Object);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1866 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:69`

`expect((contactFromJson as any)[RelationSourceId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1867 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:70`

`expect((contactFromJson as any)[RelationTargetId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1868 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:71`

`expect((contactFromJson as any)[MetaId]).toEqual({` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1869 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:88`

`expect(taskFromJson.assignee!.uri).toEqual(EID.make({ entityId: contact.id }));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1870 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:89`

`expect(taskFromJson.assignee!.target).toEqual(contact);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1871 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:90`

`expect(await taskFromJson.assignee!.load()).toEqual(contact);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1872 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:91`

`expect((taskFromJson as any)[TypeId]).toEqual(Type.getURI(TestSchema.Task));` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1873 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:92`

`expect((taskFromJson as any)[KindId]).toBe(EntityKind.Object);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1874 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:93`

`expect((taskFromJson as any)[RelationSourceId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1875 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:94`

`expect((taskFromJson as any)[RelationTargetId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1876 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:95`

`expect((taskFromJson as any)[MetaId]).toEqual({ keys: [], tags: [], annotations: {} });` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1877 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:114`

`const json = objectToJSON(expando) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1878 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:118`

`const fromJson = (await objectFromJSON(json)) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1879 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:120`

`expect(fromJson[MetaId].tags.map((ref: any) => ref.uri)).toEqual(['echo:/TAGLEGACY']);` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1880 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:132`

`expect((expandoFromJson as any)[ATTR_TYPE]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1881 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:143`

`expect((expandoFromJson as any)[ATTR_TYPE]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1882 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:159`

`const typeJson = objectToJSON(typeEntity as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1883 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:161`

`const reconstructed = (await objectFromJSON(typeJson, { refResolver })) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1884 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:250`

`const relationJson = objectToJSON(relation as any);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1885 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:258`

`const live = (await decodeLive(relationJson, { refResolver })) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1886 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.test.ts:313`

`live.tasks!.push(Ref.make(task2));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1887 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:181`

`invariant((obj as any)[ATTR_TYPE] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1888 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:182`

`invariant((obj as any)[ATTR_META] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1889 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:183`

`invariant((obj as any)[ATTR_DELETED] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1890 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:184`

`invariant((obj as any)[ATTR_SELF_URI] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1891 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:185`

`invariant((obj as any)[ATTR_SELF_URI_LEGACY] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1892 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:186`

`invariant((obj as any)[ATTR_RELATION_SOURCE] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1893 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:187`

`invariant((obj as any)[ATTR_RELATION_TARGET] === undefined, 'Invalid object model');` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1894 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:196`

`const normalizeMeta = (meta: any): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1895 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:228`

`const restoreUint8Arrays = (data: unknown): any =>` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1896 no-casts `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:246`

`} = jsonData as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1897 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:15`

`export const setValue = (obj: Mutable<any>, path: readonly (string | number)[], value: any): void => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1898 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:111`

`const getDefaultValueForType = (ast: SchemaAST.AST | undefined): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1899 no-casts `packages/core/echo/echo/src/internal/Obj/set-value.ts:142`

`const createObjectWithDefaults = (ast: SchemaAST.AST | undefined): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1900 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:74`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1901 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:78`

`snapshot[SnapshotKindId] = (obj as any)[KindId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1902 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:83`

`const source = obj as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1903 no-casts `packages/core/echo/echo/src/internal/Obj/snapshot.ts:99`

`copySymbolProperty(source, snapshot, MetaId, (meta: any) => ({` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1904 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:26`

`unsubscribeTarget = subscribe(target as any, () => {` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1905 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:27`

`const deleted = !!(target as any)[ObjectDeletedId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1906 no-casts `packages/core/echo/echo/src/internal/Ref/atoms.ts:30`

`const deleted = !!(target as any)[ObjectDeletedId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1907 no-casts `packages/core/echo/echo/src/internal/Ref/ref.test.ts:56`

`'/': getObjectEchoUri(task)!.toString(),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1908 no-casts `packages/core/echo/echo/src/internal/Ref/ref.test.ts:75`

`'tasks': [{ '/': getObjectEchoUri(task)!.toString() }],` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1909 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:92`

`export const RefTypeId: unique symbol = Symbol.for('@dxos/echo/internal/Ref') as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1910 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:161`

`export const Ref: RefFn = (input: any): RefSchema<any> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1911 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:270`

`Ref.isRef = (obj: any): obj is Ref<any> => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1912 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:322`

`const EncodedReferenceSchema = Schema.Struct({ '/': Schema.String }) as unknown as Schema.Codec<EncodedReference> &` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1913 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:341`

`$ref: echoUri ?? DXN.make(typename!),` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1914 no-casts `packages/core/echo/echo/src/internal/Ref/ref.ts:683`

`_T: null as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1915 no-casts `packages/core/echo/echo/src/internal/Type/compose.ts:25`

`const propSchema = source.properties![prop]; // TODO(dmaretskyi): Find by json-path instead.` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1916 no-casts `packages/core/echo/echo/src/Json.test.ts:23`

`} as unknown as Database.Database;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1917 no-casts `packages/core/echo/echo/src/Json.test.ts:171`

`const out = JSON.parse(safeStringify(document, replacer, 0)!);` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1918 no-casts `packages/core/echo/echo/src/Json.ts:16`

`export type JsonReplacer = (key: string, value: any) => any;` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1919 no-casts `packages/core/echo/echo/src/Json.ts:30`

`const toJson = (obj: Obj.Any): unknown => (typeof (obj as any).toJSON === 'function' ? (obj as any).toJSON() : obj);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1920 no-casts `packages/core/echo/echo/src/Json.ts:56`

`return function (this: any, key: string, value: any) {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1921 no-casts `packages/core/echo/echo/src/Migration.ts:107`

`transform: options.transform as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1922 no-casts `packages/core/echo/echo/src/Migration.ts:108`

`onMigration: options.onMigration as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1923 no-casts `packages/core/echo/echo/src/Obj.test.ts:46`

`expect((snapshot as any)[Entity.KindId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1924 no-casts `packages/core/echo/echo/src/Obj.test.ts:241`

`expect(() => Obj.getKeys(plain as any, SOURCE)).toThrow(/EntityMeta not found/);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1925 no-casts `packages/core/echo/echo/src/Obj.test.ts:441`

`person.address!.city = 'New York';` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1926 no-casts `packages/core/echo/echo/src/Obj.ts:85`

`) as unknown as internal.UnknownTypeSchema<Unknown, typeof Entity.Kind.Object>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1927 no-casts `packages/core/echo/echo/src/Obj.ts:173`

`export function make(input: Type.AnyObj, props: any): OfShape<any> {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1928 no-casts `packages/core/echo/echo/src/Obj.ts:197`

`const value = (props as any)[sym];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1929 no-casts `packages/core/echo/echo/src/Obj.ts:224`

`return typeof obj === 'object' && obj !== null && (obj as any)[Entity.SnapshotKindId] === Entity.Kind.Object;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1930 no-casts `packages/core/echo/echo/src/Obj.ts:246`

`export const getSnapshot: <T extends Unknown>(obj: T) => Snapshot<T> = objInternal.getSnapshot as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1931 no-casts `packages/core/echo/echo/src/Obj.ts:409`

`export const getValue = (obj: Unknown | Snapshot, path: readonly (string | number)[]): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1932 no-casts `packages/core/echo/echo/src/Obj.ts:439`

`export const setValue: (obj: Mutable<Unknown>, path: readonly (string | number)[], value: any) => void =` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1933 no-casts `packages/core/echo/echo/src/Obj.ts:440`

`objInternal.setValue as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1934 no-casts `packages/core/echo/echo/src/Obj.ts:488`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1935 no-casts `packages/core/echo/echo/src/Obj.ts:518`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1936 no-casts `packages/core/echo/echo/src/Obj.ts:652`

`} = Function.dual(2, (entity: Unknown | Snapshot, source?: string): ForeignKey[] => internal.getKeys(entity, source!));` uses the non-null assertion `!` to tell the compiler to skip a null/undefined check it would otherwise enforce. Per the no-casts rule, replace it with an explicit guard — an `if`, `??`/optional chaining, or an `invariant`/schema check — that proves non-nullness instead of asserting it away.

# ERROR cc9b81fc-1937 no-casts `packages/core/echo/echo/src/Obj.ts:748`

`export const Parent: unique symbol = internal.ParentId as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1938 no-casts `packages/core/echo/echo/src/Obj.ts:923`

`) => Promise<Unknown> = objInternal.objectFromJSON as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1939 no-casts `packages/core/echo/echo/src/Query.test.ts:614`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(person as any)).toThrow(TypeError);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1940 no-casts `packages/core/echo/echo/src/Query.test.ts:615`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(person as any)).toThrow(` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1941 no-casts `packages/core/echo/echo/src/Query.test.ts:621`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(undefined as any)).toThrow(TypeError);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1942 no-casts `packages/core/echo/echo/src/Query.test.ts:622`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(undefined as any)).toThrow(` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1943 no-casts `packages/core/echo/echo/src/Query.test.ts:628`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(null as any)).toThrow(TypeError);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1944 no-casts `packages/core/echo/echo/src/Query.test.ts:629`

`expect(() => Query.select(Filter.type(TestSchema.Person)).from(null as any)).toThrow(` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1945 no-casts `packages/core/echo/echo/src/Query.test.ts:1146`

`const _isAssignable: Obj.Unknown = null as any as Filter.Type<typeof filter>;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1946 no-casts `packages/core/echo/echo/src/Ref.ts:72`

`} = refInternal.Ref as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1947 no-casts `packages/core/echo/echo/src/Registry.ts:154`

`return (yield* Effect.promise(() => registry.query(queryOrFilter as any).run())) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1948 no-casts `packages/core/echo/echo/src/Relation.test.ts:26`

`expect((snapshot as any)[Entity.KindId]).toBeUndefined();` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1949 no-casts `packages/core/echo/echo/src/Relation.ts:78`

`) as unknown as internal.UnknownTypeSchema<Unknown, typeof Entity.Kind.Relation>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1950 no-casts `packages/core/echo/echo/src/Relation.ts:112`

`export const Source: unique symbol = entityInternal.RelationSourceId as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1951 no-casts `packages/core/echo/echo/src/Relation.ts:115`

`export const Target: unique symbol = entityInternal.RelationTargetId as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1952 no-casts `packages/core/echo/echo/src/Relation.ts:170`

`meta = props[internal.MetaId] as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1953 no-casts `packages/core/echo/echo/src/Relation.ts:177`

`(props as any)[internal.RelationSourceDXNId] = sourceDXN;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1954 no-casts `packages/core/echo/echo/src/Relation.ts:178`

`(props as any)[internal.RelationTargetDXNId] = targetDXN;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1955 no-casts `packages/core/echo/echo/src/Relation.ts:182`

`return internal.makeObject(schema as any, props as any, meta, type as any) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1956 no-casts `packages/core/echo/echo/src/Relation.ts:207`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1957 no-casts `packages/core/echo/echo/src/Relation.ts:222`

`const kind = (value as any)[Entity.KindId] ?? (value as any)[Entity.SnapshotKindId];` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1958 no-casts `packages/core/echo/echo/src/Relation.ts:230`

`return (value as any)[Entity.SnapshotKindId] === internal.EntityKind.Relation;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1959 no-casts `packages/core/echo/echo/src/Relation.ts:352`

`export const getSnapshot: <T extends Unknown>(rel: T) => Snapshot<T> = internal.getSnapshot as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1960 no-casts `packages/core/echo/echo/src/Relation.ts:376`

`export const getValue = (rel: Unknown | Snapshot, path: readonly (string | number)[]): any => {` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1961 no-casts `packages/core/echo/echo/src/Relation.ts:387`

`export const setValue: (rel: Mutable<Unknown>, path: readonly (string | number)[], value: any) => void =` declares a parameter or return type as `any`, widening the function's public signature and turning off type checking for every caller. Per the no-casts rule, fix the type at its source — give it the concrete type, a generic parameter, or `unknown` plus a narrowing guard — instead of `any`.

# ERROR cc9b81fc-1962 no-casts `packages/core/echo/echo/src/Relation.ts:388`

`internal.setValue as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1963 no-casts `packages/core/echo/echo/src/testing/api.test.ts:46`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const x: any = {};`

# ERROR cc9b81fc-1964 no-casts `packages/core/echo/echo/src/testing/api.test.ts:87`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const any: Entity.Unknown = null as any;`

# ERROR cc9b81fc-1965 no-casts `packages/core/echo/echo/src/testing/util.ts:27`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `getStaticTypeSchema(TypeSchema) as any,`

# ERROR cc9b81fc-1966 no-casts `packages/core/echo/echo/src/testing/util.ts:31`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `) as unknown as Type.Type;`

# ERROR cc9b81fc-1967 no-casts `packages/core/echo/echo/src/testing/util.ts:41`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const collapseOptionalUnion = (value: any): any => {`

# ERROR cc9b81fc-1968 no-casts `packages/core/echo/echo/src/testing/util.ts:60`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const stripEncodingContext = (value: any): any =>`

# ERROR cc9b81fc-1969 no-casts `packages/core/echo/echo/src/testing/util.ts:64`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `encoding: value.encoding.map((link: any) => ({ ...link, to: { ...link.to, context: undefined } })),`

# ERROR cc9b81fc-1970 no-casts `packages/core/echo/echo/src/testing/util.ts:71`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const prepareAstForCompare = (obj: SchemaAST.AST): any =>`

# ERROR cc9b81fc-1971 no-casts `packages/core/echo/echo/src/testing/util.ts:72`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `deepMapValues(obj, (value: any, recurse: any) => {`

# ERROR cc9b81fc-1972 no-casts `packages/core/echo/echo/src/testing/util.ts:84`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `for (const sym of Object.getOwnPropertySymbols(clone as any)) {`

# ERROR cc9b81fc-1973 no-casts `packages/core/echo/echo/src/Type.test.ts:81`

`expect((entity as any)[Entity.KindId]).toBe(Entity.Kind.Type);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1974 no-casts `packages/core/echo/echo/src/Type.test.ts:126`

`expect((entity as any)[Entity.KindId]).toBe(Entity.Kind.Type);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1975 no-casts `packages/core/echo/echo/src/Type.test.ts:128`

`expect((entity.jsonSchema as any).relationSource?.$ref).toBe(Type.getURI(TestSchema.Person).toString());` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1976 no-casts `packages/core/echo/echo/src/Type.test.ts:129`

`expect((entity.jsonSchema as any).relationTarget?.$ref).toBe(Type.getURI(TestSchema.Organization).toString());` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1977 no-casts `packages/core/echo/echo/src/Type.test.ts:139`

`expect((entity.jsonSchema as any).relationSource?.$ref).toBe(unknownURI);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1978 no-casts `packages/core/echo/echo/src/Type.test.ts:140`

`expect((entity.jsonSchema as any).relationTarget?.$ref).toBe(unknownURI);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1979 no-casts `packages/core/echo/echo/src/Type.test.ts:187`

`expect((TestSchema.Person as any)['~@dxos/echo/SchemaKind']).toBe(Entity.Kind.Object);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1980 no-casts `packages/core/echo/echo/src/Type.test.ts:192`

`expect((TestSchema.HasManager as any)['~@dxos/echo/SchemaKind']).toBe(Entity.Kind.Relation);` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1981 no-casts `packages/core/echo/echo/src/Type.ts:144`

`} = (dxn, options) => (schema) => internal.makeObjectType(dxn, schema, options) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1982 no-casts `packages/core/echo/echo/src/Type.ts:156`

`export const Type: Type<typeInternal.TypeSchema> = typeInternal.TypeSchema as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1983 no-casts `packages/core/echo/echo/src/Type.ts:193`

`internal.getStaticTypeSchema(typeInternal.TypeSchema) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1984 no-casts `packages/core/echo/echo/src/Type.ts:194`

`data as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1985 no-casts `packages/core/echo/echo/src/Type.ts:201`

`) as unknown as Type<typeInternal.TypeSchema>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1986 no-casts `packages/core/echo/echo/src/Type.ts:232`

`internal.getStaticTypeSchema(typeInternal.TypeSchema) as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1987 no-casts `packages/core/echo/echo/src/Type.ts:233`

`{ ...rest, jsonSchema: enrichedJsonSchema } as any,` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1988 no-casts `packages/core/echo/echo/src/Type.ts:240`

`) as unknown as Type<typeInternal.TypeSchema>;` routes the value through the `as unknown as T` double-cast escape hatch to force an otherwise incompatible type past the checker. Per the no-casts rule, fix the type at its source — adjust the producing function's return type, add a real converter, or use a type guard — instead of laundering it through `unknown`.

# ERROR cc9b81fc-1989 no-casts `packages/core/echo/echo/src/Type.ts:330`

`}) as any;` casts to `any`, discarding the type information the compiler would otherwise check on this expression. Per the no-casts rule, fix the type at its source instead — narrow or correctly type the underlying value (an interface, a type guard, or a generic parameter) rather than erasing it with `any`.

# ERROR cc9b81fc-1990 no-casts `packages/core/echo/feed/src/feed-store.test.ts:233`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(result2.blocks[0].insertionId!).toBeGreaterThan(result1.blocks[0].insertionId!);`

# ERROR cc9b81fc-1991 no-casts `packages/core/echo/feed/src/feed-store.test.ts:291`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(block1.insertionId).toBeLessThan(block2.insertionId!);`

# ERROR cc9b81fc-1992 no-casts `packages/core/echo/feed/src/feed-store.test.ts:292`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(block2.insertionId).toBeLessThan(block3.insertionId!);`

# ERROR cc9b81fc-1993 no-casts `packages/core/echo/feed/src/feed-store.test.ts:306`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `cursor: invalidCursor as any,`

# ERROR cc9b81fc-1994 no-casts `packages/core/echo/feed/src/feed-store.test.ts:330`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `cursor: validCursor as any,`

# ERROR cc9b81fc-1995 no-casts `packages/core/echo/feed/src/feed-store.test.ts:391`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(blocks[0].position).toBeLessThan(blocks[1].position!);`

# ERROR cc9b81fc-1996 no-casts `packages/core/echo/feed/src/feed-store.test.ts:392`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(blocks[1].position).toBeLessThan(blocks[2].position!);`

# ERROR cc9b81fc-1997 no-casts `packages/core/echo/feed/src/feed-store.ts:153`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feed = { spaceId, feedId: block.feedId!, feedNamespace };`

# ERROR cc9b81fc-1998 no-casts `packages/core/echo/feed/src/feed-store.ts:157`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const blockId = blockNaturalKey(block.feedId!, block.actorId, block.sequence);`

# ERROR cc9b81fc-1999 no-casts `packages/core/echo/feed/src/feed-store.ts:175`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const blockId = blockNaturalKey(row.feedId!, row.actorId, row.sequence);`

# ERROR cc9b81fc-2000 no-casts `packages/core/echo/feed/src/feed-store.ts:195`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `{ encryptionKeyId: row.encryptionKeyId!, iv: new Uint8Array(row.iv!), ciphertext: data },`

# ERROR cc9b81fc-2001 no-casts `packages/core/echo/feed/src/feed-store.ts:196`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `{ feed: { spaceId, feedId: row.feedId!, feedNamespace }, blockId },`

# ERROR cc9b81fc-2002 no-casts `packages/core/echo/feed/src/feed-store.ts:366`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `(feedId) => this.#ensureFeed(request.spaceId!, feedId),`

# ERROR cc9b81fc-2003 no-casts `packages/core/echo/feed/src/feed-store.ts:559`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const key = block.feedId!;`

# ERROR cc9b81fc-2004 no-casts `packages/core/echo/feed/src/feed-store.ts:561`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `feedKeys.set(key, { feedId: block.feedId! });`

# ERROR cc9b81fc-2005 no-casts `packages/core/echo/feed/src/feed-store.ts:570`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const id = yield* this.#ensureFeed(request.spaceId!, feedId, request.feedNamespace);`

# ERROR cc9b81fc-2006 no-casts `packages/core/echo/feed/src/feed-store.ts:597`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const key = block.feedId!;`

# ERROR cc9b81fc-2007 no-casts `packages/core/echo/feed/src/feed-store.ts:598`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feedPrivateId = feedPrivateIds.get(key)!;`

# ERROR cc9b81fc-2008 no-casts `packages/core/echo/feed/src/feed-store.ts:603`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `positionToInsert = maxPositions.get(request.feedNamespace)! + 1;`

# ERROR cc9b81fc-2009 no-casts `packages/core/echo/feed/src/feed-store.ts:626`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `positions.push(positionToInsert!);`

# ERROR cc9b81fc-2010 no-casts `packages/core/echo/feed/src/feed-store.ts:627`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `maxPositions.set(request.feedNamespace, positionToInsert!);`

# ERROR cc9b81fc-2011 no-casts `packages/core/echo/feed/src/feed-store.ts:652`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `positions.push(positionToInsert!);`

# ERROR cc9b81fc-2012 no-casts `packages/core/echo/feed/src/feed-store.ts:653`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `maxPositions.set(request.feedNamespace, positionToInsert!);`

# ERROR cc9b81fc-2013 no-casts `packages/core/echo/feed/src/feed-store.ts:705`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feedPrivateId = feedPrivateIds.get(key)!;`

# ERROR cc9b81fc-2014 no-casts `packages/core/echo/feed/src/feed-store.ts:768`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `blocksBySpaceNamespace.get(spaceNamespaceKey)!.blocks.push(block);`

# ERROR cc9b81fc-2015 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:659`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const recordIds = objects.map((o) => o.recordId!);`

# ERROR cc9b81fc-2016 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:664`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it and a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, both of which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((snapshotMap.get(objects[0].recordId!) as any).value).toBe('alpha');`

# ERROR cc9b81fc-2017 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:665`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it and a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, both of which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((snapshotMap.get(objects[1].recordId!) as any).value).toBe('beta');`

# ERROR cc9b81fc-2018 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:695`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const snapshots = yield* index.querySnapshotsJSON([object.recordId!, staleId]);`

# ERROR cc9b81fc-2019 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:698`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(snapshots[0].recordId).toBe(object.recordId!);`

# ERROR cc9b81fc-2020 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:699`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((snapshots[0].snapshot as any).value).toBe('present');`

# ERROR cc9b81fc-2021 no-casts `packages/core/echo/index-core/src/indexes/fts-index.test.ts:728`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const recordIds = objects.map((o) => o.recordId!);`

# ERROR cc9b81fc-2022 no-casts `packages/core/echo/index-core/src/indexes/reverse-ref-index.ts:136`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const refs = extractReferences(data as unknown as Record<string, unknown>);`

# ERROR cc9b81fc-2023 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:51`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const result = await verifyChain(chain, issuer, signingKey!);`

# ERROR cc9b81fc-2024 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:75`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.value = await signer.sign(signingKey ?? issuer, signedPayload);`

# ERROR cc9b81fc-2025 no-casts `packages/core/halo/credentials/src/credentials/credential-factory.ts:77`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain = chain;`

# ERROR cc9b81fc-2026 no-casts `packages/core/halo/credentials/src/credentials/signing.ts:62`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const canonicalStringify = (obj: any): string =>`

# ERROR cc9b81fc-2027 no-casts `packages/core/halo/credentials/src/credentials/signing.ts:71`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `replacer: function (this: any, key: any, value: any) {`

# ERROR cc9b81fc-2028 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:57`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.value[0]++;`

# ERROR cc9b81fc-2029 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:110`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.nonce = undefined;`

# ERROR cc9b81fc-2030 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:136`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.nonce = PublicKey.random().asUint8Array();`

# ERROR cc9b81fc-2031 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:241`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain = undefined;`

# ERROR cc9b81fc-2032 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:283`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain!.credential.proof!.value = randomBytes(`

# ERROR cc9b81fc-2033 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:284`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain!.credential.proof!.value.length,`

# ERROR cc9b81fc-2034 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:325`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain!.credential = await createCredential({`

# ERROR cc9b81fc-2035 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:376`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain!.credential = await createCredential({`

# ERROR cc9b81fc-2036 no-casts `packages/core/halo/credentials/src/credentials/verifier.test.ts:491`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `credential.proof!.chain!.credential.proof!.chain!.credential = await createCredential({`

# ERROR cc9b81fc-2037 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:21`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (!credential.issuer.equals(credential.proof!.signer)) {`

# ERROR cc9b81fc-2038 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:22`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (!credential.proof!.chain) {`

# ERROR cc9b81fc-2039 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:29`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const result = await verifyChain(credential.proof!.chain, credential.issuer, credential.proof!.signer);`

# ERROR cc9b81fc-2040 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:48`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (credential.proof!.type !== SIGNATURE_TYPE_ED25519) {`

# ERROR cc9b81fc-2041 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:51`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `errors: [`Invalid signature type: ${credential.proof!.type}`],`

# ERROR cc9b81fc-2042 no-casts `packages/core/halo/credentials/src/credentials/verifier.ts:56`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (!(await verifySignature(credential.proof!.signer, signData, credential.proof!.value))) {`

# ERROR cc9b81fc-2043 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:19`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `private _root = { id: -1, parents: [], children: [] } as any as ChainVertex<A>;`

# ERROR cc9b81fc-2044 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:24`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `private _sentinel = { id: -2, parents: [], children: [] } as any as ChainVertex<A>;`

# ERROR cc9b81fc-2045 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:48`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return this._sentinel.parents.map((v) => v.credential!.id!);`

# ERROR cc9b81fc-2046 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:63`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._vertexByCredentialId.set(credential.id!, newVertex);`

# ERROR cc9b81fc-2047 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:131`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const path = paths.pop()!;`

# ERROR cc9b81fc-2048 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:212`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `path.credentials.add(headCredential.id!);`

# ERROR cc9b81fc-2049 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:258`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const newState = this._stateHandler.createState(subjectVertex.credential!, subjectVertex.assertion);`

# ERROR cc9b81fc-2050 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:290`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const isPointInEveryPath = paths.every((p) => p.credentials.has(headCredential.id!));`

# ERROR cc9b81fc-2051 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:313`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `let it = path.forkPoint!;`

# ERROR cc9b81fc-2052 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:317`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `it = it!.forkPoint!;`

# ERROR cc9b81fc-2053 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:356`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `result.chosenPath![forkPoint.head.id].push(...(path.chosenPath![forkPoint.head.id] ?? []));`

# ERROR cc9b81fc-2054 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:368`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const vertex = branch.state.get(subject)!;`

# ERROR cc9b81fc-2055 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:379`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `stateOverrides: subjectToBranch.mapValues((v, key) => v.state.get(key)!),`

# ERROR cc9b81fc-2056 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:400`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const candidateVertex = candidate.state.get(modifiedSubject)!;`

# ERROR cc9b81fc-2057 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:401`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const currentVertex = existing.state.get(modifiedSubject)!;`

# ERROR cc9b81fc-2058 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:411`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const candidateCredential = candidateVertex.credential!;`

# ERROR cc9b81fc-2059 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:412`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const currentCredential = currentVertex.credential!;`

# ERROR cc9b81fc-2060 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:414`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (existing.credentials.has(candidateCredential.id!) !== candidate.credentials.has(currentCredential.id!)) {`

# ERROR cc9b81fc-2061 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:419`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return candidate.credentials.has(currentCredential.id!);`

# ERROR cc9b81fc-2062 no-casts `packages/core/halo/credentials/src/graph/credential-graph.ts:556`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return Object.fromEntries(Object.entries(path.chosenPath!).map(([k, vs]) => [k, vs.map((v) => v.id)]));`

# ERROR cc9b81fc-2063 no-casts `packages/core/halo/credentials/src/invitations.ts:10`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `id: any;`

# ERROR cc9b81fc-2064 no-casts `packages/core/halo/credentials/src/invitations.ts:11`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `authNonce: any;`

# ERROR cc9b81fc-2065 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:187`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `credential: { ...chain.credential, proof: { ...chain.credential.proof, signer: PublicKey.random() } as any },`

# ERROR cc9b81fc-2066 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:223`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(await verifyPresentationSignature(presentation, presentation.proofs![0])).to.deep.equal({ kind: 'pass' });`

# ERROR cc9b81fc-2067 no-casts `packages/core/halo/credentials/src/presentations/verifier.test.ts:252`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await verifyPresentationSignature(presentation, { ...presentation.proofs![0], creationDate: new Date(0) }),`

# ERROR cc9b81fc-2068 no-casts `packages/core/halo/credentials/src/state-machine/invitation-state-machine.test.ts:135`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return message.credential!.credential;`

# ERROR cc9b81fc-2069 no-casts `packages/core/halo/credentials/src/state-machine/invitation-state-machine.test.ts:144`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `'credentialId': invitation.id!,`

# ERROR cc9b81fc-2070 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:538`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const credential = feedMessage[0].credential!.credential;`

# ERROR cc9b81fc-2071 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:540`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return feedMessage[0].credential!.credential.id!;`

# ERROR cc9b81fc-2072 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:558`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const credential = feedMessage[0].credential!.credential;`

# ERROR cc9b81fc-2073 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.test.ts:560`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return feedMessage[0].credential!.credential.id!;`

# ERROR cc9b81fc-2074 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:104`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return credential!.issuer.equals(this._spaceKey);`

# ERROR cc9b81fc-2075 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:126`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const memberId = update.credential!.subject.id!;`

# ERROR cc9b81fc-2076 no-casts `packages/core/halo/credentials/src/state-machine/member-state-machine.ts:147`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return Object.entries(SpaceMember.Role).find(([_, value]) => value === role)![0];`

# ERROR cc9b81fc-2077 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:56`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `body?: any;`

# ERROR cc9b81fc-2078 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:174`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2079 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:184`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `throw processingError!;`

# ERROR cc9b81fc-2080 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:241`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2081 no-casts `packages/core/mesh/edge-client/src/base-http-client.ts:251`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `throw processingError!;`

# ERROR cc9b81fc-2082 no-casts `packages/core/mesh/edge-client/src/edge-ai-http-client.ts:193`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return send(request.body.body as any);`

# ERROR cc9b81fc-2083 no-casts `packages/core/mesh/edge-client/src/edge-client.ts:56`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `get info(): any;`

# ERROR cc9b81fc-2084 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:35`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR cc9b81fc-2085 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:57`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(String(targetCall![0])).toBe('https://edge.example.com/ai/generate/anthropic/v1/messages?beta=true');`

# ERROR cc9b81fc-2086 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:58`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(targetCall![1]?.method).toBe('POST');`

# ERROR cc9b81fc-2087 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:75`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR cc9b81fc-2088 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:120`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR cc9b81fc-2089 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:147`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((putCall![1]?.headers as Record<string, string> | undefined)?.Authorization).toBeUndefined();`

# ERROR cc9b81fc-2090 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:177`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any, _init?: RequestInit) => {`

# ERROR cc9b81fc-2091 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:203`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(String(putCall![0])).toBe('https://edge.example.com/api/file/abc123');`

# ERROR cc9b81fc-2092 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:204`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(putCall![1]?.method).toBe('POST');`

# ERROR cc9b81fc-2093 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:205`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(putCall![1]?.body).toBe(bytes);`

# ERROR cc9b81fc-2094 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:206`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((putCall![1]?.headers as Record<string, string>)['Content-Type']).toBe('application/octet-stream');`

# ERROR cc9b81fc-2095 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:216`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any, init?: RequestInit) => {`

# ERROR cc9b81fc-2096 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:276`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any) => {`

# ERROR cc9b81fc-2097 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:298`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (_input: any, init?: RequestInit) => {`

# ERROR cc9b81fc-2098 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:309`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (input: any, init?: RequestInit) => {`

# ERROR cc9b81fc-2099 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:331`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `async (_input: any, _init?: RequestInit) =>`

# ERROR cc9b81fc-2100 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:346`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((uploadCall![1]?.headers as Record<string, string>).Authorization).toBe('Bearer secret-key');`

# ERROR cc9b81fc-2101 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:351`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `async (_input: any, _init?: RequestInit) =>`

# ERROR cc9b81fc-2102 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:367`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetchMock = vi.fn(async (_input: any, _init?: RequestInit) => new Response(null, { status: 200 }));`

# ERROR cc9b81fc-2103 no-casts `packages/core/mesh/edge-client/src/edge-http-client.test.ts:376`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect((putCall![1]?.headers as Record<string, string>).Authorization).toBe('Bearer secret-key');`

# ERROR cc9b81fc-2104 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:456`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `input: any,`

# ERROR cc9b81fc-2105 no-casts `packages/core/mesh/edge-client/src/edge-http-client.ts:654`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `HttpClient.execute(HttpClientRequest.make(_args.method as any)(url.toString())),`

# ERROR cc9b81fc-2106 no-casts `packages/core/mesh/edge-client/src/edge-ws-muxer.ts:41`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `private _sendTimeout: any | undefined;`

# ERROR cc9b81fc-2107 no-casts `packages/core/mesh/edge-client/src/protocol.ts:28`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `toJson(message: Message): any {`

# ERROR cc9b81fc-2108 no-casts `packages/core/mesh/edge-client/src/protocol.ts:98`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const toUint8Array = async (data: any): Promise<Uint8Array> => {`

# ERROR cc9b81fc-2109 no-casts `packages/core/mesh/edge-client/src/service/edge-service.test.ts:30`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const fetch = (async (input: any, init: any) => {`

# ERROR cc9b81fc-2110 no-casts `packages/core/mesh/edge-client/src/service/edge-service.test.ts:33`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `return (await handler(captured)) as unknown as Response;`

# ERROR cc9b81fc-2111 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:22`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `messageHandler?: (payload: any) => Promise<Uint8Array | undefined>;`

# ERROR cc9b81fc-2112 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:34`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const messageSink: any[] = [];`

# ERROR cc9b81fc-2113 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:35`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const messageSourceLog: any[] = [];`

# ERROR cc9b81fc-2114 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:37`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const sendResponseMessage = createResponseSender(() => connection!.muxer);`

# ERROR cc9b81fc-2115 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:43`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `ws.on('message', async (data: any) => {`

# ERROR cc9b81fc-2116 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:83`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return connection!.muxer.send(msg);`

# ERROR cc9b81fc-2117 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:87`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `connection!.ws.close(1011);`

# ERROR cc9b81fc-2118 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:94`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `return (_: any, callback: (admit: boolean) => void) => {`

# ERROR cc9b81fc-2119 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:109`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const recipient = request.source!;`

# ERROR cc9b81fc-2120 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:116`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `serviceId: request.serviceId!,`

# ERROR cc9b81fc-2121 no-casts `packages/core/mesh/edge-client/src/testing/test-utils.ts:125`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `? params.payloadDecoder(request.payload!.value!)`

# ERROR cc9b81fc-2122 no-casts `packages/core/mesh/messaging/src/messenger.ts:112`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(!this._onAckCallbacks.has(reliablePayload.messageId!));`

# ERROR cc9b81fc-2123 no-casts `packages/core/mesh/messaging/src/messenger.ts:141`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._onAckCallbacks.delete(reliablePayload.messageId!);`

# ERROR cc9b81fc-2124 no-casts `packages/core/mesh/messaging/src/messenger.ts:156`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._onAckCallbacks.delete(reliablePayload.messageId!);`

# ERROR cc9b81fc-2125 no-casts `packages/core/mesh/messaging/src/messenger.ts:220`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `listeners!.delete(onMessage);`

# ERROR cc9b81fc-2126 no-casts `packages/core/mesh/messaging/src/messenger.ts:281`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (this._receivedMessages.has(reliablePayload.messageId!)) {`

# ERROR cc9b81fc-2127 no-casts `packages/core/mesh/messaging/src/messenger.ts:285`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._receivedMessages.add(reliablePayload.messageId!);`

# ERROR cc9b81fc-2128 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:43`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const connection = mesh.createConnection({ peerKey: peer.peerKey!, identityDid: peer.identityDid! });`

# ERROR cc9b81fc-2129 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:44`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const manager = new EdgeSignalManager({ edgeConnection: connection as any });`

# ERROR cc9b81fc-2130 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.test.ts:115`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `await (subscriber.manager as any)._rejoinAllSwarms();`

# ERROR cc9b81fc-2131 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.ts:287`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { joinedPeers: oldPeers } = this._swarmPeers.get(topic)!;`

# ERROR cc9b81fc-2132 no-casts `packages/core/mesh/messaging/src/signal-manager/edge-signal-manager.ts:313`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._swarmPeers.get(topic)!.joinedPeers = newPeers;`

# WARN cc9b81fc-2133 no-sleep-in-test `packages/core/mesh/messaging/src/signal-manager/memory-signal-manager.test.ts:92`

`await sleep(20)` (also lines 130, 178) is used to assert a message was *not* delivered to an unsubscribed/non-matching sink. The file already uses `expect.poll` for the positive case (line 89) — use the same bounded-poll style, or an explicit negative-window primitive, instead of a bare sleep.

# ERROR cc9b81fc-2134 no-casts `packages/core/mesh/messaging/src/signal-manager/memory-signal-manager.ts:121`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._context.swarms.get(topic)!.add(peer);`

# ERROR cc9b81fc-2135 no-casts `packages/core/mesh/messaging/src/signal-manager/memory-signal-manager.ts:153`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._context.swarms.get(topic)!.delete(peer);`

# ERROR cc9b81fc-2136 no-casts `packages/core/mesh/messaging/src/testing/test-edge-mesh.ts:147`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `get info(): any {`

# ERROR cc9b81fc-2137 no-casts `packages/core/mesh/network-manager/src/connection-log.ts:64`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `info.connections!.push(connectionInfo);`

# ERROR cc9b81fc-2138 no-casts `packages/core/mesh/network-manager/src/connection-log.ts:71`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `connectionInfo.events!.push({`

# ERROR cc9b81fc-2139 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:26`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `onSignal = (async () => {}) as any,`

# ERROR cc9b81fc-2140 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:102`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `onSignal: (async () => {}) as any,`

# ERROR cc9b81fc-2141 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:107`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `onSignal: (async () => {}) as any,`

# ERROR cc9b81fc-2142 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:209`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `onSignal: (async () => {}) as any,`

# ERROR cc9b81fc-2143 no-casts `packages/core/mesh/network-manager/src/signal/swarm-messenger.node.test.ts:218`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `onSignal: (async () => {}) as any,`

# ERROR cc9b81fc-2144 no-casts `packages/core/mesh/network-manager/src/swarm/connection-limiter.ts:68`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._waitingPromises.get(sessionId)!.reject(new CancelledError());`

# ERROR cc9b81fc-2145 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:277`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2146 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:284`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2147 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:328`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2148 no-casts `packages/core/mesh/network-manager/src/swarm/connection.ts:334`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2149 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:152`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2150 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:197`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2151 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:210`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2152 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:227`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2153 no-casts `packages/core/mesh/network-manager/src/swarm/peer.ts:319`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._connectionCtx!,`

# ERROR cc9b81fc-2154 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.test.ts:112`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await asyncTimeout(connectionDisplaced!, 1000);`

# ERROR cc9b81fc-2155 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.test.ts:138`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `void peer1.swarm._peers.get(peer2.peer)!.connection!.close();`

# ERROR cc9b81fc-2156 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:27`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getClassName = (obj: any) => Object.getPrototypeOf(obj).constructor.name;`

# ERROR cc9b81fc-2157 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:293`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (!peer!.advertizing) {`

# ERROR cc9b81fc-2158 no-casts `packages/core/mesh/network-manager/src/swarm/swarm.ts:354`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2159 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:129`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `void swarm1.protocol.connections.get(swarm2.peer.peerId)!.closeConnection(new Error('test error'));`

# ERROR cc9b81fc-2160 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:152`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `?.disconnected.waitFor(({ peerKey }) => peer1.peerId.equals(peerKey!));`

# ERROR cc9b81fc-2161 no-casts `packages/core/mesh/network-manager/src/tests/basic-test-suite.ts:164`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `.poll(() => !!peer2._networkManager.getSwarm(topic)!._peers.get({ peerKey: peer1.peerId.toHex() })?.advertizing, {`

# ERROR cc9b81fc-2162 no-casts `packages/core/mesh/network-manager/src/transport/memory-transport.ts:196`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const toError = (err: any): Error => (err instanceof Error ? err : new Error(String(err)));`

# ERROR cc9b81fc-2163 no-casts `packages/core/mesh/network-manager/src/transport/tcp/tcp-transport.ts:55`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { port } = this._server!.address() as AddressInfo;`

# ERROR cc9b81fc-2164 no-casts `packages/core/mesh/network-manager/src/transport/tcp/tcp-transport.ts:134`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this.options.stream.pipe(this._socket!).pipe(this.options.stream);`

# ERROR cc9b81fc-2165 no-casts `packages/core/mesh/network-manager/src/transport/transport.ts:83`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `rawStats?: any;`

# ERROR cc9b81fc-2166 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:58`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `}) as any;`

# ERROR cc9b81fc-2167 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:70`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `connection.onnegotiationneeded?.(null as any);`

# ERROR cc9b81fc-2168 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-connection-factory.ts:79`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return typeof (globalThis as any).RTCPeerConnection === 'undefined'`

# ERROR cc9b81fc-2169 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:145`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2170 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:169`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `onicecandidateerror: (event: any) => {`

# ERROR cc9b81fc-2171 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-peer-connection.ts:473`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `reject: (reason?: any) => void;`

# WARN cc9b81fc-2172 no-sleep-in-test `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:59`

`await sleep(10)` (also lines 154, 162) directly gates assertions/continuations after simulated channel events, even though the same file already uses the proper primitive `expect.poll(...)` two lines earlier (line 48). Prefer `expect.poll` or an explicit event/Trigger over the fixed sleep.

# ERROR cc9b81fc-2173 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:116`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const deliveredMessages: any[] = [];`

# ERROR cc9b81fc-2174 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:119`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `write: (chunk: any, _: BufferEncoding, callback: (error?: Error | null) => void) => {`

# ERROR cc9b81fc-2175 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:124`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const options = { topic: 'test', stream } as any as TransportOptions;`

# ERROR cc9b81fc-2176 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:153`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(channel as any).onmessage({ data: message });`

# ERROR cc9b81fc-2177 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.test.ts:177`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any as RtcPeerConnection,`

# ERROR cc9b81fc-2178 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:151`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `private async _handleChannelWrite(chunk: any, callback: PendingStreamFlushedCallback): Promise<void> {`

# ERROR cc9b81fc-2179 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:166`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2180 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-channel.ts:185`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# WARN cc9b81fc-2181 no-sleep-in-test `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:35`

Nearly every test in this file (e.g. lines 35, 42, 53, 80, 93, 103, 106, 128, 152, 161, 173, 181, 189, 204, 207, 209) uses `await sleep(20)` as the literal wait before asserting on `peer.proxy.isOpen`/`connected`/error state. Replace the fixed 20ms waits with the `Trigger`s already used elsewhere in the file (e.g. `failed.wait()`) or `expect.poll`.

# ERROR cc9b81fc-2182 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:364`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `write: (chunk: any, _: BufferEncoding, callback: () => void) => {`

# ERROR cc9b81fc-2183 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:369`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `let sendSignal: any | undefined;`

# ERROR cc9b81fc-2184 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:374`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `sendSignalFromTransport: async (signal: any) => {`

# ERROR cc9b81fc-2185 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:404`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `public async onSignal(signal: any): Promise<void> {`

# ERROR cc9b81fc-2186 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.test.ts:409`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return {} as any;`

# ERROR cc9b81fc-2187 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:55`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2188 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:99`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(err: any) => {`

# ERROR cc9b81fc-2189 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:127`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2190 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:133`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2191 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:168`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2192 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-proxy.ts:213`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `private _raiseIfOpen(error: any): void {`

# ERROR cc9b81fc-2193 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-service.ts:173`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2194 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-service.ts:178`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2195 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-stats.ts:48`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const statsEntries: [string, any][] = Array.from((stats as any).entries());`

# ERROR cc9b81fc-2196 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport-stats.ts:65`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `raw: Object.fromEntries(stats as any),`

# ERROR cc9b81fc-2197 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:130`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const deliverSignal = (channel: RtcTransportChannel, signal: any) => {`

# ERROR cc9b81fc-2198 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:145`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const signalBuffer: any[] = [];`

# ERROR cc9b81fc-2199 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:146`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const sendSignal = async (signal: any) => {`

# ERROR cc9b81fc-2200 no-casts `packages/core/mesh/network-manager/src/transport/webrtc/rtc-transport.test.ts:196`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(channel as any).open = async () => {`

# WARN cc9b81fc-2201 no-sleep-in-test `packages/core/mesh/rpc/src/rpc.test.ts:59`

`await sleep(5)` (also lines 78, 119, 147, 172) is used as the literal wait before reading a shared `aliceOpen`/`open`/`portOpen` flag set from a callback. Replace with a `Trigger` (or an `onOpen`-style callback resolving a promise) rather than sleeping a fixed 5ms and hoping the callback has run.

# ERROR cc9b81fc-2202 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:211`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const text = Buffer.from(msg.value!).toString();`

# ERROR cc9b81fc-2203 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:264`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2204 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:358`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(msg.value!).toEqual(encodeMessage('request'));`

# ERROR cc9b81fc-2205 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:413`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).closed).toEqual(true);`

# ERROR cc9b81fc-2206 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:414`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).error).toBeInstanceOf(Error);`

# ERROR cc9b81fc-2207 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:415`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).error.message).toEqual('Test error');`

# ERROR cc9b81fc-2208 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:452`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(msg.value!).toEqual(encodeMessage('request'));`

# ERROR cc9b81fc-2209 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:499`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).closed).toEqual(true);`

# ERROR cc9b81fc-2210 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:500`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).error).toBeInstanceOf(Error);`

# ERROR cc9b81fc-2211 no-casts `packages/core/mesh/rpc/src/rpc.test.ts:501`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `expect((msgs[0] as any).error.message).toEqual('Test error');`

# ERROR cc9b81fc-2212 no-casts `packages/core/mesh/rpc/src/rpc.ts:156`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2213 no-casts `packages/core/mesh/rpc/src/rpc.ts:159`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `}) as any;`

# ERROR cc9b81fc-2214 no-casts `packages/core/mesh/rpc/src/rpc.ts:213`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2215 no-casts `packages/core/mesh/rpc/src/rpc.ts:219`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2216 no-casts `packages/core/mesh/rpc/src/rpc.ts:317`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const item = this._outgoingRequests.get(responseId)!;`

# ERROR cc9b81fc-2217 no-casts `packages/core/mesh/rpc/src/rpc.ts:585`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2218 no-casts `packages/core/mesh/rpc/src/service.test.ts:93`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2219 no-casts `packages/core/mesh/rpc/src/service.test.ts:604`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `testCall: async (req: any, options?: RequestOptions) => {`

# ERROR cc9b81fc-2220 no-casts `packages/core/mesh/rpc/src/service.test.ts:628`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const received = receivedCtx!.getAttribute(TRACE_SPAN_ATTRIBUTE);`

# ERROR cc9b81fc-2221 no-casts `packages/core/mesh/rpc/src/service.test.ts:643`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `testCall: async (req: any, options?: RequestOptions) => {`

# ERROR cc9b81fc-2222 no-casts `packages/core/mesh/rpc/src/service.test.ts:681`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `testCall: (_req: any, options?: RequestOptions) => {`

# ERROR cc9b81fc-2223 no-casts `packages/core/mesh/rpc/src/service.test.ts:708`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const received = receivedCtx!.getAttribute(TRACE_SPAN_ATTRIBUTE);`

# ERROR cc9b81fc-2224 no-casts `packages/core/mesh/rpc/src/service.ts:235`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `rpc[serviceFqn] = services[serviceName].createServer(handlers[serviceName] as any);`

# ERROR cc9b81fc-2225 no-casts `packages/core/mesh/teleport-e2e/src/teleport.test.ts:19`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `connection1.teleport!.addExtension('example.testing.rpc', extension1);`

# ERROR cc9b81fc-2226 no-casts `packages/core/mesh/teleport-e2e/src/teleport.test.ts:21`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `connection2.teleport!.addExtension('example.testing.rpc', extension2);`

# ERROR cc9b81fc-2227 no-casts `packages/core/mesh/teleport-extension-automerge-replicator/src/automerge-replicator.test.ts:40`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(deliveredMessage!.payload[0]).to.eq(42);`

# ERROR cc9b81fc-2228 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:103`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `postMessage(channel: string, payload: any): void {`

# ERROR cc9b81fc-2229 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:131`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._listeners.get(channel)!.add(callback);`

# ERROR cc9b81fc-2230 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:135`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._listeners.get(channel)!.delete(callback);`

# ERROR cc9b81fc-2231 no-casts `packages/core/mesh/teleport-extension-gossip/src/gossip.ts:142`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._listeners.get(message.channelId)!.forEach((callback) => {`

# WARN cc9b81fc-2232 no-sleep-in-test `packages/core/mesh/teleport-extension-gossip/src/stress.test.ts:44`

`await sleep(10_000)` is used to "wait for all peers to be online," immediately followed by calls to `waitForAgentsOnline(..., 500)` — the actual condition-checking primitive. The 10-second blanket sleep should be dropped in favor of giving `waitForAgentsOnline` a sufficient timeout directly, per the rule against fixed-delay synchronization.

# ERROR cc9b81fc-2233 no-casts `packages/core/mesh/teleport-extension-gossip/src/testing.ts:39`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const extension = this.gossip.createExtension({ remotePeerId: connection.teleport!.remotePeerId });`

# WARN cc9b81fc-2234 no-sleep-in-test `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.test.ts:49`

`await sleep(5)` (line 49) and `await sleep(50)` (line 124) are the direct synchronization mechanism before asserting a feed did not yet receive data. Two other tests in the same file (lines 76, 156) already use `Event.wrap(...).waitForCondition(...)` and then redundantly add a `sleep(5)` afterward — the fix is to rely on the `waitForCondition` primitive already in use here instead of the extra sleep.

# ERROR cc9b81fc-2235 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:48`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (this._extensionContext!.initiator === false) {`

# ERROR cc9b81fc-2236 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:49`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._rpc!.rpc.ReplicatorService.updateFeeds({`

# ERROR cc9b81fc-2237 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:56`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `} else if (this._extensionContext!.initiator === true) {`

# ERROR cc9b81fc-2238 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:109`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(this._extensionContext!.initiator === true, 'Invalid call');`

# ERROR cc9b81fc-2239 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:114`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR cc9b81fc-2240 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:124`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR cc9b81fc-2241 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:192`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(this._extensionContext!.initiator === true, 'Invalid call');`

# ERROR cc9b81fc-2242 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:194`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { streamTag } = await this._rpc!.rpc.ReplicatorService.startReplication({ info: feedInfo });`

# ERROR cc9b81fc-2243 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:208`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `invariant(this._extensionContext!.initiator === false, 'Invalid call');`

# ERROR cc9b81fc-2244 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:224`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const networkStream = await this._extensionContext!.createStream(streamTag, {`

# ERROR cc9b81fc-2245 no-casts `packages/core/mesh/teleport-extension-replicator/src/replicator-extension.ts:285`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `networkStream.pipe(replicationStream as any).pipe(networkStream);`

# ERROR cc9b81fc-2246 no-casts `packages/core/mesh/teleport-extension-replicator/src/stress.test.ts:69`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const expectedLength = model.feeds.get(feedKey)!;`

# ERROR cc9b81fc-2247 no-casts `packages/core/mesh/teleport-extension-replicator/src/stress.test.ts:130`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `model.feeds.set(this.feedKey, model.feeds.get(this.feedKey)! + this.count);`

# ERROR cc9b81fc-2248 no-casts `packages/core/mesh/teleport/src/control-extension.ts:27`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `onTimeout: (err: any) => void;`

# ERROR cc9b81fc-2249 no-casts `packages/core/mesh/teleport/src/control-extension.ts:122`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# WARN cc9b81fc-2250 no-sleep-in-test `packages/core/mesh/teleport/src/muxing/balancer.test.ts:114`

`await sleep(20)` is the sole synchronization before asserting `balancer.buffersCount` reached 0. Replace with a deterministic wait on the balancer's own drain/flush completion.

# ERROR cc9b81fc-2251 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:89`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (chunk.length < dataLength!) {`

# ERROR cc9b81fc-2252 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:92`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `msgLength: dataLength!,`

# ERROR cc9b81fc-2253 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:98`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const channelBuffer = this._receiveBuffers.get(channelId)!;`

# ERROR cc9b81fc-2254 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:129`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const sendBuffer = this._sendBuffers.get(channelId)!;`

# ERROR cc9b81fc-2255 no-casts `packages/core/mesh/teleport/src/muxing/balancer.ts:186`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2256 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:117`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `while (offset < this._buffer!.length) {`

# ERROR cc9b81fc-2257 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:118`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const frame = decodeFrame(this._buffer!, offset);`

# ERROR cc9b81fc-2258 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:129`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (offset < this._buffer!.length) {`

# ERROR cc9b81fc-2259 no-casts `packages/core/mesh/teleport/src/muxing/framer.ts:131`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._buffer = this._buffer!.subarray(offset);`

# ERROR cc9b81fc-2260 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:193`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2261 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:255`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2262 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:285`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `).catch(async (err: any) => {`

# ERROR cc9b81fc-2263 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:321`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `).catch(async (err: any) => {`

# ERROR cc9b81fc-2264 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:385`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `channelId: channel.remoteId!,`

# ERROR cc9b81fc-2265 no-casts `packages/core/mesh/teleport/src/muxing/muxer.ts:412`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2266 no-casts `packages/core/mesh/teleport/src/teleport.ts:94`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2267 no-casts `packages/core/mesh/teleport/src/teleport.ts:180`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2268 no-casts `packages/core/mesh/teleport/src/teleport.ts:209`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2269 no-casts `packages/core/mesh/teleport/src/testing/test-extension-with-streams.ts:41`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const networkStream = await this.extensionContext!.createStream(streamTag, {`

# ERROR cc9b81fc-2270 no-casts `packages/core/mesh/teleport/src/testing/test-extension-with-streams.ts:108`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const stream = this._streams.get(streamTag)!;`

# ERROR cc9b81fc-2271 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:35`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._socket!.send(msg);`

# ERROR cc9b81fc-2272 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:38`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._socket!.onmessage = async (msg: WebSocket.MessageEvent) => {`

# ERROR cc9b81fc-2273 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:42`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `cb(msg.data as any);`

# ERROR cc9b81fc-2274 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:64`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._rpc!.open();`

# ERROR cc9b81fc-2275 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:68`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2276 no-casts `packages/core/mesh/websocket-rpc/src/client.ts:100`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return this._rpc!.rpc;`

# ERROR cc9b81fc-2277 no-casts `packages/core/mesh/websocket-rpc/src/e2e.node.test.ts:27`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `testCall: async (request: any) => {`

# ERROR cc9b81fc-2278 no-casts `packages/core/mesh/websocket-rpc/src/server.ts:56`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `cb(msg.data as any);`

# ERROR cc9b81fc-2279 no-casts `packages/core/protocols/scripts/gen-service-rpcs.ts:39`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const requestType = method.resolvedRequestType!.fullName.replace(/^\./, '');`

# ERROR cc9b81fc-2280 no-casts `packages/core/protocols/scripts/gen-service-rpcs.ts:40`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const responseType = method.resolvedResponseType!.fullName.replace(/^\./, '');`

# ERROR cc9b81fc-2281 no-casts `packages/core/protocols/src/edge/edge.ts:188`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `output?: any;`

# ERROR cc9b81fc-2282 no-casts `packages/core/protocols/src/edge/edge.ts:461`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `data: Record<SpaceId, { diagnostics?: any & { redFlags: string[] }; fetchError?: string }>;`

# ERROR cc9b81fc-2283 no-casts `packages/core/protocols/src/edge/edge.ts:519`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `name: 'name' in err ? err.name : (err as any).code || 'Error',`

# ERROR cc9b81fc-2284 no-casts `packages/core/protocols/src/errors/encoding.ts:8`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const encodeError = (err: any): SerializedErrorProto => {`

# ERROR cc9b81fc-2285 no-casts `packages/core/protocols/src/errors/helpers.ts:15`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const errorRegistry = new Map<string, (message?: string, context?: any) => Error>();`

# ERROR cc9b81fc-2286 no-casts `packages/core/protocols/src/errors/helpers.ts:17`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const registerError = (code: string, make: (message?: string, context?: any) => Error) => {`

# ERROR cc9b81fc-2287 no-casts `packages/core/protocols/src/errors/helpers.ts:22`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const registerErrorNoArgs = (code: string, Constructor: { new (options?: any): Error }) => {`

# ERROR cc9b81fc-2288 no-casts `packages/core/protocols/src/errors/helpers.ts:28`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `Constructor: { new (options?: { message?: string; context?: any }): Error },`

# ERROR cc9b81fc-2289 no-casts `packages/core/protocols/src/errors/helpers.ts:30`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `registerError(code, (message?: string, context?: any) => new Constructor({ message, context }));`

# ERROR cc9b81fc-2290 no-casts `packages/core/protocols/src/errors/helpers.ts:33`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const errorFromCode = (code?: string, message?: string, context?: any) => {`

# ERROR cc9b81fc-2291 no-casts `packages/core/protocols/src/FeedProtocol.ts:318`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `Object.values(WellKnownNamespaces).includes(namespace as any);`

# ERROR cc9b81fc-2292 no-casts `packages/core/protocols/src/messenger.ts:21`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `postMessage(channel: string, message: any): Promise<void>;`

# ERROR cc9b81fc-2293 no-casts `packages/core/protocols/src/proto/substitutions.ts:12`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `decode: (value: any) => PublicKey.from(value.data),`

# ERROR cc9b81fc-2294 no-casts `packages/core/protocols/src/proto/substitutions.ts:18`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `decode: (value: any) => PublicKey.from(new Uint8Array(value.data)).asBuffer(),`

# ERROR cc9b81fc-2295 no-casts `packages/core/protocols/src/proto/substitutions.ts:26`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `decode: (vector: any) =>`

# ERROR cc9b81fc-2296 no-casts `packages/core/protocols/src/proto/substitutions.ts:29`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `.filter((frame: any) => frame.feedKey != null && frame.seq != null)`

# ERROR cc9b81fc-2297 no-casts `packages/core/protocols/src/proto/substitutions.ts:30`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `.map((frame: any) => [PublicKey.from(frame.feedKey), frame.seq]),`

# ERROR cc9b81fc-2298 no-casts `packages/core/protocols/src/rpc-bridge.ts:29`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const source = handlers as unknown as Record<string, (...args: any[]) => unknown>;`

# ERROR cc9b81fc-2299 no-casts `packages/core/protocols/src/rpc-bridge.ts:30`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const normalized: Record<string, (...args: any[]) => unknown> = {};`

# ERROR cc9b81fc-2300 no-casts `packages/core/protocols/src/rpc-bridge.ts:41`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `Effect.provide(group.toLayer(normalized as unknown as RpcGroup.HandlersFrom<Rpcs>)),`

# ERROR cc9b81fc-2301 no-casts `packages/devtools/cli-util/src/testing/test-console.ts:11`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `function logToString(...args: any[]): string {`

# ERROR cc9b81fc-2302 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:68`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `valueDoc = value as unknown as Doc.Doc<any>;`

# ERROR cc9b81fc-2303 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:162`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return nestImpl(parent, key, builder!);`

# ERROR cc9b81fc-2304 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:241`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return nestedOptionImpl(builder, key, value!);`

# ERROR cc9b81fc-2305 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:251`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const whenImpl = (builder: FormBuilder, condition: any, ...ops: ((b: FormBuilder) => FormBuilder)[]): FormBuilder => {`

# ERROR cc9b81fc-2306 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:263`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export function when(builder: FormBuilder, condition: any, ...ops: ((b: FormBuilder) => FormBuilder)[]): FormBuilder;`

# ERROR cc9b81fc-2307 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:265`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `condition: any,`

# ERROR cc9b81fc-2308 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:270`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `conditionOrOp?: any | ((b: FormBuilder) => FormBuilder),`

# ERROR cc9b81fc-2309 no-casts `packages/devtools/cli-util/src/util/form-builder.ts:319`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return eachImpl(builder, items, fn!);`

# ERROR cc9b81fc-2310 no-casts `packages/devtools/cli-util/src/util/space-format.ts:162`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `epoch: any;`

# ERROR cc9b81fc-2311 no-casts `packages/devtools/cli/src/bin.ts:44`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `let leaksTracker: any;`

# ERROR cc9b81fc-2312 no-casts `packages/devtools/cli/src/commands/chat/components/Chat.tsx:144`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2313 no-casts `packages/devtools/cli/src/commands/chat/components/Markdown.tsx:108`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const children = getChildren(props.node.parent!);`

# ERROR cc9b81fc-2314 no-casts `packages/devtools/cli/src/commands/chat/components/Markdown.tsx:261`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const createJsonBlock = (content: any) => {`

# ERROR cc9b81fc-2315 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:42`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const typename = Obj.getTypename(obj as any);`

# ERROR cc9b81fc-2316 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:46`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const id = (obj as any).id as string | undefined;`

# ERROR cc9b81fc-2317 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:50`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const meta = (obj as any).__meta;`

# ERROR cc9b81fc-2318 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:90`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `.filter((obj: any) => Obj.getTypename(obj) === 'org.dxos.type.message')`

# ERROR cc9b81fc-2319 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:91`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `.filter((obj: any) => obj?.parentId === sessionFeedId || obj?.feed === sessionFeedId);`

# ERROR cc9b81fc-2320 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:143`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const live = (space.db as any).getObjectById?.(id);`

# ERROR cc9b81fc-2321 no-casts `packages/devtools/cli/src/commands/chat/non-interactive.ts:166`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `console.log(await renderText(space, (session as any).feed?.id ?? ''));`

# ERROR cc9b81fc-2322 no-casts `packages/devtools/cli/src/commands/function/deploy/echo.ts:130`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `() => functionObject.source!.load() as Promise<Script.Script>,`

# ERROR cc9b81fc-2323 no-casts `packages/devtools/cli/src/commands/function/deploy/echo.ts:132`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const source = (yield* Effect.tryPromise(() => script.source!.load())) as Text.Text;`

# ERROR cc9b81fc-2324 no-casts `packages/devtools/cli/src/commands/function/util.ts:47`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `status!,`

# ERROR cc9b81fc-2325 no-casts `packages/devtools/cli/src/commands/hub/user/list/list.ts:47`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return yield* Console.log((result as any).entries?.length + ' waitlist entries');`

# ERROR cc9b81fc-2326 no-casts `packages/devtools/cli/src/commands/hub/util.ts:60`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const envelope = result as unknown as EdgeEnvelope<T>;`

# ERROR cc9b81fc-2327 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:55`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `type Response = { id?: number; result?: any };`

# ERROR cc9b81fc-2328 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:119`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const initialize = responses.get(1)!.result;`

# ERROR cc9b81fc-2329 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:124`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller and a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, both of which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const tools: { name: string; inputSchema: any }[] = responses.get(2)!.result.tools;`

# ERROR cc9b81fc-2330 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:137`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const taskSet = tools.find((tool) => tool.name === 'taskCreate')!.inputSchema.properties.taskSet;`

# ERROR cc9b81fc-2331 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:141`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const prompts: { name: string }[] = responses.get(3)!.result.prompts;`

# ERROR cc9b81fc-2332 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:148`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const loaded = JSON.parse(responses.get(4)!.result.content[0].text);`

# ERROR cc9b81fc-2333 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:153`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const messages: { role: string; content: { type: string; text: string } }[] = responses.get(5)!.result.messages;`

# ERROR cc9b81fc-2334 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:162`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const failure = responses.get(6)!.result;`

# ERROR cc9b81fc-2335 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:171`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const names: string[] = responses.get(2)!.result.tools.map((tool: { name: string }) => tool.name);`

# ERROR cc9b81fc-2336 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:176`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const types = JSON.parse(responses.get(7)!.result.content[0].text).types;`

# ERROR cc9b81fc-2337 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:185`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `responses.get(2)!.result.tools;`

# ERROR cc9b81fc-2338 no-casts `packages/devtools/cli/src/commands/mcp/serve.test.ts:202`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const failure = responses.get(8)!.result;`

# ERROR cc9b81fc-2339 no-casts `packages/devtools/cli/src/commands/plugin-defs.ts:43`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `ObservabilityPlugin.make({} as any),`

# ERROR cc9b81fc-2340 no-casts `packages/devtools/cli/src/commands/reset.ts:29`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2341 no-casts `packages/devtools/cli/src/commands/reset.ts:44`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2342 no-casts `packages/devtools/cli/src/components/App.tsx:167`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `fallback={(err: any) => {`

# ERROR cc9b81fc-2343 no-casts `packages/devtools/cli/src/components/Table.tsx:31`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getId = props.getId ?? ((row: any) => row.id);`

# ERROR cc9b81fc-2344 no-casts `packages/devtools/cli/src/components/Table.tsx:143`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getId = props.getId ?? ((row: any) => row.id);`

# ERROR cc9b81fc-2345 no-casts `packages/devtools/cli/src/util/runtime.ts:65`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `invoke: (op: any, ...args: any[]) => {`

# ERROR cc9b81fc-2346 no-casts `packages/devtools/cli/src/util/runtime.ts:66`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handler = handlers.find((h: any) => h.meta.key === op.meta.key);`

# ERROR cc9b81fc-2347 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:130`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const wrapConsole = (variant: 'warn' | 'error', original: (...args: any[]) => void) => {`

# ERROR cc9b81fc-2348 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:131`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `return (...args: any[]) => {`

# ERROR cc9b81fc-2349 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:143`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(process.stderr as any).write = (chunk: any, ...rest: any[]): boolean => {`

# ERROR cc9b81fc-2350 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:146`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `typeof maybeCallback === 'function' ? (maybeCallback as any) : undefined;`

# ERROR cc9b81fc-2351 no-casts `packages/devtools/cli/src/util/stderr-filter.ts:158`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(process.stderr as any).write = originalWrite;`

# ERROR cc9b81fc-2352 no-casts `packages/devtools/devtools-extension/src/background.ts:20`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const messageListener = (message: any) => {`

# ERROR cc9b81fc-2353 no-casts `packages/devtools/devtools-extension/src/background.ts:40`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const messageListener = (message: any) => {`

# ERROR cc9b81fc-2354 no-casts `packages/devtools/devtools-extension/src/content.ts:12`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `port.onMessage.addListener((message: any) => {`

# ERROR cc9b81fc-2355 no-casts `packages/devtools/devtools-extension/src/panel.ts:39`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `port.onMessage.addListener((message: any) => {`

# ERROR cc9b81fc-2356 no-casts `packages/devtools/devtools-extension/src/sandbox.tsx:82`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const init = () => createRoot(document.getElementById('root')!).render(<App />);`

# ERROR cc9b81fc-2357 no-casts `packages/devtools/devtools/scripts/proxy.ts:30`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `socket.on('message', (data: any) => {`

# ERROR cc9b81fc-2358 no-casts `packages/devtools/devtools/scripts/proxy.ts:40`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `socket.on('message', (data: any) => {`

# ERROR cc9b81fc-2359 no-casts `packages/devtools/devtools/src/components/JsonView.tsx:22`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(key: any, value: any) => {`

# ERROR cc9b81fc-2360 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:17`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `data: Array<{ id: string; [key: string]: any }>;`

# ERROR cc9b81fc-2361 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:18`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `detailsTransform?: (data: any) => MaybePromise<any>;`

# ERROR cc9b81fc-2362 no-casts `packages/devtools/devtools/src/components/MasterDetailTable.tsx:35`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(row: any) => {`

# ERROR cc9b81fc-2363 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:12`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `object: any;`

# ERROR cc9b81fc-2364 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:31`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `stylesheet: any;`

# ERROR cc9b81fc-2365 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:32`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `useInlineStyles: any;`

# ERROR cc9b81fc-2366 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:43`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `onNavigate?.(URI.make((node.children![0].value as string).slice(1, -1)));`

# ERROR cc9b81fc-2367 no-casts `packages/devtools/devtools/src/components/ObjectViewer.tsx:95`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `properties?: { className: any[]; [key: string]: any };`

# ERROR cc9b81fc-2368 no-casts `packages/devtools/devtools/src/components/performance/panels/RawQueriesPanel.tsx:26`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `<td className='p-1 w-[80px] text-right'>{queries.get(key)!.toLocaleString()}</td>`

# ERROR cc9b81fc-2369 no-casts `packages/devtools/devtools/src/components/PropertiesTable.tsx:20`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `format: (value: any) => ReactNode;`

# ERROR cc9b81fc-2370 no-casts `packages/devtools/devtools/src/components/PropertiesTable.tsx:94`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const format = (key: string, value: any) => {`

# ERROR cc9b81fc-2371 no-casts `packages/devtools/devtools/src/components/Tree.tsx:18`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const Node = ({ data }: ThemedClassName<{ data?: any; root?: boolean }>) => {`

# ERROR cc9b81fc-2372 no-casts `packages/devtools/devtools/src/components/Tree.tsx:42`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const KeyValue = ({ classNames, label, data }: ThemedClassName<{ label: string; data?: any }>) => {`

# ERROR cc9b81fc-2373 no-casts `packages/devtools/devtools/src/components/Tree.tsx:70`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const Scalar = ({ classNames, value }: ThemedClassName<{ value: any }>) => {`

# ERROR cc9b81fc-2374 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:37`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `filter: any;`

# ERROR cc9b81fc-2375 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:38`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `metrics: any;`

# ERROR cc9b81fc-2376 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:116`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const memory: MemoryInfo = (window.performance as any).memory;`

# ERROR cc9b81fc-2377 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:169`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const stream = client.services.services.NetworkService!.queryStatus();`

# ERROR cc9b81fc-2378 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:184`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const stream = client.services.services.EdgeAgentService!.queryEdgeStatus();`

# ERROR cc9b81fc-2379 no-casts `packages/devtools/devtools/src/hooks/useStats.ts:212`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const removeEmpty = (obj: any): any => {`

# ERROR cc9b81fc-2380 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:21`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2381 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:28`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({});`

# ERROR cc9b81fc-2382 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:32`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({ record });`

# ERROR cc9b81fc-2383 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:36`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const { recording = false } = await client.services.services.LoggingService!.controlMetrics({ reset: true });`

# ERROR cc9b81fc-2384 no-casts `packages/devtools/devtools/src/panels/client/DiagnosticsPanel/DiagnosticsPanel.tsx:50`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `if ((window as any).chrome) {`

# ERROR cc9b81fc-2385 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:17`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const hasMatchRecursive = (object: any, pattern: RegExp): boolean => {`

# ERROR cc9b81fc-2386 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:43`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getStoredObject = (doc: DatabaseDirectory | undefined): any => {`

# ERROR cc9b81fc-2387 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:87`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `id: handle.documentId!,`

# ERROR cc9b81fc-2388 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:88`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `documentId: handle.documentId!,`

# ERROR cc9b81fc-2389 no-casts `packages/devtools/devtools/src/panels/echo/AutomergePanel/AutomergePanel.tsx:90`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `type: getStoredObjectType({ documentId: handle.documentId!, accessor: () => doc, id: handle.documentId! }),`

# ERROR cc9b81fc-2390 no-casts `packages/devtools/devtools/src/panels/echo/FeedsPanel/FeedsPanel.tsx:135`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const issuerKeys = credential ? { identity: credential.issuer, device: credential.proof!.signer } : undefined;`

# ERROR cc9b81fc-2391 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:30`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `match ||= !!String((item as any).title ?? '').match(matcher);`

# ERROR cc9b81fc-2392 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:115`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `version: Obj.getType(item) ? Type.getVersion(Obj.getType(item)!) : undefined,`

# ERROR cc9b81fc-2393 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:122`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleObjectRowClicked = useCallback((row: any) => {`

# ERROR cc9b81fc-2394 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:155`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `setSelectedVersionObject(checkoutVersion(selected!, [version.hash]));`

# ERROR cc9b81fc-2395 no-casts `packages/devtools/devtools/src/panels/echo/ObjectsPanel/ObjectsPanel.tsx:161`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(row: any) => {`

# ERROR cc9b81fc-2396 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:25`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const objects: any[] = [];`

# ERROR cc9b81fc-2397 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:38`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `return objects.map((item: any) => ({`

# ERROR cc9b81fc-2398 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:45`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleRowClicked = (row: any) => {`

# ERROR cc9b81fc-2399 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:83`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `object: any;`

# ERROR cc9b81fc-2400 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:93`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `stylesheet: any;`

# ERROR cc9b81fc-2401 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:94`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `useInlineStyles: any;`

# ERROR cc9b81fc-2402 no-casts `packages/devtools/devtools/src/panels/echo/QueuesPanel/QueuesPanel.tsx:114`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `properties?: { className: any[]; [key: string]: any };`

# ERROR cc9b81fc-2403 no-casts `packages/devtools/devtools/src/panels/echo/SchemaPanel/SchemaPanel.tsx:117`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleObjectRowClicked = useCallback((row: any) => {`

# ERROR cc9b81fc-2404 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/FeedTable.tsx:56`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleRowClick = (row: any) => {`

# ERROR cc9b81fc-2405 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:39`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const state = space!.state.get();`

# ERROR cc9b81fc-2406 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:41`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await space!.open();`

# ERROR cc9b81fc-2407 no-casts `packages/devtools/devtools/src/panels/echo/SpaceInfoPanel/SpaceInfoPanel.tsx:43`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await space!.close();`

# ERROR cc9b81fc-2408 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:68`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(row: any) => {`

# ERROR cc9b81fc-2409 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:83`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const space = spaces.find((space) => space.id === spaceId)!;`

# ERROR cc9b81fc-2410 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:95`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const space = spaces.find((space) => space.id === spaceId)!;`

# ERROR cc9b81fc-2411 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:105`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const space = spaces.find((space) => space.id === spaceId)!;`

# ERROR cc9b81fc-2412 no-casts `packages/devtools/devtools/src/panels/echo/SpaceListPanel/SpaceListPanel.tsx:158`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleRowAction = (actionId: string, item: any) => {`

# ERROR cc9b81fc-2413 no-casts `packages/devtools/devtools/src/panels/edge/EdgeDashboardPanel/EdgeDashboardPanel.tsx:29`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const formatData = (data: any) =>`

# ERROR cc9b81fc-2414 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/ExceptionPanel.tsx:48`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const context = log.context as any;`

# ERROR cc9b81fc-2415 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/InvocationTraceContainer.tsx:145`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const handleRowClick = useCallback((row: any) => {`

# ERROR cc9b81fc-2416 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/LogPanel.tsx:62`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const safeStringify = (value: any) => {`

# ERROR cc9b81fc-2417 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:34`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `properties?: { className: any[]; [key: string]: any };`

# ERROR cc9b81fc-2418 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:35`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `children?: any[];`

# ERROR cc9b81fc-2419 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:37`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `stylesheet: any;`

# ERROR cc9b81fc-2420 no-casts `packages/devtools/devtools/src/panels/edge/InvocationTracePanel/RawDataPanel.tsx:38`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `useInlineStyles: any;`

# ERROR cc9b81fc-2421 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:104`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `data?: any;`

# ERROR cc9b81fc-2422 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:129`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `let response: any;`

# ERROR cc9b81fc-2423 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:147`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2424 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowDebugPanel.tsx:279`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `data?: any;`

# ERROR cc9b81fc-2425 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:52`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `return async (data: any) => {`

# ERROR cc9b81fc-2426 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:67`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2427 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:115`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2428 no-casts `packages/devtools/devtools/src/panels/edge/WorkflowPanel/WorkflowPanel.tsx:122`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const result: any = {};`

# ERROR cc9b81fc-2429 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:66`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `radius: (node: GraphLayoutNode<NetworkGraphNode>) => (isMe(node.data!) ? 24 : 16),`

# ERROR cc9b81fc-2430 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:95`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `node.data!.member?.identity.profile?.displayName ??`

# ERROR cc9b81fc-2431 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:96`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `node.data!.member?.identity.identityKey.truncate();`

# ERROR cc9b81fc-2432 no-casts `packages/devtools/devtools/src/panels/mesh/NetworkPanel/NetworkPanel.tsx:98`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const peer = node.data!.peer?.peerId?.truncate();`

# ERROR cc9b81fc-2433 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:66`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `(response.swarmEvent!.peerAvailable && PublicKey.from(response.swarmEvent!.peerAvailable.peer).toString()) ||`

# ERROR cc9b81fc-2434 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:67`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `(response.swarmEvent!.peerLeft && PublicKey.from(response.swarmEvent!.peerLeft.peer).toString()),`

# ERROR cc9b81fc-2435 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:68`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `since: response.swarmEvent!.peerAvailable?.since ?? new Date(),`

# ERROR cc9b81fc-2436 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:94`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `author: PublicKey.from(response.message!.author).toString(),`

# ERROR cc9b81fc-2437 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:95`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `recipient: PublicKey.from(response.message!.recipient).toString(),`

# ERROR cc9b81fc-2438 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:96`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `message: response.message!.payload.messageId,`

# ERROR cc9b81fc-2439 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:97`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `topic: response.message!.payload?.payload?.topic,`

# ERROR cc9b81fc-2440 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:121`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `author: PublicKey.from(response.message!.author).toString(),`

# ERROR cc9b81fc-2441 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:122`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `recipient: PublicKey.from(response.message!.recipient).toString(),`

# ERROR cc9b81fc-2442 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:123`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `message: response.message!.payload.messageId,`

# ERROR cc9b81fc-2443 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalMessageTable.tsx:130`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const getView = (id: ViewType): View<SignalResponse> => views.find((type) => type.id === id)!;`

# ERROR cc9b81fc-2444 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:23`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `connectionStarted: server.connectionStarted!,`

# ERROR cc9b81fc-2445 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:24`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `lastStateChange: server.lastStateChange!,`

# ERROR cc9b81fc-2446 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:25`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `reconnectIn: server.reconnectIn!,`

# ERROR cc9b81fc-2447 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:26`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `host: server.host!,`

# ERROR cc9b81fc-2448 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:27`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `state: server.state!,`

# ERROR cc9b81fc-2449 no-casts `packages/devtools/devtools/src/panels/mesh/SignalPanel/SignalStatusTable.tsx:83`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const status = servers!.map(getSignalStatus);`

# ERROR cc9b81fc-2450 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:59`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `preprocessor?: (entry: any) => SerializedLogEntry;`

# ERROR cc9b81fc-2451 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:63`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `private _logs: any[] = [];`

# ERROR cc9b81fc-2452 no-casts `packages/e2e/blade-runner/src/analysys/logging.ts:110`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `export const zapPreprocessor = (entry: any): SerializedLogEntry => {`

# ERROR cc9b81fc-2453 no-casts `packages/e2e/blade-runner/src/analysys/plot.ts:17`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const image = await chartJSNodeCanvas.renderToBuffer(configuration as any);`

# ERROR cc9b81fc-2454 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:17`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const decodeVal = (val: any): any => {`

# ERROR cc9b81fc-2455 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:26`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const decoded: any = decodeVal(val);`

# ERROR cc9b81fc-2456 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:52`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `messages.get(data.message)!.sent = entry.timestamp;`

# ERROR cc9b81fc-2457 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:55`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `messages.get(data.message)!.received = entry.timestamp;`

# ERROR cc9b81fc-2458 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:63`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `.map((message) => message.received! - message.sent!);`

# ERROR cc9b81fc-2459 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:220`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const discoverStats = timings.seen.get(expectedPeer)!;`

# ERROR cc9b81fc-2460 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:221`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `discoverLag.push(discoverStats.discover! - expectedPeerTimings.join);`

# ERROR cc9b81fc-2461 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:224`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `notifyLag.push(discoverStats.notify! - expectedPeerTimings.join!);`

# ERROR cc9b81fc-2462 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:286`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `peers.get(data.peerId)!.iterations++;`

# ERROR cc9b81fc-2463 no-casts `packages/e2e/blade-runner/src/analysys/stats.ts:291`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `peers.get(data.peerId)!.errors++;`

# ERROR cc9b81fc-2464 no-casts `packages/e2e/blade-runner/src/analysys/traces.ts:51`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._traces[event.name].duration = event.ts - this._traces[event.name].begin!.ts;`

# ERROR cc9b81fc-2465 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:70`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `setReplicant(replicant: any): void {`

# ERROR cc9b81fc-2466 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:129`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const result = dataValues.map((value) => JSON.parse(value!));`

# ERROR cc9b81fc-2467 no-casts `packages/e2e/blade-runner/src/env/replicant-env.ts:138`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (parseInt(value!) === count) {`

# ERROR cc9b81fc-2468 no-casts `packages/e2e/blade-runner/src/env/replicant-registry.ts:21`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return this._replicantClasses.get(name)!;`

# ERROR cc9b81fc-2469 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-handle.ts:37`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `value: async (...args: any[]) => {`

# ERROR cc9b81fc-2470 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-handle.ts:54`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getAllProperties = (obj: any) => {`

# ERROR cc9b81fc-2471 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-server.ts:14`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `handler: any;`

# ERROR cc9b81fc-2472 no-casts `packages/e2e/blade-runner/src/env/replicant-rpc-server.ts:30`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const response = await (handler as any)[method](...args);`

# ERROR cc9b81fc-2473 no-casts `packages/e2e/blade-runner/src/env/scheduler-env.ts:142`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const result = dataValues.map((value) => JSON.parse(value!));`

# ERROR cc9b81fc-2474 no-casts `packages/e2e/blade-runner/src/env/scheduler-env.ts:154`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (parseInt(value!) === count) {`

# ERROR cc9b81fc-2475 no-casts `packages/e2e/blade-runner/src/main.ts:54`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const params: RunProps = JSON.parse(process.env.DX_RUN_PARAMS!);`

# ERROR cc9b81fc-2476 no-casts `packages/e2e/blade-runner/src/main.ts:60`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `if ((globalThis as any).DX_RUN_PARAMS) {`

# ERROR cc9b81fc-2477 no-casts `packages/e2e/blade-runner/src/main.ts:62`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const params = (globalThis as any).DX_RUN_PARAMS;`

# ERROR cc9b81fc-2478 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:25`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `spec: any;`

# ERROR cc9b81fc-2479 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:26`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `stats: any;`

# ERROR cc9b81fc-2480 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:27`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `results: any;`

# ERROR cc9b81fc-2481 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:41`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `if (typeof (globalThis as any).dxgravity_env !== 'undefined') {`

# ERROR cc9b81fc-2482 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:42`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `process.env = (globalThis as any).dxgravity_env;`

# ERROR cc9b81fc-2483 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:106`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `let result: any;`

# ERROR cc9b81fc-2484 no-casts `packages/e2e/blade-runner/src/plan/run-plan.ts:131`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `let stats: any;`

# ERROR cc9b81fc-2485 no-casts `packages/e2e/blade-runner/src/plan/run-replicant.ts:51`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(window as any).dx_runner_log?.(config, entry);`

# ERROR cc9b81fc-2486 no-casts `packages/e2e/blade-runner/src/plan/run-replicant.ts:62`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(window as any).dx_runner_done?.(code);`

# ERROR cc9b81fc-2487 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:126`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `bobReplicator.context!.peerId,`

# ERROR cc9b81fc-2488 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:127`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `aliceReplicator.context!.peerId,`

# ERROR cc9b81fc-2489 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:129`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `aliceReplicator.context!.onConnectionOpen(aliceConnection);`

# ERROR cc9b81fc-2490 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:130`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `bobReplicator.context!.onConnectionOpen(bobConnection);`

# ERROR cc9b81fc-2491 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:133`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `aliceReplicator.context!.onConnectionClosed(aliceConnection!);`

# ERROR cc9b81fc-2492 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:134`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `bobReplicator.context!.onConnectionClosed(bobConnection!);`

# ERROR cc9b81fc-2493 no-casts `packages/e2e/blade-runner/src/redis/redis.node.test.ts:146`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await using db2 = await bob.openDatabase(spaceKey, db1.rootUrl!);`

# ERROR cc9b81fc-2494 no-casts `packages/e2e/blade-runner/src/redis/util.ts:91`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `encode: (value: any): Any => ({`

# ERROR cc9b81fc-2495 no-casts `packages/e2e/blade-runner/src/redis/util.ts:95`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `decode: (value: Any): any => JSON.parse(Buffer.from(value.value).toString()),`

# ERROR cc9b81fc-2496 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.node.test.ts:67`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `} as unknown as ReplicantProps,`

# ERROR cc9b81fc-2497 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.ts:19`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `super(options.disconnectTimeout!);`

# ERROR cc9b81fc-2498 no-casts `packages/e2e/blade-runner/src/redis/websocket-connector.ts:23`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const stream: any = WebSocketStream(this.options.address);`

# ERROR cc9b81fc-2499 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:82`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._repo!.create<DocStruct>({ text: random.string.hexadecimal({ length: mutationSize }) }),`

# ERROR cc9b81fc-2500 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:100`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._repo!.flush();`

# ERROR cc9b81fc-2501 no-casts `packages/e2e/blade-runner/src/replicants/automerge-replicant.ts:116`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const handle = await this._repo!.find<DocStruct>(id);`

# ERROR cc9b81fc-2502 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:62`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._db = await this._testPeer!.createDatabase(PublicKey.fromHex(spaceKey));`

# ERROR cc9b81fc-2503 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:68`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `rootUrl: this._db.rootUrl!,`

# ERROR cc9b81fc-2504 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:74`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._db = await this._testPeer!.openDatabase(PublicKey.fromHex(spaceKey), rootUrl);`

# ERROR cc9b81fc-2505 no-casts `packages/e2e/blade-runner/src/replicants/echo-replicant.ts:101`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._db!.add(doc);`

# ERROR cc9b81fc-2506 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:55`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._client.services.services.QueryService!.setConfig(indexing, { timeout: 20_000 });`

# ERROR cc9b81fc-2507 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:88`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it and a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, both of which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._client.services.services.EdgeAgentService!.createAgent(null as any, { timeout: 10_000 });`

# ERROR cc9b81fc-2508 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:111`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const response = await this._client!.edge.createSpace({ agentKey });`

# ERROR cc9b81fc-2509 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:116`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `condition: () => this._client!.spaces.get(response.spaceId as SpaceId),`

# ERROR cc9b81fc-2510 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:136`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `client: this._client!,`

# ERROR cc9b81fc-2511 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:137`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `ownerUri: this._identity!.did,`

# ERROR cc9b81fc-2512 no-casts `packages/e2e/blade-runner/src/replicants/edge-replicant.ts:173`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const space = this._client!.spaces.get(spaceId);`

# ERROR cc9b81fc-2513 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:96`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `for (const topic of topics!) {`

# ERROR cc9b81fc-2514 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:100`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await sleep(discoverTimeout!);`

# ERROR cc9b81fc-2515 no-casts `packages/e2e/blade-runner/src/replicants/signal-replicant.ts:102`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await Promise.all(topics!.map((topic) => cancelWithContext(context, peer.leaveTopic(PublicKey.from(topic)))));`

# ERROR cc9b81fc-2516 no-casts `packages/e2e/blade-runner/src/replicants/storage-replicant.ts:101`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `filesToSave.find((f) => f.key.join() === file.key.join())!.data.equals(Buffer.from(file.data!)),`

# ERROR cc9b81fc-2517 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:60`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._edgeConnection!.send(`

# ERROR cc9b81fc-2518 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:75`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._edgeConnection!.send(`

# ERROR cc9b81fc-2519 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:109`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const payload = JSON.parse(Buffer.from(message.payload!.value).toString());`

# ERROR cc9b81fc-2520 no-casts `packages/e2e/blade-runner/src/replicants/ws-replicant.ts:126`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._edgeConnection!.send(message);`

# ERROR cc9b81fc-2521 no-casts `packages/e2e/blade-runner/src/run-test-signal.ts:23`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `_onError?: (err: any) => void,`

# ERROR cc9b81fc-2522 no-casts `packages/e2e/blade-runner/src/spec/edge-sync.ts:96`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `configEnabledEdgeSync.runtime!.client!.edgeFeatures!.subductionReplicator = true;`

# ERROR cc9b81fc-2523 no-casts `packages/e2e/blade-runner/src/spec/edge-sync.ts:99`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `configDisabledEdgeSync.runtime!.client!.edgeFeatures!.subductionReplicator = false;`

# ERROR cc9b81fc-2524 no-casts `packages/e2e/blade-runner/src/spec/transport.ts:312`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `concurrentInits![idx]++;`

# ERROR cc9b81fc-2525 no-casts `packages/e2e/blade-runner/src/test-builder.ts:46`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `onError?: (err: any) => void,`

# ERROR cc9b81fc-2526 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:17`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `[data: string]: any;`

# ERROR cc9b81fc-2527 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:23`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `args?: any;`

# ERROR cc9b81fc-2528 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:24`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `[data: string]: any;`

# ERROR cc9b81fc-2529 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:137`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._controller!.enqueue(JSON.stringify(event));`

# ERROR cc9b81fc-2530 no-casts `packages/e2e/blade-runner/src/tracing/perfetto-events.ts:142`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._controller!.close();`

# ERROR cc9b81fc-2531 no-casts `packages/e2e/proto-guard/src/generate-snapshot.ts:79`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `Obj.update(object2, (object2: any) => {`

# ERROR cc9b81fc-2532 no-casts `packages/e2e/proto-guard/src/snapshots-registry.ts:51`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `Object.keys(snapshotToRemove).every((key) => (snapshot as any)[key] !== (snapshotToRemove as any)[key]),`

# ERROR cc9b81fc-2533 no-casts `packages/experimental/discord-bot/src/bot.ts:89`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2534 no-casts `packages/experimental/discord-worker/src/register-commands.ts:60`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `console.log(`Registered ${(result as any[]).length} commands.`);`

# ERROR cc9b81fc-2535 no-casts `packages/plugins/plugin-assistant/src/components/ChatPrompt/ChatOptions.stories.tsx:72`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `component: ChatOptions as any,`

# ERROR cc9b81fc-2536 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/registry.tsx:87`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `?.map((option: any) => option._tag === 'option' && getXmlTextChild(option.children))`

# ERROR cc9b81fc-2537 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:43`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `updateWidget(_id: string, _value: any) {}`

# ERROR cc9b81fc-2538 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:299`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const updates: { id: string; value: any }[] = [];`

# ERROR cc9b81fc-2539 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/sync/sync.test.ts:301`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `override updateWidget(id: string, value: any) {`

# ERROR cc9b81fc-2540 no-casts `packages/plugins/plugin-assistant/src/components/ChatThread/widgets/ToolWidget.tsx:105`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `type ToolPanelItem = { title: string; icon?: string; content: any };`

# ERROR cc9b81fc-2541 no-casts `packages/plugins/plugin-assistant/src/components/Toolbox/Toolbox.stories.tsx:28`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `component: Toolbox as any,`

# ERROR cc9b81fc-2542 no-casts `packages/plugins/plugin-assistant/src/containers/AgentArticle/AgentArticle.stories.tsx:83`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const factory = createObjectFactory(space.db, random as any);`

# ERROR cc9b81fc-2543 no-casts `packages/plugins/plugin-assistant/src/containers/TracePanel/TracePanel.stories.tsx:135`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const subAgentMessages = subAgentFixture as unknown as Trace.Message[];`

# ERROR cc9b81fc-2544 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:839`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const event = span.events[eventIndex]!;`

# ERROR cc9b81fc-2545 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:850`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const event = span.events[eventIndex]!;`

# ERROR cc9b81fc-2546 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:885`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `pipe(...args: any) {`

# ERROR cc9b81fc-2547 no-casts `packages/plugins/plugin-assistant/src/execution-graph/execution-graph.ts:1037`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const commit = this.#commits[index]!;`

# ERROR cc9b81fc-2548 no-casts `packages/plugins/plugin-assistant/src/execution-graph/pending-block-status.test.ts:114`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `({ meta: { pid: 'pid-1' }, isEphemeral: true, events }) as unknown as Trace.Message;`

# ERROR cc9b81fc-2549 no-casts `packages/plugins/plugin-assistant/src/execution-graph/remote-snapshot.test.ts:69`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const parentIndex = idToMainIndex.get(parentId)!;`

# ERROR cc9b81fc-2550 no-casts `packages/plugins/plugin-assistant/src/execution-graph/sub-agent-delegation.test.ts:15`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const messages = (subAgentFixture as unknown as Trace.Message[])`

# ERROR cc9b81fc-2551 no-casts `packages/plugins/plugin-assistant/src/processor/processor.node.test.ts:36`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const managedRuntime = ManagedRuntime.make(Layer.empty) as unknown as Capabilities.ProcessManagerRuntime;`

# ERROR cc9b81fc-2552 no-casts `packages/plugins/plugin-assistant/src/processor/processor.node.test.ts:37`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const processor = new AiChatProcessor(session, managedRuntime, feed, Layer.empty as any);`

# WARN cc9b81fc-2553 no-compat-shims `packages/plugins/plugin-assistant/src/processor/processor.ts:51:1`

`AiChatServices` is exported with a doc comment reading "@deprecated Services type for the old direct-conversation processor path. Retained for backward compatibility with CLI and update-name" — the classic compat-shim-left-behind pattern the `no-compat-shims` rule targets. Worse, it is stale: nothing in this package or in `packages/devtools/cli` actually imports this type any more — the CLI call sites (`packages/devtools/cli/src/util/runtime.ts`, `trigger-runtime.ts`, `commands/chat/processor.ts`, `commands/chat/chat.tsx`) all reference an independently-defined, differently-shaped local `AiChatServices` in `@dxos/cli`'s own `util/runtime.ts`, not this one. So the comment's stated justification for keeping it no longer holds — the "old direct-conversation processor path" call sites were migrated without updating (or removing) this compatibility export in the same change. Delete the unused `AiChatServices` export here, or if some external consumer still needs it, update that call site to the current type and remove this one.

# ERROR cc9b81fc-2554 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:26`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `return 'recording' as any;`

# ERROR cc9b81fc-2555 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:28`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `add: (obj: any) => obj as any,`

# ERROR cc9b81fc-2556 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:29`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `addType: async () => undefined as any,`

# ERROR cc9b81fc-2557 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:38`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `queryFeed: () => ({ subscribe: () => () => {}, results: [], run: async () => [] }) as any,`

# ERROR cc9b81fc-2558 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:39`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `query: () => ({ subscribe: () => () => {}, results: [], run: async () => [] }) as any,`

# ERROR cc9b81fc-2559 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:43`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `makeRef: () => undefined as any,`

# ERROR cc9b81fc-2560 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:45`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `} as unknown as Database.Database;`

# ERROR cc9b81fc-2561 no-casts `packages/plugins/plugin-assistant/src/testing/test-generator.test.ts:66`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `createMessageGenerator()[2]!.pipe(`

# ERROR cc9b81fc-2562 no-casts `packages/plugins/plugin-assistant/src/testing/test-services.ts:9`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `const ServiceTypeSchema = AssistantService.ServiceType as unknown as Type.Obj<AssistantService.ServiceType>;`

# ERROR cc9b81fc-2563 no-casts `packages/plugins/plugin-attention/src/capabilities/attention.ts:27`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(globalThis as any).composer ??= {};`

# ERROR cc9b81fc-2564 no-casts `packages/plugins/plugin-attention/src/capabilities/attention.ts:29`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `(globalThis as any).composer.attention = {`

# ERROR cc9b81fc-2565 no-casts `packages/plugins/plugin-bookmarks/src/operations/summarize.ts:76`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any),`

# ERROR cc9b81fc-2566 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:58`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any),`

# ERROR cc9b81fc-2567 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:78`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feed = mailbox.feed.target!;`

# ERROR cc9b81fc-2568 no-casts `packages/plugins/plugin-brain/src/operations/generate-reply.test.ts:110`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feed = mailbox.feed.target!;`

# ERROR cc9b81fc-2569 no-casts `packages/plugins/plugin-brain/src/operations/operations.test.ts:62`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any),`

# ERROR cc9b81fc-2570 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:79`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`

# ERROR cc9b81fc-2571 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:80`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(routine!.spec?.kind).toBe('runnable');`

# ERROR cc9b81fc-2572 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:81`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `expect(routine!.spec?.kind === 'runnable' && routine!.spec.runnable.uri.toString()).toBe(`

# ERROR cc9b81fc-2573 no-casts `packages/plugins/plugin-brain/src/templates/mailbox-facts.test.ts:86`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const trigger = await routine!.triggers[0].tryLoad();`

# ERROR cc9b81fc-2574 no-casts `packages/plugins/plugin-calls/src/calls/call-manager.ts:272`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `?.filter((user) => user.joined && user.id !== state.self!.id)`

# ERROR cc9b81fc-2575 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:127`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.activities![key] = { lamportTimestamp, payload };`

# ERROR cc9b81fc-2576 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:268`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._reconcileSwarmStateTask!.schedule();`

# ERROR cc9b81fc-2577 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:288`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return codec.decode(peer.state!);`

# ERROR cc9b81fc-2578 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:308`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `lastActivity.lamportTimestamp!.version! > (this._state.activities![key]?.lamportTimestamp?.version ?? 0)`

# ERROR cc9b81fc-2579 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:310`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.activities![key] = lastActivity;`

# ERROR cc9b81fc-2580 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:319`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `async () => this._reconcileSwarmStateTask!.schedule(),`

# ERROR cc9b81fc-2581 no-casts `packages/plugins/plugin-calls/src/calls/call-swarm-synchronizer.ts:337`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `.sort((a, b) => a.lamportTimestamp!.id!.localeCompare(b.lamportTimestamp!.id!));`

# ERROR cc9b81fc-2582 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:130`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.videoStream!.addTrack(this._state.videoTrack);`

# ERROR cc9b81fc-2583 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:163`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await this._state.peer!.open();`

# ERROR cc9b81fc-2584 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:164`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2585 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:185`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.videoStream!.removeTrack(this._state.videoTrack);`

# ERROR cc9b81fc-2586 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:188`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.videoStream!.addTrack(this._state.videoTrack);`

# ERROR cc9b81fc-2587 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:192`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2588 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:198`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.videoTrack && this._state.videoStream!.removeTrack(this._state.videoTrack);`

# ERROR cc9b81fc-2589 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:201`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._state.videoTrack && this._state.videoStream!.addTrack(this._state.videoTrack);`

# ERROR cc9b81fc-2590 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:206`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2591 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:220`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2592 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:221`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._speakingMonitor = new SpeakingMonitor(this._state.audioTrack!);`

# ERROR cc9b81fc-2593 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:239`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2594 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:248`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2595 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:256`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pushTracksTask!.schedule();`

# ERROR cc9b81fc-2596 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:271`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `this._pullTracksTask!.schedule();`

# ERROR cc9b81fc-2597 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:367`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const track = await this._state.peer!.pullTrack({ trackData: { ...trackData, mid: undefined }, ctx });`

# ERROR cc9b81fc-2598 no-casts `packages/plugins/plugin-calls/src/calls/media-manager.ts:411`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `track: await this._state.peer!.pushTrack({`

# ERROR cc9b81fc-2599 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:39`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `body: any;`

# ERROR cc9b81fc-2600 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:47`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `json?: any;`

# ERROR cc9b81fc-2601 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:311`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const transceiver = this.session.peerConnection!.addTransceiver(track, { direction: 'sendonly' });`

# ERROR cc9b81fc-2602 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:316`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `peerConnection: this.session.peerConnection!,`

# ERROR cc9b81fc-2603 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:318`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `sessionId: this.session.sessionId!,`

# ERROR cc9b81fc-2604 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:366`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `acc.set(track.trackName!, {`

# ERROR cc9b81fc-2605 no-casts `packages/plugins/plugin-calls/src/calls/util/calls-service.ts:404`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const trackInfo = trackMap.get(trackData.trackName!);`

# ERROR cc9b81fc-2606 no-casts `packages/plugins/plugin-calls/src/calls/util/task-scheduling.ts:21`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (error: any) {`

# ERROR cc9b81fc-2607 no-casts `packages/plugins/plugin-calls/src/components/Participant/ParticipantGrid.tsx:13`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const getId = (user: UserState): string => user.id!;`

# ERROR cc9b81fc-2608 no-casts `packages/plugins/plugin-calls/src/components/Participant/ParticipantGrid.tsx:41`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any, // TODO(burdon): Remove cast.`

# ERROR cc9b81fc-2609 no-casts `packages/plugins/plugin-calls/src/components/ResponsiveGrid/ResponsiveGrid.stories.tsx:93`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `component: ResponsiveGrid as any,`

# ERROR cc9b81fc-2610 no-casts `packages/plugins/plugin-calls/src/components/ResponsiveGrid/ResponsiveGrid.tsx:54`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const defaultGetId: ResponsiveGridProps<any>['getId'] = (item: any) => item.id;`

# ERROR cc9b81fc-2611 no-casts `packages/plugins/plugin-calls/src/containers/CallDebugPanel/CallDebugPanel.tsx:128`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `.filter((user) => user.id !== self!.id)`

# ERROR cc9b81fc-2612 no-casts `packages/plugins/plugin-calls/src/containers/CallDebugPanel/CallDebugPanel.tsx:151`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `'*self* ' + (self?.name ?? truncateKey(self!.id, 8)),`

# ERROR cc9b81fc-2613 no-casts `packages/plugins/plugin-calls/src/hooks/useVideoStreamTrack.tsx:21`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const stream = (videoElement.current as any).captureStream();`

# ERROR cc9b81fc-2614 no-casts `packages/plugins/plugin-chess-com/src/operations/sync-games.test.ts:106`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const variant = yield* Database.load(gamesAfterFirst[0]!.variant);`

# ERROR cc9b81fc-2615 no-casts `packages/plugins/plugin-client/src/capabilities/migrations.ts:23`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `(_migrations: any[]) => {`

# ERROR cc9b81fc-2616 no-casts `packages/plugins/plugin-client/src/capabilities/migrations.ts:33`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `void Promise.all(spaces.map((space: any) => space.internal.db.runMigrations(migrations))).catch((err) => {`

# ERROR cc9b81fc-2617 no-casts `packages/plugins/plugin-client/src/commands/halo/create/create.test.ts:22`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `const layer = Layer.merge(TestLayer, fromPlugins([ClientPlugin({}), ObservabilityPlugin.make({} as any)]));`

# ERROR cc9b81fc-2618 no-casts `packages/plugins/plugin-client/src/commands/halo/util.ts:34`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `} else if (TERMINAL_FAILURE_STATES.has(inv.state!)) {`

# ERROR cc9b81fc-2619 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:50`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `storageConfig = config.values.runtime!.client!.storage!;`

# ERROR cc9b81fc-2620 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:60`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `if (yield* fs.exists(storageConfig.dataRoot!)) {`

# ERROR cc9b81fc-2621 no-casts `packages/plugins/plugin-client/src/commands/profile/import.ts:61`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `log.error('data directory already exists', { path: storageConfig.dataRoot! });`

# ERROR cc9b81fc-2622 no-casts `packages/plugins/plugin-client/src/containers/AccountContainer/AccountContainer.tsx:48`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `} catch (err: any) {`

# ERROR cc9b81fc-2623 no-casts `packages/plugins/plugin-client/src/containers/RecoveryCodeDialog/RecoveryCodeDialog.stories.tsx:36`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `component: RecoveryCodeDialog as any,`

# ERROR cc9b81fc-2624 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:60`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any,`

# ERROR cc9b81fc-2625 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:66`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any,`

# ERROR cc9b81fc-2626 no-casts `packages/plugins/plugin-client/src/containers/ResetDialog/ResetDialog.stories.tsx:72`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `} as any,`

# ERROR cc9b81fc-2627 no-casts `packages/plugins/plugin-client/src/operations/create-agent.ts:21`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `client.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 10_000 }),`

# ERROR cc9b81fc-2628 no-casts `packages/plugins/plugin-client/src/operations/create-passkey.ts:86`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `recoveryKey: PublicKey.from(new Uint8Array((credential as any).response.getPublicKey())),`

# ERROR cc9b81fc-2629 no-casts `packages/plugins/plugin-client/src/operations/create-passkey.ts:87`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `algorithm: (credential as any).response.getPublicKeyAlgorithm() === -7 ? 'ES256' : 'ED25519',`

# ERROR cc9b81fc-2630 no-casts `packages/plugins/plugin-code/src/components/FileTree/FileTree.tsx:111`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `{sortNodes(node.children!.values()).map((child) => (`

# ERROR cc9b81fc-2631 no-casts `packages/plugins/plugin-commerce/src/types/Search.test.ts:43`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const feed = search.feed!.target!;`

# WARN cc9b81fc-2632 no-sleep-in-test `packages/plugins/plugin-computer/src/vite-plugin/shell-middleware.test.ts:83`

`await new Promise((resolve) => setTimeout(resolve, 1_500))` is used to let a spawned process group have time to die before asserting it left no survivor file. A fixed real 1.5s sleep is exactly the pattern the rule prohibits; poll for the process's actual exit (or absence of the survivor file) with a bounded `waitForCondition` instead.

# ERROR cc9b81fc-2633 no-casts `packages/plugins/plugin-conductor/src/containers/CanvasArticle/CanvasArticle.tsx:74`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `graphMonitor={graphMonitor as any}`

# ERROR cc9b81fc-2634 no-casts `packages/plugins/plugin-conductor/src/containers/CanvasArticle/CanvasArticle.tsx:109`

This line contains an `as any` cast that forces this value past the type checker, hiding whatever real mismatch prompted it, which the no-casts rule (severity: error) forbids. Give the value a precise type — fix the upstream type, add a type guard, or model the shape properly — instead of casting to `any`. Line: `processManagerRuntime.runPromiseExit(effect.pipe(Effect.provide(spaceLayer)) as any) as Promise<`

# ERROR cc9b81fc-2635 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:148`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const actions: any[] = context.registry.get(context.graph.actions(qualifyId(Node.RootId, SUBJECT_ID)));`

# ERROR cc9b81fc-2636 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:156`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `lastManager!.contribute({ module: 'late', interface: ConnectorSpec.Connector, implementation: connectors });`

# ERROR cc9b81fc-2637 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:157`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `await lastContext!.expand(qualifyId(Node.RootId, SUBJECT_ID), 'action');`

# ERROR cc9b81fc-2638 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:158`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller and a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, both of which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `const actions: any[] = lastContext!.registry.get(lastContext!.graph.actions(qualifyId(Node.RootId, SUBJECT_ID)));`

# ERROR cc9b81fc-2639 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:163`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const getGroupChildIds = async (group: any): Promise<string[]> => {`

# ERROR cc9b81fc-2640 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:168`

This line contains a signature/binding typed `any` (a widened-any), silently disabling checking for every caller, which the no-casts rule (severity: error) forbids. Replace `any` with a precise type, a generic parameter, or `unknown` plus a narrowing guard. Line: `const children: any[] = lastContext.registry.get(lastContext.graph.actions(group.id));`

# ERROR cc9b81fc-2641 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-auth-actions.test.ts:169`

This line contains a `!` non-null assertion that asserts away a possibly-`null`/`undefined` value without proof, which the no-casts rule (severity: error) forbids. Replace it with a real null/undefined check, an `invariant()`, or narrow the type at its source so the assertion is unnecessary. Line: `return children.map((child) => child.id.split('/').at(-1)!);`

# ERROR cc9b81fc-2642 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-coordinator/create-single-cursor.test.ts:59`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>,`

# ERROR cc9b81fc-2643 no-casts `packages/plugins/plugin-connector/src/capabilities/connector-coordinator/reconcile-cursors.test.ts:64`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>,`

# ERROR cc9b81fc-2644 no-casts `packages/plugins/plugin-connector/src/capabilities/create-object.ts:47`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `object: undefined as unknown as Obj.Unknown,`

# ERROR cc9b81fc-2645 no-casts `packages/plugins/plugin-connector/src/commands/connector/oauth-flow.ts:196`

This line contains an `as unknown as T` double cast — the escape hatch the no-casts rule (severity: error) calls out by name, routing through `unknown` to defeat structural checking entirely. Replace it with a real conversion, a validated parse, or a type that actually describes the value. Line: `(edgeClient as unknown as { _authHeader?: string })._authHeader;`

# WARN cc9b81fc-2646 no-echo-internal-in-sdk `packages/plugins/plugin-connector/src/types/ConnectorAnnotations.ts:7:10`

Imports `createAnnotationHelper` from the private `@dxos/echo/internal` surface to build `ConnectorAuthAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead, which covers the same schema-annotation-helper use case with a compatibility guarantee.

# ERROR cc9b81fc-2647 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:75:33`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`

# ERROR cc9b81fc-2648 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:76:70`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const routineInstructions = await Routine.instructionsRef(routine!)?.tryLoad();`

# ERROR cc9b81fc-2649 no-casts `packages/plugins/plugin-crm/src/templates/crm-project.test.ts:83:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const trigger = await routine!.triggers[0].tryLoad();`

# ERROR cc9b81fc-2650 no-casts `packages/plugins/plugin-crx/src/page-actions.test.ts:49:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `getTarget: () => ({}) as any, // Stub database; never dereferenced by the handler.`

# ERROR cc9b81fc-2651 no-casts `packages/plugins/plugin-debug/src/components/SchemaTable/SchemaTable.tsx:12:8`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `types: any[];`

# ERROR cc9b81fc-2652 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/ObjectGenerator.tsx:21:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator: ValueGenerator = random as any;`

# ERROR cc9b81fc-2653 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:56:55`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `schemas: [CanvasBoard.CanvasBoard, Trigger.Trigger] as any[],`

# ERROR cc9b81fc-2654 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:268:59`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `functionTrigger = triggerShape.functionTrigger!.target!;`

# ERROR cc9b81fc-2655 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:622:59`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `functionTrigger = triggerShape.functionTrigger!.target!;`

# ERROR cc9b81fc-2656 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:735:51`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `functionTrigger = triggerShape.functionTrigger!.target!;`

# ERROR cc9b81fc-2657 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:739:37`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `initSpec(functionTrigger.spec as any);`

# ERROR cc9b81fc-2658 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:789:80`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const inputNode = computeModel.nodes.find((node) => node.type === NODE_INPUT)!;`

# ERROR cc9b81fc-2659 no-casts `packages/plugins/plugin-debug/src/components/SpaceGenerator/presets.ts:792:60`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `functionTrigger.runnable = Ref.make(computeModel.root) as any;`

# ERROR cc9b81fc-2660 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.stories.tsx:34:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `logStore: { export: async () => '' } as any,`

# ERROR cc9b81fc-2661 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:87:47`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const url = client.config.values.runtime!.services!.ipfs!.gateway + '/' + info.cid;`

# ERROR cc9b81fc-2662 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:111:17`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `} catch (err: any) {`

# ERROR cc9b81fc-2663 no-casts `packages/plugins/plugin-debug/src/containers/DebugSettings/DebugSettings.tsx:258:110`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const updateConfig = (config: ConfigProto, setConfig: (newConfig: ConfigProto) => void, path: string[], value: any) => {`

# ERROR cc9b81fc-2664 no-casts `packages/plugins/plugin-debug/src/containers/DebugStatus/DebugStatus.tsx:63:33`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const errorListener = (event: any) => {`

# ERROR cc9b81fc-2665 no-casts `packages/plugins/plugin-deck/src/containers/DeckLayout/DeckLayout.stories.tsx:161:33`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const subject = (data as any)?.subject;`

# ERROR cc9b81fc-2666 no-casts `packages/plugins/plugin-deck/src/containers/DeckLayout/DeckLayout.stories.tsx:162:38`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const attendableId = (data as any)?.attendableId as string | undefined;`

# ERROR cc9b81fc-2667 no-casts `packages/plugins/plugin-deck/src/containers/DeckSettings/DeckSettings.tsx:15:32`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const isSocket = !!(globalThis as any).__args;`

# ERROR cc9b81fc-2668 no-casts `packages/plugins/plugin-deck/src/containers/Sidebar/ComplementarySidebar.tsx:151:12`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `subject: any;`

# ERROR cc9b81fc-2669 no-casts `packages/plugins/plugin-deck/src/types/DeckSchema.ts:69:35`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const isLayoutMode = (value: any): value is LayoutMode => Schema.is(LayoutMode)(value);`

# ERROR cc9b81fc-2670 no-casts `packages/plugins/plugin-devtools/src/plugin.tsx:41:15`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(globalThis as any).composer ??= {};`

# ERROR cc9b81fc-2671 no-casts `packages/plugins/plugin-devtools/src/plugin.tsx:44:15`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(globalThis as any).composer.changeStorageVersionInMetadata = async (version: number) => {`

# ERROR cc9b81fc-2672 no-casts `packages/plugins/plugin-devtools/src/plugin.tsx:47:36`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const client: Client = (window as any).dxos.client;`

# ERROR cc9b81fc-2673 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:38:6`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `}) as unknown as MessageResponse;`

# ERROR cc9b81fc-2674 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:96:57`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const config: Type.Config = { channels: [channelId!], descendThreads, seed: { maxDays } };`

# ERROR cc9b81fc-2675 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:104:60`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const layer = Layer.mergeAll(discordSourceLayer(token!), coreLayer, aiLayer);`

# ERROR cc9b81fc-2676 no-casts `packages/plugins/plugin-discord/src/services/discord-source.test.ts:167:56`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `}).pipe(Effect.provide(discordSourceLayer(token!))),`

# ERROR cc9b81fc-2677 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:58:27`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect((result.entries![0] as LogRecord).m).toBe('rpc thing');`

# ERROR cc9b81fc-2678 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:90:26`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(result.entries![0]).toEqual({ t: baseRows[0].t, m: 'one' });`

# ERROR cc9b81fc-2679 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:120:51`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const byKey = Object.fromEntries(result.groups!.map((group) => [group.key, group.count]));`

# ERROR cc9b81fc-2680 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:158:26`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(result.entries![0]).toBe(formatPrettyLine(record));`

# ERROR cc9b81fc-2681 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.test.ts:159:26`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(result.entries![0]).toBe('2026-01-01T00:00:01.000Z I a.ts:7 Foo hi {"k":1} ');`

# ERROR cc9b81fc-2682 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.ts:310:9`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `} as unknown as Parameters<typeof shouldLog>[0];`

# ERROR cc9b81fc-2683 no-casts `packages/plugins/plugin-doctor/src/operations/internal/log-query.ts:326:52`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const key = getGroupKey(record, input.groupBy!);`

# ERROR cc9b81fc-2684 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.stories.tsx:13:47`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const items = CitiesData.features.map((feature: any) => ({`

# ERROR cc9b81fc-2685 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.tsx:20:9`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `items?: any[];`

# ERROR cc9b81fc-2686 no-casts `packages/plugins/plugin-explorer/src/components/Chart/Chart.tsx:49:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `containerRef.current!.append(plot);`

# ERROR cc9b81fc-2687 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:21:47`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const items = CitiesData.features.map((feature: any) => ({`

# ERROR cc9b81fc-2688 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:35:40`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const land = feature(CountriesData as any, CountriesData.objects.land as any);`

# ERROR cc9b81fc-2689 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.stories.tsx:65:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `containerRef.current!.append(drawing);`

# ERROR cc9b81fc-2690 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.tsx:20:9`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `items?: any[];`

# ERROR cc9b81fc-2691 no-casts `packages/plugins/plugin-explorer/src/components/Globe/Globe.tsx:28:47`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const land = topojson.feature(CountriesData as any, CountriesData.objects.land as any);`

# ERROR cc9b81fc-2692 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.stories.tsx:26:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator = random as any as ValueGenerator;`

# ERROR cc9b81fc-2693 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:57:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.nodeLabel((node: any) => (node.type === 'schema' ? node.data.typename : (node.data.label ?? node.id)))`

# ERROR cc9b81fc-2694 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:58:33`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.nodeAutoColorBy((node: any) => (node.type === 'schema' ? 'schema' : node.data.typename))`

# ERROR cc9b81fc-2695 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:61:33`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.linkAutoColorBy((link: any) => link.type);`

# ERROR cc9b81fc-2696 no-casts `packages/plugins/plugin-explorer/src/components/Graph/CanvasForceGraph.tsx:81:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.onNodeClick((node: any) => {`

# ERROR cc9b81fc-2697 no-casts `packages/plugins/plugin-explorer/src/components/Graph/ForceGraph.stories.tsx:31:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator = random as any as ValueGenerator;`

# ERROR cc9b81fc-2698 no-casts `packages/plugins/plugin-explorer/src/components/Lattice/Lattice.stories.tsx:29:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator = random as any as ValueGenerator;`

# ERROR cc9b81fc-2699 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:23:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator = random as any as ValueGenerator;`

# ERROR cc9b81fc-2700 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:50:38`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `data: buildOrgHierarchy(orgs as any),`

# ERROR cc9b81fc-2701 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:51:47`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `edges: connectionsToEdges(connections as any),`

# ERROR cc9b81fc-2702 no-casts `packages/plugins/plugin-explorer/src/components/Tree/EdgeBundling.stories.tsx:95:19`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `component: Tree as any,`

# ERROR cc9b81fc-2703 no-casts `packages/plugins/plugin-explorer/src/components/Visualization/variants.ts:156:33`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const relation = (edge.data as any)?.object as Obj.Unknown | undefined;`

# ERROR cc9b81fc-2704 no-casts `packages/plugins/plugin-explorer/src/components/Visualization/variants.ts:163:33`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const property = (edge.data as any)?.property as string | undefined;`

# ERROR cc9b81fc-2705 no-casts `packages/plugins/plugin-explorer/src/containers/ExplorerArticle/ExplorerArticle.stories.tsx:27:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator = random as any as ValueGenerator;`

# ERROR cc9b81fc-2706 no-casts `packages/plugins/plugin-explorer/src/containers/ExplorerArticle/ExplorerArticle.stories.tsx:40:57`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `return <ExplorerArticle role='article' subject={graph as any} attendableId={graph.id} variant={variant} />;`

# ERROR cc9b81fc-2707 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:78:34`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const org = organizations[i] as any;`

# ERROR cc9b81fc-2708 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:80:24`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `buckets.get(sector)!.push({`

# ERROR cc9b81fc-2709 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:103:50`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const source = Relation.getSource(relation as any) as any;`

# ERROR cc9b81fc-2710 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:104:50`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const target = Relation.getTarget(relation as any) as any;`

# ERROR cc9b81fc-2711 no-casts `packages/plugins/plugin-explorer/src/testing/relations.ts:111:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `kind: (relation as any).kind,`

# ERROR cc9b81fc-2712 no-casts `packages/plugins/plugin-explorer/src/util/plot.ts:7:34`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export type Accessor<T> = (object: any) => T;`

# ERROR cc9b81fc-2713 no-casts `packages/plugins/plugin-file/src/extensions/image.tsx:137:65`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const matched = await space.db.query(Filter.id(echoUri!)).first();`

# ERROR cc9b81fc-2714 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:31:92`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const manager = { acquire: () => ({ connection: fakeConnection, release: () => {} }) } as any;`

# ERROR cc9b81fc-2715 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:39:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2716 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:60:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2717 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:67:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2718 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:84:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const acquireCalls: any[] = [];`

# ERROR cc9b81fc-2719 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:87:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `acquire: (params: any) => {`

# ERROR cc9b81fc-2720 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:91:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2721 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:100:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2722 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:115:98`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const manager = { acquire: () => ({ connection: makeFakeConnection(), release: () => {} }) } as any;`

# ERROR cc9b81fc-2723 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:121:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2724 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:134:92`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const manager = { acquire: () => ({ connection: fakeConnection, release: () => {} }) } as any;`

# ERROR cc9b81fc-2725 no-casts `packages/plugins/plugin-freeq/src/capabilities/channel-backend.test.ts:140:7`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2726 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:55:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const connections: any[] = [];`

# ERROR cc9b81fc-2727 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:77:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const connections: any[] = [];`

# ERROR cc9b81fc-2728 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:96:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const connections: any[] = [];`

# ERROR cc9b81fc-2729 no-casts `packages/plugins/plugin-freeq/src/services/ConnectionManager.test.ts:170:5`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any;`

# ERROR cc9b81fc-2730 no-casts `packages/plugins/plugin-freeq/src/services/IrcConnection.test.ts:67:33`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const requestedCaps = capReq!.slice('CAP REQ :'.length).split(' ');`

# ERROR cc9b81fc-2731 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:83:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `type Listener = (event: any) => void;`

# ERROR cc9b81fc-2732 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:102:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `emit(type: string, event: any = {}): void {`

# ERROR cc9b81fc-2733 no-casts `packages/plugins/plugin-freeq/src/services/Transport.test.ts:117:84`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const transport = makeWebSocketTransport('wss://irc.freeq.at/irc', FakeWebSocket as any);`

# ERROR cc9b81fc-2734 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:40:49`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `createVariant: () => Effect.sync(() => ({}) as unknown as Obj.Any),`

# ERROR cc9b81fc-2735 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:49:49`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `createVariant: () => Effect.sync(() => ({}) as unknown as Obj.Any),`

# ERROR cc9b81fc-2736 no-casts `packages/plugins/plugin-game/src/components/CreateGamePanel.stories.tsx:64:26`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `target={{} as any}`

# ERROR cc9b81fc-2737 no-casts `packages/plugins/plugin-game/src/types/Game.ts:81:17`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `Ref.Ref(Game) as unknown as Schema.Codec<GameRef<Type.InstanceType<S>>, any>;`

# ERROR cc9b81fc-2738 no-casts `packages/plugins/plugin-github/src/services/github-api.ts:443:11`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `input as unknown as Record<string, unknown>,`

# ERROR cc9b81fc-2739 no-casts `packages/plugins/plugin-github/src/services/github-api.ts:463:11`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `input as unknown as Record<string, unknown>,`

# ERROR cc9b81fc-2740 no-casts `packages/plugins/plugin-google/src/operations/calendar/sync/sync-mock.test.ts:64:103`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `db.query(Query.select(Filter.type(Event.Event)).from(Scope.feed(Feed.getFeedUri(calendar.feed.target!)!))).run();`

# ERROR cc9b81fc-2741 no-casts `packages/plugins/plugin-google/src/operations/calendar/sync/sync-mock.test.ts:148:21`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(events[0]!.owner).toEqual({});`

# ERROR cc9b81fc-2742 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:45:51`

This line casts to `any`, silencing the type checker instead of fixing the underlying type; also uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary and add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const json = Entity.toJSON(result!.message) as any;`

# ERROR cc9b81fc-2743 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:60:51`

This line casts to `any`, silencing the type checker instead of fixing the underlying type; also uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary and add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const json = Entity.toJSON(result!.message) as any;`

# ERROR cc9b81fc-2744 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:95:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const properties = result!.message.properties;`

# ERROR cc9b81fc-2745 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:123:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const textBlocks = result!.message.blocks.filter(`

# ERROR cc9b81fc-2746 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:155:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const textBlocks = result!.message.blocks.filter(`

# ERROR cc9b81fc-2747 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:184:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const textBlocks = result!.message.blocks.filter(`

# ERROR cc9b81fc-2748 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:208:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const properties = result!.message.properties!;`

# ERROR cc9b81fc-2749 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:211:40`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(Mailbox.isReplyable(result!.message)).toBe(false);`

# ERROR cc9b81fc-2750 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:220:32`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const properties = result!.message.properties!;`

# ERROR cc9b81fc-2751 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/mapper.test.ts:223:40`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(Mailbox.isReplyable(result!.message)).toBe(true);`

# ERROR cc9b81fc-2752 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:118:94`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `db.query(Query.select(Filter.type(Message.Message)).from(Scope.feed(Feed.getFeedUri(feed)!))).run();`

# ERROR cc9b81fc-2753 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:121:73`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `Obj.getMeta(message).keys.find((key) => key.source === GMAIL_SOURCE)!.id;`

# ERROR cc9b81fc-2754 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:136:64`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const messages = await feedMessages(db, mailbox.feed.target!);`

# ERROR cc9b81fc-2755 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:168:64`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const messages = await feedMessages(db, mailbox.feed.target!);`

# ERROR cc9b81fc-2756 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:175:37`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const gmailId = gmailIdOf(target!);`

# ERROR cc9b81fc-2757 no-casts `packages/plugins/plugin-google/src/operations/mail/sync/sync-live.test.ts:178:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `Tagging.unset(target!, inboxUri, { index: tagIndex });`

# ERROR cc9b81fc-2758 no-casts `packages/plugins/plugin-google/src/testing/gmail-fixtures.test.ts:61:62`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const full = yield* api.getMessage('me', page1.messages![0].id);`

# ERROR cc9b81fc-2759 no-casts `packages/plugins/plugin-google/src/testing/gmail-fixtures.test.ts:62:42`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(full.id).toBe(page1.messages![0].id);`

# ERROR cc9b81fc-2760 no-casts `packages/plugins/plugin-graph/src/graph.ts:76:15`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(globalThis as any).composer ??= {};`

# ERROR cc9b81fc-2761 no-casts `packages/plugins/plugin-graph/src/graph.ts:77:15`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(globalThis as any).composer.graph = graph;`

# ERROR cc9b81fc-2762 no-casts `packages/plugins/plugin-ibkr/src/components/ReportSections/ReportSections.tsx:27:7`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `rows: any[];`

# ERROR cc9b81fc-2763 no-casts `packages/plugins/plugin-illustrator/src/capabilities/migrations.test.ts:32:28`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const addLegacySketch = (db: any, { name, schema = 'tldraw.com/2' }: { name?: string; schema?: string } = {}) => {`

# ERROR cc9b81fc-2764 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:58:20`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `identify: (record: any) => Identity | undefined;`

# ERROR cc9b81fc-2765 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:64:21`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `translate: (record: any, delta: Scene.Point) => void;`

# ERROR cc9b81fc-2766 no-casts `packages/plugins/plugin-illustrator/src/model/content.ts:166:66`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const nextIndex = (content: ContentMap, isCounted: (record: any) => boolean = () => true): number => {`

# ERROR cc9b81fc-2767 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:64:67`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const [element] = objects.find((object) => object.id === id)!.elements;`

# ERROR cc9b81fc-2768 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:68:65`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const frame = objects.find((object) => object.id === 'CORE')!.elements[0];`

# ERROR cc9b81fc-2769 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:75:73`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const y = (id: string) => objects.find((object) => object.id === id)!.origin!.y;`

# ERROR cc9b81fc-2770 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:87:88`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const edges = objectsOf(compile(FLOWCHART)).find((object) => object.id === 'edges')!;`

# ERROR cc9b81fc-2771 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:97:64`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const core = objects.find((object) => object.id === 'CORE')!;`

# ERROR cc9b81fc-2772 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:101:62`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const node = objects.find((object) => object.id === id)!;`

# ERROR cc9b81fc-2773 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:102:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(node.origin!.x).toBeGreaterThanOrEqual(core.origin!.x);`

# ERROR cc9b81fc-2774 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:103:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(node.origin!.y).toBeGreaterThanOrEqual(core.origin!.y);`

# ERROR cc9b81fc-2775 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:104:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(node.origin!.x).toBeLessThanOrEqual(core.origin!.x + frame.w);`

# ERROR cc9b81fc-2776 no-casts `packages/plugins/plugin-illustrator/src/model/mermaid.test.ts:105:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(node.origin!.y).toBeLessThanOrEqual(core.origin!.y + frame.h);`

# ERROR cc9b81fc-2777 no-casts `packages/plugins/plugin-inbox/src/capabilities/app-graph-builder.ts:231:93`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `...(mailboxSnapshot.filters?.map(({ name, filter }: { name: string; filter: any }) =>`

# ERROR cc9b81fc-2778 no-casts `packages/plugins/plugin-inbox/src/capabilities/app-graph-builder.ts:262:75`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const index = mailbox.filters.findIndex((f: any) => f.name === name);`

# ERROR cc9b81fc-2779 no-casts `packages/plugins/plugin-inbox/src/capabilities/skill-definition.ts:29:43`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `.map((connector) => connector.sync!.operation);`

# ERROR cc9b81fc-2780 no-casts `packages/plugins/plugin-inbox/src/components/EditMessage/EditMessage.stories.tsx:20:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator: ValueGenerator = random as any;`

# ERROR cc9b81fc-2781 no-casts `packages/plugins/plugin-inbox/src/components/EditMessage/EditMessage.stories.tsx:86:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `onSend: async (message: any) => console.log(message),`

# ERROR cc9b81fc-2782 no-casts `packages/plugins/plugin-inbox/src/components/Event/Event.stories.tsx:23:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator: ValueGenerator = random as any;`

# ERROR cc9b81fc-2783 no-casts `packages/plugins/plugin-inbox/src/components/RelatedMessages/RelatedMessages.test.ts:57:65`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `({ id, created: '2026-07-01T00:00:00.000Z', properties }) as unknown as Message.Message;`

# ERROR cc9b81fc-2784 no-casts `packages/plugins/plugin-inbox/src/containers/CalendarArticle/CalendarArticle.stories.tsx:60:72`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = yield* Effect.tryPromise(() => calendar.feed!.tryLoad());`

# ERROR cc9b81fc-2785 no-casts `packages/plugins/plugin-inbox/src/containers/MailboxArticle/mailbox-search.test.ts:88:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `expect((query.ast as any).queries).toHaveLength(2);`

# ERROR cc9b81fc-2786 no-casts `packages/plugins/plugin-inbox/src/containers/MailboxArticle/mailbox-search.test.ts:97:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `expect((query.ast as any).queries[1]).toMatchObject({ type: 'select', filter: viewFilter.ast });`

# ERROR cc9b81fc-2787 no-casts `packages/plugins/plugin-inbox/src/containers/MailboxArticle/mailbox-search.test.ts:242:88`

This line casts to `any`, silencing the type checker instead of fixing the underlying type; also types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary and replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const semiJoinArm = (query: ReturnType<typeof buildThreadSemiJoin>): any => (query.ast as any).queries[0];`

# ERROR cc9b81fc-2788 no-casts `packages/plugins/plugin-inbox/src/operations/add-mailbox.ts:21:35`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const target = input.target as any;`

# ERROR cc9b81fc-2789 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:87:53`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const messageEchoId = EID.tryParse((message as any)['@uri']);`

# ERROR cc9b81fc-2790 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:103:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = mailbox.feed!.target as Feed.Feed;`

# ERROR cc9b81fc-2791 no-casts `packages/plugins/plugin-inbox/src/operations/classify-email.ts:133:13`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any),`

# ERROR cc9b81fc-2792 no-casts `packages/plugins/plugin-inbox/src/operations/extractor/summarize-extractor.test.ts:90:93`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `generateText: () => Effect.succeed([{ type: 'text', text: MOCK_SUMMARY }] as const) as any,`

# ERROR cc9b81fc-2793 no-casts `packages/plugins/plugin-inbox/src/operations/extractor/summarize-extractor.test.ts:91:40`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `streamText: () => Stream.empty as any,`

# ERROR cc9b81fc-2794 no-casts `packages/plugins/plugin-inbox/src/operations/FeedCursor.test.ts:52:21`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const run = <A>(db: any, effect: Effect.Effect<A, any, Database.Service>) =>`

# ERROR cc9b81fc-2795 no-casts `packages/plugins/plugin-inbox/src/operations/FeedCursor.test.ts:98:32`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const addLegacyCursor = (db: any, mailbox: Mailbox.Mailbox) =>`

# ERROR cc9b81fc-2796 no-casts `packages/plugins/plugin-inbox/src/operations/subscriptions/extract-subscriptions.test.ts:74:62`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(Mailbox.parseUnsubscribe(mailbox.subscriptions![1].unsubscribe)).toEqual({`

# ERROR cc9b81fc-2797 no-casts `packages/plugins/plugin-inbox/src/testing/email-processor.ts:89:15`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const result: any =`

# ERROR cc9b81fc-2798 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:9:24`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const EntityId: any; // typeof import('@dxos/functions-sdk').EntityId;`

# ERROR cc9b81fc-2799 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:11:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const create: any; // typeof import('@dxos/functions-sdk').create;`

# ERROR cc9b81fc-2800 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:13:30`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const defineFunction: any; // typeof import('@dxos/functions-sdk').defineFunction;`

# ERROR cc9b81fc-2801 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:15:17`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const S: any; // typeof import('@dxos/functions-sdk').S;`

# ERROR cc9b81fc-2802 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:17:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const Filter: any; // typeof import('@dxos/functions-sdk').Filter;`

# ERROR cc9b81fc-2803 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:19:31`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const FunctionsClient: any; // typeof import('@dxos/functions-sdk').FunctionsClient;`

# ERROR cc9b81fc-2804 no-casts `packages/plugins/plugin-inbox/src/testing/scripts/types.ts:21:35`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const createClientFromEnv: any; //  typeof import('@dxos/functions-sdk').createClientFromEnv;`

# ERROR cc9b81fc-2805 no-casts `packages/plugins/plugin-inbox/src/types/apply-tag.test.ts:47:21`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const run = <A>(db: any, effect: Effect.Effect<A, any, Database.Service>) =>`

# ERROR cc9b81fc-2806 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:35:30`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = mailbox.feed!.target!;`

# ERROR cc9b81fc-2807 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:46:42`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect([...TagIndex.bind(mailbox.tags!.target!).objects(tagUri)]).toEqual([message.id]);`

# ERROR cc9b81fc-2808 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:90:30`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = mailbox.feed!.target!;`

# ERROR cc9b81fc-2809 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:123:97`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const merged = [...Mailbox.mergeAnnotations(feedMessages, await read(db, mailbox.annotations!.target!))];`

# ERROR cc9b81fc-2810 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:150:106`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const [entry] = [...Mailbox.mergeAnnotations(await read(db, feed), await read(db, mailbox.annotations!.target!))];`

# ERROR cc9b81fc-2811 no-casts `packages/plugins/plugin-inbox/src/types/Mailbox.test.ts:202:46`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(await read(db, mailbox.annotations!.target!)).toHaveLength(1);`

# ERROR cc9b81fc-2812 no-casts `packages/plugins/plugin-inbox/src/types/SystemTags.test.ts:36:30`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = mailbox.feed!.target!;`

# ERROR cc9b81fc-2813 no-casts `packages/plugins/plugin-inbox/src/types/SystemTags.test.ts:50:52`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const starred = SystemTags.tagAtom(mailbox.tags!.target!, starredUri);`

# ERROR cc9b81fc-2814 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.test.ts:28:6`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `}) as unknown as Message.Message;`

# ERROR cc9b81fc-2815 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:21:24`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const matchesAst = (ast: any, message: Message.Message, tags: string[]): boolean => {`

# ERROR cc9b81fc-2816 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:28:66`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `return Array.isArray(ast.filters) && ast.filters.every((sub: any) => matchesAst(sub, message, tags));`

# ERROR cc9b81fc-2817 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:30:65`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `return Array.isArray(ast.filters) && ast.filters.some((sub: any) => matchesAst(sub, message, tags));`

# ERROR cc9b81fc-2818 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:67:30`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const matchesPredicate = (ast: any, value: any, message: Message.Message, tags: string[]): boolean => {`

# ERROR cc9b81fc-2819 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:110:28`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const matchesValue = (value: any, query: any): boolean => {`

# ERROR cc9b81fc-2820 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:126:21`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const getPath = (obj: any, path: string): any => {`

# ERROR cc9b81fc-2821 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:172:32`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `if (block && typeof (block as any).text === 'string') {`

# ERROR cc9b81fc-2822 no-casts `packages/plugins/plugin-inbox/src/util/match-filter.ts:173:29`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `haystacks.push((block as any).text);`

# ERROR cc9b81fc-2823 no-casts `packages/plugins/plugin-inbox/src/util/util.test.ts:226:7`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `} as unknown as Message.Message;`

# ERROR cc9b81fc-2824 no-casts `packages/plugins/plugin-jmap/src/apis/jmap-api.test.ts:40:55`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `type MockRequest = { url: string; method: string; body: any };`

# ERROR cc9b81fc-2825 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanArticle/KanbanArticle.tsx:145:27`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `out.push(target as unknown as Obj.Unknown);`

# ERROR cc9b81fc-2826 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanProperties/KanbanProperties.tsx:52:62`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `view.projection.pivotFieldId = values.columnFieldId!;`

# ERROR cc9b81fc-2827 no-casts `packages/plugins/plugin-kanban/src/containers/KanbanProperties/KanbanProperties.tsx:87:111`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const settingsSchema = (isView ? KanbanSchema.KanbanViewSettingsSchema : KanbanSchema.KanbanSettingsSchema) as any;`

# ERROR cc9b81fc-2828 no-casts `packages/plugins/plugin-kanban/src/hooks/useEchoChangeCallback.ts:20:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `Obj.update(item, (item: any) => {`

# ERROR cc9b81fc-2829 no-casts `packages/plugins/plugin-kanban/src/hooks/useItemsProjection.ts:25:26`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const fieldProjection: any = {`

# ERROR cc9b81fc-2830 no-casts `packages/plugins/plugin-kanban/src/hooks/useItemsProjection.ts:42:17`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `return stub as unknown as ProjectionModel;`

# ERROR cc9b81fc-2831 no-casts `packages/plugins/plugin-kanban/src/util/arrangement.test.ts:104:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `columns: { a: { ids: {} as unknown as string[] }, b: { ids: ['ok'] } },`

# ERROR cc9b81fc-2832 no-casts `packages/plugins/plugin-linear/src/services/linear-api.ts:231:26`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `selectConnection: (data: any) => {`

# ERROR cc9b81fc-2833 no-casts `packages/plugins/plugin-magazine/src/containers/MagazineArticle/MagazineArticle.stories.tsx:139:71`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const echoFeed = yield* Effect.promise(() => subscriptionFeed.feed!.load());`

# ERROR cc9b81fc-2834 no-casts `packages/plugins/plugin-magazine/src/operations/sources/rss.ts:101:14`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const items: any[] = (isAtom ? channel.entry : channel.item) ?? [];`

# ERROR cc9b81fc-2835 no-casts `packages/plugins/plugin-magazine/src/operations/sources/rss.ts:107:30`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `? item.link.find((l: any) => l['@_rel'] === 'alternate')?.['@_href']`

# ERROR cc9b81fc-2836 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:36:46`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const postFeed = subscription.feed.target!;`

# ERROR cc9b81fc-2837 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:47:52`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `return { db, subscription, queuePost: queuePost! };`

# ERROR cc9b81fc-2838 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:98:46`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const postFeed = subscription.feed.target!;`

# ERROR cc9b81fc-2839 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:111:22`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `post: queuePost!,`

# ERROR cc9b81fc-2840 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:118:77`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const found = await Subscription.findPostContent(subscription, queuePost!);`

# ERROR cc9b81fc-2841 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:120:38`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(EID.equals(EID.parse(found!.post.uri), EID.parse(Obj.getURI(queuePost!)))).toBe(true);`

# ERROR cc9b81fc-2842 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:139:46`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const postFeed = subscription.feed.target!;`

# ERROR cc9b81fc-2843 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:152:22`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `post: queuePost!,`

# ERROR cc9b81fc-2844 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:157:22`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `post: queuePost!,`

# ERROR cc9b81fc-2845 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:163:79`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const query = Subscription.queryPostContentForPost(subscription, queuePost!);`

# ERROR cc9b81fc-2846 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:166:41`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const entries = await db.query(query!).run();`

# ERROR cc9b81fc-2847 no-casts `packages/plugins/plugin-magazine/src/types/Subscription.test.ts:169:78`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const latest = await Subscription.findPostContent(subscription, queuePost!);`

# ERROR cc9b81fc-2848 no-casts `packages/plugins/plugin-map/src/capabilities/app-graph-builder.ts:30:50`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const presentationRef = (node.properties as any).presentation;`

# ERROR cc9b81fc-2849 no-casts `packages/plugins/plugin-map/src/types/MapCapabilities.ts:69:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `useMarkers: (subject: any, options: { attendableId?: string }) => MarkerSet;`

# ERROR cc9b81fc-2850 no-casts `packages/plugins/plugin-markdown/src/components/MarkdownEditor/MarkdownEditorContent.tsx:179:30`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const composer = (window as any).composer;`

# ERROR cc9b81fc-2851 no-casts `packages/plugins/plugin-markdown/src/containers/MarkdownArticle/MarkdownArticle.stories.tsx:46:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const generator: ValueGenerator = random as any;`

# ERROR cc9b81fc-2852 no-casts `packages/plugins/plugin-markdown/src/hooks/useLinkQuery.ts:71:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const name = (block ? query!.slice(1) : (query ?? '')).toLowerCase();`

# ERROR cc9b81fc-2853 no-casts `packages/plugins/plugin-markdown/src/util.tsx:16:35`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const isEditorModel = (data: any): data is { id: string; text: string } =>`

# ERROR cc9b81fc-2854 no-casts `packages/plugins/plugin-meeting/src/stories/EventCall.stories.tsx:118:72`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const feed = yield* Effect.tryPromise(() => calendar.feed!.tryLoad());`

# ERROR cc9b81fc-2855 no-casts `packages/plugins/plugin-mermaid/src/extensions/mermaid-extension.ts:190:28`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `div.innerHTML = svg!;`

# ERROR cc9b81fc-2856 no-casts `packages/plugins/plugin-mermaid/src/extensions/mermaid-extension.ts:215:17`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `} catch (err: any) {`

# ERROR cc9b81fc-2857 no-casts `packages/plugins/plugin-native/src/capabilities/spotlight-listener.ts:35:65`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `await invokePromise(LayoutOperation.Open, payload as any);`

# ERROR cc9b81fc-2858 no-casts `packages/plugins/plugin-native/src/capabilities/spotlight-listener.ts:38:76`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `await invokePromise(LayoutOperation.SwitchWorkspace, payload as any);`

# ERROR cc9b81fc-2859 no-casts `packages/plugins/plugin-navtree/src/components/NavTreeItem/NavTreeItemAction.tsx:124:85`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const baseLabel = toLocalizedString(monolithicAction ? monolithicAction.properties!.label : props.label, t);`

# ERROR cc9b81fc-2860 no-casts `packages/plugins/plugin-navtree/src/components/Sidebar/L0Menu.tsx:193:30`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `itemElement.current!.closest('[data-drag-autoscroll]')?.setAttribute('data-drag-autoscroll', 'active');`

# ERROR cc9b81fc-2861 no-casts `packages/plugins/plugin-navtree/src/components/Sidebar/L0Menu.tsx:196:30`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `itemElement.current!.closest('[data-drag-autoscroll]')?.setAttribute('data-drag-autoscroll', 'idle');`

# ERROR cc9b81fc-2862 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:88:71`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const [openItems, setOpenItems] = useState<ItemMap>({ [root.children![0].id]: true });`

# ERROR cc9b81fc-2863 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:125:14`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `(parent!.children ??= []).push({`

# ERROR cc9b81fc-2864 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:130:49`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `setOpenItems((open) => ({ ...open, [parent!.id]: true }));`

# ERROR cc9b81fc-2865 no-casts `packages/plugins/plugin-navtree/src/experimental/Tree.stories.tsx:216:57`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const [openItems] = useState<ItemMap>({ [root.children![0].id]: true });`

# ERROR cc9b81fc-2866 no-casts `packages/plugins/plugin-navtree/src/testing/generator.ts:95:61`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `return Object.values(this.factories).map((f) => f.schema!);`

# ERROR cc9b81fc-2867 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:13:29`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const index = parent.nodes!.findIndex((node: Node.NodeArg<any>) => node.id === source.id);`

# ERROR cc9b81fc-2868 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:14:28`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const item = parent.nodes![index];`

# ERROR cc9b81fc-2869 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:15:15`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `parent.nodes!.splice(index, 1);`

# ERROR cc9b81fc-2870 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:22:22`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `node = node.nodes!.find((n: Node.NodeArg<any>) => n.id === part)!;`

# ERROR cc9b81fc-2871 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:43:33`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const index = parent.nodes!.findIndex(({ id }) => id === target.id);`

# ERROR cc9b81fc-2872 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:44:19`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `parent.nodes!.splice(index, 0, item);`

# ERROR cc9b81fc-2873 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:51:33`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const index = parent.nodes!.findIndex(({ id }) => id === target.id);`

# ERROR cc9b81fc-2874 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:52:19`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `parent.nodes!.splice(index + 1, 0, item);`

# ERROR cc9b81fc-2875 no-casts `packages/plugins/plugin-navtree/src/testing/update-state.ts:59:19`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `parent.nodes!.push(item);`

# ERROR cc9b81fc-2876 no-casts `packages/plugins/plugin-observability/src/plugin.test.ts:18:40`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `plugins: [ObservabilityPlugin({} as any)],`

# ERROR cc9b81fc-2877 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:288:48`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `await appendToFeed(space, mailbox.feed.target!, messages);`

# ERROR cc9b81fc-2878 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:290:49`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `await appendToFeed(space, calendar.feed.target!, events);`

# ERROR cc9b81fc-2879 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1008:84`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const seedForPerson = (key: PersonKey) => PEOPLE_SEEDS.find((s) => s.key === key)!;`

# ERROR cc9b81fc-2880 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1500:22`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `schema: tlSchema as any,`

# ERROR cc9b81fc-2881 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1501:106`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `props: { defaultName: '', assets: { upload: async () => '', resolve: () => '' }, onMount: () => {} } as any,`

# ERROR cc9b81fc-2882 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1506:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `PageRecordType.create({ id: pageId as any, name: pageName, index: 'a1' as IndexKey }),`

# ERROR cc9b81fc-2883 no-casts `packages/plugins/plugin-onboarding/scripts/build-exemplar-space.ts:1564:6`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `}) as unknown as TLRecord;`

# ERROR cc9b81fc-2884 no-casts `packages/plugins/plugin-onboarding/src/containers/WelcomeContainer/Welcome/Welcome.stories.tsx:38:22`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `component: Welcome as any,`

# ERROR cc9b81fc-2885 no-casts `packages/plugins/plugin-onboarding/src/credentials/codec.ts:10:16`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `isKey: (value: any): value is PublicKey => value instanceof PublicKey,`

# ERROR cc9b81fc-2886 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:16:14`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `isKey(value: any): value is KeyType;`

# ERROR cc9b81fc-2887 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:25:13`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `encode(obj: any): string {`

# ERROR cc9b81fc-2888 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:36:31`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `private _decodeRecursive(obj: any): any {`

# ERROR cc9b81fc-2889 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:51:34`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `private _prepareForEncoding(obj: any): any {`

# ERROR cc9b81fc-2890 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:66:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `private _recurse(obj: any, handler: (obj: object) => any | null): any {`

# ERROR cc9b81fc-2891 no-casts `packages/plugins/plugin-onboarding/src/credentials/header-codec.ts:81:17`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const result: any = {};`

# ERROR cc9b81fc-2892 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:43:15`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(globalThis as any).ethereum as Eip1193Provider | undefined;`

# ERROR cc9b81fc-2893 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:120:36`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `domain: message.domain as any,`

# ERROR cc9b81fc-2894 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:121:34`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `types: message.types as any,`

# ERROR cc9b81fc-2895 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:122:46`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `primaryType: message.primaryType as any,`

# ERROR cc9b81fc-2896 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:123:38`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `message: message.message as any,`

# ERROR cc9b81fc-2897 no-casts `packages/plugins/plugin-payments/src/services/payments-client.ts:135:55`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `return await publicClient.readContract(args as any);`

# ERROR cc9b81fc-2898 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.stories.tsx:126:60`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(space.db, random as any);`

# ERROR cc9b81fc-2899 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.stories.tsx:196:62`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(space.db, random as any);`

# ERROR cc9b81fc-2900 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:87:20`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `items: columns as any,`

# ERROR cc9b81fc-2901 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:89:25`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `get: (data) => data as unknown as Obj.Unknown,`

# ERROR cc9b81fc-2902 no-casts `packages/plugins/plugin-pipeline/src/components/PipelineComponent/PipelineComponent.tsx:90:30`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `make: (object) => object as unknown as Pipeline.Column,`

# ERROR cc9b81fc-2903 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:124:88`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `status: random.helpers.arrayElement(['todo', 'in-progress', 'done']) as any,`

# ERROR cc9b81fc-2904 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:125:84`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `priority: random.helpers.arrayElement(['low', 'medium', 'high']) as any,`

# ERROR cc9b81fc-2905 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:185:90`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `status: random.helpers.arrayElement(['todo', 'in-progress', 'done']) as any,`

# ERROR cc9b81fc-2906 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:186:86`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `priority: random.helpers.arrayElement(['low', 'medium', 'high']) as any,`

# ERROR cc9b81fc-2907 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineArticle/PipelineArticle.stories.tsx:192:75`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(defaultSpace.db, random as any);`

# ERROR cc9b81fc-2908 no-casts `packages/plugins/plugin-pipeline/src/containers/PipelineProperties/PipelineProperties.stories.tsx:127:75`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(defaultSpace.db, random as any);`

# ERROR cc9b81fc-2909 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:32:77`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `remarkPlugins={[[remarkFrontmatter, 'yaml'], remarkParseFrontmatter as any]}`

# ERROR cc9b81fc-2910 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:34:70`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `rehypePlugins={[[rehypeAddClasses, classes], rehypeHighlight as any, slideLayout]}`

# ERROR cc9b81fc-2911 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:50:8`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `(tree: any, file: any) => {`

# ERROR cc9b81fc-2912 no-casts `packages/plugins/plugin-presenter/src/components/Markdown/Slide.tsx:95:52`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const ImageWrapper = ({ node: _, ...props }: { node: any }) => {`

# ERROR cc9b81fc-2913 no-casts `packages/plugins/plugin-presenter/src/useExitPresenter.ts:16:40`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const useExitPresenter = (object: any) => {`

# ERROR cc9b81fc-2914 no-casts `packages/plugins/plugin-preview/src/capabilities/preview-popover.ts:33:47`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const resolvedLabel = Obj.getLabel(object as any, { fallback: 'typename' });`

# ERROR cc9b81fc-2915 no-casts `packages/plugins/plugin-preview/src/capabilities/preview-popover.ts:117:28`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `DX_ANCHOR_ACTIVATE as any,`

# ERROR cc9b81fc-2916 no-casts `packages/plugins/plugin-preview/src/cards/ExpandoCard.tsx:43:51`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const fieldSchema = schemaForValue((subject as any)[key]);`

# ERROR cc9b81fc-2917 no-casts `packages/plugins/plugin-preview/src/cards/ExpandoCard.tsx:52:12`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `(values: any, { changed }: { changed: Record<string, boolean> }) => {`

# ERROR cc9b81fc-2918 no-casts `packages/plugins/plugin-preview/src/cards/FormCard.tsx:80:47`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `return properties.some((prop) => (subject as any)?.[prop.name] != null);`

# ERROR cc9b81fc-2919 no-casts `packages/plugins/plugin-preview/src/cards/FormCard.tsx:89:45`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const value = Obj.getValue(values as any, parts);`

# ERROR cc9b81fc-2920 no-casts `packages/plugins/plugin-preview/src/cards/TaskCard.tsx:39:27`

This line casts to `any`, silencing the type checker instead of fixing the underlying type; also uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary and add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `(statusProperty!.type as any).types[0],`

# ERROR cc9b81fc-2921 no-casts `packages/plugins/plugin-preview/src/cards/TaskCard.tsx:43:31`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const options = (statusMeta as any).value.singleSelect.options as { id: string; title: string; color: string }[];`

# ERROR cc9b81fc-2922 no-casts `packages/plugins/plugin-preview/src/stories/fixtures.ts:16:48`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const omitImage = ({ image: _, ...rest }: any) => rest;`

# ERROR cc9b81fc-2923 no-casts `packages/plugins/plugin-preview/src/stories/fixtures.ts:67:50`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `export const createUnknown = (): Obj.Any => ({}) as unknown as Obj.Any;`

# ERROR cc9b81fc-2924 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:78:33`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(Obj.getParent(routine!)?.id).toBe(project.id);`

# ERROR cc9b81fc-2925 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:81:67`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const routineInstructionsRef = Routine.instructionsRef(routine!);`

# ERROR cc9b81fc-2926 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:89:19`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(routine!.triggers).toHaveLength(1);`

# ERROR cc9b81fc-2927 no-casts `packages/plugins/plugin-projects/src/templates/inbox-research.test.ts:90:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const trigger = await routine!.triggers[0].tryLoad();`

# ERROR cc9b81fc-2928 no-casts `packages/plugins/plugin-registry/src/commands/registry/util.ts:282:37`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `authHeader: (client.edge.http as unknown as { _authHeader?: string })._authHeader,`

# ERROR cc9b81fc-2929 no-casts `packages/plugins/plugin-registry/src/components/PluginList/PluginList.stories.tsx:41:71`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `tags: random.helpers.uniqueArray(RegistryTagType.literals as any, Math.floor(Math.random() * 3)),`

# ERROR cc9b81fc-2930 no-casts `packages/plugins/plugin-review/src/capabilities/agent-runner.ts:125:27`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const content = (text as unknown as { content: string }).content ?? '';`

# ERROR cc9b81fc-2931 no-casts `packages/plugins/plugin-review/src/capabilities/agent-runner.ts:225:47`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const original = (target.text as unknown as { content: string }).content ?? '';`

# ERROR cc9b81fc-2932 no-casts `packages/plugins/plugin-review/src/containers/CommentsArticle/CommentsArticle.tsx:56:26`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const value = (subject as unknown as Record<string, unknown>)[key];`

# ERROR cc9b81fc-2933 no-casts `packages/plugins/plugin-review/src/should-trigger-agent.test.ts:19:6`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `}) as unknown as Message.Message;`

# ERROR cc9b81fc-2934 no-casts `packages/plugins/plugin-review/src/should-trigger-agent.test.ts:26:6`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `}) as unknown as Thread.Thread;`

# ERROR cc9b81fc-2935 no-casts `packages/plugins/plugin-review/src/stories/DocumentVersioning.stories.tsx:302:68`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const text = yield* Effect.promise(() => currentDoc!.content.load());`

# ERROR cc9b81fc-2936 no-casts `packages/plugins/plugin-review/src/stories/DocumentVersioning.stories.tsx:303:54`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `seedComments(defaultSpace, currentDoc!, text, [COMMENT_ANCHOR]);`

# ERROR cc9b81fc-2937 no-casts `packages/plugins/plugin-review/src/util/author-hue.ts:13:34`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `data?: { readonly [key: string]: any };`

# ERROR cc9b81fc-2938 no-casts `packages/plugins/plugin-review/src/util/message.ts:24:34`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `data?: { readonly [key: string]: any };`

# ERROR cc9b81fc-2939 no-casts `packages/plugins/plugin-routine/src/capabilities/create-object.test.ts:15:17`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(blank!.label).toBe('Blank');`

# ERROR cc9b81fc-2940 no-casts `packages/plugins/plugin-routine/src/capabilities/create-object.test.ts:16:24`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `expect(typeof blank!.scaffold).toBe('function');`

# ERROR cc9b81fc-2941 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateEditor.stories.tsx:64:29`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `component: TemplateEditor as any,`

# ERROR cc9b81fc-2942 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:185:42`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const inputIndex = draft.inputs!.findIndex((i) => i?.name === next.name);`

# ERROR cc9b81fc-2943 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:187:25`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `draft.inputs![inputIndex].name = name;`

# ERROR cc9b81fc-2944 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:190:23`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `draft.inputs!.push({ name, kind: 'value' });`

# ERROR cc9b81fc-2945 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:197:40`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const inputIndex = draft.inputs!.findIndex((i) => i?.name === input.name);`

# ERROR cc9b81fc-2946 no-casts `packages/plugins/plugin-routine/src/components/TemplateEditor/TemplateForm.tsx:199:23`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `draft.inputs!.splice(inputIndex, 1);`

# ERROR cc9b81fc-2947 no-casts `packages/plugins/plugin-routine/src/components/TriggerEditor/TriggerEditor.stories.tsx:29:13`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `(window as any)[DEBUG_SYMBOL] = { automation };`

# ERROR cc9b81fc-2948 no-casts `packages/plugins/plugin-routine/src/components/TriggerEditor/TriggerEditor.stories.tsx:71:30`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const automation = (window as any)[DEBUG_SYMBOL]?.automation as Routine.Routine | undefined;`

# ERROR cc9b81fc-2949 no-casts `packages/plugins/plugin-script/src/capabilities/compiler.ts:45:56`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `paths: Object.fromEntries(runtimeModules.map((mod: any) => [mod.moduleName, [`./src/${mod.filename}`]])),`

# ERROR cc9b81fc-2950 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:23:8`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `data?: any;`

# ERROR cc9b81fc-2951 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:62:12`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `data?: any;`

# ERROR cc9b81fc-2952 no-casts `packages/plugins/plugin-script/src/components/TestPanel/TestPanel.tsx:102:19`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `} catch (err: any) {`

# ERROR cc9b81fc-2953 no-casts `packages/plugins/plugin-script/src/components/TypescriptEditor/TypescriptEditor.tsx:96:35`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `tsFacet.of({ env: env as any, path: `./src/${id}.ts` }),`

# ERROR cc9b81fc-2954 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.stories.tsx:84:24`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `subject: undefined as any,`

# ERROR cc9b81fc-2955 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:66:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `Obj.update(notebook!, (obj) => {`

# ERROR cc9b81fc-2956 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:86:58`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `setQueryValues((prev) => ({ ...prev, [cell.name!]: objectIds }));`

# ERROR cc9b81fc-2957 no-casts `packages/plugins/plugin-script/src/containers/NotebookArticle/NotebookArticle.tsx:230:23`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `onSuccess: (result: any) => {`

# ERROR cc9b81fc-2958 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:27:60`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const formatted = await format(script.source.target!.content, {`

# ERROR cc9b81fc-2959 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:33:40`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `Obj.update(script.source.target!, (obj) => {`

# ERROR cc9b81fc-2960 no-casts `packages/plugins/plugin-script/src/hooks/format.ts:36:19`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `} catch (err: any) {`

# ERROR cc9b81fc-2961 no-casts `packages/plugins/plugin-script/src/hooks/useCreateAndDeployScriptTemplates.ts:44:42`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `initialTemplateId: template.id as any,`

# ERROR cc9b81fc-2962 no-casts `packages/plugins/plugin-script/src/notebook/compute-graph.ts:76:27`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `getValue(cellId: string): any {`

# ERROR cc9b81fc-2963 no-casts `packages/plugins/plugin-script/src/notebook/eval.ts:104:37`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `declare var console: { log(...args: any[]): void };`

# ERROR cc9b81fc-2964 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:25:9`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `value?: any;`

# ERROR cc9b81fc-2965 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:138:22`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `let value: any;`

# ERROR cc9b81fc-2966 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:219:105`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `type: typeChecker.typeToString(typeChecker.getTypeOfSymbolAtLocation(param, param.valueDeclaration!)),`

# ERROR cc9b81fc-2967 no-casts `packages/plugins/plugin-script/src/notebook/vfs-parser.ts:294:20`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `let value: any;`

# ERROR cc9b81fc-2968 no-casts `packages/plugins/plugin-script/src/skills/functions/deploy.ts:36:93`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const buildResult = yield* Effect.promise(() => bundleFunction({ source: script.source!.target!.content }));`

# ERROR cc9b81fc-2969 no-casts `packages/plugins/plugin-script/src/templates/commentary.ts:227:13`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any),`

# ERROR cc9b81fc-2970 no-casts `packages/plugins/plugin-script/src/templates/data-generator.ts:25:4`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `}: any) => {`

# ERROR cc9b81fc-2971 no-casts `packages/plugins/plugin-script/src/templates/data-generator.ts:39:25`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const objectResult: any = {};`

# ERROR cc9b81fc-2972 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:60:102`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `handler: ({ data: { channelId, queueId, after = DEFAULT_AFTER, pageSize = 5 }, context: { space } }: any) =>`

# ERROR cc9b81fc-2973 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:64:18`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2974 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:67:64`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `try: () => space.queues.queryQueue(DXN.tryMake(queueId)!),`

# ERROR cc9b81fc-2975 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:68:18`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2976 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:83:88`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const messages = yield* rest.getChannelMessages(channelId, options).pipe((res: any) => res.json);`

# ERROR cc9b81fc-2977 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:85:26`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.map((message: any) =>`

# ERROR cc9b81fc-2978 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:97:75`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `try: () => space.queues.insertIntoQueue(DXN.tryMake(queueId)!, queueMessages),`

# ERROR cc9b81fc-2979 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:98:24`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2980 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:100:46`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `yield* Ref.update(newMessages, (n: any) => n + queueMessages.length);`

# ERROR cc9b81fc-2981 no-casts `packages/plugins/plugin-script/src/templates/discord.ts:101:46`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `yield* Ref.update(lastMessage, (m: any) => queueMessages.at(-1));`

# ERROR cc9b81fc-2982 no-casts `packages/plugins/plugin-script/src/templates/email.ts:9:65`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export default async ({ data: { bodyText }, context: { space } }: any) => {`

# ERROR cc9b81fc-2983 no-casts `packages/plugins/plugin-script/src/templates/forex-effect.ts:33:17`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const json: any = yield* HttpClientRequest.get(`https://free.ratesdb.com/v1/rates?from=${from}&to=${to}`).pipe(`

# ERROR cc9b81fc-2984 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:47:81`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `handler: ({ context: { space }, data: { mailboxId, userId, after, pageSize } }: any) =>`

# ERROR cc9b81fc-2985 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:51:18`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2986 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:61:30`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `Effect.flatMap((res: any) => res.json),`

# ERROR cc9b81fc-2987 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:74:18`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2988 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:78:18`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2989 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:94:62`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const from = messageDetails.payload.headers.find((h: any) => h.name === 'From');`

# ERROR cc9b81fc-2990 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:99:50`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `messageDetails.payload.parts?.find((p: any) => p.mimeType === 'text/plain')?.body.data;`

# ERROR cc9b81fc-2991 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:106:65`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const subject = messageDetails.payload.headers.find((h: any) => h.name === 'Subject')?.value;`

# ERROR cc9b81fc-2992 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:123:44`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `yield* Ref.update(newMessages, (n: any) => [object, ...n]);`

# ERROR cc9b81fc-2993 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:133:32`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `Stream.flatMap((batch: any) =>`

# ERROR cc9b81fc-2994 no-casts `packages/plugins/plugin-script/src/templates/gmail.ts:136:24`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `catch: (e: any) => e,`

# ERROR cc9b81fc-2995 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:17:77`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const isScriptDeployed = ({ script, fn }: { script: Script.Script; fn: any }): boolean => {`

# ERROR cc9b81fc-2996 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:54:28`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `source: script.source!.target!.content,`

# ERROR cc9b81fc-2997 no-casts `packages/plugins/plugin-script/src/util/deploy.ts:76:15`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `} catch (err: any) {`

# ERROR cc9b81fc-2998 no-casts `packages/plugins/plugin-script/src/util/functions.ts:23:5`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `fn: any;`

# ERROR cc9b81fc-2999 no-casts `packages/plugins/plugin-script/src/util/functions.ts:41:7`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `meta: any,`

# ERROR cc9b81fc-3000 no-casts `packages/plugins/plugin-search/src/containers/SearchArticle/SearchArticle.stories.tsx:58:75`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(defaultSpace.db, random as any);`

# ERROR cc9b81fc-3001 no-casts `packages/plugins/plugin-search/src/containers/SearchDialog/SearchDialog.stories.tsx:64:75`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(defaultSpace.db, random as any);`

# ERROR cc9b81fc-3002 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:57:41`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const label = Obj.getLabel(object as any);`

# ERROR cc9b81fc-3003 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:78:41`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `export const getStringProperty = (object: any, keys: string[]): string | undefined => {`

# ERROR cc9b81fc-3004 no-casts `packages/plugins/plugin-search/src/hooks/sync.ts:110:27`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const value = (object as any)[key];`

# ERROR cc9b81fc-3005 no-casts `packages/plugins/plugin-search/src/search/exa.ts:182:41`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const sanitizeObjects = (entries: { data: any; schema: Type.AnyObj }[]) => {`

# ERROR cc9b81fc-3006 no-casts `packages/plugins/plugin-sequencer/src/audio/ScorePlayer.ts:192:42`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `return this.#master ? ((this.#master as unknown as { input: AudioNode }).input ?? undefined) : undefined;`

# ERROR cc9b81fc-3007 no-casts `packages/plugins/plugin-sequencer/src/components/SequenceGrid/SequenceGrid.tsx:566:22`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `atoms={atoms as any}`

# ERROR cc9b81fc-3008 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.stories.tsx:86:35`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `applyLeadSheetToScore(score as unknown as MutableScore, document);`

# ERROR cc9b81fc-3009 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:84:26`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const score = snapshot as unknown as Score.Score;`

# ERROR cc9b81fc-3010 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:114:31`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3011 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:122:31`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3012 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:136:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3013 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:155:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3014 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:171:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3015 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:181:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore & { loopStart?: number; loopEnd?: number };`

# ERROR cc9b81fc-3016 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:232:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3017 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:249:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3018 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:287:33`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const mutable = subject as unknown as MutableScore;`

# ERROR cc9b81fc-3019 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:315:37`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const player = playerRef.current!;`

# ERROR cc9b81fc-3020 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:320:37`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const player = playerRef.current!;`

# ERROR cc9b81fc-3021 no-casts `packages/plugins/plugin-sequencer/src/containers/ScoreArticle/ScoreArticle.tsx:326:37`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const player = playerRef.current!;`

# ERROR cc9b81fc-3022 no-casts `packages/plugins/plugin-sequencer/src/operations/write.ts:23:40`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `applyLeadSheetToScore(resolved as unknown as MutableScore, document);`

# ERROR cc9b81fc-3023 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:139:46`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `model.setValue(parseCellIndex(editing!.index), value);`

# ERROR cc9b81fc-3024 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:290:35`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `index: contextMenuOpen![contextMenuAxis] + (operation === 'insert-before' ? 0 : 1),`

# ERROR cc9b81fc-3025 no-casts `packages/plugins/plugin-sheet/src/components/SheetContent/SheetContent.tsx:296:99`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `axisIndex: model.sheet[contextMenuAxis === 'row' ? 'rows' : 'columns'][contextMenuOpen![contextMenuAxis]],`

# ERROR cc9b81fc-3026 no-casts `packages/plugins/plugin-sheet/src/components/SheetToolbar/align.ts:94:38`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `} else if (model.sheet.ranges![index].value === nextRangeEntity.value) {`

# ERROR cc9b81fc-3027 no-casts `packages/plugins/plugin-sheet/src/components/SheetToolbar/SheetToolbar.stories.tsx:22:27`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `component: SheetToolbar as any,`

# ERROR cc9b81fc-3028 no-casts `packages/plugins/plugin-sheet/src/playwright/sheet-manager.ts:67:36`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `sourcePosition: { x: startBox!.width / 2, y: startBox!.height / 2 },`

# ERROR cc9b81fc-3029 no-casts `packages/plugins/plugin-sheet/src/playwright/sheet-manager.ts:68:34`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `targetPosition: { x: endBox!.width / 2, y: endBox!.height / 2 },`

# ERROR cc9b81fc-3030 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:17:26`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const getTauriWindow = (): any => {`

# ERROR cc9b81fc-3031 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:18:29`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const tauri = (globalThis as any).__TAURI__;`

# ERROR cc9b81fc-3032 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:25:24`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `const getTauriCore = (): any => {`

# ERROR cc9b81fc-3033 no-casts `packages/plugins/plugin-simple-layout/src/capabilities/spotlight-dismiss.ts:26:29`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const tauri = (globalThis as any).__TAURI__;`

# ERROR cc9b81fc-3034 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:141:80`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `window.visualViewport ? addEventListener(window.visualViewport, 'scroll' as any, resetScroll) : () => {},`

# ERROR cc9b81fc-3035 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:144:102`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `addEventListener(document, 'scroll', detectContainerScroll as EventListener, { capture: true } as any),`

# ERROR cc9b81fc-3036 no-casts `packages/plugins/plugin-simple-layout/src/components/MobileLayout/MobileLayout.tsx:312:20`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `'keyboard' as any,`

# ERROR cc9b81fc-3037 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:116:5`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `} as any,`

# ERROR cc9b81fc-3038 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:117:16`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `render: (args: any) => <DefaultStory onAction={args.onAction} />,`

# ERROR cc9b81fc-3039 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:151:12`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `args: {} as any,`

# ERROR cc9b81fc-3040 no-casts `packages/plugins/plugin-simple-layout/src/components/SimpleLayout/NavBar.stories.tsx:162:12`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `args: {} as any,`

# ERROR cc9b81fc-3041 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:17:11`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `content?: any;`

# ERROR cc9b81fc-3042 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:26:46`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `dialogContent?: { component: string; props?: any } | null;`

# ERROR cc9b81fc-3043 no-casts `packages/plugins/plugin-simple-layout/src/types/SimpleLayoutCapabilities.ts:34:47`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `popoverContent?: { component: string; props?: any } | { subject: any } | null;`

# ERROR cc9b81fc-3044 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/collections.ts:150:20`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.map((ref: any) => {`

# ERROR cc9b81fc-3045 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/collections.ts:217:20`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `.map((ref: any) => {`

# ERROR cc9b81fc-3046 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/database.ts:313:56`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `const snapshot = get(Entity.atom(mutableSchema as unknown as Entity.Unknown));`

# ERROR cc9b81fc-3047 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:60:66`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`

# ERROR cc9b81fc-3048 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:78:66`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`

# ERROR cc9b81fc-3049 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:97:66`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, allSchemas);`

# ERROR cc9b81fc-3050 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:112:66`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const viewIndex = buildViewIndex(registry.get.bind(registry) as any, { db } as any, userSchemas);`

# ERROR cc9b81fc-3051 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:127:44`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `Annotation.setDictionary(annotations as any, MigrationVersionAnnotation, version);`

# ERROR cc9b81fc-3052 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/shared.test.ts:136:80`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `({ state: { get: () => state }, properties: makeFakeProperties(version) }) as unknown as Space;`

# ERROR cc9b81fc-3053 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/spaces.ts:247:56`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const order: string[] = (spacesOrderSnapshot as any)?.order ?? [];`

# ERROR cc9b81fc-3054 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/spaces.ts:264:63`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `.sort((sortA, sortB) => orderMap.get(sortA.id)! - orderMap.get(sortB.id)!),`

# ERROR cc9b81fc-3055 no-casts `packages/plugins/plugin-space/src/capabilities/app-graph-builder/extensions/spaces.ts:365:45`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `Obj.update(spacesOrder, (spacesOrder: any) => {`

# ERROR cc9b81fc-3056 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:174:49`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const legacyRef = (space.properties as any)[Type.getTypename(Collection.Collection)];`

# ERROR cc9b81fc-3057 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:182:53`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const legacyVersion = (space.properties as any)[`${Migrations.namespace}.version`];`

# ERROR cc9b81fc-3058 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:312:50`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `ephemeral.viewersByObject[id]!.set(identityKey, {`

# ERROR cc9b81fc-3059 no-casts `packages/plugins/plugin-space/src/capabilities/spaces-ready.ts:319:65`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `ephemeral.viewersByIdentity.get(identityKey)!.add(id);`

# ERROR cc9b81fc-3060 no-casts `packages/plugins/plugin-space/src/containers/CreateObjectDialog/CreateObjectDialog.stories.tsx:30:3`

This line is the `as unknown as T` double-cast escape hatch, which bypasses structural type checking entirely by routing through `unknown`, which the `no-casts` rule (error severity) forbids. Fix: fix the source or target types so a direct, single assertion (or a real runtime conversion) type-checks instead. Offending code: `] as unknown as Space[];`

# ERROR cc9b81fc-3061 no-casts `packages/plugins/plugin-space/src/containers/CreateObjectDialog/CreateObjectDialog.stories.tsx:64:79`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `createObject: () => Effect.succeed({ id: 'mock-id', subject: [], object: {} as any }),`

# ERROR cc9b81fc-3062 no-casts `packages/plugins/plugin-space/src/containers/DefaultProperties/DefaultProperties.tsx:48:38`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `createObject: async (values: any, db: Database.Database): Promise<Obj.Unknown> => {`

# ERROR cc9b81fc-3063 no-casts `packages/plugins/plugin-space/src/containers/RecordArticle/RecordArticle.stories.tsx:105:68`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const factory = createObjectFactory(space.db, random as any);`

# ERROR cc9b81fc-3064 no-casts `packages/plugins/plugin-space/src/operations/add-object.ts:20:35`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const target = input.target as any;`

# ERROR cc9b81fc-3065 no-casts `packages/plugins/plugin-space/src/operations/add-object.ts:30:47`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `const typename = Obj.getTypename(object)!;`

# ERROR cc9b81fc-3066 no-casts `packages/plugins/plugin-space/src/operations/delete-field.ts:18:31`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const view = input.view as any;`

# ERROR cc9b81fc-3067 no-casts `packages/plugins/plugin-space/src/operations/restore-field.ts:18:31`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const view = input.view as any;`

# ERROR cc9b81fc-3068 no-casts `packages/plugins/plugin-space/src/operations/snapshot.ts:15:27`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const db = input.db as any;`

# ERROR cc9b81fc-3069 no-casts `packages/plugins/plugin-space/src/types/SpaceSchema.ts:147:8`

This line types a parameter, variable, or return as `any`, opting that value out of type checking, which the `no-casts` rule (error severity) forbids. Fix: replace `any` with the real type, a generic, or `unknown` plus a narrowing check. Offending code: `props: any,`

# ERROR cc9b81fc-3070 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:115:32`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const objId = (obj as any).id as string;`

# ERROR cc9b81fc-3071 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:243:31`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `if (obj && (obj as any).id === id) {`

# ERROR cc9b81fc-3072 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:422:30`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const objId = (obj as any).id as string;`

# ERROR cc9b81fc-3073 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:479:49`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const toolCtx = (toolManagerRef.current as any)?._ctx;`

# ERROR cc9b81fc-3074 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:487:82`

This line uses the non-null assertion `!` to assert a value is non-null without the compiler verifying it, which will throw at runtime if that assumption is ever wrong, which the `no-casts` rule (error severity) forbids. Fix: add a real null/undefined check, a default, or narrow the type at its source instead of asserting past it. Offending code: `.map((objectId) => ({ objectId, mesh: meshesRef.current.get(objectId)! }))`

# ERROR cc9b81fc-3075 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeCanvas/SpacetimeCanvas.tsx:579:18`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `id: (obj as any).id as string,`

# ERROR cc9b81fc-3076 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/import-export.ts:47:29`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const objId = (object as any).id as string | undefined;`

# ERROR cc9b81fc-3077 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:139:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `if (obj && (obj as any).id === selectedObjectId && (obj as any).color) {`

# ERROR cc9b81fc-3078 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:141:41`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `updateEditorState({ hue: (obj as any).color });`

# ERROR cc9b81fc-3079 no-casts `packages/plugins/plugin-spacetime/src/components/SpacetimeEditor/SpacetimeEditor.tsx:159:25`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `if (obj && (obj as any).id === selectedObjectId) {`

# ERROR cc9b81fc-3080 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/add-object.ts:56:27`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const objId = (object as any).id as string | undefined;`

# ERROR cc9b81fc-3081 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/delete-objects.ts:30:69`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);`

# ERROR cc9b81fc-3082 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/join-objects.ts:63:69`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);`

# ERROR cc9b81fc-3083 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/join-objects.ts:78:33`

This line casts to `any`, silencing the type checker instead of fixing the underlying type, which the `no-casts` rule (error severity) forbids. Fix: give the value (or the API it comes from) a precise type so the cast is unnecessary. Offending code: `const newObjId = (newObject as any).id as string;`

# ERROR cc9b81fc-3084 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/subtract-objects.ts:63:69`

`const index = scene.objects.findIndex((ref) => (ref?.target as any)?.id === objId);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3085 no-casts `packages/plugins/plugin-spacetime/src/tools/actions/subtract-objects.ts:78:33`

`const newObjId = (newObject as any).id as string;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3086 no-casts `packages/plugins/plugin-stack/src/containers/index.ts:7:41`

`export const StackArticle: ComponentType<any> = lazy(() => import('./StackArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3087 no-casts `packages/plugins/plugin-status-bar/src/components/StatusBar/StatusBar.stories.tsx:33:24`

`component: StatusBar as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3088 no-casts `packages/plugins/plugin-status-bar/src/containers/index.ts:7:45`

`export const StatusBarActions: ComponentType<any> = lazy(() => import('./StatusBarActions'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3089 no-casts `packages/plugins/plugin-status-bar/src/containers/index.ts:8:43`

`export const StatusBarPanel: ComponentType<any> = lazy(() => import('./StatusBarPanel'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3090 no-casts `packages/plugins/plugin-status-bar/src/containers/index.ts:9:42`

`export const VersionNumber: ComponentType<any> = lazy(() => import('./VersionNumber'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3091 no-casts `packages/plugins/plugin-studio/src/containers/index.ts:7:44`

`export const ArtifactArticle: ComponentType<any> = lazy(() => import('./ArtifactArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3092 no-casts `packages/plugins/plugin-studio/src/containers/index.ts:8:45`

`export const ArtifactsArticle: ComponentType<any> = lazy(() => import('./ArtifactsArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3093 no-casts `packages/plugins/plugin-studio/src/containers/index.ts:9:43`

`export const GalleryArticle: ComponentType<any> = lazy(() => import('./GalleryArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3094 no-casts `packages/plugins/plugin-studio/src/containers/index.ts:10:44`

`export const LightboxArticle: ComponentType<any> = lazy(() => import('./LightboxArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3095 no-casts `packages/plugins/plugin-support/src/containers/FeedbackPanel/FeedbackPanel.stories.tsx:30:39`

`captureUserFeedback: async (form: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3096 no-casts `packages/plugins/plugin-support/src/containers/FeedbackPanel/FeedbackPanel.stories.tsx:36:6`

`}) as unknown as Observability.Observability;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3097 no-casts `packages/plugins/plugin-support/src/containers/index.ts:7:41`

`export const DiscordPanel: ComponentType<any> = lazy(() => import('./DiscordPanel'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3098 no-casts `packages/plugins/plugin-support/src/containers/index.ts:8:42`

`export const FeedbackPanel: ComponentType<any> = lazy(() => import('./FeedbackPanel'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3099 no-casts `packages/plugins/plugin-support/src/containers/index.ts:9:37`

`export const HelpMenu: ComponentType<any> = lazy(() => import('./HelpMenu'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3100 no-casts `packages/plugins/plugin-support/src/containers/index.ts:10:51`

`export const ShortcutsDialogContent: ComponentType<any> = lazy(() => import('./ShortcutsDialogContent'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3101 no-casts `packages/plugins/plugin-support/src/containers/index.ts:11:43`

`export const ShortcutsHints: ComponentType<any> = lazy(() => import('./ShortcutsHints'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3102 no-casts `packages/plugins/plugin-support/src/containers/index.ts:12:42`

`export const ShortcutsList: ComponentType<any> = lazy(() => import('./ShortcutsList'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3103 no-casts `packages/plugins/plugin-support/src/containers/index.ts:13:45`

`export const SpaceHomeWelcome: ComponentType<any> = lazy(() => import('./SpaceHomeWelcome'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3104 no-casts `packages/plugins/plugin-support/src/containers/index.ts:14:43`

`export const SupportArticle: ComponentType<any> = lazy(() => import('./SupportArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3105 no-casts `packages/plugins/plugin-support/src/containers/index.ts:15:45`

`export const SupportCompanion: ComponentType<any> = lazy(() => import('./SupportCompanion'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3106 no-casts `packages/plugins/plugin-support/src/containers/index.ts:16:44`

`export const SupportSettings: ComponentType<any> = lazy(() => import('./SupportSettings'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3107 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:68:29`

`(_row: number, objects: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3108 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:102:30`

`(actionId: string, data: any) =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3109 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:117:38`

`(schema: Type.AnyEntity, values: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3110 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:160:47`

`const { props } = model.projection!.getFieldProjection(field.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3111 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:192:11`

`(row: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3112 no-casts `packages/plugins/plugin-table/src/containers/TableArticle/TableArticle.tsx:260:39`

`.map((order) => Order.property<any>(order.property, order.direction));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3113 no-casts `packages/plugins/plugin-tasks/src/containers/index.ts:7:43`

`export const JournalArticle: ComponentType<any> = lazy(() => import('./JournalArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3114 no-casts `packages/plugins/plugin-tasks/src/containers/index.ts:8:40`

`export const OutlineCard: ComponentType<any> = lazy(() => import('./OutlineCard'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3115 no-casts `packages/plugins/plugin-tasks/src/containers/index.ts:9:43`

`export const OutlineArticle: ComponentType<any> = lazy(() => import('./OutlineArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3116 no-casts `packages/plugins/plugin-tasks/src/containers/index.ts:10:45`

`export const QuickEntryDialog: ComponentType<any> = lazy(() => import('./QuickEntryDialog'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3117 no-casts `packages/plugins/plugin-tasks/src/containers/index.ts:11:43`

`export const TaskSetArticle: ComponentType<any> = lazy(() => import('./TaskSetArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3118 no-casts `packages/plugins/plugin-tasks/src/types/Journal.ts:69:18`

`db: { add: (obj: any) => any },` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3119 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:23:39`

`dialogOverlayStyle?: Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3120 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:25:17`

`dialogContent?: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3121 no-casts `packages/plugins/plugin-testing/src/types/StorybookCapabilities.ts:34:18`

`popoverContent?: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3122 no-casts `packages/plugins/plugin-thread/src/containers/index.ts:7:47`

`export const ChannelCreatePanel: ComponentType<any> = lazy(() => import('./ChannelCreatePanel'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3123 no-casts `packages/plugins/plugin-thread/src/containers/index.ts:8:43`

`export const ChannelArticle: ComponentType<any> = lazy(() => import('./ChannelArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3124 no-casts `packages/plugins/plugin-thread/src/containers/index.ts:9:42`

`export const ThreadArticle: ComponentType<any> = lazy(() => import('./ThreadArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3125 no-casts `packages/plugins/plugin-thread/src/containers/ThreadArticle/ThreadArticle.stories.tsx:55:35`

`component: ThreadComponent.Root as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3126 no-casts `packages/plugins/plugin-thread/src/hooks/useStatus.ts:23:26`

`setProcessing((prev: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3127 no-casts `packages/plugins/plugin-thread/src/util.ts:18:34`

`data?: { readonly [key: string]: any };` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3128 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:193:43`

`actions: (_editor: Editor, actions: any, _helpers: any) => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3129 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:205:39`

`tools: (_editor: Editor, tools: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3130 no-casts `packages/plugins/plugin-tldraw/src/components/Canvas/Canvas.tsx:229:24`

`Toolbar: (props: any) => (` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3131 no-casts `packages/plugins/plugin-tldraw/src/containers/index.ts:7:42`

`export const TldrawArticle: ComponentType<any> = lazy(() => import('./TldrawArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3132 no-casts `packages/plugins/plugin-tldraw/src/containers/index.ts:8:39`

`export const TldrawCard: ComponentType<any> = lazy(() => import('./TldrawCard'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3133 no-casts `packages/plugins/plugin-tldraw/src/migrations/migrate.ts:15:110`

`export type MigrateCanvas = (current: SerializedStore<TLRecord>, version?: number) => Promise<SerializedStore<any>>…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3134 no-casts `packages/plugins/plugin-tldraw/src/model/builder.ts:27:24`

`const isShape = (record: any) => record?.typeName === 'shape';` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3135 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:16:35`

`type ShapeRecord = Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3136 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:171:13`

`.sort((a: any, b: any) => Number(String(a.index).slice(1)) - Number(String(b.index).slice(1)))` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3137 no-casts `packages/plugins/plugin-tldraw/src/model/read.ts:172:16`

`.map((point: any) => ({ x: point.x, y: point.y }));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3138 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:25:57`

`const shapes = Object.values(content).filter((record: any) => record.typeName === 'shape');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3139 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:26:59`

`const bindings = Object.values(content).filter((record: any) => record.typeName === 'binding');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3140 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:29:25`

`expect((bindings[0] as any).toId).to.eq('shape:a');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3141 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:38:16`

`const arrow: any = content['shape:arrow'];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3142 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:39:15`

`const line: any = content['shape:line'];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3143 no-casts `packages/plugins/plugin-tldraw/src/model/RecordBuilder.test.ts:43:49`

`expect(Object.values(content).filter((record: any) => record.typeName === 'binding')).to.have.length(0);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3144 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:101:49`

`for (const record of Object.values(content) as any[]) {` casts with `as any`, suppressing the type checker entirely, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3145 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:107:35`

`const content: Record<string, any> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3146 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:152:35`

`const content: Record<string, any> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3147 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:173:35`

`const content: Record<string, any> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3148 no-casts `packages/plugins/plugin-tldraw/src/model/scene.test.ts:195:35`

`const content: Record<string, any> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3149 no-casts `packages/plugins/plugin-transcription/src/containers/index.ts:7:49`

`export const TranscriptionArticle: ComponentType<any> = lazy(() => import('./TranscriptionArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3150 no-casts `packages/plugins/plugin-transcription/src/operations/summarize.ts:62:13`

`} as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3151 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:23:61`

`export const enableQueryIndexes = (services: { QueryService?: any }) =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3152 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:26:28`

`services.QueryService!.setConfig({` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3153 no-casts `packages/plugins/plugin-transcription/src/testing/decorators.ts:36:54`

`yield* Effect.promise(() => services.QueryService!.reindex());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3154 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:16:20`

`(env.backends.onnx as any).wasm.numThreads = 1;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3155 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:17:20`

`(env.backends.onnx as any).provider = 'webgpu';` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3156 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:18:20`

`(env.backends.onnx as any).webgpu = {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3157 no-casts `packages/plugins/plugin-transformer/src/hooks/usePipeline.ts:89:39`

`progress_callback: (progress: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3158 no-casts `packages/plugins/plugin-transformer/src/testing/pipeline.ts:26:59`

`protected abstract generateText(prompt: string): Promise<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3159 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:136:42`

`invoke: () => Effect.succeed(undefined as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3160 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:138:58`

`invokePromise: () => Promise.resolve({ data: undefined as any }),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3161 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:212:70`

`const boardA = discovered.targets.find((t) => t.id === 'board-a')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3162 no-casts `packages/plugins/plugin-trello/src/operations/handlers.test.ts:255:45`

`const fetchLists = trelloApi.fetchLists as unknown as ReturnType<typeof vi.fn>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3163 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:50:7`

`db: any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3164 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:68:25`

`const makeKanban = (db: any) =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3165 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:125:72`

`const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3166 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:242:18`

`(localItem as unknown as Record<string, unknown>).description = 'local edit';` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3167 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:257:28`

`const item = localItem as unknown as Record<string, unknown>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3168 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:282:18`

`(localItem as unknown as Record<string, unknown>).description = 'local edit';` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3169 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:296:28`

`const item = localItem as unknown as Record<string, unknown>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3170 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:360:7`

`db: any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3171 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:414:72`

`const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3172 no-casts `packages/plugins/plugin-trello/src/operations/sync.test.ts:464:72`

`const snapshots = (binding.spec.snapshots ?? {}) as Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3173 no-casts `packages/plugins/plugin-trello/src/operations/sync.ts:234:26`

`cardsByList.get(ln)!.push(card);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3174 no-casts `packages/plugins/plugin-trello/src/operations/sync.ts:521:70`

`const toastIdSuffix = EID.getEntityId(EID.tryParse(binding.uri)!) ?? 'unknown';` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3175 no-casts `packages/plugins/plugin-trip/src/capabilities/app-graph-builder.ts:64:63`

`const target = Ref.isRef(ref) ? ref.target : (ref as unknown as Segment.Segment | undefined);` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3176 no-casts `packages/plugins/plugin-trip/src/containers/index.ts:9:43`

`export const SegmentArticle: ComponentType<any> = lazy(() => import('./SegmentArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3177 no-casts `packages/plugins/plugin-trip/src/containers/index.ts:10:40`

`export const TripArticle: ComponentType<any> = lazy(() => import('./TripArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3178 no-casts `packages/plugins/plugin-trip/src/containers/SegmentArticle/SegmentArticle.tsx:40:45`

`const value = Obj.getValue(values as any, parts);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3179 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:304:42`

`const updatedSegment = second.updated!.find((obj) => Obj.instanceOf(Segment.Segment, obj)) as Segment.Segment;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3180 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:363:27`

`expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3181 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:393:27`

`expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3182 no-casts `packages/plugins/plugin-trip/src/operations/extractor/trip-extractor.test.ts:417:27`

`expect((second.updated![0] as Trip.Trip).id).toBe(trip.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3183 no-casts `packages/plugins/plugin-trip/src/testing/routing.ts:95:70`

`const geo = coords[normalized] ?? coords[normalized.split(',')[0]!.trim()];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3184 no-casts `packages/plugins/plugin-video/src/components/Transcript/Transcript.stories.tsx:43:25`

`component: Transcript as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3185 no-casts `packages/plugins/plugin-video/src/containers/index.ts:7:41`

`export const VideoArticle: ComponentType<any> = lazy(() => import('./VideoArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3186 no-casts `packages/plugins/plugin-video/src/containers/index.ts:8:41`

`export const VideoSection: ComponentType<any> = lazy(() => import('./VideoSection'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3187 no-casts `packages/plugins/plugin-video/src/containers/index.ts:9:46`

`export const TranscriptSection: ComponentType<any> = lazy(() => import('./TranscriptSection'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3188 no-casts `packages/plugins/plugin-video/src/containers/index.ts:10:43`

`export const SummarySection: ComponentType<any> = lazy(() => import('./SummarySection'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3189 no-casts `packages/plugins/plugin-video/src/operations/summarize.ts:73:13`

`} as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3190 no-casts `packages/plugins/plugin-voxel/src/components/VoxelEditor/VoxelEditor.tsx:252:29`

`const controlsRef = useRef<any>(null);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3191 no-casts `packages/plugins/plugin-voxel/src/containers/index.ts:7:41`

`export const VoxelArticle: ComponentType<any> = lazy(() => import('./VoxelArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3192 no-casts `packages/plugins/plugin-voxel/src/containers/index.ts:8:38`

`export const VoxelCard: ComponentType<any> = lazy(() => import('./VoxelCard'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3193 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:50:41`

`const nextVoxels = lifeRef.current!.tick(currentVoxels);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3194 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:51:49`

`updateVoxels(Voxel.toVoxelMap(nextVoxels) as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3195 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:67:48`

`return { [key]: { hue: voxel.hue } } as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3196 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:101:44`

`return Voxel.toVoxelMap(newVoxels) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3197 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:116:36`

`const pattern = lifeRef.current!.randomPattern();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3198 no-casts `packages/plugins/plugin-voxel/src/containers/VoxelArticle/VoxelArticle.tsx:117:44`

`updateVoxels(Voxel.toVoxelMap(pattern) as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3199 no-casts `packages/plugins/plugin-voxel/src/types/Voxel.ts:102:42`

`voxels: voxels ? (toVoxelMap(voxels) as any) : {},` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3200 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:28:33`

`const space = { id: spaceId } as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3201 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:29:93`

`const client = { spaces: { get: (id: SpaceId) => (id === spaceId ? space : undefined) } } as unknown as Client;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3202 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:30:25`

`const blockstore = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3203 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:64:26`

`const directory = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3204 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:65:23`

`const forest = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3205 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:86:63`

`vi.mocked(loadWnfs).mockResolvedValueOnce({ directory: {} as any, forest: {} as any });` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3206 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:96:26`

`const directory = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3207 no-casts `packages/plugins/plugin-wnfs/src/capabilities/blob-backend.test.ts:97:23`

`const forest = {} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3208 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:34:37`

`const propsAny = space.properties as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3209 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:37:19`

`delete (obj as any)['wnfs_access_key'];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3210 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:38:19`

`delete (obj as any)['wnfs_private_forest_cid'];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3211 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:45:26`

`delete (properties as any)['wnfs'];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3212 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:50:48`

`const cacheKey = wnfsState?.privateForestCid as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3213 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:60:26`

`?.privateForestCid as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3214 no-casts `packages/plugins/plugin-wnfs/src/helpers/load.ts:103:102`

`const wnfsState = Annotation.get(space.properties, WnfsStateAnnotation).pipe(Option.getOrUndefined)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3215 no-casts `packages/plugins/plugin-zen/src/components/Editor/Editor.tsx:22:12`

`(values: any, { changed }: { changed: Record<string, boolean> }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3216 no-casts `packages/plugins/plugin-zen/src/containers/index.ts:7:39`

`export const ZenArticle: ComponentType<any> = lazy(() => import('./ZenArticle'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3217 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:181:25`

`this._leftOscillator!.frequency.setTargetAtTime(baseFrequency, now, rampTime);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3218 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:182:26`

`this._rightOscillator!.frequency.setTargetAtTime(baseFrequency + beatFrequency, now, rampTime);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3219 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:183:21`

`this._masterGain!.gain.setTargetAtTime(volume, now, rampTime);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3220 no-casts `packages/plugins/plugin-zen/src/generator/generator.ts:223:45`

`this._noiseGain.connect(this._masterGain!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3221 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:227:46`

`transport = transports.get(sessionId)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3222 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:236:41`

`transports.set(id, transport!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3223 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:240:24`

`if (transport!.sessionId) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3224 no-casts `packages/reflect/introspect-mcp/src/server/main.ts:241:40`

`transports.delete(transport!.sessionId);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3225 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:90:42`

`const payload = parseToolText(result as any) as { data: Array<{ name: string }> };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3226 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:101:42`

`const payload = parseToolText(result as any) as { data: Array<{ name: string }> };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3227 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:110:42`

`const payload = parseToolText(result as any) as {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3228 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:122:42`

`const payload = parseToolText(result as any) as { data: null; note: string };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3229 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:132:42`

`const payload = parseToolText(result as any) as { data: Array<{ name: string }> };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3230 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:143:42`

`const payload = parseToolText(result as any) as { data: Array<{ ref: string }> };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3231 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:152:42`

`const payload = parseToolText(result as any) as {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3232 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:164:42`

`const payload = parseToolText(result as any) as { data: { source?: string } | null };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3233 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:176:44`

`const payload = parseToolText(result as any) as { data: unknown[] };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3234 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:189:42`

`const payload = parseToolText(result as any) as { data: Array<unknown>; truncated?: string };` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3235 no-casts `packages/reflect/introspect-mcp/src/server/server.test.ts:199:11`

`})) as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3236 no-casts `packages/reflect/introspect-mcp/src/tools/tools.ts:90:29`

`inputSchema: Schema.Struct<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3237 no-casts `packages/reflect/introspect-mcp/src/tools/tools.ts:106:33`

`): Record<string, ToolDefinition<any>> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3238 no-casts `packages/reflect/introspect-tools/src/metadata.ts:66:29`

`inputSchema: Schema.Struct<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3239 no-casts `packages/reflect/introspect-tools/src/metadata.ts:75:30`

`outputSchema: Schema.Struct<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3240 no-casts `packages/reflect/introspect-tools/src/metadata.ts:209:55`

`export const inputSchemaToZod = (struct: Schema.Struct<any>): Record<string, z.ZodTypeAny> => effectFieldsToZod(str…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3241 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:30:16`

`expect(code!.package).toBe('@dxos/plugin-code');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3242 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:32:30`

`expect(Array.isArray(code!.tags)).toBe(true);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3243 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:33:30`

`expect(Array.isArray(code!.dependsOn)).toBe(true);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3244 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:37:16`

`expect(code!.dependsOn).toEqual(expect.arrayContaining(['org.dxos.plugin.graph', 'org.dxos.plugin.space']));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3245 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:43:21`

`expect(assistant!.tags).toEqual([]);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3246 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:73:17`

`expect(chess!.description).toMatch(/Full-featured chess game/);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3247 no-casts `packages/reflect/introspect/src/__sanity__/plugins.test.ts:75:17`

`expect(chess!.tags).toEqual(['game']);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3248 no-casts `packages/reflect/introspect/src/__sanity__/real-monorepo.test.ts:30:25`

`expect(introspectPkg!.entryPoints[0]).toContain('src/index.ts');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3249 no-casts `packages/reflect/introspect/src/__sanity__/real-monorepo.test.ts:37:18`

`expect(detail!.kind).toBe('function');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3250 no-casts `packages/reflect/introspect/src/indexer/symbols.ts:213:33`

`const initializer = (decl as unknown as { getInitializer?: () => Node | undefined }).getInitializer?.();` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3251 no-casts `packages/reflect/introspect/src/indexer/symbols.ts:244:30`

`const candidate = cursor as unknown as { getJsDocs?: () => JSDoc[] };` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3252 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:58:18`

`expect(detail!.workspaceDependencies).toEqual(['@dxos/echo-react', '@fixture/pkg-a']);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3253 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:59:18`

`expect(detail!.externalDependencies).toEqual(['react']);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3254 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:60:18`

`expect(detail!.entryPoints).toEqual(['src/index.ts']);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3255 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:71:21`

`expect(taskMatch!.summary).toContain('Task item');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3256 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:78:16`

`expect(make!.kind).toBe('function');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3257 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:79:16`

`expect(make!.summary).toContain('factory');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3258 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:124:18`

`expect(detail!.signature).toContain('Task');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3259 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:125:18`

`expect(detail!.location.file).toContain('pkg-a/src/Task.ts');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3260 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:126:18`

`expect(detail!.location.line).toBeGreaterThan(0);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3261 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:127:18`

`expect(detail!.summary).toContain('Task item');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3262 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:128:18`

`expect(detail!.source).toBeUndefined();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3263 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:133:18`

`expect(detail!.source).toContain('Schema.Struct');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3264 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:134:18`

`expect(detail!.source).toContain('Type.makeObject');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3265 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:135:18`

`expect(detail!.source).toContain('com.example.type.Task');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3266 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:137:18`

`expect(detail!.source).toContain("description: 'Short summary of the task.'");` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3267 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:139:18`

`expect(detail!.source).toContain("LabelAnnotation.set(['title'])");` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3268 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:146:18`

`expect(detail!.source).toContain('export class Task');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3269 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:147:18`

`expect(detail!.source).toContain('Type.makeObject');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3270 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:153:18`

`expect(detail!.source).toContain('useObject');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3271 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:154:18`

`expect(detail!.location.file).toContain('pkg-b/src/TaskCard/TaskCard.tsx');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3272 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:167:23`

`expect(taskFactory!.applies).toContain('Task');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3273 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:168:23`

`expect(taskFactory!.host.kind).toBe('symbol');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3274 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:169:23`

`expect(taskFactory!.host.symbol).toBe('make');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3275 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:170:23`

`expect(taskFactory!.host.file).toContain('pkg-a/src/Task.ts');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3276 no-casts `packages/reflect/introspect/src/introspector/introspector.test.ts:171:23`

`expect(taskFactory!.uses).toEqual(['{@link Task}']);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3277 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:23:58`

`expect(() => Role.make('org.dxos.test.role.bad-name' as any)).toThrow();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3278 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:27:38`

`expect(() => Role.make('article' as any)).toThrow();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3279 no-casts `packages/sdk/app-framework/src/common/Role.test.ts:31:42`

`expect(() => Role.make('org.article' as any)).toThrow();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3280 no-casts `packages/sdk/app-framework/src/core/capability.ts:163:52`

`export type Implementation<I extends InterfaceDef<any>> = I extends InterfaceDef<infer T> ? T : never;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3281 no-casts `packages/sdk/app-framework/src/core/capability.ts:261:6`

`}) as unknown as A extends 'multi' ? MultiTag<T, S> : Tag<T, S>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3282 no-casts `packages/sdk/app-framework/src/core/capability.ts:284:64`

`: buildTag<unknown, string, 'multi'>(identifier, 'multi')) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3283 no-casts `packages/sdk/app-framework/src/core/capability.ts:299:66`

`: buildTag<unknown, string, 'single'>(identifier, 'single')) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3284 no-casts `packages/sdk/app-framework/src/core/capability.ts:317:29`

`export type Any = Capability<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3285 no-casts `packages/sdk/app-framework/src/core/capability.ts:396:36`

`[ContributionTypeId]: capability as unknown as IdentifierOf<C>,` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3286 no-casts `packages/sdk/app-framework/src/core/capability.ts:409:36`

`[ContributionTypeId]: capability as unknown as IdentifierOf<C>,` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3287 no-casts `packages/sdk/app-framework/src/core/capability.ts:542:65`

`const props = spec.props ? spec.props(options) : (options as unknown as Props);` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3288 no-casts `packages/sdk/app-framework/src/core/capability.ts:575:63`

`const props = spec.props ? spec.props(options) : (options as unknown as Props);` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3289 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:45:70`

`const atomCounter = (registry: Registry.AtomRegistry, atom: Atom.Atom<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3290 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1633:48`

`assert.isFalse(Plugin.isLazy(registered!));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3291 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1634:38`

`assert.strictEqual(registered!.modules.length, 1);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3292 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1736:38`

`assert.isDefined(errorMessage!.error);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3293 no-casts `packages/sdk/app-framework/src/core/plugin-manager/plugin-manager.test.ts:1777:70`

`const BadFactory = Object.assign(() => ({ not: 'a plugin' }) as any, { meta: lazyMeta });` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3294 no-casts `packages/sdk/app-framework/src/core/plugin.ts:142:11`

`props?: any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3295 no-casts `packages/sdk/app-framework/src/core/plugin.ts:160:29`

`readonly activate: (props?: any) => Effect.Effect<any, any, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3296 no-casts `packages/sdk/app-framework/src/core/plugin.ts:183:37`

`? Opts['activate'] extends (props?: any) => Effect.Effect<infer A, infer E, infer R>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3297 no-casts `packages/sdk/app-framework/src/core/plugin.ts:215:29`

`readonly activate: (props?: any) => Effect.Effect<any, Error, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3298 no-casts `packages/sdk/app-framework/src/core/plugin.ts:407:46`

`moduleOrOptionsOrBuilder: Capability.Module<any> | ModuleEntry | ((options: T) => ModuleEntry) | PluginBuilder<T>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3299 no-casts `packages/sdk/app-framework/src/core/plugin.ts:413:65`

`const module = moduleOrOptionsOrBuilder as Capability.Module<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3300 no-casts `packages/sdk/app-framework/src/core/plugin.ts:467:34`

`module: ModuleEntry | ((options: any) => ModuleEntry),` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3301 no-casts `packages/sdk/app-framework/src/core/plugin.ts:468:11`

`options?: any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3302 no-casts `packages/sdk/app-framework/src/core/plugin.ts:539:40`

`type LazyPayload = { loader: LazyLoader<any>; options: unknown };` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3303 no-casts `packages/sdk/app-framework/src/core/plugin.ts:635:41`

`const { loader, options } = (plugin as unknown as { [LazyTag]: LazyPayload })[LazyTag];` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3304 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/history-tracker.test.ts:17:54`

`const testRuntime = ManagedRuntime.make(Layer.empty) as unknown as ManagedRuntime.ManagedRuntime<any, any>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3305 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:12:8`

`input: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3306 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:13:9`

`output: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3307 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/types.ts:15:15`

`inverseInput: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3308 no-casts `packages/sdk/app-framework/src/plugin-process-manager/history/undo-registry.ts:19:24`

`deriveContext: (input: any, output: any) => any | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3309 no-casts `packages/sdk/app-framework/src/plugin-process-manager/process-manager-capability.ts:248:86`

`Effect.flatMap(ProcessManager.ProcessOperationInvoker.Service, Effect.succeed) as unknown as Effect.Effect<` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3310 no-casts `packages/sdk/app-framework/src/testing/harness.ts:231:51`

`const result = await invoker.invokePromise(op as any, ...(args as [any]));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3311 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:75:26`

`spyFn?: (impl: (...args: any[]) => any) => any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3312 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:103:28`

`return delegate()!.invocations;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3313 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:106:28`

`return delegate()!.pendingFollowups;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3314 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:109:28`

`return delegate()!.awaitFollowups;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3315 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:111:43`

`invoke: (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3316 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:114:45`

`return Effect.succeed(undefined as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3317 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:116:29`

`return (delegate()!.invoke as any)(op, input, ...rest);` casts with `as any`, suppressing the type checker entirely, and also uses the non-null assertion `!` to bypass the type checker's null/undefined check — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3318 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:118:56`

`invokePromise: async (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3319 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:123:29`

`return (delegate()!.invokePromise as any)(op, input, ...rest);` casts with `as any`, suppressing the type checker entirely, and also uses the non-null assertion `!` to bypass the type checker's null/undefined check — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3320 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:125:45`

`schedule: (op: Operation.Definition<any, any>, input?: unknown, ...rest: any[]) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3321 no-casts `packages/sdk/app-framework/src/testing/operationCapture.ts:130:29`

`return (delegate()!.schedule as any)(op, input, ...rest);` casts with `as any`, suppressing the type checker entirely, and also uses the non-null assertion `!` to bypass the type checker's null/undefined check — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3322 no-casts `packages/sdk/app-framework/src/testing/react.tsx:40:56`

`export type RenderSurfaceProps<TToken extends Role.Role<any>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3323 no-casts `packages/sdk/app-framework/src/testing/react.tsx:44:38`

`fallback?: FC<{ error: Error; data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3324 no-casts `packages/sdk/app-framework/src/testing/react.tsx:51:55`

`export const renderSurface = <TToken extends Role.Role<any>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3325 no-casts `packages/sdk/app-framework/src/testing/react.tsx:59:46`

`<SurfaceComponent type={type} data={data as any} limit={limit} fallback={fallback} placeholder={placeholder} />,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3326 no-casts `packages/sdk/app-framework/src/testing/withPluginManager.stories.tsx:64:13`

`(window as any).__ERROR_BOUNDARY_ERRORS__ = [];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3327 no-casts `packages/sdk/app-framework/src/testing/withPluginManager.tsx:100:63`

`const options = typeof init === 'function' ? init(context as any) : init;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3328 no-casts `packages/sdk/app-framework/src/ui/components/PluginManager/PluginManagerContext.stories.tsx:30:33`

`const counter = (capabilities as any)[0];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3329 no-casts `packages/sdk/app-framework/src/ui/components/PluginManager/PluginManagerContext.stories.tsx:71:33`

`const counter = (capabilities as any)[0];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3330 no-casts `packages/sdk/app-framework/src/ui/components/Surface/index.ts:45:50`

`export type TypedProps<TToken extends Role.Role<any>> = SurfaceTypedProps<TToken>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3331 no-casts `packages/sdk/app-framework/src/ui/components/Surface/index.ts:48:55`

`export type ComponentProps<T extends Record<string, any> = Record<string, any>> = SurfaceComponentPropsType<T>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3332 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.test.tsx:193:64`

`const IsAvailableProbe = ({ role, testId }: { role: Role.Role<any>; testId: string }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3333 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:75:25`

`data?: Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3334 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:78:16`

`[key: string]: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3335 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:114:50`

`const previousPropsRef = useRef<Record<string, any>>({});` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3336 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:139:25`

`data?: Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3337 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:141:38`

`fallback?: FC<{ error: Error; data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3338 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:143:16`

`[key: string]: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3339 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:224:26`

`}: TypedProps<Role.Role<any>>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3340 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:292:32`

`) as (<TToken extends Role.Role<any>>(props: TypedProps<TToken>) => React.ReactNode) &` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3341 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:293:44`

`NamedExoticComponent<TypedProps<Role.Role<any>>>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3342 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:344:52`

`type IsSurfaceAvailable = <TToken extends Role.Role<any>>(args: { type: TToken; data?: Role.Data<TToken> }) => bool…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3343 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceComponent.tsx:357:29`

`(args: { type: Role.Role<any>; data?: Props['data'] }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3344 no-casts `packages/sdk/app-framework/src/ui/components/Surface/SurfaceDebug.tsx:373:24`

`const node = element as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3345 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:20:41`

`const filter: Filter<Record<string, any>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3346 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:32:44`

`const token = Role.make<Record<string, any>>('org.dxos.test.role.article');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3347 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:41:45`

`const tokenA = Role.make<Record<string, any>>('org.dxos.test.role.article');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3348 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:42:45`

`const tokenB = Role.make<Record<string, any>>('org.dxos.test.role.section');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3349 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:43:41`

`const filter: Filter<Record<string, any>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3350 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:45:53`

`{ role: tokenA.role, guard: (data) => (data as any).subject === 'a' },` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3351 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:46:53`

`{ role: tokenB.role, guard: (data) => (data as any).subject === 's' },` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3352 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:62:44`

`const token = Role.make<Record<string, any>>('org.dxos.test.role.r');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3353 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:63:41`

`const filter: Filter<Record<string, any>> = { bindings: [{ role: token.role, guard: () => true }] };` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3354 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:69:44`

`const token = Role.make<Record<string, any>>('org.dxos.test.role.article');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3355 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.test.ts:76:42`

`const token = Role.make<Record<string, any>>('org.dxos.test.role.article');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3356 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:37:58`

`export const isFilter = (value: unknown): value is Filter<any> =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3357 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:70:44`

`export type Props<T extends Record<string, any> = Record<string, unknown>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3358 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:75:38`

`fallback?: FC<{ error: Error; data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3359 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:90:48`

`export type TypedProps<TToken extends Role.Role<any>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3360 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:91:38`

`fallback?: FC<{ error: Error; data?: any }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3361 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:102:30`

`[K in keyof Record<string, any>]: K extends 'fallback' | 'placeholder' | 'id' | 'type' | 'data' | 'limit'` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3362 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:104:5`

`: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3363 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:112:48`

`export type CoreProps<T extends Record<string, any> = Record<string, unknown>> = PropsWithChildren<{` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3364 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:135:53`

`export type ComponentProps<T extends Record<string, any> = Record<string, any>> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3365 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:147:18`

`Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3366 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:152:56`

`export type ComponentFunction<T extends Record<string, any> = Record<string, any>> = (` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3367 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:166:54`

`export type ReactDefinition<T extends Record<string, any> = any> = Readonly<{` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3368 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:171:27`

`component: ComponentType<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3369 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:173:56`

`props?: (props: ComponentProps<T>) => Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3370 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:183:61`

`export type WebComponentDefinition<T extends Record<string, any> = any> = Readonly<{` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3371 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:199:49`

`export type Definition<T extends Record<string, any> = any> = ReactDefinition<T> | WebComponentDefinition<T>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3372 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:205:28`

`T extends Record<string, any> = any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3373 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:206:28`

`P extends Record<string, any> = ComponentProps<T>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3374 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:241:66`

`export type TypedWebComponentDefinition<T extends Record<string, any> = any, Id extends string = string> = Readonly…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3375 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:250:50`

`const expandBindings = <T extends Record<string, any>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3376 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:270:28`

`T extends Record<string, any> = any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3377 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:271:28`

`P extends Record<string, any> = ComponentProps<T>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3378 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:275:28`

`T extends Record<string, any> = any,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3379 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:276:28`

`P extends Record<string, any> = ComponentProps<T>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3380 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:287:52`

`export function createWeb<T extends Record<string, any> = any, const Id extends string = string>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3381 no-casts `packages/sdk/app-framework/src/ui/components/Surface/types.ts:290:52`

`export function createWeb<T extends Record<string, any> = any, const Id extends string = string>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3382 no-casts `packages/sdk/app-framework/src/ui/hooks/useApp.tsx:212:29`

`(globalThis as any).composer?.profiler?.dump();` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3383 no-casts `packages/sdk/app-framework/src/vite-plugin/boot-loader/loader-app/Loader.solid-stories.tsx:23:11`

`(Story: any) => (` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3384 no-casts `packages/sdk/app-graph/src/atoms.ts:10:70`

`const observableFamily = Atom.family((observable: MulticastObservable<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3385 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:490:43`

`const parent = (child?: Node.NodeArg<any>): Node.NodeArg<any> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3386 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:497:60`

`const inlineChild = (overrides?: Partial<Node.NodeArg<any>>): Node.NodeArg<any> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3387 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:507:49`

`const nodesAtom = Atom.make<Node.NodeArg<any>[]>([]);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3388 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:615:69`

`const withInlineAction = (actionId = 'delete'): Node.NodeArg<any> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3389 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:648:51`

`const withFilterChild = (): Node.NodeArg<any> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3390 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:676:42`

`const filterChild: Node.NodeArg<any> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3391 no-casts `packages/sdk/app-graph/src/graph-builder.test.ts:698:67`

`const makeMultiParent = (childIds: string[]): Node.NodeArg<any> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3392 no-casts `packages/sdk/app-graph/src/graph-builder.ts:46:103`

`export type ConnectorExtension = (node: Atom.Atom<Option.Option<Node.Node>>) => Atom.Atom<Node.NodeArg<any>[]>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3393 no-casts `packages/sdk/app-graph/src/graph-builder.ts:53:49`

`) => Atom.Atom<Omit<Node.NodeArg<Node.ActionData<any>>, 'type' | 'nodes' | 'edges'>[]>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3394 no-casts `packages/sdk/app-graph/src/graph-builder.ts:68:71`

`export type ResolverExtension = (id: string) => Atom.Atom<Node.NodeArg<any> | null>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3395 no-casts `packages/sdk/app-graph/src/graph-builder.ts:81:84`

`connector?: (node: Atom.Atom<Option.Option<Node.Node>>) => Atom.Atom<Node.NodeArg<any>[]>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3396 no-casts `packages/sdk/app-graph/src/graph-builder.ts:204:33`

`export type BuilderNode<TData = any> = Node.Node<TData, { urlSegment?: string } & Record<string, any>>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3397 no-casts `packages/sdk/app-graph/src/graph-builder.ts:212:21`

`node: Node.NodeArg<any>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3398 no-casts `packages/sdk/app-graph/src/graph-builder.ts:215:16`

`): Node.NodeArg<any> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3399 no-casts `packages/sdk/app-graph/src/graph-builder.ts:289:26`

`nodes: Node.NodeArg<any>[];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3400 no-casts `packages/sdk/app-graph/src/graph-builder.ts:298:65`

`readonly _connectorPreviousArgs = new Map<string, Node.NodeArg<any>[]>();` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3401 no-casts `packages/sdk/app-graph/src/graph-builder.ts:323:40`

`_constructNode: (node: Node.NodeArg<any>) => Option.Option<Node.Node>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3402 no-casts `packages/sdk/app-graph/src/graph-builder.ts:343:42`

`_constructNode: (node: Node.NodeArg<any>) => Option.Option<Node.Node>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3403 no-casts `packages/sdk/app-graph/src/graph-builder.ts:364:47`

`private _recordProvenance(node: Node.NodeArg<any>, extensionId: string): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3404 no-casts `packages/sdk/app-graph/src/graph-builder.ts:372:65`

`private _applyConnectorUpdate(key: string, nodes: Node.NodeArg<any>[], previous: string[]): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3405 no-casts `packages/sdk/app-graph/src/graph-builder.ts:426:89`

`private readonly _resolvers = Atom.family<string, Atom.Atom<Option.Option<Node.NodeArg<any>>>>((id) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3406 no-casts `packages/sdk/app-graph/src/graph-builder.ts:441:105`

`private readonly _connectors = Atom.family<string, Atom.Atom<{ extensionId: string; node: Node.NodeArg<any> }[]>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3407 no-casts `packages/sdk/app-graph/src/graph-builder.ts:462:65`

`const entries: { extensionId: string; node: Node.NodeArg<any> }[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3408 no-casts `packages/sdk/app-graph/src/graph-builder.ts:656:43`

`return removeExtensionImpl(builder, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3409 no-casts `packages/sdk/app-graph/src/graph-builder.ts:924:55`

`) => Effect.Effect<Omit<Node.NodeArg<Node.ActionData<any>, any>, 'type'>[], never, R>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3410 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1033:64`

`factory: (data: TData, get: Atom.AtomContext) => Node.NodeArg<any>[],` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3411 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1054:78`

`factory: (data: TData, get: Atom.AtomContext) => Effect.Effect<Node.NodeArg<any>[], never, R>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3412 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1085:55`

`) => Effect.Effect<Omit<Node.NodeArg<Node.ActionData<any>>, 'type'>[], never, R>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3413 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1090:98`

`connector?: (object: Type.InstanceType<T>, get: Atom.AtomContext) => Effect.Effect<Node.NodeArg<any>[], never, R>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3414 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1128:23`

`(nodes: Node.NodeArg<any>[]): Node.NodeArg<any>[] =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3415 no-casts `packages/sdk/app-graph/src/graph-builder.ts:1145:49`

`const collectAllInlineIds = (nodes: Node.NodeArg<any>[]): string[] =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3416 no-casts `packages/sdk/app-graph/src/graph.test.ts:457:13`

`let json: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3417 no-casts `packages/sdk/app-graph/src/graph.ts:103:31`

`json(id?: string): Atom.Atom<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3418 no-casts `packages/sdk/app-graph/src/graph.ts:221:49`

`readonly _json = Atom.family<string, Atom.Atom<any>>((id) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3419 no-casts `packages/sdk/app-graph/src/graph.ts:223:60`

`const toJSON = (node: Node.Node, seen: string[] = []): any => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3420 no-casts `packages/sdk/app-graph/src/graph.ts:225:35`

`const obj: Record<string, any> = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3421 no-casts `packages/sdk/app-graph/src/graph.ts:268:36`

`json(id = Node.RootId): Atom.Atom<any> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3422 no-casts `packages/sdk/app-graph/src/graph.ts:293:36`

`_constructNode(node: Node.NodeArg<any>): Option.Option<Node.Node> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3423 no-casts `packages/sdk/app-graph/src/graph.ts:308:16`

`return graph as unknown as GraphImpl;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3424 no-casts `packages/sdk/app-graph/src/graph.ts:322:65`

`const jsonImpl = (graph: BaseGraph, id = Node.RootId): Atom.Atom<any> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3425 no-casts `packages/sdk/app-graph/src/graph.ts:391:33`

`return getNodeImpl(graph, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3426 no-casts `packages/sdk/app-graph/src/graph.ts:421:40`

`return getNodeOrThrowImpl(graph, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3427 no-casts `packages/sdk/app-graph/src/graph.ts:490:36`

`return getActionsImpl(graph, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3428 no-casts `packages/sdk/app-graph/src/graph.ts:515:34`

`return getEdgesImpl(graph, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3429 no-casts `packages/sdk/app-graph/src/graph.ts:617:37`

`return getPathImpl(graph, params!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3430 no-casts `packages/sdk/app-graph/src/graph.ts:711:36`

`return initializeImpl(graph, id!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3431 no-casts `packages/sdk/app-graph/src/graph.ts:899:52`

`return sortEdgesImpl(graph, id, relation, order!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3432 no-casts `packages/sdk/app-graph/src/graph.ts:906:78`

`const addNodesImpl = <T extends WritableGraph>(graph: T, nodes: Node.NodeArg<any, Record<string, any>>[]): T => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3433 no-casts `packages/sdk/app-graph/src/graph.ts:916:81`

`export function addNodes<T extends WritableGraph>(graph: T, nodes: Node.NodeArg<any, Record<string, any>>[]): T;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3434 no-casts `packages/sdk/app-graph/src/graph.ts:917:46`

`export function addNodes(nodes: Node.NodeArg<any, Record<string, any>>[]): <T extends WritableGraph>(graph: T) => T…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3435 no-casts `packages/sdk/app-graph/src/graph.ts:919:34`

`graphOrNodes: T | Node.NodeArg<any, Record<string, any>>[],` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3436 no-casts `packages/sdk/app-graph/src/graph.ts:920:24`

`nodes?: Node.NodeArg<any, Record<string, any>>[],` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3437 no-casts `packages/sdk/app-graph/src/graph.ts:924:48`

`const nodes = graphOrNodes as Node.NodeArg<any, Record<string, any>>[];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3438 no-casts `packages/sdk/app-graph/src/graph.ts:936:79`

`const addNodeImpl = <T extends WritableGraph>(graph: T, nodeArg: Node.NodeArg<any, Record<string, any>>): T => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3439 no-casts `packages/sdk/app-graph/src/graph.ts:948:30`

`} = nodeArg as Node.NodeArg<any> & {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3440 no-casts `packages/sdk/app-graph/src/graph.ts:1035:82`

`export function addNode<T extends WritableGraph>(graph: T, nodeArg: Node.NodeArg<any, Record<string, any>>): T;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3441 no-casts `packages/sdk/app-graph/src/graph.ts:1036:47`

`export function addNode(nodeArg: Node.NodeArg<any, Record<string, any>>): <T extends WritableGraph>(graph: T) => T;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3442 no-casts `packages/sdk/app-graph/src/graph.ts:1038:36`

`graphOrNodeArg: T | Node.NodeArg<any, Record<string, any>>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3443 no-casts `packages/sdk/app-graph/src/graph.ts:1039:26`

`nodeArg?: Node.NodeArg<any, Record<string, any>>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3444 no-casts `packages/sdk/app-graph/src/graph.ts:1043:52`

`const nodeArg = graphOrNodeArg as Node.NodeArg<any, Record<string, any>>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3445 no-casts `packages/sdk/app-graph/src/node-matcher.ts:182:28`

`(...matchers: NodeMatcher<any>[]): NodeMatcher<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3446 no-casts `packages/sdk/app-graph/src/node-matcher.ts:184:28`

`(...matchers: NodeMatcher<any>[]): NodeMatcher<any> =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3447 no-casts `packages/sdk/app-graph/src/node-matcher.ts:186:29`

`let first: Option.Option<any> = Option.none();` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3448 no-casts `packages/sdk/app-graph/src/node-matcher.ts:220:28`

`(...matchers: NodeMatcher<any>[]): NodeMatcher<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3449 no-casts `packages/sdk/app-graph/src/node-matcher.ts:222:28`

`(...matchers: NodeMatcher<any>[]): NodeMatcher<any> =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3450 no-casts `packages/sdk/app-graph/src/node-matcher.ts:317:24`

`(matcher: NodeMatcher<any>): NodeMatcher<unknown> =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3451 no-casts `packages/sdk/app-graph/src/node.ts:35:26`

`export type Node<TData = any, TProperties extends Record<string, any> = Record<string, any>> = Readonly<{` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3452 no-casts `packages/sdk/app-graph/src/node.ts:67:32`

`export type NodeFilter<TData = any, TProperties extends Record<string, any> = Record<string, any>> = (` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3453 no-casts `packages/sdk/app-graph/src/node.ts:68:38`

`node: Node<unknown, Record<string, any>>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3454 no-casts `packages/sdk/app-graph/src/node.ts:91:63`

`export type NodeArg<TData, TProperties extends Record<string, any> = Record<string, any>> = MakeOptional<` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3455 no-casts `packages/sdk/app-graph/src/node.ts:100:31`

`actions?: NodeArg<ActionData<any> | typeof actionGroupSymbol>[];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3456 no-casts `packages/sdk/app-graph/src/node.ts:133:44`

`export type ActionContext = Context.Context<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3457 no-casts `packages/sdk/app-graph/src/node.ts:135:55`

`export type Action<TProperties extends Record<string, any> = Record<string, any>> = Readonly<` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3458 no-casts `packages/sdk/app-graph/src/node.ts:148:60`

`export type ActionGroup<TProperties extends Record<string, any> = Record<string, any>> = Readonly<` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3459 no-casts `packages/sdk/app-graph/src/node.ts:179:30`

`export const make = <TData = any, TProperties extends Record<string, any> = Record<string, any>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3460 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:62:29`

`.filter((space: any) => get(CreateAtom.fromObservable(space.state)) === SpaceState.SPACE_READY)` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3461 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:110:11`

`(window as any).graph = graph;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3462 no-casts `packages/sdk/app-graph/src/stories/EchoGraph.stories.tsx:228:93`

`<Select.Root value={action?.toString()} onValueChange={(action) => setAction(action as unknown as Action)}>` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3463 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:19:30`

`export const Node: FC<{ data?: any; root?: boolean }> = ({ data, root }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3464 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:43:49`

`export const KeyValue: FC<{ label: string; data?: any; className?: string }> = ({ label, data, className }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3465 no-casts `packages/sdk/app-graph/src/stories/Tree.tsx:62:25`

`const Scalar: FC<{ value: any }> = ({ value }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3466 no-casts `packages/sdk/app-graph/src/util.ts:19:63`

`export const withLabel: (name: string) => <A extends Atom.Atom<any>>(self: A) => A = ATOM_LABELS` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3467 no-casts `packages/sdk/app-graph/src/util.ts:72:53`

`export const nodeArgsUnchanged = (prev: Node.NodeArg<any>[], next: Node.NodeArg<any>[]): boolean => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3468 no-casts `packages/sdk/app-solid/src/useCapabilities.test.tsx:24:3`

`} as unknown as PluginManager.PluginManager;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3469 no-casts `packages/sdk/app-solid/src/useCapabilities.test.tsx:65:7`

`} as unknown as PluginManager.PluginManager;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3470 no-casts `packages/sdk/app-solid/src/usePluginManager.test.tsx:20:7`

`} as unknown as PluginManager.PluginManager;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3471 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:164:22`

`atom: Atom.Writable<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3472 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:259:16`

`getText: (obj: any, anchor: string) => string | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3473 no-casts `packages/sdk/app-toolkit/src/app-framework/AppCapabilities.ts:270:26`

`getTextContent: (object: any) => Promise<string | undefined>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3474 no-casts `packages/sdk/app-toolkit/src/app-framework/progress-trace-sink.test.ts:26:6`

`}) as unknown as Trace.Message;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3475 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:43:45`

`export function createFactory<TArgs extends any[], T>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3476 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:47:45`

`export function createFactory<TArgs extends any[], T>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3477 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:55:28`

`return cache.get(key)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3478 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:65:52`

`(key: string, ns: string, extra?: Record<string, any>): [string, Record<string, any>] => [key, { ns, ...extra }],` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3479 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:66:52`

`(key: string, ns: string, extra?: Record<string, any>) => `${key}\0${ns}${extra ? `\0${JSON.stringify(extra)}` : ''…` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3480 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:240:30`

`const refValue = (object as any)?.[iconFromRefProp];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3481 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:551:40`

`actions: Node.NodeArg<Node.ActionData<any>>[];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3482 no-casts `packages/sdk/app-toolkit/src/app-graph/AppNode.ts:578:32`

`): Node.NodeArg<Node.ActionData<any>> => ({` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3483 no-casts `packages/sdk/app-toolkit/src/app-graph/TypeSection.ts:114:35`

`const filter = Filter.type(type as any) as Filter.Any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3484 no-casts `packages/sdk/app-toolkit/src/echo/AppSpace.test.ts:57:73`

`const makeSpace = (tags: string[]): Space => ({ tags, properties: {} }) as unknown as Space;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# WARN cc9b81fc-3485 no-echo-internal-in-sdk `packages/sdk/app-toolkit/src/echo/TypeOptions.ts:13:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `TypeInputOptionsAnnotation`. Replace with the public `Annotation.make` (from `@dxos/echo`), the supported equivalent for defining a schema annotation helper.

# ERROR cc9b81fc-3486 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:127:75`

`const filter = SurfaceInternals.makeFilter(AppSurface.Article, (data: any) => data.custom === true);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3487 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.test.ts:182:62`

`SurfaceInternals.makeFilter(AppSurface.Article, (data: any) => data.extra === true),` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3488 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:37:68`

`export const oneOf = <TFilters extends ReadonlyArray<Surface.Filter<any>>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3489 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:49:68`

`export const allOf = <TFilters extends ReadonlyArray<Surface.Filter<any>>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3490 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:92:39`

`<TToken extends Role.Role<{ subject?: any }>, S extends Type.AnyEntity>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3491 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:97:39`

`<TToken extends Role.Role<{ subject?: any }>, S extends Type.AnyEntity[]>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3492 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:103:19`

`token: Role.Role<any>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3493 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:105:20`

`predicate?: (data: any) => boolean,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3494 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:106:18`

`): Surface.Filter<any> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3495 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:135:60`

`export const literal = <TToken extends Role.Role<{ subject?: any }>, T extends string | null>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3496 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:160:39`

`<TToken extends Role.Role<{ subject?: any }>, T>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3497 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:164:39`

`<TToken extends Role.Role<{ subject?: any }>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3498 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:168:22`

`} = (token: Role.Role<any>, check: (value: unknown) => boolean): Surface.Filter<any> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3499 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:182:61`

`export const snapshot = <TToken extends Role.Role<{ subject?: any }>, S extends Type.Obj<any>>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3500 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:203:28`

`<TToken extends Role.Role<any>>(token: TToken): Surface.Filter<{ companionTo: Obj.Any }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3501 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:204:28`

`<TToken extends Role.Role<any>, S extends Type.AnyEntity>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3502 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:208:28`

`<TToken extends Role.Role<any>, T extends string>(token: TToken, value: T): Surface.Filter<{ companionTo: T }>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3503 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:209:28`

`<TToken extends Role.Role<any>, T>(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3504 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:214:19`

`token: Role.Role<any>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3505 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:216:18`

`): Surface.Filter<any> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3506 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:246:44`

`export const Article: Role.Role<ArticleData<any>> = Role.make('org.dxos.role.article');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3507 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:252:31`

`properties?: Record<string, any>; // TODO(burdon): What is this for?` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3508 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:339:42`

`export const settings = (token: Role.Role<any>, prefix?: string): Surface.Filter<SettingsData> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3509 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:356:44`

`export const Section: Role.Role<SectionData<any>> = Role.make('org.dxos.role.section');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3510 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:359:42`

`export const Slide: Role.Role<SectionData<any>> = Role.make('org.dxos.role.slide');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3511 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:362:45`

`export const Tabpanel: Role.Role<ArticleData<any>> = Role.make('org.dxos.role.tabpanel');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3512 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:372:65`

`export const Related: Role.Role<{ attendableId?: string; subject: any }> = Role.make('org.dxos.role.related');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3513 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:413:62`

`export const ObjectProperties: Role.Role<ObjectPropertiesData<any>> = Role.make('org.dxos.role.objectProperties');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3514 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:444:42`

`export const CardIcon: Role.Role<CardData<any>> = Role.make('org.dxos.role.cardIcon');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3515 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:447:45`

`export const CardContent: Role.Role<CardData<any>> = Role.make('org.dxos.role.cardContent');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3516 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:609:69`

`export const deckCompanion = (variant: string): Role.Role<{ subject?: any }> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3517 no-casts `packages/sdk/app-toolkit/src/ui/components/app-surface.ts:615:61`

`return Role.make(`org.dxos.role.deckCompanion.${variant}` as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3518 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:147:26`

`expect(hostInvitation!.identityKey).not.to.exist;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3519 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:148:80`

`expect(guestInvitation?.identityKey).to.deep.eq(client1.halo.identity.get()!.identityKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3520 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:149:80`

`expect(guestInvitation?.identityKey).to.deep.eq(client2.halo.identity.get()!.identityKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3521 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:238:62`

`const guestSpace = client2.spaces.get(guestInvitation!.spaceKey!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3522 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:249:93`

`members.sort((m1, m2) => (m1.identity.identityKey.equals(client1.halo.identity.get()!.identityKey) ? -1 : 1));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3523 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:259:47`

`did: client1.halo.identity.get()!.did,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3524 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:260:55`

`identityKey: client1.halo.identity.get()!.identityKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3525 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:273:47`

`did: client2.halo.identity.get()!.did,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3526 no-casts `packages/sdk/client-e2e/src/client-services.test.ts:274:55`

`identityKey: client2.halo.identity.get()!.identityKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3527 no-casts `packages/sdk/client-e2e/src/client.test.ts:201:48`

`const space2 = client2.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3528 no-casts `packages/sdk/client-e2e/src/client.test.ts:226:45`

`.poll(() => thread1.messages[0].target!.blocks[0].content?.target?.content, { timeout: 1_000 })` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3529 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:154:89`

`const contact = contacts.find((c) => c.identityKey.equals(client.halo.identity.get()!.identityKey));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3530 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:156:19`

`return contact!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3531 no-casts `packages/sdk/client-e2e/src/contact-book.test.ts:165:67`

`return client.spaces.get().find((s) => s.key.equals(spaceKey))!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3532 no-casts `packages/sdk/client-e2e/src/halo-credentials.node.test.ts:52:44`

`ids: credentials.map(({ id }) => id!),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3533 no-casts `packages/sdk/client-e2e/src/halo-credentials.node.test.ts:57:33`

`expect(presentation.proofs![0].nonce).to.deep.equal(nonce);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3534 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:26:38`

`expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3535 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:40:38`

`expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3536 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:52:38`

`expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3537 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:55:38`

`expect(client.halo.identity.get()!.profile?.displayName).to.equal('test-user-updated');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3538 no-casts `packages/sdk/client-e2e/src/halo-profile.test.ts:177:39`

`expect(client2.halo.identity.get()!.profile?.displayName).to.equal('test-user-updated');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3539 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:90:29`

`object: (result as any).toJSON(),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3540 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:97:93`

`objects.every((object) => indexResults.some((entry) => objectContains(entry.result!, object))) &&` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3541 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:98:93`

`indexResults.every((entry) => objects.some((object) => objectContains(entry.result!, object)))` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3542 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:156:48`

`const space = client.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3543 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:187:48`

`const space = client.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3544 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:318:34`

`const objectContains = (container: any, content: any): boolean => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3545 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:322:97`

`isEqual: Object.entries(content).every(([key, value]) => key === 'id' || isEqual((container as any)[key], value)),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3546 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:329:55`

`(typeof value === 'object' && typeof (container as any)[key] === 'object'` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3547 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:330:37`

`? objectContains((container as any)[key], value)` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3548 no-casts `packages/sdk/client-e2e/src/indexing.test.ts:331:30`

`: isEqual((container as any)[key], value)),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3549 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:62:25`

`expect(guestInvitation!.target).to.eq(hostInvitation!.target);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3550 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:65:25`

`switch (hostInvitation!.kind) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3551 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:67:29`

`expect(guestInvitation!.spaceKey).to.exist;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3552 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:68:28`

`expect(hostInvitation!.spaceKey).to.deep.eq(guestInvitation!.spaceKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3553 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:70:35`

`expect(host.dataSpaceManager!.spaces.get(hostInvitation!.spaceKey!)).to.exist;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3554 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:71:36`

`expect(guest.dataSpaceManager!.spaces.get(guestInvitation!.spaceKey!)).to.exist;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3555 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:75:28`

`expect(hostInvitation!.identityKey).not.to.exist;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3556 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:76:29`

`expect(guestInvitation!.identityKey).to.deep.eq(host.identityManager.identity!.identityKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3557 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:77:29`

`expect(guestInvitation!.identityKey).to.deep.eq(guest.identityManager.identity!.identityKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3558 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:80:60`

`await expect.poll(() => host.identityManager.identity!.authorizedDeviceKeys.size).toEqual(2);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3559 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:81:61`

`await expect.poll(() => guest.identityManager.identity!.authorizedDeviceKeys.size).toEqual(2);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3560 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:320:44`

`space = await host.dataSpaceManager!.createSpace(Context.default());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3561 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:636:29`

`() => [(host.services as any).host.context, (guest.services as any).host.context],` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3562 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:666:29`

`() => [(host.services as any).host.context, (guest.services as any).host.context],` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3563 no-casts `packages/sdk/client-e2e/src/invitations.test.ts:704:85`

`const service = makeServicesFromRpc(rpc, EffectContext.empty()).InvitationsService!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3564 no-casts `packages/sdk/client-e2e/src/lazy-space-loading.test.ts:85:105`

`const client2State = members.find((m) => m.identity.identityKey.equals(client2.halo.identity.get()!.identityKey));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3565 no-casts `packages/sdk/client-e2e/src/lazy-space-loading.test.ts:150:60`

`return client.spaces.get().find((s) => s.id === space.id)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3566 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:131:75`

`const echoSpace = host.spaces.get().find((s) => s.key.equals(space.key))!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3567 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:133:42`

`memberKey: target.halo.identity.get()!.identityKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3568 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:142:98`

`return space.members.get().find((m) => m.identity.identityKey.equals(client.halo.identity.get()!.identityKey));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3569 no-casts `packages/sdk/client-e2e/src/space-member-management.test.ts:174:66`

`return client.spaces.get().find((s) => s.key.equals(space.key))!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3570 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:48:64`

`const space = await waitForSpace(client2, guestInvitation!.spaceKey!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3571 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:76:41`

`await waitForSpace(fred, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3572 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:105:41`

`await waitForSpace(fred, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3573 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:111:44`

`await waitForSpace(charlie, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3574 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:126:29`

`expect(genesisCredential!.subject.assertion.membershipPolicy).toEqual(MembershipPolicy.LOCKED);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3575 no-casts `packages/sdk/client-e2e/src/spaces-invitations-subduction.test.ts:134:71`

`const invitationStream = peer.services.services.InvitationsService!.queryInvitations();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3576 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:34:64`

`const space = await waitForSpace(client2, guestInvitation!.spaceKey!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3577 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:65:41`

`await waitForSpace(fred, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3578 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:94:41`

`await waitForSpace(fred, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3579 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:100:44`

`await waitForSpace(charlie, space.key!, { ready: true });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3580 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:115:29`

`expect(genesisCredential!.subject.assertion.membershipPolicy).toEqual(MembershipPolicy.LOCKED);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3581 no-casts `packages/sdk/client-e2e/src/spaces-invitations.test.ts:123:71`

`const invitationStream = peer.services.services.InvitationsService!.queryInvitations();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3582 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:128:63`

`const space2 = await waitForSpace(client2, guestInvitation!.spaceKey!, {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3583 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:173:38`

`const dataSpace1 = services1.host!.context.dataSpaceManager?.spaces.get(space1.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3584 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:174:31`

`const feedKey = dataSpace1!.inner.dataFeedKey;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3585 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:175:33`

`const feed1 = services1.host!.context.feedStore.getFeed(feedKey!)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3586 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:198:33`

`const feed2 = services2.host!.context.feedStore.getFeed(feedKey!)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3587 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:265:42`

`Obj.update(space.db.getObjectById(id)!, (obj) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3588 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:309:47`

`Obj.update(space2.db.getObjectById(obj.id)!, (obj) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3589 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:352:45`

`expect((space2.db.getObjectById(obj.id) as any).data).to.equal('test-reactive');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3590 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:379:37`

`Obj.update(hostDocument.content as any, (c: any) => {` casts with `as any`, suppressing the type checker entirely, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3591 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:386:79`

`const reloadedSpace = host.spaces.get().find((s) => s.id === hostSpace.id)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# WARN cc9b81fc-3592 no-sleep-in-test `packages/sdk/client-e2e/src/spaces.test.ts:427`

Both `sharedSpace`/`spaceA` tests (lines 427, 444) call `await sleep(50)` right after `waitForObject`/`Promise.all` to assert an object did **not** propagate into an unrelated space. This should synchronize on an explicit signal (e.g. a second `waitForObject`-style poll with a bounded timeout) rather than a bare 50ms sleep.

# ERROR cc9b81fc-3593 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:502:29`

`const getTypename = (obj: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3594 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:553:65`

`const guestRoot = guestSpace.db.getObjectById(hostRoot.id)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3595 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:676:43`

`const feedEntry = parsed.feeds.find((f: any) => f.feedObjectId === feedObj.id);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3596 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:721:27`

`expect(messages.map((m: any) => m.name).sort()).toEqual(['msg-1', 'msg-2']);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3597 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:788:71`

`.query(Query.select(Filter.everything()).from(Scope.feed(feedURI!)))` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3598 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:824:21`

`exported.feeds![0].messages.map((msg) => Obj.fromJSON(msg, { refResolver })),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3599 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:830:77`

`.query(Query.select(Filter.everything()).from(Scope.feed(targetFeedURI!)))` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3600 no-casts `packages/sdk/client-e2e/src/spaces.test.ts:905:71`

`return space.db.getObjectById<TestSchema.DocumentType>(documentId)!.content.target!.content;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3601 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:335:5`

`) as unknown as Effect.Effect<ClientServicesRpc, never, Scope.Scope>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3602 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:345:25`

`const rpcRecord = rpc as unknown as Record<string, (...args: any[]) => unknown>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3603 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:365:25`

`const rpcRecord = rpc as unknown as Record<string, (...args: any[]) => unknown>;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3604 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:393:52`

`const rpc: Record<string, Record<string, (...args: any[]) => unknown>> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3605 no-casts `packages/sdk/client-protocol/src/service-rpc.ts:407:14`

`return rpc as unknown as ClientServicesRpc;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3606 no-casts `packages/sdk/client-services/src/packlets/devtools/feeds.ts:41:34`

`feedMap.get(feed.key)!.owner = findFeedOwner(spaceManager, feed.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3607 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:20:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3608 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:79:67`

`: { peerLeft: { peer: PublicKey.from(swarmEvent.peerLeft!.peer.peerKey).asUint8Array() } },` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3609 no-casts `packages/sdk/client-services/src/packlets/devtools/network.ts:100:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3610 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:21:55`

`const spaces: Space[] = [...context.spaceManager!.spaces.values()];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3611 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:37:46`

`controlFeed: space.controlFeedKey!, // TODO(dmaretskyi): Those keys may be missing.` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3612 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:38:40`

`dataFeed: space.dataFeedKey!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3613 no-casts `packages/sdk/client-services/src/packlets/devtools/spaces.ts:46:45`

`unsubscribe = context.dataSpaceManager!.updated.on(() => update());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3614 no-casts `packages/sdk/client-services/src/packlets/diagnostics/browser-diagnostics-broadcast.ts:25:11`

`payload?: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3615 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics-collector.ts:15:32`

`broadcastDiagnosticsRequest(): any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3616 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics-collector.ts:30:13`

`): Promise<any> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3617 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:118:67`

`(await getFirstStreamValue(clientServices.DevicesService!.queryDevices(), {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3618 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:134:65`

`(await getFirstStreamValue(clientServices.DevtoolsHost!.subscribeToFeeds({}), {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3619 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:141:79`

`const status = await getFirstStreamValue(clientServices.NetworkService!.queryStatus(), {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3620 no-casts `packages/sdk/client-services/src/packlets/diagnostics/diagnostics.ts:214:37`

`for await (const filename of (dir as any)?.keys()) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3621 no-casts `packages/sdk/client-services/src/packlets/identity/authenticator.ts:65:28`

`if (!credential.proof!.nonce || !Buffer.from(nonce).equals(credential.proof!.nonce)) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3622 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:88:42`

`expect(peer2.identityManager.identity!.identityKey).to.deep.eq(identity1.identityKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3623 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:89:42`

`expect(peer2.identityManager.identity!.deviceKey).to.deep.eq(identity1.deviceKey);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3624 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:192:18`

`deviceProfile!.label = 'updated profile';` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3625 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.test.ts:193:65`

`await peer.identityManager.updateDeviceProfile(deviceProfile!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3626 no-casts `packages/sdk/client-services/src/packlets/identity/identity-manager.ts:311:34`

`identityKey: this._identity!.identityKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3627 no-casts `packages/sdk/client-services/src/packlets/identity/identity-recovery-manager.ts:191:7`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3628 no-casts `packages/sdk/client-services/src/packlets/identity/identity-recovery-manager.ts:291:7`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3629 no-casts `packages/sdk/client-services/src/packlets/identity/identity.ts:246:50`

`private _onFeedAdded = async (feed: FeedWrapper<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3630 no-casts `packages/sdk/client-services/src/packlets/identity/identity.ts:247:35`

`await this._edgeFeedReplicator!.addFeed(feed);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3631 no-casts `packages/sdk/client-services/src/packlets/invitations/edge-invitation-handler.ts:166:7`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3632 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-guest-extenstion.ts:110:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3633 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-host-extension.ts:223:13`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3634 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-host-extension.ts:255:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3635 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:26:49`

`error(lockHolder: FlowLockHolder | null, error: any): boolean;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3636 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:64:53`

`error: (lockHolder: FlowLockHolder | null, error: any): boolean => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3637 no-casts `packages/sdk/client-services/src/packlets/invitations/invitation-state.ts:78:54`

`const logStateUpdate = (invitation: Invitation, actor: any, newState: Invitation.State, error?: Error) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# WARN cc9b81fc-3638 no-sleep-in-test `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:56`

This file uses `await sleep(...)` as its primary synchronization mechanism throughout (e.g. lines 56, 77, 81, 100, 117, 132, 164, 184, 201, 223, 246, 312, 319), including two busy-poll loops that repeatedly `wake()`/re-attempt and then `await sleep(10)` on every iteration (lines 145-148 and 179-184) instead of awaiting a condition. The file already imports and uses `waitForCondition` once (around line 243) — the fix is to use that primitive (or the peers' `sink.waitFor(...)` already used elsewhere in the file) consistently instead of ad-hoc sleeps and busy-poll loops.

# ERROR cc9b81fc-3639 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:146:45`

`codeInput.wake(invitation.authCode!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3640 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:238:52`

`authCodeInput2.wake(invitation.authCode!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3641 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:317:59`

`const newHost = await createPeer(invitation.spaceKey!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3642 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:327:46`

`authCodeInput2.wake(invitation.authCode!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3643 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.test.ts:357:9`

`} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3644 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:163:13`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3645 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:192:15`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3646 no-casts `packages/sdk/client-services/src/packlets/invitations/invitations-handler.ts:430:15`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3647 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:28:46`

`const space = await peer.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3648 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:29:33`

`expect(peer.dataSpaceManager!.spaces.has(space.key)).to.be.true;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3649 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:37:46`

`const space = await peer.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3650 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:38:33`

`expect(peer.dataSpaceManager!.spaces.has(space.key)).to.be.true;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3651 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:47:47`

`const space1 = await host.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3652 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:53:43`

`const space1 = host.dataSpaceManager!.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3653 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:54:44`

`const space2 = guest.dataSpaceManager!.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3654 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:71:47`

`const space1 = await host.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3655 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:77:43`

`const space1 = host.dataSpaceManager!.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3656 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:78:44`

`const space2 = guest.dataSpaceManager!.spaces.get(spaceKey)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3657 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:100:47`

`const space1 = await host.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3658 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:137:43`

`const space1 = host.dataSpaceManager!.spaces.get(invitation1!.spaceKey!)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3659 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:138:44`

`const space2 = guest.dataSpaceManager!.spaces.get(invitation2!.spaceKey!)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3660 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:158:46`

`const space = await host.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3661 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.test.ts:184:47`

`const space1 = await host.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3662 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.ts:116:38`

`? invitation.guestKeypair!.publicKey` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3663 no-casts `packages/sdk/client-services/src/packlets/invitations/space-invitation-protocol.ts:123:47`

`return credential.credential.credential.id!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3664 no-casts `packages/sdk/client-services/src/packlets/locks/browser.ts:56:41`

`private _onMessage(event: MessageEvent<any>): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3665 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:212:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3666 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:224:11`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3667 no-casts `packages/sdk/client-services/src/packlets/metadata/metadata-store.ts:266:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3668 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:132:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3669 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:147:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3670 no-casts `packages/sdk/client-services/src/packlets/metadata/sqlite-metadata-store.ts:327:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3671 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:78:31`

`public writePromise: Promise<any> = Promise.resolve();` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3672 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:115:41`

`const promise = this.pipeline.writer!.write(message);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3673 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:144:46`

`const agent = real.agents.get(this.agent)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3674 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:161:18`

`const targets: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3675 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:180:62`

`const tf: Timeframe = real.agents.values().next().value!.pipeline.state.timeframe;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3676 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline-stress.test.ts:223:46`

`const agent = real.agents.get(this.agent)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3677 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.test.ts:49:28`

`await pipeline.writer!.write({});` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3678 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:254:33`

`return this._feedSetIterator!.feeds;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3679 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:289:32`

`await this._feedSetIterator!.open();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3680 no-casts `packages/sdk/client-services/src/packlets/pipeline/pipeline.ts:410:66`

`handle = feed.download({ start: seq + 1, linear: true }, (err: any, data: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3681 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:136:44`

`const space = await proxy.SpacesService!.createSpace({ membershipPolicy: MembershipPolicy.INVITE });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3682 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:150:68`

`const statuses = await PbStream.consumeData(proxy.SystemService!.queryStatus({}));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3683 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:162:58`

`await expect(PbStream.consumeData(proxy.SystemService!.queryStatus({}))).rejects.toThrow('stream failed');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3684 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:172:44`

`const error = await proxy.SystemService!.getConfig().then(` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3685 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:182:37`

`await expect(proxy.SystemService!.getConfig()).rejects.toThrow(` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3686 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:231:40`

`const request = proxy.SystemService!.getConfig();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# WARN cc9b81fc-3687 no-sleep-in-test `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:232`

`await sleep(50)` is used to gate `expect(called).toBe(false)`, i.e. sleep is the test's actual synchronization mechanism for a negative assertion. Use a `Trigger`/counter check via `waitForCondition` instead of a fixed real-time wait.

# ERROR cc9b81fc-3688 no-casts `packages/sdk/client-services/src/packlets/services/effect-rpc.test.ts:247:37`

`await expect(proxy.SystemService!.getConfig(undefined, { timeout: 100 })).rejects.toThrow(TimeoutError);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3689 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:55:29`

`statusChanged: new Event<any>(),` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3690 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:75:55`

`const decoded = cborDecode(routerMessage.payload!.value) as ProtocolMessage;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3691 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:132:28`

`edgeClient: edgeClient as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# WARN cc9b81fc-3692 no-sleep-in-test `packages/sdk/client-services/src/packlets/services/feed-syncer.test.ts:296`

`await new Promise((resolve) => setTimeout(resolve, 250))` is used to let server-side replication settle before querying `clientFeedStore`. Replace with an explicit completion signal (event, flush, or `waitForCondition`) rather than a fixed 250ms wait.

# ERROR cc9b81fc-3693 no-casts `packages/sdk/client-services/src/packlets/services/feed-syncer.ts:156:47`

`try: () => cborXdecode(msg.payload!.value),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3694 no-casts `packages/sdk/client-services/src/packlets/services/platform.ts:8:16`

`if ((process as any).browser) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3695 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:23:50`

`const space1 = await device1.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3696 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:24:35`

`await device2.dataSpaceManager!.waitUntilSpaceReady(space1!.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3697 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:25:50`

`const space2 = await device2.dataSpaceManager!.spaces.get(space1.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3698 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:26:17`

`await space2!.inner.controlPipeline.state.waitUntilTimeframe(space1.inner.controlPipeline.state.timeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3699 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:39:52`

`const space1 = await identity2.dataSpaceManager!.createSpace(new Context());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3700 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:48:35`

`await device2.dataSpaceManager!.waitUntilSpaceReady(space1!.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3701 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:49:50`

`const space2 = await device2.dataSpaceManager!.spaces.get(space1.key);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3702 no-casts `packages/sdk/client-services/src/packlets/services/service-context.test.ts:50:17`

`await space2!.inner.controlPipeline.state.waitUntilTimeframe(space1.inner.controlPipeline.state.timeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3703 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:59:35`

`await services.IdentityService!.createIdentity({});` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3704 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:60:54`

`const { spaceKey } = await services.SpacesService!.createSpace({ membershipPolicy: MembershipPolicy.INVITE });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3705 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:62:42`

`const stream = services.SpacesService!.queryCredentials({ spaceKey });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3706 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:79:35`

`await services.IdentityService!.createIdentity({});` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3707 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:83:52`

`issuer: host.context.identityManager.identity!.deviceKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3708 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:88:29`

`services.IdentityService!.queryIdentity()!.subscribe(({ identity }) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3709 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:99:47`

`const credentials = services.SpacesService!.queryCredentials({ spaceKey: await haloSpace.wait() });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3710 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:117:35`

`await services.IdentityService!.createIdentity({});` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3711 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:121:52`

`issuer: host.context.identityManager.identity!.deviceKey,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3712 no-casts `packages/sdk/client-services/src/packlets/services/service-host.test.ts:126:56`

`const presentation = await services.IdentityService!.signPresentation({` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3713 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:222:25`

`#feedStore?: FeedStore<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3714 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:288:70`

`return await createDiagnostics(services, this, this.#config!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3715 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:354:29`

`get feedStore(): FeedStore<any> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3716 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:454:41`

`createIceProvider(this.#config!.get('runtime.services.iceProviders')!),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3717 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:579:24`

`this.#keyring!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3718 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:580:38`

`() => this.#identityManager!.identity ?? failUndefined(),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3719 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:775:89`

`await this._setNetworkIdentity({ deviceCredential: params.authorizedDeviceCredential!, identity });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3720 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:798:33`

`await this.#dataSpaceManager!.open(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3721 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:801:33`

`await this.#edgeAgentManager!.open(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3722 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:808:33`

`this.#dataSpaceManager!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3723 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:810:24`

`this.#keyring!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3724 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:816:26`

`this.#deviceSpaceSync!.setIdentity(identity);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3725 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:817:32`

`await this.#deviceSpaceSync!.open?.(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3726 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:870:73`

`await RuntimeProvider.runPromise(this.#runtime)(this.#storageMigrate!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3727 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:879:32`

`await this.#identityManager!.open(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3728 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:880:73`

`log('identityManager opened', { hasIdentity: !!this.#identityManager!.identity });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3729 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:883:69`

`await this._setNetworkIdentity({ identity: this.#identityManager!.identity });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3730 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:889:30`

`await this.#signalManager!.open(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3731 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:892:31`

`await this.#networkManager!.open();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3732 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:897:27`

`await this.#echoHost!.addReplicator(ctx, this.#meshReplicator);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3733 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:901:27`

`await this.#echoHost!.addReplicator(ctx, this.#echoEdgeReplicator);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3734 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:905:30`

`await this.#metadataStore!.load();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3735 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:908:29`

`await this.#spaceManager!.open();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3736 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:910:30`

`if (this.#identityManager!.identity) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3737 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:912:34`

`await this.#identityManager!.identity.joinNetwork(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3738 no-casts `packages/sdk/client-services/src/packlets/services/service-host.ts:924:61`

`const loadedInvitations = await this.#invitationsManager!.loadPersistentInvitations(ctx);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3739 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.test.ts:44:17`

`const cyclic: any = new Error('boom');` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3740 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:30:15`

`for (let cur: any = err, depth = 0; cur != null && depth < 5; cur = cur.cause, depth++) {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3741 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:252:51`

`write(offset: number, data: Buffer, cb: Callback<any>): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3742 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:290:49`

`del(offset: number, size: number, cb: Callback<any>): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3743 no-casts `packages/sdk/client-services/src/packlets/services/sqlite-storage.ts:393:35`

`return files.get(fullPath)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3744 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:73:23`

`const parent = (obj as any)[ATTR_PARENT];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3745 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:78:31`

`const relationSource = (obj as any)[ATTR_RELATION_SOURCE];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3746 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:79:31`

`const relationTarget = (obj as any)[ATTR_RELATION_TARGET];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3747 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:95:12`

`if ((obj as any)[ATTR_DELETED]) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3748 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-reader.ts:99:21`

`const meta = (obj as any)[ATTR_META];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3749 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-writer.ts:158:56`

`[ATTR_TYPE]: (structure.system?.type?.['/'] ?? '') as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3750 no-casts `packages/sdk/client-services/src/packlets/space-export/serialized-space-writer.ts:210:28`

`const namespace = (obj as any).namespace === 'trace' ? 'trace' : 'data';` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3751 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive-reader.ts:76:37`

`feedBlocksByFeedId.get(feedId)!.set(chunkIndex, blocks);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3752 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive-reader.ts:85:52`

`allBlocks.push(...blockChunks.get(chunkIndex)!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3753 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:374:67`

`const directory = buildDatabaseDirectoryFromObjects(objects as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3754 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:376:42`

`const structure = directory.objects![id];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3755 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:422:9`

`} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3756 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:436:9`

`} as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3757 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:450:28`

`expect((obj['@meta'] as any).annotations).toEqual({` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3758 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:465:67`

`const directory = buildDatabaseDirectoryFromObjects(objects as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3759 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:466:42`

`const structure = directory.objects![id];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3760 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:485:67`

`const directory = buildDatabaseDirectoryFromObjects(objects as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3761 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:486:42`

`const structure = directory.objects![id];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3762 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:505:67`

`const directory = buildDatabaseDirectoryFromObjects(objects as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3763 no-casts `packages/sdk/client-services/src/packlets/space-export/space-archive.test.ts:506:42`

`const structure = directory.objects![id];` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3764 no-casts `packages/sdk/client-services/src/packlets/space/admission-discovery-extension.ts:49:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3765 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.test.ts:90:44`

`await controlPipeline.pipeline.writer!.write({` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3766 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.test.ts:114:44`

`await controlPipeline.pipeline.writer!.write({` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3767 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:88:13`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3768 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:167:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3769 no-casts `packages/sdk/client-services/src/packlets/space/control-pipeline.ts:216:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3770 no-casts `packages/sdk/client-services/src/packlets/space/space-manager.ts:166:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3771 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:32:38`

`await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3772 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:60:40`

`await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3773 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:93:30`

`space2.controlFeedKey!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3774 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:94:27`

`space2.dataFeedKey!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3775 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:109:41`

`await space1.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3776 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:112:41`

`await space2.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3777 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:138:39`

`await space1.controlPipeline.state!.waitUntilTimeframe(space1.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3778 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:147:39`

`await space2.controlPipeline.state!.waitUntilTimeframe(space2.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3779 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:167:40`

`await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3780 no-casts `packages/sdk/client-services/src/packlets/space/space.test.ts:178:40`

`await space.controlPipeline.state!.waitUntilTimeframe(space.controlPipeline.state!.endTimeframe);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3781 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:366:62`

`const newDoc = await this._echoHost.createDoc(data as any as DatabaseDirectory, {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3782 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:371:71`

`if (newDoc.doc().access !== undefined && newDoc.doc().access!.spaceKey !== spaceKey.toHex()) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3783 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:373:25`

`doc.access!.spaceKey = spaceKey.toHex();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3784 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space-manager.ts:658:13`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3785 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:289:45`

`async postMessage(channel: string, message: any): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3786 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:480:88`

`await this._metadataStore.setWritableFeedKeys(this.key, this.inner.controlFeedKey!, this.inner.dataFeedKey!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3787 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:647:50`

`private _onFeedAdded = async (feed: FeedWrapper<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3788 no-casts `packages/sdk/client-services/src/packlets/spaces/data-space.ts:648:35`

`await this._edgeFeedReplicator!.addFeed(feed);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# WARN cc9b81fc-3789 no-sleep-in-test `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.test.ts:33`

`await sleep(50)` (also 155, 168, 173, 185) gates "not called yet" assertions (e.g. `expect(sendSpy).not.toHaveBeenCalled()`), even though the same file uses the correct primitive `expect.poll(...)` elsewhere (lines 93, 158, 176). Use `expect.poll`/a bounded condition wait consistently instead of the fixed sleeps.

# ERROR cc9b81fc-3790 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.test.ts:110:47`

`sendSpy.mockImplementationOnce(async (_ctx: any, request: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3791 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.test.ts:200:37`

`messageHandler: async (message: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3792 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:36:66`

`private readonly _feeds = new ComplexMap<PublicKey, FeedWrapper<any>>(PublicKey.hash);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3793 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:75:51`

`const payload = decodeCbor(message.payload!.value) as ProtocolMessage;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3794 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:120:34`

`async addFeed(feed: FeedWrapper<any>): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3795 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:133:63`

`private async _replicateFeed(ctx: Context, feed: FeedWrapper<any>): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3796 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:140:26`

`Event.wrap(feed.core as any, 'append').on(ctx, async () => {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3797 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:198:56`

`await this._sendMessage(this._connectionCtx!, {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3798 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:206:55`

`await this._pushBlocks(this._connectionCtx!, feed, message.length, feed.length);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3799 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:233:60`

`private async _pushBlocks(ctx: Context, feed: FeedWrapper<any>, from: number, to: number): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3800 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:259:51`

`private async _integrateBlocks(feed: FeedWrapper<any>, blocks: FeedBlock[]): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3801 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:277:54`

`private async _pushBlocksIfNeeded(feed: FeedWrapper<any>): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3802 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:285:58`

`const remoteLength = this._remoteLength.get(feed.key)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3803 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:287:49`

`await this._pushBlocks(this._connectionCtx!, feed, remoteLength, feed.length);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3804 no-casts `packages/sdk/client-services/src/packlets/spaces/edge-feed-replicator.ts:294:26`

`onError: async (err: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3805 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:91:43`

`private _activeEdgePollingIntervalHandle: any | undefined = undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3806 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:173:108`

`const allNotarized = Promise.all(credentials.map((credential) => this._waitUntilProcessed(credential.id!)));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3807 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:219:104`

`credentials: credentials.filter((credential) => !this._processedCredentials.has(credential.id!)),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3808 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:224:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3809 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:256:9`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3810 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:326:9`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3811 no-casts `packages/sdk/client-services/src/packlets/spaces/notarization-plugin.ts:395:31`

`const handleEdgeError = (error: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3812 no-casts `packages/sdk/client-services/src/packlets/testing/invitation-utils.ts:99:54`

`invariant(hostInvitation.swarmKey!.equals(guestInvitation.swarmKey!));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3813 no-casts `packages/sdk/client-services/src/packlets/testing/test-builder.ts:130:24`

`feedStore?: FeedStore<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3814 no-casts `packages/sdk/client-services/src/packlets/testing/test-builder.ts:230:82`

`return new SpaceInvitationProtocol(this.dataSpaceManager, this.identity!, this.keyring, invitation.spaceKey!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3815 no-casts `packages/sdk/client-services/src/packlets/worker/worker-runtime.ts:114:27`

`const signalMetadataTags: any = { runtime: 'worker-runtime' };` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3816 no-casts `packages/sdk/client-services/src/packlets/worker/worker-runtime.ts:252:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3817 no-casts `packages/sdk/client-services/src/packlets/worker/worker-session.ts:145:11`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3818 no-casts `packages/sdk/client/src/client/client.ts:297:59`

`async diagnostics(options: JsonKeyOptions = {}): Promise<any> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3819 no-casts `packages/sdk/client/src/client/client.ts:306:26`

`async repair(): Promise<any> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3820 no-casts `packages/sdk/client/src/client/client.ts:308:24`

`const repairSummary: any = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3821 no-casts `packages/sdk/client/src/client/client.ts:315:18`

`(space as any)._data.pipeline.currentEpoch?.subject.assertion.automergeRoot.slice('automerge:'.length),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3822 no-casts `packages/sdk/client/src/client/client.ts:322:43`

`for await (const filename of (dir as any)?.keys()) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3823 no-casts `packages/sdk/client/src/client/client.ts:476:33`

`const edgeUrl = this._config!.get('runtime.services.edge.url');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3824 no-casts `packages/sdk/client/src/devtools/debug-port-controller.ts:218:21`

`dxos: (globalThis as any).__DXOS__,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3825 no-casts `packages/sdk/client/src/devtools/debug-port-controller.ts:219:25`

`composer: (globalThis as any).composer,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3826 no-casts `packages/sdk/client/src/devtools/devtools.ts:47:28`

`get?(id: string): Promise<any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3827 no-casts `packages/sdk/client/src/devtools/devtools.ts:79:13`

`joinTables: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3828 no-casts `packages/sdk/client/src/devtools/devtools.ts:309:16`

`((globalThis as any).__DXOS__ as DevtoolsHook) = hook;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3829 no-casts `packages/sdk/client/src/devtools/devtools.ts:327:22`

`delete (globalThis as any).__DXOS__;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3830 no-casts `packages/sdk/client/src/devtools/devtools.ts:328:22`

`delete (globalThis as any).dxos;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3831 no-casts `packages/sdk/client/src/echo/space-list.ts:177:27`

`await spaceProxy!._processSpaceUpdate(this._ctx, space);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3832 no-casts `packages/sdk/client/src/echo/space-proxy.ts:80:33`

`export const getSpace = (object?: any): Space | undefined => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3833 no-casts `packages/sdk/client/src/echo/space-proxy.ts:403:45`

`this._membersUpdate.emit(space.members!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3834 no-casts `packages/sdk/client/src/echo/space-proxy.ts:549:45`

`async postMessage(channel: string, message: any): Promise<void> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3835 no-casts `packages/sdk/client/src/edge/client-edge-api.ts:58:45`

`return new QueryResultImpl(queryContext as any, normalizedQuery) as QueryResult.QueryResult<Query.Type<Q>>;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3836 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:20:36`

`const edgeClient = { putBlob } as unknown as EdgeHttpClient;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3837 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:34:36`

`const edgeClient = { getBlob } as unknown as EdgeHttpClient;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3838 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:46:36`

`const edgeClient = { getBlob } as unknown as EdgeHttpClient;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3839 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:57:36`

`const edgeClient = { hasBlob } as unknown as EdgeHttpClient;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3840 no-casts `packages/sdk/client/src/edge/edge-blob-backend.test.ts:69:39`

`const edgeClient = { getBlobUrl } as unknown as EdgeHttpClient;` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3841 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:314:59`

`if (ids && !ids.some((id) => id.equals(credential.id!))) {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3842 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:333:45`

`const invitation = this._invitationProxy!.share(options);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3843 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:351:33`

`return this._invitationProxy!.join(invitation, deviceProfileWithDefaults);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3844 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:366:36`

`spaceKey: identity.spaceKey!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3845 no-casts `packages/sdk/client/src/halo/halo-proxy.ts:381:111`

`const credentialsToPresent = credentials.filter((credential) => ids.some((id) => id.equals(credential.id!)));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3846 no-casts `packages/sdk/client/src/invitations/invitations-proxy.ts:242:43`

`const invitationValue = (invitation as any)[key];` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3847 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:43:18`

`init(authToken?: any): boolean;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3848 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:90:18`

`init(authToken?: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3849 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:108:33`

`_checkAuthorization(authToken?: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3850 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:125:29`

`_checkAuthCookie(authToken: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3851 no-casts `packages/sdk/client/src/services/agent-hosting-provider.ts:193:44`

`const { deviceKey } = this._halo.device!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3852 no-casts `packages/sdk/client/src/services/dedicated/dedicated-worker-client-services.test.ts:72:48`

`const space2 = client2.spaces.get(space.id)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3853 no-casts `packages/sdk/client/src/services/dedicated/dedicated-worker-client-services.test.ts:174:39`

`expect(client2.halo.identity.get()!.profile?.displayName).toEqual(updatedDisplayName);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3854 no-casts `packages/sdk/client/src/services/fake-agent-hosting-provider.ts:30:24`

`public init(authToken: any): boolean {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3855 no-casts `packages/sdk/client/src/services/local-client-services.ts:136:21`

`signalMetadataTags: any = {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3856 no-casts `packages/sdk/client/src/services/local-client-services.ts:155:23`

`if ((globalThis as any).__args) {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3857 no-casts `packages/sdk/client/src/services/local-client-services.ts:265:54`

`makeInProcessClientServicesRpc(() => this._host!.services).pipe(Scope.provide(this._serviceScope)),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3858 no-casts `packages/sdk/client/src/services/shell-manager.ts:83:11`

`iframe!.setAttribute('style', shellStyles);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3859 no-casts `packages/sdk/client/src/services/shell-manager.ts:84:11`

`iframe!.setAttribute('name', 'dxos-shell');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3860 no-casts `packages/sdk/client/src/services/shell-manager.ts:85:11`

`iframe!.setAttribute('data-testid', 'dxos-shell');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3861 no-casts `packages/sdk/client/src/services/shell-manager.ts:87:13`

`iframe!.style.display = display === ShellDisplay.NONE ? 'none' : '';` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3862 no-casts `packages/sdk/client/src/services/shell-manager.ts:89:15`

`iframe!.blur();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3863 no-casts `packages/sdk/client/src/testing/test-builder.ts:173:52`

`this._ctx.onDispose(() => this._workerFactory!.close());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3864 no-casts `packages/sdk/client/src/testing/test-builder.ts:177:46`

`createWorker: () => this._workerFactory!.make(),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3865 no-casts `packages/sdk/client/src/testing/test-builder.ts:178:49`

`createCoordinator: () => this._coordinator!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3866 no-casts `packages/sdk/client/src/testing/test-builder.ts:199:81`

`createIceProvider(this.config.get('runtime.services.iceProviders')!),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3867 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:70:53`

`await clientA.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 10_000 });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3868 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:75:77`

`const { recoveryCode } = await clientA.services.services.IdentityService!.createRecoveryCredential({});` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3869 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:126:21`

`recoveredSpace!.waitUntilReady(),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3870 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:131:62`

`log.info('clientB space ready', { spaceId: recoveredSpace!.id });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3871 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:134:37`

`await waitForSync(recoveredSpace!.db);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3872 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:137:41`

`const objects = await recoveredSpace!.db.query(Filter.type(TestSchema.Expando)).run();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3873 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:140:23`

`objects.map((obj: any) => ({ name: obj.name, counter: obj.counter })),` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3874 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:144:29`

`expect(objects.some((obj: any) => obj.name === 'test-object-1' && obj.counter === 42)).toBe(true);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3875 no-casts `packages/sdk/client/test/e2e/edge-recovery.test.ts:145:29`

`expect(objects.some((obj: any) => obj.name === 'test-object-2' && obj.counter === 99)).toBe(true);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# WARN cc9b81fc-3876 no-sleep-in-test `packages/sdk/client/test/e2e/edge-recovery.test.ts:182`

`interval = setInterval(async () => { ... }, ...)` busy-polls `db.getAutomergeSyncState()` to detect sync completion instead of awaiting a proper completion signal (the sync APIs used elsewhere in this same file, like `waitForSync`, are the better model to follow here).

# WARN cc9b81fc-3877 no-sleep-in-test `packages/sdk/client/test/e2e/sync.test.ts:55`

Both `setInterval(async () => { ... }, ...)` blocks (lines 55 and 117) busy-poll `queryEdgeStatus()`/`getAutomergeSyncState()` on a timer for the life of the test instead of awaiting an explicit completion event. Replace with a `Trigger`/event-driven wait or `waitForCondition`.

# ERROR cc9b81fc-3878 no-casts `packages/sdk/client/test/e2e/sync.test.ts:56:87`

`const { status } = (await Stream.first(client.services.services.EdgeAgentService!.queryEdgeStatus())) ?? {};` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3879 no-casts `packages/sdk/client/test/e2e/sync.test.ts:85:39`

`space = client.spaces.get()[0]!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3880 no-casts `packages/sdk/config/src/config.test.ts:117:7`

`} as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3881 no-casts `packages/sdk/config/src/config.test.ts:148:7`

`} as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3882 no-casts `packages/sdk/config/src/config.test.ts:149:14`

`defaults as any,` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3883 no-casts `packages/sdk/config/src/config.ts:31:76`

`export const mapFromKeyValues = (spec: MappingSpec, values: Record<string, any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3884 no-casts `packages/sdk/config/src/config.ts:76:57`

`export const mapToKeyValues = (spec: MappingSpec, values: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3885 no-casts `packages/sdk/config/src/config.ts:77:32`

`const config: Record<string, any> = {};` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3886 no-casts `packages/sdk/config/src/config.ts:120:27`

`private readonly _config: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3887 no-casts `packages/sdk/config/src/loaders/index.ts:19:37`

`const maybeLoadFile = (file: string): any => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3888 no-casts `packages/sdk/config/src/loaders/index.ts:22:5`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3889 no-casts `packages/sdk/config/src/plugin/definitions.ts:41:55`

`content = parse(readFileSync(value, 'utf-8')) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3890 no-casts `packages/sdk/config/src/plugin/definitions.ts:69:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3891 no-casts `packages/sdk/migrations/src/migration-builder.ts:69:94`

`migrate: (objectStructure: EntityStructure) => MaybePromise<{ type: Type.AnyEntity; props: any }>,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3892 no-casts `packages/sdk/migrations/src/migration-builder.ts:88:64`

`type: EncodedReference.fromURI(getSchemaURI(schema)!),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3893 no-casts `packages/sdk/migrations/src/migration-builder.ts:102:52`

`this._addHandleToFlushList(newHandle.documentId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3894 no-casts `packages/sdk/migrations/src/migration-builder.ts:105:46`

`async addObject(type: Type.AnyEntity, props: any): Promise<string> {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3895 no-casts `packages/sdk/migrations/src/migration-builder.ts:137:48`

`const materialized = toJS(oldHandle.doc()!) as DatabaseDirectory;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3896 no-casts `packages/sdk/migrations/src/migration-builder.ts:144:54`

`this._addHandleToFlushList(newHandle.documentId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3897 no-casts `packages/sdk/migrations/src/migration-builder.ts:162:56`

`this._addHandleToFlushList(this._newRoot.documentId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3898 no-casts `packages/sdk/migrations/src/migration-builder.ts:212:56`

`this._addHandleToFlushList(this._newRoot.documentId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3899 no-casts `packages/sdk/migrations/src/migration-builder.ts:222:10`

`props: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3900 no-casts `packages/sdk/migrations/src/migration-builder.ts:230:63`

`core.setType(EncodedReference.fromURI(getSchemaURI(schema)!));` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3901 no-casts `packages/sdk/migrations/src/migration-builder.ts:239:44`

`this._newLinks[core.id] = newHandle.url!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3902 no-casts `packages/sdk/migrations/src/migration-builder.ts:240:52`

`this._addHandleToFlushList(newHandle.documentId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3903 no-casts `packages/sdk/migrations/src/migrations.test.ts:28:56`

`const objects = await space.db.query(Filter.props<any>({ namespace: 'test' })).run();` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3904 no-casts `packages/sdk/migrations/src/migrations.test.ts:41:56`

`const objects = await space.db.query(Filter.props<any>({ namespace: 'test' })).run();` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3905 no-casts `packages/sdk/observability/src/extensions/otel/extension.ts:253:26`

`if (typeof (globalThis as any).ServiceWorkerGlobalScope !== 'undefined') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3906 no-casts `packages/sdk/observability/src/extensions/otel/extension.ts:256:26`

`if (typeof (globalThis as any).SharedWorkerGlobalScope !== 'undefined') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3907 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:58:43`

`gauge(name: string, value: number, tags?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3908 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:64:48`

`increment(name: string, value?: number, tags?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3909 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:70:50`

`distribution(name: string, value: number, tags?: any): void {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3910 no-casts `packages/sdk/observability/src/extensions/otel/metrics.ts:87:60`

`return Object.entries(data.tags).reduce<{ [key: string]: any }>((obj, [key, value]) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3911 no-casts `packages/sdk/observability/src/providers/client-observability.ts:31:35`

`clientServices.IdentityService!.queryIdentity().subscribe((idqr) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3912 no-casts `packages/sdk/observability/src/providers/client-observability.ts:41:34`

`clientServices.DevicesService!.queryDevices().subscribe((dqr) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3913 no-casts `packages/sdk/observability/src/providers/client-observability.ts:106:34`

`clientServices.NetworkService!.queryStatus().subscribe((networkStatus) => {` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3914 no-casts `packages/sdk/observability/src/providers/client-observability.ts:123:78`

`const platform = yield* Effect.promise(() => clientServices.SystemService!.getPlatform());` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3915 no-casts `packages/sdk/observability/src/providers/client-observability.ts:138:46`

`const memory = (window.performance as any).memory;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3916 no-casts `packages/sdk/observability/test/e2e/tracing-invitation.test.ts:119:52`

`await host.services.services.EdgeAgentService!.createAgent(undefined, { timeout: 15_000 });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3917 no-casts `packages/sdk/react-client/src/client/ClientProvider.test.tsx:81:34`

`const wrapper = ({ children }: any) => <ClientProvider client={client}>{children}</ClientProvider>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3918 no-casts `packages/sdk/react-client/src/echo/useSpaces.test.tsx:43:55`

`const { result } = renderHook(() => useSpace(space!.id), { wrapper });` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3919 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:30:70`

`await client.services.services.EdgeAgentService.createAgent(null as any, { timeout: 10_000 });` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3920 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:63:67`

`const recoveryKey = PublicKey.from(new Uint8Array((credential as any).response.getPublicKey()));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3921 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:64:35`

`const algorithm = (credential as any).response.getPublicKeyAlgorithm() === -7 ? 'ES256' : 'ED25519';` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3922 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:92:65`

`const lookupKey = PublicKey.from(new Uint8Array((credential as any).response.userHandle));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3923 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:98:44`

`signature: Buffer.from((credential as any).response.signature),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3924 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:99:49`

`clientDataJson: Buffer.from((credential as any).response.clientDataJSON),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3925 no-casts `packages/sdk/react-client/src/halo/Passkey.stories.tsx:100:52`

`authenticatorData: Buffer.from((credential as any).response.authenticatorData),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3926 no-casts `packages/sdk/react-client/src/testing/ClientRepeater.stories.tsx:26:38`

`const JsonPanel = ({ value }: { value: any }) => <JsonHighlighter data={value} />;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3927 no-casts `packages/sdk/react-client/src/testing/util.tsx:34:23`

`return ({ children }: any) => <ClientContext.Provider value={{ client }}>{children}</ClientContext.Provider>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3928 no-casts `packages/sdk/react-client/src/testing/util.tsx:38:23`

`return ({ children }: any) => <ClientProvider client={client}>{children}</ClientProvider>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# WARN cc9b81fc-3929 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/api-key.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `RecognizedDomainsAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3930 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/atproto-record.ts:6:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `AtprotoRecordAnnotation` and `AtprotoPolicyAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3931 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/atproto-visibility.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `AtprotoVisibilityAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3932 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/collection-item.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `CollectionItemAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3933 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/factory.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `FactoryAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# ERROR cc9b81fc-3934 no-casts `packages/sdk/schema/src/annotations/factory.ts:24:32`

`export type FactoryFn = (values: any) => unknown;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# WARN cc9b81fc-3935 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/icon.ts:8:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `IconAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3936 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/parent-label.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `ParentLabelAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3937 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/queue.ts:5:10`

Imports `createAnnotationHelper` from `@dxos/echo/internal` to build `QueueAnnotation`. Use the public `Annotation.make` from `@dxos/echo` instead.

# WARN cc9b81fc-3938 no-echo-internal-in-sdk `packages/sdk/schema/src/annotations/view.ts:9:10`

Imports `createAnnotationHelper` (and the `AnnotationHelper` type) from `@dxos/echo/internal` to build `ViewAnnotation`. Use the public `Annotation.make` from `@dxos/echo` in place of `createAnnotationHelper`, typing the base helper with the public `Annotation<T>` interface instead of the internal `AnnotationHelper<T>`.

# ERROR cc9b81fc-3939 no-casts `packages/sdk/schema/src/experimental/json-schema.test.ts:115:41`

`const typeString = String((schema as any).type ?? '');` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3940 no-casts `packages/sdk/schema/src/graph/graph.ts:35:32`

`const target = (object as any)[prop.name]?.target;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3941 no-casts `packages/sdk/schema/src/projection/projection.test.ts:258:41`

`expect(mutable.jsonSchema.properties!.email).to.be.undefined;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3942 no-casts `packages/sdk/schema/src/projection/projection.test.ts:259:41`

`expect(mutable.jsonSchema.properties!.name).to.deep.equal(initialSchemaProps.name);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3943 no-casts `packages/sdk/schema/src/projection/projection.test.ts:717:80`

`const emailId = projectionModel.getFields().find((f) => f.path === 'email')!.id;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3944 no-casts `packages/sdk/schema/src/projection/projection.test.ts:810:34`

`draft.jsonSchema.properties!.status = { type: 'string' };` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3945 no-casts `packages/sdk/schema/src/projection/projection.test.ts:968:67`

`const { field, props } = projection.getFieldProjection(fieldId!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3946 no-casts `packages/sdk/schema/src/stories/ECHO.stories.tsx:25:26`

`const generator = random as any as ValueGenerator;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3947 no-casts `packages/sdk/schema/src/testing/generator.ts:40:38`

`async (specs: TypeSpec[]): Promise<any[]> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3948 no-casts `packages/sdk/schema/src/testing/generator.ts:41:17`

`const result: any[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3949 no-casts `packages/sdk/schema/src/testing/generator.ts:67:25`

`data?: Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3950 no-casts `packages/sdk/schema/src/testing/generator.ts:83:42`

`async (specs: RelationSpec[]): Promise<any[]> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3951 no-casts `packages/sdk/schema/src/testing/generator.ts:84:17`

`const result: any[] = [];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3952 no-casts `packages/sdk/schema/src/testing/generator.ts:115:18`

`type as unknown as Type.AnyObj,` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3953 no-casts `packages/sdk/schema/src/testing/generator.ts:120:13`

`} as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3954 no-casts `packages/sdk/schema/src/testing/generator.ts:121:60`

`result.push(addToDatabase(db)(Relation.make(type as any, props as any)));` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3955 no-casts `packages/sdk/schema/src/testing/generator.ts:142:16`

`if ((obj as any)[name] === undefined && name !== 'id') {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3956 no-casts `packages/sdk/schema/src/testing/generator.ts:143:14`

`(obj as any)[name] = createValue(generator, schema, property, force);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3957 no-casts `packages/sdk/schema/src/testing/generator.ts:159:2`

`): any | undefined => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3958 no-casts `packages/sdk/schema/src/testing/generator.ts:175:34`

`const fn = getDeep<(...args: any[]) => any>(generator, generatorName.split('.'));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3959 no-casts `packages/sdk/schema/src/testing/generator.ts:204:52`

`const refsToSet: Array<{ name: PropertyKey; ref: any }> = [];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3960 no-casts `packages/sdk/schema/src/testing/generator.ts:227:22`

`Obj.update(obj as any, (mutableObj: any) => {` casts with `as any`, suppressing the type checker entirely, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-3961 no-casts `packages/sdk/schema/src/testing/generator.ts:239:84`

`return (data: Entity.Properties<Type.InstanceType<S>>) => Obj.make<S>(type, data as any);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3962 no-casts `packages/sdk/schema/src/testing/generator.ts:244:74`

`return <T extends AnyProperties>(obj: T): Obj.OfShape<T> => db.add(obj as any) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3963 no-casts `packages/sdk/schema/src/testing/generator.ts:247:51`

`export const logObject = (message: string) => (obj: any) => log.info(message, { obj });` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3964 no-casts `packages/sdk/schema/src/util/formatting.ts:12:8`

`value: any;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3965 no-casts `packages/sdk/schema/src/util/schema.ts:89:15`

`const fields: any = Object.fromEntries(` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3966 no-casts `packages/sdk/schema/src/util/schema.ts:102:52`

`const jsonProp = schema.jsonSchema.properties![prop.name] as Mutable<JsonSchemaType>;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3967 no-casts `packages/sdk/schema/src/util/validate.ts:19:59`

`export const validateSchema = <T>(schema: Schema.Codec<T, any>, values: any): ValidationError[] | undefined => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3968 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.stories.tsx:23:30`

`export const Default = (props: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3969 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.tsx:36:74`

`member.identity.identityKey.equals(client.halo.identity.get()!.identityKey) ? -1 : 1,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3970 no-casts `packages/sdk/shell/src/components/IdentityList/SpaceMemberList.tsx:39:87`

`(member) => !member.identity.identityKey.equals(client.halo.identity.get()!.identityKey),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3971 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useEdgeAgentsHandlers.ts:74:38`

`await service.createAgent(null as any, { timeout: 7_500 });` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3972 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useEdgeAgentsHandlers.ts:81:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3973 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:39:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3974 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:62:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3975 no-casts `packages/sdk/shell/src/panels/IdentityPanel/useKubeAgentHandlers.ts:80:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3976 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:94:23`

`onError?: (error: any) => void,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3977 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:137:15`

`(error: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3978 no-casts `packages/sdk/shell/src/panels/JoinPanel/joinMachine.ts:176:98`

`src: (context) => context[Kind.toLowerCase() as Lowercase<Kind>].invitationSubscribable!,` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3979 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:14:34`

`const getComponentNames = (module: any): string[] =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3980 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:18:30`

`const getComponents = (module: any): FC[] => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3981 no-casts `packages/sdk/shell/src/panels/Panels.stories.tsx:39:30`

`export const Default = (props: any) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3982 no-casts `packages/sdk/shell/src/panels/SpacePanel/SpacePanelProps.ts:18:36`

`send: (event: SingleOrArray<Event<any>>) => void;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3983 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:35:15`

`(window as any)[`peer${id}CreateSpaceInvitation`] = (options?: Partial<Invitation>) => {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3984 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:126:13`

`(window as any)[`peer${id}CreateHaloInvitation`] = (options?: Partial<Invitation>) => {` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3985 no-casts `packages/sdk/shell/src/stories/Invitations.stories.tsx:204:77`

`<IdentityListItem identity={identity} presence={networkStatus as unknown as SpaceMember.PresenceState} />` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3986 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:121:23`

`return displayName!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3987 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:166:21`

`return spaceName!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-3988 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:295:15`

`(window as any)[`peer${id}CreateSpaceInvitation`](options);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3989 no-casts `packages/sdk/shell/src/testing/invitations-test-manager.ts:298:15`

`(window as any)[`peer${id}CreateHaloInvitation`](options);` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3990 no-casts `packages/sdk/types/src/testing/generator.test.ts:42:3`

`} as any as ValueGenerator;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-3991 no-casts `packages/sdk/types/src/testing/generator.test.ts:51:32`

`objects: objects.map((obj: any) => stripUndefined({ name: obj.name, employer: obj.employer?.name })),` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3992 no-casts `packages/sdk/versioning/src/branch.test.ts:43:33`

`const activeSuggestions = (doc: any, creator: string) =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3993 no-casts `packages/sdk/versioning/src/branch.test.ts:45:14`

`(branch: any) => branch.status === 'active' && branch.kind === 'suggestion' && branch.creator === creator,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3994 no-casts `packages/sdk/worker-framework/src/Client.ts:47:27`

`config?: Record<string, any>;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3995 no-casts `packages/sdk/worker-framework/src/Client.ts:97:36`

`readonly #config: Record<string, any> | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-3996 no-casts `packages/sdk/worker-framework/src/Client.ts:227:9`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3997 no-casts `packages/sdk/worker-framework/src/Client.ts:345:11`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3998 no-casts `packages/sdk/worker-framework/src/Client.ts:373:7`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-3999 no-casts `packages/sdk/worker-framework/src/Client.ts:408:7`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-4000 no-casts `packages/sdk/worker-framework/src/Client.ts:429:36`

`readonly #config: Record<string, any> | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4001 no-casts `packages/sdk/worker-framework/src/Client.ts:439:28`

`config: Record<string, any> | undefined,` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4002 no-casts `packages/sdk/worker-framework/src/Coordinator/coordinator-worker.test.ts:41:40`

`onConnect({ ports: [channel.port1] } as unknown as MessageEvent);` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# WARN cc9b81fc-4003 no-sleep-in-test `packages/sdk/worker-framework/src/Coordinator/coordinator-worker.test.ts:131`

Three tests (lines 131, 141, 162) use `await sleep(20)` as the literal synchronization point before asserting a message was *not* broadcast/received. Per `no-sleep-in-test`, replace the fixed delay with a deterministic wait — e.g. a `Trigger`/queue on the peer under test, or `waitForCondition` — so the assertion doesn't depend on a real 20ms window.

# ERROR cc9b81fc-4004 no-casts `packages/sdk/worker-framework/src/internal/locks.ts:59:5`

`} catch (error: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-4005 no-casts `packages/sdk/worker-framework/src/Worker.ts:58:28`

`config: Record<string, any> | undefined;` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4006 no-casts `packages/sdk/worker-framework/src/Worker.ts:64:22`

`const scope = self as unknown as WorkerProtocol.WorkerEndpoint & { close(): void };` uses the `as unknown as T` double-cast escape hatch to force an unrelated type past the checker, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-4007 no-casts `packages/sdk/worker-framework/src/Worker.ts:127:9`

`} catch (err: any) {` types the caught error as `any`, widening away TypeScript's default `unknown` and skipping the narrowing check before the error is used, which the no-casts rule bans (severity: error). Catch it as `unknown` and narrow (e.g. `instanceof Error`) before accessing its properties, instead of widening to `any`.

# ERROR cc9b81fc-4008 no-casts `packages/stories/stories-assistant/src/stories/Sketch.stories.tsx:110:57`

`const records = Object.values(canvas.content ?? {}) as any[];` casts with `as any`, suppressing the type checker entirely, and also widens to `any` in a signature/type position — both banned by the no-casts rule (severity: error). Fix the type at its source (a precise type, generic, or guard) for each construct flagged above instead of suppressing the checker.

# ERROR cc9b81fc-4009 no-casts `packages/stories/stories-assistant/src/testing/decorators.tsx:113:9`

`types?: any[];` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4010 no-casts `packages/stories/stories-assistant/src/testing/decorators.tsx:408:6`

`}) as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-4011 no-casts `packages/stories/stories-assistant/vite.config.ts:26:65`

`configureServer: async (server: { middlewares: { use: (handler: any) => void } }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4012 no-casts `packages/stories/stories-brain/src/test/model-ladder.bench.test.ts:80:111`

`const reference = variants.find((variant) => variant.name.includes(LADDER_REFERENCE)) ?? variants.at(-1)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4013 no-casts `packages/stories/stories-brain/src/test/model-ladder.bench.test.ts:124:85`

`const referenceTags = ladder.find((entry) => entry.model === reference.name)!.runs.map((run) => run.output);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4014 no-casts `packages/stories/stories-brain/src/testing/harness/internal/eval-report.ts:144:45`

`const wins = pairs.filter((pair) => pair!.winner === 'candidate').length;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4015 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:30:30`

`const feed = mailbox.feed!.target!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4016 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:60:35`

`expect(serialized.map((message: any) => message.properties?.subject).sort()).toEqual(['First', 'Second']);` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4017 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:67:36`

`const next = await mailbox.feed!.tryLoad();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4018 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:69:16`

`expect(next!.id).not.toBe(original.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4019 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:73:22`

`Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4020 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:78:54`

`const originalIds = new Set(serialized.map((entry: any) => entry.id));` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4021 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:94:36`

`const next = await mailbox.feed!.tryLoad();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4022 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:95:16`

`expect(next!.id).not.toBe(original.id);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4023 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:97:22`

`Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4024 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:130:36`

`const next = await mailbox.feed!.tryLoad();` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4025 no-casts `packages/stories/stories-inbox/src/testing/archive.test.ts:132:22`

`Feed.query(next!, Filter.type(Message.Message)).run.pipe(Effect.provide(Database.layer(db))),` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4026 no-casts `packages/stories/stories-inbox/src/testing/archive.ts:41:33`

`const reconstructMessage = (json: any): Message.Message =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4027 no-casts `packages/stories/stories-inbox/src/testing/plugins.ts:118:43`

`generateObject: (options: any) =>` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4028 no-casts `packages/stories/stories-inbox/src/testing/plugins.ts:121:19`

`} as any),` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-4029 no-casts `packages/stories/stories-lens/src/components.tsx:52:78`

`const changedOnly = <T extends object>(values: T, { changed }: FormUpdateMeta<any>): Partial<T> => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4030 no-casts `packages/stories/stories-lens/src/components.tsx:88:42`

`(values: object, meta: FormUpdateMeta<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4031 no-casts `packages/stories/stories-lens/src/components.tsx:138:42`

`(values: object, meta: FormUpdateMeta<any>) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4032 no-casts `packages/stories/stories-lens/src/stories/ObjectLens.stories.tsx:70:67`

`const onCreateSpace = async ({ space }: { space: { db: { add: (obj: any) => any } } }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4033 no-casts `packages/stories/stories-lens/src/stories/ObjectLens.stories.tsx:117:66`

`canvasElement.querySelector<T>(`[data-testid="${testId}"]`)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4034 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:51:4`

`)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4035 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:107:67`

`const onCreateSpace = async ({ space }: { space: { db: { add: (obj: any) => any } } }) => {` widens to `any` in a signature/type position, which the no-casts rule bans (severity: error). Give the parameter, return value, or field a concrete type (or a generic) instead of `any` so callers keep type checking.

# ERROR cc9b81fc-4036 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:152:66`

`canvasElement.querySelector<T>(`[data-testid="${testId}"]`)!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4037 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:171:63`

`const strong = editor.querySelector<HTMLElement>('strong')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4038 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:172:55`

`const em = editor.querySelector<HTMLElement>('em')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4039 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:180:61`

`const paragraph = editor.querySelector<HTMLElement>('p')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4040 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:181:61`

`const heading1 = editor.querySelector<HTMLElement>('h1')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4041 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:182:61`

`const heading2 = editor.querySelector<HTMLElement>('h2')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4042 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:199:57`

`const list = editor.querySelector<HTMLElement>('ul')!;` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4043 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:202:59`

`await expect(getComputedStyle(list.querySelector('li')!).display).toBe('list-item');` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4044 no-casts `packages/stories/stories-lens/src/stories/RichTextLens.stories.tsx:208:66`

`await userEvent.click(editor.querySelector<HTMLElement>('h1')!);` uses the non-null assertion `!` to bypass the type checker's null/undefined check, which the no-casts rule bans (severity: error). Fix the type at its source: narrow with a guard, use optional chaining, or change the underlying declaration so the value's presence is proven rather than asserted.

# ERROR cc9b81fc-4045 no-casts `packages/stories/storybook-testing/src/Cell.test.ts:10:34`

`const object = { id: 'obj-1' } as any;` casts with `as any`, suppressing the type checker entirely, which the no-casts rule bans (severity: error). Replace it with a precise type, a type guard, or a generic parameter so the value's shape is actually validated, per the no-casts rule.

# ERROR cc9b81fc-4046 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:12:40`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4047 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:19:36`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4048 no-casts `packages/stories/storybook-testing/src/ModuleContainer.test.tsx:29:36`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4049 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:36:20`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4050 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:38:23`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4051 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:47:14`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4052 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:48:22`

This line widens this type position to `any` (`<any>`); and also widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4053 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:65:18`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4054 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:67:23`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4055 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:73:18`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4056 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:75:22`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4057 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:145:74`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4058 no-casts `packages/stories/storybook-testing/src/ModuleContainer.tsx:163:55`

This line widens this type position to `any` (`<any>`); and also widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4059 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:31:42`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4060 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:218:20`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4061 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:237:111`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4062 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:245:91`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4063 no-casts `packages/ui/brand/src/components/experimental/experimental.tsx:252:16`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4064 no-casts `packages/ui/brand/src/components/experimental/Logo.stories.tsx:229:28`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4065 no-casts `packages/ui/brand/src/components/experimental/rive.stories.tsx:16:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4066 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:95:40`

This line uses the non-null assertion `!` (`ySelector('dx-grid')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4067 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:96:17`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4068 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:103:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4069 no-casts `packages/ui/lit-grid/src/testing/dx-grid-manager.ts:106:23`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4070 no-casts `packages/ui/lit-ui/src/dx-icon/dx-icon.ts:21:65`

This line uses the double-cast escape hatch `as unknown as RegistryHost`, forcing the value to `RegistryHost` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4071 no-casts `packages/ui/react-primitives/react-error-boundary/src/ErrorBoundary.tsx:29:13`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4072 no-casts `packages/ui/react-primitives/react-error-boundary/src/ErrorBoundary.tsx:105:24`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4073 no-casts `packages/ui/react-primitives/react-hooks/src/useAsyncState.ts:12:7`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4074 no-casts `packages/ui/react-primitives/react-hooks/src/useControlledState.ts:36:55`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4075 no-casts `packages/ui/react-primitives/react-hooks/src/useDebugDeps.ts:19:42`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4076 no-casts `packages/ui/react-primitives/react-hooks/src/useId.ts:12:32`

This line uses the double-cast escape hatch `as unknown as PrngFactory`, forcing the value to `PrngFactory` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4077 no-casts `packages/ui/react-primitives/react-hooks/src/useTimeout.ts:7:75`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4078 no-casts `packages/ui/react-primitives/react-hooks/src/useTimeout.ts:26:7`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4079 no-casts `packages/ui/react-primitives/react-hooks/src/useTransitions.ts:7:39`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4080 no-casts `packages/ui/react-ui-attention/src/components/AttentionGlyph/AttentionGlyph.stories.tsx:32:29`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4081 no-casts `packages/ui/react-ui-attention/src/components/AttentionProvider/attention-context.ts:21:24`

This line uses the double-cast escape hatch `as unknown as AttentionManager`, forcing the value to `AttentionManager` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4082 no-casts `packages/ui/react-ui-audio/src/hooks/useAudioStream.ts:58:53`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4083 no-casts `packages/ui/react-ui-audio/src/hooks/useAudioStream.ts:59:56`

This line uses the non-null assertion `!` (`ioContextRef.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4084 no-casts `packages/ui/react-ui-canvas-compute/src/compute.stories.tsx:199:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4085 no-casts `packages/ui/react-ui-canvas-compute/src/compute.stories.tsx:233:9`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4086 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:68:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4087 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:90:69`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4088 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:91:70`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4089 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:92:44`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4090 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:239:52`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4091 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:344:45`

This line widens this type position to `any` (`, any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4092 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:439:95`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4093 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:441:96`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4094 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:443:70`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4095 no-casts `packages/ui/react-ui-canvas-compute/src/graph/controller.ts:452:39`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4096 no-casts `packages/ui/react-ui-canvas-compute/src/graph/createComputeGraph.test.ts:53:70`

This line uses the non-null assertion `!` (`edge.target === gpt!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4097 no-casts `packages/ui/react-ui-canvas-compute/src/graph/createComputeGraph.test.ts:54:70`

This line uses the non-null assertion `!` (`edge.source === gpt!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4098 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:21:40`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4099 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:54:42`

This line uses the non-null assertion `!` (`.evalNode(shape.node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4100 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:69:47`

This line uses the non-null assertion `!` (`mputeNode(shape.node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4101 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:75:46`

This line uses the non-null assertion `!` (`getInputs(shape.node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4102 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:76:48`

This line uses the non-null assertion `!` (`etOutputs(shape.node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4103 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:77:42`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4104 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useComputeNodeState.ts:78:40`

This line uses the non-null assertion `!` (`setOutput(shape.node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4105 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useGraphMonitor.ts:117:53`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4106 no-casts `packages/ui/react-ui-canvas-compute/src/hooks/useGraphMonitor.ts:130:40`

This line uses the non-null assertion `!` (`gger.functionTrigger!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`ctionTrigger!.target!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4107 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:21:47`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4108 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:28:23`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4109 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/common/function-anchors.ts:29:24`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4110 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Constant.tsx:28:25`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4111 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/defs.ts:28:34`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4112 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Feed.tsx:39:70`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4113 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Thread.tsx:15:14`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4114 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Thread.tsx:36:72`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4115 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/trigger-def.ts:36:76`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4116 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/trigger-spec.ts:36:57`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4117 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Trigger.tsx:39:33`

This line uses the non-null assertion `!` (`date(functionTrigger!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4118 no-casts `packages/ui/react-ui-canvas-compute/src/shapes/Trigger.tsx:56:57`

This line uses the non-null assertion `!` (`functionTrigger.spec!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`onTrigger.spec!.kind!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4119 no-casts `packages/ui/react-ui-canvas-editor/src/components/Canvas/Shape.tsx:28:58`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4120 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:25:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4121 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:106:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4122 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:117:33`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4123 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:121:43`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4124 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:126:36`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4125 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.stories.tsx:135:55`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4126 no-casts `packages/ui/react-ui-canvas-editor/src/components/Editor/Editor.tsx:109:54`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4127 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useActionHandler.ts:216:39`

This line uses the non-null assertion `!` (`= overlayRef.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4128 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:52:25`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4129 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:97:26`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4130 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:99:64`

This line widens this type position to `any` (`: any`); and also uses the non-null assertion `!` (`any) => d.id === id)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`; handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4131 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:100:46`

This line uses the non-null assertion `!` (`h.getNode(`${id}-0`)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4132 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:197:37`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4133 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:201:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4134 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:225:24`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4135 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:226:55`

This line widens this type position to `any` (`, any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4136 no-casts `packages/ui/react-ui-canvas-editor/src/hooks/useRope.ts:230:8`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4137 no-casts `packages/ui/react-ui-canvas-editor/src/layout/bullets.ts:26:64`

This line uses the non-null assertion `!` (`ibute(DATA_SHAPE_ID)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4138 no-casts `packages/ui/react-ui-canvas-editor/src/layout/bullets.ts:89:31`

This line widens this type position to `any` (`<any, any,`); and also widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4139 no-casts `packages/ui/react-ui-canvas-editor/src/layout/layout.ts:70:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4140 no-casts `packages/ui/react-ui-canvas-editor/src/shapes/defs.ts:22:34`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4141 no-casts `packages/ui/react-ui-canvas-editor/src/testing/DragTest.tsx:63:54`

This line uses the non-null assertion `!` (`(draggingRef.current!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`rrent!.parentElement!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4142 no-casts `packages/ui/react-ui-canvas-editor/src/testing/useSelection.ts:26:41`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4143 no-casts `packages/ui/react-ui-canvas-editor/src/testing/useSelection.ts:27:30`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4144 no-casts `packages/ui/react-ui-canvas-editor/src/types/model.ts:23:24`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4145 no-casts `packages/ui/react-ui-canvas/src/components/CellGrid/CellGrid.stories.tsx:176:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4146 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:28:13`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4147 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:30:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4148 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:40:11`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4149 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:41:13`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4150 no-casts `packages/ui/react-ui-canvas/src/util/util.ts:42:13`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4151 no-casts `packages/ui/react-ui-card/src/components/Avatar/ObjectAvatar.tsx:29:25`

This line uses the double-cast escape hatch `as unknown as Record<string`, forcing the value to `Record<string` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4152 no-casts `packages/ui/react-ui-components/src/components/QueryEditor/query-extension.ts:335:62`

This line uses the non-null assertion `!` (`r.split(/\W/).at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4153 no-casts `packages/ui/react-ui-components/src/components/QueryEditor/query-extension.ts:376:38`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4154 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:23:64`

This line uses the non-null assertion `!` (`eById.get(commit.id)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4155 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:32:79`

This line uses the non-null assertion `!` (`w(commit, lanes[row]!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`maxLane, active[row]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4156 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:53:51`

This line uses the non-null assertion `!` (`d.get(commit.parents!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`t(commit.parents![0]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4157 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:129:32`

This line uses the non-null assertion `!` (`ommit = commits[row]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4158 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:140:31`

This line uses the non-null assertion `!` (`Lane.set(branches[0]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4159 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:147:32`

This line uses the non-null assertion `!` (`ommit = commits[row]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4160 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:158:58`

This line uses the non-null assertion `!` (`e.get(commit.branch)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4161 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:163:50`

This line uses the non-null assertion `!` (`anchLane.get(branch)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4162 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:178:45`

This line uses the non-null assertion `!` (`eById.get(parents[0]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4163 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:194:31`

This line uses the non-null assertion `!` (`icesByCol[lanes[row]!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`esByCol[lanes[row]!]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4164 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:203:34`

This line uses the non-null assertion `!` (`cesByCol[parentLane]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4165 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:209:41`

This line uses the non-null assertion `!` (`rowIndicesByCol[col]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4166 no-casts `packages/ui/react-ui-components/src/components/Timeline/timeline-printer.ts:217:20`

This line uses the non-null assertion `!` (`active[row]!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4167 no-casts `packages/ui/react-ui-dashboard/src/util.test.ts:21:39`

This line uses the non-null assertion `!` (`alendar.cells.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4168 no-casts `packages/ui/react-ui-dashboard/src/util.test.ts:49:33`

This line uses the non-null assertion `!` (`alendar.cells.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4169 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:22:26`

This line uses the non-null assertion `!` (`xpect(nodes.get('X')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('X')!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`an(nodes.get('CORE')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`.get('CORE')!.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4170 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:23:29`

This line uses the non-null assertion `!` (`ct(nodes.get('CORE')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`.get('CORE')!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`sThan(nodes.get('Y')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('Y')!.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4171 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:29:26`

This line uses the non-null assertion `!` (`xpect(nodes.get('A')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('A')!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`sThan(nodes.get('B')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('B')!.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4172 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:30:26`

This line uses the non-null assertion `!` (`xpect(nodes.get('B')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('B')!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`.toBe(nodes.get('C')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`des.get('C')!.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4173 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:35:35`

This line uses the non-null assertion `!` (`= nodes.get('CORE')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4174 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:38:34`

This line uses the non-null assertion `!` (`hild = nodes.get(id)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4175 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:40:26`

This line uses the non-null assertion `!` (`expect(child.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4176 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:41:26`

This line uses the non-null assertion `!` (`expect(child.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4177 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:45:73`

This line uses the non-null assertion `!` (`id) => nodes.get(id)!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`odes.get(id)!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`n!.x + nodes.get(id)!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`nodes.get(id)!.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4178 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:46:73`

This line uses the non-null assertion `!` (`id) => nodes.get(id)!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`odes.get(id)!.origin!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`n!.y + nodes.get(id)!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`nodes.get(id)!.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4179 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:47:21`

This line uses the non-null assertion `!` (`expect(core.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4180 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:48:21`

This line uses the non-null assertion `!` (`expect(core.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4181 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:54:25`

This line uses the non-null assertion `!` (`expect(node.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4182 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:55:25`

This line uses the non-null assertion `!` (`expect(node.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4183 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:56:23`

This line uses the non-null assertion `!` (`expect(node.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4184 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:57:23`

This line uses the non-null assertion `!` (`expect(node.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4185 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:66:25`

This line uses the non-null assertion `!` (`expect(node.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4186 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:67:25`

This line uses the non-null assertion `!` (`expect(node.origin!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4187 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:71:67`

This line uses the non-null assertion `!` (`node.id === 'CORE')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4188 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:72:21`

This line uses the non-null assertion `!` (`expect(core.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4189 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:73:21`

This line uses the non-null assertion `!` (`expect(core.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4190 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:81:58`

This line uses the non-null assertion `!` (`=> node.id === 'X')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4191 no-casts `packages/ui/react-ui-diagram/src/model/layout.test.ts:83:58`

This line uses the non-null assertion `!` (`=> node.id === 'Y')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4192 no-casts `packages/ui/react-ui-editor/src/components/EditorMenuProvider/EditorMenuProvider.tsx:104:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4193 no-casts `packages/ui/react-ui-editor/src/components/EditorPreviewProvider/EditorPreviewProvider.tsx:55:54`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4194 no-casts `packages/ui/react-ui-editor/src/stories/Popover.stories.tsx:31:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4195 no-casts `packages/ui/react-ui-experimental/src/components/Chaos/Chaos.tsx:95:27`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4196 no-casts `packages/ui/react-ui-experimental/src/components/Chaos/Chaos.tsx:96:24`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4197 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:99:65`

This line uses the non-null assertion `!` (`.mean(b.last ?? [0])!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4198 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:100:65`

This line uses the non-null assertion `!` (`.mean(b.last ?? [0])!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4199 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:121:47`

This line uses the non-null assertion `!` (`y.add(b.acceleration!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4200 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:240:42`

This line uses the non-null assertion `!` (`vas.getContext('2d')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4201 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:300:24`

This line uses the non-null assertion `!` (`b1.acceleration!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4202 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:520:48`

This line uses the non-null assertion `!` (`ent.getContext('2d')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4203 no-casts `packages/ui/react-ui-experimental/src/components/Flock/Flock.tsx:525:26`

This line uses the non-null assertion `!` (`tick(canvas.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4204 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:149:58`

This line uses the non-null assertion `!` (`eUniform(program, i)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4205 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:567:32`

This line uses the non-null assertion `!` (`gba = ext.formatRGBA!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4206 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:568:28`

This line uses the non-null assertion `!` (`st rg = ext.formatRG!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4207 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:569:26`

This line uses the non-null assertion `!` (`onst r = ext.formatR!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4208 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:607:39`

This line uses the non-null assertion `!` (`= gl.createTexture()!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4209 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:615:39`

This line uses the non-null assertion `!` (`.createFramebuffer()!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4210 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:646:68`

This line uses the non-null assertion `!` (`format, type, param)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4211 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:647:68`

This line uses the non-null assertion `!` (`format, type, param)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4212 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:706:18`

This line uses the non-null assertion `!` (`return newFBO!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4213 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:722:84`

This line uses the non-null assertion `!` (`format, type, param)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4214 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:723:72`

This line uses the non-null assertion `!` (`format, type, param)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4215 no-casts `packages/ui/react-ui-experimental/src/components/Ghost/ghost-renderer.tsx:978:38`

This line uses the non-null assertion `!` (`) => pointers.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4216 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:16:9`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4217 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:17:17`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4218 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:18:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4219 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:19:9`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4220 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:20:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4221 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:21:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4222 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:22:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4223 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:23:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4224 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:24:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4225 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:25:21`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4226 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:26:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4227 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:31:54`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4228 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:46:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4229 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:47:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4230 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:48:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4231 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:129:37`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4232 no-casts `packages/ui/react-ui-experimental/src/components/Kube/Kube.tsx:283:33`

This line uses the non-null assertion `!` (`t(contentRef.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4233 no-casts `packages/ui/react-ui-experimental/src/shaders/material.ts:102:88`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4234 private-new-packages `packages/ui/react-ui-feed/package.json:1:1`

This `package.json` is newly added in this change (the `react-ui-feed` package does not exist in the parent commit) and is missing `"private": true`; it additionally declares `"publishConfig": { "access": "public" }`, actively opting into publishing. Per the `private-new-packages` rule, every newly-added package must set `"private": true` until a trusted publisher exists — add that field (and drop `publishConfig` until it is ready to publish).

# ERROR cc9b81fc-4235 no-casts `packages/ui/react-ui-feed/src/components/MessageList/MessageList.tsx:420:17`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4236 no-casts `packages/ui/react-ui-feed/src/stories/assistant.stories.tsx:285:86`

This line uses the non-null assertion `!` (`.prototype, 'value')!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`otype, 'value')!.set!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4237 no-casts `packages/ui/react-ui-feed/src/stories/assistant.stories.tsx:304:95`

This line uses the non-null assertion `!` (`d="feed.viewport"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4238 no-casts `packages/ui/react-ui-feed/src/stories/assistant.stories.tsx:315:100`

This line uses the non-null assertion `!` (`assistant.prompt"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4239 no-casts `packages/ui/react-ui-feed/src/stories/assistant.stories.tsx:373:95`

This line uses the non-null assertion `!` (`d="feed.viewport"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4240 no-casts `packages/ui/react-ui-feed/src/stories/streaming.stories.tsx:96:84`

This line uses the non-null assertion `!` (`- a.tops.get(index)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4241 no-casts `packages/ui/react-ui-feed/src/stories/streaming.stories.tsx:103:95`

This line uses the non-null assertion `!` (`d="feed.viewport"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4242 no-casts `packages/ui/react-ui-feed/src/stories/streaming.stories.tsx:121:32`

This line uses the non-null assertion `!` (`ded = samples.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4243 no-casts `packages/ui/react-ui-feed/src/testing/MessageWindow.tsx:33:35`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4244 no-casts `packages/ui/react-ui-feed/src/testing/MessageWindow.tsx:129:35`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4245 no-casts `packages/ui/react-ui-feed/src/testing/scenarios.tsx:70:35`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4246 no-casts `packages/ui/react-ui-feed/src/testing/widgets.tsx:79:69`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4247 no-casts `packages/ui/react-ui-feed/src/testing/widgets.tsx:132:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4248 no-casts `packages/ui/react-ui-feed/src/virtualizer/placement.test.ts:238:42`

This line uses the non-null assertion `!` (`dexOf(offsets.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4249 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:342:95`

This line uses the non-null assertion `!` (`"window.scroller"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4250 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:343:88`

This line uses the non-null assertion `!` (`d="window.window"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4251 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:344:89`

This line uses the non-null assertion `!` (`id="window.sizer"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4252 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:361:87`

This line uses the non-null assertion `!` (`fore.rows.get(index)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4253 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:426:97`

This line uses the non-null assertion `!` (`"window.scroller"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4254 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:437:88`

This line uses the non-null assertion `!` (`rows.get(index - 20)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4255 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:526:83`

This line uses the non-null assertion `!` (`placement.report"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4256 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:542:94`

This line uses the non-null assertion `!` (`estid="${testId}"]`)!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`Id}"]`)!.textContent!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4257 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:547:45`

This line uses the non-null assertion `!` (`roller.parentElement!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4258 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:565:88`

This line uses the non-null assertion `!` (`avigation"] button')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4259 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:585:74`

This line uses the non-null assertion `!` (`-testid="minimap"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4260 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:586:70`

This line uses the non-null assertion `!` (`role="navigation"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4261 no-casts `packages/ui/react-ui-feed/src/virtualizer/Window.stories.tsx:647:97`

This line uses the non-null assertion `!` (`"window.scroller"]')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4262 no-casts `packages/ui/react-ui-form/src/annotations.ts:22:25`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4263 no-casts `packages/ui/react-ui-form/src/annotations.ts:51:27`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4264 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.stories.tsx:59:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4265 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.stories.tsx:81:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4266 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.test.tsx:37:27`

This line uses the non-null assertion `!` (`reEvent.click(option!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4267 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.test.tsx:47:63`

This line uses the non-null assertion `!` (`aseSchema.properties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4268 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.test.tsx:55:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4269 no-casts `packages/ui/react-ui-form/src/components/FieldEditor/FieldEditor.tsx:25:50`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4270 no-casts `packages/ui/react-ui-form/src/components/Form/Form.stories.tsx:102:27`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4271 no-casts `packages/ui/react-ui-form/src/components/Form/Form.stories.tsx:202:49`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4272 no-casts `packages/ui/react-ui-form/src/components/Form/Form.stories.tsx:227:39`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4273 no-casts `packages/ui/react-ui-form/src/components/Form/FormControls.tsx:190:82`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4274 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/ArrayField/ArrayField.stories.tsx:78:74`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4275 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/ArrayField/ArrayField.tsx:39:28`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4276 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/ArrayField/ArrayField.tsx:96:58`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4277 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/ArrayField/default-value.ts:17:53`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4278 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.stories.tsx:65:61`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4279 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.stories.tsx:162:79`

This line uses the non-null assertion `!` (`ment>('.cm-content')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4280 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.tsx:43:41`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4281 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.tsx:51:41`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4282 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.tsx:81:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4283 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/MarkdownField/MarkdownField.tsx:91:21`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4284 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/NumberField/numeric-constraints.ts:13:49`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4285 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/find-ref-option.ts:14:27`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4286 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/find-ref-option.ts:15:49`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4287 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:49:42`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4288 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:93:21`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4289 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:116:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4290 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/InlineRefField.tsx:139:72`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4291 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/RefField.stories.tsx:33:53`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4292 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/RefField.stories.tsx:49:32`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4293 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/RefField/RefField.tsx:133:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4294 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/fields/SelectOptionField/SelectOptionField.stories.tsx:54:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4295 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/FormField.tsx:123:52`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4296 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/FormField.tsx:279:71`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4297 no-casts `packages/ui/react-ui-form/src/components/Form/FormField/FormField.tsx:283:46`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4298 no-casts `packages/ui/react-ui-form/src/components/Form/FormFieldSet/FormFieldSet.tsx:63:21`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4299 no-casts `packages/ui/react-ui-form/src/components/Form/FormFieldSet/FormFieldSet.tsx:120:62`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4300 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:129:23`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4301 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:136:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4302 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:204:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4303 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:246:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4304 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.stories.tsx:310:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4305 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:29:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4306 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:30:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4307 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:31:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4308 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:37:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4309 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:38:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4310 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:39:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4311 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:41:81`

This line uses the non-null assertion `!` (`Schema.Top>(resolved!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`(resolved!.labelType!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4312 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:51:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4313 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:52:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4314 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:54:20`

This line uses the non-null assertion `!` (`expect(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4315 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.test.ts:55:46`

This line uses the non-null assertion `!` (`ringKeyword(resolved!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`AST.isUnion(resolved!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4316 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/FormLayout.tsx:141:24`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4317 no-casts `packages/ui/react-ui-form/src/components/Form/FormLayout/resolve-layout-field.ts:66:57`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4318 no-casts `packages/ui/react-ui-form/src/components/Form/meta-tags.test.ts:21:44`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4319 no-casts `packages/ui/react-ui-form/src/components/Form/meta-tags.test.ts:36:40`

This line uses the non-null assertion `!` (`.unwrapOptional(tags!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4320 no-casts `packages/ui/react-ui-form/src/components/Form/meta-tags.test.ts:41:48`

This line uses the non-null assertion `!` (`ptional(tagsProperty!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4321 no-casts `packages/ui/react-ui-form/src/components/Form/meta-tags.ts:30:50`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4322 no-casts `packages/ui/react-ui-form/src/components/Form/meta-tags.ts:34:29`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4323 no-casts `packages/ui/react-ui-form/src/components/ObjectForm/ObjectForm.tsx:28:24`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4324 no-casts `packages/ui/react-ui-form/src/components/ObjectForm/ObjectForm.tsx:53:65`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4325 no-casts `packages/ui/react-ui-form/src/components/ObjectForm/ObjectForm.tsx:67:47`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4326 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.stories.tsx:65:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4327 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.tsx:21:32`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4328 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.tsx:29:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4329 no-casts `packages/ui/react-ui-form/src/components/ObjectPicker/ObjectPicker.tsx:59:20`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4330 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:67:33`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4331 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:100:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4332 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:122:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4333 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:140:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4334 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.stories.tsx:183:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4335 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:19:25`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4336 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:21:15`

This line uses the non-null assertion `!` (`return debug!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4337 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:39:20`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4338 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:53:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4339 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:63:91`

This line casts `as any`, discarding the compiler's type checking for this expression; and also widens this type position to `any` (`any[]`). Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4340 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:64:41`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4341 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:70:28`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4342 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:72:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4343 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:87:89`

This line casts `as any`, discarding the compiler's type checking for this expression; and also widens this type position to `any` (`any[]`). Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4344 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:88:39`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4345 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:91:18`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4346 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:96:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4347 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.test.tsx:98:34`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4348 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:73:36`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4349 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:86:49`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4350 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:114:49`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4351 no-casts `packages/ui/react-ui-form/src/components/ObjectProperties/ObjectProperties.tsx:131:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4352 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:111:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4353 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:112:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4354 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.stories.tsx:126:35`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4355 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.tsx:23:25`

This line widens this type position to `any` (`, any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4356 no-casts `packages/ui/react-ui-form/src/components/ObjectTree/ObjectTree.tsx:36:66`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4357 no-casts `packages/ui/react-ui-form/src/components/RefEditor/RefEditor.stories.tsx:22:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4358 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.stories.tsx:120:15`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4359 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:20:32`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4360 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:77:28`

This line uses the non-null assertion `!` (`ect(schemaProperties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4361 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:78:28`

This line uses the non-null assertion `!` (`ect(schemaProperties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4362 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:81:77`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4363 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:83:28`

This line uses the non-null assertion `!` (`ect(newPropertyField!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4364 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:87:62`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4365 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:89:33`

This line uses the non-null assertion `!` (`ewPropertyProjection!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4366 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:125:28`

This line uses the non-null assertion `!` (`ect(schemaProperties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4367 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:129:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4368 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:132:30`

This line uses the non-null assertion `!` (`t(addedPropertyField!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4369 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:136:64`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4370 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:138:35`

This line uses the non-null assertion `!` (`edPropertyProjection!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4371 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:175:28`

This line uses the non-null assertion `!` (`ect(schemaProperties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4372 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:178:70`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4373 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:183:55`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4374 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.test.tsx:208:92`

This line uses the non-null assertion `!` (`> visible === false)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4375 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.tsx:58:23`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4376 no-casts `packages/ui/react-ui-form/src/components/ViewEditor/ViewEditor.tsx:182:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4377 no-casts `packages/ui/react-ui-form/src/hooks/useFormContext.ts:20:54`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4378 no-casts `packages/ui/react-ui-form/src/hooks/useFormContext.ts:84:48`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4379 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:26:26`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4380 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:268:36`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4381 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:282:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4382 no-casts `packages/ui/react-ui-form/src/hooks/useFormHandler.ts:376:25`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4383 no-casts `packages/ui/react-ui-form/src/hooks/useKeyHandler.tsx:14:89`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4384 no-casts `packages/ui/react-ui-form/src/types.ts:38:35`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4385 no-casts `packages/ui/react-ui-form/src/types.ts:48:38`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4386 no-casts `packages/ui/react-ui-form/src/types.ts:78:23`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4387 no-casts `packages/ui/react-ui-form/src/types.ts:91:55`

This line widens this type position to `any` (`<any, any>`); and also widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4388 no-casts `packages/ui/react-ui-form/src/types.ts:136:45`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4389 no-casts `packages/ui/react-ui-form/src/util/field.ts:17:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4390 no-casts `packages/ui/react-ui-form/src/util/omit.ts:14:45`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4391 no-casts `packages/ui/react-ui-form/src/util/omit.ts:16:27`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4392 no-casts `packages/ui/react-ui-form/src/util/omit.ts:21:46`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4393 no-casts `packages/ui/react-ui-form/src/util/omit.ts:27:35`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4394 no-casts `packages/ui/react-ui-form/src/util/omit.ts:53:60`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4395 no-casts `packages/ui/react-ui-form/src/util/omit.ts:60:102`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4396 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:103:8`

This line uses the non-null assertion `!` (`)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4397 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:104:111`

This line uses the non-null assertion `!` (`=> p.name === name)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4398 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:109:72`

This line uses the non-null assertion `!` (`eLiteral, 'routes'))!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4399 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:111:6`

This line uses the non-null assertion `!` (`)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4400 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:113:71`

This line uses the non-null assertion `!` (`ypeLiteral, 'legs'))!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4401 no-casts `packages/ui/react-ui-form/src/util/properties.test.ts:115:6`

This line uses the non-null assertion `!` (`)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4402 no-casts `packages/ui/react-ui-gameboard/src/components/Chessboard/Chessboard.tsx:80:61`

This line uses the non-null assertion `!` (`tion(gridRef.current!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`.current!, location)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4403 no-casts `packages/ui/react-ui-gameboard/src/components/Chessboard/Chessboard.tsx:81:61`

This line uses the non-null assertion `!` (`unds(gridRef.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4404 no-casts `packages/ui/react-ui-gameboard/src/components/Chessboard/Chessboard.tsx:145:79`

This line uses the non-null assertion `!` (`id === promoting.id)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4405 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/Gameboard.tsx:21:49`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4406 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/Gameboard.tsx:29:48`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4407 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/Gameboard.tsx:33:75`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4408 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/GameboardContext.ts:12:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4409 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/GameboardContext.ts:20:38`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4410 no-casts `packages/ui/react-ui-gameboard/src/components/Gameboard/GameboardContext.ts:22:61`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4411 no-casts `packages/ui/react-ui-geo/src/components/Globe/Globe.stories.tsx:159:7`

This line uses the double-cast escape hatch `as any as Topology`, forcing the value to `Topology` through `any`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4412 no-casts `packages/ui/react-ui-geo/src/components/Globe/Globe.stories.tsx:165:72`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4413 no-casts `packages/ui/react-ui-geo/src/components/Map/Map.stories.tsx:59:23`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4414 no-casts `packages/ui/react-ui-geo/src/util/render.ts:13:35`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4415 no-casts `packages/ui/react-ui-geo/src/util/render.ts:119:66`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4416 no-casts `packages/ui/react-ui-geo/src/util/render.ts:124:73`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4417 no-casts `packages/ui/react-ui-geo/src/util/render.ts:138:60`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4418 no-casts `packages/ui/react-ui-geo/src/util/render.ts:145:37`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4419 no-casts `packages/ui/react-ui-geo/src/util/render.ts:241:63`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4420 no-casts `packages/ui/react-ui-graph/src/components/Graph/Graph.stories.tsx:100:41`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4421 no-casts `packages/ui/react-ui-graph/src/components/Mesh/Mesh.tsx:53:27`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4422 no-casts `packages/ui/react-ui-graph/src/components/SVG/Zoom.tsx:23:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4423 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:131:20`

This line uses the non-null assertion `!` (`source.outgoing!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4424 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:132:20`

This line uses the non-null assertion `!` (`target.incoming!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4425 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:174:26`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4426 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:176:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4427 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:177:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4428 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:184:30`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4429 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:185:24`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4430 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:206:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4431 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:216:27`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4432 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:217:21`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4433 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:241:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4434 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:266:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4435 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:267:28`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4436 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:268:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4437 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:272:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4438 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:277:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4439 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:283:33`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4440 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:284:24`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4441 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:296:30`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4442 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/HierarchicalEdgeBundling.tsx:297:25`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4443 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:135:58`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4444 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:142:19`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4445 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:158:30`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4446 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:159:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4447 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:160:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4448 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:163:30`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4449 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:164:27`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4450 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:181:27`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4451 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:182:33`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4452 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:189:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4453 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:190:24`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4454 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:191:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4455 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:196:46`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4456 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:203:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4457 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:205:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4458 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:211:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4459 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:215:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4460 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:225:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4461 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:230:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4462 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:232:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4463 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:234:28`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4464 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/RadialTree.tsx:235:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4465 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:119:56`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4466 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:132:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4467 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:165:26`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4468 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:166:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4469 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:167:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4470 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:170:30`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4471 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:171:27`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4472 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:188:48`

This line widens this type position to `any` (`, any>`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4473 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:194:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4474 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:196:24`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4475 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:197:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4476 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:203:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4477 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:204:28`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4478 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:205:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4479 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:207:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4480 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:213:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4481 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:218:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4482 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:230:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4483 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:231:28`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4484 no-casts `packages/ui/react-ui-graph/src/components/Tree/layout/TidyTree.tsx:232:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4485 no-casts `packages/ui/react-ui-graph/src/fx/fx.stories.tsx:84:67`

This line uses the double-cast escape hatch `as unknown as SVGGElement`, forcing the value to `SVGGElement` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4486 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-bundle-projector.ts:62:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4487 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-bundle-projector.ts:63:47`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4488 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-bundle-projector.ts:150:21`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4489 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-bundle-projector.ts:169:29`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4490 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-bundle-projector.ts:195:52`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4491 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:55:28`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4492 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:56:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4493 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:57:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4494 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:83:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4495 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:84:48`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4496 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:228:21`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4497 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:246:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4498 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:256:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4499 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:257:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4500 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:297:35`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4501 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:298:35`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4502 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:300:13`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4503 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:320:31`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4504 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:328:27`

This line uses the non-null assertion `!` (`source: sourceNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4505 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-cluster-projector.ts:329:27`

This line uses the non-null assertion `!` (`target: targetNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4506 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-drag.ts:25:38`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4507 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:31:33`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4508 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:32:25`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4509 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:36:81`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4510 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:50:32`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4511 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:69:46`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4512 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:78:46`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4513 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:148:46`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4514 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:149:50`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4515 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-force-projector.ts:157:43`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4516 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-forces.ts:29:28`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4517 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-lattice-projector.ts:28:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4518 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-lattice-projector.ts:29:48`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4519 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-lattice-projector.ts:52:76`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4520 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:39:33`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4521 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:46:99`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4522 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.test.ts:57:5`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4523 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:61:28`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4524 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:62:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4525 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:63:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4526 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:95:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4527 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:96:47`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4528 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:165:44`

This line uses the non-null assertion `!` (`Set<string>([focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4529 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:167:48`

This line uses the non-null assertion `!` (`tionOf(edge, focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4530 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:191:45`

This line uses the non-null assertion `!` (`e[] = [{ id: focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4531 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:208:51`

This line uses the non-null assertion `!` (`rNode>({ id: focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4532 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:216:21`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4533 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:233:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4534 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:243:20`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4535 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:248:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4536 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-plexus-projector.ts:276:31`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4537 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-radial-projector.ts:21:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4538 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-radial-projector.ts:22:47`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4539 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-radial-projector.ts:50:46`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4540 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-radial-projector.ts:71:44`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4541 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:41:32`

This line widens this type position to `any` (`<any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4542 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:55:12`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4543 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:56:45`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4544 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:139:45`

This line uses the non-null assertion `!` (`Set<string>([focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4545 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:140:28`

This line uses the non-null assertion `!` (`frontier = [focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4546 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:168:61`

This line uses the non-null assertion `!` (`m>(makeDatum(focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4547 no-casts `packages/ui/react-ui-graph/src/graph/projector/graph-tree-projector.ts:169:60`

This line uses the non-null assertion `!` (`m>(makeDatum(focusId!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4548 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:20:33`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4549 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:32:35`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4550 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:36:40`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4551 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:48:43`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4552 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:94:37`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4553 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:202:34`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4554 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:209:34`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4555 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:356:23`

This line uses the non-null assertion `!` (`options.renderNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4556 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:398:26`

This line widens this type position to `any` (`<any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4557 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:405:26`

This line widens this type position to `any` (`<any,`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4558 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:439:42`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4559 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:480:19`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4560 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:487:47`

This line uses the double-cast escape hatch `as unknown as D3Selection`, forcing the value to `D3Selection` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4561 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:501:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4562 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:545:42`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4563 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:574:42`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4564 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:594:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4565 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:600:47`

This line uses the double-cast escape hatch `as unknown as D3Selection`, forcing the value to `D3Selection` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4566 no-casts `packages/ui/react-ui-graph/src/graph/renderer/graph-renderer.ts:730:34`

This line uses the non-null assertion `!` (`urrent = stack.pop()!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4567 no-casts `packages/ui/react-ui-graph/src/graph/renderer/labels.ts:27:20`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4568 no-casts `packages/ui/react-ui-graph/src/graph/renderer/labels.ts:28:20`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4569 no-casts `packages/ui/react-ui-graph/src/graph/renderer/linker.ts:14:27`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4570 no-casts `packages/ui/react-ui-graph/src/graph/renderer/linker.ts:15:27`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4571 no-casts `packages/ui/react-ui-graph/src/graph/renderer/linker.ts:28:30`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4572 no-casts `packages/ui/react-ui-graph/src/graph/types.ts:15:26`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4573 no-casts `packages/ui/react-ui-graph/src/graph/types.ts:17:51`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4574 no-casts `packages/ui/react-ui-graph/src/graph/types.ts:43:38`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4575 no-casts `packages/ui/react-ui-graph/src/graph/types.ts:68:38`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4576 no-casts `packages/ui/react-ui-graph/src/graph/types.ts:87:34`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4577 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:56:48`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4578 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:87:49`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4579 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:115:51`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4580 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:160:42`

This line uses the non-null assertion `!` (`ct(this._ref.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4581 no-casts `packages/ui/react-ui-graph/src/hooks/useGrid.ts:172:40`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4582 no-casts `packages/ui/react-ui-graph/src/hooks/useStateRef.ts:34:61`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4583 no-casts `packages/ui/react-ui-graph/src/hooks/useStateRef.ts:50:35`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4584 no-casts `packages/ui/react-ui-graph/src/hooks/useSvgContext.ts:41:29`

This line uses the non-null assertion `!` (`rn this._ref.current!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4585 no-casts `packages/ui/react-ui-graph/src/hooks/useSvgContext.ts:49:24`

This line uses the non-null assertion `!` (`return this._center!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4586 no-casts `packages/ui/react-ui-graph/src/hooks/useSvgContext.ts:82:34`

This line uses the non-null assertion `!` (`, cy] = this._center!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4587 no-casts `packages/ui/react-ui-graph/src/hooks/useSvgContext.ts:83:53`

This line uses the non-null assertion `!` (`oint(cx + x, cy + y)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4588 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:42:110`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4589 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:65:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4590 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:72:42`

This line uses the non-null assertion `!` (`_options?.onDblClick!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4591 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:91:34`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4592 no-casts `packages/ui/react-ui-graph/src/hooks/useZoom.ts:111:25`

This line uses the non-null assertion `!` (`select(ref.current!`) to assert a possibly-undefined value is defined; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4593 no-casts `packages/ui/react-ui-graph/src/stories/experimental.stories.tsx:121:32`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4594 no-casts `packages/ui/react-ui-graph/src/stories/experimental.stories.tsx:333:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4595 no-casts `packages/ui/react-ui-graph/src/stories/experimental.stories.tsx:337:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4596 no-casts `packages/ui/react-ui-graph/src/stories/experimental.stories.tsx:343:15`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4597 no-casts `packages/ui/react-ui-graph/src/testing/hooks.ts:17:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4598 no-casts `packages/ui/react-ui-graph/src/testing/hooks.ts:39:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4599 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:8:65`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4600 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:16:29`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4601 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:17:9`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4602 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:18:29`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4603 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:19:10`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4604 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:23:50`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4605 no-casts `packages/ui/react-ui-graph/src/util/d3-types.ts:25:10`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4606 no-casts `packages/ui/react-ui-introspect/src/components/ToolForm/ToolForm.tsx:57:52`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4607 no-casts `packages/ui/react-ui-introspect/src/components/ToolForm/ToolForm.tsx:99:34`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4608 no-casts `packages/ui/react-ui-introspect/src/components/ToolForm/ToolForm.tsx:100:46`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4609 no-casts `packages/ui/react-ui-introspect/src/components/types.ts:24:29`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4610 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionItemContext.ts:8:21`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4611 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionItemContext.ts:22:37`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4612 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionRoot.tsx:15:21`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4613 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionRoot.tsx:23:87`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4614 no-casts `packages/ui/react-ui-list/src/components/Accordion/AccordionRoot.tsx:29:67`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4615 no-casts `packages/ui/react-ui-list/src/components/Combobox/Combobox.stories.tsx:49:28`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4616 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedList.stories.tsx:25:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4617 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedList.stories.tsx:266:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4618 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListContext.ts:17:28`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4619 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListContext.ts:35:40`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4620 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListItem.tsx:269:38`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4621 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListRoot.tsx:31:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4622 no-casts `packages/ui/react-ui-list/src/components/OrderedList/OrderedListRoot.tsx:47:67`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4623 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:50:29`

This line uses the non-null assertion `!` (`index = parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4624 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:52:15`

This line uses the non-null assertion `!` (`parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4625 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:59:22`

This line uses the non-null assertion `!` (`item = item.items!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`d }) => id === part)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4626 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:80:33`

This line uses the non-null assertion `!` (`index = parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4627 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:81:19`

This line uses the non-null assertion `!` (`parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4628 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:88:33`

This line uses the non-null assertion `!` (`index = parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4629 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:89:19`

This line uses the non-null assertion `!` (`parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4630 no-casts `packages/ui/react-ui-list/src/components/Tree/testing.ts:96:19`

This line uses the non-null assertion `!` (`parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4631 no-casts `packages/ui/react-ui-list/src/components/Tree/Tree.stories.tsx:89:44`

This line uses the non-null assertion `!` (`Key.split('~').pop()!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4632 no-casts `packages/ui/react-ui-list/src/components/Tree/Tree.stories.tsx:101:37`

This line uses the non-null assertion `!` (`rentOf: parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4633 no-casts `packages/ui/react-ui-list/src/components/Tree/Tree.stories.tsx:102:34`

This line uses the non-null assertion `!` (`count: parent.items!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4634 no-casts `packages/ui/react-ui-list/src/components/Treegrid/Treegrid.stories.tsx:77:49`

This line uses the non-null assertion `!` (`ntOf } = stack.pop()!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4635 no-casts `packages/ui/react-ui-list/src/components/Treegrid/Treegrid.stories.tsx:91:34`

This line uses the non-null assertion `!` (`arentOf: child.nodes!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4636 no-casts `packages/ui/react-ui-list/src/hooks/useListSelection.test.ts:22:62`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4637 no-casts `packages/ui/react-ui-list/src/hooks/useListSelection.test.ts:29:64`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4638 no-casts `packages/ui/react-ui-list/src/hooks/useListSelection.test.ts:64:82`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4639 no-casts `packages/ui/react-ui-list/src/hooks/useListSelection.test.ts:84:62`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4640 no-casts `packages/ui/react-ui-list/src/hooks/useListSelection.test.ts:97:64`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4641 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/controller.test.ts:96:46`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4642 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/create-controller.ts:47:39`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4643 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/create-controller.ts:124:25`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4644 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/create-controller.ts:161:37`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4645 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/MarkdownStream.tsx:74:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4646 no-casts `packages/ui/react-ui-markdown/src/MarkdownStream/MarkdownStream.tsx:155:45`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4647 no-casts `packages/ui/react-ui-masonry/src/Masonry.tsx:40:29`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4648 no-casts `packages/ui/react-ui-masonry/src/Masonry.tsx:87:15`

This line uses the non-null assertion `!` (`Tile={Tile!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4649 no-casts `packages/ui/react-ui-masonry/src/Masonry.tsx:186:77`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4650 no-casts `packages/ui/react-ui-mcp/src/ToolForm.tsx:17:49`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4651 no-casts `packages/ui/react-ui-mcp/src/ToolForm.tsx:34:48`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4652 no-casts `packages/ui/react-ui-mcp/src/ToolForm.tsx:41:61`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4653 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:107:25`

This line uses the non-null assertion `!` (`expect(separatorNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4654 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:109:83`

This line uses the non-null assertion `!` (`et === separatorNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4655 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:110:74`

This line uses the non-null assertion `!` (`arget: separatorNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4656 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:227:21`

This line uses the non-null assertion `!` (`expect(moreGroup!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4657 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:259:20`

This line uses the non-null assertion `!` (`expect(rootNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4658 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:260:22`

This line uses the non-null assertion `!` (`expect(actionNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4659 no-casts `packages/ui/react-ui-menu/src/builder.test.ts:261:21`

This line uses the non-null assertion `!` (`expect(groupNode!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4660 no-casts `packages/ui/react-ui-menu/src/components/ActionLabel.tsx:24:75`

This line uses the non-null assertion `!` (`ng(action.properties!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4661 no-casts `packages/ui/react-ui-menu/src/hooks/useMenuActions.ts:15:44`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4662 no-casts `packages/ui/react-ui-menu/src/hooks/useMenuActions.ts:36:70`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4663 no-casts `packages/ui/react-ui-menu/src/testing/generator.ts:87:44`

This line widens this type position to `any` (`<any>`); and also widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4664 no-casts `packages/ui/react-ui-mosaic/src/components/Board/Column.tsx:31:31`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4665 no-casts `packages/ui/react-ui-mosaic/src/components/Board/Column.tsx:42:35`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4666 no-casts `packages/ui/react-ui-mosaic/src/components/Board/Column.tsx:191:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4667 no-casts `packages/ui/react-ui-mosaic/src/components/Board/Column.tsx:241:64`

This line uses the non-null assertion `!` (`j) ? refOrObj.target!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4668 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.stories.tsx:111:35`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4669 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:54:37`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4670 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:56:29`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4671 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:168:48`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4672 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:178:36`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4673 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:230:36`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4674 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:248:75`

This line uses the non-null assertion `!` (`: getId(visibleItems!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4675 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:249:40`

This line uses the non-null assertion `!` (`> getId(visibleItems!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4676 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:283:45`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4677 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:288:38`

This line uses the non-null assertion `!` (`{ data: visibleItems!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4678 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:291:36`

This line uses the non-null assertion `!` (`{ data: visibleItems!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4679 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:367:62`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4680 no-casts `packages/ui/react-ui-mosaic/src/components/Mosaic/Stack.tsx:371:21`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4681 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.test.tsx:23:83`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4682 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.test.tsx:34:6`

This line uses the double-cast escape hatch `as unknown as Virtualizer<any`, forcing the value to `Virtualizer<any` through `unknown`; and also widens this type position to `any` (`<any, any>`). Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4683 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.test.tsx:37:67`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4684 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.test.tsx:48:6`

This line uses the double-cast escape hatch `as unknown as Virtualizer<any`, forcing the value to `Virtualizer<any` through `unknown`; and also widens this type position to `any` (`<any, any>`). Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4685 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:17:49`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4686 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:37:48`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4687 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:43:47`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4688 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:59:49`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4689 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:74:38`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4690 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:98:51`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4691 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:165:27`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4692 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:227:54`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4693 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:261:27`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4694 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:292:27`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4695 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:323:27`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4696 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:379:48`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4697 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:396:44`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4698 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:412:30`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4699 no-casts `packages/ui/react-ui-mosaic/src/hooks/useVirtualizerPagination.ts:450:30`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4700 no-casts `packages/ui/react-ui-search/src/components/SearchList/hooks/useSearchListResults.ts:62:18`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4701 no-casts `packages/ui/react-ui-search/src/components/SearchList/SearchList.stories.tsx:500:30`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4702 no-casts `packages/ui/react-ui-syntax-highlighter/src/JsonHighlighter/JsonHighlighter.stories.tsx:15:28`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4703 no-casts `packages/ui/react-ui-syntax-highlighter/src/JsonHighlighter/JsonHighlighter.stories.tsx:40:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4704 no-casts `packages/ui/react-ui-syntax-highlighter/src/JsonHighlighter/JsonHighlighter.tsx:12:70`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4705 no-casts `packages/ui/react-ui-syntax-highlighter/src/JsonHighlighter/JsonHighlighter.tsx:15:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4706 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:17:28`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4707 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:41:54`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4708 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:42:31`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4709 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:77:5`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4710 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.stories.tsx:98:5`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4711 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.tsx:26:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4712 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.tsx:27:16`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4713 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.tsx:48:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4714 no-casts `packages/ui/react-ui-syntax-highlighter/src/Syntax/Syntax.tsx:93:63`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4715 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.stories.tsx:35:27`

This line widens this type position to `any` (`any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4716 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.stories.tsx:69:27`

This line widens this type position to `any` (`any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4717 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.stories.tsx:111:34`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4718 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.stories.tsx:177:29`

This line widens this type position to `any` (`any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4719 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.tsx:22:7`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4720 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.tsx:29:20`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4721 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.tsx:30:41`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4722 no-casts `packages/ui/react-ui-table/src/components/Table/DynamicTable.tsx:67:45`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4723 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:32:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4724 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:71:29`

This line uses the double-cast escape hatch `as unknown as TableRow[`, forcing the value to `TableRow[` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4725 no-casts `packages/ui/react-ui-table/src/components/Table/Relations.stories.tsx:92:36`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4726 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:110:9`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4727 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:182:74`

This line uses the non-null assertion `!` (`eld.path === 'name')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4728 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:231:62`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4729 no-casts `packages/ui/react-ui-table/src/components/Table/Table.stories.tsx:272:62`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4730 no-casts `packages/ui/react-ui-table/src/components/Table/TableContent.tsx:58:20`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4731 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:24:62`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4732 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:27:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4733 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:34:23`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4734 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:61:46`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4735 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:90:47`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4736 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:92:68`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4737 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:103:59`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4738 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:126:61`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4739 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/FormCellEditor.tsx:155:78`

This line uses the non-null assertion `!` (`.field.referencePath!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4740 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/TableCellEditor.tsx:36:7`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4741 no-casts `packages/ui/react-ui-table/src/components/TableCellEditor/TableCellEditor.tsx:130:17`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4742 no-casts `packages/ui/react-ui-table/src/hooks/useAddRow.ts:23:11`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4743 no-casts `packages/ui/react-ui-table/src/model/modal-controller.ts:21:81`

This line uses the double-cast escape hatch `as unknown as HTMLElement`, forcing the value to `HTMLElement` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4744 no-casts `packages/ui/react-ui-table/src/model/table-model.test.ts:26:12`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4745 no-casts `packages/ui/react-ui-table/src/model/table-model.test.ts:160:94`

This line uses the non-null assertion `!` (`ld.path === 'title')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4746 no-casts `packages/ui/react-ui-table/src/model/table-model.test.ts:163:80`

This line uses the non-null assertion `!` (`e-sort:${first.id}`)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4747 no-casts `packages/ui/react-ui-table/src/model/table-model.test.ts:192:51`

This line uses the non-null assertion `!` (`(key) ? map.get(key)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4748 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:51:64`

This line uses the double-cast escape hatch `as unknown as Table.Table`, forcing the value to `Table.Table` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4749 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:83:48`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4750 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:131:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4751 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:148:41`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4752 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:174:21`

This line uses the non-null assertion `!` (`private _selection!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4753 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:646:49`

This line uses the double-cast escape hatch `as any as T`, forcing the value to `T` through `any`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4754 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:732:46`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4755 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:741:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4756 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:780:78`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4757 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:817:52`

This line uses the double-cast escape hatch `as unknown as Obj.Unknown`, forcing the value to `Obj.Unknown` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4758 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:820:43`

This line uses the double-cast escape hatch `as unknown as Obj.Unknown`, forcing the value to `Obj.Unknown` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4759 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:836:52`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4760 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:877:19`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4761 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:1037:58`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4762 no-casts `packages/ui/react-ui-table/src/model/table-model.ts:1073:58`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4763 no-casts `packages/ui/react-ui-table/src/model/table-presentation.test.ts:20:13`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4764 no-casts `packages/ui/react-ui-table/src/model/table-presentation.test.ts:21:14`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4765 no-casts `packages/ui/react-ui-table/src/model/table-presentation.test.ts:97:17`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4766 no-casts `packages/ui/react-ui-table/src/model/table-presentation.ts:171:32`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4767 no-casts `packages/ui/react-ui-table/src/testing/useTestTableModel.ts:32:47`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4768 no-casts `packages/ui/react-ui-table/src/testing/useTestTableModel.ts:64:45`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4769 no-casts `packages/ui/react-ui-table/src/testing/useTestTableModel.ts:75:24`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4770 no-casts `packages/ui/react-ui-table/src/util/dynamic-table.ts:52:48`

This line uses the non-null assertion `!` (`etTypename(resolved)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4771 no-casts `packages/ui/react-ui-table/src/util/dynamic-table.ts:98:50`

This line uses the non-null assertion `!` (`ath] = property.size!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4772 no-casts `packages/ui/react-ui-table/src/util/dynamic-table.ts:114:61`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4773 no-casts `packages/ui/react-ui-table/src/util/schema.ts:18:52`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4774 no-casts `packages/ui/react-ui-table/src/util/schema.ts:22:23`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4775 no-casts `packages/ui/react-ui-table/src/util/sort.ts:11:35`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4776 no-casts `packages/ui/react-ui-table/src/util/sort.ts:61:9`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4777 no-casts `packages/ui/react-ui-table/src/util/sort.ts:62:9`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4778 no-casts `packages/ui/react-ui-thread/src/testing.ts:13:42`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4779 no-casts `packages/ui/react-ui-transcription/src/hooks/useRecordingPipeline.ts:50:25`

This line widens this type position to `any` (`<any, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4780 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:26:39`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4781 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:27:39`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4782 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:39:23`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4783 no-casts `packages/ui/react-ui-transcription/src/hooks/useSpeechRecognition.ts:43:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4784 no-casts `packages/ui/react-ui/src/components/Banner/Banner.stories.tsx:46:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4785 no-casts `packages/ui/react-ui/src/components/Breadcrumb/Breadcrumb.stories.tsx:44:30`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4786 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:138:24`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4787 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:151:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4788 no-casts `packages/ui/react-ui/src/components/Button/IconButton.stories.tsx:159:26`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4789 no-casts `packages/ui/react-ui/src/components/Button/IconButton.tsx:20:37`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4790 no-casts `packages/ui/react-ui/src/components/Dialog/AlertDialog.stories.tsx:51:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4791 no-casts `packages/ui/react-ui/src/components/Dialog/AlertDialog.stories.tsx:52:24`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4792 no-casts `packages/ui/react-ui/src/components/Dialog/Dialog.stories.tsx:103:21`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4793 no-casts `packages/ui/react-ui/src/components/ErrorFallback/ErrorFallback.stories.tsx:39:13`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4794 no-casts `packages/ui/react-ui/src/components/ErrorFallback/ErrorFallback.tsx:16:106`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4795 no-casts `packages/ui/react-ui/src/components/Input/Input.stories.tsx:88:25`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4796 no-casts `packages/ui/react-ui/src/components/Input/Input.stories.tsx:265:5`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4797 no-casts `packages/ui/react-ui/src/components/Input/SegmentedInput.tsx:146:16`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4798 no-casts `packages/ui/react-ui/src/components/Menu/ContextMenu.stories.tsx:95:31`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4799 no-casts `packages/ui/react-ui/src/components/Toast/Toast.stories.tsx:59:20`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4800 no-casts `packages/ui/react-ui/src/components/Toolbar/Toolbar.stories.tsx:72:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4801 no-casts `packages/ui/react-ui/src/components/Tooltip/Tooltip.stories.tsx:35:22`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4802 no-casts `packages/ui/react-ui/src/components/Tooltip/Tooltip.test.tsx:62:61`

This line uses the non-null assertion `!` (`('aria-describedby')!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4803 no-casts `packages/ui/react-ui/src/exemplars/virtualizer.stories.tsx:109:8`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4804 no-casts `packages/ui/react-ui/src/primitives/ThemeProvider/icon-registry.ts:63:48`

This line uses the double-cast escape hatch `as unknown as RegistryHost`, forcing the value to `RegistryHost` through `unknown`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4805 no-casts `packages/ui/react-ui/src/primitives/ThemeProvider/ThemeProvider.tsx:18:20`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4806 no-casts `packages/ui/react-ui/src/testing/Loading.tsx:12:35`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4807 no-casts `packages/ui/react-ui/src/util/slots.stories.tsx:21:13`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4808 no-casts `packages/ui/react-ui/src/util/slots.ts:84:85`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4809 no-casts `packages/ui/react-ui/src/util/slots.ts:87:32`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4810 no-casts `packages/ui/react-ui/src/util/slots.ts:107:40`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4811 no-casts `packages/ui/react-ui/src/util/slots.ts:108:14`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4812 no-casts `packages/ui/react-ui/src/util/slots.ts:135:39`

This line casts `as any`, discarding the compiler's type checking for this expression; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4813 no-casts `packages/ui/react-ui/src/util/slots.ts:136:14`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4814 no-casts `packages/ui/solid-ui-geo/src/components/Globe/Globe.solid-stories.tsx:150:7`

This line uses the double-cast escape hatch `as any as Topology`, forcing the value to `Topology` through `any`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4815 no-casts `packages/ui/solid-ui-geo/src/components/Globe/Globe.solid-stories.tsx:158:30`

This line uses the non-null assertion `!` (`reateTrip(airports()!`) to assert a possibly-undefined value is defined; and also casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source; handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4816 no-casts `packages/ui/solid-ui-geo/src/components/Map/Map.solid-stories.tsx:28:23`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4817 no-casts `packages/ui/solid-ui-geo/src/hooks/useSpinner.ts:30:36`

This line uses the non-null assertion `!` (`otation = controller!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4818 no-casts `packages/ui/solid-ui-geo/src/hooks/useSpinner.ts:42:19`

This line uses the non-null assertion `!` (`controller!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4819 no-casts `packages/ui/solid-ui-geo/src/util/render.ts:12:35`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4820 no-casts `packages/ui/solid-ui-geo/src/util/render.ts:75:60`

This line widens this type position to `any` (`: any`); and also widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4821 no-casts `packages/ui/solid-ui-geo/src/util/render.ts:82:37`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4822 no-casts `packages/ui/ui-editor/src/extensions/core/factories.test.ts:77:19`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4823 no-casts `packages/ui/ui-editor/src/extensions/debug/debug.ts:12:46`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4824 no-casts `packages/ui/ui-editor/src/extensions/debug/debug.ts:14:27`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4825 no-casts `packages/ui/ui-editor/src/extensions/debug/debug.ts:16:25`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4826 no-casts `packages/ui/ui-editor/src/extensions/decoration/replacer.test.ts:31:29`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4827 no-casts `packages/ui/ui-editor/src/extensions/decoration/replacer.test.ts:34:7`

This line casts `as any`, discarding the compiler's type checking for this expression. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4828 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:213:17`

This line uses the non-null assertion `!` (`this._node!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4829 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:248:36`

This line uses the non-null assertion `!` (`ect.update(this._ctx!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4830 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:324:33`

This line uses the non-null assertion `!` (`.vx *= particle.drag!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4831 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:325:33`

This line uses the non-null assertion `!` (`.vy *= particle.drag!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4832 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:326:19`

This line uses the non-null assertion `!` (`particle.theta!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4833 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:327:43`

This line uses the non-null assertion `!` (`h.sin(particle.theta!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4834 no-casts `packages/ui/ui-editor/src/extensions/demo/blast.ts:328:43`

This line uses the non-null assertion `!` (`h.cos(particle.theta!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4835 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/action.ts:52:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4836 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/decorate.ts:317:42`

This line uses the non-null assertion `!` (`node.node.firstChild!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4837 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/decorate.ts:321:43`

This line uses the non-null assertion `!` (`text.match(/[#\s]+/)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4838 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/decorate.ts:573:52`

This line uses the non-null assertion `!` (`te, node.node.parent!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4839 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:520:35`

This line uses the non-null assertion `!` (`if (sNode.firstChild!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4840 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:526:34`

This line uses the non-null assertion `!` (`if (sNode.lastChild!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4841 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:620:46`

This line uses the non-null assertion `!` (`m = before.lastChild!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4842 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:623:81`

This line uses the non-null assertion `!` (`^\s*/.exec(itemText)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4843 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:716:47`

This line uses the non-null assertion `!` (`>]*/.exec(line.text)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4844 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:844:47`

This line uses the non-null assertion `!` (`>]*/.exec(line.text)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4845 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/formatting.ts:903:47`

This line uses the non-null assertion `!` (`>]*/.exec(line.text)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4846 no-casts `packages/ui/ui-editor/src/extensions/language/markdown/image.test.ts:13:44`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4847 no-casts `packages/ui/ui-editor/src/extensions/language/xml/stub.ts:23:34`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4848 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:48:36`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4849 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:53:35`

This line widens this type position to `any` (`= any`); and also widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4850 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:57:12`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4851 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:125:41`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4852 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:137:54`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4853 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:153:73`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4854 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:162:39`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4855 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:167:8`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4856 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:214:50`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4857 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-tags.ts:327:38`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4858 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-util.ts:10:32`

This line widens this type position to `any` (`, any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4859 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-util.ts:41:18`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4860 no-casts `packages/ui/ui-editor/src/extensions/language/xml/xml-util.ts:65:21`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4861 no-casts `packages/ui/ui-editor/src/extensions/review/comments.ts:479:33`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4862 no-casts `packages/ui/ui-editor/src/extensions/review/comments.ts:515:32`

This line uses the non-null assertion `!` (`if (text.eq(tracked!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4863 no-casts `packages/ui/ui-editor/src/extensions/review/comments.ts:516:61`

This line uses the non-null assertion `!` (`ctions.indexOf(paste!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4864 no-casts `packages/ui/ui-editor/src/extensions/review/diff.test.ts:150:44`

This line uses the non-null assertion `!` (`rent.slice(0, splice!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`lice!.from) + splice!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`current.slice(splice!`) to assert a possibly-undefined value is defined; and also uses the non-null assertion `!` (`plice!.from + splice!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4865 no-casts `packages/ui/ui-editor/src/extensions/structure/blocks/selection.ts:46:58`

This line widens this type position to `any` (`<any>`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4866 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:39:92`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4867 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:69:14`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4868 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:70:14`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4869 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:71:14`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4870 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:78:43`

This line uses the non-null assertion `!` (`T>(itemOrCb, maybeCb!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4871 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:123:79`

This line uses the non-null assertion `!` (`item.children.at(-1)!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4872 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:136:28`

This line widens this type position to `any` (`= any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4873 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:179:22`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4874 no-casts `packages/ui/ui-editor/src/extensions/structure/outliner/tree.ts:180:44`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4875 no-casts `packages/ui/ui-editor/src/util/debug.ts:15:43`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4876 no-casts `packages/ui/ui-editor/src/util/debug.ts:16:18`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4877 no-casts `packages/ui/ui-editor/src/util/debug.ts:44:20`

This line widens this type position to `any` (`: any[]`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4878 no-casts `packages/ui/ui-editor/src/util/dom.ts:30:51`

This line uses the non-null assertion `!` (`xt, 0, dom.nodeValue!`) to assert a possibly-undefined value is defined. Per the no-casts rule, handle the possibly-undefined case explicitly (a guard, default, or a type reflecting the real invariant) instead of asserting it away.

# ERROR cc9b81fc-4879 no-casts `packages/ui/ui-editor/src/util/dom.ts:34:15`

This line uses the double-cast escape hatch `as any as DOMRectList`, forcing the value to `DOMRectList` through `any`. Per the no-casts rule, replace the cast with a precisely typed value, a type guard, or a narrower conversion at its source.

# ERROR cc9b81fc-4880 no-casts `packages/ui/ui-types/src/anchor.ts:37:10`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.

# ERROR cc9b81fc-4881 no-casts `packages/ui/ui-types/src/translations.ts:10:26`

This line widens this type position to `any` (`: any`). Per the no-casts rule, give the parameter/generic/return position a concrete type instead of `any`.
