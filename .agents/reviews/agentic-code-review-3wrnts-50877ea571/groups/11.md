<!-- Group 11: no-compat-shims. Append diagnostics below in the header format "# WARN|ERROR `file:line[:col]`". Leave empty if clean. -->

# WARN `packages/core/echo/echo-client/src/echo-handler/echo-handler.ts:745`

`getObjectCore` and `isRootDataObject` (line 757) are re-exported here with the comment "Re-export from echo-object-utils for backward compatibility" even though both are actually implemented in `./echo-object-utils.ts`. Several call sites (`text.ts`, `clone.ts`, `subscription.ts`, `edit-history.ts`, `branching.ts`, `echo-handler.test.ts`, `edit-history.test.ts`) still import `getObjectCore` from `./echo-handler` instead of the real module. Per `no-compat-shims`, update those call sites to import directly from `./echo-object-utils` and delete the two re-export lines.

# WARN `packages/core/echo/echo/src/internal/Obj/json-serializer.ts:47`

`attachTypedJsonSerializer` is re-exported here "for backward compatibility," but the real implementation lives in `../common/proxy/json-serializer.ts` (imported at the top of this file from `'../common/proxy'`). `create-object.ts` still imports it from `./json-serializer` instead of `../common/proxy`. Per `no-compat-shims`, point `create-object.ts` at the real location and drop the re-export.

# WARN `packages/core/echo/echo/src/internal/Obj/schema-validator.ts:5`

This entire file is a one-line compat shim: `export { SchemaValidator, checkIdNotPresentOnSchema } from '../common/proxy';`, explicitly labeled "Re-export from proxy for backward compatibility." `Obj/index.ts` still does `export * from './schema-validator'` and `schema-validator.test.ts` imports from this local shim rather than `../common/proxy` directly. Per `no-compat-shims`, update both call sites to the real `common/proxy` location and delete this file.

# WARN `packages/plugins/plugin-assistant/src/types/Assistant.ts:7`

`Chat` is re-exported from `@dxos/assistant-toolkit` with the comment "Re-export Chat schema for backward compatibility," and many files in this package (`ChatThread.tsx`, `registry.tsx`, `ChatOptions.tsx`, `ChatArticle.tsx`, `ChatDialog.tsx`, `AssistantSettings.tsx`, `usePresets.ts`, `useChatProcessor.ts`, `capabilities/settings.ts`, `capabilities/AssistantSurfaces.tsx`) still reach `Chat` through this `Assistant` namespace shim, while other files in the same package (`ChatOptions.stories.tsx`, `ChatPrompt.tsx`, `AssistantOperation.ts`, `processor.ts`) already import `Chat` directly from `@dxos/assistant-toolkit`. Per `no-compat-shims`, finish the migration by importing `Chat` from `@dxos/assistant-toolkit` at all remaining call sites and drop the re-export.
