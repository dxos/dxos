# Project registry — every active work-stream, one entry each. Managed via the `task-planning` skill
# ($project new/list/end; $resume/$hydrate key off it). Each project has a TASKS.md (ledger) and a
# DESIGN.md (spec + decisions) — the `tasks`/`design` fields record where they live (a package file,
# or .agents/projects/<name>/{TASKS,DESIGN}.md). Keep this committed and current.
#
# status: active | paused | blocked | ended

projects:
  - name: mailbox-research
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-05
    summary: Open-weight vs premier model routing for the mailbox pipelines (labeling / summaries / drafts).
    tasks: packages/stories/stories-brain/TASKS.md
    design: packages/stories/stories-brain/fixtures/REPORT.md
    prs: [12163, 12167, 12178]
    resume: 'PRs #12178 + #12205 merged (Topics UX v2 + Topics/Subscriptions Mosaic lists). In progress (uncommitted, NOT yet a PR): converted plugin-inbox folder articles to the ObjectArticleProps<Mailbox> pattern (db from subject, folder graph nodes carry data:mailbox, sentinels dropped) + SubscriptionsArticle unsubscribe removes on success. Build/lint/fmt + 167 unit tests green. NOTE: TopicsArticle/CreateTopic storybook play-tests fail on the committed baseline too (pre-existing, headless topic-query returns empty). Next: commit this work / open PR when ready; then Priority 1 manual topics + management. Deferred: Active Topics v2 (LLM labels + personEmails), two-tier latency, re-sync dedup bug, MailboxArticle search wiring.'

  - name: task-planning-skill
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-12
    summary: Rename the $session sentinel/concept to $project and add a numbered pick-to-resume table.
    tasks: .agents/skills/task-planning/TASKS.md
    design: .agents/skills/task-planning/SKILL.md
    prs: [12174]
    resume: 'PR #12174 opened. Next: watch the Check workflow to green and address any review comments.'

  - name: inbox-test-suite
    status: active
    user: jdw
    host: jdw-mbp
    created: 2026-07-13
    summary: Layered test suite enforcing plugin-inbox PLUGIN.mdl (unit→storybook→harness→agent-e2e→browser-e2e→bench→live); current branch delivers Tier 4 Playwright browser-e2e (task #7).
    tasks: packages/plugins/plugin-inbox/TASKS.md
    design: packages/plugins/plugin-inbox/PLUGIN.mdl
    prs: [12197]
    resume: 'PR #12197: task #7 (JMAP e2e) done, 3/3 green — survived two upstream refactors (connector-auth schema-annotation rewrite; vite+rolldown build + conversation-mosaic-toolbar redesign) and a code-review fix round (PLUGIN.mdl draftEmailAndOpen/jmapSync accuracy, http-mock Jmap.MethodCall/Filter reuse + validated parsing). Latest merge (origin/main, 29 commits) needed zero fixes. Committed but not pushed: http-mock export-path fix, reply e2e simplified to Reply All, latest merge. Next: push, then Gmail suite (#7e, DX_E2E-gated connection bridge + provider adapter) or #6/#8-#11 per user priority.'

  - name: composer-search
    status: active
    user: burdon
    host: burdon-studio-2022
    created: 2026-07-14
    summary: Unify Composer search (lexical/semantic/structured/agent) + tiered client↔EDGE vector index synced via feeds.
    tasks: packages/plugins/plugin-search/TASKS.md
    design: packages/plugins/plugin-search/docs/DESIGN.md
    prs: []
    resume: 'Audit + design + roadmap + M1 implementation plan authored in packages/plugins/plugin-search/docs. Next: execute Milestone 1 (real lexical search) per docs/M1-lexical-search-plan.md.'

  - name: document-revisions
    status: active
    user: burdon
    host: burdon-mbp-2092
    created: 2026-07-15
    summary: Document revisions & branches (checkpoints, draft branches, merge) for markdown + @dxos/versioning; convergence with PR 11829 core branching.
    tasks: .agents/projects/document-revisions/TASKS.md
    design: packages/plugins/plugin-comments/DESIGN.md
    prs: [12237, 12246, 12290, 12301, 12302, 12315]
    resume: 'PR #12315 OPEN (left for eval, NO auto-merge — user clicks). Combined follow-up: B6 perf (rebaseHunksWith hoist + trackChanges memo), Suggesting folded into the editor view-mode dropdown (react-ui-editor addViewMode items + plugin-markdown ViewModeExtension capability + plugin-comments contributes suggesting; toolbar branch-selector/review-mode groups removed — History companion covers it; react-ui-menu now shows a check on the current select value), B4 un-delete phantom (hover restore control), comment-flash-on-submit fix (persist before dropping draft + dedupe). Merged origin/main incl. #12304 view-state refactor (VersioningState atom → viewAspect). All touched packages build+lint+test+oxfmt green. NEXT: watch #12315 CI green; user evals UI pieces then merges. Deferred (own PRs): addViewMode unit test; author-side accept/reject own draft; AgentIdentity runtime-provision + naming-collision; comments/suggestions tabs; full-stack CommentsArticle test. Milestones A+B MERGED (#12301, #12302). Now on a follow-up combined PR (one branch, left green+OPEN for eval). Merged origin/main (2026-07-23) incl. #12304 view-state refactor: versioning state moved from the VersioningState atom to a per-object ViewState `viewAspect` (@dxos/react-ui-attention) — resolved by taking main for plugin-versioning/useVersioning/ObjectHistory/CommentsArticle (already contains my #12302 semantics), keeping ours for the B6 ui-editor review files. DONE: B6 perf (rebaseHunksWith hoist + trackChanges memo, 74eda25341). NEXT (this PR): view-mode refactor — Suggesting becomes a contributed editor view-mode (design settled 2026-07-23: react-ui-editor addViewMode(state,onChange,items?); plugin-markdown MarkdownCapabilities.EditorViewMode slot; plugin-comments contributes suggesting; remove toolbar branch selector — History companion covers it; react-ui-menu select must show a check on the current value); then B4 un-delete phantom (draft); comment-flash-on-Enter (draft). Deferred (own PRs): AgentIdentity runtime-provision + naming-collision; comments/suggestions tabs; full-stack CommentsArticle test.'

  - name: ai-testing-strategy
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-20
    summary: Replace the memoized-LLM e2e strategy with tiered tests (deterministic unit tiers in CI + graded evals out-of-band); retire the current e2e framework.
    tasks: .agents/projects/ai-testing-strategy/TASKS.md
    design: packages/core/compute/ai/TESTING.md
    prs: [12287, 12291, 12297, 12305]
    resume: "Design doc PR #12287 MERGED. PR #12291 MERGED — Phase 1 steps 1-3: de-gated G2/G3 memoized replay, scripted LanguageModel primitive, AiRequest loop (D) tests. PR #12297 MERGED — revised plan: kept G1 assistant-e2e instead of deleting it, extended the runMemoizedTests() gate to it + the 5 live-model tests in memoization.test.ts (dynamic value matching stays ungated), removed all 21 committed .conversations.json fixtures repo-wide, and — per reviewer request mid-review — replaced the ad hoc describeMemoized/itMemoized/testMemoized helper across all 20 gated files with native describe.skipIf/it.effect.skipIf/test.skipIf. CodeRabbit clean, CI green, merge-queued and merged. PR #12305 MERGED — added a leaner package-level companion to the D-tier harness tests: assistant/src/request/AiRequest.test.ts drives AiRequest.Request.run directly (no AssistantTestLayer/ProcessManager/AgentServiceRuntime/AiService model-resolution), using ScriptedLanguageModel.scriptedLanguageModelLayer as LanguageModel.LanguageModel directly + minimal noop/in-memory services (TestDatabaseLayer, registryLayerNoop, new operationServiceLayerNoop in @dxos/compute/testing/operation.ts, ToolExecutionService.layerEmpty, ToolResolverService.layerEmpty) since RunRequirements over-declares services run() never actually yields on this path. Explicitly NOT a replacement for scripted-loop.test.ts (agent-runtime) — that one additionally covers AiService model-resolution + the full ProcessManager/AgentServiceRuntime/ServiceResolver composition boot; keep both. No changeset (test-only, no consumer-visible change). CodeRabbit review (after marking ready): 1 actionable fix applied (operationServiceLayerNoop.invokePromise now resolves {error} per its declared type instead of rejecting; the as-unknown-as cast turned out removable, directly-typed stub compiles clean) + moved that layer into its own testing/operation.ts (was inline in testing/index.ts) per direct feedback; 1 doc-wording nitpick adopted (SKILL.md audit line softened); 1 nitpick skipped with reason (top-level `expect` import in AiRequest.test.ts — matches existing it.effect convention in scripted-loop.test.ts/memoization.test.ts, the context-expect suggestion applies to plain test(), not it.effect()); a stale draft-status line in this project's own TASKS.md (flagged by CodeRabbit) also fixed. Re-verified build/lint/test green after each change. NEXT: G2→C mocked unit conversions (plugin-markdown create/update largest; plugin-magazine, plugin-assistant, assistant-toolkit skills, AiSummarizer), then E (context-assembly snapshot) + F (schema round-trip) tiers, then G (code-side oracle). Also tracked, own PRs: delete orphaned .agent/ (singular) dir; decide whether AiRequest.run needs a max-iterations cap (none exists today); consider adding tool-error/malformed-output branch tests to one of the two D-tier files (not both)."

ended: []
