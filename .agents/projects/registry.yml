# Project registry — every active work-stream, one entry each. Managed via the `task-planning` skill
# ($project new/list/end; $resume/$hydrate key off it). Each project has a TASKS.md (ledger) and a
# DESIGN.md (spec + decisions) — the `tasks`/`design` fields record where they live (a package file,
# or .agents/projects/<name>/{TASKS,DESIGN}.md). Keep this committed and current.
#
# status: active | paused | blocked | ended

projects:
  - name: plugin-terra
    status: active
    user: burdon
    host: burdon-studio-2022
    created: 2026-07-26
    summary: Deterministic stylized 3D planet (Babylon.js) from a seed-driven Terra ECHO object; Phase 2 adds movable objects (boats/planes/satellites/tanks/rockets) with a deterministic sim engine.
    tasks: packages/plugins/plugin-terra/TASKS.md
    design: packages/plugins/plugin-terra/DESIGN.md
    prs: []
    resume: 'Phase 1 is 10/13 done (engine, Terra type, SceneManager, TerraArticle, react-ui Slider + TerraForm panel all landed and reviewed; planet renders and regenerates live in storybook). Next: Task 11 capabilities + plugin wiring, then Task 12 verification, Task 13 changeset, final whole-branch review. In flight: slider-thumb visibility fix (is-4/bs-4 generate no CSS). Phase 2 plan is written/committed; execution follows Phase 1.'

  - name: plugin-projects
    status: active
    user: burdon
    host: burdon-mbp-2092
    created: 2026-07-24
    summary: New core plugin for interactive long-running processes (Routines, Agents, artifacts); Project type succeeds Topic; obviates plugin-sidekick.
    tasks: .agents/projects/plugin-projects/TASKS.md
    design: agents/superpowers/specs/2026-07-24-plugin-projects-design.md
    prs: [12335]
    resume: 'PR #12335 GREEN + preview live, awaiting review. MS2 underway: instructions->system-prompt + stub labels + cold-load fix done (3 commits LOCAL/unpushed pending PR-strategy decision); create/edit verified live. Next: commands-authoring UI in InstructionsEditor, outliner skill design, delete-flow verify.'

  - name: sketch-scene-dsl
    status: active
    user: burdon
    host: burdon-mbp-2092
    created: 2026-07-23
    summary: Backend-neutral scene DSL so the agent can draw/edit sketches (world objects by id); tldraw backend, read/edit operations, sketch skill, live-AI storybook.
    tasks: packages/plugins/plugin-sketch/TASKS.md
    design: agents/superpowers/specs/2026-07-23-sketch-scene-dsl-design.md
    prs: [12324]
    resume: 'PR #12324 Check GREEN (preview pr-12324-composer-main.dxos.workers.dev). Next: respond to review comments (only bot stubs at checkpoint) and land via the land skill; then Phase 2 backlog (excalidraw backend, mermaid/d2/sequence dialects, rename-object).'

  - name: mailbox-research
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-05
    summary: Open-weight vs premier model routing for the mailbox pipelines (labeling / summaries / drafts).
    tasks: packages/stories/stories-brain/TASKS.md
    design: packages/stories/stories-brain/fixtures/REPORT.md
    prs: [12163, 12167, 12178]
    resume: 'PRs #12178 + #12205 merged (Topics UX v2 + Topics/Subscriptions Mosaic lists). In progress (uncommitted, NOT yet a PR): converted plugin-inbox folder articles to the ObjectArticleProps<Mailbox> pattern (db from subject, folder graph nodes carry data:mailbox, sentinels dropped) + SubscriptionsArticle unsubscribe removes on success. Build/lint/fmt + 167 unit tests green. NOTE: TopicsArticle/CreateTopic storybook play-tests fail on the committed baseline too (pre-existing, headless topic-query returns empty). Next: commit this work / open PR when ready; then Priority 1 manual topics + management. Deferred: Active Topics v2 (LLM labels + personEmails), two-tier latency, re-sync dedup bug, MailboxArticle search wiring.'

  - name: task-planning-skill
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-12
    summary: Rename the $session sentinel/concept to $project and add a numbered pick-to-resume table.
    tasks: .agents/skills/task-planning/TASKS.md
    design: .agents/skills/task-planning/SKILL.md
    prs: [12174]
    resume: 'PR #12174 opened. Next: watch the Check workflow to green and address any review comments.'

  - name: inbox-test-suite
    status: active
    user: jdw
    host: jdw-mbp
    created: 2026-07-13
    summary: Layered test suite enforcing plugin-inbox PLUGIN.mdl (unit→storybook→harness→agent-e2e→browser-e2e→bench→live); current branch delivers Tier 4 Playwright browser-e2e (task #7).
    tasks: packages/plugins/plugin-inbox/TASKS.md
    design: packages/plugins/plugin-inbox/PLUGIN.mdl
    prs: [12197]
    resume: 'PR #12197: task #7 (JMAP e2e) done, 3/3 green — survived two upstream refactors (connector-auth schema-annotation rewrite; vite+rolldown build + conversation-mosaic-toolbar redesign) and a code-review fix round (PLUGIN.mdl draftEmailAndOpen/jmapSync accuracy, http-mock Jmap.MethodCall/Filter reuse + validated parsing). Latest merge (origin/main, 29 commits) needed zero fixes. Committed but not pushed: http-mock export-path fix, reply e2e simplified to Reply All, latest merge. Next: push, then Gmail suite (#7e, DX_E2E-gated connection bridge + provider adapter) or #6/#8-#11 per user priority.'

  - name: composer-search
    status: active
    user: burdon
    host: burdon-studio-2022
    created: 2026-07-14
    summary: Unify Composer search (lexical/semantic/structured/agent) + tiered client↔EDGE vector index synced via feeds.
    tasks: packages/plugins/plugin-search/TASKS.md
    design: packages/plugins/plugin-search/docs/DESIGN.md
    prs: []
    resume: 'Audit + design + roadmap + M1 implementation plan authored in packages/plugins/plugin-search/docs. Next: execute Milestone 1 (real lexical search) per docs/M1-lexical-search-plan.md.'

  - name: document-revisions
    status: active
    user: burdon
    host: burdon-mbp-2092
    created: 2026-07-15
    summary: Document revisions & branches (checkpoints, draft branches, merge) for markdown + @dxos/versioning; convergence with PR 11829 core branching.
    tasks: .agents/projects/document-revisions/TASKS.md
    design: packages/plugins/plugin-review/DESIGN.md
    prs: [12237, 12246, 12290, 12301, 12302, 12315, 12328, 12333, 12339]
    resume: 'PR #12339 OPEN (auto-merge OFF — user lands after testing): same-view branch swap, I-2 syncBranch reconciliation, pure lifecycle model, shared scenario harness (8 scenarios, headless + plays), per-author visibility, defect arc (diff minimize / microtask reconcile / anchor-stable rebase / focus restore / widget bars / atomic markup pairs). NEXT: user F1-F6 walkthrough per plugin-review/TEST-PLAN.md on the worktree storybook (:9010), then land. Risks queued in DESIGN.md section 5.'

  - name: ai-testing-strategy
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-20
    summary: Replace the memoized-LLM e2e strategy with tiered tests (deterministic unit tiers in CI + graded evals out-of-band); retire the current e2e framework.
    tasks: .agents/projects/ai-testing-strategy/TASKS.md
    design: packages/core/compute/ai/TESTING.md
    prs: [12287, 12291, 12297, 12305, 12307]
    resume: "Design doc PR #12287 MERGED. PR #12291 MERGED — Phase 1 steps 1-3. PR #12297 MERGED — kept G1 assistant-e2e gated instead of deleting it (superseded below). PR #12305 MERGED — leaner package-level D-tier companion. PR #12307 is READY FOR REVIEW (not draft), CI green, latest commit at time of writing 3cc239de67 — the G1 migration is COMPLETE: all 6 former-G1 scenarios (database, smoke, markdown, crm-mailbox, web-search, planning) have scored evalite evals in @dxos/assistant-evals, every one passing live at 100% at least once, graded by deterministic DB-effect/tool-invocation assertions plus two native LLM-judge integrations (src/judge.ts, using @dxos/ai's LanguageModel.generateObject rather than autoevals' OpenAI-coupled classifiers): planning's haiku-quality check and crm-mailbox's data-accuracy check, each with a same-file negative case. @dxos/assistant-e2e is kept as its own deprecated package (not merged into assistant-evals) holding harness.ts + 3 not-yet-portable scenarios (inbox-enable, local-ai, sandbox), each marked TODO(wittjosiah): migrate to an eval. Per-eval timeouts (createEvalRunner's timeout option, 60s default/150s for the two multi-tool scenarios) replaced a flat 360s vitest testTimeout (now 180s, just the outer safety net — confirmed via evalite's source that it has no per-eval timeout of its own). A CodeRabbit review round after marking ready found and fixed: two TESTING.md accuracy overstatements (model-pinning/scheduling, scripted-test branch coverage — both actually deferred), a assistant-e2e/README.md rule contradicting sandbox.test.ts's randomness requirement, EvalTimeoutError converted to Data.TaggedError, expect:'failure' no longer conflates infra/setup failures with the agent's own instructed failure (new AgentRunFailure tag), the 300ms fixed sleep in completedBlocks() replaced with a real FeedTraceSink.flush() call, plus two new scorers (crm-mailbox's summary-document-linked via the ProfileOf relation, database's database-queried via toolInvocations). NEXT: verify build/lint/test green, commit, re-verify with CodeRabbit. Then: G2→C mocked unit conversions (scheduled-run/model-pinning work explicitly deferred per direct instruction), then E/F tiers. Also tracked, own PRs: delete orphaned .agent/ (singular) dir; decide whether AiRequest.run needs a max-iterations cap (none exists today); consider tool-error/malformed-output branch tests."

ended: []
