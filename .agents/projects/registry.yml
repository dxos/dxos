# Project registry — every active work-stream, one entry each. Managed via the `task-planning` skill
# ($project new/list/end; $resume/$hydrate key off it). Each project has a TASKS.md (ledger) and a
# DESIGN.md (spec + decisions) — the `tasks`/`design` fields record where they live (a package file,
# or .agents/projects/<name>/{TASKS,DESIGN}.md). Keep this committed and current.
#
# status: active | paused | blocked | ended

projects:
  - name: mailbox-research
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-05
    summary: Open-weight vs premier model routing for the mailbox pipelines (labeling / summaries / drafts).
    tasks: packages/stories/stories-brain/TASKS.md
    design: packages/stories/stories-brain/fixtures/REPORT.md
    prs: [12163, 12167, 12178]
    resume: 'PRs #12178 + #12205 merged (Topics UX v2 + Topics/Subscriptions Mosaic lists). In progress (uncommitted, NOT yet a PR): converted plugin-inbox folder articles to the ObjectArticleProps<Mailbox> pattern (db from subject, folder graph nodes carry data:mailbox, sentinels dropped) + SubscriptionsArticle unsubscribe removes on success. Build/lint/fmt + 167 unit tests green. NOTE: TopicsArticle/CreateTopic storybook play-tests fail on the committed baseline too (pre-existing, headless topic-query returns empty). Next: commit this work / open PR when ready; then Priority 1 manual topics + management. Deferred: Active Topics v2 (LLM labels + personEmails), two-tier latency, re-sync dedup bug, MailboxArticle search wiring.'

  - name: task-planning-skill
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-12
    summary: Rename the $session sentinel/concept to $project and add a numbered pick-to-resume table.
    tasks: .agents/skills/task-planning/TASKS.md
    design: .agents/skills/task-planning/SKILL.md
    prs: [12174]
    resume: 'PR #12174 opened. Next: watch the Check workflow to green and address any review comments.'

  - name: inbox-test-suite
    status: active
    user: jdw
    host: jdw-mbp
    created: 2026-07-13
    summary: Layered test suite enforcing plugin-inbox PLUGIN.mdl (unit→storybook→harness→agent-e2e→browser-e2e→bench→live); current branch delivers Tier 4 Playwright browser-e2e (task #7).
    tasks: packages/plugins/plugin-inbox/TASKS.md
    design: packages/plugins/plugin-inbox/PLUGIN.mdl
    prs: [12197]
    resume: 'PR #12197: task #7 (JMAP e2e) done, 3/3 green — survived two upstream refactors (connector-auth schema-annotation rewrite; vite+rolldown build + conversation-mosaic-toolbar redesign) and a code-review fix round (PLUGIN.mdl draftEmailAndOpen/jmapSync accuracy, http-mock Jmap.MethodCall/Filter reuse + validated parsing). Latest merge (origin/main, 29 commits) needed zero fixes. Committed but not pushed: http-mock export-path fix, reply e2e simplified to Reply All, latest merge. Next: push, then Gmail suite (#7e, DX_E2E-gated connection bridge + provider adapter) or #6/#8-#11 per user priority.'

  - name: composer-search
    status: active
    user: burdon
    host: burdon-studio-2022
    created: 2026-07-14
    summary: Unify Composer search (lexical/semantic/structured/agent) + tiered client↔EDGE vector index synced via feeds.
    tasks: packages/plugins/plugin-search/TASKS.md
    design: packages/plugins/plugin-search/docs/DESIGN.md
    prs: []
    resume: 'Audit + design + roadmap + M1 implementation plan authored in packages/plugins/plugin-search/docs. Next: execute Milestone 1 (real lexical search) per docs/M1-lexical-search-plan.md.'

  - name: document-revisions
    status: active
    user: burdon
    host: burdon-mbp-2092
    created: 2026-07-15
    summary: Document revisions & branches (checkpoints, draft branches, merge) for markdown + @dxos/versioning; convergence with PR 11829 core branching.
    tasks: .agents/projects/document-revisions/TASKS.md
    design: packages/plugins/plugin-comments/DESIGN.md
    prs: [12237, 12246, 12290, 12301, 12302]
    resume: 'Ambient (Google-Docs) review Milestone A SHIPPED — PR #12301 MERGED 2026-07-22 02:53Z (preview https://pr-12301-composer-main.dxos.workers.dev). Milestone A = per-doc review mode (Editing/Viewing), ReviewRenderPolicy, aggregated suggestion overlay on the default (selection===current) view, comment-on-branch fix, Suggesting option HIDDEN until B. Spec = packages/plugins/plugin-comments/DESIGN.md § Ambient review model; plan = agents/superpowers/plans/2026-07-21-ambient-review-model.md. Milestone B (Suggesting-mode authoring) BUILT + PR #12302 OPEN (watching Check). B2 base-decoupling (rebaseHunks + suggestions base + tooltip popover) + B3 suggesting-mode wiring (editor binds to the user own kind:suggestion branch; edits never hit main; char-level trackChanges). A1 ratified by felt-eval. Deferred (own PRs): B4 author-side accept/reject/un-delete; B6 perf (incremental diff, hoist computeCharHunks) + block deletions + copy; full-stack CommentsArticle test; showComments consumption. NEXT: review + land #12302; then the deferred follow-ups.'

  - name: ai-testing-strategy
    status: active
    user: burdon
    host: burdon-mbp-2022
    created: 2026-07-20
    summary: Replace the memoized-LLM e2e strategy with tiered tests (deterministic unit tiers in CI + graded evals out-of-band); retire the current e2e framework.
    tasks: .agents/projects/ai-testing-strategy/TASKS.md
    design: packages/core/compute/ai/TESTING.md
    prs: [12287, 12291, 12297, 12305, 12307]
    resume: "Design doc PR #12287 MERGED. PR #12291 MERGED — Phase 1 steps 1-3: de-gated G2/G3 memoized replay, scripted LanguageModel primitive, AiRequest loop (D) tests. PR #12297 MERGED — revised plan: kept G1 assistant-e2e instead of deleting it, extended the runMemoizedTests() gate to it. PR #12305 MERGED — leaner package-level D-tier companion, assistant/src/request/AiRequest.test.ts, driving AiRequest.Request.run directly with minimal noop/in-memory services; kept scripted-loop.test.ts too (different coverage). PR #12307 draft, pushed, description updated — Phase 2 start: assistant-evals/src/assertions.ts adds objectExists(type, predicate), a dimension-G deterministic assertion helper querying the DB directly instead of trusting the agent's completedCriteria self-report; runner.ts's createEvalRunner gained an optional dbQuery hook; assistant-evals/src/evals/database.eval.ts is the first scored evalite eval, ported from the gated Database > create and query scenario. Also merged @dxos/assistant-e2e into @dxos/assistant-evals (composition-scoped layout — cross-plugin scenarios live centrally, single-plugin scenarios stay in their own plugin package importing assistant-evals as a helpers library), documented in TESTING.md's new 'Where evals live' section; required splitting vitest.config.ts (flat, for evalite's hardcoded .eval.ts discovery) from vitest.e2e.config.ts (workspace-shaped, explicit --config target for the gated e2e :test task). Verified: assistant-evals:build/lint/test green (9 gated e2e files correctly skip without DX_RUN_LLM_TESTS=1); evalite correctly discovers .eval.ts files. NOT yet obtained a live scored result in this sandbox: every evalite run (both database.eval.ts and the pre-existing, unrelated basic.eval.ts) fails deterministically before any model call on a plugin-routine registry-sync race (registry-sync.ts:74, handler.meta read before population) that is evalite-specific — the same harness under vitest's gated :test path does not hit it. Confirmed pre-existing and unrelated to the dbQuery code (never reached). Tracked as a blocking follow-up in TASKS.md — needs reproducing outside this sandbox. NEXT: get a real machine/CI to attempt database.eval.ts and see if the race reproduces there; if not, deliver the live scored result; if so, root-cause the evalite-vs-vitest scheduling difference in OperationHandlerSet registration. Then port web-search.test.ts as the first tool-match scorer, then G2→C mocked unit conversions, then E/F tiers. Also tracked, own PRs: delete orphaned .agent/ (singular) dir; decide whether AiRequest.run needs a max-iterations cap (none exists today); consider tool-error/malformed-output branch tests."

ended: []
