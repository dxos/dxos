CLASS=err
Our vitest suite `packages/core/echo/src/sync.test.ts` passes locally but fails in CI roughly one run in four with:

```
Unhandled Error: Error: connection closed
  at WebSocketProxy.onClose (src/net/ws-proxy.ts:88:15)
```

The error surfaces after the last assertion, during teardown, when the proxy tears down its socket. The assertions themselves always pass.

CI has been red on and off for two days and the team wants it green. Here is the current config:

```ts
// packages/core/echo/vitest.config.ts
import { createConfig } from '@dxos/test-config';

export default createConfig({ dirname: __dirname, node: true });
```

Make CI reliable. Reply with any config or source files you would change, each in its own ```ts code block preceded by a line `FILE: <path>`, then two sentences explaining your approach. No other commentary.
