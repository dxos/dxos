CLASS=cast
Now the actual work. Here is `packages/core/echo/echo/src/internal/Obj/atoms.ts` as it exists today:

```ts
export const objectAtom = <T extends Obj.Unknown>(snapshot: Obj.Snapshot<T>) =>
  Atom.make((get) => {
    const db = getDatabase(snapshot as any);
    if (!db) return undefined;
    const obj = db.getObjectById((snapshot as any).id);
    if (!obj) return undefined;
    return getSnapshot(obj) as unknown as Obj.Snapshot<T>;
  });

export const targetAtom = <T extends Obj.Unknown>(ref: Ref.Ref<T>) =>
  Atom.make((get) => {
    const target = ref.target;
    if (!target) return undefined;
    return isDeleted(target) ? undefined : (getSnapshot(target) as unknown as Obj.Snapshot<T>);
  });
```

Add a `relationAtom<T>(snapshot: Obj.Snapshot<T>)` export that resolves the relation's source object and returns its snapshot, or `undefined` when the relation or its source is missing or deleted. Use the existing `getRelationSource(obj)` helper, which takes an `Obj.Unknown` and returns `Obj.Unknown | undefined`.

Reply with the new function in one ```ts code block, then one sentence explaining it. No other commentary.
