$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  e2e:
    command: pnpm exec playwright test
    args:
      - --config=src/playwright/playwright.config.ts
    # The service worker breaks `page.route` interception, and specs assert on this rather than
    # silently running without it — set here so no caller has to remember it.
    env:
      DX_PWA: 'false'
    deps:
      - test-utils:build
      - vite-plugin-icons:build
      - target: ^:build
        optional: true
      - target: bundle
        optional: true
        env:
          DX_EDGE_BASE_URL: 'https://main.dxos.network/'
          DX_TELEMETRY_TAG: e2e
          # Read at bundle time by `vite.config.ts` (VitePWA `selfDestroying`), so it belongs on the
          # bundle as well as on the test process above.
          DX_PWA: 'false'
    inputs:
      - src/playwright/playwright.config.ts
      - '@group(sources)'
      - '@group(tests)'
      - $CI
      - $CONFIG_DYNAMIC
      - $DX_ENVIRONMENT
      - $DX_HOST
      - $DX_HUB_URL
      - $DX_IPDATA_API_KEY
      - $DX_SHARED_WORKER
      - $PLAYWRIGHT_BROWSER

inheritedBy:
  tags: e2e
