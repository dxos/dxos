$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  compile:
    command: bunx dx-build
    deps:
      - dx-build:compile
      - ^:build
    inputs:
      - /tsconfig.base.json
      - tsconfig.json
      - '@group(production)'
    outputs:
      - dist/
    options:
      timeout: 60

inheritedBy:
  tags: ts-compile
