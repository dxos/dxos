$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  compile:
    command: tsc
    deps:
      - ^:compile
      - ^:build
    inputs:
      - /tsconfig.base.json
      - tsconfig.json
      - '@group(sources)'
    outputs:
      - dist/
