$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  compile:
    command: dx-compile
    args:
      - --outputPath=dist/lib
    deps:
      - dx-compile:compile
      - ^:compile
    inputs:
      - '@group(production)'
    outputs:
      - dist/lib/
    options:
      timeout: 10
