$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  build:
    command: tsc
    deps:
      - ^:build
      - ^:compile
    inputs:
      - /tsconfig.base.json
      - tsconfig.json
      - '@group(sources)'
      - '@group(tests)'
    outputs:
      - dist/
