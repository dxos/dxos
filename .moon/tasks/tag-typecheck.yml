$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  build:
    command: pnpm exec tsc --noEmit
    deps:
      - ^:build
    inputs:
      - /tsconfig.base.json
      - tsconfig.json
      - '@group(sources)'
    options:
      timeout: 60

inheritedBy:
  tags: typecheck
