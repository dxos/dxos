$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  storybook:
    command: storybook dev
    args:
      - --port=9009
      - --no-open
    deps:
      - ^:compile
    inputs:
      - .storybook/*
      - static/*
    preset: server
