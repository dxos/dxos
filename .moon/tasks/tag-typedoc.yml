$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  typedoc:
    command: bun run typedoc
    deps:
      - build
    inputs:
      - /tsconfig.base.json
      - tsconfig.json
      - typedoc.json
      - '@group(production)'
    outputs:
      - typedoc

inheritedBy:
  tags: typedoc
