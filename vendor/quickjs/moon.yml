id: vendor-quickjs
layer: library
language: typescript
tags:
  - pack
workspace:
  inheritedTasks:
    exclude:
      - lint
tasks:
  compile:
    command: echo "No compilation needed for vendor package"
    outputs: []
    options:
      merge: replace
